<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Android API Levels</title>
    <url>/archives/a7110e1c.html</url>
    <content><![CDATA[<span id="more"></span>
<meta name="referrer" content="no-referrer"/>

<html>
<head>
<meta charset="utf-8">
<meta http-equiv="X-UA-Compatible" content="IE=edge">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>API Levels | Android versions, SDK/API levels, version codes, codenames, and cumulative usage</title>
<meta name="description" content="A quick reference table of Android versions with SDK & API levels, version codes, codenames, cumulative usage, and more.">
<meta name="keywords" content="android,api,sdk,levels,version,codes,codename,usage,market,share,distribution">
<meta name="author" content="Eugene Belinski">
<link rel="stylesheet" href="/css/main.css">
<link rel="canonical" href="/">

<link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
<link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
<link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
<link rel="manifest" href="/site.webmanifest">
<link rel="mask-icon" href="/safari-pinned-tab.svg" color="#5bbad5">
<meta name="msapplication-TileColor" content="#da532c">
<meta name="theme-color" content="#ffffff">

<meta name="twitter:card" content="summary" />
<meta name="twitter:site" content="@EugeneBelinski" />
<meta name="twitter:creator" content="@EugeneBelinski" />
<meta property="og:type" content="website" />
<meta property="og:url" content="https://apilevels.com/" />
<meta property="og:title" content="Android API Levels" />
<meta property="og:description" content="A quick reference table of Android versions with SDK & API levels, version codes, codenames, cumulative usage, and more." />
<meta property="og:image" content="https://apilevels.com/logo.png" />
</head>
<body>
<div class="container">
<h1 id="site-title"><a href="/">Android API Levels</a></h1>
</div>
<div class="container"><div id="page-content">
<p>This is an overview of all Android versions and their corresponding identifiers for Android developers. Anyone is welcome to open <a href="https://github.com/ebelinski/apilevels">an issue or pull request</a>. Happy developing!</p>
<div id="compact-toc">
<ul id="markdown-toc">
<li><a href="#definitions" id="markdown-toc-definitions">Definitions</a> <ul>
<li><a href="#gradle-files" id="markdown-toc-gradle-files">Gradle files</a></li>
<li><a href="#code-files" id="markdown-toc-code-files">Code files</a></li>
</ul>
</li>
<li><a href="#footnotes" id="markdown-toc-footnotes">Footnotes</a></li>
<li><a href="#see-also" id="markdown-toc-see-also">See also</a></li>
</ul>
</div>
<div class="table-responsive">
<table class="full-width">
<tr>
<th>Version</th>
<th>SDK / API level</th>
<th><a href="https://developer.android.com/reference/kotlin/android/os/Build.VERSION_CODES">Version code</a></th>
<th>Codename</th>
<th>
Cumulative<br />usage
<sup id="fnref:1"><a href="#fn:1" class="footnote">1</a></sup>
</th>
<th>Year</th>
</tr>
<tr>
<td>
<b><a href="https://developer.android.com/about/versions/13">Android 13</a></b>
<sup class="beta">BETA</sup>
</td>
<td>Level 33</td>
<td><code>T</code></td>
<td>
Tiramisu
<sup id="fnref:2"><a href="#fn:2" class="footnote">2</a></sup>
</td>
<td rowspan="2"><i>No data</i></td>
<td rowspan="2"><i>TBA</i></td>
</tr>
<tr>
<td rowspan="3">
<b><a href="https://developer.android.com/about/versions/12">Android 12</a></b>
</td>
<td>
Level 32
<span class="subversion">Android 12L</span>
</td>
<td><code>S_V2</code></td>
<td rowspan="2">
Snow Cone
<sup id="fnref:2"><a href="#fn:2" class="footnote">2</a></sup>
</td>
</tr>
<tr>
<td>Level 31 <span class="subversion">Android 12</span></td>
<td><code>S</code></td>
<style>
  #cell146 {
    width: 100%
    height: 100%;
    background: linear-gradient(to right, #60ECAD 14.6%, white 14.6%);
  }

<p>  @media (prefers-color-scheme: dark) {<br>    #cell146 {<br>      background: linear-gradient(to right, #006236 14.6%, rgba(0, 0, 0, 0) 14.6%);<br>    }<br>  }<br></style></p>
<td rowspan="2" id="cell146">
14.6%
</td>
<td rowspan="2">2021</td>
</tr>
<tr class="table-notes"><td colspan="3">
<ul>
<li><code>targetSdk</code> <a href="https://developer.android.com/google/play/requirements/target-sdk">will need to be 31+</a> for new apps by August 2022 and updates by November 2022. <sup id="fnref:3"><a href="#fn:3" class="footnote">3</a></sup></li>
<li><code>targetSdk</code> <a href="https://support.google.com/googleplay/android-developer/answer/11926878">will need to be 31+</a> for <i>all existing apps</i> by November 2023. <sup id="fnref:4"><a href="#fn:4" class="footnote">4</a></sup></li>
</ul>
</td></tr>
<tr>
<td rowspan="2">
<b><a href="https://developer.android.com/about/versions/11">Android 11</a></b>
</td>
<td>Level 30</td>
<td><code>R</code></td>
<td>
Red Velvet Cake
<sup id="fnref:2"><a href="#fn:2" class="footnote">2</a></sup>
</td>
<style>
  #cell477 {
    width: 100%
    height: 100%;
    background: linear-gradient(to right, #60ECAD 47.7%, white 47.7%);
  }

<p>  @media (prefers-color-scheme: dark) {<br>    #cell477 {<br>      background: linear-gradient(to right, #006236 47.7%, rgba(0, 0, 0, 0) 47.7%);<br>    }<br>  }<br></style></p>
<td rowspan="2" id="cell477">
47.7%
</td>
<td rowspan="2">2020</td>
</tr>
<tr class="table-notes"><td colspan="3">
<ul>
<li><code>targetSdk</code> <a href="https://developer.android.com/google/play/requirements/target-sdk">must be 30+</a> for new apps and app updates. <sup id="fnref:3"><a href="#fn:3" class="footnote">3</a></sup></li>
<li><code>targetSdk</code> <a href="https://support.google.com/googleplay/android-developer/answer/11926878">will need to be 30+</a> for <i>all existing apps</i> by November 2022. <sup id="fnref:4"><a href="#fn:4" class="footnote">4</a></sup></li>
</ul>
</td></tr>
<tr>
<td>
<b><a href="https://developer.android.com/about/versions/10">Android 10</a></b>
</td>
<td>Level 29</td>
<td><code>Q</code></td>
<td>
Quince Tart
<sup id="fnref:2"><a href="#fn:2" class="footnote">2</a></sup>
</td>
<style>
  #cell703 {
    width: 100%
    height: 100%;
    background: linear-gradient(to right, #60ECAD 70.3%, white 70.3%);
  }

<p>  @media (prefers-color-scheme: dark) {<br>    #cell703 {<br>      background: linear-gradient(to right, #006236 70.3%, rgba(0, 0, 0, 0) 70.3%);<br>    }<br>  }<br></style></p>
<td rowspan="1" id="cell703">
70.3%
</td>
<td>2019</td>
</tr>
<tr>
<td rowspan="2">
<b><a href="https://developer.android.com/about/versions/pie">Android 9</a></b>
</td>
<td>Level 28</td>
<td><code>P</code></td>
<td>Pie</td>
<style>
  #cell816 {
    width: 100%
    height: 100%;
    background: linear-gradient(to right, #60ECAD 81.6%, white 81.6%);
  }

<p>  @media (prefers-color-scheme: dark) {<br>    #cell816 {<br>      background: linear-gradient(to right, #006236 81.6%, rgba(0, 0, 0, 0) 81.6%);<br>    }<br>  }<br></style></p>
<td rowspan="2" id="cell816">
81.6%
</td>
<td rowspan="2">2018</td>
</tr>
<tr class="table-notes"><td colspan="3">
<ul>
<li><code>targetSdk</code> <a href="https://developer.android.com/google/play/requirements/target-sdk">must be 28+</a> for new Wear OS apps and Wear OS app updates.</li>
</ul>
</td></tr>
<tr>
<td rowspan="2">
<b><a href="https://developer.android.com/about/versions/oreo">Android 8</a></b>
</td>
<td>Level 27 <span class="subversion">Android 8.1</span></td>
<td><code>O_MR1</code></td>
<td rowspan="2">Oreo</td>
<style>
  #cell875 {
    width: 100%
    height: 100%;
    background: linear-gradient(to right, #60ECAD 87.5%, white 87.5%);
  }

<p>  @media (prefers-color-scheme: dark) {<br>    #cell875 {<br>      background: linear-gradient(to right, #006236 87.5%, rgba(0, 0, 0, 0) 87.5%);<br>    }<br>  }<br></style></p>
<td rowspan="1" id="cell875">
87.5%
</td>
<td rowspan="2">2017</td>
</tr>
<tr>
<td>Level 26 <span class="subversion">Android 8.0</span></td>
<td><code>O</code></td>
<style>
  #cell902 {
    width: 100%
    height: 100%;
    background: linear-gradient(to right, #60ECAD 90.2%, white 90.2%);
  }

<p>  @media (prefers-color-scheme: dark) {<br>    #cell902 {<br>      background: linear-gradient(to right, #006236 90.2%, rgba(0, 0, 0, 0) 90.2%);<br>    }<br>  }<br></style></p>
<td rowspan="1" id="cell902">
90.2%
</td>
</tr>
<tr>
<td rowspan="2">
<b><a href="https://developer.android.com/about/versions/nougat">Android 7</a></b>
</td>
<td>Level 25 <span class="subversion">Android 7.1</span></td>
<td><code>N_MR1</code></td>
<td rowspan="2">Nougat</td>
<style>
  #cell918 {
    width: 100%
    height: 100%;
    background: linear-gradient(to right, #60ECAD 91.8%, white 91.8%);
  }

<p>  @media (prefers-color-scheme: dark) {<br>    #cell918 {<br>      background: linear-gradient(to right, #006236 91.8%, rgba(0, 0, 0, 0) 91.8%);<br>    }<br>  }<br></style></p>
<td rowspan="1" id="cell918">
91.8%
</td>
<td rowspan="2">2016</td>
</tr>
<tr>
<td>Level 24 <span class="subversion">Android 7.0</span></td>
<td><code>N</code></td>
<style>
  #cell946 {
    width: 100%
    height: 100%;
    background: linear-gradient(to right, #60ECAD 94.6%, white 94.6%);
  }

<p>  @media (prefers-color-scheme: dark) {<br>    #cell946 {<br>      background: linear-gradient(to right, #006236 94.6%, rgba(0, 0, 0, 0) 94.6%);<br>    }<br>  }<br></style></p>
<td rowspan="1" id="cell946">
94.6%
</td>
</tr>
<tr>
<td>
<b><a href="https://developer.android.com/about/versions/marshmallow">Android 6</a></b>
</td>
<td>Level 23</td>
<td><code>M</code></td>
<td>Marshmallow</td>
<style>
  #cell971 {
    width: 100%
    height: 100%;
    background: linear-gradient(to right, #60ECAD 97.1%, white 97.1%);
  }

<p>  @media (prefers-color-scheme: dark) {<br>    #cell971 {<br>      background: linear-gradient(to right, #006236 97.1%, rgba(0, 0, 0, 0) 97.1%);<br>    }<br>  }<br></style></p>
<td rowspan="1" id="cell971">
97.1%
</td>
<td>2015</td>
</tr>
<tr>
<td rowspan="3">
<b><a href="https://developer.android.com/about/versions/lollipop">Android 5</a></b>
</td>
<td>Level 22 <span class="subversion">Android 5.1</span></td>
<td><code>LOLLIPOP_MR1</code></td>
<td rowspan="2">Lollipop</td>
<style>
  #cell987 {
    width: 100%
    height: 100%;
    background: linear-gradient(to right, #60ECAD 98.7%, white 98.7%);
  }

<p>  @media (prefers-color-scheme: dark) {<br>    #cell987 {<br>      background: linear-gradient(to right, #006236 98.7%, rgba(0, 0, 0, 0) 98.7%);<br>    }<br>  }<br></style></p>
<td rowspan="1" id="cell987">
98.7%
</td>
<td>2015</td>
</tr>
<tr>
<td>Level 21 <span class="subversion">Android 5.0</span></td>
<td><code>LOLLIPOP</code>, <code>L</code></td>
<td rowspan="24"><i>No data</i></td>
<td rowspan="3">2014</td>
</tr>
<tr class="table-notes"><td colspan="3">
<ul>
<li><a href="https://developer.android.com/jetpack/compose">Jetpack Compose</a> requires a <code>minSdk</code> of 21 or higher.</li>
</ul>
</td></tr>
<tr>
<td rowspan="9"><b>Android 4</b></td>
<td>
Level 20
<span class="subversion">Android 4.4W</span> <sup id="fnref:5"><a href="#fn:5" class="footnote">5</a></sup>
</td>
<td><code>KITKAT_WATCH</code></td>
<td rowspan="2">KitKat</td>
</tr>
<tr>
<td>
Level 19
<span class="subversion">Android 4.4</span>
</td>
<td><code>KITKAT</code></td>
<td rowspan="3">2013</td>
</tr>
<tr class="table-notes"><td colspan="3">
<ul>
<li>Google Play services <a href="https://android-developers.googleblog.com/2021/07/google-play-services-discontinuing-jelly-bean.html">do not support Android versions</a> below API level 19.</li>
</ul>
</td></tr>
<tr>
<td>Level 18 <span class="subversion">Android 4.3</span></td>
<td><code>JELLYBEAN_MR2</code></td>
<td rowspan="3">Jelly Bean</td>
</tr>
<tr>
<td>Level 17 <span class="subversion">Android 4.2</span></td>
<td><code>JELLYBEAN_MR1</code></td>
 <td rowspan="2">2012</td>
</tr>
<tr>
<td>Level 16 <span class="subversion">Android 4.1</span></td>
<td><code>JELLYBEAN</code></td>
</tr>
<tr>
<td>Level 15 <span class="subversion">Android 4.0.3 – 4.0.4</span></td>
<td><code>ICE_CREAM_SANDWICH_MR1</code></td>
<td rowspan="2">Ice Cream Sandwich</td>
<td rowspan="7">2011</td>
</tr>
<tr>
<td>Level 14 <span class="subversion">Android 4.0.1 – 4.0.2</span></td>
<td><code>ICE_CREAM_SANDWICH</code></td>
</tr>
<tr class="table-notes"><td colspan="3">
<ul>
<li><a href="https://developer.android.com/jetpack">Jetpack</a>/<a href="https://developer.android.com/jetpack/androidx">AndroidX</a> libraries <a href="https://developer.android.com/topic/libraries/support-library#api-versions">require</a> a <code>minSdk</code> of 14 or higher.</li>
</ul>
</td></tr>
<tr>
<td rowspan="3"><b>Android 3</b></td>
<td>Level 13 <span class="subversion">Android 3.2</span></td>
<td><code>HONEYCOMB_MR2</code></td>
<td rowspan="3">Honeycomb</td>
</tr>
<tr>
<td>Level 12 <span class="subversion">Android 3.1</span></td>
<td><code>HONEYCOMB_MR1</code></td>
</tr>
<tr>
<td>Level 11 <span class="subversion">Android 3.0</span></td>
<td><code>HONEYCOMB</code></td>
</tr>
<tr>
<td rowspan="6"><b>Android 2</b></td>
<td>Level 10 <span class="subversion">Android 2.3.3 – 2.3.7</span></td>
<td><code>GINGERBREAD_MR1</code></td>
<td rowspan="2">Gingerbread</td>
</tr>
<tr>
<td>Level 9 <span class="subversion">Android 2.3.0 – 2.3.2</span></td>
<td><code>GINGERBREAD</code></td>
<td rowspan="3">2010</td>
</tr>
<tr>
<td>Level 8 <span class="subversion">Android 2.2</span></td>
<td><code>FROYO</code></td>
<td>Froyo</td>
</tr>
<tr>
<td>Level 7 <span class="subversion">Android 2.1</span></td>
<td><code>ECLAIR_MR1</code></td>
<td rowspan="3">Eclair</td>
</tr>
<tr>
<td>Level 6 <span class="subversion">Android 2.0.1</span></td>
<td><code>ECLAIR_0_1</code></td>
<td rowspan="5">2009</td>
</tr>
<tr>
<td>Level 5 <span class="subversion">Android 2.0</span></td>
<td><code>ECLAIR</code></td>
</tr>
<tr>
<td rowspan="4"><b>Android 1</b></td>
<td>Level 4 <span class="subversion">Android 1.6</span></td>
<td><code>DONUT</code></td>
<td>Donut</td>
</tr>
<tr>
<td>Level 3 <span class="subversion">Android 1.5</span></td>
<td><code>CUPCAKE</code></td>
<td>Cupcake</td>
</tr>
<tr>
<td>Level 2 <span class="subversion">Android 1.1</span></td>
<td><code>BASE_1_1</code></td>
<td>Petit Four</td>
</tr>
<tr>
<td>Level 1 <span class="subversion">Android 1.0</span></td>
<td><code>BASE</code></td>
<td><i>None</i></td>
<td>2008</td>
</tr>
</table>
</div>
<h2 id="definitions">Definitions</h2>
<h4 id="gradle-files">Gradle files</h4>
<div class="table-responsive">
<table class="full-width">
<tr>
<th class="nowrap">Kotlin variable</th>
<th class="nowrap">Groovy variable</th>
<th>Definition</th>
</tr>
<tr>
<td class="nowrap"><code>minSdk</code></td>
<td class="nowrap"><code>minSdkVersion</code></td>
<td>The minimum SDK version your app will support, defined in <code>build.gradle</code>. For example, if your <code>minSdk</code> is 26, this SDK version corresponse to API Level 26 and Android 8, so your app will only run on devices with Android 8 or higher.</td>
</tr>
<tr>
<td class="nowrap"><code>targetSdk</code></td>
<td class="nowrap"><code>targetSdkVersion</code></td>
<td>The SDK version that your app targets, defined in <code>build.gradle</code>. This should always be the same as <code>compileSdk</code>.</td>
</tr>
<tr>
<td class="nowrap"><code>compileSdk</code></td>
<td class="nowrap"><code>compileSdkVersion</code></td>
<td>The SDK version that your app compiles against, defined in <code>build.gradle</code>. Android Studio uses this SDK version to build your AABs and APKs. This should always be the same as <code>targetSdk</code>.</td>
</tr>
</table>
</div>
<h4 id="code-files">Code files</h4>
<div class="table-responsive">
<table class="full-width">
<tr>
<th>Variable</th>
<th>Definition</th>
</tr>
<tr>
<td class="nowrap"><code>Build.VERSION.SDK_INT</code></td>
<td>The SDK version of the Android OS currently running on the user's device. For example, on a device running Android 11, this value will be <code>30</code> (aka <code>Build.VERSION_CODES.R</code>), even if the target and compile SDK of the app is different.</td>
</tr>
</table>
</div>
<h2 id="footnotes">Footnotes</h2>
<div class="footnotes">
<ol>
<li id="fn:1">
<p>Cumulative usage distribution figures were last updated on <b>May 31, 2022</b> using data from <a href="https://gs.statcounter.com/android-version-market-share/mobile-tablet/worldwide">Statcounter GlobalStats</a>. These figures may have changed significantly since the last update. You may update the figures yourself with a <a href="https://github.com/ebelinski/apilevels">pull request</a>. <a href="#fnref:1" class="reversefootnote">↩</a></p>
</li>
<li id="fn:2">
<p>The codenames for Android 10 and above in the table are the internal codenames. Beginning with Android 10, Google dropped the usage of codenames publicly.</p>
</li>
<li id="fn:3">
<p>This requirement excludes new Wear OS apps and updates, which still only require a <code>targetSdk</code> <a href="https://developer.android.com/google/play/requirements/target-sdk">of 28 or higher</a>. <a href="#fnref:3" class="reversefootnote">↩</a></p>
</li>
<li id="fn:4">
<p>In 2022, Google began imposing <a href="https://support.google.com/googleplay/android-developer/answer/11926878">minimum <code>targetSdk</code> requirements for existing apps</a>, in addition new apps and app updates. Existing apps that are not updated to meet <code>targetSdk</code> requirements by their deadlines will be <a href="https://android-developers.googleblog.com/2022/04/expanding-plays-target-level-api-requirements-to-strengthen-user-security.html">subject to restrictions</a>. This requirement excludes Wear OS apps. <a href="#fnref:4" class="reversefootnote">↩</a></p>
</li>
<li id="fn:5">
<p>Android 4.4W is the first Android release for Android Wear. <a href="#fnref:5" class="reversefootnote">↩</a></p>
</li>
</ol>
</div>
<h2 id="see-also">See also</h2>
<ul>
<li><a href="https://developer.android.com/reference/android/os/Build.VERSION_CODES">Build.VERSION_CODES</a> official reference</li>
<li><a href="https://source.android.com/setup/start/build-numbers">Codenames, Tags, and Build Numbers</a></li>
</ul>
</div></div>
<div class="container">
<div id="site-footer">
<p>
<a href="https://github.com/ebelinski/apilevels">Source</a> |
Maintained by <a href="https://twitter.com/EugeneBelinski">Eugene 👨🏻‍💻</a> and <a href="https://github.com/ebelinski/apilevels/graphs/contributors">others</a>
</p>
</div>
</div>
<script async defer src="https://si.apilevels.com/latest.js"></script>
<noscript><img src="https://si.apilevels.com/noscript.gif" alt="" referrerpolicy="no-referrer-when-downgrade" /></noscript>
</body>
</html>
]]></content>
      <categories>
        <category>Android</category>
      </categories>
      <tags>
        <tag>Android</tag>
        <tag>SDK</tag>
      </tags>
  </entry>
  <entry>
    <title>Binder概述</title>
    <url>/archives/d6aea0f1.html</url>
    <content><![CDATA[<span id="more"></span>
<meta name="referrer" content="no-referrer"/>


<h1 id="Binder机制文章列表"><a href="#Binder机制文章列表" class="headerlink" title="Binder机制文章列表"></a>Binder机制文章列表</h1><p><a href="https://i-rtfsc.github.io/archives/d6aea0f1.html">Binder概述</a><br><a href="https://i-rtfsc.github.io/archives/3e87e0ce.html">Binder机制01-驱动</a><br><a href="https://i-rtfsc.github.io/archives/f17b1d52.html">Binder机制02-ServiceManager</a><br><a href="https://i-rtfsc.github.io/archives/937d16d8.html">Binder机制03-Framework-Native</a><br><a href="https://i-rtfsc.github.io/archives/73029508.html">Binder机制04-Framework-Jave</a><br><a href="https://i-rtfsc.github.io/archives/ed3eb50b.html">Binder机制05-AIDL</a><br><a href="https://i-rtfsc.github.io/archives/4e7f84b4.html">Binder机制06-框架</a><br><a href="https://i-rtfsc.github.io/archives/d30c7ecb.html">Binder机制07-实例分析</a><br><a href="https://i-rtfsc.github.io/archives/720ded9.html">Binder机制08-总结</a><br><a href="https://i-rtfsc.github.io/archives/f1684132.html">Ashmem简介（Android IPC传输大数据）</a></p>
<h1 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h1><p>在Android的中，我们知道每个应用都是一个独立的进程，有一个独立的虚拟机，应用和应用之间的内存是不能共享数据的，但是我们用到Activity、Service、Context、provider等功能恰恰做了很多数据传输。这就涉及到了我们了解的进程间通信机制-IPC。<br>在Android中常常使用的进程间通信有共享内存、管道、信号处理、socket、Binder等，大部分场景下使用的IPC都是Binder。Binder相较于传统IPC来说更适合于Android系统，具体原因的包括如下三点：</p>
<ul>
<li>Binder本身是C/S架构的，这一点更符合Android系统的架构</li>
<li>性能上更有优势：管道，消息队列，Socket的通讯都需要两次数据拷贝，而Binder只需要一次。</li>
<li>安全性更好：传统IPC形式，无法得到对方的身份标识（UID/GID)，而在使用Binder IPC时，这些身份标示是跟随调用过程而自动传递的。Server端很容易就可以知道Client端的身份，非常便于做安全检查。</li>
</ul>
<p>​</p>
<h1 id="架构"><a href="#架构" class="headerlink" title="架构"></a>架构</h1><p><img src="https://cdn.nlark.com/yuque/0/2020/png/1759879/1601210476189-c4fc849a-1c9e-44f6-bdb9-248960ed5209.png" alt="avatar"><br>从上图可以看出，Binder的实现分为：</p>
<ul>
<li>驱动层</li>
<li>framework层<ul>
<li>C++</li>
<li>JNI</li>
<li>Java</li>
</ul>
</li>
</ul>
<p>驱动层位于Linux内核中，它提供了最底层的数据传递，对象标识，线程管理，调用过程控制等功能。<strong>驱动层是整个Binder机制的核心</strong>。<br>Framework层以驱动层为基础，提供了应用开发的基础设施。Framework层既包含了C++部分的实现，也包含了Java部分的实现。为了能将C++的实现复用到Java端，中间通过JNI进行衔接。<br>开发者可以在Framework之上利用Binder提供的机制来进行具体的业务逻辑开发。其实不仅仅是第三方开发者，Android系统中本身也包含很多系统服务都是基于Binder框架开发的。其中Binder框架是典型的C/S架构。所以在后面中， 我们把服务的请求方称为Client，服务的实现方称之Server。Clinet对于Server的请求会经由Binder驱动框架由上至下传递到内核的Binder驱动中，请求中包含了Client将要调用的命令和参数。请求到了Binder驱动以后，在确定了服务的提供方之后，再讲从下至上将请求传递给具体的服务。如下图所示：<br><img src="https://cdn.nlark.com/yuque/0/2021/png/1759879/1633656658251-2fb84f98-67fb-4dc5-a1a4-2aa941a4c6c9.png" alt="avatar"></p>
<h1 id="通信原理"><a href="#通信原理" class="headerlink" title="通信原理"></a>通信原理</h1><p>Binder通信采用C/S架构，从组件视角来说，包含Client、Server、ServiceManager以及binder驱动，其中ServiceManager用于管理系统中的各种服务。架构图如下所示：<br><img src="https://cdn.nlark.com/yuque/0/2020/png/1759879/1601211334048-fa417b87-7211-4c46-b574-f7ed5cb7e4c4.png" alt="avatar"><br>此处的Service Manager是指Native层的ServiceManager（C++），并非指framework层的ServiceManager(Java)。ServiceManager是整个Binder通信机制的大管家，是Android进程间通信机制Binder的守护进程。ServiceManager对于Binder Server的管理就好比车管所对于车牌号码的的管理，派出所对于身份证号码的管理：每个公开对外提供服务的Server都需要注册到ServiceManager中（通过addService），注册的时候需要指定一个唯一的id（这个id其实就是一个字符串）。Client要对Server发出请求，就必须知道服务端的id。Client需要先根据Server的id通过ServerManager拿到Server的标示（通过getService），然后通过这个标示与Server进行通信。</p>
<h1 id="源码"><a href="#源码" class="headerlink" title="源码"></a>源码</h1><ul>
<li><p>驱动</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">https://android.googlesource.com/kernel/common/+/refs/tags/5.4-android11-0/drivers/android/</span><br><span class="line"></span><br><span class="line">$ tree kernel/drivers/android/</span><br><span class="line">├── Kconfig</span><br><span class="line">├── Makefile</span><br><span class="line">├── binder.c</span><br><span class="line">├── binder_alloc.c</span><br><span class="line">├── binder_alloc.h</span><br><span class="line">├── binder_alloc_selftest.c</span><br><span class="line">├── binder_internal.h</span><br><span class="line">├── binder_trace.h</span><br><span class="line">├── binderfs.c</span><br><span class="line">└── vendor_hooks.c</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">https://android.googlesource.com/kernel/common/+/refs/tags/5.4-android11-0/include/uapi/linux/android/</span><br><span class="line"></span><br><span class="line">$ tree kernel/include/uapi/linux/android/</span><br><span class="line">├── binder.h</span><br><span class="line">└── binderfs.h</span><br></pre></td></tr></table></figure></li>
<li><p>native</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">http://aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/</span><br><span class="line"></span><br><span class="line">$ tree frameworks/native/libs/binder/</span><br><span class="line">├── Binder.cpp</span><br><span class="line">├── BpBinder.cpp</span><br><span class="line">├── IInterface.cpp</span><br><span class="line">├── IPCThreadState.cpp</span><br><span class="line">├── IServiceManager.cpp</span><br><span class="line">├── Parcel.cpp</span><br><span class="line">├── ProcessState.cpp</span><br><span class="line">├── include</span><br><span class="line">    ├── binder</span><br><span class="line">        ├── Binder.h</span><br><span class="line">        ├── BinderService.h</span><br><span class="line">        ├── BpBinder.h</span><br><span class="line">        ├── IBinder.h</span><br><span class="line">        ├── IInterface.h</span><br><span class="line">        ├── IPCThreadState.h</span><br><span class="line">        ├── IServiceManager.h</span><br><span class="line">        ├── ProcessState.h</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">http://aospxref.com/android-11.0.0_r21/xref/frameworks/native/cmds/servicemanager/</span><br><span class="line"></span><br><span class="line">$ tree frameworks/native/cmds/servicemanager/</span><br><span class="line">├── Access.cpp</span><br><span class="line">├── Access.h</span><br><span class="line">├── Android.bp</span><br><span class="line">├── ServiceManager.cpp</span><br><span class="line">├── ServiceManager.h</span><br><span class="line">├── TEST_MAPPING</span><br><span class="line">├── main.cpp</span><br><span class="line">├── servicemanager.rc</span><br><span class="line">├── test_sm.cpp</span><br><span class="line">└── vndservicemanager.rc</span><br></pre></td></tr></table></figure></li>
<li><p>Java&amp;jni</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">http://aospxref.com/android-11.0.0_r21/xref/frameworks/base/core/jni/</span><br><span class="line"></span><br><span class="line">$ tree frameworks/base/core/jni/</span><br><span class="line">├── AndroidRuntime.cpp</span><br><span class="line">├── android_os_HidlMemory.cpp</span><br><span class="line">├── android_os_HidlMemory.h</span><br><span class="line">├── android_os_HidlSupport.cpp</span><br><span class="line">├── android_os_HwBinder.cpp</span><br><span class="line">├── android_os_HwBinder.h</span><br><span class="line">├── android_os_HwParcel.cpp</span><br><span class="line">├── android_os_HwParcel.h</span><br><span class="line">├── android_os_HwRemoteBinder.cpp</span><br><span class="line">├── android_os_HwRemoteBinder.h</span><br><span class="line">├── android_os_Parcel.cpp</span><br><span class="line">├── android_os_Parcel.h</span><br><span class="line">├── android_util_Binder.cpp</span><br><span class="line">├── android_util_Binder.h</span><br><span class="line">├── android_util_Process.cpp</span><br><span class="line">├── hwbinder</span><br><span class="line">│   ├── EphemeralStorage.cpp</span><br><span class="line">│   └── EphemeralStorage.h</span><br><span class="line">├── include</span><br><span class="line">    └── android_runtime</span><br><span class="line">        ├── AndroidRuntime.h</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">http://aospxref.com/android-11.0.0_r21/xref/frameworks/base/core/java/android/os/</span><br><span class="line"></span><br><span class="line">$ tree frameworks/base/core/java/android/os/</span><br><span class="line">├── HidlMemory.java</span><br><span class="line">├── HidlMemoryUtil.java</span><br><span class="line">├── HidlSupport.java</span><br><span class="line">├── HwBinder.java</span><br><span class="line">├── HwBlob.java</span><br><span class="line">├── HwParcel.java</span><br><span class="line">├── HwRemoteBinder.java</span><br><span class="line">├── IBinder.java</span><br><span class="line">├── IHwBinder.java</span><br><span class="line">├── IHwInterface.java</span><br><span class="line">├── IInterface.java</span><br><span class="line">├── Parcel.java</span><br><span class="line">├── Parcelable.java</span><br><span class="line">├── ServiceManager.java</span><br><span class="line">├── ServiceManagerNative.java</span><br><span class="line"></span><br></pre></td></tr></table></figure></li>
</ul>
]]></content>
      <categories>
        <category>Android</category>
      </categories>
      <tags>
        <tag>Android</tag>
        <tag>Binder</tag>
      </tags>
  </entry>
  <entry>
    <title>Android 12系统启动-简介</title>
    <url>/archives/68a972b0.html</url>
    <content><![CDATA[<span id="more"></span>
<meta name="referrer" content="no-referrer"/>

<h1 id="Android-12系统启动文章列表"><a href="#Android-12系统启动文章列表" class="headerlink" title="Android 12系统启动文章列表"></a>Android 12系统启动文章列表</h1><p><a href="https://i-rtfsc.github.io/archives/68a972b0.html">Android 12系统启动-简介</a><br><a href="https://i-rtfsc.github.io/archives/6049d7b6.html">Android 12系统启动-Zygote</a><br><a href="https://i-rtfsc.github.io/archives/ca7c5e61.html">Android 12系统启动-init</a><br><a href="https://i-rtfsc.github.io/archives/1acbe70b.html">Android 12系统启动-SystemServer</a><br><a href="https://i-rtfsc.github.io/archives/49ff48d4.html">Android 12系统启动-AMS</a><br><a href="https://i-rtfsc.github.io/archives/d6edba4e.html">Android 12系统启动-Launcher</a></p>
<p>整理中…很快就补上…</p>
]]></content>
      <categories>
        <category>Android</category>
      </categories>
      <tags>
        <tag>Android</tag>
        <tag>系统启动</tag>
      </tags>
  </entry>
  <entry>
    <title>Android SELinux介绍</title>
    <url>/archives/null.html</url>
    <content><![CDATA[<span id="more"></span>
<meta name="referrer" content="no-referrer"/>

<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>自Android L开始，Android全面启用了SELinux。本文旨在介绍SELinux背后的一些细节，Android的实现，此外还有创建，拓展，删除规则的不同选项。<br>本文会从语法、Android SE流程等详细介绍SELinux相关知识。如果读者不感兴趣，只想快速的解决某个进程缺少SELinux导致的错误，可以直接跳到“<strong>万能公式</strong>”这一节。</p>
<h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>SElinux是linux操作系统的一个MAC（mandatory access control）子系统。MAC不同于树枝的DAC（ discretionary access control）系统，它通过授权中心来决定是否有资格访问目标资源。<br>SElinux已经在LSM（Linux Security Module）框架中实现，它会识别各种内核组件，以及对这些组件的敏感操作。当执行一个操作时，会通过调用一个hook函数来确定当前操作是否被合法。hook函数由SELinux实现，SELinux基于自己的规则数据来确定当前操作的合法性。具体流程如下图所示：<br><img src="https://cdn.nlark.com/yuque/0/2021/png/1759879/1616153019477-2708a27e-3018-4162-bd7f-39c5269526a4.png#crop=0&crop=0&crop=1&crop=1&height=591&id=zB1Cd&margin=%5Bobject%20Object%5D&name=image.png&originHeight=591&originWidth=582&originalType=binary&ratio=1&rotation=0&showTitle=false&size=34048&status=done&style=none&title=&width=582" alt="image.png"></p>
<h2 id="工作模式"><a href="#工作模式" class="headerlink" title="工作模式"></a>工作模式</h2><ul>
<li>permissive：所有访问资源的操作都被允许（即没有MAC），但是如果有违反权限的话，会记录日志。</li>
<li>enforcing：所有访问资源的操作都会对其进行权限检查。</li>
</ul>
<h2 id="工作原理"><a href="#工作原理" class="headerlink" title="工作原理"></a>工作原理</h2><p>SELinux 的工作理念是用标签<strong>（labels）</strong>来匹配各种操作和各种规则。SELinuxz中，Socket，文件以及进程都会被打上标签，然后基于用户对各种标签定义的访问规则来做出判断。<br>SELinux中，标签的格式为：<strong>user:role:type:mls_level</strong>。其中，type段最为常用。<br>SELinux中，规则<strong>（rules）</strong>的格式为：**allow domains types:classes permissions;**。其中：</p>
<ul>
<li><strong>Domain</strong>：一个或一组进程的标签，理解为进程标签中的 type 部分。</li>
<li><strong>Type</strong>：一个或一组对象的标签，理解为非进程对象标签中的 type 部分。</li>
<li><strong>Class</strong>：表示对象的类型，理解：多个对象可以打上相同的标签，意味着文件，文件夹，sokcet等不同类型的文件都可能打上相同标签，所以需要class来区分不同类型。</li>
<li><strong>Permission</strong>：表示进程对对象执行的动作，比如 read，write等。</li>
</ul>
<p>示例：<strong>allow appdomain app_data_file:file rw_file_perms;</strong></p>
<h1 id="Android实现"><a href="#Android实现" class="headerlink" title="Android实现"></a>Android实现</h1><p>Android的安全模型是建立在应用沙盒的概念上的，Android4.3之前，Android通过给不同应用分配不同uid来建立沙盒环境，4.3之后，Android引入了SELinux机制，为沙盒增加了新的边界。SEAndroid采用默认拒绝的策略，即默认情况下，系统进程无法访问任何对象，所有的访问需要被明确定义，以此增强Android的安全性。这意味着一个策略文件会包含大量的信息，包括规则（rules），类型（types），类别（classes），权限（permissions）等。</p>
<h2 id="差异"><a href="#差异" class="headerlink" title="差异"></a>差异</h2><p>与标准SELinux的差异主要是：Android针对其平台特性，增加了BInder IPC机制的相关SE策略，以此来控制Binder通信策略。相关patch如下：</p>
<ul>
<li><a href="https://android.googlesource.com/kernel/common/+/c76b9f83">https://android.googlesource.com/kernel/common/+/c76b9f83</a></li>
</ul>
<p>Android简化SE标签的使用，实际上，Android只用了其中两个部分：<em>role</em> 和 _type_，另外两个部分依然存在，不过被写死成 <code>u:role:type:s0</code>。<code>role</code>部分有两个取值分别为：</p>
<ul>
<li><p><em>r</em> ：domains (processes)</p>
</li>
<li><p><em>object_r</em> ：对象 objects.</p>
<h2 id="SE相关文件"><a href="#SE相关文件" class="headerlink" title="SE相关文件"></a>SE相关文件</h2></li>
<li><p>***.te**：SE规则定义文件。该文件中定义域（domain）及其标签（label）。新增的<code>te</code>文件会与系统所有的 <code>te</code> 文件整合成一个单独的 SE 内核规则文件。</p>
</li>
<li><p><a href="http://boardconfig.mk/"><strong>BoardConfig.mk</strong></a>：引入额外存放SE规则定义目录的mk文件。SE规则文件可以存放到任意目录下，但需要在 <code>[BoardConfig.mk](http://boardconfig.mk/)</code> 中引入对应目录方可生效。</p>
</li>
<li><p><strong>file_contexts</strong>：系统中各文件贴标签处。用于为文件分配标签，被各种用户空间的组件使用。当创建新的 <code>te</code> 文件来定义新类型时，需要修改（或新建）<code>file_contexts</code> 文件使新建的类型得以生效。修改 <code>file_contexts</code> 后需要重编ROM使之生效。</p>
</li>
<li><p><strong>genfs_contexts</strong>： 系统中各种特殊文件（例如，<code>proc</code> 或 <code>vfat</code>）贴标签处。该配置会作为内核SE规则的一部分进行加载。</p>
</li>
<li><p><strong>property_contexts</strong>： Android 系统属性贴标签处。以此控制进程对属性的访问。在启动期间，<code>init</code> 进程会读取此配置。</p>
</li>
<li><p><strong>service_contexts</strong>： Android Binder 服务贴标签处。以此控制进程添加（register）和查找（lookup）Binder 引用。在启动期间，<code>servicemanager</code> 进程会读取此配置。</p>
</li>
<li><p><strong>seapp_contexts</strong>： 应用进程和 <code>/data/data</code> 目录贴标签处。在每次应用启动时，<code>zygote</code> 进程据此确定应用的SE标签；<code>installd</code> 据此确定 <code>/data/data</code> 的SE标签。</p>
</li>
<li><p><strong>mac_permissions.xml</strong>：根据应用签名和应用软件包名称（optionally）为应用分配 <code>seinfo</code> 标记。<code>seapp_contexts</code> 文件会根据 <code>seinfo</code> 标记为应用分配特定标签。在启动期间，<code>system_server</code> 会读取此配置。</p>
<h1 id="语法"><a href="#语法" class="headerlink" title="语法"></a>语法</h1><p>SE相关的语法大致可以分为如下几类：</p>
</li>
<li><p><a href="https://selinuxproject.org/page/Policy_Configuration_Statements">Policy Configuration Statements</a></p>
</li>
<li><p><a href="https://selinuxproject.org/page/DefaultRules">Default Rules</a></p>
</li>
<li><p><a href="https://selinuxproject.org/page/UserStatements">User Statements</a></p>
</li>
<li><p><a href="https://selinuxproject.org/page/RoleStatements">Role Statements</a></p>
</li>
<li><p><a href="https://selinuxproject.org/page/TypeStatements">Type Statements</a></p>
</li>
<li><p><a href="https://selinuxproject.org/page/Bounds_Rules">Bounds Rules</a></p>
</li>
<li><p><a href="https://selinuxproject.org/page/AVCRules">Access Vector Rules</a></p>
</li>
<li><p><a href="https://selinuxproject.org/page/XpermRules">Extended Permission Access Vector Rules</a></p>
</li>
<li><p><a href="https://selinuxproject.org/page/ObjectClassStatements">Object Class and Permission Statements</a></p>
</li>
<li><p><a href="https://selinuxproject.org/page/ConditionalStatements">Conditional Policy Statements</a></p>
</li>
<li><p><a href="https://selinuxproject.org/page/ConstraintStatements">Constraint Statements</a></p>
</li>
<li><p><a href="https://selinuxproject.org/page/MLSStatements">MLS Statements</a></p>
</li>
<li><p><a href="https://selinuxproject.org/page/SIDStatements">Security ID (SID) Statement</a></p>
</li>
<li><p><a href="https://selinuxproject.org/page/FileStatements">File System Labeling Statements</a></p>
</li>
<li><p><a href="https://selinuxproject.org/page/NetworkStatements">Network Labeling Statements</a></p>
</li>
<li><p><a href="https://selinuxproject.org/page/PolicyStatements">Modular Policy Support Statements</a></p>
</li>
<li><p><a href="https://selinuxproject.org/page/XENStatements">XEN Statements</a></p>
</li>
</ul>
<p>本文只列出几个常用的语法，其他语法自查上述连接。</p>
<h2 id="Type-Statements"><a href="#Type-Statements" class="headerlink" title="Type Statements"></a>Type Statements</h2><h3 id="type"><a href="#type" class="headerlink" title="type"></a>type</h3><p>在使用类型前，必须使用type语句明确地声明一个类型标识符，SELinux没有预定义类型，我们必须自行声明，声明了类型后就可以在安全上下文、TE规则和其它策略语句中使用它们了。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"># type 类型名称 [alias 别名集] [,属性集];</span><br><span class="line"># 别名集：即异名同意。如果指定的不止一个别名标识符，要在一对大括号中用空格将各个别名区别开来，如：alias &#123;aliasa_t aliasb_t&#125;。</span><br><span class="line"># 属性集：赋予该type定义的属性之中。是一个或多个预先声明的属性标识符，如果同时指定多个属性标识符，属性之间使用逗号进行分隔，如：type bin_t, file_type, exec_type;</span><br><span class="line"> </span><br><span class="line">type type_id [alias alias_id] [, attribute_id];</span><br><span class="line"># type            The type keyword.</span><br><span class="line"># type_id         The type identifier.</span><br><span class="line"># alias           Optional alias keyword that signifies alternate identifiers <span class="keyword">for</span> the type_id that are declared in the alias_id list.</span><br><span class="line"># alias_id        One or more alias identifiers that have been previously declared by the [typealias](https:<span class="comment">//selinuxproject.org/page/TypeStatements#typealias) statement.</span></span><br><span class="line">#                 <span class="function">Multiple entries consist of a space separated list enclosed in <span class="title">braces</span> <span class="params">(&#123;&#125;)</span>.</span></span><br><span class="line"><span class="function"># attribute_id    One or more optional attribute identifiers that have been previously declared by the [attribute]<span class="params">(https://selinuxproject.org/page/TypeStatements#attribute)</span></span></span><br><span class="line"><span class="function">#                 statement. Multiple entries consist of a <span class="title">comma</span> <span class="params">(,)</span> separated list, also note the lead comma.</span></span><br></pre></td></tr></table></figure>
<p>例子:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"># Using the type statement to declare a type of shell_exec_t,</span><br><span class="line"># where exec_t is used to identify a file as an executable type.</span><br><span class="line">type shell_exec_t;</span><br></pre></td></tr></table></figure>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"># Using the type statement to declare a type of</span><br><span class="line"># ssh_server_packet_t that also associates it to two previously</span><br><span class="line"># declared attributes packet_type and server_packet_type.</span><br><span class="line"> </span><br><span class="line">attribute packet_type;        # declare attribute <span class="number">1</span></span><br><span class="line">attribute server_packet_type; # declare attribute <span class="number">2</span></span><br><span class="line"> </span><br><span class="line"># Associate the type identifier with the two attributes:</span><br><span class="line"> </span><br><span class="line">type ssh_server_packet_t, packet_type, server_packet_type;</span><br></pre></td></tr></table></figure>
<h3 id="attribute"><a href="#attribute" class="headerlink" title="attribute"></a>attribute</h3><p>由于SELinux的默认的规则是拒绝所有的访问，所以任何一个访问都需要有一个规则定义与之匹配。Android中的type好几百，逐个定义耗时巨大，所以引入属性，可以将一个属性赋予多个type，以此达到批量定义规则的目的。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"># attribute 属性名称;</span><br><span class="line">attribute attribute_id;</span><br><span class="line"># attribute       The attribute keyword.</span><br><span class="line"># attribute_id    The attribute identifier.</span><br></pre></td></tr></table></figure>
<p>例子:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"># Using the attribute statement to declare attributes domain,</span><br><span class="line"># daemon, file_type and non_security_file_type:</span><br><span class="line"> </span><br><span class="line">attribute domain;</span><br><span class="line">attribute daemon;</span><br><span class="line">attribute file_type;</span><br><span class="line">attribute non_security_file_type;</span><br></pre></td></tr></table></figure>
<h2 id="Access-Vector-Rules"><a href="#Access-Vector-Rules" class="headerlink" title="Access Vector Rules"></a><a href="https://selinuxproject.org/page/AVCRules">Access Vector Rules</a></h2><p>The common format of the Access Vector Rule is:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">rule_name source_type target_type : <span class="class"><span class="keyword">class</span> <span class="title">perm_set</span></span>;</span><br><span class="line"># rule_name                The applicable allow, dontaudit, auditallow, and neverallow rule keyword.</span><br><span class="line">#</span><br><span class="line"># source_type/target_type  One or more source / target type, typealias or attribute identifiers. Multiple entries consist of</span><br><span class="line">#                          <span class="function">a space separated list enclosed in <span class="title">braces</span> <span class="params">(&#123;&#125;)</span>. Entries can be excluded from the list by using the</span></span><br><span class="line"><span class="function">#                          negative <span class="title">operator</span> <span class="params">(-)</span>.The target_type can have the self keyword instead of type, typealias or attribute</span></span><br><span class="line"><span class="function">#                          identifiers. This means that the target_type is the same as the source_type.The neverallow rule also</span></span><br><span class="line"><span class="function">#                          supports the wildcard <span class="title">operator</span> <span class="params">(*)</span> to specify that all types are to be included and the complement <span class="title">operator</span> <span class="params">(~)</span></span></span><br><span class="line"><span class="function">#                          to specify all types are to be included except those explicitly listed.</span></span><br><span class="line"><span class="function">#</span></span><br><span class="line"><span class="function"># class                    One or more object classes. Multiple entries consist of a space separated list enclosed in <span class="title">braces</span> <span class="params">(&#123;&#125;)</span>.</span></span><br><span class="line"><span class="function">#</span></span><br><span class="line"><span class="function"># perm_set                 The access permissions the source is allowed to access <span class="keyword">for</span> the target <span class="title">object</span> <span class="params">(also known as the Acess Vector)</span>.</span></span><br><span class="line"><span class="function">#                          Multiple entries consist of a space separated list enclosed in <span class="title">braces</span> <span class="params">(&#123;&#125;)</span>.The optional wildcard <span class="title">operator</span> <span class="params">(*)</span></span></span><br><span class="line"><span class="function">#                          specifies that all permissions <span class="keyword">for</span> the object class can be used.The complement <span class="title">operator</span> <span class="params">(~)</span> is used to specify</span></span><br><span class="line"><span class="function">#                          all permissions except those explicitly <span class="title">listed</span> <span class="params">(although the compiler issues a warning <span class="keyword">if</span> the dontaudit rule has <span class="string">&#x27;~&#x27;</span>)</span>.</span></span><br></pre></td></tr></table></figure>
<h3 id="allow"><a href="#allow" class="headerlink" title="allow"></a>allow</h3><p>allow规则检查定义进程是否有权访问某个系统资源。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"># Using the allow rule to show that initrc_t is allowed access</span><br><span class="line"># to files of type acct_exec_t that have the getattr, read and</span><br><span class="line"># execute file permissions:</span><br><span class="line"> </span><br><span class="line">allow initrc_t acct_exec_t:file &#123; getattr read execute &#125;;</span><br></pre></td></tr></table></figure>
<h3 id="neverallow"><a href="#neverallow" class="headerlink" title="neverallow"></a>neverallow</h3><p>该规则指定即使该操作先前已被允许，也不能为该操作生成允许规则。 neverallow语句是编译器强制执行的操作，其中checkpolicy或checkmodule [1]编译器检查策略源中是否已生成任何允许规则，如果是，它将发出警告并停止。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"># Using the neverallow rule to state that no allow rule may ever</span><br><span class="line"># grant any file read access to type shadow_t except those</span><br><span class="line"># associated with the can_read_shadow_passwords attribute:</span><br><span class="line"> </span><br><span class="line">neverallow ~can_read_shadow_passwords shadow_t:file read;</span><br></pre></td></tr></table></figure>

<h1 id="Android-SE流程"><a href="#Android-SE流程" class="headerlink" title="Android SE流程"></a>Android SE流程</h1><h2 id="SE加载流程"><a href="#SE加载流程" class="headerlink" title="SE加载流程"></a>SE加载流程</h2><h3 id="first-stage-main-main"><a href="#first-stage-main-main" class="headerlink" title="first_stage_main.main()"></a>first_stage_main.main()</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/system/core/init/first_stage_main.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;first_stage_init.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span>** argv)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> android::init::<span class="built_in">FirstStageMain</span>(argc, argv);</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<h3 id="first-stage-init-FirstStageMain"><a href="#first-stage-init-FirstStageMain" class="headerlink" title="first_stage_init.FirstStageMain()"></a>first_stage_init.FirstStageMain()</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/system/core/init/first_stage_init.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">FirstStageMain</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span>** argv)</span> </span>&#123;</span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span>* path = <span class="string">&quot;/system/bin/init&quot;</span>;</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span>* args[] = &#123;path, <span class="string">&quot;selinux_setup&quot;</span>, <span class="literal">nullptr</span>&#125;;</span><br><span class="line">    ...</span><br><span class="line">    <span class="built_in">execv</span>(path, <span class="keyword">const_cast</span>&lt;<span class="keyword">char</span>**&gt;(args));</span><br><span class="line"></span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="main-main"><a href="#main-main" class="headerlink" title="main.main()"></a>main.main()</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/system/core/init/main.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span>** argv)</span> </span>&#123;</span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (argc &gt; <span class="number">1</span>) &#123;</span><br><span class="line">        ...</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (!<span class="built_in">strcmp</span>(argv[<span class="number">1</span>], <span class="string">&quot;selinux_setup&quot;</span>)) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="built_in">SetupSelinux</span>(argv);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        ...</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<h3 id="selinux-SetupSelinux"><a href="#selinux-SetupSelinux" class="headerlink" title="selinux.SetupSelinux()"></a>selinux.SetupSelinux()</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/system/core/init/selinux.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// The SELinux setup process is carefully orchestrated around snapuserd. Policy</span></span><br><span class="line"><span class="comment">// must be loaded off dynamic partitions, and during an OTA, those partitions</span></span><br><span class="line"><span class="comment">// cannot be read without snapuserd. But, with kernel-privileged snapuserd</span></span><br><span class="line"><span class="comment">// running, loading the policy will immediately trigger audits.</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="comment">// We use a five-step process to address this:</span></span><br><span class="line"><span class="comment">//  (1) Read the policy into a string, with snapuserd running.</span></span><br><span class="line"><span class="comment">//  (2) Rewrite the snapshot device-mapper tables, to generate new dm-user</span></span><br><span class="line"><span class="comment">//      devices and to flush I/O.</span></span><br><span class="line"><span class="comment">//  (3) Kill snapuserd, which no longer has any dm-user devices to attach to.</span></span><br><span class="line"><span class="comment">//  (4) Load the sepolicy and issue critical restorecons in /dev, carefully</span></span><br><span class="line"><span class="comment">//      avoiding anything that would read from /system.</span></span><br><span class="line"><span class="comment">//  (5) Re-launch snapuserd and attach it to the dm-user devices from step (2).</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="comment">// After this sequence, it is safe to enable enforcing mode and continue booting.</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">SetupSelinux</span><span class="params">(<span class="keyword">char</span>** argv)</span> </span>&#123;</span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Read the policy before potentially killing snapuserd.</span></span><br><span class="line">    std::string policy;</span><br><span class="line">    <span class="built_in">ReadPolicy</span>(&amp;policy);</span><br><span class="line"></span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="built_in">LoadSelinuxPolicy</span>(policy);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (snapuserd_helper) &#123;</span><br><span class="line">        <span class="comment">// Before enforcing, finish the pending snapuserd transition.</span></span><br><span class="line">        snapuserd_helper-&gt;<span class="built_in">FinishTransition</span>();</span><br><span class="line">        snapuserd_helper = <span class="literal">nullptr</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">SelinuxSetEnforcement</span>();</span><br><span class="line"></span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span>* path = <span class="string">&quot;/system/bin/init&quot;</span>;</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span>* args[] = &#123;path, <span class="string">&quot;second_stage&quot;</span>, <span class="literal">nullptr</span>&#125;;</span><br><span class="line">    <span class="built_in">execv</span>(path, <span class="keyword">const_cast</span>&lt;<span class="keyword">char</span>**&gt;(args));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// execv() only returns if an error happened, in which case we</span></span><br><span class="line">    <span class="comment">// panic and never return from this function.</span></span><br><span class="line">    <span class="built_in">PLOG</span>(FATAL) &lt;&lt; <span class="string">&quot;execv(\&quot;&quot;</span> &lt;&lt; path &lt;&lt; <span class="string">&quot;\&quot;) failed&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="selinux-ReadPolicy"><a href="#selinux-ReadPolicy" class="headerlink" title="selinux.ReadPolicy()"></a>selinux.ReadPolicy()</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/system/core/init/selinux.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">ReadPolicy</span><span class="params">(std::string* policy)</span> </span>&#123;</span><br><span class="line">    PolicyFile policy_file;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//IsSplitPolicyDevice()为true</span></span><br><span class="line">    <span class="keyword">bool</span> ok = <span class="built_in">IsSplitPolicyDevice</span>() ? <span class="built_in">OpenSplitPolicy</span>(&amp;policy_file)</span><br><span class="line">                                    : <span class="built_in">OpenMonolithicPolicy</span>(&amp;policy_file);</span><br><span class="line">    <span class="keyword">if</span> (!ok) &#123;</span><br><span class="line">        <span class="built_in">LOG</span>(FATAL) &lt;&lt; <span class="string">&quot;Unable to open SELinux policy&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!android::base::<span class="built_in">ReadFdToString</span>(policy_file.fd, policy)) &#123;</span><br><span class="line">        <span class="built_in">PLOG</span>(FATAL) &lt;&lt; <span class="string">&quot;Failed to read policy file: &quot;</span> &lt;&lt; policy_file.path;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">constexpr</span> <span class="keyword">const</span> <span class="keyword">char</span> plat_policy_cil_file[] = <span class="string">&quot;/system/etc/selinux/plat_sepolicy.cil&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">IsSplitPolicyDevice</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">access</span>(plat_policy_cil_file, R_OK) != <span class="number">-1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="selinux-OpenSplitPolicy"><a href="#selinux-OpenSplitPolicy" class="headerlink" title="selinux.OpenSplitPolicy()"></a>selinux.OpenSplitPolicy()</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/system/core/init/selinux.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">OpenSplitPolicy</span><span class="params">(PolicyFile* policy_file)</span> </span>&#123;</span><br><span class="line">    ...</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (!use_userdebug_policy) &#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="keyword">auto</span> res = <span class="built_in">FindPrecompiledSplitPolicy</span>(); res.<span class="built_in">ok</span>()) &#123;</span><br><span class="line">            <span class="function">unique_fd <span class="title">fd</span><span class="params">(open(res-&gt;c_str(), O_RDONLY | O_CLOEXEC | O_BINARY))</span></span>;</span><br><span class="line">            <span class="keyword">if</span> (fd != <span class="number">-1</span>) &#123;</span><br><span class="line">                policy_file-&gt;fd = std::<span class="built_in">move</span>(fd);</span><br><span class="line">                policy_file-&gt;path = std::<span class="built_in">move</span>(*res);</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="built_in">LOG</span>(INFO) &lt;&lt; res.<span class="built_in">error</span>();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Determine which mapping file to include</span></span><br><span class="line">    <span class="comment">// Determine which mapping file to include</span></span><br><span class="line">    std::string vend_plat_vers;</span><br><span class="line">    <span class="keyword">if</span> (!<span class="built_in">GetVendorMappingVersion</span>(&amp;vend_plat_vers)) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function">std::string <span class="title">plat_mapping_file</span><span class="params">(<span class="string">&quot;/system/etc/selinux/mapping/&quot;</span> + vend_plat_vers + <span class="string">&quot;.cil&quot;</span>)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function">std::string <span class="title">plat_compat_cil_file</span><span class="params">(<span class="string">&quot;/system/etc/selinux/mapping/&quot;</span> + vend_plat_vers +</span></span></span><br><span class="line"><span class="params"><span class="function">                                     <span class="string">&quot;.compat.cil&quot;</span>)</span></span>;</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">access</span>(plat_compat_cil_file.<span class="built_in">c_str</span>(), F_OK) == <span class="number">-1</span>) &#123;</span><br><span class="line">        plat_compat_cil_file.<span class="built_in">clear</span>();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function">std::string <span class="title">system_ext_policy_cil_file</span><span class="params">(<span class="string">&quot;/system_ext/etc/selinux/system_ext_sepolicy.cil&quot;</span>)</span></span>;</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">access</span>(system_ext_policy_cil_file.<span class="built_in">c_str</span>(), F_OK) == <span class="number">-1</span>) &#123;</span><br><span class="line">        system_ext_policy_cil_file.<span class="built_in">clear</span>();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function">std::string <span class="title">system_ext_mapping_file</span><span class="params">(<span class="string">&quot;/system_ext/etc/selinux/mapping/&quot;</span> + vend_plat_vers +</span></span></span><br><span class="line"><span class="params"><span class="function">                                        <span class="string">&quot;.cil&quot;</span>)</span></span>;</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">access</span>(system_ext_mapping_file.<span class="built_in">c_str</span>(), F_OK) == <span class="number">-1</span>) &#123;</span><br><span class="line">        system_ext_mapping_file.<span class="built_in">clear</span>();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function">std::string <span class="title">system_ext_compat_cil_file</span><span class="params">(<span class="string">&quot;/system_ext/etc/selinux/mapping/&quot;</span> + vend_plat_vers +</span></span></span><br><span class="line"><span class="params"><span class="function">                                           <span class="string">&quot;.compat.cil&quot;</span>)</span></span>;</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">access</span>(system_ext_compat_cil_file.<span class="built_in">c_str</span>(), F_OK) == <span class="number">-1</span>) &#123;</span><br><span class="line">        system_ext_compat_cil_file.<span class="built_in">clear</span>();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function">std::string <span class="title">product_policy_cil_file</span><span class="params">(<span class="string">&quot;/product/etc/selinux/product_sepolicy.cil&quot;</span>)</span></span>;</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">access</span>(product_policy_cil_file.<span class="built_in">c_str</span>(), F_OK) == <span class="number">-1</span>) &#123;</span><br><span class="line">        product_policy_cil_file.<span class="built_in">clear</span>();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function">std::string <span class="title">product_mapping_file</span><span class="params">(<span class="string">&quot;/product/etc/selinux/mapping/&quot;</span> + vend_plat_vers + <span class="string">&quot;.cil&quot;</span>)</span></span>;</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">access</span>(product_mapping_file.<span class="built_in">c_str</span>(), F_OK) == <span class="number">-1</span>) &#123;</span><br><span class="line">        product_mapping_file.<span class="built_in">clear</span>();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// vendor_sepolicy.cil and plat_pub_versioned.cil are the new design to replace</span></span><br><span class="line">    <span class="comment">// nonplat_sepolicy.cil.</span></span><br><span class="line">    <span class="function">std::string <span class="title">plat_pub_versioned_cil_file</span><span class="params">(<span class="string">&quot;/vendor/etc/selinux/plat_pub_versioned.cil&quot;</span>)</span></span>;</span><br><span class="line">    <span class="function">std::string <span class="title">vendor_policy_cil_file</span><span class="params">(<span class="string">&quot;/vendor/etc/selinux/vendor_sepolicy.cil&quot;</span>)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">access</span>(vendor_policy_cil_file.<span class="built_in">c_str</span>(), F_OK) == <span class="number">-1</span>) &#123;</span><br><span class="line">        <span class="comment">// For backward compatibility.</span></span><br><span class="line">        <span class="comment">// <span class="doctag">TODO:</span> remove this after no device is using nonplat_sepolicy.cil.</span></span><br><span class="line">        vendor_policy_cil_file = <span class="string">&quot;/vendor/etc/selinux/nonplat_sepolicy.cil&quot;</span>;</span><br><span class="line">        plat_pub_versioned_cil_file.<span class="built_in">clear</span>();</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (<span class="built_in">access</span>(plat_pub_versioned_cil_file.<span class="built_in">c_str</span>(), F_OK) == <span class="number">-1</span>) &#123;</span><br><span class="line">        <span class="built_in">LOG</span>(ERROR) &lt;&lt; <span class="string">&quot;Missing &quot;</span> &lt;&lt; plat_pub_versioned_cil_file;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// odm_sepolicy.cil is default but optional.</span></span><br><span class="line">    <span class="function">std::string <span class="title">odm_policy_cil_file</span><span class="params">(<span class="string">&quot;/odm/etc/selinux/odm_sepolicy.cil&quot;</span>)</span></span>;</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">access</span>(odm_policy_cil_file.<span class="built_in">c_str</span>(), F_OK) == <span class="number">-1</span>) &#123;</span><br><span class="line">        odm_policy_cil_file.<span class="built_in">clear</span>();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">const</span> std::string version_as_string = std::<span class="built_in">to_string</span>(SEPOLICY_VERSION);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// clang-format off</span></span><br><span class="line">    std::vector&lt;<span class="keyword">const</span> <span class="keyword">char</span>*&gt; compile_args &#123;</span><br><span class="line">        <span class="string">&quot;/system/bin/secilc&quot;</span>,</span><br><span class="line">        use_userdebug_policy ? kDebugRamdiskSEPolicy: plat_policy_cil_file,</span><br><span class="line">        <span class="string">&quot;-m&quot;</span>, <span class="string">&quot;-M&quot;</span>, <span class="string">&quot;true&quot;</span>, <span class="string">&quot;-G&quot;</span>, <span class="string">&quot;-N&quot;</span>,</span><br><span class="line">        <span class="string">&quot;-c&quot;</span>, version_as_string.<span class="built_in">c_str</span>(),</span><br><span class="line">        plat_mapping_file.<span class="built_in">c_str</span>(),</span><br><span class="line">        <span class="string">&quot;-o&quot;</span>, compiled_sepolicy,</span><br><span class="line">        <span class="comment">// We don&#x27;t care about file_contexts output by the compiler</span></span><br><span class="line">        <span class="string">&quot;-f&quot;</span>, <span class="string">&quot;/sys/fs/selinux/null&quot;</span>,  <span class="comment">// /dev/null is not yet available</span></span><br><span class="line">    &#125;;</span><br><span class="line">    <span class="comment">// clang-format on</span></span><br><span class="line">    </span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="selinux-FindPrecompiledSplitPolicy"><a href="#selinux-FindPrecompiledSplitPolicy" class="headerlink" title="selinux.FindPrecompiledSplitPolicy()"></a>selinux.FindPrecompiledSplitPolicy()</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/system/core/init/selinux.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function">Result&lt;std::string&gt; <span class="title">FindPrecompiledSplitPolicy</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    std::string precompiled_sepolicy;</span><br><span class="line">    <span class="comment">// If there is an odm partition, precompiled_sepolicy will be in</span></span><br><span class="line">    <span class="comment">// odm/etc/selinux. Otherwise it will be in vendor/etc/selinux.</span></span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">constexpr</span> <span class="keyword">const</span> <span class="keyword">char</span> vendor_precompiled_sepolicy[] =</span><br><span class="line">        <span class="string">&quot;/vendor/etc/selinux/precompiled_sepolicy&quot;</span>;</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">constexpr</span> <span class="keyword">const</span> <span class="keyword">char</span> odm_precompiled_sepolicy[] =</span><br><span class="line">        <span class="string">&quot;/odm/etc/selinux/precompiled_sepolicy&quot;</span>;</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">access</span>(odm_precompiled_sepolicy, R_OK) == <span class="number">0</span>) &#123;</span><br><span class="line">        precompiled_sepolicy = odm_precompiled_sepolicy;</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (<span class="built_in">access</span>(vendor_precompiled_sepolicy, R_OK) == <span class="number">0</span>) &#123;</span><br><span class="line">        precompiled_sepolicy = vendor_precompiled_sepolicy;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">ErrnoError</span>() &lt;&lt; <span class="string">&quot;No precompiled sepolicy at &quot;</span> &lt;&lt; vendor_precompiled_sepolicy;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">   ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>查看以下两个文件是否存在，如果存在那就尝试加载，如果加载成功则不会再去编译加载cil文件，也就是编译生成的cil文件被push到手机后不能生效。</p>
<ul>
<li>“/vendor/etc/selinux/precompiled_sepolicy”</li>
<li>“/odm/etc/selinux/precompiled_sepolicy”</li>
</ul>
</li>
<li><p>对cil文件进行编译</p>
</li>
</ul>
<p>收集平台上如下cil文件，然后调用/system/bin/secilc程序对.cil文件进行编译：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">/system/etc/selinux/plat_sepolicy.cil</span><br><span class="line">/system/etc/selinux/mapping<span class="comment">/*.cil</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">/system_ext/etc/selinux/plat_sepolicy.cil</span></span><br><span class="line"><span class="comment">/system_ext/etc/selinux/mapping/*.cil</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">/product/etc/selinux/product_sepolicy.cil</span></span><br><span class="line"><span class="comment">/product/etc/selinux/mapping/*.cil</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">/vendor/etc/selinux/vendor_sepolicy.cil</span></span><br><span class="line"><span class="comment">/vendor/etc/selinux/plat_pub_versioned.cil</span></span><br><span class="line"><span class="comment">/vendor/etc/selinux/nonplat_sepolicy.cil</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">/odm/etc/selinux/odm_sepolicy.cil</span></span><br></pre></td></tr></table></figure>

<h3 id="selinux-SelinuxSetEnforcement"><a href="#selinux-SelinuxSetEnforcement" class="headerlink" title="selinux.SelinuxSetEnforcement()"></a>selinux.SelinuxSetEnforcement()</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/system/core/init/selinux.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">SelinuxSetEnforcement</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">bool</span> kernel_enforcing = (<span class="built_in">security_getenforce</span>() == <span class="number">1</span>);</span><br><span class="line">    <span class="keyword">bool</span> is_enforcing = <span class="built_in">IsEnforcing</span>();</span><br><span class="line">    <span class="keyword">if</span> (kernel_enforcing != is_enforcing) &#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">security_setenforce</span>(is_enforcing)) &#123;</span><br><span class="line">            <span class="built_in">PLOG</span>(FATAL) &lt;&lt; <span class="string">&quot;security_setenforce(&quot;</span> &lt;&lt; (is_enforcing ? <span class="string">&quot;true&quot;</span> : <span class="string">&quot;false&quot;</span>)</span><br><span class="line">                        &lt;&lt; <span class="string">&quot;) failed&quot;</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">auto</span> result = <span class="built_in">WriteFile</span>(<span class="string">&quot;/sys/fs/selinux/checkreqprot&quot;</span>, <span class="string">&quot;0&quot;</span>); !result.<span class="built_in">ok</span>()) &#123;</span><br><span class="line">        <span class="built_in">LOG</span>(FATAL) &lt;&lt; <span class="string">&quot;Unable to write to /sys/fs/selinux/checkreqprot: &quot;</span> &lt;&lt; result.<span class="built_in">error</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>设置是否启用selinux。</p>
<h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>se的初始化是在init中执行的，大概流程为：</p>
<ul>
<li>判断是否有 <code>precompiled_sepolicy</code> 的文件，如果有则直接加载后退出；</li>
<li>如果没有预编译，那么收集指定的 <code>cil</code> 文件，并用 <code>/system/bin/secilc</code> 程序进行实时编译；</li>
<li>最后加载编译好的文件，完成SE的初始化。</li>
</ul>
<h2 id="设置应用pid流程"><a href="#设置应用pid流程" class="headerlink" title="设置应用pid流程"></a>设置应用pid流程</h2><p>应用进程是通过Zygote进程fork出来的，在SpecializeCommon()中，通过调用seLinux_android_setcontext()函数来设置应用进程的安全上下文。根据进程的用户名和seinfo在seapp_contexts表中进行查找对应的项，如果找到了，把找到的domain设置到安全上下文中就完成了。整个过程如下：</p>
<h3 id="forkAndSpecialize"><a href="#forkAndSpecialize" class="headerlink" title="forkAndSpecialize"></a>forkAndSpecialize</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/core/java/com/android/internal/os/Zygote.java</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">forkAndSpecialize</span><span class="params">(<span class="keyword">int</span> uid, <span class="keyword">int</span> gid, <span class="keyword">int</span>[] gids, <span class="keyword">int</span> runtimeFlags,</span></span></span><br><span class="line"><span class="params"><span class="function">                             <span class="keyword">int</span>[][] rlimits, <span class="keyword">int</span> mountExternal, String seInfo, String niceName, <span class="keyword">int</span>[] fdsToClose,</span></span></span><br><span class="line"><span class="params"><span class="function">                             <span class="keyword">int</span>[] fdsToIgnore, <span class="keyword">boolean</span> startChildZygote, String instructionSet, String appDataDir,</span></span></span><br><span class="line"><span class="params"><span class="function">                             <span class="keyword">boolean</span> isTopApp, String[] pkgDataInfoList, String[] allowlistedDataInfoList,</span></span></span><br><span class="line"><span class="params"><span class="function">                             <span class="keyword">boolean</span> bindMountAppDataDirs, <span class="keyword">boolean</span> bindMountAppStorageDirs)</span> </span>&#123;</span><br><span class="line">    ZygoteHooks.preFork();</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">int</span> pid = nativeForkAndSpecialize(</span><br><span class="line">        uid, gid, gids, runtimeFlags, rlimits, mountExternal, seInfo, niceName, fdsToClose,</span><br><span class="line">        fdsToIgnore, startChildZygote, instructionSet, appDataDir, isTopApp,</span><br><span class="line">        pkgDataInfoList, allowlistedDataInfoList, bindMountAppDataDirs,</span><br><span class="line">        bindMountAppStorageDirs);</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>通过jni调用调到了nativeForkAndSpecialize。</p>
<h3 id="nativeForkAndSpecialize"><a href="#nativeForkAndSpecialize" class="headerlink" title="nativeForkAndSpecialize"></a>nativeForkAndSpecialize</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/core/jni/com_android_internal_os_Zygote.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> jint <span class="title">com_android_internal_os_Zygote_nativeForkAndSpecialize</span><span class="params">(</span></span></span><br><span class="line"><span class="params"><span class="function">    JNIEnv* env, jclass, jint uid, jint gid, jintArray gids, jint runtime_flags,</span></span></span><br><span class="line"><span class="params"><span class="function">    jobjectArray rlimits, jint mount_external, jstring se_info, jstring nice_name,</span></span></span><br><span class="line"><span class="params"><span class="function">    jintArray managed_fds_to_close, jintArray managed_fds_to_ignore, jboolean is_child_zygote,</span></span></span><br><span class="line"><span class="params"><span class="function">    jstring instruction_set, jstring app_data_dir, jboolean is_top_app,</span></span></span><br><span class="line"><span class="params"><span class="function">    jobjectArray pkg_data_info_list, jobjectArray allowlisted_data_info_list,</span></span></span><br><span class="line"><span class="params"><span class="function">    jboolean mount_data_dirs, jboolean mount_storage_dirs)</span> </span>&#123;</span><br><span class="line">    ...</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">if</span> (pid == <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="built_in">SpecializeCommon</span>(env, uid, gid, gids, runtime_flags, rlimits, capabilities, capabilities,</span><br><span class="line">                             mount_external, se_info, nice_name, <span class="literal">false</span>, is_child_zygote == JNI_TRUE,</span><br><span class="line">                             instruction_set, app_data_dir, is_top_app == JNI_TRUE, pkg_data_info_list,</span><br><span class="line">                             allowlisted_data_info_list, mount_data_dirs == JNI_TRUE,</span><br><span class="line">                             mount_storage_dirs == JNI_TRUE);</span><br><span class="line">        &#125;</span><br><span class="line">    <span class="keyword">return</span> pid;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>可以看到在com_android_internal_os_Zygote_nativeForkAndSpecialize主要是调用了SpecializeCommon来设置进程安全上下文。</p>
<h3 id="SpecializeCommon"><a href="#SpecializeCommon" class="headerlink" title="SpecializeCommon"></a>SpecializeCommon</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/core/jni/com_android_internal_os_Zygote.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Utility routine to specialize a zygote child process.</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">SpecializeCommon</span><span class="params">(JNIEnv* env, <span class="keyword">uid_t</span> uid, <span class="keyword">gid_t</span> gid, jintArray gids, jint runtime_flags,</span></span></span><br><span class="line"><span class="params"><span class="function">                             jobjectArray rlimits, jlong permitted_capabilities,</span></span></span><br><span class="line"><span class="params"><span class="function">                             jlong effective_capabilities, jint mount_external,</span></span></span><br><span class="line"><span class="params"><span class="function">                             jstring managed_se_info, jstring managed_nice_name,</span></span></span><br><span class="line"><span class="params"><span class="function">                             <span class="keyword">bool</span> is_system_server, <span class="keyword">bool</span> is_child_zygote,</span></span></span><br><span class="line"><span class="params"><span class="function">                             jstring managed_instruction_set, jstring managed_app_data_dir,</span></span></span><br><span class="line"><span class="params"><span class="function">                             <span class="keyword">bool</span> is_top_app, jobjectArray pkg_data_info_list,</span></span></span><br><span class="line"><span class="params"><span class="function">                             jobjectArray allowlisted_data_info_list, <span class="keyword">bool</span> mount_data_dirs,</span></span></span><br><span class="line"><span class="params"><span class="function">                             <span class="keyword">bool</span> mount_storage_dirs)</span> </span>&#123;</span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">selinux_android_setcontext</span>(uid, is_system_server, se_info_ptr, nice_name_ptr) == <span class="number">-1</span>) &#123;</span><br><span class="line">        <span class="built_in">fail_fn</span>(<span class="built_in">CREATE_ERROR</span>(<span class="string">&quot;selinux_android_setcontext(%d, %d, \&quot;%s\&quot;, \&quot;%s\&quot;) failed&quot;</span>, uid,</span><br><span class="line">                             is_system_server, se_info_ptr, nice_name_ptr));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>调用selinux_android_setcontext，并传给seinfo和包名，下面来重点分析。</p>
<h3 id="selinux-android-setcontext"><a href="#selinux-android-setcontext" class="headerlink" title="selinux_android_setcontext"></a>selinux_android_setcontext</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/external/selinux/libselinux/src/android/android_platform.c#1027</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">selinux_android_setcontext</span><span class="params">(<span class="keyword">uid_t</span> uid,</span></span></span><br><span class="line"><span class="params"><span class="function">                   <span class="keyword">bool</span> isSystemServer,</span></span></span><br><span class="line"><span class="params"><span class="function">                   <span class="keyword">const</span> <span class="keyword">char</span> *seinfo,</span></span></span><br><span class="line"><span class="params"><span class="function">                   <span class="keyword">const</span> <span class="keyword">char</span> *pkgname)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">char</span> *orig_ctx_str = <span class="literal">NULL</span>, *ctx_str;</span><br><span class="line">    <span class="keyword">context_t</span> ctx = <span class="literal">NULL</span>;</span><br><span class="line">    <span class="keyword">int</span> rc = <span class="number">-1</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">is_selinux_enabled</span>() &lt;= <span class="number">0</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//得到当前进程从Zygote进程继承的安全上下文</span></span><br><span class="line">    rc = <span class="built_in">getcon</span>(&amp;ctx_str);</span><br><span class="line">    <span class="keyword">if</span> (rc)</span><br><span class="line">        <span class="keyword">goto</span> err;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//以ctx_str为模板创建一个新的安全上下文</span></span><br><span class="line">    ctx = <span class="built_in">context_new</span>(ctx_str);</span><br><span class="line">    orig_ctx_str = ctx_str;</span><br><span class="line">    <span class="keyword">if</span> (!ctx)</span><br><span class="line">        <span class="keyword">goto</span> oom;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//在SEAPP_DOMAIN中查找新的安全上下文</span></span><br><span class="line">    rc = <span class="built_in">seapp_context_lookup</span>(SEAPP_DOMAIN, uid, isSystemServer, seinfo, pkgname, <span class="literal">NULL</span>, ctx);</span><br><span class="line">    <span class="keyword">if</span> (rc == <span class="number">-1</span>)</span><br><span class="line">        <span class="keyword">goto</span> err;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (rc == <span class="number">-2</span>)</span><br><span class="line">        <span class="keyword">goto</span> oom;</span><br><span class="line"></span><br><span class="line">    ctx_str = <span class="built_in">context_str</span>(ctx);</span><br><span class="line">    <span class="keyword">if</span> (!ctx_str)</span><br><span class="line">        <span class="keyword">goto</span> oom;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//检查新的安全上下文</span></span><br><span class="line">    rc = <span class="built_in">security_check_context</span>(ctx_str);</span><br><span class="line">    <span class="keyword">if</span> (rc &lt; <span class="number">0</span>)</span><br><span class="line">        <span class="keyword">goto</span> err;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//如果新的安全上下文和原来的不同</span></span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">strcmp</span>(ctx_str, orig_ctx_str)) &#123;</span><br><span class="line">        <span class="comment">//为当前进程设置安全上下文</span></span><br><span class="line">        rc = <span class="built_in">selinux_android_setcon</span>(ctx_str);</span><br><span class="line">        <span class="keyword">if</span> (rc &lt; <span class="number">0</span>)</span><br><span class="line">            <span class="keyword">goto</span> err;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    rc = <span class="number">0</span>;</span><br><span class="line">out:</span><br><span class="line">    <span class="built_in">freecon</span>(orig_ctx_str);</span><br><span class="line">    <span class="built_in">context_free</span>(ctx);</span><br><span class="line">    <span class="built_in">avc_netlink_close</span>();</span><br><span class="line">    <span class="keyword">return</span> rc;</span><br><span class="line">err:</span><br><span class="line">    <span class="keyword">if</span> (isSystemServer)</span><br><span class="line">        <span class="built_in">selinux_log</span>(SELINUX_ERROR,</span><br><span class="line">                <span class="string">&quot;%s:  Error setting context for system server: %s\n&quot;</span>,</span><br><span class="line">                __FUNCTION__, <span class="built_in">strerror</span>(errno));</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="built_in">selinux_log</span>(SELINUX_ERROR,</span><br><span class="line">                <span class="string">&quot;%s:  Error setting context for app with uid %d, seinfo %s: %s\n&quot;</span>,</span><br><span class="line">                __FUNCTION__, uid, seinfo, <span class="built_in">strerror</span>(errno));</span><br><span class="line"></span><br><span class="line">    rc = <span class="number">-1</span>;</span><br><span class="line">    <span class="keyword">goto</span> out;</span><br><span class="line">oom:</span><br><span class="line">    <span class="built_in">selinux_log</span>(SELINUX_ERROR, <span class="string">&quot;%s:  Out of memory\n&quot;</span>, __FUNCTION__);</span><br><span class="line">    rc = <span class="number">-1</span>;</span><br><span class="line">    <span class="keyword">goto</span> out;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>得到当前进程从Zygote进程继承的安全上下文；</p>
</li>
<li><p>以ctx_str为模板创建一个新的安全上下文；</p>
</li>
<li><p>在SEAPP_DOMAIN中查找新的安全上下文（重点分析）；</p>
</li>
<li><p>检查新的安全上下文；</p>
</li>
<li><p>如果新的安全上下文和原来的不同，为当前进程设置安全上下文。</p>
<h3 id="seapp-context-lookup"><a href="#seapp-context-lookup" class="headerlink" title="seapp_context_lookup"></a>seapp_context_lookup</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/external/selinux/libselinux/src/android/android_platform.c</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">seapp_context_lookup</span><span class="params">(<span class="keyword">enum</span> seapp_kind kind,</span></span></span><br><span class="line"><span class="params"><span class="function">                <span class="keyword">uid_t</span> uid,</span></span></span><br><span class="line"><span class="params"><span class="function">                <span class="keyword">bool</span> isSystemServer,</span></span></span><br><span class="line"><span class="params"><span class="function">                <span class="keyword">const</span> <span class="keyword">char</span> *seinfo,</span></span></span><br><span class="line"><span class="params"><span class="function">                <span class="keyword">const</span> <span class="keyword">char</span> *pkgname,</span></span></span><br><span class="line"><span class="params"><span class="function">                <span class="keyword">const</span> <span class="keyword">char</span> *path,</span></span></span><br><span class="line"><span class="params"><span class="function">                <span class="keyword">context_t</span> ctx)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">passwd</span> *<span class="title">pwd</span>;</span></span><br><span class="line">    <span class="keyword">bool</span> isOwner;</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span> *username = <span class="literal">NULL</span>;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">seapp_context</span> *<span class="title">cur</span> =</span> <span class="literal">NULL</span>;</span><br><span class="line">    <span class="keyword">int</span> i;</span><br><span class="line">    <span class="keyword">uid_t</span> userid;</span><br><span class="line">    <span class="keyword">uid_t</span> appid;</span><br><span class="line">    <span class="keyword">bool</span> isPrivApp = <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">bool</span> isEphemeralApp = <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">int32_t</span> targetSdkVersion = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">bool</span> fromRunAs = <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">char</span> parsedseinfo[BUFSIZ];</span><br><span class="line"></span><br><span class="line">    <span class="comment">//初始化 seapp_context</span></span><br><span class="line">    <span class="built_in">selinux_android_seapp_context_init</span>();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (seinfo) &#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">seinfo_parse</span>(parsedseinfo, seinfo, BUFSIZ))</span><br><span class="line">            <span class="keyword">goto</span> err;</span><br><span class="line">        isPrivApp = <span class="built_in">strstr</span>(seinfo, PRIVILEGED_APP_STR) ? <span class="literal">true</span> : <span class="literal">false</span>;</span><br><span class="line">        isEphemeralApp = <span class="built_in">strstr</span>(seinfo, EPHEMERAL_APP_STR) ? <span class="literal">true</span> : <span class="literal">false</span>;</span><br><span class="line">        fromRunAs = <span class="built_in">strstr</span>(seinfo, FROM_RUNAS_STR) ? <span class="literal">true</span> : <span class="literal">false</span>;</span><br><span class="line">        targetSdkVersion = <span class="built_in">get_app_targetSdkVersion</span>(seinfo);</span><br><span class="line">        <span class="keyword">if</span> (targetSdkVersion &lt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="built_in">selinux_log</span>(SELINUX_ERROR,</span><br><span class="line">                    <span class="string">&quot;%s:  Invalid targetSdkVersion passed for app with uid %d, seinfo %s, name %s\n&quot;</span>,</span><br><span class="line">                    __FUNCTION__, uid, seinfo, pkgname);</span><br><span class="line">            <span class="keyword">goto</span> err;</span><br><span class="line">        &#125;</span><br><span class="line">        seinfo = parsedseinfo;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    userid = uid / AID_USER;</span><br><span class="line">    isOwner = (userid == <span class="number">0</span>);</span><br><span class="line">    appid = uid % AID_USER;</span><br><span class="line">    <span class="keyword">if</span> (appid &lt; AID_APP) &#123;<span class="comment">//通过uid获取进程名</span></span><br><span class="line">            <span class="comment">/*</span></span><br><span class="line"><span class="comment">             * This code is Android specific, bionic guarantees that</span></span><br><span class="line"><span class="comment">             * calls to non-reentrant getpwuid() are thread safe.</span></span><br><span class="line"><span class="comment">             */</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifndef</span> __BIONIC__</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">warning</span> <span class="meta-string">&quot;This code assumes that getpwuid is thread safe, only true with Bionic!&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br><span class="line">        pwd = <span class="built_in">getpwuid</span>(appid);</span><br><span class="line">        <span class="keyword">if</span> (!pwd)</span><br><span class="line">            <span class="keyword">goto</span> err;</span><br><span class="line"></span><br><span class="line">        username = pwd-&gt;pw_name;</span><br><span class="line"></span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (appid &lt; AID_ISOLATED_START) &#123;<span class="comment">//如果进程号小于AID_ISOLATED_START，说明是普通进程，设置用户名为&quot;_app&quot;</span></span><br><span class="line">        username = <span class="string">&quot;_app&quot;</span>;</span><br><span class="line">        appid -= AID_APP;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;<span class="comment">//沙箱进程</span></span><br><span class="line">        username = <span class="string">&quot;_isolated&quot;</span>;</span><br><span class="line">        appid -= AID_ISOLATED_START;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (appid &gt;= CAT_MAPPING_MAX_ID || userid &gt;= CAT_MAPPING_MAX_ID)</span><br><span class="line">        <span class="keyword">goto</span> err;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; nspec; i++) &#123;<span class="comment">//在seapp_contexts表中进行查找</span></span><br><span class="line">        cur = seapp_contexts[i];</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (cur-&gt;isSystemServer != isSystemServer)<span class="comment">//如果是systemserver进程不做处理</span></span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (cur-&gt;isEphemeralAppSet &amp;&amp; cur-&gt;isEphemeralApp != isEphemeralApp)</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (cur-&gt;isOwnerSet &amp;&amp; cur-&gt;isOwner != isOwner)</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (cur-&gt;user.str) &#123;<span class="comment">//先比较文件中的user项和进程的username</span></span><br><span class="line">            <span class="keyword">if</span> (cur-&gt;user.is_prefix) &#123;</span><br><span class="line">                <span class="keyword">if</span> (<span class="built_in">strncasecmp</span>(username, cur-&gt;user.str, cur-&gt;user.len<span class="number">-1</span>))</span><br><span class="line">                    <span class="keyword">continue</span>;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="keyword">if</span> (<span class="built_in">strcasecmp</span>(username, cur-&gt;user.str))</span><br><span class="line">                    <span class="keyword">continue</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (cur-&gt;seinfo) &#123;<span class="comment">//先比较两者的seinfo是否相同</span></span><br><span class="line">            <span class="keyword">if</span> (!seinfo || <span class="built_in">strcasecmp</span>(seinfo, cur-&gt;seinfo))</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (cur-&gt;name.str) &#123;<span class="comment">//seapp_contexts中没有哪行定义了name，所以这里不用比较</span></span><br><span class="line">            <span class="keyword">if</span>(!pkgname)</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (cur-&gt;name.is_prefix) &#123;</span><br><span class="line">                <span class="keyword">if</span> (<span class="built_in">strncasecmp</span>(pkgname, cur-&gt;name.str, cur-&gt;name.len<span class="number">-1</span>))</span><br><span class="line">                    <span class="keyword">continue</span>;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="keyword">if</span> (<span class="built_in">strcasecmp</span>(pkgname, cur-&gt;name.str))</span><br><span class="line">                    <span class="keyword">continue</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (cur-&gt;isPrivAppSet &amp;&amp; cur-&gt;isPrivApp != isPrivApp)<span class="comment">//是否是priv-app</span></span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (cur-&gt;minTargetSdkVersion &gt; targetSdkVersion)</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (cur-&gt;fromRunAs != fromRunAs)</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (cur-&gt;path.str) &#123;</span><br><span class="line">            <span class="keyword">if</span> (!path)</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (cur-&gt;path.is_prefix) &#123;</span><br><span class="line">                <span class="keyword">if</span> (<span class="built_in">strncmp</span>(path, cur-&gt;path.str, cur-&gt;path.len<span class="number">-1</span>))</span><br><span class="line">                    <span class="keyword">continue</span>;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="keyword">if</span> (<span class="built_in">strcmp</span>(path, cur-&gt;path.str))</span><br><span class="line">                    <span class="keyword">continue</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (kind == SEAPP_TYPE &amp;&amp; !cur-&gt;type)<span class="comment">//参数kind等于SEAPP_DOMAIN</span></span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (kind == SEAPP_DOMAIN &amp;&amp; !cur-&gt;domain)<span class="comment">//跳过文件中没有domain的项</span></span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (kind == SEAPP_TYPE) &#123;</span><br><span class="line">            <span class="keyword">if</span> (<span class="built_in">context_type_set</span>(ctx, cur-&gt;type))</span><br><span class="line">                <span class="keyword">goto</span> oom;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (kind == SEAPP_DOMAIN) &#123;</span><br><span class="line">            <span class="keyword">if</span> (<span class="built_in">context_type_set</span>(ctx, cur-&gt;domain))<span class="comment">//设置安全上下文的域</span></span><br><span class="line">                <span class="keyword">goto</span> oom;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (cur-&gt;levelFrom != LEVELFROM_NONE) &#123;</span><br><span class="line">            <span class="keyword">int</span> res = <span class="built_in">set_range_from_level</span>(ctx, cur-&gt;levelFrom, userid, appid);</span><br><span class="line">            <span class="keyword">if</span> (res != <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="keyword">return</span> res;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (cur-&gt;level) &#123;</span><br><span class="line">            <span class="keyword">if</span> (<span class="built_in">context_range_set</span>(ctx, cur-&gt;level))</span><br><span class="line">                <span class="keyword">goto</span> oom;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (kind == SEAPP_DOMAIN &amp;&amp; i == nspec) &#123;</span><br><span class="line">        <span class="comment">/*</span></span><br><span class="line"><span class="comment">         * No match.</span></span><br><span class="line"><span class="comment">         * Fail to prevent staying in the zygote&#x27;s context.</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        <span class="built_in">selinux_log</span>(SELINUX_ERROR,</span><br><span class="line">                <span class="string">&quot;%s:  No match for app with uid %d, seinfo %s, name %s\n&quot;</span>,</span><br><span class="line">                __FUNCTION__, uid, seinfo, pkgname);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">security_getenforce</span>() == <span class="number">1</span>)</span><br><span class="line">            <span class="keyword">goto</span> err;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">err:</span><br><span class="line">    <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">oom:</span><br><span class="line">    <span class="keyword">return</span> <span class="number">-2</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>初始化seapp_context（重点分析）；</p>
</li>
<li><p>通过uid获取进程名；</p>
<ul>
<li> 如果进程号小于AID_APP，则通过uid获取进程名；</li>
<li> 如果进程号小于AID_ISOLATED_START，说明是普通进程，设置用户名为”_app”；</li>
<li> 否则为沙箱进程。</li>
</ul>
</li>
<li><p>在seapp_contexts表中进行查找；</p>
<ul>
<li>比较文件中的user项和进程的username；</li>
<li>比较两者的seinfo是否相同；</li>
<li>seapp_contexts中没有哪行定义了name，所以这里不用比较；</li>
<li>是否是priv-app；</li>
<li>最小的TargetSdk是否大于等于当前的TargetSdk。</li>
</ul>
</li>
<li><p>参数kind等于SEAPP_DOMAIN；</p>
</li>
<li><p>跳过文件中没有domain的项；</p>
</li>
<li><p>设置安全上下文的域。</p>
</li>
</ul>
<h3 id="selinux-android-seapp-context-init"><a href="#selinux-android-seapp-context-init" class="headerlink" title="selinux_android_seapp_context_init"></a>selinux_android_seapp_context_init</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/external/selinux/libselinux/src/android/android_platform.c</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">selinux_android_seapp_context_init</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123;</span><br><span class="line">    __selinux_once(once, seapp_context_init);</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>从selinux_internal.h可以看到__selinux_once是一个宏，这个宏就很简单了：请注意“参数2”。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/external/selinux/libselinux/src/selinux_internal.h#122</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/* Call handler iff the first call.  */</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> __selinux_once(ONCE_CONTROL, INIT_FUNCTION) \</span></span><br><span class="line"><span class="meta">    do &#123;                        \</span></span><br><span class="line"><span class="meta">        <span class="meta-keyword">if</span> (pthread_once != NULL)       \</span></span><br><span class="line"><span class="meta">            pthread_once (&amp;(ONCE_CONTROL), (INIT_FUNCTION));  \</span></span><br><span class="line"><span class="meta">        <span class="meta-keyword">else</span> <span class="meta-keyword">if</span> ((ONCE_CONTROL) == PTHREAD_ONCE_INIT) &#123;       \</span></span><br><span class="line"><span class="meta">            INIT_FUNCTION ();       \</span></span><br><span class="line"><span class="meta">            (ONCE_CONTROL) = 2;     \</span></span><br><span class="line"><span class="meta">        &#125;                   \</span></span><br><span class="line"><span class="meta">    &#125; while (0)</span></span><br></pre></td></tr></table></figure>

<h4 id="seapp-context-init"><a href="#seapp-context-init" class="headerlink" title="seapp_context_init"></a>seapp_context_init</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/external/selinux/libselinux/src/selinux_internal.h#122</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">seapp_context_init</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">        <span class="built_in">selinux_android_seapp_context_reload</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="selinux-android-seapp-context-reload"><a href="#selinux-android-seapp-context-reload" class="headerlink" title="selinux_android_seapp_context_reload"></a>selinux_android_seapp_context_reload</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/external/selinux/libselinux/src/android/android_platform.c</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">selinux_android_seapp_context_reload</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    FILE *fp = <span class="literal">NULL</span>;</span><br><span class="line">    <span class="keyword">char</span> line_buf[BUFSIZ];</span><br><span class="line">    <span class="keyword">char</span> *token;</span><br><span class="line">    <span class="keyword">unsigned</span> lineno;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">seapp_context</span> *<span class="title">cur</span>;</span></span><br><span class="line">    <span class="keyword">char</span> *p, *name = <span class="literal">NULL</span>, *value = <span class="literal">NULL</span>, *saveptr;</span><br><span class="line">    <span class="keyword">size_t</span> i, len, files_len = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">int</span> ret;</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span>* seapp_contexts_files[MAX_FILE_CONTEXT_SIZE];</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; <span class="built_in">ARRAY_SIZE</span>(seapp_contexts_plat); i++) &#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">access</span>(seapp_contexts_plat[i], R_OK) != <span class="number">-1</span>) &#123;</span><br><span class="line">            seapp_contexts_files[files_len++] = seapp_contexts_plat[i];</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; <span class="built_in">ARRAY_SIZE</span>(seapp_contexts_system_ext); i++) &#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">access</span>(seapp_contexts_system_ext[i], R_OK) != <span class="number">-1</span>) &#123;</span><br><span class="line">            seapp_contexts_files[files_len++] = seapp_contexts_system_ext[i];</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; <span class="built_in">ARRAY_SIZE</span>(seapp_contexts_product); i++) &#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">access</span>(seapp_contexts_product[i], R_OK) != <span class="number">-1</span>) &#123;</span><br><span class="line">            seapp_contexts_files[files_len++] = seapp_contexts_product[i];</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; <span class="built_in">ARRAY_SIZE</span>(seapp_contexts_vendor); i++) &#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">access</span>(seapp_contexts_vendor[i], R_OK) != <span class="number">-1</span>) &#123;</span><br><span class="line">            seapp_contexts_files[files_len++] = seapp_contexts_vendor[i];</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; <span class="built_in">ARRAY_SIZE</span>(seapp_contexts_odm); i++) &#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">access</span>(seapp_contexts_odm[i], R_OK) != <span class="number">-1</span>) &#123;</span><br><span class="line">            seapp_contexts_files[files_len++] = seapp_contexts_odm[i];</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">free_seapp_contexts</span>();</span><br><span class="line"></span><br><span class="line">    nspec = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; files_len; i++) &#123;</span><br><span class="line">        fp = <span class="built_in">fopen</span>(seapp_contexts_files[i], <span class="string">&quot;re&quot;</span>);</span><br><span class="line">        <span class="keyword">if</span> (!fp) &#123;</span><br><span class="line">            <span class="built_in">selinux_log</span>(SELINUX_ERROR, <span class="string">&quot;%s:  could not open seapp_contexts file: %s&quot;</span>,</span><br><span class="line">                    __FUNCTION__, seapp_contexts_files[i]);</span><br><span class="line">            <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">while</span> (<span class="built_in">fgets</span>(line_buf, <span class="keyword">sizeof</span> line_buf - <span class="number">1</span>, fp)) &#123;</span><br><span class="line">            p = line_buf;</span><br><span class="line">            <span class="keyword">while</span> (<span class="built_in">isspace</span>(*p))</span><br><span class="line">                p++;</span><br><span class="line">            <span class="keyword">if</span> (*p == <span class="string">&#x27;#&#x27;</span> || *p == <span class="number">0</span>)</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            nspec++;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="built_in">fclose</span>(fp);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    seapp_contexts = (struct seapp_context **) <span class="built_in">calloc</span>(nspec, <span class="built_in"><span class="keyword">sizeof</span></span>(struct seapp_context *));</span><br><span class="line">    <span class="keyword">if</span> (!seapp_contexts)</span><br><span class="line">        <span class="keyword">goto</span> oom;</span><br><span class="line"></span><br><span class="line">    nspec = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; files_len; i++) &#123;</span><br><span class="line">        lineno = <span class="number">1</span>;</span><br><span class="line">        fp = <span class="built_in">fopen</span>(seapp_contexts_files[i], <span class="string">&quot;re&quot;</span>);</span><br><span class="line">        <span class="keyword">if</span> (!fp) &#123;</span><br><span class="line">            <span class="built_in">selinux_log</span>(SELINUX_ERROR, <span class="string">&quot;%s:  could not open seapp_contexts file: %s&quot;</span>,</span><br><span class="line">                    __FUNCTION__, seapp_contexts_files[i]);</span><br><span class="line">            <span class="built_in">free_seapp_contexts</span>();</span><br><span class="line">            <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">while</span> (<span class="built_in">fgets</span>(line_buf, <span class="keyword">sizeof</span> line_buf - <span class="number">1</span>, fp)) &#123;</span><br><span class="line">            len = <span class="built_in">strlen</span>(line_buf);</span><br><span class="line">            <span class="keyword">if</span> (len == <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="comment">// line contains a NUL byte as its first entry</span></span><br><span class="line">                <span class="keyword">goto</span> err;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (line_buf[len - <span class="number">1</span>] == <span class="string">&#x27;\n&#x27;</span>)</span><br><span class="line">                line_buf[len - <span class="number">1</span>] = <span class="number">0</span>;</span><br><span class="line">            p = line_buf;</span><br><span class="line">            <span class="keyword">while</span> (<span class="built_in">isspace</span>(*p))</span><br><span class="line">                p++;</span><br><span class="line">            <span class="keyword">if</span> (*p == <span class="string">&#x27;#&#x27;</span> || *p == <span class="number">0</span>)</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line"></span><br><span class="line">            cur = (struct seapp_context *) <span class="built_in">calloc</span>(<span class="number">1</span>, <span class="built_in"><span class="keyword">sizeof</span></span>(struct seapp_context));</span><br><span class="line">            <span class="keyword">if</span> (!cur)</span><br><span class="line">                <span class="keyword">goto</span> oom;</span><br><span class="line"></span><br><span class="line">            token = <span class="built_in">strtok_r</span>(p, <span class="string">&quot; \t&quot;</span>, &amp;saveptr);</span><br><span class="line">            <span class="keyword">if</span> (!token) &#123;</span><br><span class="line">                <span class="built_in">free_seapp_context</span>(cur);</span><br><span class="line">                <span class="keyword">goto</span> err;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">                name = token;</span><br><span class="line">                value = <span class="built_in">strchr</span>(name, <span class="string">&#x27;=&#x27;</span>);</span><br><span class="line">                <span class="keyword">if</span> (!value) &#123;</span><br><span class="line">                    <span class="built_in">free_seapp_context</span>(cur);</span><br><span class="line">                    <span class="keyword">goto</span> err;</span><br><span class="line">                &#125;</span><br><span class="line">                *value++ = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">                <span class="keyword">if</span> (!<span class="built_in">strcasecmp</span>(name, <span class="string">&quot;isSystemServer&quot;</span>)) &#123;</span><br><span class="line">                    <span class="keyword">if</span> (!<span class="built_in">strcasecmp</span>(value, <span class="string">&quot;true&quot;</span>))</span><br><span class="line">                        cur-&gt;isSystemServer = <span class="literal">true</span>;</span><br><span class="line">                    <span class="keyword">else</span> <span class="keyword">if</span> (!<span class="built_in">strcasecmp</span>(value, <span class="string">&quot;false&quot;</span>))</span><br><span class="line">                        cur-&gt;isSystemServer = <span class="literal">false</span>;</span><br><span class="line">                    <span class="keyword">else</span> &#123;</span><br><span class="line">                        <span class="built_in">free_seapp_context</span>(cur);</span><br><span class="line">                        <span class="keyword">goto</span> err;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125; <span class="keyword">else</span> <span class="keyword">if</span> (!<span class="built_in">strcasecmp</span>(name, <span class="string">&quot;isEphemeralApp&quot;</span>)) &#123;</span><br><span class="line">                    cur-&gt;isEphemeralAppSet = <span class="literal">true</span>;</span><br><span class="line">                    <span class="keyword">if</span> (!<span class="built_in">strcasecmp</span>(value, <span class="string">&quot;true&quot;</span>))</span><br><span class="line">                        cur-&gt;isEphemeralApp = <span class="literal">true</span>;</span><br><span class="line">                    <span class="keyword">else</span> <span class="keyword">if</span> (!<span class="built_in">strcasecmp</span>(value, <span class="string">&quot;false&quot;</span>))</span><br><span class="line">                        cur-&gt;isEphemeralApp = <span class="literal">false</span>;</span><br><span class="line">                    <span class="keyword">else</span> &#123;</span><br><span class="line">                        <span class="built_in">free_seapp_context</span>(cur);</span><br><span class="line">                        <span class="keyword">goto</span> err;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125; <span class="keyword">else</span> <span class="keyword">if</span> (!<span class="built_in">strcasecmp</span>(name, <span class="string">&quot;isOwner&quot;</span>)) &#123;</span><br><span class="line">                    cur-&gt;isOwnerSet = <span class="literal">true</span>;</span><br><span class="line">                    <span class="keyword">if</span> (!<span class="built_in">strcasecmp</span>(value, <span class="string">&quot;true&quot;</span>))</span><br><span class="line">                        cur-&gt;isOwner = <span class="literal">true</span>;</span><br><span class="line">                    <span class="keyword">else</span> <span class="keyword">if</span> (!<span class="built_in">strcasecmp</span>(value, <span class="string">&quot;false&quot;</span>))</span><br><span class="line">                        cur-&gt;isOwner = <span class="literal">false</span>;</span><br><span class="line">                    <span class="keyword">else</span> &#123;</span><br><span class="line">                        <span class="built_in">free_seapp_context</span>(cur);</span><br><span class="line">                        <span class="keyword">goto</span> err;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125; <span class="keyword">else</span> <span class="keyword">if</span> (!<span class="built_in">strcasecmp</span>(name, <span class="string">&quot;user&quot;</span>)) &#123;</span><br><span class="line">                    <span class="keyword">if</span> (cur-&gt;user.str) &#123;</span><br><span class="line">                        <span class="built_in">free_seapp_context</span>(cur);</span><br><span class="line">                        <span class="keyword">goto</span> err;</span><br><span class="line">                    &#125;</span><br><span class="line">                    cur-&gt;user.str = <span class="built_in">strdup</span>(value);</span><br><span class="line">                    <span class="keyword">if</span> (!cur-&gt;user.str) &#123;</span><br><span class="line">                        <span class="built_in">free_seapp_context</span>(cur);</span><br><span class="line">                        <span class="keyword">goto</span> oom;</span><br><span class="line">                    &#125;</span><br><span class="line">                    cur-&gt;user.len = <span class="built_in">strlen</span>(cur-&gt;user.str);</span><br><span class="line">                    <span class="keyword">if</span> (cur-&gt;user.str[cur-&gt;user.len<span class="number">-1</span>] == <span class="string">&#x27;*&#x27;</span>)</span><br><span class="line">                        cur-&gt;user.is_prefix = <span class="number">1</span>;</span><br><span class="line">                &#125; <span class="keyword">else</span> <span class="keyword">if</span> (!<span class="built_in">strcasecmp</span>(name, <span class="string">&quot;seinfo&quot;</span>)) &#123;</span><br><span class="line">                    <span class="keyword">if</span> (cur-&gt;seinfo) &#123;</span><br><span class="line">                        <span class="built_in">free_seapp_context</span>(cur);</span><br><span class="line">                        <span class="keyword">goto</span> err;</span><br><span class="line">                    &#125;</span><br><span class="line">                    cur-&gt;seinfo = <span class="built_in">strdup</span>(value);</span><br><span class="line">                    <span class="keyword">if</span> (!cur-&gt;seinfo) &#123;</span><br><span class="line">                        <span class="built_in">free_seapp_context</span>(cur);</span><br><span class="line">                        <span class="keyword">goto</span> oom;</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="keyword">if</span> (<span class="built_in">strstr</span>(value, <span class="string">&quot;:&quot;</span>)) &#123;</span><br><span class="line">                        <span class="built_in">free_seapp_context</span>(cur);</span><br><span class="line">                        <span class="keyword">goto</span> err;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125; <span class="keyword">else</span> <span class="keyword">if</span> (!<span class="built_in">strcasecmp</span>(name, <span class="string">&quot;name&quot;</span>)) &#123;</span><br><span class="line">                    <span class="keyword">if</span> (cur-&gt;name.str) &#123;</span><br><span class="line">                        <span class="built_in">free_seapp_context</span>(cur);</span><br><span class="line">                        <span class="keyword">goto</span> err;</span><br><span class="line">                    &#125;</span><br><span class="line">                    cur-&gt;name.str = <span class="built_in">strdup</span>(value);</span><br><span class="line">                    <span class="keyword">if</span> (!cur-&gt;name.str) &#123;</span><br><span class="line">                        <span class="built_in">free_seapp_context</span>(cur);</span><br><span class="line">                        <span class="keyword">goto</span> oom;</span><br><span class="line">                    &#125;</span><br><span class="line">                    cur-&gt;name.len = <span class="built_in">strlen</span>(cur-&gt;name.str);</span><br><span class="line">                    <span class="keyword">if</span> (cur-&gt;name.str[cur-&gt;name.len<span class="number">-1</span>] == <span class="string">&#x27;*&#x27;</span>)</span><br><span class="line">                        cur-&gt;name.is_prefix = <span class="number">1</span>;</span><br><span class="line">                &#125; <span class="keyword">else</span> <span class="keyword">if</span> (!<span class="built_in">strcasecmp</span>(name, <span class="string">&quot;domain&quot;</span>)) &#123;</span><br><span class="line">                    <span class="keyword">if</span> (cur-&gt;domain) &#123;</span><br><span class="line">                        <span class="built_in">free_seapp_context</span>(cur);</span><br><span class="line">                        <span class="keyword">goto</span> err;</span><br><span class="line">                    &#125;</span><br><span class="line">                    cur-&gt;domain = <span class="built_in">strdup</span>(value);</span><br><span class="line">                    <span class="keyword">if</span> (!cur-&gt;domain) &#123;</span><br><span class="line">                        <span class="built_in">free_seapp_context</span>(cur);</span><br><span class="line">                        <span class="keyword">goto</span> oom;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125; <span class="keyword">else</span> <span class="keyword">if</span> (!<span class="built_in">strcasecmp</span>(name, <span class="string">&quot;type&quot;</span>)) &#123;</span><br><span class="line">                    <span class="keyword">if</span> (cur-&gt;type) &#123;</span><br><span class="line">                        <span class="built_in">free_seapp_context</span>(cur);</span><br><span class="line">                        <span class="keyword">goto</span> err;</span><br><span class="line">                    &#125;</span><br><span class="line">                    cur-&gt;type = <span class="built_in">strdup</span>(value);</span><br><span class="line">                    <span class="keyword">if</span> (!cur-&gt;type) &#123;</span><br><span class="line">                        <span class="built_in">free_seapp_context</span>(cur);</span><br><span class="line">                        <span class="keyword">goto</span> oom;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125; <span class="keyword">else</span> <span class="keyword">if</span> (!<span class="built_in">strcasecmp</span>(name, <span class="string">&quot;levelFromUid&quot;</span>)) &#123;</span><br><span class="line">                    <span class="keyword">if</span> (cur-&gt;levelFrom) &#123;</span><br><span class="line">                        <span class="built_in">free_seapp_context</span>(cur);</span><br><span class="line">                        <span class="keyword">goto</span> err;</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="keyword">if</span> (!<span class="built_in">strcasecmp</span>(value, <span class="string">&quot;true&quot;</span>))</span><br><span class="line">                        cur-&gt;levelFrom = LEVELFROM_APP;</span><br><span class="line">                    <span class="keyword">else</span> <span class="keyword">if</span> (!<span class="built_in">strcasecmp</span>(value, <span class="string">&quot;false&quot;</span>))</span><br><span class="line">                        cur-&gt;levelFrom = LEVELFROM_NONE;</span><br><span class="line">                    <span class="keyword">else</span> &#123;</span><br><span class="line">                        <span class="built_in">free_seapp_context</span>(cur);</span><br><span class="line">                        <span class="keyword">goto</span> err;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125; <span class="keyword">else</span> <span class="keyword">if</span> (!<span class="built_in">strcasecmp</span>(name, <span class="string">&quot;levelFrom&quot;</span>)) &#123;</span><br><span class="line">                    <span class="keyword">if</span> (cur-&gt;levelFrom) &#123;</span><br><span class="line">                        <span class="built_in">free_seapp_context</span>(cur);</span><br><span class="line">                        <span class="keyword">goto</span> err;</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="keyword">if</span> (!<span class="built_in">strcasecmp</span>(value, <span class="string">&quot;none&quot;</span>))</span><br><span class="line">                        cur-&gt;levelFrom = LEVELFROM_NONE;</span><br><span class="line">                    <span class="keyword">else</span> <span class="keyword">if</span> (!<span class="built_in">strcasecmp</span>(value, <span class="string">&quot;app&quot;</span>))</span><br><span class="line">                        cur-&gt;levelFrom = LEVELFROM_APP;</span><br><span class="line">                    <span class="keyword">else</span> <span class="keyword">if</span> (!<span class="built_in">strcasecmp</span>(value, <span class="string">&quot;user&quot;</span>))</span><br><span class="line">                        cur-&gt;levelFrom = LEVELFROM_USER;</span><br><span class="line">                    <span class="keyword">else</span> <span class="keyword">if</span> (!<span class="built_in">strcasecmp</span>(value, <span class="string">&quot;all&quot;</span>))</span><br><span class="line">                        cur-&gt;levelFrom = LEVELFROM_ALL;</span><br><span class="line">                    <span class="keyword">else</span> &#123;</span><br><span class="line">                        <span class="built_in">free_seapp_context</span>(cur);</span><br><span class="line">                        <span class="keyword">goto</span> err;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125; <span class="keyword">else</span> <span class="keyword">if</span> (!<span class="built_in">strcasecmp</span>(name, <span class="string">&quot;level&quot;</span>)) &#123;</span><br><span class="line">                    <span class="keyword">if</span> (cur-&gt;level) &#123;</span><br><span class="line">                        <span class="built_in">free_seapp_context</span>(cur);</span><br><span class="line">                        <span class="keyword">goto</span> err;</span><br><span class="line">                    &#125;</span><br><span class="line">                    cur-&gt;level = <span class="built_in">strdup</span>(value);</span><br><span class="line">                    <span class="keyword">if</span> (!cur-&gt;level) &#123;</span><br><span class="line">                        <span class="built_in">free_seapp_context</span>(cur);</span><br><span class="line">                        <span class="keyword">goto</span> oom;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125; <span class="keyword">else</span> <span class="keyword">if</span> (!<span class="built_in">strcasecmp</span>(name, <span class="string">&quot;path&quot;</span>)) &#123;</span><br><span class="line">                    <span class="keyword">if</span> (cur-&gt;path.str) &#123;</span><br><span class="line">                        <span class="built_in">free_seapp_context</span>(cur);</span><br><span class="line">                        <span class="keyword">goto</span> err;</span><br><span class="line">                    &#125;</span><br><span class="line">                    cur-&gt;path.str = <span class="built_in">strdup</span>(value);</span><br><span class="line">                    <span class="keyword">if</span> (!cur-&gt;path.str) &#123;</span><br><span class="line">                        <span class="built_in">free_seapp_context</span>(cur);</span><br><span class="line">                    <span class="keyword">goto</span> oom;</span><br><span class="line">                    &#125;</span><br><span class="line">                    cur-&gt;path.len = <span class="built_in">strlen</span>(cur-&gt;path.str);</span><br><span class="line">                    <span class="keyword">if</span> (cur-&gt;path.str[cur-&gt;path.len<span class="number">-1</span>] == <span class="string">&#x27;*&#x27;</span>)</span><br><span class="line">                        cur-&gt;path.is_prefix = <span class="number">1</span>;</span><br><span class="line">                &#125; <span class="keyword">else</span> <span class="keyword">if</span> (!<span class="built_in">strcasecmp</span>(name, <span class="string">&quot;isPrivApp&quot;</span>)) &#123;</span><br><span class="line">                    cur-&gt;isPrivAppSet = <span class="literal">true</span>;</span><br><span class="line">                    <span class="keyword">if</span> (!<span class="built_in">strcasecmp</span>(value, <span class="string">&quot;true&quot;</span>))</span><br><span class="line">                        cur-&gt;isPrivApp = <span class="literal">true</span>;</span><br><span class="line">                    <span class="keyword">else</span> <span class="keyword">if</span> (!<span class="built_in">strcasecmp</span>(value, <span class="string">&quot;false&quot;</span>))</span><br><span class="line">                        cur-&gt;isPrivApp = <span class="literal">false</span>;</span><br><span class="line">                    <span class="keyword">else</span> &#123;</span><br><span class="line">                        <span class="built_in">free_seapp_context</span>(cur);</span><br><span class="line">                        <span class="keyword">goto</span> err;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125; <span class="keyword">else</span> <span class="keyword">if</span> (!<span class="built_in">strcasecmp</span>(name, <span class="string">&quot;minTargetSdkVersion&quot;</span>)) &#123;</span><br><span class="line">                    cur-&gt;minTargetSdkVersion = <span class="built_in">get_minTargetSdkVersion</span>(value);</span><br><span class="line">                    <span class="keyword">if</span> (cur-&gt;minTargetSdkVersion &lt; <span class="number">0</span>) &#123;</span><br><span class="line">                        <span class="built_in">free_seapp_context</span>(cur);</span><br><span class="line">                        <span class="keyword">goto</span> err;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125; <span class="keyword">else</span> <span class="keyword">if</span> (!<span class="built_in">strcasecmp</span>(name, <span class="string">&quot;fromRunAs&quot;</span>)) &#123;</span><br><span class="line">                    <span class="keyword">if</span> (!<span class="built_in">strcasecmp</span>(value, <span class="string">&quot;true&quot;</span>))</span><br><span class="line">                        cur-&gt;fromRunAs = <span class="literal">true</span>;</span><br><span class="line">                    <span class="keyword">else</span> <span class="keyword">if</span> (!<span class="built_in">strcasecmp</span>(value, <span class="string">&quot;false&quot;</span>))</span><br><span class="line">                        cur-&gt;fromRunAs = <span class="literal">false</span>;</span><br><span class="line">                    <span class="keyword">else</span> &#123;</span><br><span class="line">                        <span class="built_in">free_seapp_context</span>(cur);</span><br><span class="line">                        <span class="keyword">goto</span> err;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="built_in">free_seapp_context</span>(cur);</span><br><span class="line">                    <span class="keyword">goto</span> err;</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                token = <span class="built_in">strtok_r</span>(<span class="literal">NULL</span>, <span class="string">&quot; \t&quot;</span>, &amp;saveptr);</span><br><span class="line">                <span class="keyword">if</span> (!token)</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (!cur-&gt;isPrivApp &amp;&amp; cur-&gt;name.str &amp;&amp;</span><br><span class="line">                (!cur-&gt;seinfo || !<span class="built_in">strcmp</span>(cur-&gt;seinfo, <span class="string">&quot;default&quot;</span>))) &#123;</span><br><span class="line">                <span class="built_in">selinux_log</span>(SELINUX_ERROR, <span class="string">&quot;%s:  No specific seinfo value specified with name=\&quot;%s\&quot;, on line %u:  insecure configuration!\n&quot;</span>,</span><br><span class="line">                        seapp_contexts_files[i], cur-&gt;name.str, lineno);</span><br><span class="line">                <span class="built_in">free_seapp_context</span>(cur);</span><br><span class="line">                <span class="keyword">goto</span> err;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            seapp_contexts[nspec] = cur;</span><br><span class="line">            nspec++;</span><br><span class="line">            lineno++;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="built_in">fclose</span>(fp);</span><br><span class="line">        fp = <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">qsort</span>(seapp_contexts, nspec, <span class="built_in"><span class="keyword">sizeof</span></span>(struct seapp_context *),</span><br><span class="line">          seapp_context_cmp);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (seapp_contexts_dup)</span><br><span class="line">        <span class="keyword">goto</span> err_no_log;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">if</span> DEBUG</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">int</span> i;</span><br><span class="line">        <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; nspec; i++) &#123;</span><br><span class="line">            cur = seapp_contexts[i];</span><br><span class="line">            <span class="built_in">selinux_log</span>(SELINUX_INFO, <span class="string">&quot;%s:  isSystemServer=%s  isEphemeralApp=%s isOwner=%s user=%s seinfo=%s &quot;</span></span><br><span class="line">                    <span class="string">&quot;name=%s path=%s isPrivApp=%s minTargetSdkVersion=%d fromRunAs=%s -&gt; domain=%s type=%s level=%s levelFrom=%s&quot;</span>,</span><br><span class="line">                __FUNCTION__,</span><br><span class="line">                cur-&gt;isSystemServer ? <span class="string">&quot;true&quot;</span> : <span class="string">&quot;false&quot;</span>,</span><br><span class="line">                cur-&gt;isEphemeralAppSet ? (cur-&gt;isEphemeralApp ? <span class="string">&quot;true&quot;</span> : <span class="string">&quot;false&quot;</span>) : <span class="string">&quot;null&quot;</span>,</span><br><span class="line">                cur-&gt;isOwnerSet ? (cur-&gt;isOwner ? <span class="string">&quot;true&quot;</span> : <span class="string">&quot;false&quot;</span>) : <span class="string">&quot;null&quot;</span>,</span><br><span class="line">                cur-&gt;user.str,</span><br><span class="line">                cur-&gt;seinfo, cur-&gt;name.str, cur-&gt;path.str,</span><br><span class="line">                cur-&gt;isPrivAppSet ? (cur-&gt;isPrivApp ? <span class="string">&quot;true&quot;</span> : <span class="string">&quot;false&quot;</span>) : <span class="string">&quot;null&quot;</span>,</span><br><span class="line">                cur-&gt;minTargetSdkVersion,</span><br><span class="line">                cur-&gt;fromRunAs ? <span class="string">&quot;true&quot;</span> : <span class="string">&quot;false&quot;</span>,</span><br><span class="line">                cur-&gt;domain, cur-&gt;type, cur-&gt;level,</span><br><span class="line">                levelFromName[cur-&gt;levelFrom]);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br><span class="line"></span><br><span class="line">    ret = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">out:</span><br><span class="line">    <span class="keyword">if</span> (fp) &#123;</span><br><span class="line">        <span class="built_in">fclose</span>(fp);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> ret;</span><br><span class="line"></span><br><span class="line">err:</span><br><span class="line">    <span class="built_in">selinux_log</span>(SELINUX_ERROR, <span class="string">&quot;%s:  Invalid entry on line %u\n&quot;</span>,</span><br><span class="line">            seapp_contexts_files[i], lineno);</span><br><span class="line">err_no_log:</span><br><span class="line">    <span class="built_in">free_seapp_contexts</span>();</span><br><span class="line">    ret = <span class="number">-1</span>;</span><br><span class="line">    <span class="keyword">goto</span> out;</span><br><span class="line">oom:</span><br><span class="line">    <span class="built_in">selinux_log</span>(SELINUX_ERROR,</span><br><span class="line">            <span class="string">&quot;%s:  Out of memory\n&quot;</span>, __FUNCTION__);</span><br><span class="line">    <span class="built_in">free_seapp_contexts</span>();</span><br><span class="line">    ret = <span class="number">-1</span>;</span><br><span class="line">    <span class="keyword">goto</span> out;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这个函数代码很长，也很复杂。因为这流程是加载se，所以我们只关心相关代码，也就是从以下数组中收集文件并汇总到seapp_contexts_files数组，最后通过fopen打开并作相应的处理。</p>
<ul>
<li>seapp_contexts_plat<ul>
<li>“/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/system/&project=external">system</a>/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/system/etc/&project=external">etc</a>/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/system/etc/selinux/&project=external">selinux</a>/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/system/etc/selinux/plat_seapp_contexts&project=external">plat_seapp_contexts</a>“</li>
<li>“/plat_seapp_contexts”</li>
</ul>
</li>
<li>seapp_contexts_system_ext<ul>
<li>“/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/system_ext/&project=external">system_ext</a>/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/system_ext/etc/&project=external">etc</a>/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/system_ext/etc/selinux/&project=external">selinux</a>/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/system_ext/etc/selinux/system_ext_seapp_contexts&project=external">system_ext_seapp_contexts</a>“</li>
<li>“/system_ext_seapp_contexts”</li>
</ul>
</li>
<li>seapp_contexts_product<ul>
<li>“/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/product/&project=external">product</a>/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/product/etc/&project=external">etc</a>/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/product/etc/selinux/&project=external">selinux</a>/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/product/etc/selinux/product_seapp_contexts&project=external">product_seapp_contexts</a>“</li>
<li>“/product_seapp_contexts”</li>
</ul>
</li>
<li>seapp_contexts_vendor<ul>
<li>“/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/vendor/&project=external">vendor</a>/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/vendor/etc/&project=external">etc</a>/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/vendor/etc/selinux/&project=external">selinux</a>/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/vendor/etc/selinux/vendor_seapp_contexts&project=external">vendor_seapp_contexts</a>“</li>
<li>“/vendor_seapp_contexts”</li>
<li>“/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/vendor/&project=external">vendor</a>/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/vendor/etc/&project=external">etc</a>/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/vendor/etc/selinux/&project=external">selinux</a>/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/vendor/etc/selinux/nonplat_seapp_contexts&project=external">nonplat_seapp_contexts</a>“</li>
<li>“/nonplat_seapp_contexts” </li>
</ul>
</li>
<li>seapp_contexts_odm<ul>
<li>“/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/odm/&project=external">odm</a>/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/odm/etc/&project=external">etc</a>/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/odm/etc/selinux/&project=external">selinux</a>/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/odm/etc/selinux/odm_seapp_contexts&project=external">odm_seapp_contexts</a>“,</li>
<li>“/odm_seapp_contexts”</li>
</ul>
</li>
</ul>
<h3 id="疑问"><a href="#疑问" class="headerlink" title="疑问"></a>疑问</h3><p>可以看到，<code>seinfo</code> 是贯穿Java到Native的一个重要线索，Native的seinfo是从一堆的 <code>xxx_seapp_contests</code> 中获取的，那Java层的<code>seinfo</code>是来自何处呢？</p>
<h2 id="Java-seinfo"><a href="#Java-seinfo" class="headerlink" title="Java seinfo"></a>Java seinfo</h2><p>在PackageManagerService扫描apk文件时赋值给各个applicationInfo对象的：</p>
<ul>
<li>PMS在扫描apk时将seinfo赋值给pkg信息对象，其seinfo通过调用 <code>SELinuxMMAC.getSeinfo()</code> 获取。</li>
<li>SELinuxMMAC中的seinfo是在PMS初始化时从各种 <code>/etc/selinux/???_mac_permissions.xml</code> 文件中获取的。<blockquote>
<p> java部分的我们就不做详细分析，只列出调用流程。</p>
</blockquote>
</li>
</ul>
<h3 id="processInstallRequestsAsync"><a href="#processInstallRequestsAsync" class="headerlink" title="processInstallRequestsAsync"></a>processInstallRequestsAsync</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/pm/PackageManagerService.java</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Queue up an async operation since the package installation may take a little while.</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">processInstallRequestsAsync</span><span class="params">(<span class="keyword">boolean</span> success,</span></span></span><br><span class="line"><span class="params"><span class="function">                                         List&lt;InstallRequest&gt; installRequests)</span> </span>&#123;</span><br><span class="line">    ...</span><br><span class="line">        <span class="keyword">synchronized</span> (mInstallLock) &#123;</span><br><span class="line">        installPackagesTracedLI(apkInstallRequests);</span><br><span class="line">    &#125;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="installPackagesTracedLI"><a href="#installPackagesTracedLI" class="headerlink" title="installPackagesTracedLI"></a>installPackagesTracedLI</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/pm/PackageManagerService.java</span></span><br><span class="line"></span><br><span class="line"><span class="meta">@GuardedBy(&quot;mInstallLock&quot;)</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">installPackagesTracedLI</span><span class="params">(List&lt;InstallRequest&gt; requests)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        Trace.traceBegin(TRACE_TAG_PACKAGE_MANAGER, <span class="string">&quot;installPackages&quot;</span>);</span><br><span class="line">        installPackagesLI(requests);</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        Trace.traceEnd(TRACE_TAG_PACKAGE_MANAGER);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="installPackagesLI"><a href="#installPackagesLI" class="headerlink" title="installPackagesLI"></a>installPackagesLI</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/pm/PackageManagerService.java</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Installs one or more packages atomically. This operation is broken up into four phases:</span></span><br><span class="line"><span class="comment"> * &lt;ul&gt;</span></span><br><span class="line"><span class="comment"> *     &lt;li&gt;&lt;b&gt;Prepare&lt;/b&gt;</span></span><br><span class="line"><span class="comment"> *         &lt;br/&gt;Analyzes any current install state, parses the package and does initial</span></span><br><span class="line"><span class="comment"> *         validation on it.&lt;/li&gt;</span></span><br><span class="line"><span class="comment"> *     &lt;li&gt;&lt;b&gt;Scan&lt;/b&gt;</span></span><br><span class="line"><span class="comment"> *         &lt;br/&gt;Interrogates the parsed packages given the context collected in prepare.&lt;/li&gt;</span></span><br><span class="line"><span class="comment"> *     &lt;li&gt;&lt;b&gt;Reconcile&lt;/b&gt;</span></span><br><span class="line"><span class="comment"> *         &lt;br/&gt;Validates scanned packages in the context of each other and the current system</span></span><br><span class="line"><span class="comment"> *         state to ensure that the install will be successful.</span></span><br><span class="line"><span class="comment"> *     &lt;li&gt;&lt;b&gt;Commit&lt;/b&gt;</span></span><br><span class="line"><span class="comment"> *         &lt;br/&gt;Commits all scanned packages and updates system state. This is the only place</span></span><br><span class="line"><span class="comment"> *         that system state may be modified in the install flow and all predictable errors</span></span><br><span class="line"><span class="comment"> *         must be determined before this phase.&lt;/li&gt;</span></span><br><span class="line"><span class="comment"> * &lt;/ul&gt;</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * Failure at any phase will result in a full failure to install all packages.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@GuardedBy(&quot;mInstallLock&quot;)</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">installPackagesLI</span><span class="params">(List&lt;InstallRequest&gt; requests)</span> </span>&#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">final</span> ScanResult result = scanPackageTracedLI(</span><br><span class="line">        prepareResult.packageToScan, prepareResult.parseFlags,</span><br><span class="line">        prepareResult.scanFlags, System.currentTimeMillis(),</span><br><span class="line">        request.args.user, request.args.abiOverride);</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="scanPackageTracedLI"><a href="#scanPackageTracedLI" class="headerlink" title="scanPackageTracedLI"></a>scanPackageTracedLI</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/pm/PackageManagerService.java</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> *  Traces a package scan.</span></span><br><span class="line"><span class="comment"> *  <span class="doctag">@see</span> #scanPackageLI(File, int, int, long, UserHandle)</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@GuardedBy(&#123;&quot;mInstallLock&quot;, &quot;mLock&quot;&#125;)</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> AndroidPackage <span class="title">scanPackageTracedLI</span><span class="params">(File scanFile, <span class="keyword">final</span> <span class="keyword">int</span> parseFlags,</span></span></span><br><span class="line"><span class="params"><span class="function">        <span class="keyword">int</span> scanFlags, <span class="keyword">long</span> currentTime, UserHandle user)</span> <span class="keyword">throws</span> PackageManagerException </span>&#123;</span><br><span class="line">    Trace.traceBegin(TRACE_TAG_PACKAGE_MANAGER, <span class="string">&quot;scanPackage [&quot;</span> + scanFile.toString() + <span class="string">&quot;]&quot;</span>);</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> scanPackageLI(scanFile, parseFlags, scanFlags, currentTime, user);</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        Trace.traceEnd(TRACE_TAG_PACKAGE_MANAGER);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="scanPackageLI"><a href="#scanPackageLI" class="headerlink" title="scanPackageLI"></a>scanPackageLI</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/pm/PackageManagerService.java</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> *  Scans a package and returns the newly parsed package.</span></span><br><span class="line"><span class="comment"> *  Returns &#123;<span class="doctag">@code</span> null&#125; in case of errors and the error code is stored in mLastScanError</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@GuardedBy(&#123;&quot;mInstallLock&quot;, &quot;mLock&quot;&#125;)</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> AndroidPackage <span class="title">scanPackageLI</span><span class="params">(File scanFile, <span class="keyword">int</span> parseFlags, <span class="keyword">int</span> scanFlags,</span></span></span><br><span class="line"><span class="params"><span class="function">        <span class="keyword">long</span> currentTime, UserHandle user)</span> <span class="keyword">throws</span> PackageManagerException </span>&#123;</span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> addForInitLI(parsedPackage, parseFlags, scanFlags, currentTime, user);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="addForInitLI"><a href="#addForInitLI" class="headerlink" title="addForInitLI"></a>addForInitLI</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/pm/PackageManagerService.java</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Adds a new package to the internal data structures during platform initialization.</span></span><br><span class="line"><span class="comment"> * &lt;p&gt;After adding, the package is known to the system and available for querying.</span></span><br><span class="line"><span class="comment"> * &lt;p&gt;For packages located on the device ROM [eg. packages located in /system, /vendor,</span></span><br><span class="line"><span class="comment"> * etc...], additional checks are performed. Basic verification [such as ensuring</span></span><br><span class="line"><span class="comment"> * matching signatures, checking version codes, etc...] occurs if the package is</span></span><br><span class="line"><span class="comment"> * identical to a previously known package. If the package fails a signature check,</span></span><br><span class="line"><span class="comment"> * the version installed on /data will be removed. If the version of the new package</span></span><br><span class="line"><span class="comment"> * is less than or equal than the version on /data, it will be ignored.</span></span><br><span class="line"><span class="comment"> * &lt;p&gt;Regardless of the package location, the results are applied to the internal</span></span><br><span class="line"><span class="comment"> * structures and the package is made available to the rest of the system.</span></span><br><span class="line"><span class="comment"> * &lt;p&gt;<span class="doctag">NOTE:</span> The return value should be removed. It&#x27;s the passed in package object.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@GuardedBy(&#123;&quot;mInstallLock&quot;, &quot;mLock&quot;&#125;)</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> AndroidPackage <span class="title">addForInitLI</span><span class="params">(ParsedPackage parsedPackage,</span></span></span><br><span class="line"><span class="params"><span class="function">        <span class="meta">@ParseFlags</span> <span class="keyword">int</span> parseFlags, <span class="meta">@ScanFlags</span> <span class="keyword">int</span> scanFlags, <span class="keyword">long</span> currentTime,</span></span></span><br><span class="line"><span class="params"><span class="function">        <span class="meta">@Nullable</span> UserHandle user)</span></span></span><br><span class="line"><span class="function">                <span class="keyword">throws</span> PackageManagerException </span>&#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">synchronized</span> (mLock) &#123;</span><br><span class="line">        ...</span><br><span class="line">        <span class="keyword">final</span> ScanResult scanResult = scanPackageOnlyLI(request, mInjector, mFactoryTest, -<span class="number">1L</span>);</span><br><span class="line">        ...</span><br><span class="line">    &#125;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="scanPackageOnlyLI"><a href="#scanPackageOnlyLI" class="headerlink" title="scanPackageOnlyLI"></a>scanPackageOnlyLI</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/pm/PackageManagerService.java</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Just scans the package without any side effects.</span></span><br><span class="line"><span class="comment"> * &lt;p&gt;Not entirely true at the moment. There is still one side effect -- this</span></span><br><span class="line"><span class="comment"> * method potentially modifies a live &#123;<span class="doctag">@link</span> PackageSetting&#125; object representing</span></span><br><span class="line"><span class="comment"> * the package being scanned. This will be resolved in the future.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> injector injector for acquiring dependencies</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> request Information about the package to be scanned</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> isUnderFactoryTest Whether or not the device is under factory test</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> currentTime The current time, in millis</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@return</span> The results of the scan</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@GuardedBy(&quot;mInstallLock&quot;)</span></span><br><span class="line"><span class="meta">@VisibleForTesting</span></span><br><span class="line"><span class="meta">@NonNull</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> ScanResult <span class="title">scanPackageOnlyLI</span><span class="params">(<span class="meta">@NonNull</span> ScanRequest request,</span></span></span><br><span class="line"><span class="params"><span class="function">        Injector injector,</span></span></span><br><span class="line"><span class="params"><span class="function">        <span class="keyword">boolean</span> isUnderFactoryTest, <span class="keyword">long</span> currentTime)</span></span></span><br><span class="line"><span class="function">        <span class="keyword">throws</span> PackageManagerException </span>&#123;</span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    parsedPackage</span><br><span class="line">            .setSeInfo(SELinuxMMAC.getSeInfo(parsedPackage, sharedUserSetting,</span><br><span class="line">                    injector.getCompatibility()))</span><br><span class="line">            .setSeInfoUser(SELinuxUtil.assignSeinfoUser(pkgSetting.readUserState(</span><br><span class="line">                    userId == UserHandle.USER_ALL ? UserHandle.USER_SYSTEM : userId)));</span><br><span class="line"></span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="getSeInfo"><a href="#getSeInfo" class="headerlink" title="getSeInfo"></a>getSeInfo</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/pm/SELinuxMMAC.java</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">     * Selects a security label to a package based on input parameters and the seinfo tag taken</span></span><br><span class="line"><span class="comment">     * from a matched policy. All signature based policy stanzas are consulted and, if no match</span></span><br><span class="line"><span class="comment">     * is found, the default seinfo label of &#x27;default&#x27; is used. The security label is attached to</span></span><br><span class="line"><span class="comment">     * the ApplicationInfo instance of the package.</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> pkg               object representing the package to be labeled.</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> sharedUserSetting if the app shares a sharedUserId, then this has the shared setting.</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> compatibility     the PlatformCompat service to ask about state of compat changes.</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> String representing the resulting seinfo.</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> String <span class="title">getSeInfo</span><span class="params">(AndroidPackage pkg, SharedUserSetting sharedUserSetting,</span></span></span><br><span class="line"><span class="params"><span class="function">                               PlatformCompat compatibility)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">final</span> <span class="keyword">int</span> targetSdkVersion = getTargetSdkVersionForSeInfo(pkg, sharedUserSetting,</span><br><span class="line">                                                              compatibility);</span><br><span class="line">    <span class="comment">// TODO(b/71593002): isPrivileged for sharedUser and appInfo should never be out of sync.</span></span><br><span class="line">    <span class="comment">// They currently can be if the sharedUser apps are signed with the platform key.</span></span><br><span class="line">    <span class="keyword">final</span> <span class="keyword">boolean</span> isPrivileged = (sharedUserSetting != <span class="keyword">null</span>)</span><br><span class="line">        ? sharedUserSetting.isPrivileged() | pkg.isPrivileged() : pkg.isPrivileged();</span><br><span class="line">    <span class="keyword">return</span> getSeInfo(pkg, isPrivileged, targetSdkVersion);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Selects a security label to a package based on input parameters and the seinfo tag taken</span></span><br><span class="line"><span class="comment"> * from a matched policy. All signature based policy stanzas are consulted and, if no match</span></span><br><span class="line"><span class="comment"> * is found, the default seinfo label of &#x27;default&#x27; is used. The security label is attached to</span></span><br><span class="line"><span class="comment"> * the ApplicationInfo instance of the package.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> pkg object representing the package to be labeled.</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> isPrivileged boolean.</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> targetSdkVersion int. If this pkg runs as a sharedUser, targetSdkVersion is the</span></span><br><span class="line"><span class="comment"> *        greater of: lowest targetSdk for all pkgs in the sharedUser, or</span></span><br><span class="line"><span class="comment"> *        MINIMUM_TARGETSDKVERSION.</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@return</span> String representing the resulting seinfo.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> String <span class="title">getSeInfo</span><span class="params">(AndroidPackage pkg, <span class="keyword">boolean</span> isPrivileged,</span></span></span><br><span class="line"><span class="params"><span class="function">        <span class="keyword">int</span> targetSdkVersion)</span> </span>&#123;</span><br><span class="line">    String seInfo = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">synchronized</span> (sPolicies) &#123;</span><br><span class="line">        <span class="keyword">if</span> (!sPolicyRead) &#123;</span><br><span class="line">            <span class="keyword">if</span> (DEBUG_POLICY) &#123;</span><br><span class="line">                Slog.d(TAG, <span class="string">&quot;Policy not read&quot;</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">for</span> (Policy policy : sPolicies) &#123;</span><br><span class="line">                seInfo = policy.getMatchedSeInfo(pkg);</span><br><span class="line">                <span class="keyword">if</span> (seInfo != <span class="keyword">null</span>) &#123;</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (seInfo == <span class="keyword">null</span>) &#123;</span><br><span class="line">        seInfo = DEFAULT_SEINFO;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (isPrivileged) &#123;</span><br><span class="line">        seInfo += PRIVILEGED_APP_STR;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    seInfo += TARGETSDKVERSION_STR + targetSdkVersion;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (DEBUG_POLICY_INSTALL) &#123;</span><br><span class="line">        Slog.i(TAG, <span class="string">&quot;package (&quot;</span> + pkg.getPackageName() + <span class="string">&quot;) labeled with &quot;</span></span><br><span class="line">                + <span class="string">&quot;seinfo=&quot;</span> + seInfo);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> seInfo;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h3 id="xxx-mac-permissions-xml"><a href="#xxx-mac-permissions-xml" class="headerlink" title="xxx_mac_permissions.xml"></a>xxx_mac_permissions.xml</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/pm/SELinuxMMAC.java</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Only initialize sMacPermissions once.</span></span><br><span class="line"><span class="keyword">static</span> &#123;</span><br><span class="line">    <span class="comment">// Platform mac permissions.</span></span><br><span class="line">    sMacPermissions.add(<span class="keyword">new</span> File(</span><br><span class="line">        Environment.getRootDirectory(), <span class="string">&quot;/etc/selinux/plat_mac_permissions.xml&quot;</span>));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// SystemExt mac permissions (optional).</span></span><br><span class="line">    <span class="keyword">final</span> File systemExtMacPermission = <span class="keyword">new</span> File(</span><br><span class="line">            Environment.getSystemExtDirectory(), <span class="string">&quot;/etc/selinux/system_ext_mac_permissions.xml&quot;</span>);</span><br><span class="line">    <span class="keyword">if</span> (systemExtMacPermission.exists()) &#123;</span><br><span class="line">        sMacPermissions.add(systemExtMacPermission);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Product mac permissions (optional).</span></span><br><span class="line">    <span class="keyword">final</span> File productMacPermission = <span class="keyword">new</span> File(</span><br><span class="line">            Environment.getProductDirectory(), <span class="string">&quot;/etc/selinux/product_mac_permissions.xml&quot;</span>);</span><br><span class="line">    <span class="keyword">if</span> (productMacPermission.exists()) &#123;</span><br><span class="line">        sMacPermissions.add(productMacPermission);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Vendor mac permissions.</span></span><br><span class="line">    <span class="comment">// The filename has been renamed from nonplat_mac_permissions to</span></span><br><span class="line">    <span class="comment">// vendor_mac_permissions. Either of them should exist.</span></span><br><span class="line">    <span class="keyword">final</span> File vendorMacPermission = <span class="keyword">new</span> File(</span><br><span class="line">        Environment.getVendorDirectory(), <span class="string">&quot;/etc/selinux/vendor_mac_permissions.xml&quot;</span>);</span><br><span class="line">    <span class="keyword">if</span> (vendorMacPermission.exists()) &#123;</span><br><span class="line">        sMacPermissions.add(vendorMacPermission);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="comment">// For backward compatibility.</span></span><br><span class="line">        sMacPermissions.add(<span class="keyword">new</span> File(Environment.getVendorDirectory(),</span><br><span class="line">                                     <span class="string">&quot;/etc/selinux/nonplat_mac_permissions.xml&quot;</span>));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// ODM mac permissions (optional).</span></span><br><span class="line">    <span class="keyword">final</span> File odmMacPermission = <span class="keyword">new</span> File(</span><br><span class="line">        Environment.getOdmDirectory(), <span class="string">&quot;/etc/selinux/odm_mac_permissions.xml&quot;</span>);</span><br><span class="line">    <span class="keyword">if</span> (odmMacPermission.exists()) &#123;</span><br><span class="line">        sMacPermissions.add(odmMacPermission);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">boolean</span> <span class="title">readInstallPolicy</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">final</span> <span class="keyword">int</span> count = sMacPermissions.size();</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; count; ++i) &#123;</span><br><span class="line">        <span class="keyword">final</span> File macPermission = sMacPermissions.get(i);</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            policyFile = <span class="keyword">new</span> FileReader(macPermission);</span><br><span class="line">            Slog.d(TAG, <span class="string">&quot;Using policy file &quot;</span> + macPermission);</span><br><span class="line"></span><br><span class="line">            parser.setInput(policyFile);</span><br><span class="line">            ...</span><br><span class="line"></span><br><span class="line">            policies.add(readSignerOrThrow(parser));</span><br><span class="line">        &#125;</span><br><span class="line">        ...</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">synchronized</span> (sPolicies) &#123;</span><br><span class="line">        sPolicies.clear();</span><br><span class="line">        sPolicies.addAll(policies);</span><br><span class="line">        </span><br><span class="line">        ...</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>“/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/etc/&project=frameworks">etc</a>/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/etc/selinux/&project=frameworks">selinux</a>/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/etc/selinux/plat_mac_permissions.xml&project=frameworks">plat_mac_permissions.xml</a>“</li>
<li>“/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/etc/&project=frameworks">etc</a>/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/etc/selinux/&project=frameworks">selinux</a>/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/etc/selinux/system_ext_mac_permissions.xml&project=frameworks">system_ext_mac_permissions.xml</a>“</li>
<li>“/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/etc/&project=frameworks">etc</a>/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/etc/selinux/&project=frameworks">selinux</a>/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/etc/selinux/product_mac_permissions.xml&project=frameworks">product_mac_permissions.xml</a>“</li>
<li>“/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/etc/&project=frameworks">etc</a>/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/etc/selinux/&project=frameworks">selinux</a>/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/etc/selinux/vendor_mac_permissions.xml&project=frameworks">vendor_mac_permissions.xml</a>“</li>
<li>“/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/etc/&project=frameworks">etc</a>/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/etc/selinux/&project=frameworks">selinux</a>/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/etc/selinux/nonplat_mac_permissions.xml&project=frameworks">nonplat_mac_permissions.xml</a>“</li>
<li>“/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/etc/&project=frameworks">etc</a>/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/etc/selinux/&project=frameworks">selinux</a>/<a href="http://aospxref.com/android-12.0.0_r3/s?path=/etc/selinux/odm_mac_permissions.xml&project=frameworks">odm_mac_permissions.xml</a>“</li>
</ul>
<p>比如： <a href="http://aospxref.com/android-12.0.0_r3/xref/system/sepolicy/private/mac_permissions.xml">system/sepolicy/private/mac_permissions.xml</a> 的内容如下：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=&quot;1.0&quot; encoding=&quot;utf-8&quot;?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">policy</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- Platform dev key in AOSP --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">signer</span> <span class="attr">signature</span>=<span class="string">&quot;@PLATFORM&quot;</span> &gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">seinfo</span> <span class="attr">value</span>=<span class="string">&quot;platform&quot;</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">signer</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!-- Media key in AOSP --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">signer</span> <span class="attr">signature</span>=<span class="string">&quot;@MEDIA&quot;</span> &gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">seinfo</span> <span class="attr">value</span>=<span class="string">&quot;media&quot;</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">signer</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">signer</span> <span class="attr">signature</span>=<span class="string">&quot;@NETWORK_STACK&quot;</span> &gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">seinfo</span> <span class="attr">value</span>=<span class="string">&quot;network_stack&quot;</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">signer</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">policy</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>至此，简要的说明了一个app进程在启动时被贴SE标签的过程。</p>
<h1 id="编译验证"><a href="#编译验证" class="headerlink" title="编译验证"></a>编译验证</h1><p>从上一小节可知，系统会实时编译 CIL 文件，所以编译验证按如下步骤：</p>
<ul>
<li><p>删除 <code>/odm/etc/selinux/precompiled_sepolicy</code> 文件</p>
</li>
<li><p>编译生成 <code>&quot;/system/etc/selinux/plat_sepolicy.cil&quot;</code> 或 <code>&quot;/vendor/etc/selinux/vendor_sepolicy.cil&quot;</code></p>
</li>
<li><p>将编译生成的 <code>.cil</code> 文件push到手机</p>
</li>
<li><p>重启手机</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> time prebuilts/build-tools/linux-x86/bin/ninja -f out/combined-lemonadep.ninja -j64 plat_sepolicy.cil</span></span><br><span class="line">[2/2] Install: out/target/product/lemonadep/system/etc/selinux/plat_sepolicy.cil</span><br><span class="line">real    0m0.932s</span><br><span class="line">user    0m17.872s</span><br><span class="line">sys 0m1.028s</span><br><span class="line"> </span><br><span class="line">adb push /out/target/product/lemonadep/system/etc/selinux/plat_sepolicy.cil /system/etc/selinux/plat_sepolicy.cil</span><br><span class="line"> </span><br><span class="line">adb reboot</span><br></pre></td></tr></table></figure>
<h1 id="辅助工具"><a href="#辅助工具" class="headerlink" title="辅助工具"></a>辅助工具</h1><p>Android平台上提供了一些辅助工具，以便可以更好的来管理和调试SE规则：</p>
</li>
<li><p><strong>chcon</strong>: 变更文件的SE标签。</p>
</li>
<li><p><strong>getenforce/setenforce</strong>: 获取/设置当前环境的 SELinux 模式。</p>
</li>
<li><p><strong>ls -Z</strong>: 查看文件的SE标签。</p>
</li>
<li><p><strong>ps -Z</strong>: 查看进程的SE标签。</p>
</li>
<li><p><strong>restorecon</strong>: 根据平台SE配置，回复某个文件的SE标签。</p>
</li>
<li><p><strong>runcon</strong>: 以指定的SE标签启动某个进程。</p>
<h2 id="内核-sys-驱动节点"><a href="#内核-sys-驱动节点" class="headerlink" title="内核/sys/驱动节点"></a>内核/sys/驱动节点</h2><p><code>/sys/fs/selinux/</code> 目录是SELinux的相关内核驱动节点，可以通过 <code>cat</code> 获取一些关于SE的状态信息。如，查看当前SE的enforce状态：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">lemonadep:/sys/fs/selinux # cat /sys/fs/selinux/enforce</span><br><span class="line">1</span><br></pre></td></tr></table></figure></li>
</ul>
<h1 id="FAQ"><a href="#FAQ" class="headerlink" title="FAQ"></a>FAQ</h1><h2 id="安全上下文-security-context"><a href="#安全上下文-security-context" class="headerlink" title="安全上下文(security context)"></a>安全上下文(security context)</h2><p>SEAndroid是一种基于安全策略的MAC安全机制。这种安全策略又是建立在对象的安全上下文的基础上的。这里所说的对象分为两种类型，一种称主体（Subject），一种称为客体（Object）。主体通常就是指进程，而客观就是指进程所要访问的资源，例如文件、系统属性等。<br>安全上下文实际上就是一个附加在对象上的标签（Tag）。这个标签实际上就是一个字符串，它由四部分内容组成，分别是SELinux用户、SELinux角色、类型、安全级别，每一个部分都通过一个冒号来分隔，格式为：<strong>user:role:type:sensitivity</strong>。</p>
<ul>
<li><strong>安全级别</strong>：最开始的目的是用来对美国政府分类文件进行访问控制的。在基于安全级别的MAC安全机制中，主体（subject）和客体（object）都关联有一个安全级别。其中，安全级别较高的主体可以读取安全级别较低的客体，而安全级别较低的主体可以写入安全级别较高的客体。前者称为“read down”，而后者称为“write up”。通过这种规则，可以允许数据从安全级别较低的主体流向安全级别较高的主体，而限制数据从安全级别较高的主体流向安全级别较低的主体，从而有效地保护了数据。注意，如果主体和客体的安全级别是相同的，那么主体是可以对客体进行读和写的。<h2 id="file-contexts-如何生效？"><a href="#file-contexts-如何生效？" class="headerlink" title="file_contexts 如何生效？"></a>file_contexts 如何生效？</h2>在创建新 <code>te</code> 时，请创建或更新该文件，以便为文件分配新标签。如需应用新的 <code>file_contexts</code>，请重新构建文件系统映像，或对要重新添加标签的文件运行 <code>restorecon</code>。在升级时，对 <code>file_contexts</code> 所做的更改会在升级过程中自动应用于系统和用户数据分区。此外，您还可以通过以下方式使这些更改在升级过程中自动应用于其他分区：在以允许读写的方式装载相应分区后，将 <code>restorecon_recursive</code> 调用添加到 init.board.rc 文件中。<br><em>创建文件时， 若无特殊指定， 会继承其父目录的安全上下文</em> （<a href="http://lishiwen4.github.io/android/selinux-security-context">####7.2 文件的安全上下文</a>）<br>SELinux中所有的文件系统的初始安全上下文是在挂载时标记的的，根据文件系统的不同，其labeling的规则有所差异。<h2 id="Check-the-policies-built-through-the-intermediates-files"><a href="#Check-the-policies-built-through-the-intermediates-files" class="headerlink" title="Check the policies built through the intermediates files."></a>Check the policies built through the intermediates files.</h2><code>$OUT/obj/ETC/sepolicy_intermediates/policy.conf</code><br>The file will list all the processed policies with a comment detailing the source<h2 id="How-to-check-that-your-modifications-are-allowed？"><a href="#How-to-check-that-your-modifications-are-allowed？" class="headerlink" title="How to check that your modifications are allowed？"></a>How to check that your modifications are allowed？</h2>build only the sepolicy project：<code>mmm -B external/sepolicy</code><h2 id="How-to-disable-it"><a href="#How-to-disable-it" class="headerlink" title="How to disable it?"></a>How to disable it?</h2>Since SELinux can impede development, it might be useful to disable it during debugging phase. For a runtime disablement, you can use setenforce.<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> adb shell setenforce 0</span></span><br></pre></td></tr></table></figure></li>
</ul>
<p><strong>kernel</strong>中彻底关闭：修改<code>LINUX/android/kernel/arch/arm64/configs/xxx_defconfig</code>文件（xxx一般为手机产品名）， 去掉<code>CONFIG_SECURITY_SELINUX=y</code>的配置项<br>For a more permanent option, we’ve added a trigger to the boot script that allows you to disable enforcement from <strong>U-Boot</strong>.</p>
<ul>
<li><p>If you want to be permissive (denials logged in dmesg but not enforced)</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">U-Boot &gt; setenv selinux permissive</span><br></pre></td></tr></table></figure></li>
<li><p>If you want to disable selinux completely (no logs)</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">U-Boot &gt; setenv selinux disabled</span><br></pre></td></tr></table></figure></li>
</ul>
<blockquote>
<p>NOTE: disabling SELinux completely isn’t possible since Android Nougat due to <a href="https://android.googlesource.com/platform/system/core/+/d34e407aeb5898f19d4f042b7558420bbb3a1817">this patch</a>.</p>
</blockquote>
<h2 id="如何在设备上默认打开或关闭？"><a href="#如何在设备上默认打开或关闭？" class="headerlink" title="如何在设备上默认打开或关闭？"></a>如何在设备上默认打开或关闭？</h2><p>如下code会检测cmdline对selinux的配置，可以直接写死 <code>status</code> 变量来打开/关闭 SELinux。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/system/core/init/selinux.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">enum</span> <span class="title">EnforcingStatus</span> &#123;</span> SELINUX_PERMISSIVE, SELINUX_ENFORCING &#125;;</span><br><span class="line"></span><br><span class="line"><span class="function">EnforcingStatus <span class="title">StatusFromProperty</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    EnforcingStatus status = SELINUX_ENFORCING;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">ImportKernelCmdline</span>([&amp;](<span class="keyword">const</span> std::string&amp; key, <span class="keyword">const</span> std::string&amp; value) &#123;</span><br><span class="line">        <span class="keyword">if</span> (key == <span class="string">&quot;androidboot.selinux&quot;</span> &amp;&amp; value == <span class="string">&quot;permissive&quot;</span>) &#123;</span><br><span class="line">            status = SELINUX_PERMISSIVE;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (status == SELINUX_ENFORCING) &#123;</span><br><span class="line">        <span class="built_in">ImportBootconfig</span>([&amp;](<span class="keyword">const</span> std::string&amp; key, <span class="keyword">const</span> std::string&amp; value) &#123;</span><br><span class="line">            <span class="keyword">if</span> (key == <span class="string">&quot;androidboot.selinux&quot;</span> &amp;&amp; value == <span class="string">&quot;permissive&quot;</span>) &#123;</span><br><span class="line">                status = SELINUX_PERMISSIVE;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> status;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>编译和运行</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> time prebuilts/build-tools/linux-x86/bin/ninja -f out/combined-lemonadep.ninja -j64 init_second_stage</span></span><br><span class="line">[7/7] Install: out/target/product/lemonadep/system/bin/init</span><br><span class="line"> </span><br><span class="line">real    0m3.198s</span><br><span class="line">user    0m19.220s</span><br><span class="line">sys 0m1.744s</span><br><span class="line"> </span><br><span class="line"></span><br><span class="line">adb push out/target/product/lemonadep/system/bin/init /system/bin/init</span><br></pre></td></tr></table></figure>
<h2 id="How-to-create-extend-a-policy"><a href="#How-to-create-extend-a-policy" class="headerlink" title="How to create/extend a policy?"></a>How to create/extend a policy?</h2><p>The first step is to be able to identify the denials from. If SELinux is set to be <em>permissive</em> or <em>enforcing</em>, you can simply look at the kernel logs.</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> adb shell dmesg | ``grep` `avc</span></span><br></pre></td></tr></table></figure>
<p>Some tools exist to generate policies directly from the above logs.</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> sudo apt-get install policycoreutils</span></span><br><span class="line"><span class="meta">$</span><span class="bash"> adb shell dmesg | grep avc | audit2allow</span></span><br></pre></td></tr></table></figure>
<p>Once the policy has been generated, it needs to be included to the SELinux system configuration.<br>The first rule to add a policy is to <strong>NEVER</strong> modify the <code>external/sepolicy</code> project. Instead your <code>[BoardConfig.mk](http://boardconfig.mk/)</code> should specify some <code>BOARD_SEPOLICY_DIRS</code> directories from which policy can be overridden or unionised. In our BSP, every <code>[BoardConfig.mk](http://boardconfig.mk/)</code> include <code>[sepolicy.mk](https://github.com/boundarydevices/android_device_boundary/blob/boundary-imx-l5.0.0_1.0.0-ga/sepolicy.mk)</code> which specify the .te files location/</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">BOARD_SEPOLICY_DIRS := \</span><br><span class="line"> device/boundary/sepolicy \</span><br><span class="line"> device/fsl/sabresd_6dq/sepolicy</span><br><span class="line">  </span><br><span class="line">BOARD_SEPOLICY_UNION := \</span><br><span class="line"> app.te \</span><br><span class="line"> file_contexts \</span><br><span class="line"> fs_use \</span><br><span class="line"> genfs_contexts \</span><br><span class="line"> netd.te \</span><br><span class="line"> untrusted_app.te \</span><br></pre></td></tr></table></figure>
<p>We invite you to browse our policy changes made for the Lollipop release: <a href="https://github.com/boundarydevices/android_device_boundary/tree/boundary-imx-l5.0.0_1.0.0-ga/sepolicy">https://github.com/boundarydevices/android_device_boundary/tree/boundary-imx-l5.0.0_1.0.0-ga/sepolicy</a></p>
<h2 id="How-to-custom-Security-Context-of-process"><a href="#How-to-custom-Security-Context-of-process" class="headerlink" title="How to custom Security Context of process?"></a>How to custom Security Context of process?</h2><p><strong>1. Define a Context</strong><br>Let’s assume you want to create a custom context called <code>np_mybinary</code> for your binary.<br>This is what your <code>.te</code> file will have to contain:</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">type np_mybinary, domain;</span><br><span class="line">type np_mybinary_exec, exec_type, vendor_file_type, file_type;</span><br></pre></td></tr></table></figure>
<p>This is what your <code>file_contexts</code> file will have to contain:</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">/system/bin/mymodule  u:object_r:np_mybinary_exec:s0</span><br></pre></td></tr></table></figure>

<p><strong>2. Define a Transition</strong><br>You need to define the transition from the parent process context to <code>np_mybinary_exec</code>.</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">domain_auto_trans(&lt;parent-context&gt;, np_mybinary, np_mybinary_exec)</span><br></pre></td></tr></table></figure>
<p>If your process is started by init, there is a <a href="https://android.googlesource.com/platform/system/sepolicy/+/refs/heads/pie-release/public/te_macros#162">simple macro</a> available:</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">init_daemon_domain(np_mybinary);</span><br></pre></td></tr></table></figure>
<p>手动添加SEPolicy权限。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash"> denied <span class="built_in">log</span></span></span><br><span class="line">type=1400 audit(32.939:25): avc: denied &#123; open &#125; for pid=2592 comm=&quot;chmod&quot; path=&quot;/dev/block/mmcblk0p25&quot; dev=&quot;tmpfs&quot; ino=6494</span><br><span class="line">scontext=u:r:init_shell:s0 tcontext=u:object_r:block_device:s0 tclass=blk_file permissive=1</span><br><span class="line"><span class="meta"> </span></span><br><span class="line"><span class="meta">#</span><span class="bash"> 关键信息</span></span><br><span class="line"><span class="meta">#</span><span class="bash"> avc: denied &#123;操作权限&#125; <span class="keyword">for</span> pid=7201 comm=<span class="string">&quot;进程名&quot;</span> scontext=u:r:源类型:s0 tcontext=u:r:目标类型:s0 tclass=访问类型 permissive=0</span></span><br><span class="line">denied &#123; open &#125;             u:r:init_shell:s0            u:object_r:block_device:s0       blk_file</span><br><span class="line">             A              B                            C                                D</span><br><span class="line"><span class="meta"> </span></span><br><span class="line"><span class="meta">#</span><span class="bash"> 生成规则：/system/sepolicy/private/init_shell.te</span></span><br><span class="line"><span class="meta">#</span><span class="bash"> 格式：allow  源类型 目标类型:访问类型 &#123;操作权限&#125;;</span></span><br><span class="line">allow init_shell  block_device:blk_file open;</span><br><span class="line">      B           C            D        A</span><br></pre></td></tr></table></figure>

<h1 id="万能公式"><a href="#万能公式" class="headerlink" title="万能公式"></a>万能公式</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="number">11</span>-<span class="number">04</span> <span class="number">18</span>:<span class="number">37</span>:<span class="number">12.143</span>   <span class="number">653</span>   <span class="number">653</span> W boot@<span class="number">1.1</span>-servic: type=<span class="number">1400</span> audit(<span class="number">0.0</span>:<span class="number">93</span>): avc: denied &#123; getattr &#125; <span class="keyword">for</span> path=<span class="string">&quot;/dev/block/sde21&quot;</span> dev=<span class="string">&quot;tmpfs&quot;</span> ino=<span class="number">2240</span> scontext=u:r:hal_bootctl_default:s0 tcontext=u:object_r:vendor_uefi_block_device:s0 tclass=blk_file permissive=<span class="number">0</span></span><br></pre></td></tr></table></figure>
<p>如上log是<a href="mailto:&#97;&#110;&#x64;&#x72;&#x6f;&#105;&#x64;&#x2e;&#x68;&#x61;&#114;&#x64;&#x77;&#x61;&#x72;&#x65;&#46;&#98;&#x6f;&#111;&#x74;&#x40;&#49;&#46;&#49;&#x2d;&#115;&#x65;&#114;&#118;&#x69;&#x63;&#x65;">&#97;&#110;&#x64;&#x72;&#x6f;&#105;&#x64;&#x2e;&#x68;&#x61;&#114;&#x64;&#x77;&#x61;&#x72;&#x65;&#46;&#98;&#x6f;&#111;&#x74;&#x40;&#49;&#46;&#49;&#x2d;&#115;&#x65;&#114;&#118;&#x69;&#x63;&#x65;</a>没有权限。<br>根据打印出来的avclog：</p>
<ul>
<li>缺少什么权限：avc: denied { getattr }</li>
<li>谁缺少权限：scontext=u:r:hal_bootctl_default</li>
<li>对哪个文件缺少权限：tcontext=u:object_r:vendor_uefi_block_device:s0</li>
<li>什么类型的文件：tclass=blk_file</li>
</ul>
<p>也就是说：hal_bootctl_default进程对vendor_uefi_block_device类型的blk_file缺少getattr权限。所以添加的权限如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">allow hal_bootctl_default vendor_uefi_block_device:blk_file &#123; getattr &#125;</span><br></pre></td></tr></table></figure>
<p>但是，这有一个巨大的坑！有时候avc denied的log不是一次性暴露所有权限问题，要等解决一个权限问题之后，才会暴露另外一个权限问题。比如提示缺少某个目录的read权限，加入read之后，才显示缺少write权限，要一次次一次试，一次一次加，时间成本极大。所以我们直接给最高的权限（一般不建议），代码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">allow hal_bootctl_default vendor_uefi_block_device:blk_file create_dir_perms</span><br></pre></td></tr></table></figure>
<p>有的同学比较喜欢用audit2allow，我刚接触seliunx时也是极度依赖audit2allow。后来学习了selinux一些知识并总结这个万能公式后，就不再用audit2allow了。</p>
<h1 id="Reference"><a href="#Reference" class="headerlink" title="Reference"></a>Reference</h1><ul>
<li><a href="https://selinuxproject.org/page/NewUsers">https://selinuxproject.org/page/NewUsers</a></li>
<li><a href="https://boundarydevices.com/android-security-part-3-security-enhanced-linux-in-android/">https://boundarydevices.com/android-security-part-3-security-enhanced-linux-in-android/</a></li>
<li>CIL (Common Intermediate Language)：<a href="https://github.com/SELinuxProject/selinux/blob/master/secilc/docs/README.md">https://github.com/SELinuxProject/selinux/blob/master/secilc/docs/README.md</a></li>
<li><a href="https://source.android.com/security/selinux">https://source.android.com/security/selinux</a></li>
</ul>
]]></content>
      <categories>
        <category>Android</category>
      </categories>
      <tags>
        <tag>Android</tag>
        <tag>SELinux</tag>
      </tags>
  </entry>
  <entry>
    <title>bootanimation程序简介</title>
    <url>/archives/b4eccfd8.html</url>
    <content><![CDATA[<span id="more"></span>
<meta name="referrer" content="no-referrer"/>

<p>bootanimation是在kernel启动完毕后，由init程序根据rc文件call起来的程序，其主要功能是在启动Android服务时，屏幕显示一个开机动画，以此来改善用户体验的一个程序。本文分析该程序主要流程，以及个功能模块的代码实现。</p>
<h1 id="代码结构"><a href="#代码结构" class="headerlink" title="代码结构"></a>代码结构</h1><p><img src="https://cdn.nlark.com/yuque/0/2021/png/1759879/1630486679128-4ebf2049-bfae-47b4-a3a2-13785c709b45.png#clientId=ued01eebf-0ff0-4&crop=0&crop=0&crop=1&crop=1&from=ui&id=u912425bb&margin=%5Bobject%20Object%5D&name=bootanimationall.png&originHeight=499&originWidth=791&originalType=binary&ratio=1&rotation=0&showTitle=false&size=67632&status=done&style=none&taskId=uac8d1314-96f1-4e2a-b114-70f2f4127f7&title=" alt="bootanimationall.png"><br>（图片来源于网络，从Android12.0的代码没有看到支持video的部分，请忽略）</p>
<p>如上图所示，程序的主要功能类都已经在上图中描述。应用代码位于<a href="http://aospxref.com/android-12.0.0_r3/xref/frameworks/base/cmds/bootanimation/">frameworks/base/cmds/bootanimation</a>，各代码文件如下表所示：</p>
<table>
<thead>
<tr>
<th><strong>文件名</strong></th>
<th><strong>简要说明</strong></th>
</tr>
</thead>
<tbody><tr>
<td>Android.bp</td>
<td>编译脚本</td>
</tr>
<tr>
<td>bootanim.rc</td>
<td>开机启动脚本</td>
</tr>
<tr>
<td>bootanimation_main.cpp</td>
<td>程序入口</td>
</tr>
<tr>
<td>BootAnimation.cpp</td>
<td>应用主类，功能代码基本在该文件中实现</td>
</tr>
<tr>
<td>BootAnimation.h</td>
<td>–</td>
</tr>
<tr>
<td>BootAnimationUtil.cpp</td>
<td>–</td>
</tr>
<tr>
<td>BootAnimationUtil.h</td>
<td>–</td>
</tr>
<tr>
<td>audioplay.cpp</td>
<td>–</td>
</tr>
<tr>
<td>audioplay.h</td>
<td>–</td>
</tr>
</tbody></table>
<h1 id="创建BootAnimation对象"><a href="#创建BootAnimation对象" class="headerlink" title="创建BootAnimation对象"></a>创建BootAnimation对象</h1><p>BootAnimation的程序入口为bootanimation_main.cpp中的main方法，该方法中会创建BootAnimation对象。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/cmds/bootanimation/bootanimation_main.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    ...</span><br><span class="line">        <span class="keyword">bool</span> noBootAnimation = <span class="built_in">bootAnimationDisabled</span>();</span><br><span class="line">    <span class="keyword">if</span> (!noBootAnimation) &#123;</span><br><span class="line">        </span><br><span class="line">        ...</span><br><span class="line">            </span><br><span class="line">            <span class="comment">// create the boot animation object (may take up to 200ms for 2MB zip)</span></span><br><span class="line">            sp&lt;BootAnimation&gt; boot = <span class="keyword">new</span> <span class="built_in">BootAnimation</span>(audioplay::<span class="built_in">createAnimationCallbacks</span>());</span><br><span class="line">        </span><br><span class="line">        <span class="built_in">waitForSurfaceFlinger</span>();</span><br><span class="line">        </span><br><span class="line">        ...</span><br><span class="line">        &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>main方法中创建BootAnimation对象后会将其赋值给一个sp<BootAnimation>对象，sp类重载了=运算符，赋值时会调用右值对象的incStrong()方法，以此标记当前对象正在使用。赋值的最终目的是将BootAnimation对象对象指针保存到sp对象的m_ptr成员中。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/cmds/bootanimation/BootAnimation.cpp</span></span><br><span class="line"></span><br><span class="line">BootAnimation::<span class="built_in">BootAnimation</span>(sp&lt;Callbacks&gt; callbacks)</span><br><span class="line">        : <span class="built_in">Thread</span>(<span class="literal">false</span>), <span class="built_in">mLooper</span>(<span class="keyword">new</span> <span class="built_in">Looper</span>(<span class="literal">false</span>)), <span class="built_in">mClockEnabled</span>(<span class="literal">true</span>), <span class="built_in">mTimeIsAccurate</span>(<span class="literal">false</span>),</span><br><span class="line">        <span class="built_in">mTimeFormat12Hour</span>(<span class="literal">false</span>), <span class="built_in">mTimeCheckThread</span>(<span class="literal">nullptr</span>), <span class="built_in">mCallbacks</span>(callbacks) &#123;</span><br><span class="line">    mSession = <span class="keyword">new</span> <span class="built_in">SurfaceComposerClient</span>();</span><br><span class="line"></span><br><span class="line">    std::string powerCtl = android::base::<span class="built_in">GetProperty</span>(<span class="string">&quot;sys.powerctl&quot;</span>, <span class="string">&quot;&quot;</span>);</span><br><span class="line">    <span class="keyword">if</span> (powerCtl.<span class="built_in">empty</span>()) &#123;</span><br><span class="line">        mShuttingDown = <span class="literal">false</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        mShuttingDown = <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">ALOGD</span>(<span class="string">&quot;%sAnimationStartTiming start time: %&quot;</span> PRId64 <span class="string">&quot;ms&quot;</span>, mShuttingDown ? <span class="string">&quot;Shutdown&quot;</span> : <span class="string">&quot;Boot&quot;</span>,</span><br><span class="line">            <span class="built_in">elapsedRealtime</span>());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p> BootAnimation的构造函数中：</p>
<ul>
<li>mSession是BootAnimation类的一个成员变量，它的类型为SurfaceComposerClient，是用来和SurfaceFlinger执行Binder进程间通信。</li>
</ul>
<p>SurfaceComposerClient类内部有一个实现了ISurfaceComposerClient接口的Binder代理对象mClient，这个Binder代理对象引用了SurfaceFlinger服务，SurfaceComposerClient类就是通过它来和SurfaceFlinger服务通信的。<br>由于BootAnimation类引用了SurfaceFlinger服务，因此，当SurfaceFlinger服务意外死亡时，BootAnimation类就需要得到通知，并执行binderDied函数。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/cmds/bootanimation/BootAnimation.cpp</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">BootAnimation::binderDied</span><span class="params">(<span class="keyword">const</span> wp&lt;IBinder&gt;&amp;)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// woah, surfaceflinger died!</span></span><br><span class="line">    <span class="built_in">SLOGD</span>(<span class="string">&quot;SurfaceFlinger died, exiting...&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// calling requestExit() is not enough here because the Surface code</span></span><br><span class="line">    <span class="comment">// might be blocked on a condition variable that will never be updated.</span></span><br><span class="line">    <span class="built_in">kill</span>( <span class="built_in">getpid</span>(), SIGKILL );</span><br><span class="line">    <span class="built_in">requestExit</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>binderDied函数会杀死当前进程，并退出。</p>
<ul>
<li>通过读取属性“sys.powerctl”来确定当前播放的是开机动画还是关机动画。</li>
</ul>
<p>对于不熟悉cpp代码的同学看到这就觉得很奇怪，在bootanimation_main.cpp的main还是里调用BootAnimation构造函数，也没看到BootAnimation的构造函数里继续做任何工作啊，那这个程序怎么跑呢？<br> 首先我们看下BootAnimation的头文件：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/cmds/bootanimation/BootAnimation.h</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">BootAnimation</span> :</span> <span class="keyword">public</span> Thread, <span class="keyword">public</span> IBinder::DeathRecipient    <span class="comment">//继承了Thread类和IBinder::DeathRecipient类</span></span><br><span class="line">&#123;</span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line"> <span class="keyword">private</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">bool</span>        <span class="title">threadLoop</span><span class="params">()</span></span>;<span class="comment">//线程体，如果返回true，且requestExit()没有被调用，则该函数会再次执行；如果返回false，则threadloop中的内容仅仅执行一次，线程就会退出</span></span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">status_t</span>    <span class="title">readyToRun</span><span class="params">()</span></span>;<span class="comment">//线程体执行前的初始化工作</span></span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span>        <span class="title">onFirstRef</span><span class="params">()</span></span>;<span class="comment">//属于其父类RefBase，该函数在强引用sp新增引用计数時调用，就是当有sp包装的类初始化的时候调用    </span></span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span>        <span class="title">binderDied</span><span class="params">(<span class="keyword">const</span> wp&lt;IBinder&gt;&amp; who)</span></span>;<span class="comment">//当对象死掉或者其他情况导致该Binder结束时，就会回调binderDied()方法</span></span><br><span class="line"></span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="onFirstRef"><a href="#onFirstRef" class="headerlink" title="onFirstRef()"></a>onFirstRef()</h2><p>BootAnimation类继承了Thread类和IBinder::DeathRecipient类，BootAnimation类间接地继承了RefBase类，并且重写了RefBase类的成员函数onFirstRef，当一个BootAnimation对象第一次被智能指针引用时，这个BootAnimation对象的成员函数onFirstRef就会被调用。</p>
<h2 id="readyToRun"><a href="#readyToRun" class="headerlink" title="readyToRun()"></a>readyToRun()</h2><p>BootAnimation类继承了Thread类，当BootAnimation类的成员函数onFirstRef调用了父类Thread的成员函数run之后，系统就会创建一个线程，这个线程在第一次运行之前，会调用BootAnimation类的成员函数readyToRun来执行一些Thread执行前的初始化工作。</p>
<h2 id="threadLoop"><a href="#threadLoop" class="headerlink" title="threadLoop()"></a>threadLoop()</h2><p>后面再调用BootAnimation类的成员函数threadLoop来显示开机画面,每个线程类都要实现的，在这里定义thread的执行内容。这个函数如果返回true，且没有调用requestExit()，则该函数会再次执行；如果返回false，则threadloop中的内容仅仅执行一次，线程就会退出。</p>
<h2 id="android-或者movie"><a href="#android-或者movie" class="headerlink" title="android()或者movie()"></a>android()或者movie()</h2><p>显示完成之后，就会销毁前面所创建的EGLContext对象mContext、EGLSurface对象mSurface，以及EGLDisplay对象mDisplay等。android()方法里主要是使用方法绘制安卓字样，这边的退出使用的是checkExit(）；一般使用自定义动画播放，因此主要看一下movie方法里的主要调用方法。</p>
<h1 id="初始化动画文件"><a href="#初始化动画文件" class="headerlink" title="初始化动画文件"></a>初始化动画文件</h1><p>前面讲到在sp的赋值过程中会调用RefBase的incStrong方法，该方法会在首次引用时调用当前类的onFirstRef方法，该方法被BootAnimation类重写了。<br>也就是说BootAnimation类间接继承了RefBase类，且上面的main函数创建BootAnimation对象的时候使用智能指针引用，所以执行BootAnimation类的构造函数创建对象时，也会执行onFirstRef函数，下面是onFirstRef函数：</p>
<h2 id="onFirstRef-1"><a href="#onFirstRef-1" class="headerlink" title="onFirstRef()"></a>onFirstRef()</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/cmds/bootanimation/BootAnimation.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">BootAnimation::onFirstRef</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="comment">//注册SurfaceFlinger服务的死亡接收通知</span></span><br><span class="line">    <span class="keyword">status_t</span> err = mSession-&gt;<span class="built_in">linkToComposerDeath</span>(<span class="keyword">this</span>);</span><br><span class="line">    <span class="built_in">SLOGE_IF</span>(err, <span class="string">&quot;linkToComposerDeath failed (%s) &quot;</span>, <span class="built_in">strerror</span>(-err));</span><br><span class="line">    <span class="keyword">if</span> (err == NO_ERROR) &#123;</span><br><span class="line">        <span class="comment">// Load the animation content -- this can be slow (eg 200ms)</span></span><br><span class="line">        <span class="comment">// called before waitForSurfaceFlinger() in main() to avoid wait</span></span><br><span class="line">        <span class="built_in">ALOGD</span>(<span class="string">&quot;%sAnimationPreloadTiming start time: %&quot;</span> PRId64 <span class="string">&quot;ms&quot;</span>,</span><br><span class="line">                mShuttingDown ? <span class="string">&quot;Shutdown&quot;</span> : <span class="string">&quot;Boot&quot;</span>, <span class="built_in">elapsedRealtime</span>());</span><br><span class="line">        <span class="built_in">preloadAnimation</span>();</span><br><span class="line">        <span class="built_in">ALOGD</span>(<span class="string">&quot;%sAnimationPreloadStopTiming start time: %&quot;</span> PRId64 <span class="string">&quot;ms&quot;</span>,</span><br><span class="line">                mShuttingDown ? <span class="string">&quot;Shutdown&quot;</span> : <span class="string">&quot;Boot&quot;</span>, <span class="built_in">elapsedRealtime</span>());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>onFirstRef先注册SurfaceFlinger服务的死亡接收通知，接着调用preloadAnimation加载开机动画资源文件。</p>
<h2 id="preloadAnimation"><a href="#preloadAnimation" class="headerlink" title="preloadAnimation()"></a>preloadAnimation()</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/cmds/bootanimation/BootAnimation.cpp</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">BootAnimation::preloadAnimation</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="built_in">findBootAnimationFile</span>();</span><br><span class="line">    <span class="keyword">if</span> (!mZipFileName.<span class="built_in">isEmpty</span>()) &#123;</span><br><span class="line">        mAnimation = <span class="built_in">loadAnimation</span>(mZipFileName);</span><br><span class="line">        <span class="keyword">return</span> (mAnimation != <span class="literal">nullptr</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>调用findBootAnimationFile找到动画文件，也就是初始化 mZipFileName。</p>
<h2 id="findBootAnimationFile"><a href="#findBootAnimationFile" class="headerlink" title="findBootAnimationFile()"></a>findBootAnimationFile()</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/cmds/bootanimation/BootAnimation.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">BootAnimation::findBootAnimationFile</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="comment">// If the device has encryption turned on or is in process</span></span><br><span class="line">    <span class="comment">// of being encrypted we show the encrypted boot animation.</span></span><br><span class="line">    <span class="keyword">char</span> decrypt[PROPERTY_VALUE_MAX];</span><br><span class="line">    <span class="built_in">property_get</span>(<span class="string">&quot;vold.decrypt&quot;</span>, decrypt, <span class="string">&quot;&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">bool</span> encryptedAnimation = <span class="built_in">atoi</span>(decrypt) != <span class="number">0</span> ||</span><br><span class="line">        !<span class="built_in">strcmp</span>(<span class="string">&quot;trigger_restart_min_framework&quot;</span>, decrypt);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!mShuttingDown &amp;&amp; encryptedAnimation) &#123;</span><br><span class="line">        <span class="keyword">static</span> <span class="keyword">const</span> std::vector&lt;std::string&gt; encryptedBootFiles = &#123;</span><br><span class="line">            PRODUCT_ENCRYPTED_BOOTANIMATION_FILE, SYSTEM_ENCRYPTED_BOOTANIMATION_FILE,</span><br><span class="line">        &#125;;</span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">findBootAnimationFileInternal</span>(encryptedBootFiles)) &#123;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">bool</span> playDarkAnim = android::base::<span class="built_in">GetIntProperty</span>(<span class="string">&quot;ro.boot.theme&quot;</span>, <span class="number">0</span>) == <span class="number">1</span>;</span><br><span class="line">    <span class="comment">//开机动画文件</span></span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">const</span> std::vector&lt;std::string&gt; bootFiles = &#123;</span><br><span class="line">        APEX_BOOTANIMATION_FILE, playDarkAnim ? PRODUCT_BOOTANIMATION_DARK_FILE : PRODUCT_BOOTANIMATION_FILE,</span><br><span class="line">        OEM_BOOTANIMATION_FILE, SYSTEM_BOOTANIMATION_FILE</span><br><span class="line">    &#125;;</span><br><span class="line">    <span class="comment">//关机动画文件</span></span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">const</span> std::vector&lt;std::string&gt; shutdownFiles = &#123;</span><br><span class="line">        PRODUCT_SHUTDOWNANIMATION_FILE, OEM_SHUTDOWNANIMATION_FILE, SYSTEM_SHUTDOWNANIMATION_FILE, <span class="string">&quot;&quot;</span></span><br><span class="line">    &#125;;</span><br><span class="line">    <span class="comment">//重启动画文件</span></span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">const</span> std::vector&lt;std::string&gt; userspaceRebootFiles = &#123;</span><br><span class="line">        PRODUCT_USERSPACE_REBOOT_ANIMATION_FILE, OEM_USERSPACE_REBOOT_ANIMATION_FILE,</span><br><span class="line">        SYSTEM_USERSPACE_REBOOT_ANIMATION_FILE,</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//判断是重启、关机还是开机</span></span><br><span class="line">    <span class="keyword">if</span> (android::base::<span class="built_in">GetBoolProperty</span>(<span class="string">&quot;sys.init.userspace_reboot.in_progress&quot;</span>, <span class="literal">false</span>)) &#123;</span><br><span class="line">        <span class="built_in">findBootAnimationFileInternal</span>(userspaceRebootFiles);</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (mShuttingDown) &#123;</span><br><span class="line">        <span class="built_in">findBootAnimationFileInternal</span>(shutdownFiles);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="built_in">findBootAnimationFileInternal</span>(bootFiles);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>首先根据系统属性vold.decrypt来判断系统是否启动加密或者正在加密处理，如果是，则会根据优先级降级的方式，去播放如下两个路径的加密动画：<ul>
<li>static const char PRODUCT_ENCRYPTED_BOOTANIMATION_FILE[] = “/product/media/bootanimation-encrypted.zip”</li>
<li>static const char SYSTEM_ENCRYPTED_BOOTANIMATION_FILE[] = “/system/media/bootanimation-encrypted.zip”</li>
</ul>
</li>
<li>其次根据系统属性sys.init.userspace_reboot.in_progress来判断系统是否正在重启，如果是，则会根据优先级降级的方式，去播放如下三个路径的重启动画：<ul>
<li>static constexpr const char* PRODUCT_USERSPACE_REBOOT_ANIMATION_FILE = “/product/media/userspace-reboot.zip”</li>
<li>static constexpr const char* OEM_USERSPACE_REBOOT_ANIMATION_FILE = “/oem/media/userspace-reboot.zip”</li>
<li>static constexpr const char* SYSTEM_USERSPACE_REBOOT_ANIMATION_FILE = “/system/media/userspace-reboot.zip”</li>
</ul>
</li>
<li>再次根据Bootanimation构造函数中获取到的系统属性sys.powerctl判断系统是否正在关机，如果是，则会根据优先级降级的方式，去播放如下三个路径的关机动画：<ul>
<li>static const char PRODUCT_SHUTDOWNANIMATION_FILE[] = “/product/media/shutdownanimation.zip”</li>
<li>static const char OEM_SHUTDOWNANIMATION_FILE[] = “/oem/media/shutdownanimation.zip”</li>
<li>static const char SYSTEM_SHUTDOWNANIMATION_FILE[] = “/system/media/shutdownanimation.zip”</li>
</ul>
</li>
<li>否则为开机，会根据优先级降级的方式，去播放如下五个路径的开机动画：<ul>
<li>static const char APEX_BOOTANIMATION_FILE[] = “/apex/com.android.bootanimation/etc/bootanimation.zip” （apex升级的方式最优先）</li>
<li>static const char PRODUCT_BOOTANIMATION_DARK_FILE[] = “/product/media/bootanimation-dark.zip” （根据系统属性<a href="http://aospxref.com/android-12.0.0_r3/s?path=ro.boot.t&project=frameworks">ro.boot.t</a>heme判断是暗黑主题还是明亮主题）</li>
<li>static const char PRODUCT_BOOTANIMATION_FILE[] = “/product/media/bootanimation.zip”</li>
<li>static const char OEM_BOOTANIMATION_FILE[] = “/oem/media/bootanimation.zip”</li>
<li>static const char SYSTEM_BOOTANIMATION_FILE[] = “/system/media/bootanimation.zip”<blockquote>
<p>总结：优先级的顺序一般是 product &gt; oem &gt; system</p>
</blockquote>
</li>
</ul>
</li>
</ul>
<h2 id="findBootAnimationFileInternal"><a href="#findBootAnimationFileInternal" class="headerlink" title="findBootAnimationFileInternal()"></a>findBootAnimationFileInternal()</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/cmds/bootanimation/BootAnimation.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">BootAnimation::findBootAnimationFileInternal</span><span class="params">(<span class="keyword">const</span> std::vector&lt;std::string&gt; &amp;files)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">const</span> std::string&amp; f : files) &#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">access</span>(f.<span class="built_in">c_str</span>(), R_OK) == <span class="number">0</span>) &#123;</span><br><span class="line">            mZipFileName = f.<span class="built_in">c_str</span>();</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>可以看出来就是把数据的第一个元素赋值给mZipFileName。</p>
<h1 id="解析动画文件"><a href="#解析动画文件" class="headerlink" title="解析动画文件"></a>解析动画文件</h1><p>在preloadAnimation()初始化mZipFileName后调用loadAnimation(mZipFileName)来解析动画文件。</p>
<h2 id="loadAnimation"><a href="#loadAnimation" class="headerlink" title="loadAnimation()"></a>loadAnimation()</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/cmds/bootanimation/BootAnimation.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function">BootAnimation::Animation* <span class="title">BootAnimation::loadAnimation</span><span class="params">(<span class="keyword">const</span> String8&amp; fn)</span> </span>&#123;</span><br><span class="line">	<span class="comment">//如果已经加载，则返回</span></span><br><span class="line">    <span class="keyword">if</span> (mLoadedFiles.<span class="built_in">indexOf</span>(fn) &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">SLOGE</span>(<span class="string">&quot;File \&quot;%s\&quot; is already loaded. Cyclic ref is not allowed&quot;</span>,</span><br><span class="line">            fn.<span class="built_in">string</span>());</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nullptr</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//打开zip文件</span></span><br><span class="line">    ZipFileRO *zip = ZipFileRO::<span class="built_in">open</span>(fn);</span><br><span class="line">    <span class="keyword">if</span> (zip == <span class="literal">nullptr</span>) &#123;</span><br><span class="line">        <span class="built_in">SLOGE</span>(<span class="string">&quot;Failed to open animation zip \&quot;%s\&quot;: %s&quot;</span>,</span><br><span class="line">            fn.<span class="built_in">string</span>(), <span class="built_in">strerror</span>(errno));</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nullptr</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    Animation *animation =  <span class="keyword">new</span> Animation;</span><br><span class="line">    animation-&gt;fileName = fn;</span><br><span class="line">    animation-&gt;zip = zip;</span><br><span class="line">    animation-&gt;clockFont.map = <span class="literal">nullptr</span>;</span><br><span class="line">    mLoadedFiles.<span class="built_in">add</span>(animation-&gt;fileName);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//解析描述文件&quot;desc.txt&quot;</span></span><br><span class="line">    <span class="built_in">parseAnimationDesc</span>(*animation);</span><br><span class="line">    <span class="keyword">if</span> (!<span class="built_in">preloadZip</span>(*animation)) &#123;</span><br><span class="line">        <span class="built_in">releaseAnimation</span>(animation);</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nullptr</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    mLoadedFiles.<span class="built_in">remove</span>(fn);</span><br><span class="line">    <span class="keyword">return</span> animation;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>打开zip文件并调用parseAnimationDesc解析描述文件”desc.txt”。</p>
<h2 id="parseAnimationDesc"><a href="#parseAnimationDesc" class="headerlink" title="parseAnimationDesc()"></a>parseAnimationDesc()</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/cmds/bootanimation/BootAnimation.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">BootAnimation::parseAnimationDesc</span><span class="params">(Animation&amp; animation)</span>  </span>&#123;</span><br><span class="line">    String8 desString;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!<span class="built_in">readFile</span>(animation.zip, <span class="string">&quot;desc.txt&quot;</span>, desString)) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">char</span> <span class="keyword">const</span>* s = desString.<span class="built_in">string</span>();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Parse the description file</span></span><br><span class="line">    <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">        <span class="keyword">const</span> <span class="keyword">char</span>* endl = <span class="built_in">strstr</span>(s, <span class="string">&quot;\n&quot;</span>);</span><br><span class="line">        <span class="keyword">if</span> (endl == <span class="literal">nullptr</span>) <span class="keyword">break</span>;</span><br><span class="line">        <span class="function">String8 <span class="title">line</span><span class="params">(s, endl - s)</span></span>;</span><br><span class="line">        <span class="keyword">const</span> <span class="keyword">char</span>* l = line.<span class="built_in">string</span>();</span><br><span class="line">        <span class="keyword">int</span> fps = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">int</span> width = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">int</span> height = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">int</span> count = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">int</span> pause = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">int</span> progress = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">int</span> framesToFadeCount = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">char</span> path[ANIM_ENTRY_NAME_MAX];</span><br><span class="line">        <span class="keyword">char</span> color[<span class="number">7</span>] = <span class="string">&quot;000000&quot;</span>; <span class="comment">// default to black if unspecified</span></span><br><span class="line">        <span class="keyword">char</span> clockPos1[TEXT_POS_LEN_MAX + <span class="number">1</span>] = <span class="string">&quot;&quot;</span>;</span><br><span class="line">        <span class="keyword">char</span> clockPos2[TEXT_POS_LEN_MAX + <span class="number">1</span>] = <span class="string">&quot;&quot;</span>;</span><br><span class="line">        <span class="keyword">char</span> pathType;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">int</span> nextReadPos;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">int</span> topLineNumbers = <span class="built_in">sscanf</span>(l, <span class="string">&quot;%d %d %d %d&quot;</span>, &amp;width, &amp;height, &amp;fps, &amp;progress);</span><br><span class="line">        <span class="keyword">if</span> (topLineNumbers == <span class="number">3</span> || topLineNumbers == <span class="number">4</span>) &#123;</span><br><span class="line">            <span class="comment">// SLOGD(&quot;&gt; w=%d, h=%d, fps=%d, progress=%d&quot;, width, height, fps, progress);</span></span><br><span class="line">            animation.width = width;</span><br><span class="line">            animation.height = height;</span><br><span class="line">            animation.fps = fps;</span><br><span class="line">            <span class="keyword">if</span> (topLineNumbers == <span class="number">4</span>) &#123;</span><br><span class="line">              animation.progressEnabled = (progress != <span class="number">0</span>);</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">              animation.progressEnabled = <span class="literal">false</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (<span class="built_in">sscanf</span>(l, <span class="string">&quot;%c %d %d %&quot;</span> <span class="built_in">STRTO</span>(ANIM_PATH_MAX) <span class="string">&quot;s%n&quot;</span>,</span><br><span class="line">                          &amp;pathType, &amp;count, &amp;pause, path, &amp;nextReadPos) &gt;= <span class="number">4</span>) &#123;</span><br><span class="line">            <span class="keyword">if</span> (pathType == <span class="string">&#x27;f&#x27;</span>) &#123;</span><br><span class="line">                <span class="built_in">sscanf</span>(l + nextReadPos, <span class="string">&quot; %d #%6s %16s %16s&quot;</span>, &amp;framesToFadeCount, color, clockPos1,</span><br><span class="line">                       clockPos2);</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="built_in">sscanf</span>(l + nextReadPos, <span class="string">&quot; #%6s %16s %16s&quot;</span>, color, clockPos1, clockPos2);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// SLOGD(&quot;&gt; type=%c, count=%d, pause=%d, path=%s, framesToFadeCount=%d, color=%s, &quot;</span></span><br><span class="line">            <span class="comment">//       &quot;clockPos1=%s, clockPos2=%s&quot;,</span></span><br><span class="line">            <span class="comment">//       pathType, count, pause, path, framesToFadeCount, color, clockPos1, clockPos2);</span></span><br><span class="line">            Animation::Part part;</span><br><span class="line">            part.playUntilComplete = pathType == <span class="string">&#x27;c&#x27;</span>;</span><br><span class="line">            part.framesToFadeCount = framesToFadeCount;</span><br><span class="line">            part.count = count;</span><br><span class="line">            part.pause = pause;</span><br><span class="line">            part.path = path;</span><br><span class="line">            part.audioData = <span class="literal">nullptr</span>;</span><br><span class="line">            part.animation = <span class="literal">nullptr</span>;</span><br><span class="line">            <span class="keyword">if</span> (!<span class="built_in">parseColor</span>(color, part.backgroundColor)) &#123;</span><br><span class="line">                <span class="built_in">SLOGE</span>(<span class="string">&quot;&gt; invalid color &#x27;#%s&#x27;&quot;</span>, color);</span><br><span class="line">                part.backgroundColor[<span class="number">0</span>] = <span class="number">0.0f</span>;</span><br><span class="line">                part.backgroundColor[<span class="number">1</span>] = <span class="number">0.0f</span>;</span><br><span class="line">                part.backgroundColor[<span class="number">2</span>] = <span class="number">0.0f</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="built_in">parsePosition</span>(clockPos1, clockPos2, &amp;part.clockPosX, &amp;part.clockPosY);</span><br><span class="line">            animation.parts.<span class="built_in">add</span>(part);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (<span class="built_in">strcmp</span>(l, <span class="string">&quot;$SYSTEM&quot;</span>) == <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="comment">// SLOGD(&quot;&gt; SYSTEM&quot;);</span></span><br><span class="line">            Animation::Part part;</span><br><span class="line">            part.playUntilComplete = <span class="literal">false</span>;</span><br><span class="line">            part.framesToFadeCount = <span class="number">0</span>;</span><br><span class="line">            part.count = <span class="number">1</span>;</span><br><span class="line">            part.pause = <span class="number">0</span>;</span><br><span class="line">            part.audioData = <span class="literal">nullptr</span>;</span><br><span class="line">            part.animation = <span class="built_in">loadAnimation</span>(<span class="built_in">String8</span>(SYSTEM_BOOTANIMATION_FILE));</span><br><span class="line">            <span class="keyword">if</span> (part.animation != <span class="literal">nullptr</span>)</span><br><span class="line">                animation.parts.<span class="built_in">add</span>(part);</span><br><span class="line">        &#125;</span><br><span class="line">        s = ++endl;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>乍一看这个代码很复杂，其实主要是通过sscanf函数按照顺序从上到下依次解析每一行。如果第一行包含3或者4个数字则是：用来描述开机动画在屏幕显示的大小及速度。否则为一个播放片段，播放片段分为标识符、循环的次数、阶段切换间隔时间、图片的目录、<strong>动画淡出帧。</strong><br>下面来结合描述文件的具体内容一起分析。</p>
<h2 id="desc-txt描述文件"><a href="#desc-txt描述文件" class="headerlink" title="desc.txt描述文件"></a>desc.txt描述文件</h2><p>我们找到一个bootanimation.zip的解压：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">╭─[solo@10.0.12.7] ➤ [/Users/solo/Downloads]</span><br><span class="line">╰─(py39tf2.x) ❯❯❯❯❯❯ ll bootanimation</span><br><span class="line">total 8</span><br><span class="line">drwx------@   8 solo  staff   256B Jul  1 12:30 .</span><br><span class="line">drwx------+  45 solo  staff   1.4K Jul  1 12:13 ..</span><br><span class="line">-rw-------@   1 solo  staff    75B Jan 14  2021 desc.txt</span><br><span class="line">drwxr-xr-x@   3 solo  staff    96B Jul  1 12:13 part0</span><br><span class="line">drwxr-xr-x@  36 solo  staff   1.1K Jul  1 12:13 part1</span><br><span class="line">drwxr-xr-x@  41 solo  staff   1.3K Jul  1 12:13 part2</span><br><span class="line">drwxr-xr-x@ 187 solo  staff   5.8K Jul  1 12:13 part3</span><br><span class="line">drwxr-xr-x@ 104 solo  staff   3.3K Jul  1 12:13 part4</span><br></pre></td></tr></table></figure>
<p>其”desc.txt”文件中的内容如下：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">512 416 60</span><br><span class="line">c 1 0 part0</span><br><span class="line">c 2 15 part1</span><br><span class="line">c 1 0 part2</span><br><span class="line">c 1 0 part3</span><br><span class="line">f 0 0 part4 10</span><br></pre></td></tr></table></figure>
<p>结合上面的代码，我们把desc.txt拆分成两个部分：topLine和非topLine。</p>
<ul>
<li>如果topLine包含3或者4个数字，则是用来描述开机动画在屏幕显示的大小及速度。<table>
<thead>
<tr>
<th>图片的宽</th>
<th>图片的高</th>
<th>每秒显示的帧数</th>
<th>显示百分比进度</th>
</tr>
</thead>
<tbody><tr>
<td>512</td>
<td>416</td>
<td>60</td>
<td>–</td>
</tr>
</tbody></table>
</li>
</ul>
<p>具体为：开机动画的宽度为512个像素，高度为416个像素，显示频率为每秒60帧，即每帧显示1/60秒。<br>Android12.0中加入了“显示百分比”，就是代码中的progressEnabled。</p>
<ul>
<li><p>否则为一个播放片段</p>
<table>
<thead>
<tr>
<th>标识符</th>
<th>循环的次数</th>
<th>阶段切换间隔时间</th>
<th>相应图片的目录</th>
<th>动画淡出帧</th>
</tr>
</thead>
<tbody><tr>
<td>c</td>
<td>1</td>
<td>0</td>
<td>part0</td>
<td>–</td>
</tr>
<tr>
<td>c</td>
<td>2</td>
<td>15</td>
<td>part1</td>
<td>–</td>
</tr>
<tr>
<td>f</td>
<td>0</td>
<td>0</td>
<td>part4</td>
<td>10</td>
</tr>
</tbody></table>
<ul>
<li>标识符：Android5.1之前只有p；Android5.1中加入了c；Android12.0中加入了f。</li>
<li>循环的次数：表示该片段显示的次数，如果为‘0’，表示会无限重复显示。</li>
<li>阶段切换间隔时间：一个片段或与下一个片段之间的时间间隔；<strong>阶段切换间隔时间 * （1/每秒显示的帧数）。</strong></li>
<li>相应图片的目录：bootanimation.zip解析出来后的文件目录，</li>
<li><strong>动画淡出帧：</strong>Android12.0中加入了动画淡出帧，也就是代码中的framesToFadeCount，到第几帧后动画开始淡出。</li>
</ul>
</li>
</ul>
<p>举例：</p>
<ul>
<li>c 1 0 part0：代表该片段显示1次，与下一个片段间隔0s，该片段的显示图片路径为bootanimation.zip/part0。</li>
<li>c 2 15 part1：代表该片段显示2次，与下一个片段间隔15*(1/60)=(1/4)s，与下一个片段间隔15*(1/60)=(1/4)s，该片段的显示图片路径为bootanimation.zip/part1。</li>
<li>f 0 0 part4 10：代表该片段无限循环显示，且两次显示的间隔为0s，该片段的显示图片路径为bootanimation.zip/part4，到第10帧动画开始淡出。</li>
</ul>
<p>通过”desc.txt”文件结合代码（看一眼就能知道的）我们大概知道了其运作原理，但刚才我们是遗留几个问题没有阐述清楚：</p>
<ul>
<li>标识符</li>
<li><strong>显示百分比</strong>进度</li>
<li><strong>动画淡出帧</strong></li>
</ul>
<h3 id="标识符-和-动画淡出帧"><a href="#标识符-和-动画淡出帧" class="headerlink" title="标识符 和 动画淡出帧"></a>标识符 和 动画淡出帧</h3><p>如果标识符为c：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/cmds/bootanimation/BootAnimation.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">BootAnimation::parseAnimationDesc</span><span class="params">(Animation&amp; animation)</span>  </span>&#123;</span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (<span class="built_in">sscanf</span>(l, <span class="string">&quot;%c %d %d %&quot;</span> <span class="built_in">STRTO</span>(ANIM_PATH_MAX) <span class="string">&quot;s%n&quot;</span>,</span><br><span class="line">                      &amp;pathType, &amp;count, &amp;pause, path, &amp;nextReadPos) &gt;= <span class="number">4</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> (pathType == <span class="string">&#x27;f&#x27;</span>) &#123;</span><br><span class="line">            <span class="built_in">sscanf</span>(l + nextReadPos, <span class="string">&quot; %d #%6s %16s %16s&quot;</span>, &amp;framesToFadeCount, color, clockPos1,</span><br><span class="line">                   clockPos2);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="built_in">sscanf</span>(l + nextReadPos, <span class="string">&quot; #%6s %16s %16s&quot;</span>, color, clockPos1, clockPos2);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// SLOGD(&quot;&gt; type=%c, count=%d, pause=%d, path=%s, framesToFadeCount=%d, color=%s, &quot;</span></span><br><span class="line">        <span class="comment">//       &quot;clockPos1=%s, clockPos2=%s&quot;,</span></span><br><span class="line">        <span class="comment">//       pathType, count, pause, path, framesToFadeCount, color, clockPos1, clockPos2);</span></span><br><span class="line">        Animation::Part part;</span><br><span class="line">        part.playUntilComplete = pathType == <span class="string">&#x27;c&#x27;</span>;</span><br><span class="line">        part.framesToFadeCount = framesToFadeCount;</span><br><span class="line">        ...</span><br><span class="line">    &#125;</span><br><span class="line">   ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>可以看到，如果pathType==‘c’，part.playUntilComplete等于true，否则为false。接着，看一下显示代码：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/cmds/bootanimation/BootAnimation.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">BootAnimation::shouldStopPlayingPart</span><span class="params">(<span class="keyword">const</span> Animation::Part&amp; part,</span></span></span><br><span class="line"><span class="params"><span class="function">                                          <span class="keyword">const</span> <span class="keyword">int</span> fadedFramesCount,</span></span></span><br><span class="line"><span class="params"><span class="function">                                          <span class="keyword">const</span> <span class="keyword">int</span> lastDisplayedProgress)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// stop playing only if it is time to exit and it&#x27;s a partial part which has been faded out</span></span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">exitPending</span>() &amp;&amp; !part.playUntilComplete &amp;&amp; fadedFramesCount &gt;= part.framesToFadeCount &amp;&amp;</span><br><span class="line">        (lastDisplayedProgress == <span class="number">0</span> || lastDisplayedProgress == <span class="number">100</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">BootAnimation::playAnimation</span><span class="params">(<span class="keyword">const</span> Animation&amp; animation)</span> </span>&#123;</span><br><span class="line">    ...</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">size_t</span> i=<span class="number">0</span> ; i&lt;pcount ; i++) &#123;</span><br><span class="line">            ...</span><br><span class="line">                <span class="comment">// process the part not only while the count allows but also if already fading</span></span><br><span class="line">                <span class="keyword">for</span> (<span class="keyword">int</span> r=<span class="number">0</span> ; !part.count || r&lt;part.count || fadedFramesCount &gt; <span class="number">0</span> ; r++) &#123;</span><br><span class="line">                    <span class="keyword">if</span> (<span class="built_in">shouldStopPlayingPart</span>(part, fadedFramesCount, lastDisplayedProgress)) <span class="keyword">break</span>;</span><br><span class="line">                    </span><br><span class="line">                    ...</span><br><span class="line">                        </span><br><span class="line">                        <span class="keyword">for</span> (<span class="keyword">size_t</span> j=<span class="number">0</span> ; j&lt;fcount ; j++) &#123;</span><br><span class="line">                            <span class="keyword">if</span> (<span class="built_in">shouldStopPlayingPart</span>(part, fadedFramesCount, lastDisplayedProgress)) <span class="keyword">break</span>;</span><br><span class="line">                            </span><br><span class="line">                            ...</span><br><span class="line">                            &#125;</span><br><span class="line">                    </span><br><span class="line">                    ...</span><br><span class="line">                    &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    </span><br><span class="line">    ....</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>可以看到，如果exitPending()返回值为true且part.playUntilComplete=false，则会break。即：当SurfaceFlinger服务要求bootanimation停止显示动画时，以‘p’标识的片段会停止，而以’c’标识的片段会继续显示。这就是两者之间的主要区别。<br>这里有个问题：重复循环显示的’c’标识片段，会不受任何约束的一直显示下去，这显然是不合适的。所以在Android12.0又新增了条件：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">fadedFramesCount &gt;= part.framesToFadeCount &amp;&amp; (lastDisplayedProgress == <span class="number">0</span> || lastDisplayedProgress == <span class="number">100</span></span><br></pre></td></tr></table></figure>
<p>当前播放的帧大于等于描述文件里设置的framesToFadeCount，就停止动画。</p>
<h3 id="显示百分比进度"><a href="#显示百分比进度" class="headerlink" title="显示百分比进度"></a>显示百分比进度</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/cmds/bootanimation/BootAnimation.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">BootAnimation::parseAnimationDesc</span><span class="params">(Animation&amp; animation)</span>  </span>&#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">int</span> topLineNumbers = <span class="built_in">sscanf</span>(l, <span class="string">&quot;%d %d %d %d&quot;</span>, &amp;width, &amp;height, &amp;fps, &amp;progress);</span><br><span class="line">    <span class="keyword">if</span> (topLineNumbers == <span class="number">3</span> || topLineNumbers == <span class="number">4</span>) &#123;</span><br><span class="line">        ...</span><br><span class="line">        <span class="keyword">if</span> (topLineNumbers == <span class="number">4</span>) &#123;</span><br><span class="line">          animation.progressEnabled = (progress != <span class="number">0</span>);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">          animation.progressEnabled = <span class="literal">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; </span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>如果描述文件里第一行有第四个数字，并且不为0，则显示播放动画的时候显示进度。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/cmds/bootanimation/BootAnimation.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">BootAnimation::playAnimation</span><span class="params">(<span class="keyword">const</span> Animation&amp; animation)</span> </span>&#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">size_t</span> i=<span class="number">0</span> ; i&lt;pcount ; i++) &#123;</span><br><span class="line">        ...</span><br><span class="line"></span><br><span class="line">        <span class="comment">// process the part not only while the count allows but also if already fading</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> r=<span class="number">0</span> ; !part.count || r&lt;part.count || fadedFramesCount &gt; <span class="number">0</span> ; r++) &#123;</span><br><span class="line">            ...</span><br><span class="line"></span><br><span class="line">            <span class="comment">// For the last animation, if we have progress indicator from</span></span><br><span class="line">            <span class="comment">// the system, display it.</span></span><br><span class="line">            <span class="keyword">int</span> currentProgress = android::base::<span class="built_in">GetIntProperty</span>(PROGRESS_PROP_NAME, <span class="number">0</span>);</span><br><span class="line">            <span class="keyword">bool</span> displayProgress = animation.progressEnabled &amp;&amp;</span><br><span class="line">                (i == (pcount <span class="number">-1</span>)) &amp;&amp; currentProgress != <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">size_t</span> j=<span class="number">0</span> ; j&lt;fcount ; j++) &#123;</span><br><span class="line">                ...</span><br><span class="line"></span><br><span class="line">                <span class="keyword">if</span> (displayProgress) &#123;</span><br><span class="line">                    <span class="keyword">int</span> newProgress = android::base::<span class="built_in">GetIntProperty</span>(PROGRESS_PROP_NAME, <span class="number">0</span>);</span><br><span class="line">                    <span class="comment">// In case the new progress jumped suddenly, still show an</span></span><br><span class="line">                    <span class="comment">// increment of 1.</span></span><br><span class="line">                    <span class="keyword">if</span> (lastDisplayedProgress != <span class="number">100</span>) &#123;</span><br><span class="line">                      <span class="comment">// Artificially sleep 1/10th a second to slow down the animation.</span></span><br><span class="line">                      <span class="built_in">usleep</span>(<span class="number">100000</span>);</span><br><span class="line">                      <span class="keyword">if</span> (lastDisplayedProgress &lt; newProgress) &#123;</span><br><span class="line">                        lastDisplayedProgress++;</span><br><span class="line">                      &#125;</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="comment">// Put the progress percentage right below the animation.</span></span><br><span class="line">                    <span class="keyword">int</span> posY = animation.height / <span class="number">3</span>;</span><br><span class="line">                    <span class="keyword">int</span> posX = TEXT_CENTER_VALUE;</span><br><span class="line">                    <span class="built_in">drawProgress</span>(lastDisplayedProgress, animation.progressFont, posX, posY);</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                ...</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            ...</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>代码细节这里不展开叙述。</p>
<h2 id="preloadZip"><a href="#preloadZip" class="headerlink" title="preloadZip()"></a>preloadZip()</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/cmds/bootanimation/BootAnimation.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">BootAnimation::preloadZip</span><span class="params">(Animation&amp; animation)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// read all the data structures</span></span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">size_t</span> pcount = animation.parts.<span class="built_in">size</span>();</span><br><span class="line">    <span class="keyword">void</span> *cookie = <span class="literal">nullptr</span>;</span><br><span class="line">    ZipFileRO* zip = animation.zip;</span><br><span class="line">    <span class="keyword">if</span> (!zip-&gt;<span class="built_in">startIteration</span>(&amp;cookie)) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    ZipEntryRO entry;</span><br><span class="line">    <span class="keyword">char</span> name[ANIM_ENTRY_NAME_MAX];</span><br><span class="line">    <span class="keyword">while</span> ((entry = zip-&gt;<span class="built_in">nextEntry</span>(cookie)) != <span class="literal">nullptr</span>) &#123;</span><br><span class="line">        <span class="keyword">const</span> <span class="keyword">int</span> foundEntryName = zip-&gt;<span class="built_in">getEntryFileName</span>(entry, name, ANIM_ENTRY_NAME_MAX);</span><br><span class="line">        <span class="keyword">if</span> (foundEntryName &gt; ANIM_ENTRY_NAME_MAX || foundEntryName == <span class="number">-1</span>) &#123;</span><br><span class="line">            <span class="built_in">SLOGE</span>(<span class="string">&quot;Error fetching entry file name&quot;</span>);</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="keyword">const</span> String8 <span class="title">entryName</span><span class="params">(name)</span></span>;</span><br><span class="line">        <span class="function"><span class="keyword">const</span> String8 <span class="title">path</span><span class="params">(entryName.getPathDir())</span></span>;</span><br><span class="line">        <span class="function"><span class="keyword">const</span> String8 <span class="title">leaf</span><span class="params">(entryName.getPathLeaf())</span></span>;</span><br><span class="line">        <span class="keyword">if</span> (leaf.<span class="built_in">size</span>() &gt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">if</span> (entryName == CLOCK_FONT_ZIP_NAME) &#123;</span><br><span class="line">                FileMap* map = zip-&gt;<span class="built_in">createEntryFileMap</span>(entry);</span><br><span class="line">                <span class="keyword">if</span> (map) &#123;</span><br><span class="line">                    animation.clockFont.map = map;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (entryName == PROGRESS_FONT_ZIP_NAME) &#123;</span><br><span class="line">                FileMap* map = zip-&gt;<span class="built_in">createEntryFileMap</span>(entry);</span><br><span class="line">                <span class="keyword">if</span> (map) &#123;</span><br><span class="line">                    animation.progressFont.map = map;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">size_t</span> j = <span class="number">0</span>; j &lt; pcount; j++) &#123;</span><br><span class="line">                <span class="keyword">if</span> (path == animation.parts[j].path) &#123;</span><br><span class="line">                    <span class="keyword">uint16_t</span> method;</span><br><span class="line">                    <span class="comment">// supports only stored png files</span></span><br><span class="line">                    <span class="keyword">if</span> (zip-&gt;<span class="built_in">getEntryInfo</span>(entry, &amp;method, <span class="literal">nullptr</span>, <span class="literal">nullptr</span>, <span class="literal">nullptr</span>, <span class="literal">nullptr</span>, <span class="literal">nullptr</span>)) &#123;</span><br><span class="line">                        <span class="keyword">if</span> (method == ZipFileRO::kCompressStored) &#123;</span><br><span class="line">                            FileMap* map = zip-&gt;<span class="built_in">createEntryFileMap</span>(entry);</span><br><span class="line">                            <span class="keyword">if</span> (map) &#123;</span><br><span class="line">                                <span class="function">Animation::Part&amp; <span class="title">part</span><span class="params">(animation.parts.editItemAt(j))</span></span>;</span><br><span class="line">                                <span class="keyword">if</span> (leaf == <span class="string">&quot;audio.wav&quot;</span>) &#123;</span><br><span class="line">                                    <span class="comment">// a part may have at most one audio file</span></span><br><span class="line">                                    part.audioData = (<span class="keyword">uint8_t</span> *)map-&gt;<span class="built_in">getDataPtr</span>();</span><br><span class="line">                                    part.audioLength = map-&gt;<span class="built_in">getDataLength</span>();</span><br><span class="line">                                &#125; <span class="keyword">else</span> <span class="keyword">if</span> (leaf == <span class="string">&quot;trim.txt&quot;</span>) &#123;</span><br><span class="line">                                    part.trimData.<span class="built_in">setTo</span>((<span class="keyword">char</span> <span class="keyword">const</span>*)map-&gt;<span class="built_in">getDataPtr</span>(),</span><br><span class="line">                                                        map-&gt;<span class="built_in">getDataLength</span>());</span><br><span class="line">                                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                                    Animation::Frame frame;</span><br><span class="line">                                    frame.name = leaf;</span><br><span class="line">                                    frame.map = map;</span><br><span class="line">                                    frame.trimWidth = animation.width;</span><br><span class="line">                                    frame.trimHeight = animation.height;</span><br><span class="line">                                    frame.trimX = <span class="number">0</span>;</span><br><span class="line">                                    frame.trimY = <span class="number">0</span>;</span><br><span class="line">                                    part.frames.<span class="built_in">add</span>(frame);</span><br><span class="line">                                &#125;</span><br><span class="line">                            &#125;</span><br><span class="line">                        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                            <span class="built_in">SLOGE</span>(<span class="string">&quot;bootanimation.zip is compressed; must be only stored&quot;</span>);</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// If there is trimData present, override the positioning defaults.</span></span><br><span class="line">    <span class="keyword">for</span> (Animation::Part&amp; part : animation.parts) &#123;</span><br><span class="line">        <span class="keyword">const</span> <span class="keyword">char</span>* trimDataStr = part.trimData.<span class="built_in">string</span>();</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">size_t</span> frameIdx = <span class="number">0</span>; frameIdx &lt; part.frames.<span class="built_in">size</span>(); frameIdx++) &#123;</span><br><span class="line">            <span class="keyword">const</span> <span class="keyword">char</span>* endl = <span class="built_in">strstr</span>(trimDataStr, <span class="string">&quot;\n&quot;</span>);</span><br><span class="line">            <span class="comment">// No more trimData for this part.</span></span><br><span class="line">            <span class="keyword">if</span> (endl == <span class="literal">nullptr</span>) &#123;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="function">String8 <span class="title">line</span><span class="params">(trimDataStr, endl - trimDataStr)</span></span>;</span><br><span class="line">            <span class="keyword">const</span> <span class="keyword">char</span>* lineStr = line.<span class="built_in">string</span>();</span><br><span class="line">            trimDataStr = ++endl;</span><br><span class="line">            <span class="keyword">int</span> width = <span class="number">0</span>, height = <span class="number">0</span>, x = <span class="number">0</span>, y = <span class="number">0</span>;</span><br><span class="line">            <span class="keyword">if</span> (<span class="built_in">sscanf</span>(lineStr, <span class="string">&quot;%dx%d+%d+%d&quot;</span>, &amp;width, &amp;height, &amp;x, &amp;y) == <span class="number">4</span>) &#123;</span><br><span class="line">                <span class="function">Animation::Frame&amp; <span class="title">frame</span><span class="params">(part.frames.editItemAt(frameIdx))</span></span>;</span><br><span class="line">                frame.trimWidth = width;</span><br><span class="line">                frame.trimHeight = height;</span><br><span class="line">                frame.trimX = x;</span><br><span class="line">                frame.trimY = y;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="built_in">SLOGE</span>(<span class="string">&quot;Error parsing trim.txt, line: %s&quot;</span>, lineStr);</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    zip-&gt;<span class="built_in">endIteration</span>(cookie);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>解析bootanimation.zip中的png或者音频文件audio.wav。</p>
<h1 id="启动BootAnimation线程"><a href="#启动BootAnimation线程" class="headerlink" title="启动BootAnimation线程"></a>启动BootAnimation线程</h1><h2 id="readyToRun-1"><a href="#readyToRun-1" class="headerlink" title="readyToRun()"></a>readyToRun()</h2><p>在前面讲到BootAnimation对象创建的时候讲到过，线程的创建先readyToRun()来执行一些Thread执行前的初始化工作。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/cmds/bootanimation/BootAnimation.cpp</span></span><br><span class="line"></span><br><span class="line">status_t BootAnimation::readyToRun() &#123;</span><br><span class="line">    mAssets.addDefaultAssets();</span><br><span class="line"></span><br><span class="line">    mDisplayToken = SurfaceComposerClient::getInternalDisplayToken();</span><br><span class="line">    <span class="keyword">if</span> (mDisplayToken == nullptr)</span><br><span class="line">        <span class="keyword">return</span> NAME_NOT_FOUND;</span><br><span class="line"></span><br><span class="line">    DisplayMode displayMode;</span><br><span class="line">    <span class="keyword">const</span> status_t error =</span><br><span class="line">            SurfaceComposerClient::getActiveDisplayMode(mDisplayToken, &amp;displayMode);</span><br><span class="line">    <span class="keyword">if</span> (error != NO_ERROR)</span><br><span class="line">        <span class="keyword">return</span> error;</span><br><span class="line"></span><br><span class="line">    mMaxWidth = android::base::GetIntProperty(<span class="string">&quot;ro.surface_flinger.max_graphics_width&quot;</span>, <span class="number">0</span>);</span><br><span class="line">    mMaxHeight = android::base::GetIntProperty(<span class="string">&quot;ro.surface_flinger.max_graphics_height&quot;</span>, <span class="number">0</span>);</span><br><span class="line">    ui::Size resolution = displayMode.resolution;</span><br><span class="line">    resolution = limitSurfaceSize(resolution.width, resolution.height);</span><br><span class="line">    <span class="comment">// create the native surface</span></span><br><span class="line">    sp&lt;SurfaceControl&gt; control = session()-&gt;createSurface(String8(<span class="string">&quot;BootAnimation&quot;</span>),</span><br><span class="line">            resolution.getWidth(), resolution.getHeight(), PIXEL_FORMAT_RGB_565);</span><br><span class="line"></span><br><span class="line">    SurfaceComposerClient::Transaction t;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// this guest property specifies multi-display IDs to show the boot animation</span></span><br><span class="line">    <span class="comment">// multiple ids can be set with comma (,) as separator, for example:</span></span><br><span class="line">    <span class="comment">// setprop persist.boot.animation.displays 19260422155234049,19261083906282754</span></span><br><span class="line">    Vector&lt;PhysicalDisplayId&gt; physicalDisplayIds;</span><br><span class="line">    <span class="keyword">char</span> displayValue[PROPERTY_VALUE_MAX] = <span class="string">&quot;&quot;</span>;</span><br><span class="line">    property_get(DISPLAYS_PROP_NAME, displayValue, <span class="string">&quot;&quot;</span>);</span><br><span class="line">    bool isValid = displayValue[<span class="number">0</span>] != <span class="string">&#x27;\0&#x27;</span>;</span><br><span class="line">    <span class="keyword">if</span> (isValid) &#123;</span><br><span class="line">        <span class="keyword">char</span> *p = displayValue;</span><br><span class="line">        <span class="keyword">while</span> (*p) &#123;</span><br><span class="line">            <span class="keyword">if</span> (!isdigit(*p) &amp;&amp; *p != <span class="string">&#x27;,&#x27;</span>) &#123;</span><br><span class="line">                isValid = <span class="keyword">false</span>;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            p ++;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (!isValid)</span><br><span class="line">            SLOGE(<span class="string">&quot;Invalid syntax for the value of system prop: %s&quot;</span>, DISPLAYS_PROP_NAME);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (isValid) &#123;</span><br><span class="line">        std::<span class="function">istringstream <span class="title">stream</span><span class="params">(displayValue)</span></span>;</span><br><span class="line">        <span class="keyword">for</span> (PhysicalDisplayId id; stream &gt;&gt; id.value; ) &#123;</span><br><span class="line">            physicalDisplayIds.add(id);</span><br><span class="line">            <span class="keyword">if</span> (stream.peek() == <span class="string">&#x27;,&#x27;</span>)</span><br><span class="line">                stream.ignore();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// In the case of multi-display, boot animation shows on the specified displays</span></span><br><span class="line">        <span class="comment">// in addition to the primary display</span></span><br><span class="line">        auto ids = SurfaceComposerClient::getPhysicalDisplayIds();</span><br><span class="line">        constexpr uint32_t LAYER_STACK = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span> (auto id : physicalDisplayIds) &#123;</span><br><span class="line">            <span class="keyword">if</span> (std::find(ids.begin(), ids.end(), id) != ids.end()) &#123;</span><br><span class="line">                sp&lt;IBinder&gt; token = SurfaceComposerClient::getPhysicalDisplayToken(id);</span><br><span class="line">                <span class="keyword">if</span> (token != nullptr)</span><br><span class="line">                    t.setDisplayLayerStack(token, LAYER_STACK);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        t.setLayerStack(control, LAYER_STACK);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    t.setLayer(control, <span class="number">0x40000000</span>)</span><br><span class="line">        .apply();</span><br><span class="line"></span><br><span class="line">    sp&lt;Surface&gt; s = control-&gt;getSurface();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// initialize opengl and egl</span></span><br><span class="line">    EGLDisplay display = eglGetDisplay(EGL_DEFAULT_DISPLAY);</span><br><span class="line">    eglInitialize(display, nullptr, nullptr);</span><br><span class="line">    EGLConfig config = getEglConfig(display);</span><br><span class="line">    EGLSurface surface = eglCreateWindowSurface(display, config, s.get(), nullptr);</span><br><span class="line">    EGLContext context = eglCreateContext(display, config, nullptr, nullptr);</span><br><span class="line">    EGLint w, h;</span><br><span class="line">    eglQuerySurface(display, surface, EGL_WIDTH, &amp;w);</span><br><span class="line">    eglQuerySurface(display, surface, EGL_HEIGHT, &amp;h);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (eglMakeCurrent(display, surface, surface, context) == EGL_FALSE)</span><br><span class="line">        <span class="keyword">return</span> NO_INIT;</span><br><span class="line"></span><br><span class="line">    mDisplay = display;</span><br><span class="line">    mContext = context;</span><br><span class="line">    mSurface = surface;</span><br><span class="line">    mWidth = w;</span><br><span class="line">    mHeight = h;</span><br><span class="line">    mFlingerSurfaceControl = control;</span><br><span class="line">    mFlingerSurface = s;</span><br><span class="line">    mTargetInset = -<span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">    projectSceneToWindow();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Register a display event receiver</span></span><br><span class="line">    mDisplayEventReceiver = std::make_unique&lt;DisplayEventReceiver&gt;();</span><br><span class="line">    status_t status = mDisplayEventReceiver-&gt;initCheck();</span><br><span class="line">    SLOGE_IF(status != NO_ERROR, <span class="string">&quot;Initialization of DisplayEventReceiver failed with status: %d&quot;</span>,</span><br><span class="line">            status);</span><br><span class="line">    mLooper-&gt;addFd(mDisplayEventReceiver-&gt;getFd(), <span class="number">0</span>, Looper::EVENT_INPUT,</span><br><span class="line">            <span class="keyword">new</span> DisplayEventCallback(<span class="keyword">this</span>), nullptr);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> NO_ERROR;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<ul>
<li>获取到SurfaceComposer</li>
<li>创建Surface</li>
<li>初始化opengl 和 egl</li>
<li>监听display事件</li>
</ul>
<h2 id="threadLoop-1"><a href="#threadLoop-1" class="headerlink" title="threadLoop()"></a>threadLoop()</h2><p>这部分可以说是真正的动画入口，threadLoop函数中会调用到具体的动画实现方法。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/cmds/bootanimation/BootAnimation.cpp</span></span><br><span class="line"></span><br><span class="line">bool BootAnimation::threadLoop() &#123;</span><br><span class="line">    bool result;</span><br><span class="line">    <span class="comment">// We have no bootanimation file, so we use the stock android logo</span></span><br><span class="line">    <span class="comment">// animation.</span></span><br><span class="line">    <span class="keyword">if</span> (mZipFileName.isEmpty()) &#123;</span><br><span class="line">        result = android();</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        result = movie();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    mCallbacks-&gt;shutdown();</span><br><span class="line">    eglMakeCurrent(mDisplay, EGL_NO_SURFACE, EGL_NO_SURFACE, EGL_NO_CONTEXT);</span><br><span class="line">    eglDestroyContext(mDisplay, mContext);</span><br><span class="line">    eglDestroySurface(mDisplay, mSurface);</span><br><span class="line">    mFlingerSurface.clear();</span><br><span class="line">    mFlingerSurfaceControl.clear();</span><br><span class="line">    eglTerminate(mDisplay);</span><br><span class="line">    eglReleaseThread();</span><br><span class="line">    IPCThreadState::self()-&gt;stopProcess();</span><br><span class="line">    <span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>如果线程起来后，mZipFileName还没初始化完成，则播放“原生动画”，否则就播放自定义的动画。<br>播放完成后，需要处理的资源释放与清理工作。</p>
<h1 id="播放动画"><a href="#播放动画" class="headerlink" title="播放动画"></a>播放动画</h1><h2 id="原生动画"><a href="#原生动画" class="headerlink" title="原生动画"></a>原生动画</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/cmds/bootanimation/BootAnimation.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">BootAnimation::android</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="built_in">SLOGD</span>(<span class="string">&quot;%sAnimationShownTiming start time: %&quot;</span> PRId64 <span class="string">&quot;ms&quot;</span>, mShuttingDown ? <span class="string">&quot;Shutdown&quot;</span> : <span class="string">&quot;Boot&quot;</span>,</span><br><span class="line">            <span class="built_in">elapsedRealtime</span>());</span><br><span class="line">    <span class="built_in">initTexture</span>(&amp;mAndroid[<span class="number">0</span>], mAssets, <span class="string">&quot;images/android-logo-mask.png&quot;</span>);</span><br><span class="line">    <span class="built_in">initTexture</span>(&amp;mAndroid[<span class="number">1</span>], mAssets, <span class="string">&quot;images/android-logo-shine.png&quot;</span>);</span><br><span class="line"></span><br><span class="line">    mCallbacks-&gt;<span class="built_in">init</span>(&#123;&#125;);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// clear screen</span></span><br><span class="line">    <span class="built_in">glShadeModel</span>(GL_FLAT);</span><br><span class="line">    <span class="built_in">glDisable</span>(GL_DITHER);</span><br><span class="line">    <span class="built_in">glDisable</span>(GL_SCISSOR_TEST);</span><br><span class="line">    <span class="built_in">glClearColor</span>(<span class="number">0</span>,<span class="number">0</span>,<span class="number">0</span>,<span class="number">1</span>);</span><br><span class="line">    <span class="built_in">glClear</span>(GL_COLOR_BUFFER_BIT);</span><br><span class="line">    <span class="built_in">eglSwapBuffers</span>(mDisplay, mSurface);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">glEnable</span>(GL_TEXTURE_2D);</span><br><span class="line">    <span class="built_in">glTexEnvx</span>(GL_TEXTURE_ENV, GL_TEXTURE_ENV_MODE, GL_REPLACE);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Blend state</span></span><br><span class="line">    <span class="built_in">glBlendFunc</span>(GL_SRC_ALPHA, GL_ONE_MINUS_SRC_ALPHA);</span><br><span class="line">    <span class="built_in">glTexEnvx</span>(GL_TEXTURE_ENV, GL_TEXTURE_ENV_MODE, GL_REPLACE);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">nsecs_t</span> startTime = <span class="built_in">systemTime</span>();</span><br><span class="line">    <span class="keyword">do</span> &#123;</span><br><span class="line">        <span class="built_in">processDisplayEvents</span>();</span><br><span class="line">        <span class="keyword">const</span> GLint xc = (mWidth  - mAndroid[<span class="number">0</span>].w) / <span class="number">2</span>;</span><br><span class="line">        <span class="keyword">const</span> GLint yc = (mHeight - mAndroid[<span class="number">0</span>].h) / <span class="number">2</span>;</span><br><span class="line">        <span class="function"><span class="keyword">const</span> Rect <span class="title">updateRect</span><span class="params">(xc, yc, xc + mAndroid[<span class="number">0</span>].w, yc + mAndroid[<span class="number">0</span>].h)</span></span>;</span><br><span class="line">        <span class="built_in">glScissor</span>(updateRect.left, mHeight - updateRect.bottom, updateRect.<span class="built_in">width</span>(),</span><br><span class="line">                updateRect.<span class="built_in">height</span>());</span><br><span class="line"></span><br><span class="line">        <span class="keyword">nsecs_t</span> now = <span class="built_in">systemTime</span>();</span><br><span class="line">        <span class="keyword">double</span> time = now - startTime;</span><br><span class="line">        <span class="keyword">float</span> t = <span class="number">4.0f</span> * <span class="built_in"><span class="keyword">float</span></span>(time / <span class="built_in">us2ns</span>(<span class="number">16667</span>)) / mAndroid[<span class="number">1</span>].w;</span><br><span class="line">        GLint offset = (<span class="number">1</span> - (t - <span class="built_in">floorf</span>(t))) * mAndroid[<span class="number">1</span>].w;</span><br><span class="line">        GLint x = xc - offset;</span><br><span class="line"></span><br><span class="line">        <span class="built_in">glDisable</span>(GL_SCISSOR_TEST);</span><br><span class="line">        <span class="built_in">glClear</span>(GL_COLOR_BUFFER_BIT);</span><br><span class="line"></span><br><span class="line">        <span class="built_in">glEnable</span>(GL_SCISSOR_TEST);</span><br><span class="line">        <span class="built_in">glDisable</span>(GL_BLEND);</span><br><span class="line">        <span class="built_in">glBindTexture</span>(GL_TEXTURE_2D, mAndroid[<span class="number">1</span>].name);</span><br><span class="line">        <span class="built_in">glDrawTexiOES</span>(x,                 yc, <span class="number">0</span>, mAndroid[<span class="number">1</span>].w, mAndroid[<span class="number">1</span>].h);</span><br><span class="line">        <span class="built_in">glDrawTexiOES</span>(x + mAndroid[<span class="number">1</span>].w, yc, <span class="number">0</span>, mAndroid[<span class="number">1</span>].w, mAndroid[<span class="number">1</span>].h);</span><br><span class="line"></span><br><span class="line">        <span class="built_in">glEnable</span>(GL_BLEND);</span><br><span class="line">        <span class="built_in">glBindTexture</span>(GL_TEXTURE_2D, mAndroid[<span class="number">0</span>].name);</span><br><span class="line">        <span class="built_in">glDrawTexiOES</span>(xc, yc, <span class="number">0</span>, mAndroid[<span class="number">0</span>].w, mAndroid[<span class="number">0</span>].h);</span><br><span class="line"></span><br><span class="line">        EGLBoolean res = <span class="built_in">eglSwapBuffers</span>(mDisplay, mSurface);</span><br><span class="line">        <span class="keyword">if</span> (res == EGL_FALSE)</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 12fps: don&#x27;t animate too fast to preserve CPU</span></span><br><span class="line">        <span class="keyword">const</span> <span class="keyword">nsecs_t</span> sleepTime = <span class="number">83333</span> - <span class="built_in">ns2us</span>(<span class="built_in">systemTime</span>() - now);</span><br><span class="line">        <span class="keyword">if</span> (sleepTime &gt; <span class="number">0</span>)</span><br><span class="line">            <span class="built_in">usleep</span>(sleepTime);</span><br><span class="line"></span><br><span class="line">        <span class="built_in">checkExit</span>();</span><br><span class="line">    &#125; <span class="keyword">while</span> (!<span class="built_in">exitPending</span>());</span><br><span class="line"></span><br><span class="line">    <span class="built_in">glDeleteTextures</span>(<span class="number">1</span>, &amp;mAndroid[<span class="number">0</span>].name);</span><br><span class="line">    <span class="built_in">glDeleteTextures</span>(<span class="number">1</span>, &amp;mAndroid[<span class="number">1</span>].name);</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">status_t</span> <span class="title">BootAnimation::initTexture</span><span class="params">(Texture* texture, AssetManager&amp; assets,</span></span></span><br><span class="line"><span class="params"><span class="function">        <span class="keyword">const</span> <span class="keyword">char</span>* name)</span> </span>&#123;</span><br><span class="line">    Asset* asset = assets.<span class="built_in">open</span>(name, Asset::ACCESS_BUFFER);</span><br><span class="line">    <span class="keyword">if</span> (asset == <span class="literal">nullptr</span>)</span><br><span class="line">        <span class="keyword">return</span> NO_INIT;</span><br><span class="line"></span><br><span class="line">    AndroidBitmapInfo bitmapInfo;</span><br><span class="line">    <span class="keyword">void</span>* pixels = <span class="built_in">decodeImage</span>(asset-&gt;<span class="built_in">getBuffer</span>(<span class="literal">false</span>), asset-&gt;<span class="built_in">getLength</span>(), &amp;bitmapInfo);</span><br><span class="line">    <span class="keyword">auto</span> pixelDeleter = std::unique_ptr&lt;<span class="keyword">void</span>, <span class="keyword">decltype</span>(free)*&gt;&#123; pixels, free &#125;;</span><br><span class="line"></span><br><span class="line">    asset-&gt;<span class="built_in">close</span>();</span><br><span class="line">    <span class="keyword">delete</span> asset;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!pixels) &#123;</span><br><span class="line">        <span class="keyword">return</span> NO_INIT;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">int</span> w = bitmapInfo.width;</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">int</span> h = bitmapInfo.height;</span><br><span class="line"></span><br><span class="line">    GLint crop[<span class="number">4</span>] = &#123; <span class="number">0</span>, h, w, -h &#125;;</span><br><span class="line">    texture-&gt;w = w;</span><br><span class="line">    texture-&gt;h = h;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">glGenTextures</span>(<span class="number">1</span>, &amp;texture-&gt;name);</span><br><span class="line">    <span class="built_in">glBindTexture</span>(GL_TEXTURE_2D, texture-&gt;name);</span><br><span class="line"></span><br><span class="line">    <span class="built_in"><span class="keyword">switch</span></span> (bitmapInfo.format) &#123;</span><br><span class="line">        <span class="keyword">case</span> ANDROID_BITMAP_FORMAT_A_8:</span><br><span class="line">            <span class="built_in">glTexImage2D</span>(GL_TEXTURE_2D, <span class="number">0</span>, GL_ALPHA, w, h, <span class="number">0</span>, GL_ALPHA,</span><br><span class="line">                    GL_UNSIGNED_BYTE, pixels);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> ANDROID_BITMAP_FORMAT_RGBA_4444:</span><br><span class="line">            <span class="built_in">glTexImage2D</span>(GL_TEXTURE_2D, <span class="number">0</span>, GL_RGBA, w, h, <span class="number">0</span>, GL_RGBA,</span><br><span class="line">                    GL_UNSIGNED_SHORT_4_4_4_4, pixels);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> ANDROID_BITMAP_FORMAT_RGBA_8888:</span><br><span class="line">            <span class="built_in">glTexImage2D</span>(GL_TEXTURE_2D, <span class="number">0</span>, GL_RGBA, w, h, <span class="number">0</span>, GL_RGBA,</span><br><span class="line">                    GL_UNSIGNED_BYTE, pixels);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> ANDROID_BITMAP_FORMAT_RGB_565:</span><br><span class="line">            <span class="built_in">glTexImage2D</span>(GL_TEXTURE_2D, <span class="number">0</span>, GL_RGB, w, h, <span class="number">0</span>, GL_RGB,</span><br><span class="line">                    GL_UNSIGNED_SHORT_5_6_5, pixels);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">default</span>:</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">glTexParameteriv</span>(GL_TEXTURE_2D, GL_TEXTURE_CROP_RECT_OES, crop);</span><br><span class="line">    <span class="built_in">glTexParameterx</span>(GL_TEXTURE_2D, GL_TEXTURE_MIN_FILTER, GL_NEAREST);</span><br><span class="line">    <span class="built_in">glTexParameterx</span>(GL_TEXTURE_2D, GL_TEXTURE_MAG_FILTER, GL_NEAREST);</span><br><span class="line">    <span class="built_in">glTexParameterx</span>(GL_TEXTURE_2D, GL_TEXTURE_WRAP_S, GL_REPEAT);</span><br><span class="line">    <span class="built_in">glTexParameterx</span>(GL_TEXTURE_2D, GL_TEXTURE_WRAP_T, GL_REPEAT);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> NO_ERROR;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>原生的开机动画位于<a href="http://aospxref.com/android-12.0.0_r3/xref/frameworks/base/core/res/assets/images/">frameworks/base/core/res/assets/images</a>目录下，由android-logo-shine.png和android-logo-mask.png两张png图片组成。熟悉framework-res的同学都知道这两张图片其实是编译到了framework-res.apk中。initTexture()函数会根据这两张图片来分别创建两个纹理对象，并存储在Bootanimation类的成员变量数组mAndroid中。通过混合渲染这两个纹理对象，我们就可以得到一个开机动画，这是通过中间的while循环语句来实现的。<br>图片android-logo-mask.png用作动画前景，它是一个镂空的“ANDROID”图像。图片android-logo-shine.png用作动画背景，它的中间包含有一个高亮的呈45度角的条纹。在每一次循环中，图片android-logo-shine.png被划分成左右两部分内容来显示。左右两个部分的图像宽度随着时间的推移而此消彼长，这样就可以使得图片android-logo-shine.png中间高亮的条纹好像在移动一样。另一方面，在每一次循环中，图片android-logo-shine.png都作为一个整体来渲染，而且它的位置是恒定不变的。由于它是一个镂空的“ANDROID”图像，因此，我们就可以通过它的镂空来看到它背后的图片android-logo-shine.png的条纹一闪一闪地划过。</p>
<h2 id="自定义动画"><a href="#自定义动画" class="headerlink" title="自定义动画"></a>自定义动画</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/cmds/bootanimation/BootAnimation.cpp</span></span><br><span class="line"></span><br><span class="line">bool BootAnimation::movie() &#123;</span><br><span class="line">    <span class="keyword">if</span> (mAnimation == nullptr) &#123;</span><br><span class="line">        mAnimation = loadAnimation(mZipFileName);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (mAnimation == nullptr)</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// mCallbacks-&gt;init() may get called recursively,</span></span><br><span class="line">    <span class="comment">// this loop is needed to get the same results</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">const</span> Animation::Part&amp; part : mAnimation-&gt;parts) &#123;</span><br><span class="line">        <span class="keyword">if</span> (part.animation != nullptr) &#123;</span><br><span class="line">            mCallbacks-&gt;init(part.animation-&gt;parts);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    mCallbacks-&gt;init(mAnimation-&gt;parts);</span><br><span class="line"></span><br><span class="line">    bool anyPartHasClock = <span class="keyword">false</span>;</span><br><span class="line">    <span class="keyword">for</span> (size_t i=<span class="number">0</span>; i &lt; mAnimation-&gt;parts.size(); i++) &#123;</span><br><span class="line">        <span class="keyword">if</span>(validClock(mAnimation-&gt;parts[i])) &#123;</span><br><span class="line">            anyPartHasClock = <span class="keyword">true</span>;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (!anyPartHasClock) &#123;</span><br><span class="line">        mClockEnabled = <span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Check if npot textures are supported</span></span><br><span class="line">    mUseNpotTextures = <span class="keyword">false</span>;</span><br><span class="line">    String8 gl_extensions;</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span>* exts = reinterpret_cast&lt;<span class="keyword">const</span> <span class="keyword">char</span>*&gt;(glGetString(GL_EXTENSIONS));</span><br><span class="line">    <span class="keyword">if</span> (!exts) &#123;</span><br><span class="line">        glGetError();</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        gl_extensions.setTo(exts);</span><br><span class="line">        <span class="keyword">if</span> ((gl_extensions.find(<span class="string">&quot;GL_ARB_texture_non_power_of_two&quot;</span>) != -<span class="number">1</span>) ||</span><br><span class="line">            (gl_extensions.find(<span class="string">&quot;GL_OES_texture_npot&quot;</span>) != -<span class="number">1</span>)) &#123;</span><br><span class="line">            mUseNpotTextures = <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Blend required to draw time on top of animation frames.</span></span><br><span class="line">    glBlendFunc(GL_SRC_ALPHA, GL_ONE_MINUS_SRC_ALPHA);</span><br><span class="line">    glShadeModel(GL_FLAT);</span><br><span class="line">    glDisable(GL_DITHER);</span><br><span class="line">    glDisable(GL_SCISSOR_TEST);</span><br><span class="line">    glDisable(GL_BLEND);</span><br><span class="line"></span><br><span class="line">    glBindTexture(GL_TEXTURE_2D, <span class="number">0</span>);</span><br><span class="line">    glEnable(GL_TEXTURE_2D);</span><br><span class="line">    glTexEnvx(GL_TEXTURE_ENV, GL_TEXTURE_ENV_MODE, GL_REPLACE);</span><br><span class="line">    glTexParameterx(GL_TEXTURE_2D, GL_TEXTURE_WRAP_S, GL_REPEAT);</span><br><span class="line">    glTexParameterx(GL_TEXTURE_2D, GL_TEXTURE_WRAP_T, GL_REPEAT);</span><br><span class="line">    glTexParameterx(GL_TEXTURE_2D, GL_TEXTURE_MIN_FILTER, GL_LINEAR);</span><br><span class="line">    glTexParameterx(GL_TEXTURE_2D, GL_TEXTURE_MAG_FILTER, GL_LINEAR);</span><br><span class="line"></span><br><span class="line">    bool clockFontInitialized = <span class="keyword">false</span>;</span><br><span class="line">    <span class="keyword">if</span> (mClockEnabled) &#123;</span><br><span class="line">        clockFontInitialized =</span><br><span class="line">            (initFont(&amp;mAnimation-&gt;clockFont, CLOCK_FONT_ASSET) == NO_ERROR);</span><br><span class="line">        mClockEnabled = clockFontInitialized;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    initFont(&amp;mAnimation-&gt;progressFont, PROGRESS_FONT_ASSET);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (mClockEnabled &amp;&amp; !updateIsTimeAccurate()) &#123;</span><br><span class="line">        mTimeCheckThread = <span class="keyword">new</span> TimeCheckThread(<span class="keyword">this</span>);</span><br><span class="line">        mTimeCheckThread-&gt;run(<span class="string">&quot;BootAnimation::TimeCheckThread&quot;</span>, PRIORITY_NORMAL);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    playAnimation(*mAnimation);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (mTimeCheckThread != nullptr) &#123;</span><br><span class="line">        mTimeCheckThread-&gt;requestExit();</span><br><span class="line">        mTimeCheckThread = nullptr;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (clockFontInitialized) &#123;</span><br><span class="line">        glDeleteTextures(<span class="number">1</span>, &amp;mAnimation-&gt;clockFont.texture.name);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    releaseAnimation(mAnimation);</span><br><span class="line">    mAnimation = nullptr;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>如果在onFirstRef()的流程里还没有加载好动画，则需要调用loadAnimation加载动画。（参考之前的流程）</p>
</li>
<li><p>调用playAnimation()播放动画。</p>
</li>
<li><p>动画播放结束后，释放资源。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/cmds/bootanimation/BootAnimation.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">BootAnimation::playAnimation</span><span class="params">(<span class="keyword">const</span> Animation&amp; animation)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">size_t</span> pcount = animation.parts.<span class="built_in">size</span>();</span><br><span class="line">    <span class="keyword">nsecs_t</span> frameDuration = <span class="built_in">s2ns</span>(<span class="number">1</span>) / animation.fps;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">SLOGD</span>(<span class="string">&quot;%sAnimationShownTiming start time: %&quot;</span> PRId64 <span class="string">&quot;ms&quot;</span>, mShuttingDown ? <span class="string">&quot;Shutdown&quot;</span> : <span class="string">&quot;Boot&quot;</span>,</span><br><span class="line">            <span class="built_in">elapsedRealtime</span>());</span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> fadedFramesCount = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">int</span> lastDisplayedProgress = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">size_t</span> i=<span class="number">0</span> ; i&lt;pcount ; i++) &#123;</span><br><span class="line">        <span class="function"><span class="keyword">const</span> Animation::Part&amp; <span class="title">part</span><span class="params">(animation.parts[i])</span></span>;</span><br><span class="line">        <span class="keyword">const</span> <span class="keyword">size_t</span> fcount = part.frames.<span class="built_in">size</span>();</span><br><span class="line">        <span class="built_in">glBindTexture</span>(GL_TEXTURE_2D, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// Handle animation package</span></span><br><span class="line">        <span class="keyword">if</span> (part.animation != <span class="literal">nullptr</span>) &#123;</span><br><span class="line">            <span class="built_in">playAnimation</span>(*part.animation);</span><br><span class="line">            <span class="keyword">if</span> (<span class="built_in">exitPending</span>())</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="keyword">continue</span>; <span class="comment">//to next part</span></span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// process the part not only while the count allows but also if already fading</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> r=<span class="number">0</span> ; !part.count || r&lt;part.count || fadedFramesCount &gt; <span class="number">0</span> ; r++) &#123;</span><br><span class="line">            <span class="keyword">if</span> (<span class="built_in">shouldStopPlayingPart</span>(part, fadedFramesCount, lastDisplayedProgress)) <span class="keyword">break</span>;</span><br><span class="line"></span><br><span class="line">            mCallbacks-&gt;<span class="built_in">playPart</span>(i, part, r);</span><br><span class="line"></span><br><span class="line">            <span class="built_in">glClearColor</span>(</span><br><span class="line">                    part.backgroundColor[<span class="number">0</span>],</span><br><span class="line">                    part.backgroundColor[<span class="number">1</span>],</span><br><span class="line">                    part.backgroundColor[<span class="number">2</span>],</span><br><span class="line">                    <span class="number">1.0f</span>);</span><br><span class="line"></span><br><span class="line">            <span class="comment">// For the last animation, if we have progress indicator from</span></span><br><span class="line">            <span class="comment">// the system, display it.</span></span><br><span class="line">            <span class="keyword">int</span> currentProgress = android::base::<span class="built_in">GetIntProperty</span>(PROGRESS_PROP_NAME, <span class="number">0</span>);</span><br><span class="line">            <span class="keyword">bool</span> displayProgress = animation.progressEnabled &amp;&amp;</span><br><span class="line">                (i == (pcount <span class="number">-1</span>)) &amp;&amp; currentProgress != <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">size_t</span> j=<span class="number">0</span> ; j&lt;fcount ; j++) &#123;</span><br><span class="line">                <span class="keyword">if</span> (<span class="built_in">shouldStopPlayingPart</span>(part, fadedFramesCount, lastDisplayedProgress)) <span class="keyword">break</span>;</span><br><span class="line"></span><br><span class="line">                <span class="built_in">processDisplayEvents</span>();</span><br><span class="line"></span><br><span class="line">                <span class="keyword">const</span> <span class="keyword">int</span> animationX = (mWidth - animation.width) / <span class="number">2</span>;</span><br><span class="line">                <span class="keyword">const</span> <span class="keyword">int</span> animationY = (mHeight - animation.height) / <span class="number">2</span>;</span><br><span class="line"></span><br><span class="line">                <span class="function"><span class="keyword">const</span> Animation::Frame&amp; <span class="title">frame</span><span class="params">(part.frames[j])</span></span>;</span><br><span class="line">                <span class="keyword">nsecs_t</span> lastFrame = <span class="built_in">systemTime</span>();</span><br><span class="line"></span><br><span class="line">                <span class="keyword">if</span> (r &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                    <span class="built_in">glBindTexture</span>(GL_TEXTURE_2D, frame.tid);</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="keyword">if</span> (part.count != <span class="number">1</span>) &#123;</span><br><span class="line">                        <span class="built_in">glGenTextures</span>(<span class="number">1</span>, &amp;frame.tid);</span><br><span class="line">                        <span class="built_in">glBindTexture</span>(GL_TEXTURE_2D, frame.tid);</span><br><span class="line">                        <span class="built_in">glTexParameterx</span>(GL_TEXTURE_2D, GL_TEXTURE_MIN_FILTER, GL_LINEAR);</span><br><span class="line">                        <span class="built_in">glTexParameterx</span>(GL_TEXTURE_2D, GL_TEXTURE_MAG_FILTER, GL_LINEAR);</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="keyword">int</span> w, h;</span><br><span class="line">                    <span class="built_in">initTexture</span>(frame.map, &amp;w, &amp;h);</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                <span class="keyword">const</span> <span class="keyword">int</span> xc = animationX + frame.trimX;</span><br><span class="line">                <span class="keyword">const</span> <span class="keyword">int</span> yc = animationY + frame.trimY;</span><br><span class="line">                <span class="function">Region <span class="title">clearReg</span><span class="params">(Rect(mWidth, mHeight))</span></span>;</span><br><span class="line">                clearReg.<span class="built_in">subtractSelf</span>(<span class="built_in">Rect</span>(xc, yc, xc+frame.trimWidth, yc+frame.trimHeight));</span><br><span class="line">                <span class="keyword">if</span> (!clearReg.<span class="built_in">isEmpty</span>()) &#123;</span><br><span class="line">                    <span class="function">Region::const_iterator <span class="title">head</span><span class="params">(clearReg.begin())</span></span>;</span><br><span class="line">                    <span class="function">Region::const_iterator <span class="title">tail</span><span class="params">(clearReg.end())</span></span>;</span><br><span class="line">                    <span class="built_in">glEnable</span>(GL_SCISSOR_TEST);</span><br><span class="line">                    <span class="keyword">while</span> (head != tail) &#123;</span><br><span class="line">                        <span class="function"><span class="keyword">const</span> Rect&amp; <span class="title">r2</span><span class="params">(*head++)</span></span>;</span><br><span class="line">                        <span class="built_in">glScissor</span>(r2.left, mHeight - r2.bottom, r2.<span class="built_in">width</span>(), r2.<span class="built_in">height</span>());</span><br><span class="line">                        <span class="built_in">glClear</span>(GL_COLOR_BUFFER_BIT);</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="built_in">glDisable</span>(GL_SCISSOR_TEST);</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="comment">// specify the y center as ceiling((mHeight - frame.trimHeight) / 2)</span></span><br><span class="line">                <span class="comment">// which is equivalent to mHeight - (yc + frame.trimHeight)</span></span><br><span class="line">                <span class="keyword">const</span> <span class="keyword">int</span> frameDrawY = mHeight - (yc + frame.trimHeight);</span><br><span class="line">                <span class="built_in">glDrawTexiOES</span>(xc, frameDrawY, <span class="number">0</span>, frame.trimWidth, frame.trimHeight);</span><br><span class="line"></span><br><span class="line">                <span class="comment">// if the part hasn&#x27;t been stopped yet then continue fading if necessary</span></span><br><span class="line">                <span class="keyword">if</span> (<span class="built_in">exitPending</span>() &amp;&amp; part.<span class="built_in">hasFadingPhase</span>()) &#123;</span><br><span class="line">                    <span class="built_in">fadeFrame</span>(xc, frameDrawY, frame.trimWidth, frame.trimHeight, part,</span><br><span class="line">                              ++fadedFramesCount);</span><br><span class="line">                    <span class="keyword">if</span> (fadedFramesCount &gt;= part.framesToFadeCount) &#123;</span><br><span class="line">                        fadedFramesCount = MAX_FADED_FRAMES_COUNT; <span class="comment">// no more fading</span></span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                <span class="keyword">if</span> (mClockEnabled &amp;&amp; mTimeIsAccurate &amp;&amp; <span class="built_in">validClock</span>(part)) &#123;</span><br><span class="line">                    <span class="built_in">drawClock</span>(animation.clockFont, part.clockPosX, part.clockPosY);</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                <span class="keyword">if</span> (displayProgress) &#123;</span><br><span class="line">                    <span class="keyword">int</span> newProgress = android::base::<span class="built_in">GetIntProperty</span>(PROGRESS_PROP_NAME, <span class="number">0</span>);</span><br><span class="line">                    <span class="comment">// In case the new progress jumped suddenly, still show an</span></span><br><span class="line">                    <span class="comment">// increment of 1.</span></span><br><span class="line">                    <span class="keyword">if</span> (lastDisplayedProgress != <span class="number">100</span>) &#123;</span><br><span class="line">                      <span class="comment">// Artificially sleep 1/10th a second to slow down the animation.</span></span><br><span class="line">                      <span class="built_in">usleep</span>(<span class="number">100000</span>);</span><br><span class="line">                      <span class="keyword">if</span> (lastDisplayedProgress &lt; newProgress) &#123;</span><br><span class="line">                        lastDisplayedProgress++;</span><br><span class="line">                      &#125;</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="comment">// Put the progress percentage right below the animation.</span></span><br><span class="line">                    <span class="keyword">int</span> posY = animation.height / <span class="number">3</span>;</span><br><span class="line">                    <span class="keyword">int</span> posX = TEXT_CENTER_VALUE;</span><br><span class="line">                    <span class="built_in">drawProgress</span>(lastDisplayedProgress, animation.progressFont, posX, posY);</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                <span class="built_in">handleViewport</span>(frameDuration);</span><br><span class="line"></span><br><span class="line">                <span class="built_in">eglSwapBuffers</span>(mDisplay, mSurface);</span><br><span class="line"></span><br><span class="line">                <span class="keyword">nsecs_t</span> now = <span class="built_in">systemTime</span>();</span><br><span class="line">                <span class="keyword">nsecs_t</span> delay = frameDuration - (now - lastFrame);</span><br><span class="line">                <span class="comment">//SLOGD(&quot;%lld, %lld&quot;, ns2ms(now - lastFrame), ns2ms(delay));</span></span><br><span class="line">                lastFrame = now;</span><br><span class="line"></span><br><span class="line">                <span class="keyword">if</span> (delay &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                    <span class="class"><span class="keyword">struct</span> <span class="title">timespec</span> <span class="title">spec</span>;</span></span><br><span class="line">                    spec.tv_sec  = (now + delay) / <span class="number">1000000000</span>;</span><br><span class="line">                    spec.tv_nsec = (now + delay) % <span class="number">1000000000</span>;</span><br><span class="line">                    <span class="keyword">int</span> err;</span><br><span class="line">                    <span class="keyword">do</span> &#123;</span><br><span class="line">                        err = <span class="built_in">clock_nanosleep</span>(CLOCK_MONOTONIC, TIMER_ABSTIME, &amp;spec, <span class="literal">nullptr</span>);</span><br><span class="line">                    &#125; <span class="keyword">while</span> (err&lt;<span class="number">0</span> &amp;&amp; errno == EINTR);</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                <span class="built_in">checkExit</span>();</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="built_in">usleep</span>(part.pause * <span class="built_in">ns2us</span>(frameDuration));</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (<span class="built_in">exitPending</span>() &amp;&amp; !part.count &amp;&amp; mCurrentInset &gt;= mTargetInset &amp;&amp;</span><br><span class="line">                !part.<span class="built_in">hasFadingPhase</span>()) &#123;</span><br><span class="line">                <span class="keyword">if</span> (lastDisplayedProgress != <span class="number">0</span> &amp;&amp; lastDisplayedProgress != <span class="number">100</span>) &#123;</span><br><span class="line">                    android::base::<span class="built_in">SetProperty</span>(PROGRESS_PROP_NAME, <span class="string">&quot;100&quot;</span>);</span><br><span class="line">                    <span class="keyword">continue</span>;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">break</span>; <span class="comment">// exit the infinite non-fading part when it has been played at least once</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Free textures created for looping parts now that the animation is done.</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">const</span> Animation::Part&amp; part : animation.parts) &#123;</span><br><span class="line">        <span class="keyword">if</span> (part.count != <span class="number">1</span>) &#123;</span><br><span class="line">            <span class="keyword">const</span> <span class="keyword">size_t</span> fcount = part.frames.<span class="built_in">size</span>();</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">size_t</span> j = <span class="number">0</span>; j &lt; fcount; j++) &#123;</span><br><span class="line">                <span class="function"><span class="keyword">const</span> Animation::Frame&amp; <span class="title">frame</span><span class="params">(part.frames[j])</span></span>;</span><br><span class="line">                <span class="built_in">glDeleteTextures</span>(<span class="number">1</span>, &amp;frame.tid);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>const size_t pcount = animation.parts.size(); 也就是desc.txt里播放片段的数量；所以for (size_t i=0 ; i&lt;pcount ; i++) 循环是有多少个播放片段就循环多少次。</p>
</li>
<li><p>在preloadZip()函数里会把每一个播放片段包含的帧数存在Part.frames，如：part.frames.add(frame)；所以for (size_t j=0 ; j&lt;fcount ; j++) 循环是当前播放有多少张图片就循环多少次。</p>
</li>
<li><p>调用shouldStopPlayingPart()判断是否要退出动画。（前面聊标识符和动画淡出帧的时候有介绍）</p>
</li>
<li><p>调用initTexture()显示</p>
</li>
<li><p>drawClock()显示时间</p>
</li>
<li><p>drawProgress()是否显示进度（Android12.0新增功能）</p>
</li>
<li><p>checkExit()检查是否退出动画</p>
</li>
</ul>
<h1 id="退出动画"><a href="#退出动画" class="headerlink" title="退出动画"></a>退出动画</h1><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/cmds/bootanimation/BootAnimation.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">const</span> <span class="keyword">char</span> EXIT_PROP_NAME[] = <span class="string">&quot;service.bootanim.exit&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">BootAnimation::checkExit</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="comment">// Allow surface flinger to gracefully request shutdown</span></span><br><span class="line">    <span class="keyword">char</span> value[PROPERTY_VALUE_MAX];</span><br><span class="line">    <span class="built_in">property_get</span>(EXIT_PROP_NAME, value, <span class="string">&quot;0&quot;</span>);</span><br><span class="line">    <span class="keyword">int</span> exitnow = <span class="built_in">atoi</span>(value);</span><br><span class="line">    <span class="keyword">if</span> (exitnow) &#123;</span><br><span class="line">        <span class="built_in">requestExit</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<pre><code>检测service.bootanim.exit的值，当属性值为1的时候，开机动画会requestExit()，从而结束开机动画。这个属性值的更改主要涉及以下内容：
</code></pre>
<p><img src="https://cdn.nlark.com/yuque/0/2022/png/1759879/1656665331226-ca1cedfa-4e0c-47f1-ad33-1826b8d6f3e5.png#clientId=udd0090bf-37f1-4&crop=0&crop=0&crop=1&crop=1&from=paste&height=1424&id=u9d1002c1&margin=%5Bobject%20Object%5D&name=173a11d95b404190b7139ace70ae0d21.png&originHeight=2848&originWidth=3700&originalType=binary&ratio=1&rotation=0&showTitle=false&size=329132&status=done&style=none&taskId=udfe229d5-fc93-4982-a1ee-0418af30f87&title=&width=1850" alt="173a11d95b404190b7139ace70ae0d21.png"></p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>BootAnimation.cpp方法主要作用如下:</p>
<ul>
<li>onFirstRef() : 建立BootAnimation进程与surfaceFlinger进程的通信，及加载资源</li>
<li>preloadAnimation() : 加载开机动画资源文件</li>
<li>findBootAnimationFile() : 主要是初始化 mZipFileName</li>
<li>findBootAnimationFileInternal() : 将mZipFileName存入索引</li>
<li>loadAnimation() : 解析资源，加载动画文件，这里的mZipFileName就是在readyToRun中获取的动画文件位置</li>
<li>parseAnimationDesc() : 解析读取desc.txt文件，设置相应animation参数</li>
<li>parseColor() : 解析颜色</li>
<li>parseTextCoord() : 解析文本坐标</li>
<li>parsePosition() : 解析位置</li>
<li>readFile() : 读取文件</li>
<li>preloadZip() : 用于图像的预加载阶段</li>
<li>decodeImage() : 解析图像信息，并存储</li>
<li>readyToRun() : 判断开机动画的压缩包是否存在，主要是对opengl工作环境进行初始化，初始化EGL环境，为送图做准备工作，做一个动画播放的预操作</li>
<li>threadLoop() : 显示开机画面,每个线程类都要实现的，在这里定义thread的执行内容。这个函数如果返回true，且没有调用requestExit()，则该函数会再次执行；如果返回false，则threadloop中的内容仅仅执行一次，线程就会退出。</li>
<li>doThreadLoop() : 超时检测线程的执行函数</li>
<li>playAnimation() : 会拿到 mAnimation的图片，还有desc.txt中定义的图片分辨率，帧率等信息，依次播放part0,part1中图片，合成Surface，然后调用eglSwapBuffers(mDisplay, mSurface);动图给显示设备．</li>
<li>movie() : 自定义的开机动画</li>
<li>getEglConfig() : 绘制目标framebuffer的配置属性及显示窗口内容</li>
<li>limitSurfaceSize() : 该方法的作用是将width和height限制在设备GPU支持的范围内</li>
<li>resizeSurface() : 调整开机动画的surface大小</li>
<li>releaseAnimation() : 释放动画资源</li>
<li>initFont() : 加载字体资源</li>
<li>initTexture() : 加载系统默认UI资源，通过decodeImage来解码图片，并显示在SurfaceLayer之上</li>
<li>drawClock() : 绘制时钟进行当前时间的显示</li>
<li>updateIsTimeAccurate() : 记录最新修改的时间</li>
<li>addTimeDirWatch() : 增加时间监测</li>
<li>handleEvent() : handle事件，更新UI操作信息</li>
<li>handleViewport() : 负责图表视图中可见的内容</li>
<li>processDisplayEvents() : 处理显示事件</li>
<li>checkExit() : 检测开机动画是否停止</li>
<li>binderDied() : 当Binder机制的客户端死掉，导致了该Binder结束，会回调此方法(此处一般指surfaceflinger）</li>
<li>DisplayEventCallback : 进行事件的处理以及调用surfaceComposerClient里的getPhysicalDisplayToken()进行物理屏幕的显示</li>
<li>TimeCheckThread : 超时检测机制线程</li>
</ul>
<p>整个BootAnimation程序的流程图下图：<br><img src="https://cdn.nlark.com/yuque/0/2022/png/1759879/1656667809093-d90fc04f-88a5-4aee-82e6-cb392d78464d.png#clientId=udd0090bf-37f1-4&crop=0&crop=0&crop=1&crop=1&from=paste&id=ud3788e45&margin=%5Bobject%20Object%5D&name=7a7ecc7caf394453a981c4e936d4469d.png&originHeight=4335&originWidth=1329&originalType=binary&ratio=1&rotation=0&showTitle=false&size=251730&status=done&style=none&taskId=uf75400d2-e836-4621-9f96-718800e2020&title=" alt="7a7ecc7caf394453a981c4e936d4469d.png"></p>
]]></content>
      <categories>
        <category>Android</category>
      </categories>
      <tags>
        <tag>Android</tag>
        <tag>bootanimation</tag>
      </tags>
  </entry>
  <entry>
    <title>tmux介绍</title>
    <url>/archives/1717e1a3.html</url>
    <content><![CDATA[<span id="more"></span>
<meta name="referrer" content="no-referrer"/>

<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>tmux 是一个终端复用器（terminal multiplexer），非常有用，属于常用的开发工具。<br>我们在linux服务器上的工作一般都是通过一个远程的终端连接软件连接到远端系统进行操作，例如使用xshell等工具通过ssh进行远程连接。在使用过程中，如果要做比较耗时的操作，例如有时候进行编译，或者下载大文件需要比较长的时间，一般情况下是下班之后直接运行希望第二天早上过来运行完成，这样就不用耽误工作时间。但是网络有时候不稳定，可能在半夜会出现连接断掉的情况，一旦连接断掉，我们所执行的程序也就中断，我们当然可以写一个脚本后台运行，但是还是不方便。</p>
<h2 id="会话与进程"><a href="#会话与进程" class="headerlink" title="会话与进程"></a>会话与进程</h2><p>用户与计算机的这种临时的交互，称为一次”会话”（session）。会话的一个重要特点是，窗口与其中启动的进程是连在一起的。打开窗口，会话开始；关闭窗口，会话结束，会话内部的进程也会随之终止，不管有没有运行完。一个典型的例子就是，SSH 登录远程计算机，打开一个远程窗口执行命令。这时，网络突然断线，再次登录的时候，是找不回上一次执行的命令的。因为上一次 SSH 会话已经终止了，里面的进程也随之消失了。<br>为了解决这个问题，会话与窗口可以”解绑”：窗口关闭时，会话并不终止，而是继续运行，等到以后需要的时候，再让会话”绑定”其他窗口。</p>
<h1 id="tmux"><a href="#tmux" class="headerlink" title="tmux"></a>tmux</h1><h2 id="优势"><a href="#优势" class="headerlink" title="优势"></a>优势</h2><ul>
<li>它允许在单个窗口中，同时访问多个会话。这对于同时运行多个命令行程序很有用。</li>
<li>它可以让新窗口”接入”已经存在的会话。</li>
<li>它允许每个会话有多个连接窗口，因此可以多人实时共享会话。</li>
<li>它还支持窗口任意的垂直和水平拆分。</li>
</ul>
<h2 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h2><p>在tmux逻辑中，需要分清楚 session &gt; window &gt; pane这个大小和层级顺序是极其重要的，直接关系到工作效率。</p>
<ul>
<li><p>session ：是tmux的会话。我之前就错把这个session当成窗口用，造成了很多不便。一般只要保存一个session就足够了。</p>
</li>
<li><p>window ：相当于一个工作区，包含很多分屏，可以针对每种任务分一个window。如下载一个window，编程一个window。</p>
</li>
<li><p>pane ：是在window里面的小分屏，最常用也最好用。</p>
</li>
</ul>
<h1 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Ubuntu 或 Debian</span></span><br><span class="line">$ sudo apt-get install tmux</span><br><span class="line"></span><br><span class="line"><span class="comment"># CentOS 或 Fedora</span></span><br><span class="line">$ sudo yum install tmux</span><br><span class="line"></span><br><span class="line"><span class="comment"># Mac</span></span><br><span class="line">$ brew install tmux</span><br></pre></td></tr></table></figure>

<h1 id="基本用法"><a href="#基本用法" class="headerlink" title="基本用法"></a>基本用法</h1><h2 id="启动"><a href="#启动" class="headerlink" title="启动"></a>启动</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tmux</span><br></pre></td></tr></table></figure>
<p>输入tmux可以新建一个session，其中包含一个window，该window中包含一个pane，pane里打开了一个shell对话框。<br><img src="https://cdn.nlark.com/yuque/0/2022/png/1759879/1655875516047-d4d6568f-ac08-453c-a889-67936630b978.png#clientId=uffc6ccd6-ab7a-4&crop=0&crop=0&crop=1&crop=1&from=paste&height=424&id=uc4141f26&margin=%5Bobject%20Object%5D&name=image.png&originHeight=848&originWidth=1406&originalType=binary&ratio=1&rotation=0&showTitle=false&size=144395&status=done&style=none&taskId=u412cf76d-be00-4e0c-9709-c92470b0ed6&title=&width=703" alt="image.png"><br>如上图底部有一个状态栏。状态栏的左侧是窗口信息（编号和名称），右侧是系统信息。</p>
<h2 id="退出"><a href="#退出" class="headerlink" title="退出"></a>退出</h2><p>按下Ctrl+d或者显式输入exit命令，就可以退出 Tmux 窗口(即:该会话结束)。</p>
<h2 id="前缀键"><a href="#前缀键" class="headerlink" title="前缀键"></a>前缀键</h2><p>tmux 窗口有大量的快捷键。所有快捷键都要通过前缀键唤起。默认的前缀键是Ctrl+b，即先按下Ctrl+b，快捷键才会生效。<br>举例来说，帮助命令的快捷键是Ctrl+b ?。它的用法是，在tmux窗口中，先按下Ctrl+b，再按下?，就会显示帮助信息。然后，按下 ESC 键或q键，就可以退出帮助。</p>
<blockquote>
<ol>
<li>按下Ctrl + b后手指松开</li>
<li>然后再按其他键。</li>
</ol>
</blockquote>
<h1 id="session-会话-管理"><a href="#session-会话-管理" class="headerlink" title="session(会话)管理"></a>session(会话)管理</h1><h2 id="新建会话"><a href="#新建会话" class="headerlink" title="新建会话"></a>新建会话</h2><p>tmux创建的session的名字默认都是按数字排序，也就是说第一个启动的tmux窗口，编号是0，第二个窗口的编号是1，以此类推。使用编号区分会话，不太直观，更好的方法是为会话起名。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tmux new -s &lt;session-name&gt;</span><br></pre></td></tr></table></figure>
<p>上面命令新建一个指定名称的会话，如下图：<br><img src="https://cdn.nlark.com/yuque/0/2022/png/1759879/1655876442362-a288bfa8-8c42-4746-8662-203feedf336d.png#clientId=uffc6ccd6-ab7a-4&crop=0&crop=0&crop=1&crop=1&from=paste&height=424&id=u354ed69e&margin=%5Bobject%20Object%5D&name=image.png&originHeight=848&originWidth=1406&originalType=binary&ratio=1&rotation=0&showTitle=false&size=204658&status=done&style=none&taskId=ub4f51f33-1989-4933-a794-d18339f7fbd&title=&width=703" alt="image.png"></p>
<h2 id="退出会话"><a href="#退出会话" class="headerlink" title="退出会话"></a>退出会话</h2><p>在tmux里，如果需要重新退回terminal，可输入Ctrl + b d 命令（或者输入：tmux detach命令）。其中d表示detaching。运行该命令后后并不会真正关闭session，而是将session挂起，在tmux session里面的程序还是会在后台继续运行。</p>
<h2 id="查看会话"><a href="#查看会话" class="headerlink" title="查看会话"></a>查看会话</h2><p>tmux ls命令可以查看当前所有的 Tmux 会话。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ tmux ls</span><br><span class="line"><span class="comment"># or</span></span><br><span class="line">$ tmux list-session</span><br></pre></td></tr></table></figure>
<p><img src="https://cdn.nlark.com/yuque/0/2022/png/1759879/1655876906202-b624ba70-6e57-4be2-85ed-329c7d0cdd16.png#clientId=uffc6ccd6-ab7a-4&crop=0&crop=0&crop=1&crop=1&from=paste&height=441&id=ua0d35e83&margin=%5Bobject%20Object%5D&name=image.png&originHeight=882&originWidth=1406&originalType=binary&ratio=1&rotation=0&showTitle=false&size=221053&status=done&style=none&taskId=u35f5bf80-5445-4a60-a24f-5bcc53b0876&title=&width=703" alt="image.png"></p>
<h2 id="进入会话"><a href="#进入会话" class="headerlink" title="进入会话"></a>进入会话</h2><p>tmux attach命令用于重新接入某个已存在的会话。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 使用会话编号</span></span><br><span class="line">$ tmux attach -t 0</span><br><span class="line"></span><br><span class="line"><span class="comment"># 使用会话名称</span></span><br><span class="line">$ tmux attach -t &lt;session-name&gt;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>也可以将attach简写成a<br>$ tmux a -t <session-name></p>
</blockquote>
<h2 id="切换会话"><a href="#切换会话" class="headerlink" title="切换会话"></a>切换会话</h2><p>tmux switch命令用于切换会话。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 使用会话编号</span></span><br><span class="line">$ tmux switch -t 0</span><br><span class="line"></span><br><span class="line"><span class="comment"># 使用会话名称</span></span><br><span class="line">$ tmux switch -t &lt;session-name&gt;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>使用快捷键Ctrl+b s快速查看session，并使用方向键切换会话，非常方便。</p>
</blockquote>
<h2 id="重命名会话"><a href="#重命名会话" class="headerlink" title="重命名会话"></a>重命名会话</h2><p>tmux rename-session命令用于重命名会话。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tmux rename-session -t 0 &lt;new-name&gt;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>也可以使用Ctrl+b $：快速重命名当前会话。</p>
</blockquote>
<h2 id="杀死会话"><a href="#杀死会话" class="headerlink" title="杀死会话"></a>杀死会话</h2><p>tmux kill-session命令用于杀死某个会话。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 使用会话编号</span></span><br><span class="line">tmux kill-session -t 0</span><br><span class="line"><span class="comment"># 使用会话名称</span></span><br><span class="line">tmux kill-session -t &lt;session-name&gt;</span><br></pre></td></tr></table></figure>

<h1 id="window-窗口-管理"><a href="#window-窗口-管理" class="headerlink" title="window(窗口)管理"></a>window(窗口)管理</h1><h2 id="新建窗口"><a href="#新建窗口" class="headerlink" title="新建窗口"></a>新建窗口</h2><p>tmux new-window命令用来创建新窗口。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ tmux new-window</span><br><span class="line"></span><br><span class="line"><span class="comment"># 新建一个指定名称的窗口</span></span><br><span class="line">$ tmux new-window -n &lt;window-name&gt;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>或者使用快捷键 Ctrl+b c</p>
</blockquote>
<h2 id="切换窗口"><a href="#切换窗口" class="headerlink" title="切换窗口"></a>切换窗口</h2><p>tmux select-window命令用来切换窗口。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 切换到指定编号的窗口</span></span><br><span class="line">$ tmux select-window -t &lt;window-number&gt;</span><br><span class="line"></span><br><span class="line"><span class="comment"># 切换到指定名称的窗口</span></span><br><span class="line">$ tmux select-window -t &lt;window-name&gt;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>快捷键 Ctrl+b <number> 切换到指定编号的窗口，其中的<number>是状态栏上的窗口编号。<br>从列表中选择窗口。</p>
</blockquote>
<h2 id="重命名窗口"><a href="#重命名窗口" class="headerlink" title="重命名窗口"></a>重命名窗口</h2><p>tmux rename-window命令用于为当前窗口起名（或重命名）。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tmux rename-window &lt;window-name&gt;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>快捷键 Ctrl+b ,</p>
</blockquote>
<h2 id="杀死窗口"><a href="#杀死窗口" class="headerlink" title="杀死窗口"></a>杀死窗口</h2><p>tmux kill-window命令用于杀死窗口。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tmux kill-window -t &lt;window-name&gt;</span><br></pre></td></tr></table></figure>

<h2 id="快捷键"><a href="#快捷键" class="headerlink" title="快捷键"></a>快捷键</h2><p>下面是一些会话相关的快捷键。</p>
<ul>
<li><p>Ctrl+b c ：可以创建新的window。</p>
</li>
<li><p>Ctrl+b 0 ：可以切换到0号window。</p>
</li>
<li><p>Ctrl+b p ：切换到上一个window。</p>
</li>
<li><p>Ctrl+b n ：切换到下一个window。</p>
</li>
<li><p>Ctrl+b , ：对当前window进行重命名。</p>
</li>
<li><p>Ctrl+b w ：可以从window列表里选择window，该显示结果与Ctrl + b + s 一样。</p>
</li>
</ul>
<h1 id="pane-窗格-管理"><a href="#pane-窗格-管理" class="headerlink" title="pane(窗格)管理"></a>pane(窗格)管理</h1><p>tmux 可以将窗口分成多个窗格（pane），每个窗格运行不同的命令。以下命令都是在tmux窗口中执行。</p>
<h2 id="分割窗格"><a href="#分割窗格" class="headerlink" title="分割窗格"></a>分割窗格</h2><p>tmux split-window命令用来分割窗格。</p>
<h3 id="垂直"><a href="#垂直" class="headerlink" title="垂直"></a>垂直</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 划分上下两个窗格</span></span><br><span class="line">$ tmux split-window</span><br></pre></td></tr></table></figure>
<blockquote>
<p>快捷键 Ctrl+b “</p>
</blockquote>
<h3 id="水平"><a href="#水平" class="headerlink" title="水平"></a>水平</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 划分左右两个窗格</span></span><br><span class="line">$ tmux split-window -h</span><br></pre></td></tr></table></figure>
<blockquote>
<p>快捷键 Ctrl+b %</p>
</blockquote>
<h2 id="切换窗格"><a href="#切换窗格" class="headerlink" title="切换窗格"></a>切换窗格</h2><p>tmux select-pane命令用来移动光标位置。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 光标切换到上方窗格</span></span><br><span class="line">$ tmux select-pane -U</span><br><span class="line"></span><br><span class="line"><span class="comment"># 光标切换到上方窗格</span></span><br><span class="line">$ tmux select-pane -D</span><br><span class="line"></span><br><span class="line"><span class="comment"># 光标切换到左边窗格</span></span><br><span class="line">$ tmux select-pane -L</span><br><span class="line"></span><br><span class="line"><span class="comment"># 光标切换到右边窗格</span></span><br><span class="line">$ tmux select-pane -R</span><br></pre></td></tr></table></figure>
<blockquote>
<p>快捷键Ctrl+b 键盘方向键(上下左右)</p>
</blockquote>
<h2 id="调整窗格大小"><a href="#调整窗格大小" class="headerlink" title="调整窗格大小"></a>调整窗格大小</h2><p>使用快捷键Ctrl+b Alt+方向键</p>
<h2 id="杀死窗格"><a href="#杀死窗格" class="headerlink" title="杀死窗格"></a>杀死窗格</h2><p>tmux kill-pane命令用来杀死窗格。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tmux kill-pane -t &lt;pane-name&gt;</span><br></pre></td></tr></table></figure>

<h2 id="快捷键-1"><a href="#快捷键-1" class="headerlink" title="快捷键"></a>快捷键</h2><ul>
<li><p>Ctrl+b “ ：划分上下两个窗格。</p>
</li>
<li><p>Ctrl+b % ：划分左右两个窗格。</p>
</li>
<li><p>Ctrl+b <arrow key> ：光标切换到其他窗格。<arrow key>是指向要切换到的窗格的方向键，比如切换到下方窗格，就按方向键↓。</p>
</li>
<li><p>Ctrl+b ; ：光标切换到上一个窗格。</p>
</li>
<li><p>Ctrl+b o ：光标切换到下一个窗格。</p>
</li>
<li><p>Ctrl+b { ：当前窗格与上一个窗格交换位置。</p>
</li>
<li><p>Ctrl+b } ：当前窗格与下一个窗格交换位置。</p>
</li>
<li><p>Ctrl+b Ctrl+o ：所有窗格向前移动一个位置，第一个窗格变成最后一个窗格。</p>
</li>
<li><p>Ctrl+b Alt+o ：所有窗格向后移动一个位置，最后一个窗格变成第一个窗格。</p>
</li>
<li><p>Ctrl+b x ：关闭当前窗格。</p>
</li>
<li><p>Ctrl+b ! ：将当前窗格拆分为一个独立窗口。</p>
</li>
<li><p><strong>Ctrl+b z</strong> ：当前窗格全屏显示，再使用一次会变回原来大小。</p>
</li>
<li><p>Ctrl+b Ctrl+<arrow key> ：按箭头方向调整窗格大小。</p>
</li>
<li><p>Ctrl+b q ：显示窗格编号。</p>
</li>
</ul>
]]></content>
      <categories>
        <category>tools</category>
      </categories>
      <tags>
        <tag>tmux</tag>
      </tags>
  </entry>
  <entry>
    <title>Android自动背光机制</title>
    <url>/archives/5bc7b994.html</url>
    <content><![CDATA[<span id="more"></span>
<meta name="referrer" content="no-referrer"/>


<h1 id="类结构图"><a href="#类结构图" class="headerlink" title="类结构图"></a>类结构图</h1><p><img src="https://cdn.nlark.com/yuque/0/2021/png/1759879/1630486992636-3c54c65b-7456-43ac-99e3-a25d5b520903.png#clientId=u5df7ab52-a4d3-4&crop=0&crop=0&crop=1&crop=1&from=ui&id=u7326b4a2&margin=%5Bobject%20Object%5D&name=20190104201601366.png&originHeight=369&originWidth=744&originalType=binary&ratio=1&rotation=0&showTitle=false&size=62210&status=done&style=none&taskId=u8f5a6342-feb9-4989-8705-7524dd46d15&title=" alt="20190104201601366.png"></p>
<ul>
<li>AutomaticBrightnessController：只进行环境光强的监听后的一些处理，将背光曲线的创建等工作，交给了BrightnessMappingStrategy</li>
<li>BrightnessMappingStrategy：将负责曲线的创建，自动背光值的计算等。</li>
</ul>
<p>当获取自动背光值时，AutomaticBrightnessController将调用BrightnessMappingStrategy的接口获取。而BrightnessMappingStrategy在创建曲线时，则需要从BrightnessConfigure类中读取两个数组源:<strong>config_autoBrightnessLevels</strong>和<strong>config_autoBrightnessDisplayValuesNits</strong>。</p>
<h1 id="创建背光样条曲线"><a href="#创建背光样条曲线" class="headerlink" title="创建背光样条曲线"></a>创建背光样条曲线</h1><p>自动背光是通过创建多个样条曲线来完成环境光照度与屏幕背光值的映射，系统会使用已知数据创建三条样条曲线：mNitsToBacklightSpline、mBacklightToNitsSpline、mBrightnessSpline。下图描述了这三个样条曲线的创建过程：<br><img src="https://cdn.nlark.com/yuque/0/2021/png/1759879/1630487040191-67d07d38-cdcf-4edd-8630-eb1bab12551b.png#clientId=u5df7ab52-a4d3-4&crop=0&crop=0&crop=1&crop=1&from=ui&id=u829c5c07&margin=%5Bobject%20Object%5D&name=image2020-9-25%2014_13_21.png&originHeight=573&originWidth=1027&originalType=binary&ratio=1&rotation=0&showTitle=false&size=114629&status=done&style=none&taskId=u0c175c37-6ca8-4662-b0e6-3ce7718dbd4&title=" alt="image2020-9-25 14_13_21.png"><br>创建样条曲线的时序图如下：<br><img src="https://cdn.nlark.com/yuque/0/2021/png/1759879/1630487071982-3e1f46e1-2473-42fc-b464-f7497521981c.png#clientId=u5df7ab52-a4d3-4&crop=0&crop=0&crop=1&crop=1&from=ui&id=ud70d2c17&margin=%5Bobject%20Object%5D&name=image2020-9-25%2014_17_29.png&originHeight=838&originWidth=1015&originalType=binary&ratio=1&rotation=0&showTitle=false&size=159982&status=done&style=none&taskId=ua0ddedda-f7f9-4848-a282-18beefc0c50&title=" alt="image2020-9-25 14_17_29.png"></p>
<h2 id="DisplayPowerController"><a href="#DisplayPowerController" class="headerlink" title="DisplayPowerController"></a>DisplayPowerController</h2><p>自动背光样条曲线的创建放在了BrightnessMappingStrategy中，当系统启动进入DisplayPowerController构造方法后，就会开始创建背光样条曲线。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/display/DisplayPowerController.java</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">DisplayPowerController</span><span class="params">(Context context,</span></span></span><br><span class="line"><span class="params"><span class="function">       DisplayPowerCallbacks callbacks, Handler handler,</span></span></span><br><span class="line"><span class="params"><span class="function">       SensorManager sensorManager, DisplayBlanker blanker, LogicalDisplay logicalDisplay,</span></span></span><br><span class="line"><span class="params"><span class="function">       BrightnessTracker brightnessTracker, BrightnessSetting brightnessSetting,</span></span></span><br><span class="line"><span class="params"><span class="function">       Runnable onBrightnessChangeRunnable)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">       ...</span><br><span class="line">       <span class="comment">//获取映射Lux-Nits-Backlight值的对象</span></span><br><span class="line">       setUpAutoBrightness(resources, handler);</span><br><span class="line">       ...</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">setUpAutoBrightness</span><span class="params">(Resources resources, Handler handler)</span> </span>&#123;</span><br><span class="line">   <span class="keyword">if</span> (!mUseSoftwareAutoBrightnessConfig) &#123;</span><br><span class="line">       <span class="keyword">return</span>;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   mBrightnessMapper = BrightnessMappingStrategy.create(resources, mDisplayDeviceConfig);</span><br><span class="line"></span><br><span class="line">   ...</span><br><span class="line">   <span class="keyword">if</span> (mBrightnessMapper != <span class="keyword">null</span>) &#123;</span><br><span class="line">       mAutomaticBrightnessController = <span class="keyword">new</span> AutomaticBrightnessController(<span class="keyword">this</span>,</span><br><span class="line">                    handler.getLooper(), mSensorManager, mLightSensor, mBrightnessMapper,</span><br><span class="line">                    lightSensorWarmUpTimeConfig, PowerManager.BRIGHTNESS_MIN,</span><br><span class="line">                    PowerManager.BRIGHTNESS_MAX, dozeScaleFactor, lightSensorRate,</span><br><span class="line">                    initialLightSensorRate, brighteningLightDebounce, darkeningLightDebounce,</span><br><span class="line">                    autoBrightnessResetAmbientLuxAfterWarmUp, ambientBrightnessThresholds,</span><br><span class="line">                    screenBrightnessThresholds, mLogicalDisplay, mContext, mHbmController);</span><br><span class="line">   &#125;</span><br><span class="line">   ...</span><br><span class="line">&#125;   </span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>我们从BrightnessMappingStrategy.create()函数开始来查看曲线的绘制，create()方法如下，相关代码已进行注释：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/display/BrightnessMappingStrategy.java</span></span><br><span class="line"></span><br><span class="line"><span class="meta">@Nullable</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> BrightnessMappingStrategy <span class="title">create</span><span class="params">(Resources resources,</span></span></span><br><span class="line"><span class="params"><span class="function">       DisplayDeviceConfig displayDeviceConfig)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Display independent values</span></span><br><span class="line">   <span class="comment">//Lux值的数组，getLuxLevels()中会添加一个0元素作为Lux[0]</span></span><br><span class="line">   <span class="keyword">float</span>[] luxLevels = getLuxLevels(resources.getIntArray(</span><br><span class="line">           com.android.internal.R.array.config_autoBrightnessLevels));</span><br><span class="line">   <span class="comment">//Lux值对应的背光值，仅在简单映射情况下会使用</span></span><br><span class="line">   <span class="keyword">int</span>[] brightnessLevelsBacklight = resources.getIntArray(</span><br><span class="line">           com.android.internal.R.array.config_autoBrightnessLcdBacklightValues);</span><br><span class="line">   <span class="comment">//描述和Lux值对应屏幕亮度的Nits值数组，长度比原始Lux值数组大1。</span></span><br><span class="line">   <span class="comment">//如果配置了该值，则必须配置 config_screenBrightnessNits 和 config_screenBrightnessBacklight</span></span><br><span class="line">   <span class="keyword">float</span>[] brightnessLevelsNits = getFloatArray(resources.obtainTypedArray(</span><br><span class="line">           com.android.internal.R.array.config_autoBrightnessDisplayValuesNits));</span><br><span class="line">   <span class="comment">//用户可调整的最大Gamma值</span></span><br><span class="line">   <span class="keyword">float</span> autoBrightnessAdjustmentMaxGamma = resources.getFraction(</span><br><span class="line">           com.android.internal.R.fraction.config_autoBrightnessAdjustmentMaxGamma,</span><br><span class="line">           <span class="number">1</span>, <span class="number">1</span>);</span><br><span class="line">   <span class="keyword">long</span> shortTermModelTimeout = resources.getInteger(</span><br><span class="line">           com.android.internal.R.integer.config_autoBrightnessShortTermModelTimeout);</span><br><span class="line"></span><br><span class="line">   <span class="comment">// Display dependent values - used for physical mapping strategy nits -&gt; brightness</span></span><br><span class="line">   <span class="comment">//描述屏幕亮度的 nits 值数组</span></span><br><span class="line">   <span class="keyword">final</span> <span class="keyword">float</span>[] nitsRange = displayDeviceConfig.getNits();</span><br><span class="line">   <span class="comment">//描述与 nitsRange 数组中的亮度值(单位为Nits)相对应的屏幕背光值</span></span><br><span class="line">   <span class="keyword">final</span> <span class="keyword">float</span>[] brightnessRange = displayDeviceConfig.getBrightness();</span><br><span class="line"></span><br><span class="line">   <span class="comment">//isValidMapping()用来检查映射的有效性，两个参数需要满足以下条件:</span></span><br><span class="line">   <span class="comment">//（1）非空;</span></span><br><span class="line">   <span class="comment">//（2）长度相同;</span></span><br><span class="line">   <span class="comment">//（3）长度&gt;=0;</span></span><br><span class="line">   <span class="comment">//（4）参数1必须严格单调递增,参数2必须单调递增</span></span><br><span class="line">   <span class="keyword">if</span> (isValidMapping(nitsRange, brightnessRange)</span><br><span class="line">           &amp;&amp; isValidMapping(luxLevels, brightnessLevelsNits)) &#123;</span><br><span class="line"></span><br><span class="line">       <span class="comment">//获取BrightnessConfiguration.Builder实例，并且将读取的Lux值和Nits值保存在builder对象中</span></span><br><span class="line">       BrightnessConfiguration.Builder builder = <span class="keyword">new</span> BrightnessConfiguration.Builder(</span><br><span class="line">               luxLevels, brightnessLevelsNits);</span><br><span class="line">       builder.setShortTermModelTimeoutMillis(shortTermModelTimeout);</span><br><span class="line">       builder.setShortTermModelLowerLuxMultiplier(SHORT_TERM_MODEL_THRESHOLD_RATIO);</span><br><span class="line">       builder.setShortTermModelUpperLuxMultiplier(SHORT_TERM_MODEL_THRESHOLD_RATIO);</span><br><span class="line">       <span class="comment">//映射Lux值和Nits值，物理映射</span></span><br><span class="line">       <span class="keyword">return</span> <span class="keyword">new</span> PhysicalMappingStrategy(builder.build(), nitsRange, brightnessRange,</span><br><span class="line">               autoBrightnessAdjustmentMaxGamma);</span><br><span class="line">   &#125; <span class="keyword">else</span> <span class="keyword">if</span> (isValidMapping(luxLevels, brightnessLevelsBacklight)) &#123;</span><br><span class="line">       <span class="comment">//简单映射</span></span><br><span class="line">       <span class="keyword">return</span> <span class="keyword">new</span> SimpleMappingStrategy(luxLevels, brightnessLevelsBacklight,</span><br><span class="line">               autoBrightnessAdjustmentMaxGamma, shortTermModelTimeout);</span><br><span class="line">   &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">       <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在create()方法中，读取config.xml文件中的多个配置值，然后根据配置值来决定映射方式。</p>
<h2 id="DisplayDeviceConfig"><a href="#DisplayDeviceConfig" class="headerlink" title="DisplayDeviceConfig"></a>DisplayDeviceConfig</h2><p>屏幕亮度的 nits 值数组、屏幕亮度的 nits 值数组相对应的屏幕背光值是通过DisplayDeviceConfig获取，而在DisplayDeviceConfig里，其实也是通过读取config.xml文件中的config_screenBrightnessNits和config_screenBrightnessBacklight配置值，跟Android11的代码一样，只是换到了DisplayDeviceConfig中。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/display/DisplayDeviceConfig.java</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">loadBrightnessMapFromConfigXml</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="comment">// Use the config.xml mapping</span></span><br><span class="line">    <span class="keyword">final</span> Resources res = mContext.getResources();</span><br><span class="line">    <span class="keyword">final</span> <span class="keyword">float</span>[] sysNits = BrightnessMappingStrategy.getFloatArray(res.obtainTypedArray(</span><br><span class="line">        com.android.internal.R.array.config_screenBrightnessNits));</span><br><span class="line">    <span class="keyword">final</span> <span class="keyword">int</span>[] sysBrightness = res.getIntArray(</span><br><span class="line">        com.android.internal.R.array.config_screenBrightnessBacklight);</span><br><span class="line">    <span class="keyword">final</span> <span class="keyword">float</span>[] sysBrightnessFloat = <span class="keyword">new</span> <span class="keyword">float</span>[sysBrightness.length];</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; sysBrightness.length; i++) &#123;</span><br><span class="line">        sysBrightnessFloat[i] = BrightnessSynchronizer.brightnessIntToFloat(</span><br><span class="line">            sysBrightness[i]);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// These arrays are allowed to be empty, we set null values so that</span></span><br><span class="line">    <span class="comment">// BrightnessMappingStrategy will create a SimpleMappingStrategy instead.</span></span><br><span class="line">    <span class="keyword">if</span> (sysBrightnessFloat.length == <span class="number">0</span> || sysNits.length == <span class="number">0</span>) &#123;</span><br><span class="line">        setSimpleMappingStrategyValues();</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    mRawNits = sysNits;</span><br><span class="line">    mRawBacklight = sysBrightnessFloat;</span><br><span class="line">    constrainNitsAndBacklightArrays();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Change the nits and backlight arrays, so that they cover only the allowed backlight values</span></span><br><span class="line"><span class="comment"> * Use the brightness minimum and maximum values to clamp these arrays.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">constrainNitsAndBacklightArrays</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (mRawBacklight[<span class="number">0</span>] &gt; mBacklightMinimum</span><br><span class="line">        || mRawBacklight[mRawBacklight.length - <span class="number">1</span>] &lt; mBacklightMaximum</span><br><span class="line">        || mBacklightMinimum &gt; mBacklightMaximum) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> IllegalStateException(<span class="string">&quot;Min or max values are invalid&quot;</span></span><br><span class="line">                                        + <span class="string">&quot;; raw min=&quot;</span> + mRawBacklight[<span class="number">0</span>]</span><br><span class="line">                                        + <span class="string">&quot;; raw max=&quot;</span> + mRawBacklight[mRawBacklight.length - <span class="number">1</span>]</span><br><span class="line">                                        + <span class="string">&quot;; backlight min=&quot;</span> + mBacklightMinimum</span><br><span class="line">                                        + <span class="string">&quot;; backlight max=&quot;</span> + mBacklightMaximum);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">float</span>[] newNits = <span class="keyword">new</span> <span class="keyword">float</span>[mRawBacklight.length];</span><br><span class="line">    <span class="keyword">float</span>[] newBacklight = <span class="keyword">new</span> <span class="keyword">float</span>[mRawBacklight.length];</span><br><span class="line">    <span class="comment">// Find the starting index of the clamped arrays. This may be less than the min so</span></span><br><span class="line">    <span class="comment">// we&#x27;ll need to clamp this value still when actually doing the remapping.</span></span><br><span class="line">    <span class="keyword">int</span> newStart = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; mRawBacklight.length - <span class="number">1</span>; i++) &#123;</span><br><span class="line">        <span class="keyword">if</span> (mRawBacklight[i + <span class="number">1</span>] &gt; mBacklightMinimum) &#123;</span><br><span class="line">            newStart = i;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">boolean</span> isLastValue = <span class="keyword">false</span>;</span><br><span class="line">    <span class="keyword">int</span> newIndex = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = newStart; i &lt; mRawBacklight.length &amp;&amp; !isLastValue; i++) &#123;</span><br><span class="line">        newIndex = i - newStart;</span><br><span class="line">        <span class="keyword">final</span> <span class="keyword">float</span> newBacklightVal;</span><br><span class="line">        <span class="keyword">final</span> <span class="keyword">float</span> newNitsVal;</span><br><span class="line">        isLastValue = mRawBacklight[i] &gt;= mBacklightMaximum</span><br><span class="line">            || i &gt;= mRawBacklight.length - <span class="number">1</span>;</span><br><span class="line">        <span class="comment">// Clamp beginning and end to valid backlight values.</span></span><br><span class="line">        <span class="keyword">if</span> (newIndex == <span class="number">0</span>) &#123;</span><br><span class="line">            newBacklightVal = MathUtils.max(mRawBacklight[i], mBacklightMinimum);</span><br><span class="line">            newNitsVal = rawBacklightToNits(i, newBacklightVal);</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (isLastValue) &#123;</span><br><span class="line">            newBacklightVal = MathUtils.min(mRawBacklight[i], mBacklightMaximum);</span><br><span class="line">            newNitsVal = rawBacklightToNits(i - <span class="number">1</span>, newBacklightVal);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            newBacklightVal = mRawBacklight[i];</span><br><span class="line">            newNitsVal = mRawNits[i];</span><br><span class="line">        &#125;</span><br><span class="line">        newBacklight[newIndex] = newBacklightVal;</span><br><span class="line">        newNits[newIndex] = newNitsVal;</span><br><span class="line">    &#125;</span><br><span class="line">    mBacklight = Arrays.copyOf(newBacklight, newIndex + <span class="number">1</span>);</span><br><span class="line">    mNits = Arrays.copyOf(newNits, newIndex + <span class="number">1</span>);</span><br><span class="line">    createBacklightConversionSplines();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// This method creates a brightness spline that is of equal length with proportional increments</span></span><br><span class="line"><span class="comment">// to the backlight spline. The values of this array range from 0.0f to 1.0f instead of the</span></span><br><span class="line"><span class="comment">// potential constrained range that the backlight array covers</span></span><br><span class="line"><span class="comment">// These splines are used to convert from the system brightness value to the HAL backlight</span></span><br><span class="line"><span class="comment">// value</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">createBacklightConversionSplines</span><span class="params">()</span> </span>&#123;</span><br><span class="line">   mBrightness = <span class="keyword">new</span> <span class="keyword">float</span>[mBacklight.length];</span><br><span class="line">   <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; mBrightness.length; i++) &#123;</span><br><span class="line">       mBrightness[i] = MathUtils.map(mBacklight[<span class="number">0</span>],</span><br><span class="line">               mBacklight[mBacklight.length - <span class="number">1</span>],</span><br><span class="line">               PowerManager.BRIGHTNESS_MIN, PowerManager.BRIGHTNESS_MAX, mBacklight[i]);</span><br><span class="line">   &#125;</span><br><span class="line">   mBrightnessToBacklightSpline = Spline.createSpline(mBrightness, mBacklight);</span><br><span class="line">   mBacklightToBrightnessSpline = Spline.createSpline(mBacklight, mBrightness);</span><br><span class="line">   mBacklightToNitsSpline = Spline.createSpline(mBacklight, mNits);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>综上涉及到的配置值如下：</p>
<ul>
<li>config_autoBrightnessLevels：Lux值数组</li>
<li>config_autoBrightnessLcdBacklightValues：Lux值对应的背光值数组</li>
<li>config_autoBrightnessDisplayValuesNits：Lux值对应的Nits值数组</li>
<li>config_screenBrightnessNits：屏幕发光强度的Nits值数组</li>
<li>config_screenBrightnessBacklight：和发光强度Nits值对应的背光值数组<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/core/res/res/values/config.xml</span></span><br><span class="line"></span><br><span class="line">&lt;!-- Lux值数组--&gt;</span><br><span class="line">&lt;!-- Array of light sensor lux values to define our levels <span class="keyword">for</span> auto backlight brightness support.</span><br><span class="line">    The N entries of <span class="keyword">this</span> array define N + <span class="number">1</span> control points as follows:</span><br><span class="line">    (<span class="number">1</span>-based arrays)</span><br><span class="line"></span><br><span class="line">    Point <span class="number">1</span>:            (<span class="number">0</span>, value[<span class="number">1</span>]):             lux &lt;= <span class="number">0</span></span><br><span class="line">    Point <span class="number">2</span>:     (level[<span class="number">1</span>], value[<span class="number">2</span>]):  <span class="number">0</span>        &lt; lux &lt;= level[<span class="number">1</span>]</span><br><span class="line">    Point <span class="number">3</span>:     (level[<span class="number">2</span>], value[<span class="number">3</span>]):  level[<span class="number">2</span>] &lt; lux &lt;= level[<span class="number">3</span>]</span><br><span class="line">    ...</span><br><span class="line">    Point N+<span class="number">1</span>: (level[N], value[N+<span class="number">1</span>]):  level[N] &lt; lux</span><br><span class="line"></span><br><span class="line">    The control points must be strictly increasing.  Each control point</span><br><span class="line">    corresponds to an entry in the brightness backlight values arrays.</span><br><span class="line">    For example, <span class="keyword">if</span> lux == level[<span class="number">1</span>] (first element of the levels array)</span><br><span class="line">    then the brightness will be determined by value[<span class="number">2</span>] (second element</span><br><span class="line">    of the brightness values array).</span><br><span class="line"></span><br><span class="line">    Spline interpolation is used to determine the auto-brightness</span><br><span class="line">    backlight values <span class="keyword">for</span> lux levels between these control points.</span><br><span class="line"></span><br><span class="line">    Must be overridden in platform specific overlays --&gt;</span><br><span class="line">&lt;integer-array name=<span class="string">&quot;config_autoBrightnessLevels&quot;</span>&gt;</span><br><span class="line">&lt;/integer-array&gt;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">&lt;!-- Lux值对应的背光值数组 --&gt;</span><br><span class="line">&lt;!-- Array of output values <span class="keyword">for</span> LCD backlight corresponding to the lux values</span><br><span class="line">    in the config_autoBrightnessLevels array.  This array should have size one greater</span><br><span class="line">    than the size of the config_autoBrightnessLevels array.</span><br><span class="line">    The brightness values must be between <span class="number">0</span> and <span class="number">255</span> and be non-decreasing.</span><br><span class="line">    This must be overridden in platform specific overlays --&gt;</span><br><span class="line">&lt;integer-array name=<span class="string">&quot;config_autoBrightnessLcdBacklightValues&quot;</span>&gt;</span><br><span class="line">&lt;/integer-array&gt;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">&lt;!-- Lux值对应的Nits值数组 --&gt;</span><br><span class="line">&lt;!-- Array of desired screen brightness in nits corresponding to the lux values</span><br><span class="line">    in the config_autoBrightnessLevels array. As with config_screenBrightnessMinimumNits and</span><br><span class="line">    config_screenBrightnessMaximumNits, the display brightness is defined as the measured</span><br><span class="line">    brightness of an all-white image.</span><br><span class="line"></span><br><span class="line">    If <span class="keyword">this</span> is defined then:</span><br><span class="line">       - config_autoBrightnessLcdBacklightValues should not be defined</span><br><span class="line">       - config_screenBrightnessNits must be defined</span><br><span class="line">       - config_screenBrightnessBacklight must be defined</span><br><span class="line"></span><br><span class="line">    This array should have size one greater than the size of the config_autoBrightnessLevels</span><br><span class="line">    array. The brightness values must be non-negative and non-decreasing. This must be</span><br><span class="line">    overridden in platform specific overlays --&gt;</span><br><span class="line">&lt;array name=<span class="string">&quot;config_autoBrightnessDisplayValuesNits&quot;</span>&gt;</span><br><span class="line">&lt;/array&gt;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">&lt;!-- 描述屏幕发光强度的Nits值数组 --&gt;</span><br><span class="line">&lt;!-- An array of floats describing the screen brightness in nits corresponding to the backlight</span><br><span class="line">    values in the config_screenBrightnessBacklight array.  On OLED displays these  values</span><br><span class="line">    should be measured with an all white image <span class="keyword">while</span> the display is in the fully on state.</span><br><span class="line">    Note that <span class="keyword">this</span> value should *not* reflect the maximum brightness value <span class="keyword">for</span> any high</span><br><span class="line">    brightness modes but only the maximum brightness value obtainable in a sustainable manner.</span><br><span class="line"></span><br><span class="line">    This array should be equal in size to config_screenBrightnessBacklight --&gt;</span><br><span class="line">&lt;array name=<span class="string">&quot;config_screenBrightnessNits&quot;</span>&gt;</span><br><span class="line">&lt;/array&gt;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">&lt;!-- 和发光强度Nits值对应的背光值数组 --&gt;</span><br><span class="line">&lt;!-- An array describing the screen<span class="string">&#x27;s backlight values corresponding to the brightness</span></span><br><span class="line"><span class="string">    values in the config_screenBrightnessNits array.</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">    This array should be equal in size to config_screenBrightnessBacklight. --&gt;</span></span><br><span class="line"><span class="string">&lt;integer-array name=&quot;config_screenBrightnessBacklight&quot;&gt;</span></span><br><span class="line"><span class="string">&lt;/integer-array&gt;</span></span><br></pre></td></tr></table></figure>
<blockquote>
<p>思考：<br>从这里其实没看到有对背光值对任何限制。如果背光为0或者很大，那要么就看不见无法调节，要么亮度过高导致“烧屏”（危言耸听）。<br>所以这里是不是可以在config.xml新增两个配置值，不同的项目通过overlay的方式设置合适的配置值。<br>然后做如下限制：</p>
<ul>
<li>brightnessRange[0] &gt; miniBrightness</li>
<li>brightnessRange[brightnessRange.length - 1] &lt; maxBrightness</li>
</ul>
</blockquote>
</li>
</ul>
<h2 id="BrightnessConfiguration"><a href="#BrightnessConfiguration" class="headerlink" title="BrightnessConfiguration"></a>BrightnessConfiguration</h2><p>读取完配置值后，Lux值数组及其对应的Nits值数组将会用来构建BrightnessConfiguration.Builder实例builder，Builder首先会检查这两个数组的有效性，该过程与上面的isValidMapping()方法类似：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/core/java/android/hardware/display/BrightnessConfiguration.java</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Constructs the builder with the control points for the brightness curve.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * Brightness curves must have strictly increasing ambient brightness values in lux and</span></span><br><span class="line"><span class="comment"> * monotonically increasing display brightness values in nits. In addition, the initial</span></span><br><span class="line"><span class="comment"> * control point must be 0 lux.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@throws</span> IllegalArgumentException if the initial control point is not at 0 lux.</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@throws</span> IllegalArgumentException if the lux levels are not strictly increasing.</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@throws</span> IllegalArgumentException if the nit levels are not monotonically increasing.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">Builder</span><span class="params">(<span class="keyword">float</span>[] lux, <span class="keyword">float</span>[] nits)</span> </span>&#123;</span><br><span class="line">     Objects.requireNonNull(lux);</span><br><span class="line">     Objects.requireNonNull(nits);</span><br><span class="line">     <span class="comment">// 检查两个数组是否非空</span></span><br><span class="line">     <span class="keyword">if</span> (lux.length == <span class="number">0</span> || nits.length == <span class="number">0</span>) &#123;</span><br><span class="line">          <span class="keyword">throw</span> <span class="keyword">new</span> IllegalArgumentException(<span class="string">&quot;Lux and nits arrays must not be empty&quot;</span>);</span><br><span class="line">     &#125;</span><br><span class="line">     <span class="comment">// 检查两个数组的长度是否相同</span></span><br><span class="line">     <span class="keyword">if</span> (lux.length != nits.length) &#123;</span><br><span class="line">          <span class="keyword">throw</span> <span class="keyword">new</span> IllegalArgumentException(<span class="string">&quot;Lux and nits arrays must be the same length&quot;</span>);</span><br><span class="line">     &#125;</span><br><span class="line">     <span class="keyword">if</span> (lux[<span class="number">0</span>] != <span class="number">0</span>) &#123;</span><br><span class="line">          <span class="keyword">throw</span> <span class="keyword">new</span> IllegalArgumentException(<span class="string">&quot;Initial control point must be for 0 lux&quot;</span>);</span><br><span class="line">     &#125;</span><br><span class="line">     <span class="comment">// 检查Lux数组和Nits数组是否有序</span></span><br><span class="line">     Preconditions.checkArrayElementsInRange(lux, <span class="number">0</span>, Float.MAX_VALUE, <span class="string">&quot;lux&quot;</span>);</span><br><span class="line">     Preconditions.checkArrayElementsInRange(nits, <span class="number">0</span>, Float.MAX_VALUE, <span class="string">&quot;nits&quot;</span>);</span><br><span class="line">     <span class="comment">// 检查Lux数组是否严格单调递增</span></span><br><span class="line">     checkMonotonic(lux, <span class="keyword">true</span> <span class="comment">/*strictly increasing*/</span>, <span class="string">&quot;lux&quot;</span>);</span><br><span class="line">     <span class="comment">// 检查Nits数组是否单调递增</span></span><br><span class="line">     checkMonotonic(nits, <span class="keyword">false</span> <span class="comment">/*strictly increasing*/</span>, <span class="string">&quot;nits&quot;</span>);</span><br><span class="line">     <span class="comment">// 暂存Lux数组</span></span><br><span class="line">     mCurveLux = lux;</span><br><span class="line">     <span class="comment">// 暂存Nits数组</span></span><br><span class="line">     mCurveNits = nits;</span><br><span class="line">     <span class="comment">// 针对App的背光校正</span></span><br><span class="line">     mCorrectionsByPackageName = <span class="keyword">new</span> HashMap&lt;&gt;();</span><br><span class="line">     <span class="comment">// 针对类别的背光校正</span></span><br><span class="line">     mCorrectionsByCategory = <span class="keyword">new</span> HashMap&lt;&gt;();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>之后bulider.bulid()被用来构建PhysicalMappingStrategy实例，此时会将Lux值数组及其对应的Nits值数组保存为BrightnessConfiguration类的全局变量：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/core/java/android/hardware/display/BrightnessConfiguration.java</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Builds the &#123;<span class="doctag">@link</span> BrightnessConfiguration&#125;.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@NonNull</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> BrightnessConfiguration <span class="title">build</span><span class="params">()</span> </span>&#123;</span><br><span class="line">     <span class="keyword">if</span> (mCurveLux == <span class="keyword">null</span> || mCurveNits == <span class="keyword">null</span>) &#123;</span><br><span class="line">          <span class="keyword">throw</span> <span class="keyword">new</span> IllegalStateException(<span class="string">&quot;A curve must be set!&quot;</span>);</span><br><span class="line">     &#125;</span><br><span class="line">     <span class="comment">//创建BrightnessConfiguration实例</span></span><br><span class="line">     <span class="keyword">return</span> <span class="keyword">new</span> BrightnessConfiguration(mCurveLux, mCurveNits, mCorrectionsByPackageName,</span><br><span class="line">          mCorrectionsByCategory, mDescription, mShouldCollectColorSamples,</span><br><span class="line">          mShortTermModelTimeout, mShortTermModelLowerLuxMultiplier,</span><br><span class="line">          mShortTermModelUpperLuxMultiplier);</span><br><span class="line">&#125;</span><br><span class="line">  </span><br><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="class"><span class="keyword">class</span> <span class="title">BrightnessConfiguration</span> <span class="keyword">implements</span> <span class="title">Parcelable</span> </span>&#123;</span><br><span class="line">    <span class="comment">// ......</span></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="title">BrightnessConfiguration</span><span class="params">(<span class="keyword">float</span>[] lux, <span class="keyword">float</span>[] nits,</span></span></span><br><span class="line"><span class="params"><span class="function">           Map&lt;String, BrightnessCorrection&gt; correctionsByPackageName,</span></span></span><br><span class="line"><span class="params"><span class="function">           Map&lt;Integer, BrightnessCorrection&gt; correctionsByCategory, String description,</span></span></span><br><span class="line"><span class="params"><span class="function">           <span class="keyword">boolean</span> shouldCollectColorSamples,</span></span></span><br><span class="line"><span class="params"><span class="function">           <span class="keyword">long</span> shortTermModelTimeout,</span></span></span><br><span class="line"><span class="params"><span class="function">           <span class="keyword">float</span> shortTermModelLowerLuxMultiplier,</span></span></span><br><span class="line"><span class="params"><span class="function">           <span class="keyword">float</span> shortTermModelUpperLuxMultiplier)</span> </span>&#123;</span><br><span class="line">         <span class="comment">//保存Lux数组到BrightnessConfiguration全局变量</span></span><br><span class="line">         mLux = lux;</span><br><span class="line">         <span class="comment">//保存Nits数组到BrightnessConfiguration全局变量</span></span><br><span class="line">         mNits = nits;</span><br><span class="line">         mCorrectionsByPackageName = correctionsByPackageName;</span><br><span class="line">         mCorrectionsByCategory = correctionsByCategory;</span><br><span class="line">         mDescription = description;</span><br><span class="line">         mShouldCollectColorSamples = shouldCollectColorSamples;</span><br><span class="line">         mShortTermModelTimeout = shortTermModelTimeout;</span><br><span class="line">         mShortTermModelLowerLuxMultiplier = shortTermModelLowerLuxMultiplier;</span><br><span class="line">         mShortTermModelUpperLuxMultiplier = shortTermModelUpperLuxMultiplier;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// ......</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>BrightnessConfiguration类还提供了一个getCurve()方法，用于获取它的mLux和mNits。</p>
<h2 id="PhysicalMappingStrategy"><a href="#PhysicalMappingStrategy" class="headerlink" title="PhysicalMappingStrategy"></a>PhysicalMappingStrategy</h2><p>下面进入到物理映射关系对象PhysicalMappingStrategy中，分析其构造方法。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/display/BrightnessMappingStrategy.java</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> config: BrightnessConfiguration对象，携带有用于创建样条曲线的Lux数组和对应的Nits数组</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> nits: 描述屏幕发光强度的nits值数组</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> backlight: 描述与nits值数组对应的背光值</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> maxGamma: 用户可调整最大Gamma值</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">PhysicalMappingStrategy</span><span class="params">(BrightnessConfiguration config, <span class="keyword">float</span>[] nits,</span></span></span><br><span class="line"><span class="params"><span class="function">          <span class="keyword">float</span>[] brightness, <span class="keyword">float</span> maxGamma)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">     <span class="comment">//一些检查过程</span></span><br><span class="line">     Preconditions.checkArgument(nits.length != <span class="number">0</span> &amp;&amp; brightness.length != <span class="number">0</span>,</span><br><span class="line">          <span class="string">&quot;Nits and brightness arrays must not be empty!&quot;</span>);</span><br><span class="line"></span><br><span class="line">     Preconditions.checkArgument(nits.length == brightness.length,</span><br><span class="line">          <span class="string">&quot;Nits and brightness arrays must be the same length!&quot;</span>);</span><br><span class="line">     Objects.requireNonNull(config);</span><br><span class="line">     Preconditions.checkArrayElementsInRange(nits, <span class="number">0</span>, Float.MAX_VALUE, <span class="string">&quot;nits&quot;</span>);</span><br><span class="line">     Preconditions.checkArrayElementsInRange(brightness,</span><br><span class="line">          PowerManager.BRIGHTNESS_MIN, PowerManager.BRIGHTNESS_MAX, <span class="string">&quot;brightness&quot;</span>);</span><br><span class="line"></span><br><span class="line">     <span class="comment">//用户可调整的最大Gamma值</span></span><br><span class="line">     mMaxGamma = maxGamma;</span><br><span class="line">     <span class="comment">//自动背光调节值</span></span><br><span class="line">     mAutoBrightnessAdjustment = <span class="number">0</span>;</span><br><span class="line">     <span class="comment">//在自动背光开启的情况下，用户手动调节亮度时的当前Lux值</span></span><br><span class="line">     mUserLux = -<span class="number">1</span>;</span><br><span class="line">     <span class="comment">//在自动背光开启的情况下，用户手动调节设置的亮度</span></span><br><span class="line">     mUserBrightness = -<span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">     mNits = nits;</span><br><span class="line">     mBrightness = brightness;</span><br><span class="line">     computeNitsBrightnessSplines(mNits);</span><br><span class="line"></span><br><span class="line">     mDefaultConfig = config;</span><br><span class="line">     <span class="keyword">if</span> (mLoggingEnabled) &#123;</span><br><span class="line">          PLOG.start(<span class="string">&quot;physical mapping strategy&quot;</span>);</span><br><span class="line">     &#125;</span><br><span class="line">     mConfig = config;</span><br><span class="line">     <span class="comment">//创建Lux-Nits曲线</span></span><br><span class="line">     computeSpline();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">computeNitsBrightnessSplines</span><span class="params">(<span class="keyword">float</span>[] nits)</span> </span>&#123;</span><br><span class="line">     <span class="comment">//创建Nits-Backlight曲线</span></span><br><span class="line">     mNitsToBrightnessSpline = Spline.createSpline(nits, mBrightness);</span><br><span class="line">     <span class="comment">//创建Backlight-Nits曲线</span></span><br><span class="line">     mBrightnessToNitsSpline = Spline.createSpline(mBrightness, nits);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>在PhysicalMappingStrategy的构造方法中，首先根据nits数组和normalizedBacklight数组，创建两条样条曲线：Nits-Backlight曲线和Backlight-Nits曲线。然后调用computeSpline()方法，创建Lux-Nits曲线</p>
<h3 id="computeSpline-（插值算法）"><a href="#computeSpline-（插值算法）" class="headerlink" title="computeSpline() （插值算法）"></a>computeSpline() （插值算法）</h3><p>computeSpline()是很重要的一个方法，这个方法中除了创建另外一条Lux-Nits曲线外，还会根据用户当前对亮度的调整，插入用户调整后的值，并调整Lux-Nits曲线，该方法如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/display/BrightnessMappingStrategy.java</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">computeSpline</span><span class="params">()</span> </span>&#123;</span><br><span class="line">     <span class="comment">//得到BrightnessConfiguration中的Lux数组及其对应的Nits数组，并放入Pair对象中</span></span><br><span class="line">     Pair&lt;<span class="keyword">float</span>[], <span class="keyword">float</span>[]&gt; defaultCurve = mConfig.getCurve();</span><br><span class="line">     <span class="comment">//Lux数组</span></span><br><span class="line">     <span class="keyword">float</span>[] defaultLux = defaultCurve.first;</span><br><span class="line">     <span class="comment">//Lux对应的Nits数组</span></span><br><span class="line">     <span class="keyword">float</span>[] defaultNits = defaultCurve.second;</span><br><span class="line">     </span><br><span class="line">     <span class="comment">//创建一个和defaultNits数组等长的数组 defaultBrightness ，用来存放对应的背光值</span></span><br><span class="line">     <span class="comment">//根据Lux-Nit值，从NitsToBacklight曲线中获取背光值</span></span><br><span class="line">     <span class="comment">//即根据 config_autoBrightnessDisplayValuesNits 值从config_screenBrightnessNits 和 config_screenBrightnessBacklight 构建的mNitsToBacklightSpline曲线中获取背光值</span></span><br><span class="line">     <span class="keyword">float</span>[] defaultBrightness = <span class="keyword">new</span> <span class="keyword">float</span>[defaultNits.length];</span><br><span class="line">     <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; defaultBrightness.length; i++) &#123;</span><br><span class="line">          defaultBrightness[i] = mNitsToBrightnessSpline.interpolate(defaultNits[i]);</span><br><span class="line">     &#125;</span><br><span class="line"></span><br><span class="line">     <span class="comment">//对得到的默认背光值进一步加工,如果期间用户手动调节过亮度，需要将调节时的Lux值与背光值添加进对应数组</span></span><br><span class="line">     <span class="comment">//得到调整后的Lux值数组和Backlight值数组</span></span><br><span class="line">     Pair&lt;<span class="keyword">float</span>[], <span class="keyword">float</span>[]&gt; curve = getAdjustedCurve(defaultLux, defaultBrightness, mUserLux,</span><br><span class="line">          mUserBrightness, mAutoBrightnessAdjustment, mMaxGamma);</span><br><span class="line"></span><br><span class="line">     <span class="comment">//最终的Lux值和背光值</span></span><br><span class="line">     <span class="keyword">float</span>[] lux = curve.first;</span><br><span class="line">     <span class="keyword">float</span>[] brightness = curve.second;</span><br><span class="line">     <span class="keyword">float</span>[] nits = <span class="keyword">new</span> <span class="keyword">float</span>[brightness.length];</span><br><span class="line">     </span><br><span class="line">     <span class="comment">//根据背光值，从 config_screenBrightnessNits 和 config_screenBrightnessBacklight 构建的 mBacklightToNitsSpline 曲线中获取Nit值</span></span><br><span class="line">     <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; nits.length; i++) &#123;</span><br><span class="line">          nits[i] = mBrightnessToNitsSpline.interpolate(brightness[i]);</span><br><span class="line">     &#125;</span><br><span class="line">     <span class="comment">//创建Lux-Nits曲线,最终的背光值将根据该曲线和mNitsToBacklightSpline曲线映射而来</span></span><br><span class="line">     mBrightnessSpline = Spline.createSpline(lux, nits);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在这个方法中，首先调用mConfig.getCurve()方法，获取了mConfig对象中的mLux和mNits的拷贝，这两个值就是在创建mConfig时传入的config_autoBrightnessLevels数组和config_autoBrightnessDisplayValuesNits数组。然后利用得到的nits值从曲线mNitsToBacklightSpline中得到背光值数组defaultBacklight。之后调用getAdjustedCurve()方法对defaultBacklight做进一步加工，如果在自动背光打开的情况下，用户没有通过亮度条调节背光，则将返回原数据。此处暂且认为没有操作过亮度条，对getAdjustedCurve()先不做分析。<br>最后，利用defaultBacklight数组从曲线mBacklightToNitsSpline中得到Nits值，然后，创建表示Lux值和对应Nits值的曲线mBrightnessSpline，这也是创建的最后一条全局样条曲线(在某些方法中会创建一些临时曲线)。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>到这里为止，对自动背光样条曲线的创建就分析完成了，整体来看，共创建了三条样条曲线，来对Lux-Nit-Backlight进行映射。这里并没有直接使用Lux和Baclight的映射，而是将Nit作为Lux和Backlight的中间介质。<br>从光学角度而言，我们感知的亮度是指从屏幕反射到眼球中的光的强度，而且这个强度跟光亮度(Nit)有一定关系，光亮度又跟光照度(Lux)有一定关系，因此，如果严格考虑光照度、光亮度、入射光、反射光等调节，相比通过Lux值决定背光值而言，通过Lux值决定Nit值，再由Nit值决定背光值，无疑是最精准的。</p>
<h1 id="AutomaticBrightnessController"><a href="#AutomaticBrightnessController" class="headerlink" title="AutomaticBrightnessController"></a>AutomaticBrightnessController</h1><p>了解了样条曲线的创建后，下面我们开始分析自动背光的调节过程。这个过程还是在DisplayPowerController的setUpAutoBrightness()中。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/display/DisplayPowerController.java</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">DisplayPowerController</span><span class="params">(Context context,</span></span></span><br><span class="line"><span class="params"><span class="function">       DisplayPowerCallbacks callbacks, Handler handler,</span></span></span><br><span class="line"><span class="params"><span class="function">       SensorManager sensorManager, DisplayBlanker blanker, LogicalDisplay logicalDisplay,</span></span></span><br><span class="line"><span class="params"><span class="function">       BrightnessTracker brightnessTracker, BrightnessSetting brightnessSetting,</span></span></span><br><span class="line"><span class="params"><span class="function">       Runnable onBrightnessChangeRunnable)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">       ...</span><br><span class="line">       <span class="comment">//获取映射Lux-Nits-Backlight值的对象</span></span><br><span class="line">       setUpAutoBrightness(resources, handler);</span><br><span class="line">       ...</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">setUpAutoBrightness</span><span class="params">(Resources resources, Handler handler)</span> </span>&#123;</span><br><span class="line">   <span class="keyword">if</span> (!mUseSoftwareAutoBrightnessConfig) &#123;</span><br><span class="line">       <span class="keyword">return</span>;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   mBrightnessMapper = BrightnessMappingStrategy.create(resources, mDisplayDeviceConfig);</span><br><span class="line"></span><br><span class="line">   ...</span><br><span class="line">   <span class="keyword">if</span> (mBrightnessMapper != <span class="keyword">null</span>) &#123;</span><br><span class="line">       ...</span><br><span class="line">       mAutomaticBrightnessController = <span class="keyword">new</span> AutomaticBrightnessController(<span class="keyword">this</span>,</span><br><span class="line">                    handler.getLooper(), mSensorManager, mLightSensor, mBrightnessMapper,</span><br><span class="line">                    lightSensorWarmUpTimeConfig, PowerManager.BRIGHTNESS_MIN,</span><br><span class="line">                    PowerManager.BRIGHTNESS_MAX, dozeScaleFactor, lightSensorRate,</span><br><span class="line">                    initialLightSensorRate, brighteningLightDebounce, darkeningLightDebounce,</span><br><span class="line">                    autoBrightnessResetAmbientLuxAfterWarmUp, ambientBrightnessThresholds,</span><br><span class="line">                    screenBrightnessThresholds, mLogicalDisplay, mContext, mHbmController);</span><br><span class="line">   &#125;</span><br><span class="line">   ...</span><br><span class="line">&#125;  </span><br></pre></td></tr></table></figure>
<p>其中通过AutomaticBrightnessController来采集光照强度、获取自动背光，下面简单介绍了它的初始化过程以及配置入口。</p>
<h2 id="初始化"><a href="#初始化" class="headerlink" title="初始化"></a>初始化</h2><p>AutomaticBrightnessController通过其构造函数完成初始化，其中会对许多成员变量赋值：</p>
<ul>
<li>mBrightnessMapper ：BrightnessMappingStretagy实例，由构造方法传入，用来创建样条曲线、获取自动背光值。</li>
<li>mShortTermModelValid ：表示短期模型是否有效，在构造函数中初始化为true。</li>
<li>mShortTermModelAnchor ：记录用户添加的背光控制点的Lux值，在构造函数中初始化为-1。<br>“短期模型”指的是添加了用户手动调节背光控制点的样条曲线。当短期模型失效后，并不会立即重置数据，而是等待环境光发生较大变化后，才清除用户设置的数据。<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">  </span><br><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/display/AutomaticBrightnessController.java</span></span><br><span class="line"></span><br><span class="line">AutomaticBrightnessController(Injector injector, Callbacks callbacks, Looper looper,</span><br><span class="line">       SensorManager sensorManager, Sensor lightSensor, BrightnessMappingStrategy mapper,</span><br><span class="line">       <span class="keyword">int</span> lightSensorWarmUpTime, <span class="keyword">float</span> brightnessMin, <span class="keyword">float</span> brightnessMax,</span><br><span class="line">       <span class="keyword">float</span> dozeScaleFactor, <span class="keyword">int</span> lightSensorRate, <span class="keyword">int</span> initialLightSensorRate,</span><br><span class="line">       <span class="keyword">long</span> brighteningLightDebounceConfig, <span class="keyword">long</span> darkeningLightDebounceConfig,</span><br><span class="line">       <span class="keyword">boolean</span> resetAmbientLuxAfterWarmUpConfig, HysteresisLevels ambientBrightnessThresholds,</span><br><span class="line">       HysteresisLevels screenBrightnessThresholds, LogicalDisplay display, Context context,</span><br><span class="line">       HighBrightnessModeController hbmController) &#123;</span><br><span class="line">   <span class="comment">//构造函数中初始化多个成员变量</span></span><br><span class="line">   mInjector = injector;</span><br><span class="line">   mContext = context;</span><br><span class="line">   mCallbacks = callbacks;</span><br><span class="line">   mSensorManager = sensorManager;</span><br><span class="line">   <span class="comment">//BrightnessMappingStrategy实例</span></span><br><span class="line">   mBrightnessMapper = mapper;</span><br><span class="line">   <span class="comment">//最小背光值</span></span><br><span class="line">   mScreenBrightnessRangeMinimum = brightnessMin;</span><br><span class="line">   <span class="comment">//最大背光值</span></span><br><span class="line">   mScreenBrightnessRangeMaximum = brightnessMax;</span><br><span class="line">   mLightSensorWarmUpTimeConfig = lightSensorWarmUpTime;</span><br><span class="line">   mDozeScaleFactor = dozeScaleFactor;</span><br><span class="line">   mNormalLightSensorRate = lightSensorRate;</span><br><span class="line">   mInitialLightSensorRate = initialLightSensorRate;</span><br><span class="line">   mCurrentLightSensorRate = -<span class="number">1</span>;</span><br><span class="line">   mBrighteningLightDebounceConfig = brighteningLightDebounceConfig;</span><br><span class="line">   mDarkeningLightDebounceConfig = darkeningLightDebounceConfig;</span><br><span class="line">   mResetAmbientLuxAfterWarmUpConfig = resetAmbientLuxAfterWarmUpConfig;</span><br><span class="line">   mAmbientLightHorizon = AMBIENT_LIGHT_LONG_HORIZON_MILLIS;</span><br><span class="line">   mWeightingIntercept = AMBIENT_LIGHT_LONG_HORIZON_MILLIS;</span><br><span class="line">   mAmbientBrightnessThresholds = ambientBrightnessThresholds;</span><br><span class="line">   mScreenBrightnessThresholds = screenBrightnessThresholds;</span><br><span class="line">   <span class="comment">//短期模型是否有效</span></span><br><span class="line">   mShortTermModelValid = <span class="keyword">true</span>;</span><br><span class="line">   mShortTermModelAnchor = -<span class="number">1</span>;</span><br><span class="line">   <span class="comment">//创建处理Handler的实例</span></span><br><span class="line">   mHandler = <span class="keyword">new</span> AutomaticBrightnessHandler(looper);</span><br><span class="line">   mAmbientLightRingBuffer =</span><br><span class="line">       <span class="keyword">new</span> AmbientLightRingBuffer(mNormalLightSensorRate, mAmbientLightHorizon);</span><br><span class="line"></span><br><span class="line">   <span class="keyword">if</span> (!DEBUG_PRETEND_LIGHT_SENSOR_ABSENT) &#123;</span><br><span class="line">       mLightSensor = lightSensor;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   mActivityTaskManager = ActivityTaskManager.getService();</span><br><span class="line">   mPackageManager = mContext.getPackageManager();</span><br><span class="line">   mTaskStackListener = <span class="keyword">new</span> TaskStackListenerImpl();</span><br><span class="line">   mForegroundAppPackageName = <span class="keyword">null</span>;</span><br><span class="line">   mPendingForegroundAppPackageName = <span class="keyword">null</span>;</span><br><span class="line">   mForegroundAppCategory = ApplicationInfo.CATEGORY_UNDEFINED;</span><br><span class="line">   mPendingForegroundAppCategory = ApplicationInfo.CATEGORY_UNDEFINED;</span><br><span class="line">   mHbmController = hbmController;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h2 id="配置"><a href="#配置" class="headerlink" title="配置"></a>配置</h2><p>在DisplayPowerController中，每次调用updatePowerState()更新状态时，都会对AutomaticBrightnessController进行配置：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/display/DisplayPowerController.java</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">updatePowerState</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    ...</span><br><span class="line">   </span><br><span class="line">    <span class="comment">// Configure auto-brightness.</span></span><br><span class="line">    <span class="keyword">if</span> (mAutomaticBrightnessController != <span class="keyword">null</span>) &#123;</span><br><span class="line">        <span class="comment">//曲线中是否有用户设置的背光控制点</span></span><br><span class="line">        hadUserBrightnessPoint = mAutomaticBrightnessController.hasUserDataPoints();</span><br><span class="line">        <span class="comment">//调用configure()方法开始配置</span></span><br><span class="line">        mAutomaticBrightnessController.configure(autoBrightnessEnabled,</span><br><span class="line">                mBrightnessConfiguration,</span><br><span class="line">                mLastUserSetScreenBrightness,</span><br><span class="line">                userSetBrightnessChanged, autoBrightnessAdjustment,</span><br><span class="line">                autoBrightnessAdjustmentChanged, mPowerRequest.policy);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在以上逻辑中，hadUserBrightnessPoint表示是否在自动背光打开的情况下拖动背光条调节过背光，判断依据是BrightnessMappingStrategy中的mUserLux成员，它表示用户在开启自动背光后手动设置背光时的Lux值：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/display/BrightnessMappingStrategy.java</span></span><br><span class="line"></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">hasUserDataPoints</span><span class="params">()</span> </span>&#123;</span><br><span class="line">	<span class="keyword">return</span> mUserLux != -<span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>有关AutomaticBrightnessController的配置后文会详细讲解，下图展示了使用样条曲线获取自动背光的过程：<br><img src="https://cdn.nlark.com/yuque/0/2021/png/1759879/1630487525698-d6b24c30-d6c8-42ca-a2ce-dfc7bbd77693.png#clientId=u5df7ab52-a4d3-4&crop=0&crop=0&crop=1&crop=1&from=ui&id=u2cdc2793&margin=%5Bobject%20Object%5D&name=image2020-9-25%2014_50_15.png&originHeight=586&originWidth=846&originalType=binary&ratio=1&rotation=0&showTitle=false&size=103181&status=done&style=none&taskId=u9fd1798f-1e2e-4117-a0f9-f43c6d25575&title=" alt="image2020-9-25 14_50_15.png"></p>
<h1 id="AutomaticBrightnessController-configure"><a href="#AutomaticBrightnessController-configure" class="headerlink" title="AutomaticBrightnessController.configure()"></a>AutomaticBrightnessController.configure()</h1><p>由上一节可知在updatePowerState()中会调用configure()方法来配置AutomaticBrightnessController.configure()方法如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/display/AutomaticBrightnessController.java</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">configure</span><span class="params">(<span class="keyword">boolean</span> enable, <span class="meta">@Nullable</span> BrightnessConfiguration configuration,</span></span></span><br><span class="line"><span class="params"><span class="function">     <span class="keyword">float</span> brightness, <span class="keyword">boolean</span> userChangedBrightness, <span class="keyword">float</span> adjustment,</span></span></span><br><span class="line"><span class="params"><span class="function">     <span class="keyword">boolean</span> userChangedAutoBrightnessAdjustment, <span class="keyword">int</span> displayPolicy)</span> </span>&#123;</span><br><span class="line">     mHbmController.setAutoBrightnessEnabled(enable);</span><br><span class="line">     <span class="comment">// While dozing, the application processor may be suspended which will prevent us from</span></span><br><span class="line">     <span class="comment">// receiving new information from the light sensor. On some devices, we may be able to</span></span><br><span class="line">     <span class="comment">// switch to a wake-up light sensor instead but for now we will simply disable the sensor</span></span><br><span class="line">     <span class="comment">// and hold onto the last computed screen auto brightness.  We save the dozing flag for</span></span><br><span class="line">     <span class="comment">// debugging purposes.</span></span><br><span class="line">     <span class="comment">//屏幕是否要进入doze状态</span></span><br><span class="line">     <span class="keyword">boolean</span> dozing = (displayPolicy == DisplayPowerRequest.POLICY_DOZE);</span><br><span class="line"></span><br><span class="line">     <span class="comment">//设置BrightnessConfigure对象，若BrightnessConfigure发生改变，会重置短期模型，并返回true</span></span><br><span class="line">     <span class="keyword">boolean</span> changed = setBrightnessConfiguration(configuration);</span><br><span class="line"></span><br><span class="line">     <span class="comment">//设置Display状态，若发生改变，返回true</span></span><br><span class="line">     changed |= setDisplayPolicy(displayPolicy);</span><br><span class="line">     <span class="keyword">if</span> (userChangedAutoBrightnessAdjustment) &#123;</span><br><span class="line">          changed |= setAutoBrightnessAdjustment(adjustment);</span><br><span class="line">     &#125;</span><br><span class="line"></span><br><span class="line">     <span class="comment">//如果在自动背光开启的情况下调节了背光，需要将当前的Lux值和用户设置的背光添加到样条曲线中</span></span><br><span class="line">     <span class="keyword">if</span> (userChangedBrightness &amp;&amp; enable) &#123;</span><br><span class="line">          <span class="comment">// Update the brightness curve with the new user control point. It&#x27;s critical this</span></span><br><span class="line">          <span class="comment">// happens after we update the autobrightness adjustment since it may reset it.</span></span><br><span class="line">          changed |= setScreenBrightnessByUser(brightness);</span><br><span class="line">     &#125;</span><br><span class="line">     <span class="keyword">final</span> <span class="keyword">boolean</span> userInitiatedChange =</span><br><span class="line">           userChangedBrightness || userChangedAutoBrightnessAdjustment;</span><br><span class="line">     <span class="keyword">if</span> (userInitiatedChange &amp;&amp; enable &amp;&amp; !dozing) &#123;</span><br><span class="line">          <span class="comment">//记录旧值</span></span><br><span class="line">          prepareBrightnessAdjustmentSample();</span><br><span class="line">     &#125;</span><br><span class="line"></span><br><span class="line">     <span class="comment">//注册或解除注册LightSensor</span></span><br><span class="line">     changed |= setLightSensorEnabled(enable &amp;&amp; !dozing);</span><br><span class="line">     <span class="keyword">if</span> (changed) &#123;</span><br><span class="line">          updateAutoBrightness(<span class="keyword">false</span> <span class="comment">/*sendUpdate*/</span>, userInitiatedChange);</span><br><span class="line">     &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>enable ：表示自动背光是否可用。</li>
<li>configuration ：BrightnessConfiguration对象，携带有用于创建样条曲线的Lux值数组和对应的Nit值数组，每一个用户可对应一个该对象，由DisplayManagerService负责设置。</li>
<li>brightness ：标准化后的当前背光值，介于[0, 1]。</li>
<li>userChangedBrightness ：表示用户是否手动通过拖动背光条设置过背光。</li>
<li>adjustment ：自动背光调整值。</li>
<li>userChangedAutoBrightnessAdjustment ：表示自动背光调整值adjustment是否发生变化。</li>
<li>displayPolicy ：当前请求的屏幕状态。</li>
</ul>
<p>在configure()方法中涉及了许多与自适应背光调节相关的配置方法：</p>
<ul>
<li>setBrightnessConfiguration()</li>
<li>setDisplayPolicy()</li>
<li>setAutoBrightnessAdjustment()</li>
<li>setScreenBrightnessByUser()</li>
<li>prepareBrightnessAdjustmentSample()</li>
<li>setLightSensorEnabled()</li>
<li>updateAutoBrightness()</li>
</ul>
<p>下面将逐一分析每个配置方法。</p>
<h2 id="setBrightnessConfiguration"><a href="#setBrightnessConfiguration" class="headerlink" title="setBrightnessConfiguration()"></a>setBrightnessConfiguration()</h2><p>这个方法负责配置当前自动背光的BrighnessConfiguration对象，该对象中带有Lux数组和Nits数组，用来创建样条曲线和获取背光值。该方法如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/display/AutomaticBrightnessController.java</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">setBrightnessConfiguration</span><span class="params">(BrightnessConfiguration configuration)</span> </span>&#123;</span><br><span class="line">    <span class="comment">//调用进入mBrightnessMapper中，也就是BrightnessMappingStrategy</span></span><br><span class="line">    <span class="keyword">if</span> (mBrightnessMapper.setBrightnessConfiguration(configuration)) &#123;</span><br><span class="line">        <span class="comment">//如果 BrightnessConfiguration对象发生改变，重置用户设置的短期模型</span></span><br><span class="line">        resetShortTermModel();</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/display/BrightnessMappingStrategy.java</span></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">setBrightnessConfiguration</span><span class="params">(<span class="meta">@Nullable</span> BrightnessConfiguration config)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (config == <span class="keyword">null</span>) &#123;</span><br><span class="line">        config = mDefaultConfig;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (config.equals(mConfig)) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (mLoggingEnabled) &#123;</span><br><span class="line">        PLOG.start(<span class="string">&quot;brightness configuration&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 如果BrightnessConfiguration对象发生改变，则使用该新对象，并调用computeSpline()方法重新创建Lux-Nits曲线</span></span><br><span class="line">    mConfig = config;</span><br><span class="line">    <span class="comment">//重新创建Lux-Nits曲线</span></span><br><span class="line">    computeSpline();</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>如果BrightnessConfiguration对象发生改变，则会根据BrightnessConfiguration中携带的Lux数组和Nits数组重新创建曲线，并清除之前用户的配置。那么BrightnessConfiguration对象何时会改变呢？答案是在切换用户时。针对每个用户，都会有一个对应的BrightnessConfiguration对象，所以当切换用户后，设备的自适应背光调节方式将可能发生改变。<br>当BrightnessConfiguration对象发生改变且在BrighnessMappingStragety中设置完成后，将会通过resetShortTermModel()方法清除原有用户的配置：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/display/AutomaticBrightnessController.java</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">resetShortTermModel</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="comment">//清除用户添加的背光控制点</span></span><br><span class="line">    mBrightnessMapper.clearUserDataPoints();</span><br><span class="line">    <span class="comment">//表示短期模型仍旧有效</span></span><br><span class="line">    mShortTermModelValid = <span class="keyword">true</span>;</span><br><span class="line">    mShortTermModelAnchor = -<span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/display/BrightnessMappingStrategy.java</span></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">clearUserDataPoints</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (mUserLux != -<span class="number">1</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> (mLoggingEnabled) &#123;</span><br><span class="line">            Slog.d(TAG, <span class="string">&quot;clearUserDataPoints: &quot;</span> + mAutoBrightnessAdjustment + <span class="string">&quot; =&gt; 0&quot;</span>);</span><br><span class="line">            PLOG.start(<span class="string">&quot;clear user data points&quot;</span>)</span><br><span class="line">                .logPoint(<span class="string">&quot;user data point&quot;</span>, mUserLux, mUserBrightness);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//重置自动背光调节值为0</span></span><br><span class="line">        mAutoBrightnessAdjustment = <span class="number">0</span>;</span><br><span class="line">        <span class="comment">//重置用户设置背光时的Lux值为-1</span></span><br><span class="line">        mUserLux = -<span class="number">1</span>;</span><br><span class="line">        <span class="comment">//重置用户设置的背光值为-1</span></span><br><span class="line">        mUserBrightness = -<span class="number">1</span>;</span><br><span class="line">        <span class="comment">//重新创建Lux-Nits曲线</span></span><br><span class="line">        computeSpline();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在clearUserDataPoint()方法中将全局自动背光调节值、用户设置背光时Lux值和用户设置的背光值进行重置，然后重新创建Lux-Nits曲线。</p>
<h2 id="setDisplayPolicy"><a href="#setDisplayPolicy" class="headerlink" title="setDisplayPolicy()"></a>setDisplayPolicy()</h2><p>这个方法主要根据屏幕状态来设置mShortTermModelValid的值，该方法如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/display/AutomaticBrightnessController.java</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">boolean</span> <span class="title">setDisplayPolicy</span><span class="params">(<span class="keyword">int</span> policy)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (mDisplayPolicy == policy) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">final</span> <span class="keyword">int</span> oldPolicy = mDisplayPolicy;</span><br><span class="line">    mDisplayPolicy = policy;</span><br><span class="line">    <span class="keyword">if</span> (mLoggingEnabled) &#123;</span><br><span class="line">        Slog.d(TAG, <span class="string">&quot;Display policy transitioning from &quot;</span> + oldPolicy + <span class="string">&quot; to &quot;</span> + policy);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//如果Display状态值由交互变为不可交互状态</span></span><br><span class="line">    <span class="keyword">if</span> (!isInteractivePolicy(policy) &amp;&amp; isInteractivePolicy(oldPolicy)) &#123;</span><br><span class="line">        <span class="comment">//送一个300s的延迟消息，300s后将mShortTermModelValid置为false</span></span><br><span class="line">        mHandler.sendEmptyMessageDelayed(MSG_INVALIDATE_SHORT_TERM_MODEL,</span><br><span class="line">                                         mBrightnessMapper.getShortTermModelTimeout());</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (isInteractivePolicy(policy) &amp;&amp; !isInteractivePolicy(oldPolicy)) &#123;</span><br><span class="line">        <span class="comment">//如果Display状态值由不可交互变为交互状态，则移除延时消息的发送</span></span><br><span class="line">        mHandler.removeMessages(MSG_INVALIDATE_SHORT_TERM_MODEL);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>从这个方法中可以看出，当屏幕状态进入doze或者asleep后，会发送一个延时Handler消息，并在到达时间后将mShortTermModelValid值置为false，该值用来标示用户设置的短期模型失效。<br>其中mShortTermModelTimeout最终来源如下，表示300秒。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="number">1.</span> mBrightnessMapper.getShortTermModelTimeout() 也就是 BrightnessMappingStrategy.getShortTermModelTimeout()</span><br><span class="line">    </span><br><span class="line"><span class="number">2.</span> mBrightnessMapper.mShortTermModelTimeout</span><br><span class="line"></span><br><span class="line"><span class="number">3.</span> <span class="function"><span class="keyword">private</span> <span class="title">SimpleMappingStrategy</span><span class="params">(<span class="keyword">float</span>[] lux, <span class="keyword">int</span>[] brightness, <span class="keyword">float</span> maxGamma,</span></span></span><br><span class="line"><span class="params"><span class="function">                <span class="keyword">long</span> timeout)</span> </span>&#123;</span><br><span class="line">       mShortTermModelTimeout = timeout;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line"><span class="number">4.</span> <span class="keyword">new</span> SimpleMappingStrategy(luxLevels, brightnessLevelsBacklight, autoBrightnessAdjustmentMaxGamma, shortTermModelTimeout);</span><br><span class="line"></span><br><span class="line"><span class="number">5.</span> <span class="keyword">long</span> shortTermModelTimeout = resources.getInteger(com.android.internal.R.integer.config_autoBrightnessShortTermModelTimeout);</span><br><span class="line"></span><br><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/core/res/res/values/config.xml</span></span><br><span class="line">&lt;integer name=<span class="string">&quot;config_autoBrightnessShortTermModelTimeout&quot;</span>&gt;<span class="number">300000</span>&lt;/integer&gt;</span><br></pre></td></tr></table></figure>

<h2 id="setAutoBrightnessAdjustment"><a href="#setAutoBrightnessAdjustment" class="headerlink" title="setAutoBrightnessAdjustment()"></a>setAutoBrightnessAdjustment()</h2><p>设置自动背光调整值，前提是自动背光调整值已经发生变化，调用此方法后，将调用到BrightnessMappingStragety中的同名方法，如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/display/AutomaticBrightnessController.java</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">boolean</span> <span class="title">setAutoBrightnessAdjustment</span><span class="params">(<span class="keyword">float</span> adjustment)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> mBrightnessMapper.setAutoBrightnessAdjustment(adjustment);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/display/BrightnessMappingStrategy.java</span></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">setAutoBrightnessAdjustment</span><span class="params">(<span class="keyword">float</span> adjustment)</span> </span>&#123;</span><br><span class="line">    <span class="comment">//对adjustment进行限制，取值范围为[-1,1]</span></span><br><span class="line">    adjustment = MathUtils.constrain(adjustment, -<span class="number">1</span>, <span class="number">1</span>);</span><br><span class="line">    <span class="keyword">if</span> (adjustment == mAutoBrightnessAdjustment) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (mLoggingEnabled) &#123;</span><br><span class="line">        Slog.d(TAG, <span class="string">&quot;setAutoBrightnessAdjustment: &quot;</span> + mAutoBrightnessAdjustment + <span class="string">&quot; =&gt; &quot;</span> +</span><br><span class="line">               adjustment);</span><br><span class="line">        PLOG.start(<span class="string">&quot;auto-brightness adjustment&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//设置自动背光调节值</span></span><br><span class="line">    mAutoBrightnessAdjustment = adjustment;</span><br><span class="line">    <span class="comment">//重新创建Lux-Nits曲线</span></span><br><span class="line">    computeSpline();</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>如果mAutoBrightnessAdjustment发生变化，也会重新创建Lux-Nits曲线。可以说，Lux-Nits曲线的创建和自动背光调整值有着莫大的关系，在默认情况下，这个值为0。</p>
<h2 id="setScreenBrightnessByUser"><a href="#setScreenBrightnessByUser" class="headerlink" title="setScreenBrightnessByUser()"></a>setScreenBrightnessByUser()</h2><p>将用户拖动背光条设置的背光和当时的Lux值添加到用于创建曲线的数组中，并重新创建Lux-Nits曲线：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/display/AutomaticBrightnessController.java</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">boolean</span> <span class="title">setScreenBrightnessByUser</span><span class="params">(<span class="keyword">float</span> brightness)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!mAmbientLuxValid) &#123;</span><br><span class="line">        <span class="comment">// If we don&#x27;t have a valid ambient lux then we don&#x27;t have a valid brightness anyways,</span></span><br><span class="line">        <span class="comment">// and we can&#x27;t use this data to add a new control point to the short-term model.</span></span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//将当前的Lux值和用户设置的背光值添加到曲线中</span></span><br><span class="line">    mBrightnessMapper.addUserDataPoint(mAmbientLux, brightness);</span><br><span class="line">    <span class="comment">//设置短期模型有效</span></span><br><span class="line">    mShortTermModelValid = <span class="keyword">true</span>;</span><br><span class="line">    <span class="comment">//记录该背光控制点的Lux值</span></span><br><span class="line">    mShortTermModelAnchor = mAmbientLux;</span><br><span class="line">    <span class="keyword">if</span> (mLoggingEnabled) &#123;</span><br><span class="line">        Slog.d(TAG, <span class="string">&quot;ShortTermModel: anchor=&quot;</span> + mShortTermModelAnchor);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h2 id="prepareBrightnessAdjustmentSample"><a href="#prepareBrightnessAdjustmentSample" class="headerlink" title="prepareBrightnessAdjustmentSample()"></a>prepareBrightnessAdjustmentSample()</h2><p>当配置元素发生改变后，对Lux和背光旧值进行记录：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/display/AutomaticBrightnessController.java</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">prepareBrightnessAdjustmentSample</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!mBrightnessAdjustmentSamplePending) &#123;</span><br><span class="line">        mBrightnessAdjustmentSamplePending = <span class="keyword">true</span>;</span><br><span class="line">        <span class="comment">//记录Lux和背光旧值</span></span><br><span class="line">        mBrightnessAdjustmentSampleOldLux = mAmbientLuxValid ? mAmbientLux : -<span class="number">1</span>;</span><br><span class="line">        mBrightnessAdjustmentSampleOldBrightness = mScreenAutoBrightness;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        mHandler.removeMessages(MSG_BRIGHTNESS_ADJUSTMENT_SAMPLE);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    mHandler.sendEmptyMessageDelayed(MSG_BRIGHTNESS_ADJUSTMENT_SAMPLE,</span><br><span class="line">                                     BRIGHTNESS_ADJUSTMENT_SAMPLE_DEBOUNCE_MILLIS);</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h2 id="setLightSensorEnabled"><a href="#setLightSensorEnabled" class="headerlink" title="setLightSensorEnabled()"></a>setLightSensorEnabled()</h2><p>负责LightSensor的注册和解除注册，当设备处于亮屏状态且打开自适应背光调节时，将注册一个LightSensor，以监听环境光的强度变化，该方法如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/display/AutomaticBrightnessController.java</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">boolean</span> <span class="title">setLightSensorEnabled</span><span class="params">(<span class="keyword">boolean</span> enable)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (enable) &#123;<span class="comment">//注册</span></span><br><span class="line">        <span class="keyword">if</span> (!mLightSensorEnabled) &#123;</span><br><span class="line">            <span class="comment">//启用LightSensor</span></span><br><span class="line">            mLightSensorEnabled = <span class="keyword">true</span>;</span><br><span class="line">            <span class="comment">//LightSensor开启时间</span></span><br><span class="line">            mLightSensorEnableTime = SystemClock.uptimeMillis();</span><br><span class="line">            <span class="comment">//LightSensor的事件速率</span></span><br><span class="line">            mCurrentLightSensorRate = mInitialLightSensorRate;</span><br><span class="line">            registerForegroundAppUpdater();</span><br><span class="line">            <span class="comment">//注册LightSensor</span></span><br><span class="line">            mSensorManager.registerListener(mLightSensorListener, mLightSensor,</span><br><span class="line">                                            mCurrentLightSensorRate * <span class="number">1000</span>, mHandler);</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (mLightSensorEnabled) &#123;<span class="comment">//解除注册</span></span><br><span class="line">        mLightSensorEnabled = <span class="keyword">false</span>;</span><br><span class="line">        mAmbientLuxValid = !mResetAmbientLuxAfterWarmUpConfig;</span><br><span class="line">        mScreenAutoBrightness = PowerManager.BRIGHTNESS_INVALID_FLOAT;</span><br><span class="line">        mRecentLightSamples = <span class="number">0</span>;</span><br><span class="line">        mAmbientLightRingBuffer.clear();</span><br><span class="line">        mCurrentLightSensorRate = -<span class="number">1</span>;</span><br><span class="line">        mHandler.removeMessages(MSG_UPDATE_AMBIENT_LUX);</span><br><span class="line">        unregisterForegroundAppUpdater();</span><br><span class="line">        mSensorManager.unregisterListener(mLightSensorListener);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>当环境光强度发生变化后，将回调mLightSensorListener接口的方法来更新：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/display/AutomaticBrightnessController.java</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> SensorEventListener mLightSensorListener = <span class="keyword">new</span> SensorEventListener() &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">onSensorChanged</span><span class="params">(SensorEvent event)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (mLightSensorEnabled) &#123;</span><br><span class="line">            <span class="keyword">final</span> <span class="keyword">long</span> time = SystemClock.uptimeMillis();</span><br><span class="line">            <span class="keyword">final</span> <span class="keyword">float</span> lux = event.values[<span class="number">0</span>];</span><br><span class="line">            <span class="comment">//处理LightSensor的上报事件，并更新Lux值</span></span><br><span class="line">            handleLightSensorEvent(time, lux);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">onAccuracyChanged</span><span class="params">(Sensor sensor, <span class="keyword">int</span> accuracy)</span> </span>&#123;</span><br><span class="line">        <span class="comment">// Not used.</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">handleLightSensorEvent</span><span class="params">(<span class="keyword">long</span> time, <span class="keyword">float</span> lux)</span> </span>&#123;</span><br><span class="line">    Trace.traceCounter(Trace.TRACE_TAG_POWER, <span class="string">&quot;ALS&quot;</span>, (<span class="keyword">int</span>) lux);</span><br><span class="line">    mHandler.removeMessages(MSG_UPDATE_AMBIENT_LUX);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (mAmbientLightRingBuffer.size() == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="comment">// switch to using the steady-state sample rate after grabbing the initial light sample</span></span><br><span class="line">        adjustLightSensorRate(mNormalLightSensorRate);</span><br><span class="line">    &#125;</span><br><span class="line">    applyLightSensorMeasurement(time, lux);</span><br><span class="line">    updateAmbientLux(time);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="updateAutoBrightness"><a href="#updateAutoBrightness" class="headerlink" title="updateAutoBrightness()"></a>updateAutoBrightness()</h2><p>更新最终计算的自动背光值，其中会将这个值与调节屏幕变暗或变亮的阈值做判断，然后再决定是否要应用该背光值：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/display/AutomaticBrightnessController.java</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">updateAutoBrightness</span><span class="params">(<span class="keyword">boolean</span> sendUpdate, <span class="keyword">boolean</span> isManuallySet)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!mAmbientLuxValid) &#123;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//根据Lux值从样条曲线中获取对应的背光值，区间为(0,1.0]的浮点数</span></span><br><span class="line">    <span class="keyword">float</span> value = mBrightnessMapper.getBrightness(mAmbientLux, mForegroundAppPackageName,</span><br><span class="line">            mForegroundAppCategory);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//计算最终的背光值</span></span><br><span class="line">    <span class="keyword">float</span> newScreenAutoBrightness = clampScreenBrightness(value);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// The min/max range can change for brightness due to HBM. See if the current brightness</span></span><br><span class="line">    <span class="comment">// value still falls within the current range (which could have changed).</span></span><br><span class="line">    <span class="comment">//对背光值进行合法检测</span></span><br><span class="line">    <span class="keyword">final</span> <span class="keyword">boolean</span> currentBrightnessWithinAllowedRange = BrightnessSynchronizer.floatEquals(</span><br><span class="line">            mScreenAutoBrightness, clampScreenBrightness(mScreenAutoBrightness));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// If screenAutoBrightness is set, we should have screen&#123;Brightening,Darkening&#125;Threshold,</span></span><br><span class="line">    <span class="comment">// in which case we ignore the new screen brightness if it doesn&#x27;t differ enough from the</span></span><br><span class="line">    <span class="comment">// previous one.</span></span><br><span class="line">    <span class="comment">//如果新的背光值介于变暗和变亮阈值之间，则不进行背光调节</span></span><br><span class="line">    <span class="keyword">if</span> (!Float.isNaN(mScreenAutoBrightness)</span><br><span class="line">            &amp;&amp; !isManuallySet</span><br><span class="line">            &amp;&amp; newScreenAutoBrightness &gt; mScreenDarkeningThreshold</span><br><span class="line">            &amp;&amp; newScreenAutoBrightness &lt; mScreenBrighteningThreshold</span><br><span class="line">            &amp;&amp; currentBrightnessWithinAllowedRange) &#123;</span><br><span class="line">        <span class="keyword">if</span> (mLoggingEnabled) &#123;</span><br><span class="line">            Slog.d(TAG, <span class="string">&quot;ignoring newScreenAutoBrightness: &quot;</span></span><br><span class="line">                    + mScreenDarkeningThreshold + <span class="string">&quot; &lt; &quot;</span> + newScreenAutoBrightness</span><br><span class="line">                    + <span class="string">&quot; &lt; &quot;</span> + mScreenBrighteningThreshold);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (!BrightnessSynchronizer.floatEquals(mScreenAutoBrightness,</span><br><span class="line">            newScreenAutoBrightness)) &#123;</span><br><span class="line">        <span class="keyword">if</span> (mLoggingEnabled) &#123;</span><br><span class="line">            Slog.d(TAG, <span class="string">&quot;updateAutoBrightness: &quot;</span></span><br><span class="line">                    + <span class="string">&quot;mScreenAutoBrightness=&quot;</span> + mScreenAutoBrightness + <span class="string">&quot;, &quot;</span></span><br><span class="line">                    + <span class="string">&quot;newScreenAutoBrightness=&quot;</span> + newScreenAutoBrightness);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//否则使用新的背光值</span></span><br><span class="line">        mScreenAutoBrightness = newScreenAutoBrightness;</span><br><span class="line">        mScreenBrighteningThreshold = clampScreenBrightness(</span><br><span class="line">                mScreenBrightnessThresholds.getBrighteningThreshold(newScreenAutoBrightness));</span><br><span class="line">        mScreenDarkeningThreshold = clampScreenBrightness(</span><br><span class="line">                mScreenBrightnessThresholds.getDarkeningThreshold(newScreenAutoBrightness));</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (sendUpdate) &#123;</span><br><span class="line">            <span class="comment">//更新背光</span></span><br><span class="line">            mCallbacks.updateBrightness();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>通过当前的Lux值从mBrightnessMapper中获取到了最终的背光值，getBrightness()方法会使用mBrightnessSpline曲线和mNitsToBacklightSpline曲线完成Lux值到Nits值再到Backlight值的转换：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/display/BrightnessMappingStrategy.java</span></span><br><span class="line"></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">float</span> <span class="title">getBrightness</span><span class="params">(<span class="keyword">float</span> lux, String packageName,</span></span></span><br><span class="line"><span class="params"><span class="function">                           <span class="meta">@ApplicationInfo</span>.Category <span class="keyword">int</span> category)</span> </span>&#123;</span><br><span class="line">    <span class="comment">//根据Lux值，从mBrightnessSpline曲线中获取对应的Nits值</span></span><br><span class="line">    <span class="keyword">float</span> nits = mBrightnessSpline.interpolate(lux);</span><br><span class="line">    <span class="comment">//再根据Nits值，从mNitsToBacklightSpline曲线中获取对应的背光值</span></span><br><span class="line">    <span class="keyword">float</span> brightness = mNitsToBrightnessSpline.interpolate(nits);</span><br><span class="line">    <span class="comment">// Correct the brightness according to the current application and its category, but</span></span><br><span class="line">    <span class="comment">// only if no user data point is set (as this will override the user setting).</span></span><br><span class="line">    <span class="keyword">if</span> (mUserLux == -<span class="number">1</span>) &#123;</span><br><span class="line">        brightness = correctBrightness(brightness, packageName, category);</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (mLoggingEnabled) &#123;</span><br><span class="line">        Slog.d(TAG, <span class="string">&quot;user point set, correction not applied&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> brightness;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="adjustment值的计算"><a href="#adjustment值的计算" class="headerlink" title="adjustment值的计算"></a>adjustment值的计算</h1><p>在Android 8.1中，当用户在开启自动背光的场景下手动拖动背光条调节时，实际上是调用setScreenAutoBrightnessAdjustment()方法调整了自动背光调整值，然后通过自动背光调整值计算出新的自动背光值。<br>而从Android 9.0开始就完全不同，它会直接设置用户选择的背光值作为设备新的背光值，还会将此时的环境光强Lux值和设置的背光值作为控制点添加到创建样条曲线的数组中，并重新创建样条曲线。除此之外，自动背光的调整值将不再由用户主动设置(虽然在DMS中保留了setTemporaryAutoBrightnessAdjustment()用于调整adjustment值)，而是根据用户的调整自动推断得出，下面就从代码中开始分析这些新机制的实现。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/display/DisplayPowerController.java</span></span><br><span class="line"></span><br><span class="line"><span class="number">1.</span> <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setTemporaryAutoBrightnessAdjustment</span><span class="params">(<span class="keyword">float</span> adjustment)</span> </span>&#123;</span><br><span class="line">        Message msg = mHandler.obtainMessage(MSG_SET_TEMPORARY_AUTO_BRIGHTNESS_ADJUSTMENT,</span><br><span class="line">                Float.floatToIntBits(adjustment), <span class="number">0</span> <span class="comment">/*unused*/</span>);</span><br><span class="line">        msg.sendToTarget();</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line"><span class="number">2.</span> <span class="keyword">case</span> MSG_SET_TEMPORARY_AUTO_BRIGHTNESS_ADJUSTMENT:</span><br><span class="line">        mTemporaryAutoBrightnessAdjustment = Float.intBitsToFloat(msg.arg1);</span><br><span class="line">        updatePowerState();</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line"><span class="number">3.</span> updatePowerState() &#123;</span><br><span class="line">       AutomaticBrightnessController.configure() </span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/display/AutomaticBrightnessController.java</span></span><br><span class="line"><span class="number">4.</span> AutomaticBrightnessController.configure() </span><br><span class="line"></span><br><span class="line"><span class="number">5.</span> <span class="keyword">final</span> <span class="keyword">boolean</span> userSetBrightnessChanged = updateUserSetScreenBrightness();</span><br><span class="line">   AutomaticBrightnessController.setScreenBrightnessByUser() </span><br><span class="line">    </span><br><span class="line"><span class="number">6.</span> mBrightnessMapper.addUserDataPoint(mAmbientLux, brightness);</span><br><span class="line"></span><br><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/display/BrightnessMappingStrategy.java</span></span><br><span class="line"><span class="number">7.</span> BrightnessMappingStrategy.addUserDataPoint()</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h2 id="addUserDataPoint"><a href="#addUserDataPoint" class="headerlink" title="addUserDataPoint()"></a>addUserDataPoint()</h2><p>当通过背光条调节背光时，将调用DisplayPowerController中的setTemporaryAutoBrightnessAdjustment()方法，并执行手动背光调节流程，然后执行自动背光控制器的配置流程。由于用户对背光的手动设置，因此将有:</p>
<ul>
<li>mLastUserSetScreenBrightness：由默认的0变为用户设置的背光值;</li>
<li>userChangedBrightness：由false变为true。</li>
</ul>
<p>此时这两个值已经发生改变，但其他值还未发生变化。那么结合AutomaticBrightnessController的配置，在调用configure()方法时将调用setScreenBrightnessByUser()方法，用于将用户拖动背光条设置的背光和当时的Lux值添加到用于创建样条曲线的数组中，具体是调用BrigtnessMappingStrategy对象的addUserDataPoint()方法实现的，在这个方法中将会通过计算得出新的adjustment值。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/display/BrightnessMappingStrategy.java</span></span><br><span class="line"></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">addUserDataPoint</span><span class="params">(<span class="keyword">float</span> lux, <span class="keyword">float</span> brightness)</span> </span>&#123;</span><br><span class="line">    <span class="comment">//获取默认配置值创建的样条曲线中Lux值对应的背光值</span></span><br><span class="line">    <span class="keyword">float</span> unadjustedBrightness = getUnadjustedBrightness(lux);</span><br><span class="line">    <span class="keyword">if</span> (mLoggingEnabled) &#123;</span><br><span class="line">        Slog.d(TAG, <span class="string">&quot;addUserDataPoint: (&quot;</span> + lux + <span class="string">&quot;,&quot;</span> + brightness + <span class="string">&quot;)&quot;</span>);</span><br><span class="line">        PLOG.start(<span class="string">&quot;add user data point&quot;</span>)</span><br><span class="line">                .logPoint(<span class="string">&quot;user data point&quot;</span>, lux, brightness)</span><br><span class="line">                .logPoint(<span class="string">&quot;current brightness&quot;</span>, lux, unadjustedBrightness);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//通过传入的三个值推断自动背光调整值</span></span><br><span class="line">    <span class="keyword">float</span> adjustment = inferAutoBrightnessAdjustment(mMaxGamma,</span><br><span class="line">            brightness <span class="comment">/* desiredBrightness */</span>,</span><br><span class="line">            unadjustedBrightness <span class="comment">/* currentBrightness */</span>);</span><br><span class="line">    <span class="keyword">if</span> (mLoggingEnabled) &#123;</span><br><span class="line">        Slog.d(TAG, <span class="string">&quot;addUserDataPoint: &quot;</span> + mAutoBrightnessAdjustment + <span class="string">&quot; =&gt; &quot;</span> +</span><br><span class="line">                adjustment);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//更新自动背光调整值</span></span><br><span class="line">    mAutoBrightnessAdjustment = adjustment;</span><br><span class="line">    <span class="comment">//用户在开启自动背光情况下拖动背光条调节背光时的Lux值</span></span><br><span class="line">    mUserLux = lux;</span><br><span class="line">    <span class="comment">//用户在开启自动背光情况下拖动背光条调节时的背光值</span></span><br><span class="line">    mUserBrightness = brightness;</span><br><span class="line">    <span class="comment">//重新创建Lux-Nits样条曲线</span></span><br><span class="line">    computeSpline();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>首先将从配置文件中的数组值创建的原始样条曲线中获取当前Lux的值。调用getUnadjustedBrightness()获取默认配置值创建的样条曲线中Lux值对应的背光值。</p>
<h3 id="getUnadjustedBrightness"><a href="#getUnadjustedBrightness" class="headerlink" title="getUnadjustedBrightness()"></a>getUnadjustedBrightness()</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/display/BrightnessMappingStrategy.java</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">float</span> <span class="title">getUnadjustedBrightness</span><span class="params">(<span class="keyword">float</span> lux)</span> </span>&#123;</span><br><span class="line">    <span class="comment">//获取BrightnessConfiguration中的Lux-Nits映射</span></span><br><span class="line">    Pair&lt;<span class="keyword">float</span>[], <span class="keyword">float</span>[]&gt; curve = mConfig.getCurve();</span><br><span class="line">    <span class="comment">//创建一个临时Lux-Nits样条曲线</span></span><br><span class="line">    Spline spline = Spline.createSpline(curve.first, curve.second);</span><br><span class="line">    <span class="comment">//根据Lux值得到Nits值，再根据Nits值从mNitsToBacklightSpline中得到对应的背光值</span></span><br><span class="line">    <span class="keyword">return</span> mNitsToBacklightSpline.interpolate(spline.interpolate(lux));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="inferAutoBrightnessAdjustment"><a href="#inferAutoBrightnessAdjustment" class="headerlink" title="inferAutoBrightnessAdjustment()"></a>inferAutoBrightnessAdjustment()</h3><p>开始计算自动背光调整值，调用inferAutoBrightnessAdjustment()方法，并且将根据以下三个值用公式计算出新的adjustment值：</p>
<ul>
<li>配置文件中的MaxGamma值；</li>
<li>原始样条曲线中此Lux值对应的标准化背光值；</li>
<li>用户所调整的目标背光值。<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/display/BrightnessMappingStrategy.java</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">float</span> <span class="title">inferAutoBrightnessAdjustment</span><span class="params">(<span class="keyword">float</span> maxGamma, <span class="keyword">float</span> desiredBrightness,</span></span></span><br><span class="line"><span class="params"><span class="function">        <span class="keyword">float</span> currentBrightness)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">float</span> adjustment = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">float</span> gamma = Float.NaN;</span><br><span class="line">    <span class="comment">// Extreme edge cases: use a simpler heuristic, as proper gamma correction around the edges</span></span><br><span class="line">    <span class="comment">// affects the curve rather drastically.</span></span><br><span class="line">    <span class="keyword">if</span> (currentBrightness &lt;= <span class="number">0.1f</span> || currentBrightness &gt;= <span class="number">0.9f</span>) &#123;</span><br><span class="line">        <span class="comment">//如果当前标准化的背光值介于[0.1, 0.9]之间</span></span><br><span class="line">        adjustment = (desiredBrightness - currentBrightness);</span><br><span class="line">    <span class="comment">// Edge case: darkest adjustment possible.</span></span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (desiredBrightness == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="comment">//如果Lux值对应的背光值为0</span></span><br><span class="line">        adjustment = -<span class="number">1</span>;</span><br><span class="line">    <span class="comment">// Edge case: brightest adjustment possible.</span></span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (desiredBrightness == <span class="number">1</span>) &#123;</span><br><span class="line">        <span class="comment">//如果Lux值对应的背光值为最大值</span></span><br><span class="line">        adjustment = +<span class="number">1</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="comment">// current^gamma = desired =&gt; gamma = log[current](desired)</span></span><br><span class="line">        <span class="comment">//计算adjustment值的数学公式</span></span><br><span class="line">        gamma = MathUtils.log(desiredBrightness) / MathUtils.log(currentBrightness);</span><br><span class="line">        <span class="comment">// max^-adjustment = gamma =&gt; adjustment = -log[max](gamma)</span></span><br><span class="line">        adjustment = -MathUtils.log(gamma) / MathUtils.log(maxGamma);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//对adjustment值进行范围检查，保证介于[-1, 1]之间</span></span><br><span class="line">    adjustment = MathUtils.constrain(adjustment, -<span class="number">1</span>, +<span class="number">1</span>);</span><br><span class="line">    <span class="keyword">if</span> (mLoggingEnabled) &#123;</span><br><span class="line">        Slog.d(TAG, <span class="string">&quot;inferAutoBrightnessAdjustment: &quot;</span> + maxGamma + <span class="string">&quot;^&quot;</span> + -adjustment + <span class="string">&quot;=&quot;</span> +</span><br><span class="line">                MathUtils.pow(maxGamma, -adjustment) + <span class="string">&quot; == &quot;</span> + gamma);</span><br><span class="line">        Slog.d(TAG, <span class="string">&quot;inferAutoBrightnessAdjustment: &quot;</span> + currentBrightness + <span class="string">&quot;^&quot;</span> + gamma + <span class="string">&quot;=&quot;</span> +</span><br><span class="line">                MathUtils.pow(currentBrightness, gamma) + <span class="string">&quot; == &quot;</span> + desiredBrightness);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> adjustment;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
到这里，自动背光的调整值adjustment已经计算出了，接下来再了解有了adjustment之后该如何调整样条曲线。</li>
</ul>
<h2 id="getAdjustedCurve"><a href="#getAdjustedCurve" class="headerlink" title="getAdjustedCurve()"></a>getAdjustedCurve()</h2><p>在addUserDataPoint()方法中，计算完adjustment值后，会将用户设置的Lux值和Brightness值分别赋给BrightnessMappingStrategy对象成员mUserLux和mUserBrightness，并调用computeSpline()重新创建Lux-Nits样条曲线。<br>上文有对computeSpline讲述，但是并没有对其中调用的getAdjustedCurve()进行分析，下面对它做一些补充说明。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/display/BrightnessMappingStrategy.java</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">protected</span> Pair&lt;<span class="keyword">float</span>[], <span class="keyword">float</span>[]&gt; getAdjustedCurve(<span class="keyword">float</span>[] lux, <span class="keyword">float</span>[] brightness,</span><br><span class="line">        <span class="keyword">float</span> userLux, <span class="keyword">float</span> userBrightness, <span class="keyword">float</span> adjustment, <span class="keyword">float</span> maxGamma) &#123;</span><br><span class="line">    <span class="comment">//初始化两个新数组，分别存储Lux值和背光值</span></span><br><span class="line">    <span class="keyword">float</span>[] newLux = lux;</span><br><span class="line">    <span class="keyword">float</span>[] newBrightness = Arrays.copyOf(brightness, brightness.length);</span><br><span class="line">    <span class="keyword">if</span> (mLoggingEnabled) &#123;</span><br><span class="line">        PLOG.logCurve(<span class="string">&quot;unadjusted curve&quot;</span>, newLux, newBrightness);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//限定adjustment介于[-1, 1]之间</span></span><br><span class="line">    adjustment = MathUtils.constrain(adjustment, -<span class="number">1</span>, <span class="number">1</span>);</span><br><span class="line">    <span class="keyword">float</span> gamma = MathUtils.pow(maxGamma, -adjustment);</span><br><span class="line">    <span class="keyword">if</span> (mLoggingEnabled) &#123;</span><br><span class="line">        Slog.d(TAG, <span class="string">&quot;getAdjustedCurve: &quot;</span> + maxGamma + <span class="string">&quot;^&quot;</span> + -adjustment + <span class="string">&quot;=&quot;</span> +</span><br><span class="line">                MathUtils.pow(maxGamma, -adjustment) + <span class="string">&quot; == &quot;</span> + gamma);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//gamma != 1说明adjustment不为0</span></span><br><span class="line">    <span class="keyword">if</span> (gamma != <span class="number">1</span>) &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; newBrightness.length; i++) &#123;</span><br><span class="line">            newBrightness[i] = MathUtils.pow(newBrightness[i], gamma);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (mLoggingEnabled) &#123;</span><br><span class="line">        PLOG.logCurve(<span class="string">&quot;gamma adjusted curve&quot;</span>, newLux, newBrightness);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//如果用户在背光条上手动调节了背光</span></span><br><span class="line">    <span class="keyword">if</span> (userLux != -<span class="number">1</span>) &#123;</span><br><span class="line">        Pair&lt;<span class="keyword">float</span>[], <span class="keyword">float</span>[]&gt; curve = insertControlPoint(newLux, newBrightness, userLux,</span><br><span class="line">                userBrightness);</span><br><span class="line">        newLux = curve.first;</span><br><span class="line">        newBrightness = curve.second;</span><br><span class="line">        <span class="comment">//如果允许对应等级的log，下面将计算并打印旧值，用于对比</span></span><br><span class="line">        <span class="keyword">if</span> (mLoggingEnabled) &#123;</span><br><span class="line">            PLOG.logCurve(<span class="string">&quot;gamma and user adjusted curve&quot;</span>, newLux, newBrightness);</span><br><span class="line">            <span class="comment">// This is done for comparison.</span></span><br><span class="line">            curve = insertControlPoint(lux, brightness, userLux, userBrightness);</span><br><span class="line">            PLOG.logCurve(<span class="string">&quot;user adjusted curve&quot;</span>, curve.first ,curve.second);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> Pair.create(newLux, newBrightness);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>getAdjustedCurve方法接收以下6个参数：</p>
<ul>
<li>lux：Lux值数组</li>
<li>brightness：根据Lux-&gt; Nits-&gt;Backlight转换得到的最终背光值数组</li>
<li>userLux：用户设置时的Lux值</li>
<li>userBrightness：用户设置的背光值</li>
<li>adjustment：自动背光调整值</li>
<li>maxGamma：最大Gamma值</li>
</ul>
<p>通过adustment值获取gamma值，再通过gamma值调整整个背光数组元素。其实这里计算新的背光值的算法就是inferAutoBrightnessAdjustment()中的反操作，通过adjustment值得到gamma值，再由gamma值得到新的背光值数组。<br>根据上面的分析可以发现，getAdjustedCurve()中的maxGamma、adjustment值有如下关系：<br><img src="https://cdn.nlark.com/yuque/0/2021/png/1759879/1630488051787-955931ba-5ab6-42a2-a089-7b49382981d9.png#clientId=u5df7ab52-a4d3-4&crop=0&crop=0&crop=1&crop=1&from=ui&id=ua2a007be&margin=%5Bobject%20Object%5D&name=image2020-9-25%2015_43_15.png&originHeight=571&originWidth=1012&originalType=binary&ratio=1&rotation=0&showTitle=false&size=144750&status=done&style=none&taskId=u6308acc4-7c44-4e0e-a78e-7e79b4136e5&title=" alt="image2020-9-25 15_43_15.png"><br>计算出新的背光值后，调用insertControlPoint()将用户设置的Lux值及对应背光值作为曲线控制点插入相关数组中：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/display/BrightnessMappingStrategy.java</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> Pair&lt;<span class="keyword">float</span>[], <span class="keyword">float</span>[]&gt; insertControlPoint(</span><br><span class="line">        <span class="keyword">float</span>[] luxLevels, <span class="keyword">float</span>[] brightnessLevels, <span class="keyword">float</span> lux, <span class="keyword">float</span> brightness) &#123;</span><br><span class="line">    <span class="comment">//找到插入点索引，以保持数组元素递增</span></span><br><span class="line">    <span class="keyword">final</span> <span class="keyword">int</span> idx = findInsertionPoint(luxLevels, lux);</span><br><span class="line">    <span class="keyword">final</span> <span class="keyword">float</span>[] newLuxLevels;</span><br><span class="line">    <span class="keyword">final</span> <span class="keyword">float</span>[] newBrightnessLevels;</span><br><span class="line">    <span class="keyword">if</span> (idx == luxLevels.length) &#123; <span class="comment">//如果插入索引等于数组长度，则需要插入到末尾位置，且新Lux数组长度比原来大1</span></span><br><span class="line">        newLuxLevels = Arrays.copyOf(luxLevels, luxLevels.length + <span class="number">1</span>);</span><br><span class="line">        newBrightnessLevels  = Arrays.copyOf(brightnessLevels, brightnessLevels.length + <span class="number">1</span>);</span><br><span class="line">        newLuxLevels[idx] = lux;</span><br><span class="line">        newBrightnessLevels[idx] = brightness;</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (luxLevels[idx] == lux) &#123; <span class="comment">//如果用户设置的lux值已处于Lux值数组中，则直接复制原数组即可</span></span><br><span class="line">        newLuxLevels = Arrays.copyOf(luxLevels, luxLevels.length);</span><br><span class="line">        newBrightnessLevels = Arrays.copyOf(brightnessLevels, brightnessLevels.length);</span><br><span class="line">        newBrightnessLevels[idx] = brightness;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123; <span class="comment">//否则表示需要插入在数组中的某个位置</span></span><br><span class="line">        <span class="comment">//初始化新Lux值数组，长度比原数组大1</span></span><br><span class="line">        newLuxLevels = Arrays.copyOf(luxLevels, luxLevels.length + <span class="number">1</span>);</span><br><span class="line">        <span class="comment">//将原数组idx位置之后的元素向后移动1位，然后将新的Lux值插入到idx位置</span></span><br><span class="line">        System.arraycopy(newLuxLevels, idx, newLuxLevels, idx+<span class="number">1</span>, luxLevels.length - idx);</span><br><span class="line">        newLuxLevels[idx] = lux;</span><br><span class="line">        <span class="comment">//初始化新背光值数组，长度比原数组大1</span></span><br><span class="line">        newBrightnessLevels  = Arrays.copyOf(brightnessLevels, brightnessLevels.length + <span class="number">1</span>);</span><br><span class="line">        <span class="comment">//将原数组idx位置之后的元素向后移动1位，然后将新的背光值插入到idx位置</span></span><br><span class="line">        System.arraycopy(newBrightnessLevels, idx, newBrightnessLevels, idx+<span class="number">1</span>,</span><br><span class="line">                brightnessLevels.length - idx);</span><br><span class="line">        newBrightnessLevels[idx] = brightness;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//平滑曲线方法，新Lux值之后对应的背光值将全部为newBrightnessLevels</span></span><br><span class="line">    smoothCurve(newLuxLevels, newBrightnessLevels, idx);</span><br><span class="line">    <span class="keyword">return</span> Pair.create(newLuxLevels, newBrightnessLevels);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>该方法调用完毕后，返回一个携带有Lux值和对应背光值的Pair对象给getAdjustedCurve()，在getAdjustedCurve()中将返回该Pair对象给到computeSpline()中，computeSpline()将从返回的Pair对像中取出Lux数组值和背光数组值，完成曲线的创建。<br>这些逻辑执行完毕后，之后回到configure()方法中由LightSensor检测环境光强，并最终调节背光。</p>
<h2 id="updatePowerState"><a href="#updatePowerState" class="headerlink" title="updatePowerState()"></a>updatePowerState()</h2><p>我们再回到DisplayPowerController的updatePowerState()方法中，当configure()执行完毕后，DPC中将拿到调整后的adjustment值，并完成保存，这部分代码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/display/DisplayPowerController.java</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">updatePowerState</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Apply auto-brightness.</span></span><br><span class="line">    <span class="keyword">boolean</span> slowChange = <span class="keyword">false</span>;</span><br><span class="line">    <span class="keyword">if</span> (Float.isNaN(brightnessState)) &#123;</span><br><span class="line">        <span class="keyword">float</span> newAutoBrightnessAdjustment = autoBrightnessAdjustment;</span><br><span class="line">        <span class="keyword">if</span> (autoBrightnessEnabled) &#123;</span><br><span class="line">            <span class="comment">//获取自动背光值</span></span><br><span class="line">            brightnessState = mAutomaticBrightnessController.getAutomaticScreenBrightness();</span><br><span class="line">            <span class="comment">//获取新的adjustment值</span></span><br><span class="line">            newAutoBrightnessAdjustment =</span><br><span class="line">                    mAutomaticBrightnessController.getAutomaticScreenBrightnessAdjustment();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (isValidBrightnessValue(brightnessState)</span><br><span class="line">                || brightnessState == PowerManager.BRIGHTNESS_OFF_FLOAT) &#123;</span><br><span class="line">            <span class="comment">// Use current auto-brightness value and slowly adjust to changes.</span></span><br><span class="line">            <span class="comment">//使用当前背光值并通过慢适应过程进行调整</span></span><br><span class="line">            brightnessState = clampScreenBrightness(brightnessState);</span><br><span class="line">            <span class="keyword">if</span> (mAppliedAutoBrightness &amp;&amp; !autoBrightnessAdjustmentChanged) &#123;</span><br><span class="line">                slowChange = <span class="keyword">true</span>; <span class="comment">// slowly adapt to auto-brightness</span></span><br><span class="line">            &#125;</span><br><span class="line">            updateScreenBrightnessSetting = <span class="keyword">true</span>;</span><br><span class="line">            mAppliedAutoBrightness = <span class="keyword">true</span>;</span><br><span class="line">            mBrightnessReasonTemp.setReason(BrightnessReason.REASON_AUTOMATIC);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            mAppliedAutoBrightness = <span class="keyword">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (autoBrightnessAdjustment != newAutoBrightnessAdjustment) &#123;</span><br><span class="line">            <span class="comment">// If the autobrightness controller has decided to change the adjustment value</span></span><br><span class="line">            <span class="comment">// used, make sure that&#x27;s reflected in settings.</span></span><br><span class="line">            <span class="comment">//将adjustment值保存到SettingsProvider中</span></span><br><span class="line">            putAutoBrightnessAdjustmentSetting(newAutoBrightnessAdjustment);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="comment">// Adjustment values resulted in no change</span></span><br><span class="line">            brightnessAdjustmentFlags = <span class="number">0</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>putAutoBrightnessAdjustmentSetting()方法会将AutomaticBrightnessController中计算得出的adjustment值保存在SettingsProvider中，以确保Settings等其他进程能使用它：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/display/DisplayPowerController.java</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">putAutoBrightnessAdjustmentSetting</span><span class="params">(<span class="keyword">float</span> adjustment)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (mDisplayId == Display.DEFAULT_DISPLAY) &#123;</span><br><span class="line">        mAutoBrightnessAdjustment = adjustment;</span><br><span class="line">        Settings.System.putFloatForUser(mContext.getContentResolver(),</span><br><span class="line">                Settings.System.SCREEN_AUTO_BRIGHTNESS_ADJ, adjustment,</span><br><span class="line">                UserHandle.USER_CURRENT);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>从用户拖动背光条调节自动背光开始，到adjustment值计算完成，并最终在SettingsProvider中完成保存，整个过程时序图如下：<br><img src="https://cdn.nlark.com/yuque/0/2021/png/1759879/1630488166907-e52450e0-8066-4a7b-8053-a97fda0aabf0.png#clientId=u5df7ab52-a4d3-4&crop=0&crop=0&crop=1&crop=1&from=ui&id=uddb8ee60&margin=%5Bobject%20Object%5D&name=image2020-9-25%2015_46_43.png&originHeight=636&originWidth=1033&originalType=binary&ratio=1&rotation=0&showTitle=false&size=284653&status=done&style=none&taskId=u730099b8-f779-48c0-b755-d821b8697d6&title=" alt="image2020-9-25 15_46_43.png"></p>
<h1 id="清除用户短期模型"><a href="#清除用户短期模型" class="headerlink" title="清除用户短期模型"></a>清除用户短期模型</h1><p>使用AutomaticBrightnessController的resetShortTermModel()可以清除短期模型中用户设置的Lux值及其对应的背光值。有以下三种情况：</p>
<h2 id="切换用户"><a href="#切换用户" class="headerlink" title="切换用户"></a>切换用户</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/display/DisplayPowerController.java</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">handleSettingsChange</span><span class="params">(<span class="keyword">boolean</span> userSwitch)</span> </span>&#123;</span><br><span class="line">    mPendingScreenBrightnessSetting = getScreenBrightnessSetting();</span><br><span class="line">    <span class="keyword">if</span> (userSwitch) &#123; <span class="comment">//用户改变</span></span><br><span class="line">        <span class="comment">// Don&#x27;t treat user switches as user initiated change.</span></span><br><span class="line">        setCurrentScreenBrightness(mPendingScreenBrightnessSetting);</span><br><span class="line">        <span class="keyword">if</span> (mAutomaticBrightnessController != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="comment">//清除用户短期模型</span></span><br><span class="line">            mAutomaticBrightnessController.resetShortTermModel();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    mPendingAutoBrightnessAdjustment = getAutoBrightnessAdjustmentSetting();</span><br><span class="line">    <span class="comment">// We don&#x27;t bother with a pending variable for VR screen brightness since we just</span></span><br><span class="line">    <span class="comment">// immediately adapt to it.</span></span><br><span class="line">    mScreenBrightnessForVr = getScreenBrightnessForVrSetting();</span><br><span class="line">    sendUpdatePowerState();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="BrightnessConfigure对象发生变化"><a href="#BrightnessConfigure对象发生变化" class="headerlink" title="BrightnessConfigure对象发生变化"></a>BrightnessConfigure对象发生变化</h2><p>这种情况在上文分析setBrightnessConfiguration()时有说明。</p>
<h2 id="timeout"><a href="#timeout" class="headerlink" title="timeout"></a>timeout</h2><p>屏幕进入不可交互状态超过300s，且再次进入交互状态后环境光强有一定范围变化。在上文分析setDisplayPolicy()时有说明，若屏幕由交互状态进入非交互状态时，将会发送一个延时Handler消息，并在到达时间后将mShortTermModelValid值设置为false。当屏幕状态再进入交互状态时，会有设置Lux值的流程，在这个流程中，mShortTermModelValid变量将发挥作用：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/display/AutomaticBrightnessController.java</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">setAmbientLux</span><span class="params">(<span class="keyword">float</span> lux)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (mLoggingEnabled) &#123;</span><br><span class="line">        Slog.d(TAG, <span class="string">&quot;setAmbientLux(&quot;</span> + lux + <span class="string">&quot;)&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (lux &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        Slog.w(TAG, <span class="string">&quot;Ambient lux was negative, ignoring and setting to 0&quot;</span>);</span><br><span class="line">        lux = <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    mAmbientLux = lux;</span><br><span class="line">    mAmbientBrighteningThreshold = mAmbientBrightnessThresholds.getBrighteningThreshold(lux);</span><br><span class="line">    mAmbientDarkeningThreshold = mAmbientBrightnessThresholds.getDarkeningThreshold(lux);</span><br><span class="line">    mHbmController.onAmbientLuxChange(mAmbientLux);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// If the short term model was invalidated and the change is drastic enough, reset it.</span></span><br><span class="line">    <span class="comment">//如果环境光强介于下面两个值之间，则不会清除用户短期模型，否则进行该操作</span></span><br><span class="line">    <span class="keyword">if</span> (!mShortTermModelValid &amp;&amp; mShortTermModelAnchor != -<span class="number">1</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> (mBrightnessMapper.shouldResetShortTermModel(mAmbientLux, mShortTermModelAnchor)) &#123;</span><br><span class="line">            resetShortTermModel();</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            mShortTermModelValid = <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>只有进入非交互状态时mShortTermModelValid才会为false，表示用户设置的短期模型变为无效状态，但不会立即清除，只有环境光强变化幅度超过60%时才会进行该操作。</p>
<h1 id="配置-1"><a href="#配置-1" class="headerlink" title="配置"></a>配置</h1><h2 id="亮度值"><a href="#亮度值" class="headerlink" title="亮度值"></a>亮度值</h2><h3 id="亮度条最小值"><a href="#亮度条最小值" class="headerlink" title="亮度条最小值"></a>亮度条最小值</h3><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line">http://aospxref.com/android-12.0.0_r3/xref/frameworks/base/core/res/res/values/config.xml</span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- Minimum screen brightness setting allowed by power manager.</span></span><br><span class="line"><span class="comment">    -2 is invalid so setting will resort to int value specified above.</span></span><br><span class="line"><span class="comment">    Set this to 0.0 to allow screen to go to minimal brightness.</span></span><br><span class="line"><span class="comment">    The user is forbidden from setting the brightness below this level. --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">item</span> <span class="attr">name</span>=<span class="string">&quot;config_screenBrightnessSettingMinimumFloat&quot;</span> <span class="attr">format</span>=<span class="string">&quot;float&quot;</span> <span class="attr">type</span>=<span class="string">&quot;dimen&quot;</span>&gt;</span>-2<span class="tag">&lt;/<span class="name">item</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="亮度条最大值"><a href="#亮度条最大值" class="headerlink" title="亮度条最大值"></a>亮度条最大值</h3><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line">http://aospxref.com/android-12.0.0_r3/xref/frameworks/base/core/res/res/values/config.xml</span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- Maximum screen brightness allowed by the power manager.</span></span><br><span class="line"><span class="comment">    -2 is invalid so setting will resort to int value specified above.</span></span><br><span class="line"><span class="comment">    Set this to 1.0 for maximum brightness range.</span></span><br><span class="line"><span class="comment">    The user is forbidden from setting the brightness above this level. --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">item</span> <span class="attr">name</span>=<span class="string">&quot;config_screenBrightnessSettingMaximumFloat&quot;</span> <span class="attr">format</span>=<span class="string">&quot;float&quot;</span> <span class="attr">type</span>=<span class="string">&quot;dimen&quot;</span>&gt;</span>-2<span class="tag">&lt;/<span class="name">item</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="默认亮度值"><a href="#默认亮度值" class="headerlink" title="默认亮度值"></a>默认亮度值</h3><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line">http://aospxref.com/android-12.0.0_r3/xref/frameworks/base/core/res/res/values/config.xml</span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- Default screen brightness setting set.</span></span><br><span class="line"><span class="comment">     -2 is invalid so setting will resort to int value specified above.</span></span><br><span class="line"><span class="comment">     Must be in the range specified by minimum and maximum. --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">item</span> <span class="attr">name</span>=<span class="string">&quot;config_screenBrightnessSettingDefaultFloat&quot;</span> <span class="attr">format</span>=<span class="string">&quot;float&quot;</span> <span class="attr">type</span>=<span class="string">&quot;dimen&quot;</span>&gt;</span>-2<span class="tag">&lt;/<span class="name">item</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>设备初始化或配置重置时使用的亮度值。</p>
<h3 id="屏幕超时灭屏前的亮度值"><a href="#屏幕超时灭屏前的亮度值" class="headerlink" title="屏幕超时灭屏前的亮度值"></a>屏幕超时灭屏前的亮度值</h3><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line">http://aospxref.com/android-12.0.0_r3/xref/frameworks/base/core/res/res/values/config.xml</span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- Screen brightness used to dim the screen when the user activity</span></span><br><span class="line"><span class="comment">     timeout expires.  May be less than the minimum allowed brightness setting</span></span><br><span class="line"><span class="comment">     that can be set by the user. --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">integer</span> <span class="attr">name</span>=<span class="string">&quot;config_screenBrightnessDim&quot;</span>&gt;</span>10<span class="tag">&lt;/<span class="name">integer</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>该值不能小于亮度条最小值，否则将导致夜光屏无法生效。一般比亮度条最小值大1。</p>
<h2 id="调节速率"><a href="#调节速率" class="headerlink" title="调节速率"></a>调节速率</h2><h3 id="快速调节速率"><a href="#快速调节速率" class="headerlink" title="快速调节速率"></a>快速调节速率</h3><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line">http://aospxref.com/android-12.0.0_r3/xref/frameworks/base/core/res/res/values/config.xml</span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- Fast brightness animation ramp rate in brightness units per second--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">integer</span> <span class="attr">translatable</span>=<span class="string">&quot;false&quot;</span> <span class="attr">name</span>=<span class="string">&quot;config_brightness_ramp_rate_fast&quot;</span>&gt;</span>180<span class="tag">&lt;/<span class="name">integer</span>&gt;</span></span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h3 id="慢速调节速率"><a href="#慢速调节速率" class="headerlink" title="慢速调节速率"></a>慢速调节速率</h3><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line">http://aospxref.com/android-12.0.0_r3/xref/frameworks/base/core/res/res/values/config.xml</span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- Slow brightness animation ramp rate in brightness units per second--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">integer</span> <span class="attr">translatable</span>=<span class="string">&quot;false&quot;</span> <span class="attr">name</span>=<span class="string">&quot;config_brightness_ramp_rate_slow&quot;</span>&gt;</span>60<span class="tag">&lt;/<span class="name">integer</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="防抖时间"><a href="#防抖时间" class="headerlink" title="防抖时间"></a>防抖时间</h2><h3 id="调亮防抖时间"><a href="#调亮防抖时间" class="headerlink" title="调亮防抖时间"></a>调亮防抖时间</h3><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">http://aospxref.com/android-12.0.0_r3/xref/frameworks/base/core/res/res/values/config.xml</span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- Stability requirements in milliseconds for accepting a new brightness level.  This is used</span></span><br><span class="line"><span class="comment">     for debouncing the light sensor.  Different constants are used to debounce the light sensor</span></span><br><span class="line"><span class="comment">     when adapting to brighter or darker environments.  This parameter controls how quickly</span></span><br><span class="line"><span class="comment">     brightness changes occur in response to an observed change in light level that exceeds the</span></span><br><span class="line"><span class="comment">     hysteresis threshold. --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">integer</span> <span class="attr">name</span>=<span class="string">&quot;config_autoBrightnessBrighteningLightDebounce&quot;</span>&gt;</span>4000<span class="tag">&lt;/<span class="name">integer</span>&gt;</span></span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>单位为ms，环境光增强且持续超过该时间才会触发亮度增加。</p>
<h3 id="调暗防抖时间"><a href="#调暗防抖时间" class="headerlink" title="调暗防抖时间"></a>调暗防抖时间</h3><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line">http://aospxref.com/android-12.0.0_r3/xref/frameworks/base/core/res/res/values/config.xml</span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- Stability requirements in milliseconds for accepting a new brightness level.  This is used</span></span><br><span class="line"><span class="comment">     for debouncing the light sensor.  Different constants are used to debounce the light sensor</span></span><br><span class="line"><span class="comment">     when adapting to brighter or darker environments.  This parameter controls how quickly</span></span><br><span class="line"><span class="comment">     brightness changes occur in response to an observed change in light level that exceeds the</span></span><br><span class="line"><span class="comment">     hysteresis threshold. --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">integer</span> <span class="attr">name</span>=<span class="string">&quot;config_autoBrightnessDarkeningLightDebounce&quot;</span>&gt;</span>8000<span class="tag">&lt;/<span class="name">integer</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>单位为ms，环境光减弱且持续超过该时间才会触发亮度减少。</p>
<h2 id="自动亮度数组"><a href="#自动亮度数组" class="headerlink" title="自动亮度数组"></a>自动亮度数组</h2><h3 id="config-autoBrightnessLevels"><a href="#config-autoBrightnessLevels" class="headerlink" title="config_autoBrightnessLevels"></a>config_autoBrightnessLevels</h3><p><strong>lux值数组，</strong>描述环境光强度（lux），整形数组，其值均为区间点，数组长度比config_autoBrightnessLcdBacklightValues和config_autoBrightnessDisplayValuesNits少1。该数组长度越大，亮度划分粒度越细，自动亮度曲线就越平滑。（目前项目中一般使用50多个级别）</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line">http://aospxref.com/android-12.0.0_r3/xref/frameworks/base/core/res/res/values/config.xml</span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- Array of light sensor lux values to define our levels for auto backlight brightness support.</span></span><br><span class="line"><span class="comment">     The N entries of this array define N + 1 control points as follows:</span></span><br><span class="line"><span class="comment">     (1-based arrays)</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">     Point 1:            (0, value[1]):             lux &lt;= 0</span></span><br><span class="line"><span class="comment">     Point 2:     (level[1], value[2]):  0        &lt; lux &lt;= level[1]</span></span><br><span class="line"><span class="comment">     Point 3:     (level[2], value[3]):  level[2] &lt; lux &lt;= level[3]</span></span><br><span class="line"><span class="comment">     ...</span></span><br><span class="line"><span class="comment">     Point N+1: (level[N], value[N+1]):  level[N] &lt; lux</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">     The control points must be strictly increasing.  Each control point</span></span><br><span class="line"><span class="comment">     corresponds to an entry in the brightness backlight values arrays.</span></span><br><span class="line"><span class="comment">     For example, if lux == level[1] (first element of the levels array)</span></span><br><span class="line"><span class="comment">     then the brightness will be determined by value[2] (second element</span></span><br><span class="line"><span class="comment">     of the brightness values array).</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">     Spline interpolation is used to determine the auto-brightness</span></span><br><span class="line"><span class="comment">     backlight values for lux levels between these control points.</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">     Must be overridden in platform specific overlays --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">integer-array</span> <span class="attr">name</span>=<span class="string">&quot;config_autoBrightnessLevels&quot;</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">integer-array</span>&gt;</span></span><br></pre></td></tr></table></figure>
<blockquote>
<p>必须overlays这个配置。</p>
</blockquote>
<h3 id="config-autoBrightnessLcdBacklightValues"><a href="#config-autoBrightnessLcdBacklightValues" class="headerlink" title="config_autoBrightnessLcdBacklightValues"></a>config_autoBrightnessLcdBacklightValues</h3><p><strong>backlight值数组：</strong>它描述lux值对应的backlight值，整形数组，用于将L-Sensor获取的lux值转换为backlight。（最大值不受255的限制，与“最大亮度值”一致即可）</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line">http://aospxref.com/android-12.0.0_r3/xref/frameworks/base/core/res/res/values/config.xml</span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- Array of output values for LCD backlight corresponding to the lux values</span></span><br><span class="line"><span class="comment">     in the config_autoBrightnessLevels array.  This array should have size one greater</span></span><br><span class="line"><span class="comment">     than the size of the config_autoBrightnessLevels array.</span></span><br><span class="line"><span class="comment">     The brightness values must be between 0 and 255 and be non-decreasing.</span></span><br><span class="line"><span class="comment">     This must be overridden in platform specific overlays --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">integer-array</span> <span class="attr">name</span>=<span class="string">&quot;config_autoBrightnessLcdBacklightValues&quot;</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">integer-array</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>由此可知，简单映射方案仅需要定义2个数组：config_autoBrightnessLevels, config_autoBrightnessLcdBacklightValues。</p>
<blockquote>
<p>必须overlays这个配置。</p>
</blockquote>
<h3 id="config-autoBrightnessDisplayValuesNits"><a href="#config-autoBrightnessDisplayValuesNits" class="headerlink" title="config_autoBrightnessDisplayValuesNits"></a>config_autoBrightnessDisplayValuesNits</h3><p><strong>仅物理映射需要定义</strong><br><strong>nits值数组：</strong>它描述lux值对应的屏幕nits值，通俗的讲，就是不同环境光强度（lux）下最适应人眼的屏幕亮度（nits），该数组值一般与设备无关。（该数据最初是通过苹果手机显示全白图像测试而来的）<br><strong>tip</strong> ：config_screenBrightnessNits和config_screenBrightnessBacklight两个数组也需要定义。config_autoBrightnessDisplayValuesNits与config_autoBrightnessLcdBacklightValues互斥，如果定义了config_autoBrightnessDisplayValuesNits，将不会使用简单映射，同时config_autoBrightnessLcdBacklightValues也失效。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line">http://aospxref.com/android-12.0.0_r3/xref/frameworks/base/core/res/res/values/config.xml</span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- Array of desired screen brightness in nits corresponding to the lux values</span></span><br><span class="line"><span class="comment">     in the config_autoBrightnessLevels array. As with config_screenBrightnessMinimumNits and</span></span><br><span class="line"><span class="comment">     config_screenBrightnessMaximumNits, the display brightness is defined as the measured</span></span><br><span class="line"><span class="comment">     brightness of an all-white image.</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">     If this is defined then:</span></span><br><span class="line"><span class="comment">        - config_autoBrightnessLcdBacklightValues should not be defined</span></span><br><span class="line"><span class="comment">        - config_screenBrightnessNits must be defined</span></span><br><span class="line"><span class="comment">        - config_screenBrightnessBacklight must be defined</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">     This array should have size one greater than the size of the config_autoBrightnessLevels</span></span><br><span class="line"><span class="comment">     array. The brightness values must be non-negative and non-decreasing. This must be</span></span><br><span class="line"><span class="comment">     overridden in platform specific overlays --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">array</span> <span class="attr">name</span>=<span class="string">&quot;config_autoBrightnessDisplayValuesNits&quot;</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">array</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="config-screenBrightnessNits"><a href="#config-screenBrightnessNits" class="headerlink" title="config_screenBrightnessNits"></a>config_screenBrightnessNits</h3><p><strong>仅物理映射需要定义。</strong><br><strong>屏幕nits值数组：</strong>它描述不同backlight对应的屏幕发光亮度（nits），数组长度与config_screenBrightnessBacklight一致，其值与屏幕硬件相关，需要通过照度计测试得到。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line">http://aospxref.com/android-12.0.0_r3/xref/frameworks/base/core/res/res/values/config.xml</span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- An array of floats describing the screen brightness in nits corresponding to the backlight</span></span><br><span class="line"><span class="comment">     values in the config_screenBrightnessBacklight array.  On OLED displays these  values</span></span><br><span class="line"><span class="comment">     should be measured with an all white image while the display is in the fully on state.</span></span><br><span class="line"><span class="comment">     Note that this value should *not* reflect the maximum brightness value for any high</span></span><br><span class="line"><span class="comment">     brightness modes but only the maximum brightness value obtainable in a sustainable manner.</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">     This array should be equal in size to config_screenBrightnessBacklight --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">array</span> <span class="attr">name</span>=<span class="string">&quot;config_screenBrightnessNits&quot;</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">array</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="config-screenBrightnessBacklight"><a href="#config-screenBrightnessBacklight" class="headerlink" title="config_screenBrightnessBacklight"></a>config_screenBrightnessBacklight</h3><p><strong>仅物理映射需要定义。</strong><br><strong>屏幕backlight值数组：</strong>它描述屏幕从最小到最大的backlight值，整形数组，其最大值与“最大亮度值”一致，数组长度与config_screenBrightnessNits一致。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line">http://aospxref.com/android-12.0.0_r3/xref/frameworks/base/core/res/res/values/config.xml</span><br><span class="line"></span><br><span class="line"> <span class="comment">&lt;!-- An array describing the screen&#x27;s backlight values corresponding to the brightness</span></span><br><span class="line"><span class="comment">     values in the config_screenBrightnessNits array.</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">     This array should be equal in size to config_screenBrightnessBacklight. --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">integer-array</span> <span class="attr">name</span>=<span class="string">&quot;config_screenBrightnessBacklight&quot;</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">integer-array</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>物理映射方案存在两种形式：</p>
<ul>
<li>使用nits-backlight简单线性映射，需要定义2个数组：config_autoBrightnessLevels、config_autoBrightnessDisplayValuesNits。</li>
<li>使用nits-backlight复杂线性映射，需要定义4个数组：config_autoBrightnessLevels、config_autoBrightnessDisplayValuesNits、config_screenBrightnessNits、config_screenBrightnessBacklight。</li>
</ul>
<h3 id="亮度调节阈值数组"><a href="#亮度调节阈值数组" class="headerlink" title="亮度调节阈值数组"></a>亮度调节阈值数组</h3><h4 id="config-ambientThresholdLevels"><a href="#config-ambientThresholdLevels" class="headerlink" title="config_ambientThresholdLevels"></a>config_ambientThresholdLevels</h4><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line">http://aospxref.com/android-12.0.0_r3/xref/frameworks/base/core/res/res/values/config.xml</span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- Array of ambient lux threshold values. This is used for determining hysteresis constraint</span></span><br><span class="line"><span class="comment">     values by calculating the index to use for lookup and then setting the constraint value</span></span><br><span class="line"><span class="comment">     to the corresponding value of the array. The new brightening hysteresis constraint value</span></span><br><span class="line"><span class="comment">     is the n-th element of config_ambientBrighteningThresholds, and the new darkening</span></span><br><span class="line"><span class="comment">     hysteresis constraint value is the n-th element of config_ambientDarkeningThresholds.</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">     The (zero-based) index is calculated as follows: (MAX is the largest index of the array)</span></span><br><span class="line"><span class="comment">     condition                       calculated index</span></span><br><span class="line"><span class="comment">     value &lt; level[0]                0</span></span><br><span class="line"><span class="comment">     level[n] &lt;= value &lt; level[n+1]  n+1</span></span><br><span class="line"><span class="comment">     level[MAX] &lt;= value             MAX+1 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">integer-array</span> <span class="attr">name</span>=<span class="string">&quot;config_ambientThresholdLevels&quot;</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">integer-array</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h4 id="config-ambientBrighteningThresholds"><a href="#config-ambientBrighteningThresholds" class="headerlink" title="config_ambientBrighteningThresholds"></a>config_ambientBrighteningThresholds</h4><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line">http://aospxref.com/android-12.0.0_r3/xref/frameworks/base/core/res/res/values/config.xml</span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- Array of hysteresis constraint values for brightening, represented as tenths of a</span></span><br><span class="line"><span class="comment">     percent. The length of this array is assumed to be one greater than</span></span><br><span class="line"><span class="comment">     config_ambientThresholdLevels. The brightening threshold is calculated as</span></span><br><span class="line"><span class="comment">     lux * (1.0f + CONSTRAINT_VALUE). When the current lux is higher than this threshold,</span></span><br><span class="line"><span class="comment">     the screen brightness is recalculated. See the config_ambientThresholdLevels</span></span><br><span class="line"><span class="comment">     description for how the constraint value is chosen. --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">integer-array</span> <span class="attr">name</span>=<span class="string">&quot;config_ambientBrighteningThresholds&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">item</span>&gt;</span>100<span class="tag">&lt;/<span class="name">item</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">integer-array</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h4 id="config-ambientDarkeningThresholds"><a href="#config-ambientDarkeningThresholds" class="headerlink" title="config_ambientDarkeningThresholds"></a>config_ambientDarkeningThresholds</h4><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line">http://aospxref.com/android-12.0.0_r3/xref/frameworks/base/core/res/res/values/config.xml</span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- Array of hysteresis constraint values for darkening, represented as tenths of a</span></span><br><span class="line"><span class="comment">     percent. The length of this array is assumed to be one greater than</span></span><br><span class="line"><span class="comment">     config_ambientThresholdLevels. The darkening threshold is calculated as</span></span><br><span class="line"><span class="comment">     lux * (1.0f - CONSTRAINT_VALUE). When the current lux is lower than this threshold,</span></span><br><span class="line"><span class="comment">     the screen brightness is recalculated. See the config_ambientThresholdLevels</span></span><br><span class="line"><span class="comment">     description for how the constraint value is chosen. --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">integer-array</span> <span class="attr">name</span>=<span class="string">&quot;config_ambientDarkeningThresholds&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">item</span>&gt;</span>200<span class="tag">&lt;/<span class="name">item</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">integer-array</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h1 id="总结-1"><a href="#总结-1" class="headerlink" title="总结"></a>总结</h1><p>点击下拉任务栏中的亮度滑动条。</p>
<h2 id="LightsService"><a href="#LightsService" class="headerlink" title="LightsService"></a>LightsService</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/java/com/android/server/lights/LightsService.java</span></span><br><span class="line"><span class="number">1.</span> setLightLocked()</span><br><span class="line"><span class="number">2.</span> setLightUnchecked()</span><br><span class="line"><span class="number">3.</span> setLight_native()</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h2 id="jni"><a href="#jni" class="headerlink" title="jni"></a>jni</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/jni/com_android_server_lights_LightsService.cpp</span></span><br><span class="line"><span class="number">4.</span> setLight_native()</span><br><span class="line"><span class="number">5.</span> hal-&gt;setLight(type, state);</span><br></pre></td></tr></table></figure>

<h2 id="hal"><a href="#hal" class="headerlink" title="hal"></a>hal</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/frameworks/base/services/core/jni/com_android_server_lights_LightsService.cpp</span></span><br><span class="line"><span class="number">1.</span> hal初始化</span><br><span class="line">sp&lt;ILight&gt; hal = ILight::getService();</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/hardware/interfaces/light/2.0/ILight.hal</span></span><br><span class="line"><span class="number">2.</span> setLight的hal定义</span><br><span class="line"><span class="class"><span class="keyword">interface</span> <span class="title">ILight</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * Set the provided lights to the provided values.</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> type logical light to set</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> state describes what the light should look like.</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> status result of applying state transformation.</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    setLight(Type type, LightState state) generates (Status status);</span><br><span class="line"></span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/hardware/interfaces/light/2.0/default/Light.cpp</span></span><br><span class="line"><span class="number">3.</span> passthrough的hal调用</span><br><span class="line">light_device_t* getLightDevice(<span class="keyword">const</span> <span class="keyword">char</span>* name) &#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">int</span> ret = hw_get_module (LIGHTS_HARDWARE_MODULE_ID, &amp;hwModule);</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/hardware/qcom/display/liblight/lights.c</span></span><br><span class="line"><span class="number">4.</span> hal stub的实现</span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * The lights Module</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">struct hw_module_t HAL_MODULE_INFO_SYM = &#123;</span><br><span class="line">    .tag = HARDWARE_MODULE_TAG,</span><br><span class="line">    .version_major = <span class="number">1</span>,</span><br><span class="line">    .version_minor = <span class="number">0</span>,</span><br><span class="line">    .id = LIGHTS_HARDWARE_MODULE_ID,</span><br><span class="line">    .name = <span class="string">&quot;lights Module&quot;</span>,</span><br><span class="line">    .author = <span class="string">&quot;Google, Inc.&quot;</span>,</span><br><span class="line">    .methods = &amp;lights_module_methods,</span><br><span class="line">&#125;;</span><br><span class="line">     </span><br><span class="line"></span><br><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/hardware/interfaces/light/2.0/default/Light.cpp</span></span><br><span class="line"><span class="number">5.</span> 背光light_device_t的获取</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">static</span> std::map&lt;Type, <span class="keyword">const</span> <span class="keyword">char</span>*&gt; kLogicalLights = &#123;</span><br><span class="line">    &#123;Type::BACKLIGHT,     LIGHT_ID_BACKLIGHT&#125;,</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">ILight* HIDL_FETCH_ILight(<span class="keyword">const</span> <span class="keyword">char</span>* <span class="comment">/* name */</span>) &#123;</span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span>(auto <span class="keyword">const</span> &amp;pair : kLogicalLights) &#123;</span><br><span class="line">        Type type = pair.first;</span><br><span class="line">        <span class="keyword">const</span> <span class="keyword">char</span>* name = pair.second;</span><br><span class="line"></span><br><span class="line">        light_device_t* light = getLightDevice(name);</span><br><span class="line"></span><br><span class="line">        ...</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/hardware/qcom/display/liblight/lights.c</span></span><br><span class="line"><span class="number">6.</span> 打开驱动</span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">open_lights</span><span class="params">(<span class="keyword">const</span> struct hw_module_t* <span class="keyword">module</span>, <span class="keyword">char</span> <span class="keyword">const</span>* name,</span></span></span><br><span class="line"><span class="params"><span class="function">        struct hw_device_t** device)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (<span class="number">0</span> == strcmp(LIGHT_ID_BACKLIGHT, name)) &#123;</span><br><span class="line">        g_has_persistence_node = !access(PERSISTENCE_FILE, F_OK);</span><br><span class="line">        <span class="comment">//注意这个set_light_backlight方法</span></span><br><span class="line">        set_light = set_light_backlight;</span><br><span class="line">    &#125;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/hardware/interfaces/light/2.0/default/Light.cpp</span></span><br><span class="line"><span class="number">7.</span> 调setLight接口（ILight.hal定义此接口）</span><br><span class="line">Return&lt;Status&gt; Light::setLight(Type type, <span class="keyword">const</span> LightState&amp; state)  &#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="comment">//此处set_light对接上/hardware/qcom/display/liblight/lights.c中的set_light_backlight</span></span><br><span class="line">    <span class="comment">//看第6步</span></span><br><span class="line">    <span class="keyword">int</span> ret = hwLight-&gt;set_light(hwLight, &amp;legacyState);</span><br><span class="line"></span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h2 id="syscall写节点"><a href="#syscall写节点" class="headerlink" title="syscall写节点"></a>syscall写节点</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-12.0.0_r3/xref/hardware/qcom/display/liblight/lights.c</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 背光设置节点</span></span><br><span class="line"><span class="keyword">char</span> <span class="keyword">const</span>*<span class="keyword">const</span> LCD_FILE2</span><br><span class="line">        = <span class="string">&quot;/sys/class/backlight/panel0-backlight/brightness&quot;</span>;</span><br><span class="line">  </span><br><span class="line"><span class="number">1.</span> 设置背光</span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span></span></span><br><span class="line"><span class="function"><span class="title">set_light_backlight</span><span class="params">(struct light_device_t* dev,</span></span></span><br><span class="line"><span class="params"><span class="function">        struct light_state_t <span class="keyword">const</span>* state)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!err) &#123;</span><br><span class="line">        <span class="keyword">if</span> (!access(LCD_FILE, F_OK)) &#123;</span><br><span class="line">            err = write_int(LCD_FILE, brightness);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            err = write_int(LCD_FILE2, brightness);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>如上就是应用层到写节点的调用过程。可以在adb shell下直接用echo命令设置：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">adb root</span><br><span class="line">adb shell</span><br><span class="line">echo <span class="number">1023</span> &gt; /sys/<span class="class"><span class="keyword">class</span>/<span class="title">backlight</span>/<span class="title">panel0</span>-<span class="title">backlight</span>/<span class="title">brightness</span></span></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Android</category>
      </categories>
      <tags>
        <tag>Framework</tag>
        <tag>Service</tag>
      </tags>
  </entry>
  <entry>
    <title>Linux开机自启动服务</title>
    <url>/archives/87884557.html</url>
    <content><![CDATA[<span id="more"></span>
<meta name="referrer" content="no-referrer"/>


<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>Linux上要用翻墙还是比较费劲的，当然如果你能忍受蓝灯这种龟速的网络就当我没说。因为我用的服务商推荐用clash，当然用ssr这些也行。但既然推荐是clash，咱还是尝试着linux上装clash，总的来说还是比较麻烦的。为了防止后续换系统忘记也好，或者是分享给需要的朋友也好，这里还是做一个简单的记录。</p>
<h1 id="下载"><a href="#下载" class="headerlink" title="下载"></a>下载</h1><p>clash正式的发行版，是通过github上下载的。对应的地址：<a href="https://github.com/Dreamacro/clash/releases">https://github.com/Dreamacro/clash/releases</a><br>目前最新版本是1.9.1，下载之后允许bin文件是会发生段错误的。我一路降级到1.6.0才能正常运行。这是真坑啊，差点放弃了。</p>
<h1 id="运行"><a href="#运行" class="headerlink" title="运行"></a>运行</h1><p>如果只是简单的使用，下载之后运行bin文件，然后再把配置文件改成自己服务商的配置即可。默认的配置目录在home目录下的~/.config/clash/</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">drwxr-xr-x  2 solo solo 4.0K 1月  20 15:16 .</span><br><span class="line">drwxr-xr-x 30 solo solo 4.0K 1月  20 15:07 ..</span><br><span class="line">-rw-r--r--  1 solo solo  16K 1月  19 10:12 cache.db</span><br><span class="line">-rw-r--r--  1 solo solo  57K 1月  20 15:15 config.yaml</span><br><span class="line">-rw-r--r--  1 solo solo 4.0M 1月  20 15:18 Country.mmdb</span><br></pre></td></tr></table></figure>
<p>更新好config.yaml、Country.mmdb之后再打开<a href="http://clash.razord.top/#/settings">http://clash.razord.top/#/settings</a>更改配置。<br><img src="https://cdn.nlark.com/yuque/0/2022/png/1759879/1642670363380-989e1e27-fd76-4d1a-9acb-105c3da60a8d.png#clientId=u6b02b9ef-b1c0-4&from=ui&id=u3111df5e&margin=%5Bobject%20Object%5D&name=clash_setting.png&originHeight=615&originWidth=1906&originalType=binary&ratio=1&size=76351&status=done&style=none&taskId=u7c66b74b-f8ab-4a21-a8c8-272b5963bdd" alt="clash_setting.png"></p>
<h1 id="linux系统配置"><a href="#linux系统配置" class="headerlink" title="linux系统配置"></a>linux系统配置</h1><p><img src="https://cdn.nlark.com/yuque/0/2022/png/1759879/1642670884200-0e4d14a9-5c75-4e5e-98af-555c5c2e174b.png#clientId=u6b02b9ef-b1c0-4&from=ui&id=uaca87408&margin=%5Bobject%20Object%5D&name=vpn_setting.png&originHeight=784&originWidth=790&originalType=binary&ratio=1&size=80236&status=done&style=none&taskId=u87a9c6bc-cb01-4ef1-944d-523d4639270" alt="vpn_setting.png"><br>端口号对应上就可以了。</p>
<h1 id="开机自启动"><a href="#开机自启动" class="headerlink" title="开机自启动"></a>开机自启动</h1><p>配置一个服务，在/etc/systemd/system/下创建clash.service文件</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sudo vim /etc/systemd/system/clash.service</span><br></pre></td></tr></table></figure>
<p>把以下内容复制到clash.service</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[Unit] </span><br><span class="line">Description=clash daemon</span><br><span class="line">[Service] </span><br><span class="line">Type=simple </span><br><span class="line">User=root </span><br><span class="line">ExecStart=/home/solo/bin/clash -d /home/solo/bin/config/clash/</span><br><span class="line">Restart=on-failure  </span><br><span class="line">[Install] </span><br><span class="line">WantedBy=multi-user.target</span><br></pre></td></tr></table></figure>
<p>其中：ExecStart后面带上需要执行的脚本或者bin文件，这里多加-d /home/solo/bin/config/clash是因为我指定其配置文件在/home/solo/bin/config/clash目录。<br>最后通过systemctl命令reload服务，启用服务，启动服务即可。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sudo systemctl daemon-reload </span><br><span class="line">sudo systemctl enable clash</span><br><span class="line">sudo systemctl start clash</span><br><span class="line">sudo systemctl status clash</span><br></pre></td></tr></table></figure>
<p> </p>
<h1 id="终端代理命令"><a href="#终端代理命令" class="headerlink" title="终端代理命令"></a>终端代理命令</h1><p>export https_proxy=<a href="http://127.0.0.1:7890/">http://127.0.0.1:</a>4780 http_proxy=<a href="http://127.0.0.1:7890/">http://127.0.0.1:</a>4780 all_proxy=socks5://127.0.0.1:4781</p>
<h1 id="（额外）机器人服务"><a href="#（额外）机器人服务" class="headerlink" title="（额外）机器人服务"></a>（额外）机器人服务</h1><ul>
<li><p>sudo vim /etc/systemd/system/bot.service</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[Unit] </span><br><span class="line">Description=bot daemon</span><br><span class="line">[Service] </span><br><span class="line">Type=simple </span><br><span class="line">User=root </span><br><span class="line">ExecStart=/home/solo/miniconda3/envs/py36tf1.15/bin/python /home/solo/ext-data/code/github/global_scripts/bot_gerrit_review_schedule.py</span><br><span class="line">Restart=on-failure  </span><br><span class="line">[Install] </span><br><span class="line">WantedBy=multi-user.target</span><br></pre></td></tr></table></figure></li>
<li><p>sudo systemctl daemon-reload &amp; sudo systemctl enable bot</p>
</li>
<li><p>sudo systemctl restart bot </p>
</li>
<li><p>sudo systemctl status bot</p>
</li>
</ul>
]]></content>
      <categories>
        <category>Linux</category>
      </categories>
      <tags>
        <tag>Service</tag>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>如何学习Android Audio</title>
    <url>/archives/c7d23d1f.html</url>
    <content><![CDATA[<span id="more"></span>
<meta name="referrer" content="no-referrer"/>

<h1 id="音频框架"><a href="#音频框架" class="headerlink" title="音频框架"></a>音频框架</h1><p><img src="https://source.android.google.cn/devices/audio/images/ape_fwk_audio.png" alt="N|Solid"></p>
<h1 id="音频术语"><a href="#音频术语" class="headerlink" title="音频术语"></a>音频术语</h1><ul>
<li><p>PCM 脉冲编码调制<br>  对应的数据格式有几种:    Q0.15    Q0.7 1    Q0.23    Q0.31    浮点数<br>  <a href="https://source.android.google.cn/devices/audio/data_formats">数据格式定义</a>:<a href="https://source.android.google.cn/devices/audio/data_formats">https://source.android.google.cn/devices/audio/data_formats</a></p>
</li>
<li><p>奈奎斯特频率<br>  可由离散信号以指定采样率的一半表示的最大频率分量。例如，由于人类的听力范围可达到近 20 kHz，因此数字音频信号的采样率必须至少有 40 kHz 才能代表该范围。在实践中，44.1 kHz 和 48 kHz 的采样率比较常用，对应的奈奎斯特频率分别为 22.05 kHz 和 24 kHz。如需了解详情，请参阅奈奎斯特频率和听力范围。</p>
</li>
<li><p>每样本位数或位深<br>  每个样本的信息位数。</p>
</li>
<li><p>声道<br>  单个音频信息流，通常与一个录音位置或播放位置相对应。</p>
</li>
<li><p>Hz<br>  采样率或帧率的单位。</p>
</li>
<li><p>采样率或帧率<br>  每秒帧数。“帧率”这一用法更为准确，但业内习惯使用“采样率”来表示帧率。</p>
</li>
<li><p>FIFO<br>  先进先出。用于实现数据队列先进先出的硬件模块或软件数据结构。谈到音频时，存储在队列中的数据通常是音频帧。FIFO 可通过环形缓冲区来实现。</p>
</li>
<li><p>立体声<br>  两个声道。</p>
</li>
<li><p>音量<br>  响度，音频信号的主观强度。</p>
</li>
<li><p>帧<br>  某个时间点上的样本集，每个声道对应一个样本。</p>
</li>
<li><p>每缓冲区帧数<br>  同时从一个模块传递到另一个模块的帧数。音频 HAL 接口会使用每缓冲区帧数这一概念。</p>
</li>
<li><p>欠载/溢出<br>  类似underrun和overrun</p>
</li>
</ul>
<p><a href="https://source.android.google.cn/devices/audio/terminology">更多音频术语</a>:<a href="https://source.android.google.cn/devices/audio/terminology">https://source.android.google.cn/devices/audio/terminology</a></p>
<h1 id="手机的音量管理"><a href="#手机的音量管理" class="headerlink" title="手机的音量管理"></a>手机的音量管理</h1><p>Android上的音量集中在AudioService AudioPolicyManager AudioFlinger 3个地方，<br>他们的设置过程是从AudioService -&gt; AudioPolicyManager -&gt; AudioFlinger<br>AudioFlinger 主要是去获取音频设备的属性，比如他的音量多大，是不是静音<br>AudioPolicyManager 会去加载音频场景，音量曲线，音频类型<br>AudioService 负责管理不同音频流类型的音量，设备音量，用户交互。其中音量框的展示调节交互是在SystemUI中实现IVolumeController接口来绑定的</p>
<h2 id="音频场景"><a href="#音频场景" class="headerlink" title="音频场景"></a>音频场景</h2><p>为了适配不同场景的需求。比如铃声，音乐，通知，系统，耳机，蓝牙等，Andorid里定义了很多音频场景来满足。</p>
<p>AudioAttributes中有很多使用场景枚举，这些会决定在AudioPolicy侧选用对应的策略执行</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">/**</span><br><span class="line"> * Content <span class="built_in">type</span> value to use when the content <span class="built_in">type</span> is unknown, or other than the ones defined.</span><br><span class="line"> */</span><br><span class="line">public final static int CONTENT_TYPE_UNKNOWN = 0;</span><br><span class="line">/**</span><br><span class="line"> * Content <span class="built_in">type</span> value to use when the content <span class="built_in">type</span> is speech.</span><br><span class="line"> */</span><br><span class="line">public final static int CONTENT_TYPE_SPEECH = 1;</span><br><span class="line">/**</span><br><span class="line"> * Content <span class="built_in">type</span> value to use when the content <span class="built_in">type</span> is music.</span><br><span class="line"> */</span><br><span class="line">public final static int CONTENT_TYPE_MUSIC = 2;</span><br><span class="line">/**</span><br><span class="line"> * Content <span class="built_in">type</span> value to use when the content <span class="built_in">type</span> is a soundtrack, typically accompanying</span><br><span class="line"> * a movie or TV program.</span><br><span class="line"> */</span><br><span class="line">public final static int CONTENT_TYPE_MOVIE = 3;</span><br><span class="line">/**</span><br><span class="line"> * Content <span class="built_in">type</span> value to use when the content <span class="built_in">type</span> is a sound used to accompany a user</span><br><span class="line"> * action, such as a beep or sound effect expressing a key click, or event, such as the</span><br><span class="line"> * <span class="built_in">type</span> of a sound <span class="keyword">for</span> a bonus being received <span class="keyword">in</span> a game. These sounds are mostly synthesized</span><br><span class="line"> * or short Foley sounds.</span><br><span class="line"> */</span><br><span class="line">public final static int CONTENT_TYPE_SONIFICATION = 4;</span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line"> * Usage value to use when the usage is unknown.</span><br><span class="line"> */</span><br><span class="line">public final static int USAGE_UNKNOWN = 0;</span><br><span class="line">/**</span><br><span class="line"> * Usage value to use when the usage is media, such as music, or movie</span><br><span class="line"> * soundtracks.</span><br><span class="line"> */</span><br><span class="line">public final static int USAGE_MEDIA = 1;</span><br><span class="line">/**</span><br><span class="line"> * Usage value to use when the usage is voice communications, such as telephony</span><br><span class="line"> * or VoIP.</span><br><span class="line"> */</span><br><span class="line">public final static int USAGE_VOICE_COMMUNICATION = 2;</span><br></pre></td></tr></table></figure>

<p>AudioSteamType 音频流类型：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">/** Used to identify the default audio stream volume */</span><br><span class="line">public static final int STREAM_DEFAULT = -1;</span><br><span class="line">/** Used to identify the volume of audio streams <span class="keyword">for</span> phone calls */</span><br><span class="line">public static final int STREAM_VOICE_CALL = 0;</span><br><span class="line">/** Used to identify the volume of audio streams <span class="keyword">for</span> system sounds */</span><br><span class="line">public static final int STREAM_SYSTEM = 1;</span><br><span class="line">/** Used to identify the volume of audio streams <span class="keyword">for</span> the phone ring and message alerts */</span><br><span class="line">public static final int STREAM_RING = 2;</span><br><span class="line">/** Used to identify the volume of audio streams <span class="keyword">for</span> music playback */</span><br><span class="line">public static final int STREAM_MUSIC = 3;</span><br><span class="line">/** Used to identify the volume of audio streams <span class="keyword">for</span> alarms */</span><br><span class="line">public static final int STREAM_ALARM = 4;</span><br><span class="line">/** Used to identify the volume of audio streams <span class="keyword">for</span> notifications */</span><br><span class="line">public static final int STREAM_NOTIFICATION = 5;</span><br><span class="line">/** Used to identify the volume of audio streams <span class="keyword">for</span> phone calls when connected on bluetooth */</span><br><span class="line">public static final int STREAM_BLUETOOTH_SCO = 6;</span><br><span class="line">/** Used to identify the volume of audio streams <span class="keyword">for</span> enforced system sounds <span class="keyword">in</span> certain</span><br><span class="line"> * countries (e.g camera <span class="keyword">in</span> Japan) */</span><br><span class="line">@UnsupportedAppUsage</span><br><span class="line">public static final int STREAM_SYSTEM_ENFORCED = 7;</span><br><span class="line">/** Used to identify the volume of audio streams <span class="keyword">for</span> DTMF tones */</span><br><span class="line">public static final int STREAM_DTMF = 8;</span><br><span class="line">/** Used to identify the volume of audio streams exclusively transmitted through the</span><br><span class="line"> *  speaker (TTS) of the device */</span><br><span class="line">public static final int STREAM_TTS = 9;</span><br><span class="line">/** Used to identify the volume of audio streams <span class="keyword">for</span> accessibility prompts */</span><br><span class="line">public static final int STREAM_ACCESSIBILITY = 10;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">//对齐音频流，代表哪些音量类型是同步一起变化的。</span><br><span class="line">private final int[] STREAM_VOLUME_ALIAS_VOICE = new int[] &#123;</span><br><span class="line">    AudioSystem.STREAM_VOICE_CALL,      // STREAM_VOICE_CALL</span><br><span class="line">    AudioSystem.STREAM_RING,            // STREAM_SYSTEM</span><br><span class="line">    AudioSystem.STREAM_RING,            // STREAM_RING</span><br><span class="line">    AudioSystem.STREAM_MUSIC,           // STREAM_MUSIC</span><br><span class="line">    AudioSystem.STREAM_ALARM,           // STREAM_ALARM</span><br><span class="line">    AudioSystem.STREAM_RING,            // STREAM_NOTIFICATION</span><br><span class="line">    AudioSystem.STREAM_BLUETOOTH_SCO,   // STREAM_BLUETOOTH_SCO</span><br><span class="line">    AudioSystem.STREAM_RING,            // STREAM_SYSTEM_ENFORCED</span><br><span class="line">    AudioSystem.STREAM_RING,            // STREAM_DTMF</span><br><span class="line">    AudioSystem.STREAM_MUSIC,           // STREAM_TTS</span><br><span class="line">    AudioSystem.STREAM_MUSIC            // STREAM_ACCESSIBILITY</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h2 id="音频流的音量初始化"><a href="#音频流的音量初始化" class="headerlink" title="音频流的音量初始化"></a>音频流的音量初始化</h2><p>每一个音频流类型都会创建一个VolumeStreamState实例，并且在开机后会加载每一个流的最大，最小音量。<br>并且从VolumeStreamState.readSettings()接口从Settings中初始化音量等级。</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">private void <span class="function"><span class="title">createStreamStates</span></span>() &#123;</span><br><span class="line">    int numStreamTypes = AudioSystem.getNumStreamTypes();</span><br><span class="line">    VolumeStreamState[] streams = mStreamStates = new VolumeStreamState[numStreamTypes];</span><br></pre></td></tr></table></figure>


<p>VolumeStreamState.readSettings</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">for (int i = 0; remainingDevices != 0; i++) &#123;</span><br><span class="line">    int device = (1 &lt;&lt; i);</span><br><span class="line">    if ((device &amp; remainingDevices) == 0) &#123;</span><br><span class="line">        continue;</span><br><span class="line">    &#125;</span><br><span class="line">    remainingDevices &amp;= ~device;</span><br><span class="line"></span><br><span class="line">    // retrieve current volume for device</span><br><span class="line">    // if no volume stored for current stream and device, use default volume if default</span><br><span class="line">    // device, continue otherwise</span><br><span class="line">    int defaultIndex = (device == AudioSystem.DEVICE_OUT_DEFAULT) ?</span><br><span class="line">            AudioSystem.DEFAULT_STREAM_VOLUME[mStreamType] : -1;</span><br><span class="line">    int index;</span><br><span class="line">    if (!hasValidSettingsName()) &#123;</span><br><span class="line">        index = defaultIndex;</span><br><span class="line">    &#125; else &#123;</span><br><span class="line">        String name = getSettingNameForDevice(device);</span><br><span class="line">        index = Settings.System.getIntForUser(</span><br><span class="line">                mContentResolver, name, defaultIndex, UserHandle.USER_CURRENT);</span><br><span class="line">    &#125;</span><br><span class="line">    if (index == -1) &#123;</span><br><span class="line">        continue;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    mIndexMap.put(device, getValidIndex(10 * index));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>最大最小音量：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">/** Maximum volume index values <span class="keyword">for</span> audio streams */</span><br><span class="line"> protected static int[] MAX_STREAM_VOLUME = new int[] &#123;</span><br><span class="line">     5,  // STREAM_VOICE_CALL</span><br><span class="line">     7,  // STREAM_SYSTEM</span><br><span class="line">     7,  // STREAM_RING</span><br><span class="line">     15, // STREAM_MUSIC</span><br><span class="line">     7,  // STREAM_ALARM</span><br><span class="line">     7,  // STREAM_NOTIFICATION</span><br><span class="line">     15, // STREAM_BLUETOOTH_SCO</span><br><span class="line">     7,  // STREAM_SYSTEM_ENFORCED</span><br><span class="line">     15, // STREAM_DTMF</span><br><span class="line">     15, // STREAM_TTS</span><br><span class="line">     15  // STREAM_ACCESSIBILITY</span><br><span class="line"> &#125;;</span><br><span class="line"> </span><br><span class="line"> /** Minimum volume index values <span class="keyword">for</span> audio streams */</span><br><span class="line"> protected static int[] MIN_STREAM_VOLUME = new int[] &#123;</span><br><span class="line">     1,  // STREAM_VOICE_CALL</span><br><span class="line">     0,  // STREAM_SYSTEM</span><br><span class="line">     0,  // STREAM_RING</span><br><span class="line">     0,  // STREAM_MUSIC</span><br><span class="line">     1,  // STREAM_ALARM</span><br><span class="line">     0,  // STREAM_NOTIFICATION</span><br><span class="line">     0,  // STREAM_BLUETOOTH_SCO</span><br><span class="line">     0,  // STREAM_SYSTEM_ENFORCED</span><br><span class="line">     0,  // STREAM_DTMF</span><br><span class="line">     0,  // STREAM_TTS</span><br><span class="line">     1   // STREAM_ACCESSIBILITY</span><br><span class="line"> &#125;;</span><br></pre></td></tr></table></figure>


<h2 id="音量调节"><a href="#音量调节" class="headerlink" title="音量调节"></a>音量调节</h2><p><img src="http://assets.processon.com/chart_image/5abcf222e4b018c271d42392.png" alt="流程图"></p>
<p>音量的设置过程：<br>1，首先判断当前哪个音频流处于激活状态，如果没有则默认调节指定的默认音频流（MUSIC）<br>2，获取到调节的音频流后，继续从音频流获取对应的音频设备<br>3，增减音量，并设置到当前的音量到播放线程（同步对齐其他音频流）<br>4，AudioFlinger根据类型，下发给AudioHal或者设置到播放线程上生效<br>5, 保存音量到Settings数据库中</p>
<h2 id="音量曲线"><a href="#音量曲线" class="headerlink" title="音量曲线"></a>音量曲线</h2><p>不同的场景，不同的设备，音量的调节幅度，上下限都是不一样，可以参考audio_policy_volumes.xml中的配置<br>实际也是为了满足相同音量等级下，不同场景的，不同设备的细微调整</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">&lt;volume stream=<span class="string">&quot;AUDIO_STREAM_MUSIC&quot;</span> deviceCategory=<span class="string">&quot;DEVICE_CATEGORY_HEADSET&quot;</span>&gt;</span><br><span class="line">    &lt;point&gt;1,-6000&lt;/point&gt;</span><br><span class="line">    &lt;point&gt;13,-5500&lt;/point&gt;</span><br><span class="line">    &lt;point&gt;20,-4200&lt;/point&gt;</span><br><span class="line">    &lt;point&gt;27,-3700&lt;/point&gt;</span><br><span class="line">    &lt;point&gt;33,-3300&lt;/point&gt;</span><br><span class="line">    &lt;point&gt;40,-2900&lt;/point&gt;</span><br><span class="line">    &lt;point&gt;47,-2500&lt;/point&gt;</span><br><span class="line">    &lt;point&gt;53,-2050&lt;/point&gt;</span><br><span class="line">    &lt;point&gt;60,-1700&lt;/point&gt;</span><br><span class="line">    &lt;point&gt;66,-1400&lt;/point&gt;</span><br><span class="line">    &lt;point&gt;73,-1100&lt;/point&gt;</span><br><span class="line">    &lt;point&gt;80,-800&lt;/point&gt;</span><br><span class="line">    &lt;point&gt;87,-500&lt;/point&gt;</span><br><span class="line">    &lt;point&gt;93,-250&lt;/point&gt;</span><br><span class="line">    &lt;point&gt;100,0&lt;/point&gt;</span><br><span class="line">&lt;/volume&gt;</span><br><span class="line"></span><br><span class="line">&lt;volume stream=<span class="string">&quot;AUDIO_STREAM_MUSIC&quot;</span> deviceCategory=<span class="string">&quot;DEVICE_CATEGORY_SPEAKER&quot;</span>&gt;</span><br><span class="line">    &lt;point&gt;1,-5500&lt;/point&gt;</span><br><span class="line">    &lt;point&gt;13,-4400&lt;/point&gt;</span><br><span class="line">    &lt;point&gt;20,-3800&lt;/point&gt;</span><br><span class="line">    &lt;point&gt;27,-3000&lt;/point&gt;</span><br><span class="line">    &lt;point&gt;33,-2200&lt;/point&gt;</span><br><span class="line">    &lt;point&gt;40,-2000&lt;/point&gt;</span><br><span class="line">    &lt;point&gt;47,-1800&lt;/point&gt;</span><br><span class="line">    &lt;point&gt;53,-1400&lt;/point&gt;</span><br><span class="line">    &lt;point&gt;60,-1200&lt;/point&gt;</span><br><span class="line">    &lt;point&gt;66,-1000&lt;/point&gt;</span><br><span class="line">    &lt;point&gt;73,-800&lt;/point&gt;</span><br><span class="line">    &lt;point&gt;80,-600&lt;/point&gt;</span><br><span class="line">    &lt;point&gt;87,-400&lt;/point&gt;</span><br><span class="line">    &lt;point&gt;93,-200&lt;/point&gt;</span><br><span class="line">    &lt;point&gt;100,0&lt;/point&gt;</span><br><span class="line">&lt;/volume&gt;</span><br></pre></td></tr></table></figure>


<h1 id="AudioTrack创建流程"><a href="#AudioTrack创建流程" class="headerlink" title="AudioTrack创建流程"></a>AudioTrack创建流程</h1><p><img src="http://dtimage.oss-cn-shanghai.aliyuncs.com/2017/0627/27182315gmwx.jpg" alt="类图"><br>图解：可以从图上看到，AudioFlinger这个类涵盖了很多成员类<br>AudioTrack 中实际持有的是TrackHandler，他实现了IAudioTrack的接口，用于控制对应在AudioFlinger里创建的Track，比如start stop pause这些控制操作<br>AudioRecord 也是类似的，持有RecordHandle，实现了IAudioRecord接口，控制录音的start， stop等操作</p>
<p>Track：在AudioFlinger上的播放/录音 客户端，于AudioTrack，AudioRecord 是一一对应的关系<br>ThreadBase 是继承Thread的一个在AudioFlinger中最基础的单元线程<br>PlaybackThread 继承ThreadBase，属于播放线程<br>RecordThread 继承ThreadBase，属于录音线程<br>AudioFlinger主要是如下几个播放线程以及录音线程组成<br>1，MixerThread ：混音线程，该线程上可以把多个Track<br>2，DirectOutputThread：直输线程，该线程上一般就一个Track，所以不能混音<br>3，OffloadThread：继承DirectOutputThread，一般用于播放一些非PCM格式的数据<br>4，MmapPlaybackThread：用于AAudio创建的一种新播放线程（图片比较早，还没有列出来）<br>5，RecordThread：录音线程<br>6，MmapCaptureThread：用于AAudio创建的一种新录音线程</p>
<p>AudioEffect：音效处理</p>
<p><img src="http://assets.processon.com/chart_image/5c076f96e4b0eb0da01fe56c.png" alt="AudioTrack创建流程图"></p>
<p>从内存分配方式分为以下两种，静态和动态<br>MODE_STATIC    应用进程将回放数据一次性付给 AudioTrack，适用于数据量小、时延要求高的场景<br>MODE_STREAM    用进程需要持续调用 write() 写数据到 FIFO，写数据时有可能遭遇阻塞（等待 AudioFlinger::PlaybackThread 消费之前的数据），基本适用所有的音频场景</p>
<p>MODE_STREAM模式下，从写入数据的方式又可以细分如下：<br>TRANSFER_SYNC  对应走AudioTrack.write<br>TRANSFER_CALLBACK 对应走cbf通知应用写入</p>
<p>创建比较重要的接口：<br>getMinBufferSize() 接口，字面意思是返回最小数据缓冲区的大小，它是声音能正常播放的最低保障，从函数参数来看，返回值取决于采样率、采样深度、声道数这三个属性。MODE_STREAM 模式下，应用程序重点参考其返回值然后确定分配多大的数据缓冲区。如果数据缓冲区分配得过小，那么播放声音会频繁遭遇 underrun，underrun 是指生产者（AudioTrack）提供数据的速度跟不上消费者（AudioFlinger::PlaybackThread）消耗数据的速度，反映到现实的后果就是声音断续卡顿，严重影响听觉体验。</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">/* static */ size_t AudioSystem::calculateMinFrameCount(</span><br><span class="line">        uint32_t afLatencyMs, uint32_t afFrameCount, uint32_t afSampleRate,</span><br><span class="line">        uint32_t sampleRate, <span class="built_in">float</span> speed /*, uint32_t notificationsPerBufferReq*/)</span><br><span class="line">&#123;</span><br><span class="line">    // Ensure that buffer depth covers at least audio hardware latency</span><br><span class="line">    uint32_t minBufCount = afLatencyMs / ((1000 * afFrameCount) / afSampleRate);</span><br><span class="line">    <span class="keyword">if</span> (minBufCount &lt; 2) &#123;</span><br><span class="line">        minBufCount = 2;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    ALOGV(<span class="string">&quot;calculateMinFrameCount afLatency %u  afFrameCount %u  afSampleRate %u  &quot;</span></span><br><span class="line">            <span class="string">&quot;sampleRate %u  speed %f  minBufCount: %u&quot;</span> /*<span class="string">&quot;  notificationsPerBufferReq %u&quot;</span>*/,</span><br><span class="line">            afLatencyMs, afFrameCount, afSampleRate, sampleRate, speed, minBufCount</span><br><span class="line">            /*, notificationsPerBufferReq*/);</span><br><span class="line">    <span class="built_in">return</span> minBufCount * sourceFramesNeededWithTimestretch(</span><br><span class="line">            sampleRate, afFrameCount, afSampleRate, speed);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>举例：采样率44.1kHz 采样深度是16bit， 声道数是立体声<br>一般来说选到的afSampleRate=48kHz，afFrameCount=1920， speed=1.000000<br>计算出来 2<em>（（1920</em>（44100/48000）+1 +1 ）* 1.000000 +1 +1）* 2（位宽）* 2（声道数）= 14144<br>+1 这种是为了调节四舍五入的问题</p>
<h1 id="AudioServer启动流程"><a href="#AudioServer启动流程" class="headerlink" title="AudioServer启动流程"></a>AudioServer启动流程</h1><p>从Android7.0 开始，Audio服务就从MediaServer中独立出来AudioServer，单独处理音频相关事务</p>
<h2 id="main-audioserver-cpp"><a href="#main-audioserver-cpp" class="headerlink" title="main_audioserver.cpp"></a>main_audioserver.cpp</h2><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">···</span><br><span class="line">AudioFlinger::instantiate();</span><br><span class="line">AudioPolicyService::instantiate();</span><br><span class="line">···</span><br></pre></td></tr></table></figure>

<h2 id="AudioPolicyService-cpp"><a href="#AudioPolicyService-cpp" class="headerlink" title="AudioPolicyService.cpp"></a>AudioPolicyService.cpp</h2><pre><code>···
void AudioPolicyService::onFirstRef()
&#123;
    &#123;
        Mutex::Autolock _l(mLock);

        // start audio commands thread
        mAudioCommandThread = new AudioCommandThread(String8(&quot;ApmAudio&quot;), this);
        // start output activity command thread
        mOutputCommandThread = new AudioCommandThread(String8(&quot;ApmOutput&quot;), this);

        mAudioPolicyClient = new AudioPolicyClient(this);
        mAudioPolicyManager = createAudioPolicyManager(mAudioPolicyClient);
    &#125;
    // load audio processing modules
    sp&lt;AudioPolicyEffects&gt;audioPolicyEffects = new AudioPolicyEffects();
    &#123;
        Mutex::Autolock _l(mLock);
        mAudioPolicyEffects = audioPolicyEffects;
    &#125;

    mUidPolicy = new UidPolicy(this);
    mUidPolicy-&gt;registerSelf();

    mSensorPrivacyPolicy = new SensorPrivacyPolicy(this);
    mSensorPrivacyPolicy-&gt;registerSelf();
&#125;
···
</code></pre>
<h2 id="AudioPolicyManager-cpp"><a href="#AudioPolicyManager-cpp" class="headerlink" title="AudioPolicyManager.cpp"></a>AudioPolicyManager.cpp</h2><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">···</span><br><span class="line">loadConfig();//初始化配置 =》 deserializeAudioPolicyXmlConfig（） 加载<span class="string">&quot;audio_policy_configuration.xml&quot;</span> 策略文件</span><br><span class="line">initialize();</span><br><span class="line">···</span><br></pre></td></tr></table></figure>

<h2 id="AudioPolicyManager-initialize"><a href="#AudioPolicyManager-initialize" class="headerlink" title="AudioPolicyManager::initialize()"></a>AudioPolicyManager::initialize()</h2><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">···</span><br><span class="line">// Retrieve the Policy Manager Interface 策略执行逻辑</span><br><span class="line">mEngine = engineInstance-&gt;queryInterface&lt;AudioPolicyManagerInterface&gt;();</span><br><span class="line"><span class="keyword">if</span> (mEngine == NULL) &#123;</span><br><span class="line">    ALOGE(<span class="string">&quot;%s: Failed to get Policy Engine Interface&quot;</span>, __FUNCTION__);</span><br><span class="line">    <span class="built_in">return</span> NO_INIT;</span><br><span class="line">&#125;</span><br><span class="line">mEngine-&gt;setObserver(this);</span><br><span class="line"></span><br><span class="line">//加载硬件设备模块，并打开设备</span><br><span class="line"><span class="keyword">for</span> (const auto&amp; hwModule : mHwModulesAll) &#123;</span><br><span class="line">    ···</span><br><span class="line">    sp&lt;SwAudioOutputDescriptor&gt; outputDesc = new SwAudioOutputDescriptor(outProfile,</span><br><span class="line">                                                                         mpClientInterface);</span><br><span class="line">    audio_io_handle_t output = AUDIO_IO_HANDLE_NONE;</span><br><span class="line">    status_t status = outputDesc-&gt;open(nullptr, DeviceVector(supportedDevice),</span><br><span class="line">                                       AUDIO_STREAM_DEFAULT,</span><br><span class="line">                                       AUDIO_OUTPUT_FLAG_NONE, &amp;output);</span><br><span class="line">    ...</span><br><span class="line">    addOutput(output, outputDesc); //add to mOutputs</span><br><span class="line">    ...</span><br><span class="line">    sp&lt;AudioInputDescriptor&gt; inputDesc =</span><br><span class="line">            new AudioInputDescriptor(inProfile, mpClientInterface);</span><br><span class="line"></span><br><span class="line">    audio_io_handle_t input = AUDIO_IO_HANDLE_NONE;</span><br><span class="line">    status_t status = inputDesc-&gt;open(nullptr,</span><br><span class="line">                                      availProfileDevices.itemAt(0),</span><br><span class="line">                                      AUDIO_SOURCE_MIC,</span><br><span class="line">                                      AUDIO_INPUT_FLAG_NONE,</span><br><span class="line">                                      &amp;input);</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="AudioFlinger-openOutput"><a href="#AudioFlinger-openOutput" class="headerlink" title="AudioFlinger.openOutput"></a>AudioFlinger.openOutput</h2><pre><code>#define AUDIO_HARDWARE_MODULE_ID_PRIMARY &quot;primary&quot;
#define AUDIO_HARDWARE_MODULE_ID_A2DP &quot;a2dp&quot;
#define AUDIO_HARDWARE_MODULE_ID_USB &quot;usb&quot;
    static const char * const audio_interfaces[] = &#123;
    AUDIO_HARDWARE_MODULE_ID_PRIMARY,
    AUDIO_HARDWARE_MODULE_ID_A2DP,
    AUDIO_HARDWARE_MODULE_ID_USB,
&#125;;
</code></pre>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">...</span><br><span class="line">AudioStreamOut *outputStream = NULL;</span><br><span class="line">//最终会走到对应的audio hal去打开具体的设备以及stream</span><br><span class="line">status_t status = outHwDev-&gt;openOutputStream(</span><br><span class="line">        &amp;outputStream,</span><br><span class="line">        *output,</span><br><span class="line">        devices,</span><br><span class="line">        flags,</span><br><span class="line">        config,</span><br><span class="line">        address.string());</span><br><span class="line">...</span><br></pre></td></tr></table></figure>


<h1 id="拓展学习"><a href="#拓展学习" class="headerlink" title="拓展学习"></a>拓展学习</h1><ul>
<li>设备切换时，路由策略选择</li>
<li>AudioEffect的流程学习</li>
<li>AudioHal中的设备切换，路由切换等</li>
<li>soundtrrigger 与audioserver的关系</li>
</ul>
]]></content>
      <categories>
        <category>Android</category>
      </categories>
      <tags>
        <tag>Android</tag>
        <tag>Audio</tag>
      </tags>
  </entry>
  <entry>
    <title>Android Device Feature</title>
    <url>/archives/50f93046.html</url>
    <content><![CDATA[<span id="more"></span>
<meta name="referrer" content="no-referrer"/>

<h1 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h1><p>为了满足对代码功能模块的定制，新增feature模块，其可以方便模块化功能的去与留，涉及makefile，c/c++，java等三个部分。开发者只需要定义一个值，在编译时会自动生成三个代码空间的控制变量，以此达到定义功能模块的目的。<br>feature分为两个层级：</p>
<ul>
<li>common</li>
<li>具体产品名，比如lemonadep</li>
</ul>
<p>一个feature开关可以同时在common和具体产品名中定义，他们的关系是：</p>
<ul>
<li>当只有common中有定义时，以common为准</li>
<li>当只有具体产品中有定义时，以产品名为准</li>
<li>当同时定义时，产品名中的定义会overlay common中的定义</li>
</ul>
<h1 id="结构说明"><a href="#结构说明" class="headerlink" title="结构说明"></a>结构说明</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">feature</span><br><span class="line">├── Android.bp</span><br><span class="line">├── build_out</span><br><span class="line">│   ├── include</span><br><span class="line">│   │   └── JosFeature.h</span><br><span class="line">│   ├── java</span><br><span class="line">│   │   └── com</span><br><span class="line">│   │       └── i-rtfsc</span><br><span class="line">│   │           └── JosFeature.java</span><br><span class="line">│   └── makefile</span><br><span class="line">│       └── JosFeature.mk</span><br><span class="line">├── jos_feature.mk</span><br><span class="line">├── common_feature.mk</span><br><span class="line">├── device</span><br><span class="line">│   ├── common</span><br><span class="line">│   │   └── features.cof</span><br><span class="line">│   └── lemonadep</span><br><span class="line">│       └── features.cof</span><br><span class="line">└── tools</span><br><span class="line">    ├── config_parse.py</span><br><span class="line">    ├── feature.py</span><br><span class="line">    ├── generator_header.py</span><br><span class="line">    ├── generator_java.py</span><br><span class="line">    ├── generator_makefile.py</span><br><span class="line">    ├── __init__.py</span><br><span class="line">    └── utils.py</span><br></pre></td></tr></table></figure>
<p>其中目录大致说明如下：</p>
<ul>
<li><p>tools：编译工具，用于生成对应目标代码文件</p>
</li>
<li><p>device/common：通用控制定义文件</p>
</li>
<li><p>device/lemonadep：产品控制定义文件</p>
</li>
<li><p>jos_feature.mk：feature引入编译系统makefile</p>
</li>
<li><p>common_feature.mk：定义一些aosp的宏，如 <strong>TARGET_EXCLUDES_AUDIOFX := true</strong> , 则表示不编译audiofx到rom中。</p>
<h1 id="使用方法"><a href="#使用方法" class="headerlink" title="使用方法"></a>使用方法</h1><p>feature使用方法很简单，分成两个部分：</p>
</li>
<li><p>feature定义</p>
</li>
<li><p>代码使用</p>
<h2 id="feature定义"><a href="#feature定义" class="headerlink" title="feature定义"></a>feature定义</h2><p>一般来说，feature的定义如下所示：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">device/common/features.cof</span><br><span class="line"></span><br><span class="line"># jos feature <span class="keyword">default</span></span><br><span class="line">JOS_FEATURE_DEFAULT = <span class="number">1</span></span><br><span class="line"># jos ai service</span><br><span class="line">JOS_FEATURE_AI_SERVICE = <span class="keyword">false</span></span><br><span class="line">#jos device</span><br><span class="line">JOS_FEATURE_DEVICE = <span class="string">&quot;lemonadep&quot;</span></span><br></pre></td></tr></table></figure>
<p>这类似于.ini文件，是键值对的定义。下面说明基本语法。</p>
<h3 id="注释"><a href="#注释" class="headerlink" title="注释"></a>注释</h3><p>feature定义只支持单行注释，注释是以”<strong>#</strong>“开头。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"># jos feature <span class="keyword">default</span></span><br><span class="line">JOS_FEATURE_DEFAULT = <span class="number">1</span></span><br></pre></td></tr></table></figure>
<h3 id="键值对"><a href="#键值对" class="headerlink" title="键值对"></a>键值对</h3><p>键值对定义格式为：<strong>KEY = VALUE</strong>。如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">JOS_FEATURE_AI_SERVICE = <span class="keyword">false</span></span><br></pre></td></tr></table></figure>
<p>其中key的命名规范为：A-Z，a-z，0-9，_，这些字符的组成，开头首字母不能是数字。<br>value支持的类型</p>
</li>
</ul>
<table>
<thead>
<tr>
<th><strong>类型</strong></th>
<th><strong>解析器定义</strong></th>
<th><strong>说明</strong></th>
<th><strong>举例</strong></th>
</tr>
</thead>
<tbody><tr>
<td>int</td>
<td>TYPE_INT</td>
<td>整数类型</td>
<td>JOS_FEATURE_DEFAULT = 1</td>
</tr>
<tr>
<td>boolean</td>
<td>TYPE_BOOLEAN</td>
<td>布尔类型</td>
<td>JOS_FEATURE_AI_SERVICE = false</td>
</tr>
<tr>
<td>String</td>
<td>TYPE_STRING</td>
<td>字符类型</td>
<td>JOS_FEATURE_DEVICE = “lemonadep”</td>
</tr>
</tbody></table>
<h2 id="代码使用"><a href="#代码使用" class="headerlink" title="代码使用"></a>代码使用</h2><p>feature可以在三个代码空间使用：</p>
<ul>
<li>java</li>
<li>c/cpp</li>
<li>makefile</li>
</ul>
<p>编译完成后，上述的代码会生成到：feature/build_out/ 目录下。</p>
<h3 id="java"><a href="#java" class="headerlink" title="java"></a>java</h3><p>java中使用feature时，先import com.i-rtfsc.JosFeature，然后就可以直接使用对应的变量了。如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> com.i-rtfsc.<span class="function">JosFeature</span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function"><span class="title">if</span> <span class="params">(JosFeature.JOS_FEATURE_AI_SERVICE)</span> </span>&#123;</span><br><span class="line">    <span class="comment">//TODO</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="c-c"><a href="#c-c" class="headerlink" title="c/c++"></a>c/c++</h3><p>C中定义的都是宏变量，使用方法举例如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">#include &lt;product/JosFeature.h&gt;</span><br><span class="line"></span><br><span class="line">#<span class="keyword">if</span> JOS_FEATURE_AI_SERVICE</span><br><span class="line"></span><br><span class="line"><span class="comment">//TODO</span></span><br><span class="line"></span><br><span class="line">#endif</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<h3 id="Makefile"><a href="#Makefile" class="headerlink" title="Makefile"></a>Makefile</h3><p>mk中可以直接使用，比如：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">ifeq ($(JOS_FEATURE_AI_SERVICE),<span class="keyword">true</span>)</span><br><span class="line"></span><br><span class="line"># TODO</span><br><span class="line"></span><br><span class="line">endif</span><br></pre></td></tr></table></figure>

<h1 id="代码原理"><a href="#代码原理" class="headerlink" title="代码原理"></a>代码原理</h1><p>features.cof是由python脚本来解释的，解释程序的路径为：feature/tools/。整个feature实现比较简单，处理流程分如下几个步骤：</p>
<ul>
<li>将jos_feature.mk文件添加到android的编译系统中（build/core/product_config.mk）。</li>
<li>当lunch的时候，会先运行feature.py解析feature.def文件</li>
<li>config_parse.py先会检查feature.def文件的语法错误</li>
<li>错误检查通过后，config_parse.py继续将feature.def文件转换成字典</li>
<li>将生成的字典分别提交给不同code的生成者生成对应的代码。</li>
</ul>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>综上，feature功能是一个在编译期用python脚本解析features.cof文件，生成三个代码空间的控制变量代码文件，最终被静态添加到目标程序中的过程。</p>
<h1 id="附加"><a href="#附加" class="headerlink" title="附加"></a>附加</h1><p>生成的com.i-rtfsc.JosFeature还可以编译到framework.jar中</p>
<h1 id="代码"><a href="#代码" class="headerlink" title="代码"></a>代码</h1><p><a href="https://github.com/i-rtfsc/feature">feature</a></p>
]]></content>
      <categories>
        <category>Android</category>
      </categories>
      <tags>
        <tag>Android</tag>
        <tag>Feature</tag>
      </tags>
  </entry>
  <entry>
    <title>Android SeLinux</title>
    <url>/archives/dfb6706d.html</url>
    <content><![CDATA[<span id="more"></span>
<meta name="referrer" content="no-referrer"/>

<p>整理中…很快就补上…</p>
]]></content>
      <categories>
        <category>Android</category>
      </categories>
      <tags>
        <tag>Android</tag>
        <tag>SeLinux</tag>
      </tags>
  </entry>
  <entry>
    <title>Android Overlays</title>
    <url>/archives/cdf2c394.html</url>
    <content><![CDATA[<span id="more"></span>
<meta name="referrer" content="no-referrer"/>

<p>整理中…很快就补上…</p>
]]></content>
      <categories>
        <category>Android</category>
      </categories>
      <tags>
        <tag>Android</tag>
        <tag>overlays</tag>
        <tag>sro</tag>
        <tag>rro</tag>
      </tags>
  </entry>
  <entry>
    <title>Android 12系统启动-Launcher</title>
    <url>/archives/d6edba4e.html</url>
    <content><![CDATA[<span id="more"></span>
<meta name="referrer" content="no-referrer"/>


<h1 id="Android-12系统启动文章列表"><a href="#Android-12系统启动文章列表" class="headerlink" title="Android 12系统启动文章列表"></a>Android 12系统启动文章列表</h1><p><a href="https://i-rtfsc.github.io/archives/68a972b0.html">Android 12系统启动-简介</a><br><a href="https://i-rtfsc.github.io/archives/6049d7b6.html">Android 12系统启动-Zygote</a><br><a href="https://i-rtfsc.github.io/archives/ca7c5e61.html">Android 12系统启动-init</a><br><a href="https://i-rtfsc.github.io/archives/1acbe70b.html">Android 12系统启动-SystemServer</a><br><a href="https://i-rtfsc.github.io/archives/49ff48d4.html">Android 12系统启动-AMS</a><br><a href="https://i-rtfsc.github.io/archives/d6edba4e.html">Android 12系统启动-Launcher</a></p>
<p>整理中…很快就补上…</p>
]]></content>
      <categories>
        <category>Android</category>
      </categories>
      <tags>
        <tag>Android</tag>
        <tag>系统启动</tag>
      </tags>
  </entry>
  <entry>
    <title>Android 12系统启动-AMS</title>
    <url>/archives/49ff48d4.html</url>
    <content><![CDATA[<span id="more"></span>
<meta name="referrer" content="no-referrer"/>


<h1 id="Android-12系统启动文章列表"><a href="#Android-12系统启动文章列表" class="headerlink" title="Android 12系统启动文章列表"></a>Android 12系统启动文章列表</h1><p><a href="https://i-rtfsc.github.io/archives/68a972b0.html">Android 12系统启动-简介</a><br><a href="https://i-rtfsc.github.io/archives/6049d7b6.html">Android 12系统启动-Zygote</a><br><a href="https://i-rtfsc.github.io/archives/ca7c5e61.html">Android 12系统启动-init</a><br><a href="https://i-rtfsc.github.io/archives/1acbe70b.html">Android 12系统启动-SystemServer</a><br><a href="https://i-rtfsc.github.io/archives/49ff48d4.html">Android 12系统启动-AMS</a><br><a href="https://i-rtfsc.github.io/archives/d6edba4e.html">Android 12系统启动-Launcher</a></p>
<p>整理中…很快就补上…</p>
]]></content>
      <categories>
        <category>Android</category>
      </categories>
      <tags>
        <tag>Android</tag>
        <tag>系统启动</tag>
      </tags>
  </entry>
  <entry>
    <title>Android 12系统启动-SystemServer</title>
    <url>/archives/1acbe70b.html</url>
    <content><![CDATA[<span id="more"></span>
<meta name="referrer" content="no-referrer"/>


<h1 id="Android-12系统启动文章列表"><a href="#Android-12系统启动文章列表" class="headerlink" title="Android 12系统启动文章列表"></a>Android 12系统启动文章列表</h1><p><a href="https://i-rtfsc.github.io/archives/68a972b0.html">Android 12系统启动-简介</a><br><a href="https://i-rtfsc.github.io/archives/6049d7b6.html">Android 12系统启动-Zygote</a><br><a href="https://i-rtfsc.github.io/archives/ca7c5e61.html">Android 12系统启动-init</a><br><a href="https://i-rtfsc.github.io/archives/1acbe70b.html">Android 12系统启动-SystemServer</a><br><a href="https://i-rtfsc.github.io/archives/49ff48d4.html">Android 12系统启动-AMS</a><br><a href="https://i-rtfsc.github.io/archives/d6edba4e.html">Android 12系统启动-Launcher</a></p>
<p>整理中…很快就补上…</p>
]]></content>
      <categories>
        <category>Android</category>
      </categories>
      <tags>
        <tag>Android</tag>
        <tag>系统启动</tag>
      </tags>
  </entry>
  <entry>
    <title>Android 12系统启动-Zygote</title>
    <url>/archives/6049d7b6.html</url>
    <content><![CDATA[<span id="more"></span>
<meta name="referrer" content="no-referrer"/>


<h1 id="Android-12系统启动文章列表"><a href="#Android-12系统启动文章列表" class="headerlink" title="Android 12系统启动文章列表"></a>Android 12系统启动文章列表</h1><p><a href="https://i-rtfsc.github.io/archives/68a972b0.html">Android 12系统启动-简介</a><br><a href="https://i-rtfsc.github.io/archives/6049d7b6.html">Android 12系统启动-Zygote</a><br><a href="https://i-rtfsc.github.io/archives/ca7c5e61.html">Android 12系统启动-init</a><br><a href="https://i-rtfsc.github.io/archives/1acbe70b.html">Android 12系统启动-SystemServer</a><br><a href="https://i-rtfsc.github.io/archives/49ff48d4.html">Android 12系统启动-AMS</a><br><a href="https://i-rtfsc.github.io/archives/d6edba4e.html">Android 12系统启动-Launcher</a></p>
<p>整理中…很快就补上…</p>
]]></content>
      <categories>
        <category>Android</category>
      </categories>
      <tags>
        <tag>Android</tag>
        <tag>系统启动</tag>
      </tags>
  </entry>
  <entry>
    <title>Android 12系统启动-init</title>
    <url>/archives/ca7c5e61.html</url>
    <content><![CDATA[<span id="more"></span>
<meta name="referrer" content="no-referrer"/>


<h1 id="Android-12系统启动文章列表"><a href="#Android-12系统启动文章列表" class="headerlink" title="Android 12系统启动文章列表"></a>Android 12系统启动文章列表</h1><p><a href="https://i-rtfsc.github.io/archives/68a972b0.html">Android 12系统启动-简介</a><br><a href="https://i-rtfsc.github.io/archives/6049d7b6.html">Android 12系统启动-Zygote</a><br><a href="https://i-rtfsc.github.io/archives/ca7c5e61.html">Android 12系统启动-init</a><br><a href="https://i-rtfsc.github.io/archives/1acbe70b.html">Android 12系统启动-SystemServer</a><br><a href="https://i-rtfsc.github.io/archives/49ff48d4.html">Android 12系统启动-AMS</a><br><a href="https://i-rtfsc.github.io/archives/d6edba4e.html">Android 12系统启动-Launcher</a></p>
<p>整理中…很快就补上…</p>
]]></content>
      <categories>
        <category>Android</category>
      </categories>
      <tags>
        <tag>Android</tag>
        <tag>系统启动</tag>
      </tags>
  </entry>
  <entry>
    <title>Ashmem简介（Android IPC传输大数据）</title>
    <url>/archives/f1684132.html</url>
    <content><![CDATA[<span id="more"></span>
<meta name="referrer" content="no-referrer"/>


<h1 id="Binder机制文章列表"><a href="#Binder机制文章列表" class="headerlink" title="Binder机制文章列表"></a>Binder机制文章列表</h1><p><a href="https://i-rtfsc.github.io/archives/d6aea0f1.html">Binder概述</a><br><a href="https://i-rtfsc.github.io/archives/3e87e0ce.html">Binder机制01-驱动</a><br><a href="https://i-rtfsc.github.io/archives/f17b1d52.html">Binder机制02-ServiceManager</a><br><a href="https://i-rtfsc.github.io/archives/937d16d8.html">Binder机制03-Framework-Native</a><br><a href="https://i-rtfsc.github.io/archives/73029508.html">Binder机制04-Framework-Jave</a><br><a href="https://i-rtfsc.github.io/archives/ed3eb50b.html">Binder机制05-AIDL</a><br><a href="https://i-rtfsc.github.io/archives/4e7f84b4.html">Binder机制06-框架</a><br><a href="https://i-rtfsc.github.io/archives/d30c7ecb.html">Binder机制07-实例分析</a><br><a href="https://i-rtfsc.github.io/archives/720ded9.html">Binder机制08-总结</a><br><a href="https://i-rtfsc.github.io/archives/f1684132.html">Ashmem简介（Android IPC传输大数据）</a></p>
<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>Ashmem为什么会诞生？作用是什么？在聊这个问题之前我们先来了解Android的IPC都用那些，还有就是网上传得很火的binder限制1M是怎么回事，如何解决限制1M的问题？</p>
<h2 id="IPC-方式"><a href="#IPC-方式" class="headerlink" title="IPC 方式"></a>IPC 方式</h2><h3 id="Activity"><a href="#Activity" class="headerlink" title="Activity"></a>Activity</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Intent callIntent = <span class="keyword">new</span> Intent(Intent.ACTION_CALL, Uri.parse(<span class="string">&quot;tel:021-1234567&quot;</span> ); </span><br><span class="line">mContext.startActivity(callIntent);</span><br></pre></td></tr></table></figure>
<h3 id="Content-Provider"><a href="#Content-Provider" class="headerlink" title="Content Provider"></a>Content Provider</h3><p>Android应用程序可以使用文件或SqlLite数据库来存储数据。<br>Content Provider提供了一种在多个应用程序之间数据共享的方式（跨进程共享数据），应用程序可以利用Content Provider完成下面的工作：</p>
<ul>
<li>查询数据</li>
<li>修改数据</li>
<li>添加数据</li>
<li>删除数据<h3 id="Broadcast"><a href="#Broadcast" class="headerlink" title="Broadcast"></a>Broadcast</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Intent intent = <span class="keyword">new</span> Intent(<span class="string">&quot;com.android.ACTION_TEST&quot;</span>);</span><br><span class="line">intent.putExtra(<span class="string">&quot;value&quot;</span>,<span class="string">&quot;content&quot;</span>);</span><br><span class="line">mContext.sendBroadcast(intent);</span><br></pre></td></tr></table></figure>
<h3 id="LocalSocket"><a href="#LocalSocket" class="headerlink" title="LocalSocket"></a>LocalSocket</h3><h4 id="客户端"><a href="#客户端" class="headerlink" title="客户端"></a>客户端</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">客户端LocalSocket代码</span><br><span class="line"></span><br><span class="line"><span class="comment">//创建对象</span></span><br><span class="line">LocalSocket localSocket = <span class="keyword">new</span> LocalSocket();</span><br><span class="line"><span class="comment">//连接socketServerSocket</span></span><br><span class="line">localSocket.connect(<span class="keyword">new</span> LocalSocketAddress(String addrStr));</span><br><span class="line">获取localSocket的输入输出流：</span><br><span class="line">outputStream = localSocket.getOutputStream();</span><br><span class="line">inputStream = localSocket.getInputStream();</span><br><span class="line">写入数据：</span><br><span class="line">outputStream.write(<span class="string">&quot;数据&quot;</span>.getBytes());</span><br><span class="line">循环接收数据：</span><br><span class="line"><span class="keyword">try</span> &#123;    </span><br><span class="line">	<span class="keyword">int</span> readed = inputStream.read();</span><br><span class="line">	<span class="keyword">int</span> size = <span class="number">0</span>;</span><br><span class="line">	<span class="keyword">byte</span>[] bytes = <span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">0</span>];</span><br><span class="line">	<span class="keyword">while</span> (readed != -<span class="number">1</span>) &#123;</span><br><span class="line">	  <span class="keyword">byte</span>[] copy = <span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">500000</span>]; </span><br><span class="line">	  System.arraycopy(bytes, <span class="number">0</span>, copy, <span class="number">0</span>, bytes.length);</span><br><span class="line">	  bytes = copy;</span><br><span class="line">	  bytes[size++] = (<span class="keyword">byte</span>) readed; </span><br><span class="line">	  <span class="comment">//以换行符标识成结束</span></span><br><span class="line">	  <span class="keyword">if</span> (<span class="string">&#x27;\n&#x27;</span> == (<span class="keyword">byte</span>) readed) &#123;</span><br><span class="line">	      String resultStr = <span class="keyword">new</span> String(bytes, <span class="number">0</span>, size); </span><br><span class="line">	      <span class="keyword">break</span>;                                                         				     </span><br><span class="line">	&#125; </span><br><span class="line">	readed = inputStream.read();</span><br><span class="line">	&#125;</span><br><span class="line">&#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">	<span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="服务端"><a href="#服务端" class="headerlink" title="服务端"></a>服务端</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">服务端LocalServerSocket代码</span><br><span class="line"></span><br><span class="line"><span class="comment">//初始化</span></span><br><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">    <span class="comment">//socketAddress需跟localSocket地址一致，否则无法连接上</span></span><br><span class="line">    serverSocket = <span class="keyword">new</span> LocalServerSocket(socketAddress);</span><br><span class="line">&#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">    LoggerHelper.e(<span class="string">&quot;Server to establish connection exception:&quot;</span> + e.toString());</span><br><span class="line">    e.printStackTrace();</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">    <span class="comment">//获取接收的LocalSocket</span></span><br><span class="line">    localSocket = serverSocket.accept();</span><br><span class="line">    <span class="comment">//设置缓冲大小</span></span><br><span class="line">    localSocket.setReceiveBufferSize(ConstantConfig.BUFFER_SIZE);</span><br><span class="line">    localSocket.setSendBufferSize(ConstantConfig.BUFFER_SIZE);</span><br><span class="line">&#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">    e.printStackTrace();</span><br><span class="line">    LoggerHelper.d(<span class="string">&quot;Waiting to be linked to a bug,error:&quot;</span> + e.toString());</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">&#125;</span><br><span class="line">获取输入输出流一致：</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (localSocket != <span class="keyword">null</span>) &#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        inputStream = localSocket.getInputStream();</span><br><span class="line">        outputStream = localSocket.getOutputStream();</span><br><span class="line">        <span class="comment">/** 允许一直接收数据，一直到连接被断开，则认为应用端退出，自己也退出 */</span></span><br><span class="line">        <span class="keyword">while</span> (isLock &amp;&amp; receiveData()) ;</span><br><span class="line">    &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">        LoggerHelper.e(<span class="string">&quot;Get stream exception:&quot;</span> + e.toString());        e.printStackTrace();</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="AIDL-Service"><a href="#AIDL-Service" class="headerlink" title="AIDL Service"></a>AIDL Service</h3>aidl的基本用法请自行百度</li>
</ul>
<h3 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h3><p>除了scocket的通信方式，其他的通信方式都是基于binder机制，所以肯定也受binder的一些限制，那么binder限制1M是从哪里看出来的呢?</p>
<h2 id="Binder限制"><a href="#Binder限制" class="headerlink" title="Binder限制"></a>Binder限制</h2><p>我们先来看看Binder初始化时候的代码：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/ProcessState.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> BINDER_VM_SIZE ((1 * 1024 * 1024) - sysconf(_SC_PAGE_SIZE) * 2)</span></span><br><span class="line"></span><br><span class="line">ProcessState::<span class="built_in">ProcessState</span>(<span class="keyword">const</span> <span class="keyword">char</span> *driver)</span><br><span class="line">    : <span class="built_in">mDriverName</span>(<span class="built_in">String8</span>(driver))</span><br><span class="line">    , <span class="built_in">mDriverFD</span>(<span class="built_in">open_driver</span>(driver))</span><br><span class="line">    , <span class="built_in">mVMStart</span>(MAP_FAILED)</span><br><span class="line">    , <span class="built_in">mThreadCountLock</span>(PTHREAD_MUTEX_INITIALIZER)</span><br><span class="line">    , <span class="built_in">mThreadCountDecrement</span>(PTHREAD_COND_INITIALIZER)</span><br><span class="line">    , <span class="built_in">mExecutingThreadsCount</span>(<span class="number">0</span>)</span><br><span class="line">    , <span class="built_in">mMaxThreads</span>(DEFAULT_MAX_BINDER_THREADS)</span><br><span class="line">    , <span class="built_in">mStarvationStartTimeMs</span>(<span class="number">0</span>)</span><br><span class="line">    , <span class="built_in">mBinderContextCheckFunc</span>(<span class="literal">nullptr</span>)</span><br><span class="line">    , <span class="built_in">mBinderContextUserData</span>(<span class="literal">nullptr</span>)</span><br><span class="line">    , <span class="built_in">mThreadPoolStarted</span>(<span class="literal">false</span>)</span><br><span class="line">    , <span class="built_in">mThreadPoolSeq</span>(<span class="number">1</span>)</span><br><span class="line">    , <span class="built_in">mCallRestriction</span>(CallRestriction::NONE)</span><br><span class="line">&#123;</span><br><span class="line"></span><br><span class="line">	...</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (mDriverFD &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="comment">// mmap the binder, providing a chunk of virtual address space to receive transactions.</span></span><br><span class="line">        mVMStart = <span class="built_in">mmap</span>(<span class="literal">nullptr</span>, BINDER_VM_SIZE, PROT_READ, MAP_PRIVATE | MAP_NORESERVE, mDriverFD, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">        ...</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line"> &#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>能看到应用在初始化Binder的时候，已经限制了大小为1M-2页(1页=4k)的大小也就是1016k大小，所以网上说的限制1M其实不对，应该是1016K。<br>如果只是传输命令的话还可以，但是要传输图像数据这个大小根本不够。加上Binder内部有对每一个Binder内核缓冲区有自己的调度算法，没办法满足以最快的速度传输到SF进程中。也因此，Android选择使用共享内存的方式传递数据，也就是Ashmem匿名内存。<br>到这来其实我们还是不知道Ashmem到底是什么，了解Linux的同学应该都知道linux共享内存，那么我们先来了解一下Linux共享内存。</p>
<h2 id="Linux共享内存"><a href="#Linux共享内存" class="headerlink" title="Linux共享内存"></a>Linux共享内存</h2><p>首先看一下两个关键函数，</p>
<ul>
<li>int shmget(key_t key, size_t size, int shmflg); 该函数用来创建共享内存</li>
<li>void *shmat(int shm_id, const void *shm_addr, int shmflg); 要想访问共享内存，必须将其映射到当前进程的地址空间<blockquote>
<p>其中key_t是共享内存的唯一标识，可以说，Linux的共享内存其实是<strong>有名</strong>共享内存，而名字就是key，具体用法如下</p>
</blockquote>
</li>
</ul>
<h3 id="读取进程"><a href="#读取进程" class="headerlink" title="读取进程"></a>读取进程</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span>  </span></span><br><span class="line"><span class="function"></span>&#123;  </span><br><span class="line">    <span class="keyword">void</span> *shm = NULL;<span class="comment">//分配的共享内存的原始首地址  </span></span><br><span class="line">    struct shared_use_st *shared;<span class="comment">//指向shm  </span></span><br><span class="line">    <span class="keyword">int</span> shmid;<span class="comment">//共享内存标识符  </span></span><br><span class="line">    <span class="comment">//创建共享内存  </span></span><br><span class="line">    shmid = shmget((key_t)<span class="number">12345</span>, sizeof(struct shared_use_st), <span class="number">0666</span>|IPC_CREAT);   </span><br><span class="line">    <span class="comment">//将共享内存映射到当前进程的地址空间  </span></span><br><span class="line">    shm = shmat(shmid, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line">    <span class="comment">//设置共享内存  </span></span><br><span class="line">    shared = (struct shared_use_st*)shm;  </span><br><span class="line">    shared-&gt;written = <span class="number">0</span>;  </span><br><span class="line">    <span class="comment">//访问共享内存</span></span><br><span class="line">    <span class="keyword">while</span>(<span class="number">1</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span>(shared-&gt;written != <span class="number">0</span>) &#123; </span><br><span class="line">            printf(<span class="string">&quot;You wrote: %s&quot;</span>, shared-&gt;text);</span><br><span class="line">             <span class="keyword">if</span>(strncmp(shared-&gt;text, <span class="string">&quot;end&quot;</span>, <span class="number">3</span>) == <span class="number">0</span>)  </span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;&#125;</span><br><span class="line">    <span class="comment">//把共享内存从当前进程中分离  </span></span><br><span class="line">    <span class="keyword">if</span>(shmdt(shm) == -<span class="number">1</span>) &#123; &#125;  </span><br><span class="line">    <span class="comment">//删除共享内存  </span></span><br><span class="line">    <span class="keyword">if</span>(shmctl(shmid, IPC_RMID, <span class="number">0</span>) == -<span class="number">1</span>) &#123;  &#125;  </span><br><span class="line">    exit(EXIT_SUCCESS);  </span><br><span class="line">&#125;  </span><br></pre></td></tr></table></figure>
<h3 id="写进程"><a href="#写进程" class="headerlink" title="写进程"></a>写进程</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span>  </span></span><br><span class="line"><span class="function"></span>&#123;  </span><br><span class="line">    <span class="keyword">void</span> *shm = NULL;  </span><br><span class="line">    struct shared_use_st *shared = NULL;  </span><br><span class="line">    <span class="keyword">char</span> buffer[BUFSIZ + <span class="number">1</span>];<span class="comment">//用于保存输入的文本  </span></span><br><span class="line">    <span class="keyword">int</span> shmid;  </span><br><span class="line">    <span class="comment">//创建共享内存  </span></span><br><span class="line">    shmid = shmget((key_t) <span class="number">12345</span>, sizeof(struct shared_use_st), <span class="number">0666</span>|IPC_CREAT);  </span><br><span class="line">    <span class="comment">//将共享内存连接到当前进程的地址空间  </span></span><br><span class="line">    shm = shmat(shmid, (<span class="keyword">void</span>*)<span class="number">0</span>, <span class="number">0</span>);  </span><br><span class="line">    printf(<span class="string">&quot;Memory attached at %X\n&quot;</span>, (<span class="keyword">int</span>)shm);  </span><br><span class="line">    <span class="comment">//设置共享内存  </span></span><br><span class="line">    shared = (struct shared_use_st*)shm;  </span><br><span class="line">    <span class="keyword">while</span>(<span class="number">1</span>)<span class="comment">//向共享内存中写数据  </span></span><br><span class="line">    &#123;  </span><br><span class="line">        <span class="comment">//数据还没有被读取，则等待数据被读取,不能向共享内存中写入文本  </span></span><br><span class="line">        <span class="keyword">while</span>(shared-&gt;written == <span class="number">1</span>)  </span><br><span class="line">        &#123;  </span><br><span class="line">            sleep(<span class="number">1</span>);  </span><br><span class="line">        &#125;  </span><br><span class="line">        <span class="comment">//向共享内存中写入数据  </span></span><br><span class="line">        fgets(buffer, BUFSIZ, stdin);  </span><br><span class="line">        strncpy(shared-&gt;text, buffer, TEXT_SZ);  </span><br><span class="line">        shared-&gt;written = <span class="number">1</span>;  </span><br><span class="line">        <span class="keyword">if</span>(strncmp(buffer, <span class="string">&quot;end&quot;</span>, <span class="number">3</span>) == <span class="number">0</span>)  </span><br><span class="line">            running = <span class="number">0</span>;  </span><br><span class="line">    &#125;  </span><br><span class="line">    <span class="comment">//把共享内存从当前进程中分离  </span></span><br><span class="line">    <span class="keyword">if</span>(shmdt(shm) == -<span class="number">1</span>) &#123;    &#125;  </span><br><span class="line">    sleep(<span class="number">2</span>);  </span><br><span class="line">    exit(EXIT_SUCCESS);  </span><br><span class="line">&#125; </span><br></pre></td></tr></table></figure>
<p>可以看到，Linux共享内存通信效率非常高，进程间不需要传递数据，便可以直接访问，缺点也很明显，Linux共享内存没有提供同步的机制，在使用时，要借助其他的手段来处理进程间同步。</p>
<h2 id="思考"><a href="#思考" class="headerlink" title="思考"></a>思考</h2><p>在Android系统中，APP端View视图的数据是如何传递SurfaceFlinger服务的呢？View绘制的数据最终是按照一帧一帧显示到屏幕的，而每一帧都会占用一定的存储空间，在APP端执行draw的时候，数据很明显是要绘制到APP的进程空间，但是视图窗口要经过SurfaceFlinger图层混排才会生成最终的帧，而SurfaceFlinger又运行在另一个独立的服务进程，那么View视图的数据是如何在两个进程间传递的呢，普通的Binder通信肯定不行，因为Binder不太适合这种数据量较大的通信，那么View数据的通信采用的是什么IPC手段呢？<br>答案就是共享内存，更精确的说是匿名共享内存。也就是我们之前一直提到的Ashmem。<br>Android直接在Linux共享内存机制上做出改进，进而形成了Android的匿名共享内存（Anonymous Shared Memory-Ashmem）。<br>通过Ashmem，APP进程同SurfaceFlinger共用一块内存，如此，就不需要进行数据拷贝，APP端绘制完毕，通知SurfaceFlinger端合成，再输出到硬件进行显示即可。<br><img src="https://cdn.nlark.com/yuque/0/2021/png/1759879/1631929666796-8fbcc6a4-562e-4191-9109-485875386c19.png#clientId=u0b0c0eaa-bd62-4&from=paste&id=u32755f18&margin=%5Bobject%20Object%5D&name=image.png&originHeight=2844&originWidth=4182&originalType=url&ratio=1&size=2248211&status=done&style=none&taskId=u108b0194-0de3-4387-8023-f6d69d19341" alt="image.png"><br>（图片来源于网络）</p>
<h1 id="使用"><a href="#使用" class="headerlink" title="使用"></a>使用</h1><p>其实Ashmem不仅仅只是内核中能够使用，其实在Java层Android也提供了一个名为MemoryFile的类提供方便使用匿名共享内存，本次就以MemoryFile为切口，来聊聊Ashmem匿名内存的使用。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * IMemoryAidlInterface.aidl</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">package com.i-rtfsc.ipcbigdata;</span><br><span class="line"><span class="keyword">import</span> android.os.ParcelFileDescriptor;</span><br><span class="line"></span><br><span class="line">interface IMemoryAidlInterface &#123;</span><br><span class="line">    <span class="function">ParcelFileDescriptor <span class="title">getParcelFileDescriptor</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * MemoryFetchService.java</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MemoryFetchService</span> <span class="title">extends</span> <span class="title">Service</span> &#123;</span></span><br><span class="line">    @Nullable</span><br><span class="line">    @<span class="function">Override</span></span><br><span class="line"><span class="function">    <span class="keyword">public</span> IBinder <span class="title">onBind</span><span class="params">(Intent intent)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">MemoryFetchStub</span>();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">MemoryFetchStub</span> <span class="title">extends</span> <span class="title">IMemoryAidlInterface</span>.<span class="title">Stub</span> &#123;</span></span><br><span class="line">        @<span class="function">Override</span></span><br><span class="line"><span class="function">        <span class="keyword">public</span> ParcelFileDescriptor <span class="title">getParcelFileDescriptor</span><span class="params">()</span> throws RemoteException </span>&#123;</span><br><span class="line">            MemoryFile memoryFile = null;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                memoryFile = <span class="keyword">new</span> <span class="built_in">MemoryFile</span>(<span class="string">&quot;test_memory&quot;</span>, <span class="number">1024</span>);</span><br><span class="line">                memoryFile.<span class="built_in">getOutputStream</span>().<span class="built_in">write</span>(<span class="keyword">new</span> byte[]&#123;<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>&#125;);</span><br><span class="line">                Method method = MemoryFile.class.<span class="built_in">getDeclaredMethod</span>(<span class="string">&quot;getFileDescriptor&quot;</span>);</span><br><span class="line">                FileDescriptor des = (FileDescriptor) method.<span class="built_in">invoke</span>(memoryFile);</span><br><span class="line">                <span class="keyword">return</span> ParcelFileDescriptor.<span class="built_in">dup</span>(des);</span><br><span class="line">            &#125; <span class="built_in"><span class="keyword">catch</span></span> (Exception e) &#123;&#125;</span><br><span class="line">            <span class="keyword">return</span> null;</span><br><span class="line">     &#125;&#125;&#125;</span><br><span class="line"></span><br><span class="line">     &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * TestActivity.java</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">Intent intent = <span class="keyword">new</span> <span class="built_in">Intent</span>(MainActivity.<span class="keyword">this</span>, MemoryFetchService.class);</span><br><span class="line"><span class="built_in">bindService</span>(intent, <span class="keyword">new</span> <span class="built_in">ServiceConnection</span>() &#123;</span><br><span class="line">    @Override</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="built_in">onServiceConnected</span>(ComponentName name, IBinder service) &#123;</span><br><span class="line"></span><br><span class="line">        byte[] content = <span class="keyword">new</span> byte[<span class="number">10</span>];</span><br><span class="line">        IMemoryAidlInterface iMemoryAidlInterface</span><br><span class="line">                = IMemoryAidlInterface.Stub.<span class="built_in">asInterface</span>(service);</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            ParcelFileDescriptor parcelFileDescriptor = iMemoryAidlInterface.<span class="built_in">getParcelFileDescriptor</span>();</span><br><span class="line">            FileDescriptor descriptor = parcelFileDescriptor.<span class="built_in">getFileDescriptor</span>();</span><br><span class="line">            FileInputStream fileInputStream = <span class="keyword">new</span> <span class="built_in">FileInputStream</span>(descriptor);</span><br><span class="line">            fileInputStream.<span class="built_in">read</span>(content);</span><br><span class="line">        &#125; <span class="built_in"><span class="keyword">catch</span></span> (Exception e) &#123;</span><br><span class="line">        &#125;&#125;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="built_in">onServiceDisconnected</span>(ComponentName name) &#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;, Service.BIND_AUTO_CREATE);   </span><br></pre></td></tr></table></figure>
<p>封装好的或者更详细的代码可以参考：<a href="https://github.com/CCLCM/AndroidSharedMemoryDemo">https://github.com/CCLCM/AndroidSharedMemoryDemo</a><br>本文的重心在于分析原理，而不是使用方法。<br>能看到操作和普通的File操作一模一样，好像根本没有什么区别。File本身也可以作为数据中转站做传递信息。那么MemoryFile比起普通的File优势强在哪里呢？接下来，让我们剖析一下源码，来比较看看匿名内存和File相比有什么区别，和Binder驱动又有什么区别。</p>
<h1 id="MemoryFile"><a href="#MemoryFile" class="headerlink" title="MemoryFile"></a>MemoryFile</h1><p>MemoryFile是android在最开始就引入的一套框架，其内部实际上是封装了android特有的内存共享机制Ashmem匿名共享内存，简单来说Ashmem在Android内核中是被注册成一个特殊的字符设备，Ashmem驱动通过在内核的一个自定义slab缓冲区中初始化一段内存区域，然后通过mmap把申请的内存映射到用户的进程空间中（通过tmpfs），就可以在用户进程中使用这里申请的内存了，另外Ashmem的一个特性就是可以在系统内存不足的时候，回收掉被标记为”unpin”的内存(可自行查看网上资料)，MemoryFile也可以通过Binder跨进程调用来让两个进程共享一段内存区域。由于整个申请内存的过程并不再Java层上，可以很明显的看出使用MemoryFile申请的内存实际上是并不会占用Java堆内存的。</p>
<h2 id="MemoryFile创建"><a href="#MemoryFile创建" class="headerlink" title="MemoryFile创建"></a>MemoryFile创建</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/base/core/java/android/os/MemoryFile.java</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Allocates a new ashmem region. The region is initially not purgable.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> name optional name for the file (can be null).</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> length of the memory file in bytes, must be positive.</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@throws</span> IOException if the memory file could not be created.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">MemoryFile</span><span class="params">(String name, <span class="keyword">int</span> length)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        mSharedMemory = SharedMemory.create(name, length);</span><br><span class="line">        mMapping = mSharedMemory.mapReadWrite();</span><br><span class="line">    &#125; <span class="keyword">catch</span> (ErrnoException ex) &#123;</span><br><span class="line">        ex.rethrowAsIOException();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<h3 id="SharedMemory-create"><a href="#SharedMemory-create" class="headerlink" title="SharedMemory.create"></a>SharedMemory.create</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/base/core/java/android/os/SharedMemory.java</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="meta">@NonNull</span> <span class="function">SharedMemory <span class="title">create</span><span class="params">(<span class="meta">@Nullable</span> String name, <span class="keyword">int</span> size)</span></span></span><br><span class="line"><span class="function">        <span class="keyword">throws</span> ErrnoException </span>&#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> SharedMemory(nCreate(name, size));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>能看到实际上MemoryFile内部有一个核心的类SharedMemory作为核心操作类。我们去看看SharedMemory创建了什么。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="title">SharedMemory</span><span class="params">(FileDescriptor fd)</span> </span>&#123;</span><br><span class="line">   	...</span><br><span class="line">    mFileDescriptor = fd;</span><br><span class="line">    mSize = nGetSize(mFileDescriptor);</span><br><span class="line">    ...</span><br><span class="line">    mMemoryRegistration = <span class="keyword">new</span> MemoryRegistration(mSize);</span><br><span class="line">    mCleaner = Cleaner.create(mFileDescriptor,</span><br><span class="line">            <span class="keyword">new</span> Closer(mFileDescriptor, mMemoryRegistration));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>SharedMemory构造函数做了以下这么几件事：</p>
<ul>
<li><p>通过nCreate在native下创建一个文件描述符，并且关联到到SharedMemory</p>
</li>
<li><p>通过nGetSize获取当前共享内存大小</p>
</li>
<li><p>通过MemoryRegistration把当前大小注册到Java 虚拟机中的native堆栈大小中</p>
</li>
<li><p>初始化Cleaner等到合适的时候通过gc联动Cleaner销毁native下的对象。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/base/core/java/android/os/SharedMemory.java#354</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Helper class that ensures that the native allocation pressure against the VM heap stays</span></span><br><span class="line"><span class="comment"> * active until the FD is closed as well as all mappings from that FD are closed.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="class"><span class="keyword">class</span> <span class="title">MemoryRegistration</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> mSize;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> mReferenceCount;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="title">MemoryRegistration</span><span class="params">(<span class="keyword">int</span> size)</span> </span>&#123;</span><br><span class="line">        mSize = size;</span><br><span class="line">        mReferenceCount = <span class="number">1</span>;</span><br><span class="line">        VMRuntime.getRuntime().registerNativeAllocation(mSize);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">synchronized</span> MemoryRegistration <span class="title">acquire</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        mReferenceCount++;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title">release</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        mReferenceCount--;</span><br><span class="line">        <span class="keyword">if</span> (mReferenceCount == <span class="number">0</span>) &#123;</span><br><span class="line">            VMRuntime.getRuntime().registerNativeFree(mSize);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>MemoryRegistration 本质上就是注册了Java虚拟机中native堆的大小，每一次一个引用都有一次计数，只有减到0才销毁，毕竟这是共享内存，不应该完全由Java虚拟机的GC机制决定。<br>还记得前面我们提到SharedMemory首先通过nCreate在native下创建一个文件描述符。下面我们就来分析native代码都干些什么。</p>
<h4 id="nCreate-name-size"><a href="#nCreate-name-size" class="headerlink" title="nCreate(name, size)"></a>nCreate(name, size)</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/base/core/java/android/os/SharedMemory.java#377</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">native</span> FileDescriptor <span class="title">nCreate</span><span class="params">(String name, <span class="keyword">int</span> size)</span> <span class="keyword">throws</span> ErrnoException</span>;</span><br></pre></td></tr></table></figure>
<p>nCreate在native下创建一个文件描述符。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/base/core/jni/android_os_SharedMemory.cpp#53</span></span><br><span class="line"></span><br><span class="line"><span class="function">jobject <span class="title">SharedMemory_nCreate</span><span class="params">(JNIEnv* env, jobject, jstring jname, jint size)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Name is optional so we can&#x27;t use ScopedUtfChars for this as it throws NPE on null</span></span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span>* name = jname ? env-&gt;<span class="built_in">GetStringUTFChars</span>(jname, <span class="literal">nullptr</span>) : <span class="literal">nullptr</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> fd = <span class="built_in">ashmem_create_region</span>(name, size);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Capture the error, if there is one, before calling ReleaseStringUTFChars</span></span><br><span class="line">    <span class="keyword">int</span> err = fd &lt; <span class="number">0</span> ? errno : <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (name) &#123;</span><br><span class="line">        env-&gt;<span class="built_in">ReleaseStringUTFChars</span>(jname, name);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (fd &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">throwErrnoException</span>(env, <span class="string">&quot;SharedMemory_create&quot;</span>, err);</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nullptr</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">jniCreateFileDescriptor</span>(env, fd);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>通过ashmem_create_region，创建一个共享内存的区域。还记得Linux中那句话，一切皆为文件，实际上匿名共享内存创建出来也是一个文件，不过因为是在tmpfs临时文件系统才叫做匿名的。</p>
<h4 id="ashmem-create-region"><a href="#ashmem-create-region" class="headerlink" title="ashmem_create_region"></a>ashmem_create_region</h4><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/system/core/libcutils/ashmem-dev.cpp#357</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">ashmem_create_region</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *name, <span class="keyword">size_t</span> size)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> ret, save_errno;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (has_memfd_support()) &#123;<span class="comment">//属性sys.use_memfd是否为true</span></span><br><span class="line">        <span class="keyword">return</span> memfd_create_region(name ? name : <span class="string">&quot;none&quot;</span>, size);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> fd = __ashmem_open();</span><br><span class="line">    <span class="keyword">if</span> (fd &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> fd;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (name) &#123;</span><br><span class="line">        <span class="keyword">char</span> buf[ASHMEM_NAME_LEN] = &#123;<span class="number">0</span>&#125;;</span><br><span class="line"></span><br><span class="line">        strlcpy(buf, name, <span class="keyword">sizeof</span>(buf));</span><br><span class="line">        ret = TEMP_FAILURE_RETRY(ioctl(fd, ASHMEM_SET_NAME, buf));</span><br><span class="line">        ...</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    ret = TEMP_FAILURE_RETRY(ioctl(fd, ASHMEM_SET_SIZE, size));</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">return</span> fd;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>创建匿名共享内存分为三个步骤:</p>
</li>
<li><p>__ashmem_open 创建匿名共享内存</p>
</li>
<li><p>通过ioctl 给匿名共享内存命名，只有命名了才能通过命名找到对应的匿名共享内存</p>
</li>
<li><p>ioctl通过ASHMEM_SET_SIZE命令设置匿名共享内存的大小</p>
<h4 id="ashmem-open"><a href="#ashmem-open" class="headerlink" title="__ashmem_open"></a>__ashmem_open</h4><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/system/core/libcutils/ashmem-dev.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">int</span> __ashmem_open()</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">int</span> fd;</span><br><span class="line"></span><br><span class="line">    pthread_mutex_lock(&amp;__ashmem_lock);</span><br><span class="line">    fd = __ashmem_open_locked();</span><br><span class="line">    pthread_mutex_unlock(&amp;__ashmem_lock);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> fd;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* logistics of getting file descriptor for ashmem */</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">int</span> __ashmem_open_locked()</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">const</span> <span class="built_in">std</span>::<span class="built_in">string</span> ashmem_device_path = get_ashmem_device_path();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (ashmem_device_path.empty()) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> fd = TEMP_FAILURE_RETRY(open(ashmem_device_path.c_str(), O_RDWR | O_CLOEXEC));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// fallback for APEX w/ use_vendor on Q, which would have still used /dev/ashmem</span></span><br><span class="line">    <span class="keyword">if</span> (fd &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        fd = TEMP_FAILURE_RETRY(open(<span class="string">&quot;/dev/ashmem&quot;</span>, O_RDWR | O_CLOEXEC));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (fd &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> fd;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">stat</span> <span class="title">st</span>;</span></span><br><span class="line">    <span class="keyword">int</span> ret = TEMP_FAILURE_RETRY(fstat(fd, &amp;st));</span><br><span class="line">    <span class="keyword">if</span> (ret &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">int</span> save_errno = errno;</span><br><span class="line">        close(fd);</span><br><span class="line">        errno = save_errno;</span><br><span class="line">        <span class="keyword">return</span> ret;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (!S_ISCHR(st.st_mode) || !st.st_rdev) &#123;</span><br><span class="line">        close(fd);</span><br><span class="line">        errno = ENOTTY;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    __ashmem_rdev = st.st_rdev;</span><br><span class="line">    <span class="keyword">return</span> fd;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>终于看到了，类似于Binder驱动的打开方式一样，通过/dev/ashmem的方式访问ashmem驱动的file_operation的open方法，最后获得对应的文件描述符fd。<br>驱动相关的内容我们放到最后一节来分析。</p>
</li>
</ul>
<h3 id="SharedMemory-mapReadWrite"><a href="#SharedMemory-mapReadWrite" class="headerlink" title="SharedMemory.mapReadWrite"></a>SharedMemory.mapReadWrite</h3><p>ShareMemory当创建好ashmem匿名共享内存之后，将会调用mapReadWrite。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/base/core/java/android/os/SharedMemory.java</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="meta">@NonNull</span> <span class="function">ByteBuffer <span class="title">mapReadWrite</span><span class="params">()</span> <span class="keyword">throws</span> ErrnoException </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> map(OsConstants.PROT_READ | OsConstants.PROT_WRITE, <span class="number">0</span>, mSize);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"> <span class="keyword">public</span> <span class="meta">@NonNull</span> <span class="function">ByteBuffer <span class="title">map</span><span class="params">(<span class="keyword">int</span> prot, <span class="keyword">int</span> offset, <span class="keyword">int</span> length)</span> <span class="keyword">throws</span> ErrnoException </span>&#123;</span><br><span class="line">    checkOpen();</span><br><span class="line">    validateProt(prot);</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">long</span> address = Os.mmap(<span class="number">0</span>, length, prot, OsConstants.MAP_SHARED, mFileDescriptor, offset);</span><br><span class="line">    <span class="keyword">boolean</span> readOnly = (prot &amp; OsConstants.PROT_WRITE) == <span class="number">0</span>;</span><br><span class="line">    Runnable unmapper = <span class="keyword">new</span> Unmapper(address, length, mMemoryRegistration.acquire());</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> DirectByteBuffer(length, address, mFileDescriptor, unmapper, readOnly);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>map调用 Os.mmap。其实这个方法的本质就是调用系统调用mmap。这里面的意思就是调用Ashmem对应的文件描述符mmap方法，也就是下午在讲驱动的时候提到的ashmem_mmap。通过这种常规的mmap，让用户态的虚拟内存直接和物理内存映射起来，就能通过0次拷贝的方式映射起来。</p>
</li>
<li><p>直接映射一段逻辑上的虚拟内存和文件file关联起来。当系统正式访问这一段虚拟内存，如果找不到就会触发缺页中断(或者尝试的从磁盘执行物理页的换入换出)，此时就会把这一段逻辑绑定的虚拟内存和file正式映射到物理内存。</p>
<h4 id="Os-mmap"><a href="#Os-mmap" class="headerlink" title="Os.mmap"></a>Os.mmap</h4><p>这个Os.mmap就比较有意思了，特别是对于没怎么接触过Android源码的同学看到这个，就很难找到原函数在哪个地方。那么这一小节我们就简单聊一聊看到一个陌生的方法，如何找到其原函数的。</p>
</li>
<li><p>找到这个类的位置</p>
</li>
</ul>
<p><img src="https://cdn.nlark.com/yuque/0/2021/png/1759879/1631939050162-56af4a9a-5f00-46fa-b3ae-129a6e9b0c31.png#clientId=ua831b86f-6d5e-4&from=paste&height=361&id=u939ba81b&margin=%5Bobject%20Object%5D&name=image.png&originHeight=721&originWidth=1888&originalType=binary&ratio=1&size=147849&status=done&style=none&taskId=u7a644786-e66b-46a6-acea-ea2ee662104&width=944" alt="image.png"></p>
<ul>
<li><p>因为我们不知道是在哪里，所以选择了所有文件夹都搜索。但是熟悉Android源码的同学都知道，基本上常用到的java代码都是在framework、system、libcore等目录下。</p>
</li>
<li><p>File Path输入：Os.java。搜索出来有非常多并且是一些“乱七八糟”的类。那我们换一个思路，既然是File Path那我们把包名也加上好了。如android/system/Os.java。好的，现在搜索结果只有两个，那么这个结果显然就是<a href="http://aospxref.com/android-11.0.0_r21/xref/libcore/luni/src/main/java/android/system/">/libcore/luni/src/main/java/android/system/</a><a href="http://aospxref.com/android-11.0.0_r21/xref/libcore/luni/src/main/java/android/system/Os.java">Os.java</a>。这个也跟我们之前猜测的目录相符合。</p>
</li>
<li><p>找到类里实现的方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/libcore/luni/src/main/java/android/system/Os.java#392</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">  * See &lt;a href=&quot;http://man7.org/linux/man-pages/man2/mmap.2.html&quot;&gt;mmap(2)&lt;/a&gt;.</span></span><br><span class="line"><span class="comment">  */</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">long</span> <span class="title">mmap</span><span class="params">(<span class="keyword">long</span> address, <span class="keyword">long</span> byteCount, <span class="keyword">int</span> prot, <span class="keyword">int</span> flags, FileDescriptor fd, <span class="keyword">long</span> offset)</span> <span class="keyword">throws</span> ErrnoException </span>&#123; <span class="keyword">return</span> Libcore.os.mmap(address, byteCount, prot, flags, fd, offset); &#125;</span><br></pre></td></tr></table></figure>
<p>好家伙，这里又调 <a href="http://aospxref.com/android-11.0.0_r21/s?defs=Libcore&project=libcore">Libcore</a>.<a href="http://aospxref.com/android-11.0.0_r21/s?defs=os&project=libcore">os</a>.<a href="http://aospxref.com/android-11.0.0_r21/xref/libcore/luni/src/main/java/android/system/Os.java#mmap">mmap</a>。那么现在我们就不停的重复上面两个步骤。</p>
</li>
<li><p>Libcore</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/libcore/luni/src/main/java/libcore/io/Libcore.java</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> Os rawOs = <span class="keyword">new</span> Linux();</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">volatile</span> Os os = <span class="keyword">new</span> BlockGuardOs(rawOs);</span><br></pre></td></tr></table></figure></li>
<li><p>BlockGuardOs</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/libcore/luni/src/main/java/libcore/io/BlockGuardOs.java#49</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">BlockGuardOs</span> <span class="keyword">extends</span> <span class="title">ForwardingOs</span> </span>&#123;</span><br><span class="line">    <span class="meta">@UnsupportedAppUsage</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">BlockGuardOs</span><span class="params">(Os os)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">super</span>(os);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>ForwardingOs</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ForwardingOs</span> <span class="keyword">implements</span> <span class="title">Os</span> </span>&#123;</span><br><span class="line">    <span class="meta">@UnsupportedAppUsage</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> Os os;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@UnsupportedAppUsage</span></span><br><span class="line">    <span class="meta">@libcore</span>.api.<span class="function">CorePlatformApi</span></span><br><span class="line"><span class="function">    <span class="keyword">protected</span> <span class="title">ForwardingOs</span><span class="params">(Os os)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.os = Objects.requireNonNull(os);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">long</span> <span class="title">mmap</span><span class="params">(<span class="keyword">long</span> address, <span class="keyword">long</span> byteCount, <span class="keyword">int</span> prot, <span class="keyword">int</span> flags, FileDescriptor fd, <span class="keyword">long</span> offset)</span> <span class="keyword">throws</span> ErrnoException </span>&#123; <span class="keyword">return</span> os.mmap(address, byteCount, prot, flags, fd, offset); &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>Os</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/libcore/luni/src/main/java/libcore/io/Os.java#132</span></span><br><span class="line"></span><br><span class="line"><span class="meta">@UnsupportedAppUsage</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">long</span> <span class="title">mmap</span><span class="params">(<span class="keyword">long</span> address, <span class="keyword">long</span> byteCount, <span class="keyword">int</span> prot, <span class="keyword">int</span> flags, FileDescriptor fd, <span class="keyword">long</span> offset)</span> <span class="keyword">throws</span> ErrnoException</span>;</span><br></pre></td></tr></table></figure>
<p>看到这发现我们走远了，其实真正调用的是Libcore中的Linux。</p>
</li>
<li><p>Linux</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/libcore/luni/src/main/java/libcore/io/Linux.java#129</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">native</span> <span class="keyword">long</span> <span class="title">mmap</span><span class="params">(<span class="keyword">long</span> address, <span class="keyword">long</span> byteCount, <span class="keyword">int</span> prot, <span class="keyword">int</span> flags, FileDescriptor fd, <span class="keyword">long</span> offset)</span> <span class="keyword">throws</span> ErrnoException</span>;</span><br></pre></td></tr></table></figure>
<p>好，发现调用native。不要慌，其实写过JNI的同学都知道，全局搜索一下看JNINativeMethod里java的mmap方法对应cpp的哪个方法就好了。<br>当然如果现在直接在源码下这么搜索，出来的结果可想而知，我们换一种更简单的方式。</p>
<h5 id="查找native方法"><a href="#查找native方法" class="headerlink" title="查找native方法"></a>查找native方法</h5><p><a href="http://gityuan.com/2016/05/28/android-jni/">参考gityuan的Android JNI原理分析</a></p>
</li>
<li><p>找到文件</p>
</li>
</ul>
<p>Linux.java的包是libcore.io，那么我们写成libcore_io_Linux.c或者libcore_io_Linux.cpp在搜索，发现是可以搜到libcore_io_Linux.cpp的。并且也是libcore目录下，那看到是它没错了。</p>
<ul>
<li>找到方法名<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/libcore/luni/src/main/native/libcore_io_Linux.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> JNINativeMethod gMethods[] = &#123;</span><br><span class="line">    <span class="built_in">NATIVE_METHOD</span>(Linux, mmap, <span class="string">&quot;(JJIILjava/io/FileDescriptor;J)J&quot;</span>),</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">jniRegisterNativeMethods</span>(env, <span class="string">&quot;libcore/io/Linux&quot;</span>, gMethods, <span class="built_in">NELEM</span>(gMethods));</span><br></pre></td></tr></table></figure>
看到了<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/libcore/luni/src/main/native/libcore_io_Linux.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> jlong <span class="title">Linux_mmap</span><span class="params">(JNIEnv* env, jobject, jlong address, jlong byteCount, jint prot, jint flags, jobject javaFd, jlong offset)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> fd = <span class="built_in">jniGetFDFromFileDescriptor</span>(env, javaFd);</span><br><span class="line">    <span class="keyword">void</span>* suggestedPtr = <span class="keyword">reinterpret_cast</span>&lt;<span class="keyword">void</span>*&gt;(<span class="keyword">static_cast</span>&lt;<span class="keyword">uintptr_t</span>&gt;(address));</span><br><span class="line">    <span class="keyword">void</span>* ptr = <span class="built_in">mmap64</span>(suggestedPtr, byteCount, prot, flags, fd, offset);</span><br><span class="line">    <span class="keyword">if</span> (ptr == MAP_FAILED) &#123;</span><br><span class="line">        <span class="built_in">throwErrnoException</span>(env, <span class="string">&quot;mmap&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">static_cast</span>&lt;jlong&gt;(<span class="keyword">reinterpret_cast</span>&lt;<span class="keyword">uintptr_t</span>&gt;(ptr));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
到这里我们就可以明确Os.mmap最终就是调用系统调用mmap。<br>那么我们现在也知道了如何通过OpenGrok搜索我们不熟悉的java类、java函数、native方法等等。<h4 id="Unmapper"><a href="#Unmapper" class="headerlink" title="Unmapper"></a>Unmapper</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/base/core/java/android/os/SharedMemory.java</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="class"><span class="keyword">class</span> <span class="title">Unmapper</span> <span class="keyword">implements</span> <span class="title">Runnable</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">long</span> mAddress;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> mSize;</span><br><span class="line">    <span class="keyword">private</span> MemoryRegistration mMemoryReference;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="title">Unmapper</span><span class="params">(<span class="keyword">long</span> address, <span class="keyword">int</span> size, MemoryRegistration memoryReference)</span> </span>&#123;</span><br><span class="line">        mAddress = address;</span><br><span class="line">        mSize = size;</span><br><span class="line">        mMemoryReference = memoryReference;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Os.munmap(mAddress, mSize);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (ErrnoException e) &#123; <span class="comment">/* swallow exception */</span> &#125;</span><br><span class="line">        mMemoryReference.release();</span><br><span class="line">        mMemoryReference = <span class="keyword">null</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
在DirectByteBuffer设置解开映射的回调Unmapper，如果通过mMemoryRegistration察觉到引用计数为0，就会调用munmap解映射。因此我们可以推敲出，MemoryFile将会以mapReadWrite产生出来的mMapping为基准，不断的从这一段虚拟内存读写。接下来让我们来看看MemoryFile的读写方法。</li>
</ul>
<h2 id="MemoryFile写数据"><a href="#MemoryFile写数据" class="headerlink" title="MemoryFile写数据"></a>MemoryFile写数据</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/base/core/java/android/os/MemoryFile.java</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="class"><span class="keyword">class</span> <span class="title">MemoryOutputStream</span> <span class="keyword">extends</span> <span class="title">OutputStream</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> mOffset = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">byte</span>[] mSingleByte;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">write</span><span class="params">(<span class="keyword">byte</span> buffer[], <span class="keyword">int</span> offset, <span class="keyword">int</span> count)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        writeBytes(buffer, offset, mOffset, count);</span><br><span class="line">        mOffset += count;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">write</span><span class="params">(<span class="keyword">int</span> oneByte)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (mSingleByte == <span class="keyword">null</span>) &#123;</span><br><span class="line">            mSingleByte = <span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">1</span>];</span><br><span class="line">        &#125;</span><br><span class="line">        mSingleByte[<span class="number">0</span>] = (<span class="keyword">byte</span>)oneByte;</span><br><span class="line">        write(mSingleByte, <span class="number">0</span>, <span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>写入操作能看到就是获取MemoryFile的OutputStream对象进行操作。能看到在write方法中，本质上还是调用writeBytes作为核心写入方法。</p>
<h3 id="writeBytes"><a href="#writeBytes" class="headerlink" title="writeBytes"></a>writeBytes</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/base/core/java/android/os/MemoryFile.java</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">writeBytes</span><span class="params">(<span class="keyword">byte</span>[] buffer, <span class="keyword">int</span> srcOffset, <span class="keyword">int</span> destOffset, <span class="keyword">int</span> count)</span></span></span><br><span class="line"><span class="function">            <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">    beginAccess();</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        mMapping.position(destOffset);</span><br><span class="line">        mMapping.put(buffer, srcOffset, count);</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        endAccess();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">beginAccess</span><span class="params">()</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">    checkActive();</span><br><span class="line">    <span class="keyword">if</span> (mAllowPurging) &#123;</span><br><span class="line">        <span class="keyword">if</span> (native_pin(mSharedMemory.getFileDescriptor(), <span class="keyword">true</span>)) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> IOException(<span class="string">&quot;MemoryFile has been purged&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>native_pin</li>
</ul>
<p>调用native_pin进行锁定这一块大小的虚拟内存，避免被系统回收。</p>
<ul>
<li>mMapping.position</li>
</ul>
<p>记录写完后的位置。</p>
<ul>
<li>mMapping.put</li>
</ul>
<p>把buffer数据写入到mMapping中。</p>
<h3 id="DirectByteBuffer-put"><a href="#DirectByteBuffer-put" class="headerlink" title="DirectByteBuffer.put"></a>DirectByteBuffer.put</h3><p>这里稍微不注意就会出错，从代码里看到：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/base/core/java/android/os/MemoryFile.java</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> ByteBuffer mMapping;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>但是大家还记得赋值的地方吗，也就是：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/base/core/java/android/os/MemoryFile.java</span></span><br><span class="line"></span><br><span class="line">mMapping = mSharedMemory.mapReadWrite();</span><br></pre></td></tr></table></figure>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/base/core/java/android/os/SharedMemory.java#map</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="meta">@NonNull</span> <span class="function">ByteBuffer <span class="title">mapReadWrite</span><span class="params">()</span> <span class="keyword">throws</span> ErrnoException </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> map(OsConstants.PROT_READ | OsConstants.PROT_WRITE, <span class="number">0</span>, mSize);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="meta">@NonNull</span> <span class="function">ByteBuffer <span class="title">map</span><span class="params">(<span class="keyword">int</span> prot, <span class="keyword">int</span> offset, <span class="keyword">int</span> length)</span> <span class="keyword">throws</span> ErrnoException </span>&#123;</span><br><span class="line">   	...</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> DirectByteBuffer(length, address, mFileDescriptor, unmapper, readOnly);</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>好家伙，看到了吧，最终是DirectByteBuffer。<br>所以现在我们有一个问题，什么不直接用我们常见的write函数而是改用DirectByteBuffer.put呢，这其中有什么深意吗？</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/libcore/ojluni/src/main/java/java/nio/DirectByteBuffer.java#285</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> ByteBuffer <span class="title">put</span><span class="params">(<span class="keyword">byte</span>[] src, <span class="keyword">int</span> srcOffset, <span class="keyword">int</span> length)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!memoryRef.isAccessible) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> IllegalStateException(<span class="string">&quot;buffer is inaccessible&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (isReadOnly) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> ReadOnlyBufferException();</span><br><span class="line">    &#125;</span><br><span class="line">    checkBounds(srcOffset, length, src.length);</span><br><span class="line">    <span class="keyword">int</span> pos = position();</span><br><span class="line">    <span class="keyword">int</span> lim = limit();</span><br><span class="line">    <span class="keyword">assert</span> (pos &lt;= lim);</span><br><span class="line">    <span class="keyword">int</span> rem = (pos &lt;= lim ? lim - pos : <span class="number">0</span>);</span><br><span class="line">    <span class="keyword">if</span> (length &gt; rem)</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> BufferOverflowException();</span><br><span class="line">    Memory.pokeByteArray(ix(pos),</span><br><span class="line">            src, srcOffset, length);</span><br><span class="line">    position = pos + length;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>根据设置进来的position设定已经写入了多少数据，从哪里开始写入。接着会通过传进来的数据长度以及要写入的偏移量来确定要写入哪一块内存。</li>
<li>调用Memory.pokeByteArray方法，把内容写到虚拟地址偏移量的起点到数据长度结束中，也就是写入到对应位置的物理页中。</li>
</ul>
<p>那么现在能理解为什么不用write而是用DirectByteBuffer.put了，因为mmap的核心原理就是把物理页和虚拟内存页映射起来。<br>我们接着看Memory.pokeByteArray，看看是不是我们猜想的那样。</p>
<h3 id="Memory-pokeByteArray"><a href="#Memory-pokeByteArray" class="headerlink" title="Memory.pokeByteArray"></a>Memory.pokeByteArray</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/libcore/luni/src/main/java/libcore/io/Memory.java#242</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">native</span> <span class="keyword">void</span> <span class="title">pokeByteArray</span><span class="params">(<span class="keyword">long</span> address, <span class="keyword">byte</span>[] src, <span class="keyword">int</span> offset, <span class="keyword">int</span> count)</span></span>;</span><br></pre></td></tr></table></figure>
<p>native方法，那么还是像上文提到的方法一样找到其真正的native方法。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/libcore/luni/src/main/native/libcore_io_Memory.cpp#111</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">Memory_peekByteArray</span><span class="params">(JNIEnv* env, jclass, jlong srcAddress, jbyteArray dst, jint dstOffset, jint byteCount)</span> </span>&#123;</span><br><span class="line">    env-&gt;SetByteArrayRegion(dst, dstOffset, byteCount, cast&lt;<span class="keyword">const</span> jbyte*&gt;(srcAddress));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="https://cdn.nlark.com/yuque/0/2021/png/1759879/1631944067239-ab3e365b-38eb-4bee-a895-abb2e2b3a505.png#clientId=ua831b86f-6d5e-4&from=paste&id=u1a9f819c&margin=%5Bobject%20Object%5D&name=image.png&originHeight=234&originWidth=561&originalType=url&ratio=1&size=18222&status=done&style=none&taskId=u5f2c0676-fac7-4cbb-afe3-7cd1c3aced8" alt="image.png"></p>
<h2 id="MemoryFile读数据"><a href="#MemoryFile读数据" class="headerlink" title="MemoryFile读数据"></a>MemoryFile读数据</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/base/core/java/android/os/MemoryFile.java#247</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="class"><span class="keyword">class</span> <span class="title">MemoryInputStream</span> <span class="keyword">extends</span> <span class="title">InputStream</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> mMark = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> mOffset = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">byte</span>[] mSingleByte;</span><br><span class="line"></span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">read</span><span class="params">()</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (mSingleByte == <span class="keyword">null</span>) &#123;</span><br><span class="line">            mSingleByte = <span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">1</span>];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">int</span> result = read(mSingleByte, <span class="number">0</span>, <span class="number">1</span>);</span><br><span class="line">        <span class="keyword">if</span> (result != <span class="number">1</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> -<span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> mSingleByte[<span class="number">0</span>];</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">read</span><span class="params">(<span class="keyword">byte</span> buffer[], <span class="keyword">int</span> offset, <span class="keyword">int</span> count)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (offset &lt; <span class="number">0</span> || count &lt; <span class="number">0</span> || offset + count &gt; buffer.length) &#123;</span><br><span class="line">            <span class="comment">// readBytes() also does this check, but we need to do it before</span></span><br><span class="line">            <span class="comment">// changing count.</span></span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> IndexOutOfBoundsException();</span><br><span class="line">        &#125;</span><br><span class="line">        count = Math.min(count, available());</span><br><span class="line">        <span class="keyword">if</span> (count &lt; <span class="number">1</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> -<span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">int</span> result = readBytes(buffer, mOffset, offset, count);</span><br><span class="line">        <span class="keyword">if</span> (result &gt; <span class="number">0</span>) &#123;</span><br><span class="line">            mOffset += result;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> result;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>写入操作能看到就是获取MemoryFile的InputStream对象进行操作。能看到在read方法中，本质上还是调用readBytes作为核心写入方法。</p>
<h3 id="readBytes"><a href="#readBytes" class="headerlink" title="readBytes"></a>readBytes</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/base/core/java/android/os/MemoryFile.java</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">readBytes</span><span class="params">(<span class="keyword">byte</span>[] buffer, <span class="keyword">int</span> srcOffset, <span class="keyword">int</span> destOffset, <span class="keyword">int</span> count)</span></span></span><br><span class="line"><span class="function">            <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">    beginAccess();</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        mMapping.position(srcOffset);</span><br><span class="line">        mMapping.get(buffer, destOffset, count);</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        endAccess();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> count;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>native_pin</li>
</ul>
<p>调用native_pin进行锁定这一块大小的虚拟内存，避免被系统回收。</p>
<ul>
<li>mMapping.position</li>
</ul>
<p>记录位置。</p>
<ul>
<li>mMapping.get</li>
</ul>
<p>把buffer数据读到mMapping中。</p>
<h3 id="DirectByteBuffer-get"><a href="#DirectByteBuffer-get" class="headerlink" title="DirectByteBuffer.get"></a>DirectByteBuffer.get</h3><p>在DirectByteBuffer.put已经分析过，所以我们直接看原函数：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/libcore/ojluni/src/main/java/java/nio/DirectByteBuffer.java#229</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> ByteBuffer <span class="title">get</span><span class="params">(<span class="keyword">byte</span>[] dst, <span class="keyword">int</span> dstOffset, <span class="keyword">int</span> length)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!memoryRef.isAccessible) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> IllegalStateException(<span class="string">&quot;buffer is inaccessible&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    checkBounds(dstOffset, length, dst.length);</span><br><span class="line">    <span class="keyword">int</span> pos = position();</span><br><span class="line">    <span class="keyword">int</span> lim = limit();</span><br><span class="line">    <span class="keyword">assert</span> (pos &lt;= lim);</span><br><span class="line">    <span class="keyword">int</span> rem = (pos &lt;= lim ? lim - pos : <span class="number">0</span>);</span><br><span class="line">    <span class="keyword">if</span> (length &gt; rem)</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> BufferUnderflowException();</span><br><span class="line">    Memory.peekByteArray(ix(pos),</span><br><span class="line">            dst, dstOffset, length);</span><br><span class="line">    position = pos + length;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>一样也是获取当前已经写入的位置，从该位置+偏移量作为读取数据的起点，读取数据的长度即为所得。</p>
<h3 id="Memory-peekByteArray"><a href="#Memory-peekByteArray" class="headerlink" title="Memory.peekByteArray"></a>Memory.peekByteArray</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/libcore/luni/src/main/native/libcore_io_Memory.cpp#111</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">Memory_peekByteArray</span><span class="params">(JNIEnv* env, jclass, jlong srcAddress, jbyteArray dst, jint dstOffset, jint byteCount)</span> </span>&#123;</span><br><span class="line">    env-&gt;SetByteArrayRegion(dst, dstOffset, byteCount, cast&lt;<span class="keyword">const</span> jbyte*&gt;(srcAddress));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>能看到此时就是获取目标区域内存的数据，设置到srcAddress中。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>Ashmem匿名共享内存使用的步骤可以分以下几步：</p>
<ul>
<li>open ashmem驱动</li>
<li>ioctl 发送ASHMEM_SET_NAME命令为该ashmem创建名字</li>
<li>ioctl 发送ASHMEM_SET_SIZE命令为ashmem设置大小</li>
<li>mmap</li>
<li>对该文件描述符进行读写</li>
</ul>
<h1 id="Ashmem驱动"><a href="#Ashmem驱动" class="headerlink" title="Ashmem驱动"></a>Ashmem驱动</h1><p>本人没做过驱动，所有的驱动知识都是通过网上学习或者RTFSC，有讲错之处请指点。也望驱动大佬多多海涵。<br>ASHMEM_DEVICE其实就是抽象的共享内存设备，它是一个杂项设备（字符设备的一种），在驱动加载之后，就会在/dev下穿件ashem文件，之后用户就能够访问该设备文件，同一般的设备文件不同，它仅仅是通过内存抽象的，同普通的磁盘设备文件、串行端口字段设备文件不一样：</p>
<blockquote>
<p>aospxref网站没有收录kernel代码，googlesource需要自行科学上网。<br><a href="https://android.googlesource.com/kernel">https://android.googlesource.com/kernel</a></p>
</blockquote>
<h2 id="初始化（ashmem-init）"><a href="#初始化（ashmem-init）" class="headerlink" title="初始化（ashmem_init）"></a>初始化（ashmem_init）</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">https:<span class="comment">//android.googlesource.com/kernel/common/+/refs/tags/5.4-android11-0/drivers/staging/android/ashmem.c</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> __init <span class="title">ashmem_init</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> ret = -ENOMEM;</span><br><span class="line"></span><br><span class="line">	ashmem_area_cachep = kmem_cache_create(<span class="string">&quot;ashmem_area_cache&quot;</span>,</span><br><span class="line">					       <span class="keyword">sizeof</span>(struct ashmem_area),</span><br><span class="line">					       <span class="number">0</span>, <span class="number">0</span>, <span class="literal">NULL</span>);</span><br><span class="line">	<span class="keyword">if</span> (!ashmem_area_cachep) &#123;</span><br><span class="line">		pr_err(<span class="string">&quot;failed to create slab cache\n&quot;</span>);</span><br><span class="line">		<span class="keyword">goto</span> out;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	ashmem_range_cachep = kmem_cache_create(<span class="string">&quot;ashmem_range_cache&quot;</span>,</span><br><span class="line">						<span class="keyword">sizeof</span>(struct ashmem_range),</span><br><span class="line">						<span class="number">0</span>, <span class="number">0</span>, <span class="literal">NULL</span>);</span><br><span class="line">	<span class="keyword">if</span> (!ashmem_range_cachep) &#123;</span><br><span class="line">		pr_err(<span class="string">&quot;failed to create slab cache\n&quot;</span>);</span><br><span class="line">		<span class="keyword">goto</span> out_free1;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	ret = misc_register(&amp;ashmem_misc);</span><br><span class="line">	<span class="keyword">if</span> (ret) &#123;</span><br><span class="line">		pr_err(<span class="string">&quot;failed to register misc device!\n&quot;</span>);</span><br><span class="line">		<span class="keyword">goto</span> out_free2;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	ret = register_shrinker(&amp;ashmem_shrinker);</span><br><span class="line">	<span class="keyword">if</span> (ret) &#123;</span><br><span class="line">		pr_err(<span class="string">&quot;failed to register shrinker!\n&quot;</span>);</span><br><span class="line">		<span class="keyword">goto</span> out_demisc;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	pr_info(<span class="string">&quot;initialized\n&quot;</span>);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">out_demisc:</span><br><span class="line">	misc_deregister(&amp;ashmem_misc);</span><br><span class="line">out_free2:</span><br><span class="line">	kmem_cache_destroy(ashmem_range_cachep);</span><br><span class="line">out_free1:</span><br><span class="line">	kmem_cache_destroy(ashmem_area_cachep);</span><br><span class="line">out:</span><br><span class="line">	<span class="keyword">return</span> ret;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">device_initcall(ashmem_init);</span><br><span class="line"></span><br><span class="line"></span><br></pre></td></tr></table></figure>

<ul>
<li>在slab高速缓存开辟了ashmem_area</li>
<li>ashmem_range两个结构体的cache</li>
<li>通过register_shrinker向内存管理系统注册Ashmem回收函数<blockquote>
<p>如果Linux内存管理slab是什么可以浏览以下文章：</p>
</blockquote>
</li>
</ul>
<blockquote>
<p><a href="https://zhuanlan.zhihu.com/p/105582468">Linux中的Slab</a><br><a href="https://blog.csdn.net/rockrockwu/article/details/79976833">Linux内存管理之SLAB原理浅析</a></p>
</blockquote>
<h2 id="file-operations"><a href="#file-operations" class="headerlink" title="file_operations"></a>file_operations</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">https:<span class="comment">//android.googlesource.com/kernel/common/+/refs/tags/5.4-android11-0/drivers/staging/android/ashmem.c</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="class"><span class="keyword">struct</span> <span class="title">miscdevice</span> <span class="title">ashmem_misc</span> =</span> &#123;</span><br><span class="line">	.minor = MISC_DYNAMIC_MINOR,</span><br><span class="line">	.name = <span class="string">&quot;ashmem&quot;</span>,</span><br><span class="line">	.fops = &amp;ashmem_fops,</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">const</span> <span class="class"><span class="keyword">struct</span> <span class="title">file_operations</span> <span class="title">ashmem_fops</span> =</span> &#123;</span><br><span class="line">	.owner = THIS_MODULE,</span><br><span class="line">	.open = ashmem_open,</span><br><span class="line">	.release = ashmem_release,</span><br><span class="line">	.read_iter = ashmem_read_iter,</span><br><span class="line">	.llseek = ashmem_llseek,</span><br><span class="line">	.mmap = ashmem_mmap,</span><br><span class="line">	.unlocked_ioctl = ashmem_ioctl,</span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifdef</span> CONFIG_COMPAT</span></span><br><span class="line">	.compat_ioctl = compat_ashmem_ioctl,</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifdef</span> CONFIG_PROC_FS</span></span><br><span class="line">	.show_fdinfo = ashmem_show_fdinfo,</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>结构体file_operations在头文件 linux/fs.h中定义，用来存储驱动内核模块提供的对设备进行各种操作的函数的指针。<br>能看到里面有open，read_iter，mmap，unlocked_ioctl这四个核心的方法。</p>
<h2 id="ashmem-open-1"><a href="#ashmem-open-1" class="headerlink" title="ashmem_open"></a>ashmem_open</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">https:<span class="comment">//android.googlesource.com/kernel/common/+/refs/tags/5.4-android11-0/drivers/staging/android/ashmem.c</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">ashmem_open</span><span class="params">(struct inode *inode, struct file *file)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">ashmem_area</span> *<span class="title">asma</span>;</span></span><br><span class="line">	<span class="keyword">int</span> ret;</span><br><span class="line"></span><br><span class="line">	ret = generic_file_open(inode, file);</span><br><span class="line">	<span class="keyword">if</span> (ret)</span><br><span class="line">		<span class="keyword">return</span> ret;</span><br><span class="line"></span><br><span class="line">	asma = kmem_cache_zalloc(ashmem_area_cachep, GFP_KERNEL);</span><br><span class="line">	<span class="keyword">if</span> (!asma)</span><br><span class="line">		<span class="keyword">return</span> -ENOMEM;</span><br><span class="line"></span><br><span class="line">	INIT_LIST_HEAD(&amp;asma-&gt;unpinned_list);</span><br><span class="line">	<span class="built_in">memcpy</span>(asma-&gt;name, ASHMEM_NAME_PREFIX, ASHMEM_NAME_PREFIX_LEN);</span><br><span class="line">	asma-&gt;prot_mask = PROT_MASK;</span><br><span class="line">	file-&gt;private_data = asma;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>open函数很普通，创建一块ashmem共享内存时，其实是在内核层打开了一个ashmem file，而且这个file的private_data里记录了一块ashmem_area。</p>
<h2 id="ashmem-ioctl"><a href="#ashmem-ioctl" class="headerlink" title="ashmem_ioctl"></a>ashmem_ioctl</h2><p>ashmem_open()之后，紧接着要设置刚打开的共享内存文件的一些属性，于是调用到ioctl()对应的ashmem_ioctl()。主要的设置动作其实就是向ashmem_area里写入一些数据。ashmem_ioctl()函数的定义如下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">https:<span class="comment">//android.googlesource.com/kernel/common/+/refs/tags/5.4-android11-0/drivers/staging/android/ashmem.c</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">long</span> <span class="title">ashmem_ioctl</span><span class="params">(struct file *file, <span class="keyword">unsigned</span> <span class="keyword">int</span> cmd, <span class="keyword">unsigned</span> <span class="keyword">long</span> arg)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">ashmem_area</span> *<span class="title">asma</span> =</span> file-&gt;private_data;</span><br><span class="line">	<span class="keyword">long</span> ret = -ENOTTY;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">switch</span> (cmd) &#123;</span><br><span class="line">	...</span><br><span class="line">	<span class="keyword">case</span> ASHMEM_SET_SIZE:</span><br><span class="line">		ret = -EINVAL;</span><br><span class="line">		mutex_lock(&amp;ashmem_mutex);</span><br><span class="line">		<span class="keyword">if</span> (!asma-&gt;file) &#123;</span><br><span class="line">			ret = <span class="number">0</span>;</span><br><span class="line">			asma-&gt;size = (<span class="keyword">size_t</span>)arg;</span><br><span class="line">		&#125;</span><br><span class="line">		mutex_unlock(&amp;ashmem_mutex);</span><br><span class="line">		<span class="keyword">break</span>;</span><br><span class="line">	...</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> ret;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>设置共享内存的大小。可以看到，其实并未真正的分配内存，这也符合Linux的风格，只有等到真正的使用的时候，才会通过缺页中断分配内存。</p>
<h2 id="ashmem-mmap"><a href="#ashmem-mmap" class="headerlink" title="ashmem_mmap"></a>ashmem_mmap</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">https:<span class="comment">//android.googlesource.com/kernel/common/+/refs/tags/5.4-android11-0/drivers/staging/android/ashmem.c</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">ashmem_mmap</span><span class="params">(struct file *file, struct vm_area_struct *vma)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">static</span> <span class="class"><span class="keyword">struct</span> <span class="title">file_operations</span> <span class="title">vmfile_fops</span>;</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">ashmem_area</span> *<span class="title">asma</span> =</span> file-&gt;private_data;</span><br><span class="line">	<span class="keyword">int</span> ret = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">	mutex_lock(&amp;ashmem_mutex);</span><br><span class="line"></span><br><span class="line">	<span class="comment">/* user needs to SET_SIZE before mapping */</span></span><br><span class="line">	<span class="comment">//1. 设置asma的size，不然会抛异常。</span></span><br><span class="line">	<span class="keyword">if</span> (!asma-&gt;size) &#123;</span><br><span class="line">		ret = -EINVAL;</span><br><span class="line">		<span class="keyword">goto</span> out;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">/* requested mapping size larger than object size */</span></span><br><span class="line">	<span class="comment">//2. 检测需要映射的vma虚拟内存是否符合权限，否则抛异常。</span></span><br><span class="line">	<span class="keyword">if</span> (vma-&gt;vm_end - vma-&gt;vm_start &gt; PAGE_ALIGN(asma-&gt;size)) &#123;</span><br><span class="line">		ret = -EINVAL;</span><br><span class="line">		<span class="keyword">goto</span> out;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">/* requested protection bits must match our allowed protection mask */</span></span><br><span class="line">	<span class="keyword">if</span> ((vma-&gt;vm_flags &amp; ~calc_vm_prot_bits(asma-&gt;prot_mask, <span class="number">0</span>)) &amp;</span><br><span class="line">	    calc_vm_prot_bits(PROT_MASK, <span class="number">0</span>)) &#123;</span><br><span class="line">		ret = -EPERM;</span><br><span class="line">		<span class="keyword">goto</span> out;</span><br><span class="line">	&#125;</span><br><span class="line">	vma-&gt;vm_flags &amp;= ~calc_vm_may_flags(~asma-&gt;prot_mask);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (!asma-&gt;file) &#123;</span><br><span class="line">		<span class="keyword">char</span> *name = ASHMEM_NAME_DEF;</span><br><span class="line">		<span class="class"><span class="keyword">struct</span> <span class="title">file</span> *<span class="title">vmfile</span>;</span></span><br><span class="line">		<span class="class"><span class="keyword">struct</span> <span class="title">inode</span> *<span class="title">inode</span>;</span></span><br><span class="line"></span><br><span class="line">		<span class="comment">//3. 检查asma中的file文件结构体是否创建</span></span><br><span class="line">		<span class="keyword">if</span> (asma-&gt;name[ASHMEM_NAME_PREFIX_LEN] != <span class="string">&#x27;\0&#x27;</span>)</span><br><span class="line">			name = asma-&gt;name;</span><br><span class="line"></span><br><span class="line">		<span class="comment">/* ... and allocate the backing shmem file */</span></span><br><span class="line">		<span class="comment">//4. </span></span><br><span class="line">		vmfile = shmem_file_setup(name, asma-&gt;size, vma-&gt;vm_flags);</span><br><span class="line">		<span class="keyword">if</span> (IS_ERR(vmfile)) &#123;</span><br><span class="line">			ret = PTR_ERR(vmfile);</span><br><span class="line">			<span class="keyword">goto</span> out;</span><br><span class="line">		&#125;</span><br><span class="line">		vmfile-&gt;f_mode |= FMODE_LSEEK;</span><br><span class="line">		inode = file_inode(vmfile);</span><br><span class="line">		lockdep_set_class(&amp;inode-&gt;i_rwsem, &amp;backing_shmem_inode_class);</span><br><span class="line">		asma-&gt;file = vmfile;</span><br><span class="line">		<span class="comment">/*</span></span><br><span class="line"><span class="comment">		 * override mmap operation of the vmfile so that it can&#x27;t be</span></span><br><span class="line"><span class="comment">		 * remapped which would lead to creation of a new vma with no</span></span><br><span class="line"><span class="comment">		 * asma permission checks. Have to override get_unmapped_area</span></span><br><span class="line"><span class="comment">		 * as well to prevent VM_BUG_ON check for f_ops modification.</span></span><br><span class="line"><span class="comment">		 */</span></span><br><span class="line">		<span class="keyword">if</span> (!vmfile_fops.mmap) &#123;</span><br><span class="line">			vmfile_fops = *vmfile-&gt;f_op;</span><br><span class="line">			vmfile_fops.mmap = ashmem_vmfile_mmap;</span><br><span class="line">			vmfile_fops.get_unmapped_area =</span><br><span class="line">					ashmem_vmfile_get_unmapped_area;</span><br><span class="line">		&#125;</span><br><span class="line">		vmfile-&gt;f_op = &amp;vmfile_fops;</span><br><span class="line">	&#125;</span><br><span class="line">	get_file(asma-&gt;file);</span><br><span class="line"></span><br><span class="line">	<span class="comment">/*</span></span><br><span class="line"><span class="comment">	 * XXX - Reworked to use shmem_zero_setup() instead of</span></span><br><span class="line"><span class="comment">	 * shmem_set_file while we&#x27;re in staging. -jstultz</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="keyword">if</span> (vma-&gt;vm_flags &amp; VM_SHARED) &#123;</span><br><span class="line">		<span class="comment">//5. 检查如果当前的vma虚拟内存允许共享则调用shmem_zero_setup映射文件</span></span><br><span class="line">		ret = shmem_zero_setup(vma);</span><br><span class="line">		<span class="keyword">if</span> (ret) &#123;</span><br><span class="line">			fput(asma-&gt;file);</span><br><span class="line">			<span class="keyword">goto</span> out;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">		vma_set_anonymous(vma);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (vma-&gt;vm_file)</span><br><span class="line">		fput(vma-&gt;vm_file);</span><br><span class="line">	vma-&gt;vm_file = asma-&gt;file;</span><br><span class="line"></span><br><span class="line">out:</span><br><span class="line">	mutex_unlock(&amp;ashmem_mutex);</span><br><span class="line">	<span class="keyword">return</span> ret;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>设置asma的size，不然会抛异常</p>
</li>
<li><p>检测需要映射的vma虚拟内存是否符合权限，否则抛异常</p>
</li>
<li><p>检查asma中的file文件结构体是否创建</p>
</li>
<li><p>没有则获取asma名字和大小通过shmem_file_setup创建一个文件描述符</p>
</li>
<li><p>检查如果当前的vma虚拟内存允许共享则调用shmem_zero_setup映射文件</p>
<h3 id="shmem-file-setup"><a href="#shmem-file-setup" class="headerlink" title="shmem_file_setup"></a>shmem_file_setup</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">https:<span class="comment">//android.googlesource.com/kernel/common/+/refs/tags/5.4-android11-0/mm/shmem.c</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="class"><span class="keyword">struct</span> <span class="title">file</span> *__<span class="title">shmem_file_setup</span>(<span class="keyword">struct</span> <span class="title">vfsmount</span> *<span class="title">mnt</span>, <span class="title">const</span> <span class="title">char</span> *<span class="title">name</span>, <span class="title">loff_t</span> <span class="title">size</span>,</span></span><br><span class="line"><span class="class">				       <span class="title">unsigned</span> <span class="title">long</span> <span class="title">flags</span>, <span class="title">unsigned</span> <span class="title">int</span> <span class="title">i_flags</span>)</span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">inode</span> *<span class="title">inode</span>;</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">file</span> *<span class="title">res</span>;</span></span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (IS_ERR(mnt))</span><br><span class="line">		<span class="keyword">return</span> ERR_CAST(mnt);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (size &lt; <span class="number">0</span> || size &gt; MAX_LFS_FILESIZE)</span><br><span class="line">		<span class="keyword">return</span> ERR_PTR(-EINVAL);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (shmem_acct_size(flags, size))</span><br><span class="line">		<span class="keyword">return</span> ERR_PTR(-ENOMEM);</span><br><span class="line">	<span class="comment">//分配inode，分配成功就好比建立了文件，也许并未存在真实文件映射</span></span><br><span class="line">	inode = shmem_get_inode(mnt-&gt;mnt_sb, <span class="literal">NULL</span>, S_IFREG | S_IRWXUGO, <span class="number">0</span>,</span><br><span class="line">				flags);</span><br><span class="line">	<span class="keyword">if</span> (unlikely(!inode)) &#123;</span><br><span class="line">		shmem_unacct_size(flags, size);</span><br><span class="line">		<span class="keyword">return</span> ERR_PTR(-ENOSPC);</span><br><span class="line">	&#125;</span><br><span class="line">	inode-&gt;i_flags |= i_flags;</span><br><span class="line">	inode-&gt;i_size = size;</span><br><span class="line">	clear_nlink(inode);	<span class="comment">/* It is unlinked */</span></span><br><span class="line">	res = ERR_PTR(ramfs_nommu_expand_for_mapping(inode, size));</span><br><span class="line">	<span class="keyword">if</span> (!IS_ERR(res))</span><br><span class="line">		res = alloc_file_pseudo(inode, mnt, name, O_RDWR,</span><br><span class="line">				&amp;shmem_file_operations);</span><br><span class="line">	<span class="keyword">if</span> (IS_ERR(res))</span><br><span class="line">		iput(inode);</span><br><span class="line">	<span class="keyword">return</span> res;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>通过shmem_file_setup在tmpfs临时文件系统中创建一个临时文件（也许只是内核中的一个inode节点），该文件与Ashmem驱动程序创建的匿名共享内存对应，不过用户态并不能看到该临时文件。</p>
<h3 id="shmem-zero-setup"><a href="#shmem-zero-setup" class="headerlink" title="shmem_zero_setup"></a>shmem_zero_setup</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">https:<span class="comment">//android.googlesource.com/kernel/common/+/refs/tags/5.4-android11-0/mm/shmem.c</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">shmem_zero_setup</span><span class="params">(struct vm_area_struct *vma)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">file</span> *<span class="title">file</span>;</span></span><br><span class="line">	<span class="keyword">loff_t</span> size = vma-&gt;vm_end - vma-&gt;vm_start;</span><br><span class="line"></span><br><span class="line">	<span class="comment">/*</span></span><br><span class="line"><span class="comment">	 * Cloning a new file under mmap_sem leads to a lock ordering conflict</span></span><br><span class="line"><span class="comment">	 * between XFS directory reading and selinux: since this file is only</span></span><br><span class="line"><span class="comment">	 * accessible to the user through its mapping, use S_PRIVATE flag to</span></span><br><span class="line"><span class="comment">	 * bypass file security, in the same way as shmem_kernel_file_setup().</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	file = shmem_kernel_file_setup(<span class="string">&quot;dev/zero&quot;</span>, size, vma-&gt;vm_flags);</span><br><span class="line">	<span class="keyword">if</span> (IS_ERR(file))</span><br><span class="line">		<span class="keyword">return</span> PTR_ERR(file);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (vma-&gt;vm_file)</span><br><span class="line">		fput(vma-&gt;vm_file);</span><br><span class="line">	vma-&gt;vm_file = file;</span><br><span class="line">	vma-&gt;vm_ops = &amp;shmem_vm_ops;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (IS_ENABLED(CONFIG_TRANSPARENT_HUGE_PAGECACHE) &amp;&amp;</span><br><span class="line">			((vma-&gt;vm_start + ~HPAGE_PMD_MASK) &amp; HPAGE_PMD_MASK) &lt;</span><br><span class="line">			(vma-&gt;vm_end &amp; HPAGE_PMD_MASK)) &#123;</span><br><span class="line">		khugepaged_enter(vma, vma-&gt;vm_flags);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function">struct file *<span class="title">shmem_kernel_file_setup</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *name, <span class="keyword">loff_t</span> size, <span class="keyword">unsigned</span> <span class="keyword">long</span> flags)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">return</span> __shmem_file_setup(shm_mnt, name, size, flags, S_PRIVATE);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="class"><span class="keyword">struct</span> <span class="title">file</span> *__<span class="title">shmem_file_setup</span>(<span class="keyword">struct</span> <span class="title">vfsmount</span> *<span class="title">mnt</span>, <span class="title">const</span> <span class="title">char</span> *<span class="title">name</span>, <span class="title">loff_t</span> <span class="title">size</span>,</span></span><br><span class="line"><span class="class">				       <span class="title">unsigned</span> <span class="title">long</span> <span class="title">flags</span>, <span class="title">unsigned</span> <span class="title">int</span> <span class="title">i_flags</span>)</span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">inode</span> *<span class="title">inode</span>;</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">file</span> *<span class="title">res</span>;</span></span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (IS_ERR(mnt))</span><br><span class="line">		<span class="keyword">return</span> ERR_CAST(mnt);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (size &lt; <span class="number">0</span> || size &gt; MAX_LFS_FILESIZE)</span><br><span class="line">		<span class="keyword">return</span> ERR_PTR(-EINVAL);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (shmem_acct_size(flags, size))</span><br><span class="line">		<span class="keyword">return</span> ERR_PTR(-ENOMEM);</span><br><span class="line"></span><br><span class="line">	inode = shmem_get_inode(mnt-&gt;mnt_sb, <span class="literal">NULL</span>, S_IFREG | S_IRWXUGO, <span class="number">0</span>,</span><br><span class="line">				flags);</span><br><span class="line">	<span class="keyword">if</span> (unlikely(!inode)) &#123;</span><br><span class="line">		shmem_unacct_size(flags, size);</span><br><span class="line">		<span class="keyword">return</span> ERR_PTR(-ENOSPC);</span><br><span class="line">	&#125;</span><br><span class="line">	inode-&gt;i_flags |= i_flags;</span><br><span class="line">	inode-&gt;i_size = size;</span><br><span class="line">	clear_nlink(inode);	<span class="comment">/* It is unlinked */</span></span><br><span class="line">	res = ERR_PTR(ramfs_nommu_expand_for_mapping(inode, size));</span><br><span class="line">	<span class="keyword">if</span> (!IS_ERR(res))</span><br><span class="line">		res = alloc_file_pseudo(inode, mnt, name, O_RDWR,</span><br><span class="line">				&amp;shmem_file_operations);</span><br><span class="line">	<span class="keyword">if</span> (IS_ERR(res))</span><br><span class="line">		iput(inode);</span><br><span class="line">	<span class="keyword">return</span> res;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>通过shmem_get_inode设置共享的inode，inode是Linux访问硬盘文件系统的基本单位，里面包含如superblock等元数据。</p>
</li>
<li><p>alloc_file_pseudo申请一个file结构体，同时复写file的结构中的file_operation文件操作</p>
</li>
</ul>
<p>继续看file_operations有哪些操作</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">https:<span class="comment">//android.googlesource.com/kernel/common/+/refs/tags/5.4-android11-0/mm/shmem.c</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">const</span> <span class="class"><span class="keyword">struct</span> <span class="title">file_operations</span> <span class="title">shmem_file_operations</span> =</span> &#123;</span><br><span class="line">	.mmap		= shmem_mmap,</span><br><span class="line">	.get_unmapped_area = shmem_get_unmapped_area,</span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifdef</span> CONFIG_TMPFS</span></span><br><span class="line">	.llseek		= shmem_file_llseek,</span><br><span class="line">	.read_iter	= shmem_file_read_iter,</span><br><span class="line">	.write_iter	= generic_file_write_iter,</span><br><span class="line">	.fsync		= noop_fsync,</span><br><span class="line">	.splice_read	= generic_file_splice_read,</span><br><span class="line">	.splice_write	= iter_file_splice_write,</span><br><span class="line">	.fallocate	= shmem_fallocate,</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>通过shmem_file_setup，ashmem驱动程序就把vma中的file文件结构体转化为共享内存了。<br>不过看到shmem这个名字就应该知道其实这就是Linux中的共享内存。</p>
<p>共享内存机制真正使用map的对象其实是这个临时文件，而不是ashmem设备文件，这里之所以是一次mmap，主要是通过vma-&gt;vm_file = asma-&gt;file完成map对象的替换，当映射的内存引起缺页中断的时候，就会调用shmem_file_setup创建的对象的函数，而不是ashmem的。</p>
<h1 id="FileDescriptor"><a href="#FileDescriptor" class="headerlink" title="FileDescriptor"></a>FileDescriptor</h1><p>文章有点长，看完MemoryFile和Ashmen的分析。可能就已经懵了，啥啊这是。那么到现在为止我们还不知道如何传输大数据。<br>原生Linux共享内存是通过传递已知的key来处理的，但是Android中不存在这种机制，Android是怎么处理的呢？那就是通过Binder传递文件描述符来处理，Android的Binder对于fd的传递也做了适配，原理其实就是<strong>在内核层为要传递的目标进程转换fd</strong>，因为在linux中fd只是对本进程是有效、且唯一，进程A打开一个文件得到一个fd，不能直接为进程B使用，因为B中那个fd可能压根无效、或者对应其他文件。虽然同一个文件可以有多个文件描述符，但是文件只有一个，在内核层也只会对应一个inode节点与file对象，这也是内核层可以传递fd的基础，Binder驱动通过当前进程的fd找到对应的文件，然后为目标进程新建fd，并传递给目标进程，核心就是把进程A中的fd转化成进程B中的fd。<br>在MemoryFile的构造函数里，会创建出一块共享内存，并用一个FileDescriptor文件描述符记录它，这个在前文。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/base/core/java/android/os/SharedMemory.java#377</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">static</span> native FileDescriptor <span class="title">nCreate</span><span class="params">(String name, <span class="keyword">int</span> size)</span> throws ErrnoException</span>;</span><br></pre></td></tr></table></figure>
<p>那么很明显，MemoryFile内部最核心的东西，也就来源于这个文件描述符。<br>在“使用”这一节了其实也用到了fd。</p>
<h1 id="QA"><a href="#QA" class="headerlink" title="QA"></a>QA</h1><ul>
<li>为了减少Java堆中的大小而把部分数据通过匿名共享内存传递，这样的内存优化方案是否可行？</li>
</ul>
<p>从前面的分析，我们知道匿名共享内存不会占用Dalvik Heap与Native Heap，不会导致OOM。但是如果说匿名共享内存是内存优化方案就是大错特错了。这种方式只是Android版本检测内存的漏洞而已。<br><img src="https://cdn.nlark.com/yuque/0/2021/png/1759879/1631953107698-2b5672f4-f6be-4a73-9b9e-c4b8f9c44e9a.png#clientId=ua831b86f-6d5e-4&from=paste&id=u1c8426b1&margin=%5Bobject%20Object%5D&name=image.png&originHeight=739&originWidth=749&originalType=url&ratio=1&size=374025&status=done&style=none&taskId=ua095e2b5-fef1-433f-b7e8-0c8e1c5b46d" alt="image.png"><br>（网上图片）<br>另外共享存占用空间的计算，只会计算到第一个创建它的进程中，其他进程不将ashmem计算在内。</p>
<h1 id="putBinder"><a href="#putBinder" class="headerlink" title="putBinder"></a>putBinder</h1><p>现在我们继续分析网上传得很神奇的putBinder可以传输大图片到底是为何。</p>
<h2 id="代码"><a href="#代码" class="headerlink" title="代码"></a>代码</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//发送端</span></span><br><span class="line">Intent intent = <span class="keyword">new</span> Intent(<span class="keyword">this</span>, SecondActivity.class);</span><br><span class="line">Bundle bundle = <span class="keyword">new</span> Bundle();</span><br><span class="line"><span class="keyword">if</span> (Build.VERSION.SDK_INT &gt;= Build.VERSION_CODES.JELLY_BEAN_MR2) &#123;</span><br><span class="line">    bundle.putBinder(<span class="string">&quot;bitmap&quot;</span>, <span class="keyword">new</span> ImageBinder(mBitmap));</span><br><span class="line">&#125;</span><br><span class="line">intent.putExtras(bundle);</span><br><span class="line">startActivity(intent);</span><br><span class="line"></span><br><span class="line"><span class="comment">//接收端</span></span><br><span class="line">Bundle bundle = getIntent().getExtras();</span><br><span class="line"><span class="keyword">if</span> (bundle != <span class="keyword">null</span>) &#123;</span><br><span class="line">    <span class="keyword">if</span> (Build.VERSION.SDK_INT &gt;= Build.VERSION_CODES.JELLY_BEAN_MR2) &#123;</span><br><span class="line">        ImageBinder imageBinder = (ImageBinder) bundle.getBinder(<span class="string">&quot;bitmap&quot;</span>);</span><br><span class="line">        Bitmap bitmap = imageBinder.getBitmap();</span><br><span class="line">        mTv.setText(String.format((<span class="string">&quot;bitmap大小为%dkB&quot;</span>), bitmap.getByteCount() / <span class="number">1024</span>));</span><br><span class="line">        mIv.setImageBitmap(bitmap);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="源码"><a href="#源码" class="headerlink" title="源码"></a>源码</h2><p>startActivity的流程请阅读Activity启动分析，这里只贴出关键代码片段。</p>
<h3 id="writeToParcel"><a href="#writeToParcel" class="headerlink" title="writeToParcel"></a>writeToParcel</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">startActivity</span><span class="params">(..., Intent intent, ...)</span> </span>&#123;</span><br><span class="line">    Parcel data = Parcel.obtain();</span><br><span class="line">    ......  </span><br><span class="line">    intent.writeToParcel(data, <span class="number">0</span>);    </span><br><span class="line">    ......</span><br><span class="line">    mRemote.transact(START_ACTIVITY_TRANSACTION, data, reply, <span class="number">0</span>);  </span><br><span class="line">    ......</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">writeToParcel</span><span class="params">(Parcel out, <span class="keyword">int</span> flags)</span> </span>&#123;</span><br><span class="line">    ......    </span><br><span class="line">    out.writeBundle(mExtras);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">writeToParcel</span><span class="params">(Parcel out, <span class="keyword">int</span> flags)</span> </span>&#123;    </span><br><span class="line">    ......    </span><br><span class="line">    out.writeBundle(mExtras);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>writeToParcel函数，其实就是给Intent里的Bundle写到Parcel了。</p>
<h3 id="writeBundle"><a href="#writeBundle" class="headerlink" title="writeBundle"></a>writeBundle</h3><p>继续往下走，看Bundle怎么写到Parcel的，原来是调到了Bundle的writeToParcel函数。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title">writeBundle</span><span class="params">(Bundle val)</span> </span>&#123;    </span><br><span class="line">    val.writeToParcel(<span class="keyword">this</span>, <span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="writeToParcelInner"><a href="#writeToParcelInner" class="headerlink" title="writeToParcelInner"></a>writeToParcelInner</h3><p>继续往下走，又调到了writeToParcelInner。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">writeToParcel</span><span class="params">(Parcel parcel, <span class="keyword">int</span> flags)</span> </span>&#123;    </span><br><span class="line">    <span class="keyword">final</span> <span class="keyword">boolean</span> oldAllowFds = parcel.pushAllowFds(mAllowFds); </span><br><span class="line">    <span class="keyword">super</span>.writeToParcelInner(parcel, flags);    </span><br><span class="line">    parcel.restoreAllowFds(oldAllowFds);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="pushAllowFds"><a href="#pushAllowFds" class="headerlink" title="pushAllowFds"></a>pushAllowFds</h3><p>这不就是上文我们提到的fd吗，继续往下看。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">bool Parcel::pushAllowFds(bool allowFds) &#123;    </span><br><span class="line">    <span class="keyword">const</span> bool origValue = mAllowFds;    </span><br><span class="line">    <span class="keyword">if</span> (!allowFds) &#123;        </span><br><span class="line">        mAllowFds = <span class="keyword">false</span>;    </span><br><span class="line">    &#125;    </span><br><span class="line">    <span class="keyword">return</span> origValue;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>返回true，不再深入分析</p>
</blockquote>
<h3 id="writeToParcelInner-1"><a href="#writeToParcelInner-1" class="headerlink" title="writeToParcelInner"></a>writeToParcelInner</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">writeToParcelInner</span><span class="params">(Parcel parcel, <span class="keyword">int</span> flags)</span> </span>&#123;    </span><br><span class="line">    ......    </span><br><span class="line">    parcel.writeArrayMapInternal(mMap);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">writeArrayMapInternal</span><span class="params">(ArrayMap&lt;String, Object&gt; val)</span> </span>&#123;      </span><br><span class="line">    <span class="keyword">final</span> <span class="keyword">int</span> N = val.size();   </span><br><span class="line">    writeInt(N);    </span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; N; i++) &#123;        </span><br><span class="line">        writeString(val.keyAt(i));        </span><br><span class="line">        writeValue(val.valueAt(i));    </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title">writeValue</span><span class="params">(Object v)</span> </span>&#123;    </span><br><span class="line">    ......     </span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (v <span class="keyword">instanceof</span> Parcelable) &#123;        </span><br><span class="line">        writeInt(VAL_PARCELABLE);        </span><br><span class="line">        writeParcelable((Parcelable) v, <span class="number">0</span>);    </span><br><span class="line">    &#125;     </span><br><span class="line">    ......</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">writeParcelable</span><span class="params">(Parcelable p, <span class="keyword">int</span> parcelableFlags)</span> </span>&#123;    </span><br><span class="line">    writeParcelableCreator(p);    </span><br><span class="line">    p.writeToParcel(<span class="keyword">this</span>, parcelableFlags);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>就是在一个for循环里给map的key和value依次写到parcel。因为Bitmap是Parcelable的，所以我们只关注这个分支，这又调到了Bitmap的writeToParcel函数。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">writeParcelable</span><span class="params">(Parcelable p, <span class="keyword">int</span> parcelableFlags)</span> </span>&#123;    </span><br><span class="line">    writeParcelableCreator(p);    </span><br><span class="line">    p.writeToParcel(<span class="keyword">this</span>, parcelableFlags);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>进入了native层</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function">jboolean <span class="title">Bitmap_writeToParcel</span><span class="params">(JNIEnv* env, jobject, ...)</span> </span>&#123;    </span><br><span class="line">    android::Bitmap* androidBitmap = reinterpret_cast&lt;Bitmap*&gt;(bitmapHandle);    </span><br><span class="line">    androidBitmap-&gt;getSkBitmap(&amp;bitmap);  </span><br><span class="line">     </span><br><span class="line">    <span class="comment">// 往parcel里写Bitmap的各种配置参数    </span></span><br><span class="line">    <span class="keyword">int</span> fd = androidBitmap-&gt;getAshmemFd();    </span><br><span class="line">    <span class="keyword">if</span> (fd &gt;= <span class="number">0</span> &amp;&amp; !isMutable &amp;&amp; p-&gt;allowFds()) &#123;        </span><br><span class="line">        status = p-&gt;writeDupImmutableBlobFileDescriptor(fd);        </span><br><span class="line">        <span class="keyword">return</span> JNI_TRUE;    </span><br><span class="line">    &#125;    </span><br><span class="line">     </span><br><span class="line">    android::Parcel::WritableBlob blob;    </span><br><span class="line">    status = p-&gt;writeBlob(size, mutableCopy, &amp;blob);    </span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">void</span>* pSrc =  bitmap.getPixels();    </span><br><span class="line">    memcpy(blob.data(), pSrc, size);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这里首先拿到native层的Bitmap对象，叫androidBitmap，然后拿到对应的SkBitmap。先看bitmap里带不带ashmemFd，如果带，并且这个Bitmap不能改，并且Parcel是允许带fd的话，就给fd写到parcel里，然后返回。否则的话继续往下，先有个WriteBlob对象，通过writeBlob函数给这个blob在parcel里分配了一块空间，然后给bitmap拷贝到这块空间里。我们看这个writeBlob函数。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">status_t Parcel::writeBlob(size_t len, bool mutableCopy, WritableBlob* outBlob) &#123;    </span><br><span class="line">    <span class="keyword">if</span> (!mAllowFds || len &lt;= BLOB_INPLACE_LIMIT) &#123;        </span><br><span class="line">        status = writeInt32(BLOB_INPLACE);        </span><br><span class="line">        <span class="keyword">void</span>* ptr = writeInplace(len);        </span><br><span class="line">        outBlob-&gt;init(-<span class="number">1</span>, ptr, len, <span class="keyword">false</span>);        </span><br><span class="line">        <span class="keyword">return</span> NO_ERROR;    </span><br><span class="line">    &#125;    </span><br><span class="line">    <span class="keyword">int</span> fd = ashmem_create_region(<span class="string">&quot;Parcel Blob&quot;</span>, len);    </span><br><span class="line">    <span class="keyword">void</span>* ptr = mmap(NULL, len, ..., MAP_SHARED, fd, <span class="number">0</span>);    </span><br><span class="line">    ......    </span><br><span class="line">    status = writeFileDescriptor(fd, <span class="keyword">true</span>);    </span><br><span class="line">    outBlob-&gt;init(fd, ptr, len, mutableCopy);    </span><br><span class="line">    <span class="keyword">return</span> NO_ERROR;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这个writeBlob函数，首先看如果不允许带fd，或者这个数据小于16K，就直接在parcel的缓冲区里分配一块空间来保存这个数据。不然的话呢，就另外开辟一个ashmem，映射出一块内存，数据就保存在ashmem的内存里，parcel里只写个fd就好了，这样就算数据量很大，parcel自己的缓冲区也不用很大。</p>
<p>看到这里就明白了，其实还有用了Ashmen机制。</p>
<h1 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h1><p><a href="https://my.oschina.net/youranhongcha/blog/3075518">Ashmem机制讲解</a><br><a href="https://www.jianshu.com/p/d9bc9c668ba6">Android匿名共享内存（Ashmem）原理</a></p>
]]></content>
      <categories>
        <category>Android</category>
      </categories>
      <tags>
        <tag>Android</tag>
        <tag>Binder</tag>
      </tags>
  </entry>
  <entry>
    <title>Binder机制08-总结</title>
    <url>/archives/720ded9.html</url>
    <content><![CDATA[<span id="more"></span>
<meta name="referrer" content="no-referrer"/>


<h1 id="Binder机制文章列表"><a href="#Binder机制文章列表" class="headerlink" title="Binder机制文章列表"></a>Binder机制文章列表</h1><p><a href="https://i-rtfsc.github.io/archives/d6aea0f1.html">Binder概述</a><br><a href="https://i-rtfsc.github.io/archives/3e87e0ce.html">Binder机制01-驱动</a><br><a href="https://i-rtfsc.github.io/archives/f17b1d52.html">Binder机制02-ServiceManager</a><br><a href="https://i-rtfsc.github.io/archives/937d16d8.html">Binder机制03-Framework-Native</a><br><a href="https://i-rtfsc.github.io/archives/73029508.html">Binder机制04-Framework-Jave</a><br><a href="https://i-rtfsc.github.io/archives/ed3eb50b.html">Binder机制05-AIDL</a><br><a href="https://i-rtfsc.github.io/archives/4e7f84b4.html">Binder机制06-框架</a><br><a href="https://i-rtfsc.github.io/archives/d30c7ecb.html">Binder机制07-实例分析</a><br><a href="https://i-rtfsc.github.io/archives/720ded9.html">Binder机制08-总结</a><br><a href="https://i-rtfsc.github.io/archives/f1684132.html">Ashmem简介（Android IPC传输大数据）</a></p>
<p>关于这篇文章还在语雀笔记中的整理中…很快就补上…</p>
]]></content>
      <categories>
        <category>Android</category>
      </categories>
      <tags>
        <tag>Android</tag>
        <tag>Binder</tag>
      </tags>
  </entry>
  <entry>
    <title>Binder机制07-实例分析</title>
    <url>/archives/d30c7ecb.html</url>
    <content><![CDATA[<span id="more"></span>
<meta name="referrer" content="no-referrer"/>


<h1 id="Binder机制文章列表"><a href="#Binder机制文章列表" class="headerlink" title="Binder机制文章列表"></a>Binder机制文章列表</h1><p><a href="https://i-rtfsc.github.io/archives/d6aea0f1.html">Binder概述</a><br><a href="https://i-rtfsc.github.io/archives/3e87e0ce.html">Binder机制01-驱动</a><br><a href="https://i-rtfsc.github.io/archives/f17b1d52.html">Binder机制02-ServiceManager</a><br><a href="https://i-rtfsc.github.io/archives/937d16d8.html">Binder机制03-Framework-Native</a><br><a href="https://i-rtfsc.github.io/archives/73029508.html">Binder机制04-Framework-Jave</a><br><a href="https://i-rtfsc.github.io/archives/ed3eb50b.html">Binder机制05-AIDL</a><br><a href="https://i-rtfsc.github.io/archives/4e7f84b4.html">Binder机制06-框架</a><br><a href="https://i-rtfsc.github.io/archives/d30c7ecb.html">Binder机制07-实例分析</a><br><a href="https://i-rtfsc.github.io/archives/720ded9.html">Binder机制08-总结</a><br><a href="https://i-rtfsc.github.io/archives/f1684132.html">Ashmem简介（Android IPC传输大数据）</a></p>
<p>关于这篇文章还在语雀笔记中的整理中…很快就补上…</p>
]]></content>
      <categories>
        <category>Android</category>
      </categories>
      <tags>
        <tag>Android</tag>
        <tag>Binder</tag>
      </tags>
  </entry>
  <entry>
    <title>Binder机制06-框架</title>
    <url>/archives/4e7f84b4.html</url>
    <content><![CDATA[<span id="more"></span>
<meta name="referrer" content="no-referrer"/>


<h1 id="Binder机制文章列表"><a href="#Binder机制文章列表" class="headerlink" title="Binder机制文章列表"></a>Binder机制文章列表</h1><p><a href="https://i-rtfsc.github.io/archives/d6aea0f1.html">Binder概述</a><br><a href="https://i-rtfsc.github.io/archives/3e87e0ce.html">Binder机制01-驱动</a><br><a href="https://i-rtfsc.github.io/archives/f17b1d52.html">Binder机制02-ServiceManager</a><br><a href="https://i-rtfsc.github.io/archives/937d16d8.html">Binder机制03-Framework-Native</a><br><a href="https://i-rtfsc.github.io/archives/73029508.html">Binder机制04-Framework-Jave</a><br><a href="https://i-rtfsc.github.io/archives/ed3eb50b.html">Binder机制05-AIDL</a><br><a href="https://i-rtfsc.github.io/archives/4e7f84b4.html">Binder机制06-框架</a><br><a href="https://i-rtfsc.github.io/archives/d30c7ecb.html">Binder机制07-实例分析</a><br><a href="https://i-rtfsc.github.io/archives/720ded9.html">Binder机制08-总结</a><br><a href="https://i-rtfsc.github.io/archives/f1684132.html">Ashmem简介（Android IPC传输大数据）</a></p>
<p>关于这篇文章还在语雀笔记中的整理中…很快就补上…</p>
]]></content>
      <categories>
        <category>Android</category>
      </categories>
      <tags>
        <tag>Android</tag>
        <tag>Binder</tag>
      </tags>
  </entry>
  <entry>
    <title>Binder机制05-AIDL</title>
    <url>/archives/ed3eb50b.html</url>
    <content><![CDATA[<span id="more"></span>
<meta name="referrer" content="no-referrer"/>


<h1 id="Binder机制文章列表"><a href="#Binder机制文章列表" class="headerlink" title="Binder机制文章列表"></a>Binder机制文章列表</h1><p><a href="https://i-rtfsc.github.io/archives/d6aea0f1.html">Binder概述</a><br><a href="https://i-rtfsc.github.io/archives/3e87e0ce.html">Binder机制01-驱动</a><br><a href="https://i-rtfsc.github.io/archives/f17b1d52.html">Binder机制02-ServiceManager</a><br><a href="https://i-rtfsc.github.io/archives/937d16d8.html">Binder机制03-Framework-Native</a><br><a href="https://i-rtfsc.github.io/archives/73029508.html">Binder机制04-Framework-Jave</a><br><a href="https://i-rtfsc.github.io/archives/ed3eb50b.html">Binder机制05-AIDL</a><br><a href="https://i-rtfsc.github.io/archives/4e7f84b4.html">Binder机制06-框架</a><br><a href="https://i-rtfsc.github.io/archives/d30c7ecb.html">Binder机制07-实例分析</a><br><a href="https://i-rtfsc.github.io/archives/720ded9.html">Binder机制08-总结</a><br><a href="https://i-rtfsc.github.io/archives/f1684132.html">Ashmem简介（Android IPC传输大数据）</a></p>
<p>关于这篇文章还在语雀笔记中的整理中…很快就补上…</p>
]]></content>
      <categories>
        <category>Android</category>
      </categories>
      <tags>
        <tag>Android</tag>
        <tag>Binder</tag>
      </tags>
  </entry>
  <entry>
    <title>Binder机制04-Framework-Java</title>
    <url>/archives/73029508.html</url>
    <content><![CDATA[<span id="more"></span>
<meta name="referrer" content="no-referrer"/>


<h1 id="Binder机制文章列表"><a href="#Binder机制文章列表" class="headerlink" title="Binder机制文章列表"></a>Binder机制文章列表</h1><p><a href="https://i-rtfsc.github.io/archives/d6aea0f1.html">Binder概述</a><br><a href="https://i-rtfsc.github.io/archives/3e87e0ce.html">Binder机制01-驱动</a><br><a href="https://i-rtfsc.github.io/archives/f17b1d52.html">Binder机制02-ServiceManager</a><br><a href="https://i-rtfsc.github.io/archives/937d16d8.html">Binder机制03-Framework-Native</a><br><a href="https://i-rtfsc.github.io/archives/73029508.html">Binder机制04-Framework-Jave</a><br><a href="https://i-rtfsc.github.io/archives/ed3eb50b.html">Binder机制05-AIDL</a><br><a href="https://i-rtfsc.github.io/archives/4e7f84b4.html">Binder机制06-框架</a><br><a href="https://i-rtfsc.github.io/archives/d30c7ecb.html">Binder机制07-实例分析</a><br><a href="https://i-rtfsc.github.io/archives/720ded9.html">Binder机制08-总结</a><br><a href="https://i-rtfsc.github.io/archives/f1684132.html">Ashmem简介（Android IPC传输大数据）</a></p>
<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>在<a href="https://i-rtfsc.github.io/archives/937d16d8.html">Binder机制03-Framework-Native</a>章节里我们分析了native(C/C++)层的binder架构，Android应用程序使用Java语言开发，Binder框架自然也少不了在Java层提供接口。所以Binder在framework层，采用JNI技术来调用native(C/C++)层的binder架构，最终把服务注册到Native层的ServiceManager中。<br>应用层的进程通过Framework的接口，其实也就是经过JNI技术进入Native C\C++，最终在Native层的ServiceManager中得到服务handle，最终转成相应的服务对象。</p>
<ul>
<li>Binder Framework JNI：<a href="http://aospxref.com/android-11.0.0_r21/xref/frameworks/base/core/jni/">http://aospxref.com/android-11.0.0_r21/xref/frameworks/base/core/jni/</a><ul>
<li>android_util_Binder.h</li>
<li>android_util_Binder.cpp</li>
<li>android_os_Parcel.h</li>
<li>android_os_Parcel.cpp</li>
<li>android_os_HwBinder.h</li>
<li>android_os_HwBinder.cpp</li>
<li>android_os_HwParcel.h</li>
<li>android_os_HwParcel.cpp</li>
</ul>
</li>
<li>Binder Framework JAVA：<a href="http://aospxref.com/android-11.0.0_r21/xref/frameworks/base/core/java/android/os/">http://aospxref.com/android-11.0.0_r21/xref/frameworks/base/core/java/android/os/</a><ul>
<li>Binder.java</li>
<li>IBinder.java</li>
<li>IInterface.java</li>
<li>Parcel.java</li>
<li>HwBinder.java</li>
<li>IHwBinder.java</li>
<li>IHwInterface.java</li>
<li>HwParcel.java</li>
<li>IServiceManager.java</li>
<li>ServiceManager.java</li>
<li>ServiceManagerNative.java(内含ServiceManagerProxy类)</li>
<li>BinderInternal.java(<a href="http://aospxref.com/android-11.0.0_r21/xref/frameworks/base/core/java/com/android/internal/os/">http://aospxref.com/android-11.0.0_r21/xref/frameworks/base/core/java/com/android/internal/os/</a>)</li>
</ul>
</li>
</ul>
<h1 id="框架"><a href="#框架" class="headerlink" title="框架"></a>框架</h1><p>下图描述了Binder Framework Java层到C++层的衔接关系。<br><img src="https://cdn.nlark.com/yuque/0/2021/png/1759879/1634445154428-ccc5066f-4a5b-4fc4-b7e1-5ee2668d1f54.png#clientId=u971b965b-56bd-4&from=paste&height=886&id=u48c0f65a&margin=%5Bobject%20Object%5D&name=image.png&originHeight=1772&originWidth=1858&originalType=binary&ratio=1&size=201047&status=done&style=none&taskId=udd7f8bd1-13cc-4f53-a586-33497c6e494&width=929" alt="image.png"><br>这里对图中Java层和JNI层的几个类做一下说明：</p>
<ul>
<li>IInterface(interface) ：供Java层Binder服务接口继承的接口</li>
<li>IBinder(interface)：Java层IBinder类，提供了transaction方法来调用远程服务</li>
<li>Binder(class)：实现了IBinder接口，封装了JNI的实现。Java层Binder服务的基类</li>
<li>BInderProxy(class)：实现了IBinder接口，封装了JNI的实现。提供transac()方法调用远程服务</li>
<li>JavaBBinderHolder(class) ：内部存储了JavaBBinder</li>
<li>JavaBBinder(class)：将C++端的onTransact调用传递到Java端</li>
<li>Parcel(class)：Java层的数据包装器。</li>
</ul>
<p>这里的IInterface，IBinder和C++层的两个类是同名的。这个同名并不是巧合：它们不仅仅同名，它们所起的作用，以及其中包含的接口都是几乎一样的，区别仅仅在于一个是C++层，一个是Java层而已。<br>除了IInterface，IBinder之外，这里Binder与BinderProxy类也是与C++的类对应的，下面列出了Java层和C++层类的对应关系：</p>
<table>
<thead>
<tr>
<th><strong>C++层</strong></th>
<th><strong>Java层</strong></th>
</tr>
</thead>
<tbody><tr>
<td>IInterface</td>
<td>IInterface</td>
</tr>
<tr>
<td>IBinder</td>
<td>IBinder</td>
</tr>
<tr>
<td>BBinder</td>
<td>BBinder</td>
</tr>
<tr>
<td>BpProxy</td>
<td>BpProxy</td>
</tr>
<tr>
<td>Parcel</td>
<td>Parcel</td>
</tr>
</tbody></table>
<h1 id="JNI"><a href="#JNI" class="headerlink" title="JNI"></a>JNI</h1><p>JNI全称是Java Native Interface，这个是由Java虚拟机提供的机制。这个机制使得natvie代码可以和Java代码相互调用。简单的来说就是：我们可以在C/C++端调用Java代码，也可以在Java端调用C/C++代码。<br>实际上，在Android中很多的服务或者机制都是在C/C++层实现的，想要将这些实现复用到Java层就必须通过JNI进行衔接。AOSP中：</p>
<ul>
<li>frameworks/base/core/jni/ 目录下的源码就是专门用来对接Framework层的JNI实现</li>
<li>frameworks/base/core/java/ 目录下的源码就是Framework层的调用JNI代码</li>
</ul>
<p>其实大家看一下Binder.java的实现就会发现，这里面有不少的方法都是native关键字修饰的，并且没有方法实现体，这些方法其实都是在C++中实现的。</p>
<h2 id="Java"><a href="#Java" class="headerlink" title="Java"></a>Java</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/base/core/java/android/os/Binder.java</span></span><br><span class="line"></span><br><span class="line">package android.os;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Binder</span> <span class="title">implements</span> <span class="title">IBinder</span> &#123;</span></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">static</span> native <span class="keyword">long</span> <span class="title">getNativeFinalizer</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> native <span class="keyword">int</span> <span class="title">getCallingPid</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> native <span class="keyword">int</span> <span class="title">getCallingUid</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> native boolean <span class="title">isHandlingTransaction</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> native <span class="keyword">long</span> <span class="title">clearCallingIdentity</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> native <span class="keyword">void</span> <span class="title">restoreCallingIdentity</span><span class="params">(<span class="keyword">long</span> token)</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> native <span class="keyword">void</span> <span class="title">setThreadStrictModePolicy</span><span class="params">(<span class="keyword">int</span> policyMask)</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> native <span class="keyword">int</span> <span class="title">getThreadStrictModePolicy</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> native <span class="keyword">long</span> <span class="title">setCallingWorkSourceUid</span><span class="params">(<span class="keyword">int</span> workSource)</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> native <span class="keyword">int</span> <span class="title">getCallingWorkSourceUid</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> native <span class="keyword">long</span> <span class="title">clearCallingWorkSource</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> native <span class="keyword">void</span> <span class="title">restoreCallingWorkSource</span><span class="params">(<span class="keyword">long</span> token)</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">final</span> native <span class="keyword">void</span> <span class="title">markVintfStability</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> native <span class="keyword">void</span> <span class="title">flushPendingCommands</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> native <span class="keyword">void</span> <span class="title">blockUntilThreadAvailable</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">final</span> native @<span class="function">Nullable IBinder <span class="title">getExtension</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">final</span> native <span class="keyword">void</span> <span class="title">setExtension</span><span class="params">(@Nullable IBinder extension)</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">static</span> native <span class="keyword">long</span> <span class="title">getNativeBBinderHolder</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">static</span> native <span class="keyword">long</span> <span class="title">getFinalizer</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>  方法都是native关键字修饰的，并且没有方法实现体。</p>
<h2 id="JNINativeMethod"><a href="#JNINativeMethod" class="headerlink" title="JNINativeMethod"></a>JNINativeMethod</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/base/core/jni/android_util_Binder.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">const</span> JNINativeMethod gBinderMethods[] = &#123;</span><br><span class="line">     <span class="comment">/* name, signature, funcPtr */</span></span><br><span class="line">    &#123; <span class="string">&quot;getCallingPid&quot;</span>, <span class="string">&quot;()I&quot;</span>, (<span class="keyword">void</span>*)android_os_Binder_getCallingPid &#125;,</span><br><span class="line">    &#123; <span class="string">&quot;getCallingUid&quot;</span>, <span class="string">&quot;()I&quot;</span>, (<span class="keyword">void</span>*)android_os_Binder_getCallingUid &#125;,</span><br><span class="line">    &#123; <span class="string">&quot;isHandlingTransaction&quot;</span>, <span class="string">&quot;()Z&quot;</span>, (<span class="keyword">void</span>*)android_os_Binder_isHandlingTransaction &#125;,</span><br><span class="line">    &#123; <span class="string">&quot;clearCallingIdentity&quot;</span>, <span class="string">&quot;()J&quot;</span>, (<span class="keyword">void</span>*)android_os_Binder_clearCallingIdentity &#125;,</span><br><span class="line">    &#123; <span class="string">&quot;restoreCallingIdentity&quot;</span>, <span class="string">&quot;(J)V&quot;</span>, (<span class="keyword">void</span>*)android_os_Binder_restoreCallingIdentity &#125;,</span><br><span class="line">    &#123; <span class="string">&quot;setThreadStrictModePolicy&quot;</span>, <span class="string">&quot;(I)V&quot;</span>, (<span class="keyword">void</span>*)android_os_Binder_setThreadStrictModePolicy &#125;,</span><br><span class="line">    &#123; <span class="string">&quot;getThreadStrictModePolicy&quot;</span>, <span class="string">&quot;()I&quot;</span>, (<span class="keyword">void</span>*)android_os_Binder_getThreadStrictModePolicy &#125;,</span><br><span class="line">    &#123; <span class="string">&quot;setCallingWorkSourceUid&quot;</span>, <span class="string">&quot;(I)J&quot;</span>, (<span class="keyword">void</span>*)android_os_Binder_setCallingWorkSourceUid &#125;,</span><br><span class="line">    &#123; <span class="string">&quot;getCallingWorkSourceUid&quot;</span>, <span class="string">&quot;()I&quot;</span>, (<span class="keyword">void</span>*)android_os_Binder_getCallingWorkSourceUid &#125;,</span><br><span class="line">    &#123; <span class="string">&quot;clearCallingWorkSource&quot;</span>, <span class="string">&quot;()J&quot;</span>, (<span class="keyword">void</span>*)android_os_Binder_clearCallingWorkSource &#125;,</span><br><span class="line">    &#123; <span class="string">&quot;restoreCallingWorkSource&quot;</span>, <span class="string">&quot;(J)V&quot;</span>, (<span class="keyword">void</span>*)android_os_Binder_restoreCallingWorkSource &#125;,</span><br><span class="line">    &#123; <span class="string">&quot;markVintfStability&quot;</span>, <span class="string">&quot;()V&quot;</span>, (<span class="keyword">void</span>*)android_os_Binder_markVintfStability&#125;,</span><br><span class="line">    &#123; <span class="string">&quot;flushPendingCommands&quot;</span>, <span class="string">&quot;()V&quot;</span>, (<span class="keyword">void</span>*)android_os_Binder_flushPendingCommands &#125;,</span><br><span class="line">    &#123; <span class="string">&quot;getNativeBBinderHolder&quot;</span>, <span class="string">&quot;()J&quot;</span>, (<span class="keyword">void</span>*)android_os_Binder_getNativeBBinderHolder &#125;,</span><br><span class="line">    &#123; <span class="string">&quot;getNativeFinalizer&quot;</span>, <span class="string">&quot;()J&quot;</span>, (<span class="keyword">void</span>*)android_os_Binder_getNativeFinalizer &#125;,</span><br><span class="line">    &#123; <span class="string">&quot;blockUntilThreadAvailable&quot;</span>, <span class="string">&quot;()V&quot;</span>, (<span class="keyword">void</span>*)android_os_Binder_blockUntilThreadAvailable &#125;,</span><br><span class="line">    &#123; <span class="string">&quot;getExtension&quot;</span>, <span class="string">&quot;()Landroid/os/IBinder;&quot;</span>, (<span class="keyword">void</span>*)android_os_Binder_getExtension &#125;,</span><br><span class="line">    &#123; <span class="string">&quot;setExtension&quot;</span>, <span class="string">&quot;(Landroid/os/IBinder;)V&quot;</span>, (<span class="keyword">void</span>*)android_os_Binder_setExtension &#125;,</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">char</span>* <span class="keyword">const</span> kBinderPathName = <span class="string">&quot;android/os/Binder&quot;</span>;</span><br></pre></td></tr></table></figure>
<p>  定义了一个JNINativeMethod类型的数组，数组的名字是gBinderMethods，里面存储的是native方法于JNI层函数的对应关系。只定义是没有用的，还需要注册它，注册的函数为：int_register_android_os_Binder(JNIEnv* env)。<br>关于JNI更详细的分析，我们以后也会有专门的章节讨论，这里就不再深入分析。我们这里只把Android系统起来后注册JNI的流程简单梳理。</p>
<pre class="mermaid">sequenceDiagram
Zygote ->> app_main:main()
app_main ->> AndroidRuntime:start()
AndroidRuntime ->> AndroidRuntime:startVm()
AndroidRuntime ->>+ AndroidRuntime:startReg()
AndroidRuntime ->> AndroidRuntime:REG_JNI(register_android_os_Binder)
AndroidRuntime ->> android_util_Binder:register_android_os_Binder()
android_util_Binder ->> android_util_Binder:int_register_android_os_Binder()
android_util_Binder ->> core_jni_helpers:RegisterMethodsOrDie()
core_jni_helpers -->> AndroidRuntime:registerNativeMethods()
AndroidRuntime ->>- JNIHelp:jniRegisterNativeMethods()</pre>

<h2 id="（附）C调用Java"><a href="#（附）C调用Java" class="headerlink" title="（附）C调用Java"></a>（附）C调用Java</h2><p>上面我们知道了Java通过JNI调用C代码，那么C代码如何反过来调用Java代码呢？libbinder中的<strong>BBinder::onTransacts</strong>是如何能能够调用到Java中的<strong>Binder::onTransact</strong>的？</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/base/core/jni/android_util_Binder.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">JavaBBinder</span> :</span> <span class="keyword">public</span> BBinder</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">status_t</span> <span class="title">onTransact</span><span class="params">(</span></span></span><br><span class="line"><span class="params"><span class="function">        <span class="keyword">uint32_t</span> code, <span class="keyword">const</span> Parcel&amp; data, Parcel* reply, <span class="keyword">uint32_t</span> flags = <span class="number">0</span>)</span> <span class="keyword">override</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        JNIEnv* env = <span class="built_in">javavm_to_jnienv</span>(mVM);</span><br><span class="line"></span><br><span class="line">        IPCThreadState* thread_state = IPCThreadState::<span class="built_in">self</span>();</span><br><span class="line">        <span class="keyword">const</span> <span class="keyword">int32_t</span> strict_policy_before = thread_state-&gt;<span class="built_in">getStrictModePolicy</span>();</span><br><span class="line">    <span class="comment">//调到了android.os.Binder类的exectTransac方法</span></span><br><span class="line">        jboolean res = env-&gt;<span class="built_in">CallBooleanMethod</span>(mObject, gBinderOffsets.mExecTransact,</span><br><span class="line">            code, <span class="keyword">reinterpret_cast</span>&lt;jlong&gt;(&amp;data), <span class="keyword">reinterpret_cast</span>&lt;jlong&gt;(reply), flags);</span><br><span class="line"></span><br><span class="line">        ...</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> res != JNI_FALSE ? NO_ERROR : UNKNOWN_TRANSACTION;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">char</span>* <span class="keyword">const</span> kBinderPathName = <span class="string">&quot;android/os/Binder&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">int_register_android_os_Binder</span><span class="params">(JNIEnv* env)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    jclass clazz = <span class="built_in">FindClassOrDie</span>(env, kBinderPathName);</span><br><span class="line"></span><br><span class="line">    gBinderOffsets.mClass = <span class="built_in">MakeGlobalRefOrDie</span>(env, clazz);</span><br><span class="line">    <span class="comment">//gBinderOffsets.mExecTransact也就是android.os.Binder类的exectTransac方法</span></span><br><span class="line">    gBinderOffsets.mExecTransact = <span class="built_in">GetMethodIDOrDie</span>(env, clazz, <span class="string">&quot;execTransact&quot;</span>, <span class="string">&quot;(IJJI)Z&quot;</span>);</span><br><span class="line">    gBinderOffsets.mGetInterfaceDescriptor = <span class="built_in">GetMethodIDOrDie</span>(env, clazz, <span class="string">&quot;getInterfaceDescriptor&quot;</span>,</span><br><span class="line">        <span class="string">&quot;()Ljava/lang/String;&quot;</span>);</span><br><span class="line">    gBinderOffsets.mObject = <span class="built_in">GetFieldIDOrDie</span>(env, clazz, <span class="string">&quot;mObject&quot;</span>, <span class="string">&quot;J&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">RegisterMethodsOrDie</span>(</span><br><span class="line">        env, kBinderPathName,</span><br><span class="line">        gBinderMethods, <span class="built_in">NELEM</span>(gBinderMethods));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>kBinderPathName为”android/os/Binder”，也就是Java的类是：android.os.Binder（<a href="http://aospxref.com/android-11.0.0_r21/xref/frameworks/base/core/java/android/os/Binder.java">http://aospxref.com/android-11.0.0_r21/xref/frameworks/base/core/java/android/os/Binder.java</a>）</li>
<li>GetMethodIDOrDie(env, clazz, “execTransact”, “(IJJI)Z”)<ul>
<li>函数是：android.os.Binder.execTransact()</li>
<li>(IJJI)Z表示：bool execTransact(int x1, long x2, long x3, int x4)<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/base/core/java/android/os/Binder.java</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Entry point from android_util_Binder.cpp&#x27;s onTransact</span></span><br><span class="line">@<span class="function">UnsupportedAppUsage</span></span><br><span class="line"><span class="function">    <span class="keyword">private</span> boolean <span class="title">execTransact</span><span class="params">(<span class="keyword">int</span> code, <span class="keyword">long</span> dataObj, <span class="keyword">long</span> replyObj,</span></span></span><br><span class="line"><span class="params"><span class="function">                                 <span class="keyword">int</span> flags)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// At that point, the parcel request headers haven&#x27;t been parsed so we do not know what</span></span><br><span class="line">    <span class="comment">// WorkSource the caller has set. Use calling uid as the default.</span></span><br><span class="line">    <span class="keyword">final</span> <span class="keyword">int</span> callingUid = Binder.<span class="built_in">getCallingUid</span>();</span><br><span class="line">    <span class="keyword">final</span> <span class="keyword">long</span> origWorkSource = ThreadLocalWorkSource.<span class="built_in">setUid</span>(callingUid);</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">execTransactInternal</span>(code, dataObj, replyObj, flags, callingUid);</span><br><span class="line">    &#125; finally &#123;</span><br><span class="line">        ThreadLocalWorkSource.<span class="built_in">restore</span>(origWorkSource);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
看到注释也能知道是C代码调用到了这里，细节就不讨论了。</li>
</ul>
</li>
</ul>
<h1 id="注册服务"><a href="#注册服务" class="headerlink" title="注册服务"></a>注册服务</h1><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/base/services/java/com/android/server/SystemServer.java</span></span><br><span class="line"></span><br><span class="line">connectivity = <span class="keyword">new</span> <span class="built_in">ConnectivityService</span>(</span><br><span class="line">        context, networkManagement, networkStats, networkPolicy);</span><br><span class="line">ServiceManager.<span class="built_in">addService</span>(Context.CONNECTIVITY_SERVICE, connectivity,</span><br><span class="line">        <span class="comment">/* allowIsolated= */</span> <span class="literal">false</span>,</span><br><span class="line">        DUMP_FLAG_PRIORITY_HIGH | DUMP_FLAG_PRIORITY_NORMAL);</span><br></pre></td></tr></table></figure>
<p>  SystemServer进程注册很多服务，其注册服务到方法很简单通过调用的是ServiceManager.java的addService()，那我们就从addService()开始分析。</p>
<h2 id="ServiceManager-addService"><a href="#ServiceManager-addService" class="headerlink" title="ServiceManager.addService()"></a>ServiceManager.addService()</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/base/core/java/android/os/ServiceManager.java</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">addService</span><span class="params">(String name, IBinder service, boolean allowIsolated,</span></span></span><br><span class="line"><span class="params"><span class="function">        <span class="keyword">int</span> dumpPriority)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="built_in">getIServiceManager</span>().<span class="built_in">addService</span>(name, service, allowIsolated, dumpPriority);</span><br><span class="line">    &#125; <span class="built_in"><span class="keyword">catch</span></span> (RemoteException e) &#123;</span><br><span class="line">        Log.<span class="built_in">e</span>(TAG, <span class="string">&quot;error in addService&quot;</span>, e);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>  获取ServiceManagerProxy对象，执行addService操作。</p>
<h2 id="ServiceManager-getIServiceManager"><a href="#ServiceManager-getIServiceManager" class="headerlink" title="ServiceManager.getIServiceManager()"></a>ServiceManager.getIServiceManager()</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/base/core/java/android/os/ServiceManager.java</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">static</span> IServiceManager <span class="title">getIServiceManager</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (sServiceManager != null) &#123;</span><br><span class="line">        <span class="keyword">return</span> sServiceManager;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Find the service manager</span></span><br><span class="line">    sServiceManager = ServiceManagerNative</span><br><span class="line">            .<span class="built_in">asInterface</span>(Binder.<span class="built_in">allowBlocking</span>(BinderInternal.<span class="built_in">getContextObject</span>()));</span><br><span class="line">    <span class="keyword">return</span> sServiceManager;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>  采用单例模式来获取ServiceManagerProxy对象。这里有三方法，先从BinderInternal.getContextObject()函数开始分析，其次是Binder.allowBlocking()函数，最后是ServiceManagerNative.asInterface()函数。</p>
<h3 id="BinderInternal-getContextObject"><a href="#BinderInternal-getContextObject" class="headerlink" title="BinderInternal.getContextObject()"></a>BinderInternal.getContextObject()</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/base/core/java/com/android/internal/os/BinderInternal.java</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> native IBinder <span class="title">getContextObject</span><span class="params">()</span></span>;</span><br></pre></td></tr></table></figure>
<p>BinderInternal.java中有一个native方法getContextObject()，通过JNI调用执行C的方法。在JNI的gBinderInternalMethods数组中找到了getContextObject的对应关系，即最终调用android_os_BinderInternal_getContextObject()方法。</p>
<h4 id="android-os-BinderInternal-getContextObject"><a href="#android-os-BinderInternal-getContextObject" class="headerlink" title="android_os_BinderInternal_getContextObject()"></a>android_os_BinderInternal_getContextObject()</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/base/core/jni/android_util_Binder.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> jobject <span class="title">android_os_BinderInternal_getContextObject</span><span class="params">(JNIEnv* env, jobject clazz)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    sp&lt;IBinder&gt; b = ProcessState::<span class="built_in">self</span>()-&gt;<span class="built_in">getContextObject</span>(<span class="literal">NULL</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">javaObjectForIBinder</span>(env, b);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>看到ProcessState::self()-&gt;getContextObject(NULL)这行代码，不能发现在<a href="https://i-rtfsc.github.io/archives/937d16d8.html">Binder机制03-Framework-Native</a>已经分析过，最终等价于 new BpBinder(0)，这里就不重复展开了。</p>
<h4 id="javaObjectForIBinder"><a href="#javaObjectForIBinder" class="headerlink" title="javaObjectForIBinder()"></a>javaObjectForIBinder()</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/base/core/jni/android_util_Binder.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function">jobject <span class="title">javaObjectForIBinder</span><span class="params">(JNIEnv* env, <span class="keyword">const</span> sp&lt;IBinder&gt;&amp; val)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// N.B. This function is called from a @FastNative JNI method, so don&#x27;t take locks around</span></span><br><span class="line">    <span class="comment">// calls to Java code or block the calling thread for a long time for any reason.</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (val == <span class="literal">NULL</span>) <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (val-&gt;<span class="built_in">checkSubclass</span>(&amp;gBinderOffsets)) &#123;</span><br><span class="line">        <span class="comment">//如果参数是JavaBBinder，返回用于创建它的Java对象；否则返回一个BinderProxy的对象。</span></span><br><span class="line">        jobject object = <span class="keyword">static_cast</span>&lt;JavaBBinder*&gt;(val.<span class="built_in">get</span>())-&gt;<span class="built_in">object</span>();</span><br><span class="line">        <span class="keyword">return</span> object;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//申请一个BinderProxyNativeData的内存</span></span><br><span class="line">    BinderProxyNativeData* nativeData = <span class="keyword">new</span> <span class="built_in">BinderProxyNativeData</span>();</span><br><span class="line">    nativeData-&gt;mOrgue = <span class="keyword">new</span> DeathRecipientList;</span><br><span class="line">    nativeData-&gt;mObject = val;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//创建BinderProxy对象，设置BinderProxy的相关参数，能够与JAVA层的BinderProx参与工作</span></span><br><span class="line">    jobject object = env-&gt;<span class="built_in">CallStaticObjectMethod</span>(gBinderProxyOffsets.mClass,</span><br><span class="line">            gBinderProxyOffsets.mGetInstance, (jlong) nativeData, (jlong) val.<span class="built_in">get</span>());</span><br><span class="line">    <span class="keyword">if</span> (env-&gt;<span class="built_in">ExceptionCheck</span>()) &#123;</span><br><span class="line">        <span class="comment">// In the exception case, getInstance still took ownership of nativeData.</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    BinderProxyNativeData* actualNativeData = <span class="built_in">getBPNativeData</span>(env, object);</span><br><span class="line">    <span class="keyword">if</span> (actualNativeData == nativeData) &#123;</span><br><span class="line">        <span class="comment">// Created a new Proxy</span></span><br><span class="line">        <span class="keyword">uint32_t</span> numProxies = gNumProxies.<span class="built_in">fetch_add</span>(<span class="number">1</span>, std::memory_order_relaxed);</span><br><span class="line">        <span class="keyword">uint32_t</span> numLastWarned = gProxiesWarned.<span class="built_in">load</span>(std::memory_order_relaxed);</span><br><span class="line">        <span class="keyword">if</span> (numProxies &gt;= numLastWarned + PROXY_WARN_INTERVAL) &#123;</span><br><span class="line">            <span class="comment">// Multiple threads can get here, make sure only one of them gets to</span></span><br><span class="line">            <span class="comment">// update the warn counter.</span></span><br><span class="line">            <span class="keyword">if</span> (gProxiesWarned.<span class="built_in">compare_exchange_strong</span>(numLastWarned,</span><br><span class="line">                        numLastWarned + PROXY_WARN_INTERVAL, std::memory_order_relaxed)) &#123;</span><br><span class="line">                <span class="built_in">ALOGW</span>(<span class="string">&quot;Unexpectedly many live BinderProxies: %d\n&quot;</span>, numProxies);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">delete</span> nativeData;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> object;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>javaObjectForIBinder()中，申请一个BinderProxyNativeData的内存，传入的BpBinder的对象地址保存到BinderProxyNativeData.mObject成员变量中，通过虚拟机的转换，BinderProxyNativeData在JAVA空间会被转换成 BinderProxy对象。<br>最终，BinderInternal.getContextObject()等价于 new BinderProxy()，所以getIServiceManager等价于new ServiceManagerProxy(new BinderProxy())。</p>
<h3 id="Binder-allowBlocking"><a href="#Binder-allowBlocking" class="headerlink" title="Binder.allowBlocking()"></a>Binder.allowBlocking()</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/base/core/java/android/os/Binder.java</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> IBinder <span class="title">allowBlocking</span><span class="params">(IBinder binder)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (binder instanceof BinderProxy) &#123;</span><br><span class="line">            <span class="comment">//如果binder是代理类，则设置非阻塞式</span></span><br><span class="line">            ((BinderProxy) binder).mWarnOnBlocking = <span class="literal">false</span>;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (binder != null &amp;&amp; binder.<span class="built_in">getInterfaceDescriptor</span>() != null</span><br><span class="line">                &amp;&amp; binder.<span class="built_in">queryLocalInterface</span>(binder.<span class="built_in">getInterfaceDescriptor</span>()) == null) &#123;</span><br><span class="line">            <span class="comment">//如果binder是本地对象，binder描述符不为空，且和本地binder描述符不相同</span></span><br><span class="line">            Log.<span class="built_in">w</span>(TAG, <span class="string">&quot;Unable to allow blocking on interface &quot;</span> + binder);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="built_in"><span class="keyword">catch</span></span> (RemoteException ignored) &#123;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> binder;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>允许在给定接口上阻塞调用，重写请求的{setWarnOnBlocking（boolean）}值。</li>
<li>只有当您完全确定远程接口是一个永远无法升级的内置系统组件时，才应该很少调用此命令。尤其是，决不能对包托管的、可升级或替换的接口调用此命令，否则，如果远程接口接入，将有系统不稳定的风险。</li>
</ul>
<h3 id="ServiceManagerNative-asInterface"><a href="#ServiceManagerNative-asInterface" class="headerlink" title="ServiceManagerNative.asInterface()"></a>ServiceManagerNative.asInterface()</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/base/core/java/android/os/ServiceManagerNative.java</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> IServiceManager <span class="title">asInterface</span><span class="params">(IBinder obj)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (obj == null) &#123;</span><br><span class="line">        <span class="keyword">return</span> null;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// ServiceManager is never local</span></span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">ServiceManagerProxy</span>(obj);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ServiceManagerProxy</span> <span class="title">implements</span> <span class="title">IServiceManager</span> &#123;</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">ServiceManagerProxy</span><span class="params">(IBinder remote)</span> </span>&#123;</span><br><span class="line">        mRemote = remote;</span><br><span class="line">        mServiceManager = IServiceManager.Stub.<span class="built_in">asInterface</span>(remote);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> IBinder mRemote;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> IServiceManager mServiceManager;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>ServiceManagerProxy是ServiceManagerNative类的内部类, mRemote为BinderProxy对象，该BinderProxy对象对应于BpBinder(0)，其作为binder代理端，指向native的层的Service Manager。<br>ServiceManager.getIServiceManager()最终等价于new ServiceManagerProxy(new BinderProxy())。所以getIServiceManager().addService()也就是ServiceManagerNative.addService();<br>Framework层的ServiceManager的调用实际的工作确实交给了ServiceManagerProxy的成员变量BinderProxy；而BinderProxy通过JNI的方式，最终会调用BpBinder对象；可见上层binder结构的核心功能依赖native架构的服务来完成的。</p>
<h2 id="ServiceManagerProxy-addService"><a href="#ServiceManagerProxy-addService" class="headerlink" title="ServiceManagerProxy.addService()"></a>ServiceManagerProxy.addService()</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/base/core/java/android/os/ServiceManagerNative.java</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ServiceManagerProxy</span> <span class="title">implements</span> <span class="title">IServiceManager</span> &#123;</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">ServiceManagerProxy</span><span class="params">(IBinder remote)</span> </span>&#123;</span><br><span class="line">        mRemote = remote;</span><br><span class="line">        mServiceManager = IServiceManager.Stub.<span class="built_in">asInterface</span>(remote);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">addService</span><span class="params">(String name, IBinder service, boolean allowIsolated, <span class="keyword">int</span> dumpPriority)</span></span></span><br><span class="line"><span class="function">            throws RemoteException </span>&#123;</span><br><span class="line">        mServiceManager.<span class="built_in">addService</span>(name, service, allowIsolated, dumpPriority);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> IServiceManager mServiceManager;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>  IServiceManager其实就是<a href="http://aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/aidl/android/os/IServiceManager.aidl#30">IServiceManager.aidl</a>，最终调到ServiceManager.ddService()方法里。<a href="https://i-rtfsc.github.io/archives/937d16d8.html">Binder机制03-Framework-Native</a>有分析ServiceManager.ddService()。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/cmds/servicemanager/ServiceManager.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function">Status <span class="title">ServiceManager::addService</span><span class="params">(<span class="keyword">const</span> std::string&amp; name, <span class="keyword">const</span> sp&lt;IBinder&gt;&amp; binder, <span class="keyword">bool</span> allowIsolated, <span class="keyword">int32_t</span> dumpPriority)</span> </span>&#123;</span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="comment">//添加 Service 到 mNameToService 中，完成 Binder Service 注册过程</span></span><br><span class="line">    mNameToService[name] = Service &#123;</span><br><span class="line">        .binder = binder,</span><br><span class="line">        .allowIsolated = allowIsolated,</span><br><span class="line">        .dumpPriority = dumpPriority,</span><br><span class="line">        .debugPid = ctx.debugPid,</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">auto</span> it = mNameToRegistrationCallback.<span class="built_in">find</span>(name);</span><br><span class="line">    <span class="keyword">if</span> (it != mNameToRegistrationCallback.<span class="built_in">end</span>()) &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">const</span> sp&lt;IServiceCallback&gt;&amp; cb : it-&gt;second) &#123;</span><br><span class="line">            mNameToService[name].guaranteeClient = <span class="literal">true</span>;</span><br><span class="line">            <span class="comment">// permission checked in registerForNotifications</span></span><br><span class="line">            cb-&gt;<span class="built_in">onRegistration</span>(name, binder);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> Status::<span class="built_in">ok</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>Framework层的ServiceManager的调用实际的工作确实交给ServiceManagerProxy的成员变量BinderProxy；而BinderProxy通过jni方式，最终会调用BpBinder对象；可见上层binder架构的核心功能依赖native架构的服务来完成的。</p>
<h1 id="获取服务"><a href="#获取服务" class="headerlink" title="获取服务"></a>获取服务</h1><h2 id="ServiceManager-getService"><a href="#ServiceManager-getService" class="headerlink" title="ServiceManager.getService()"></a>ServiceManager.getService()</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/base/core/java/android/os/ServiceManager.java</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> IBinder <span class="title">getService</span><span class="params">(String name)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="comment">//先从缓存中查看</span></span><br><span class="line">        IBinder service = sCache.<span class="built_in">get</span>(name);</span><br><span class="line">        <span class="keyword">if</span> (service != null) &#123;</span><br><span class="line">            <span class="keyword">return</span> service;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="comment">//从Native层服务列表中取服务对象</span></span><br><span class="line">            <span class="keyword">return</span> Binder.<span class="built_in">allowBlocking</span>(<span class="built_in">rawGetService</span>(name));</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="built_in"><span class="keyword">catch</span></span> (RemoteException e) &#123;</span><br><span class="line">        Log.<span class="built_in">e</span>(TAG, <span class="string">&quot;error in getService&quot;</span>, e);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> null;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">static</span> IBinder <span class="title">rawGetService</span><span class="params">(String name)</span> throws RemoteException </span>&#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="comment">//即为ServiceManagerProxy.getService()</span></span><br><span class="line">    <span class="keyword">final</span> IBinder binder = <span class="built_in">getIServiceManager</span>().<span class="built_in">getService</span>(name);</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">return</span> binder;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>先从缓存中查看</li>
<li>缓存中没有则从Native层服务列表中取服务对象即为ServiceManagerProxy.getService()</li>
</ul>
<h2 id="ServiceManagerProxy-getService"><a href="#ServiceManagerProxy-getService" class="headerlink" title="ServiceManagerProxy.getService()"></a>ServiceManagerProxy.getService()</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/base/core/java/android/os/ServiceManagerNative.java</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ServiceManagerProxy</span> <span class="title">implements</span> <span class="title">IServiceManager</span> &#123;</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">ServiceManagerProxy</span><span class="params">(IBinder remote)</span> </span>&#123;</span><br><span class="line">        mRemote = remote;</span><br><span class="line">        mServiceManager = IServiceManager.Stub.<span class="built_in">asInterface</span>(remote);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> IBinder <span class="title">getService</span><span class="params">(String name)</span> throws RemoteException </span>&#123;</span><br><span class="line">        <span class="comment">// Same as checkService (old versions of servicemanager had both methods).</span></span><br><span class="line">        <span class="keyword">return</span> mServiceManager.<span class="built_in">checkService</span>(name);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> IServiceManager mServiceManager;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>IServiceManager其实就是<a href="http://aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/aidl/android/os/IServiceManager.aidl#30">IServiceManager.aidl</a>，最终调到ServiceManager.checkService()方法里。<a href="https://i-rtfsc.github.io/archives/937d16d8.html">Binder机制03-Framework-Native</a>有分析ServiceManager.checkService()。</p>
<h2 id="ServiceManager-checkService"><a href="#ServiceManager-checkService" class="headerlink" title="ServiceManager.checkService()"></a>ServiceManager.checkService()</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">ttp:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/cmds/servicemanager/ServiceManager.h</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//定义Service结构体</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Service</span> &#123;</span></span><br><span class="line">    sp&lt;IBinder&gt; binder; <span class="comment">// not null</span></span><br><span class="line">    <span class="keyword">bool</span> allowIsolated;</span><br><span class="line">    <span class="keyword">int32_t</span> dumpPriority;</span><br><span class="line">    <span class="keyword">bool</span> hasClients = <span class="literal">false</span>; <span class="comment">// notifications sent on true -&gt; false.</span></span><br><span class="line">    <span class="keyword">bool</span> guaranteeClient = <span class="literal">false</span>; <span class="comment">// forces the client check to true</span></span><br><span class="line">    <span class="keyword">pid_t</span> debugPid = <span class="number">0</span>; <span class="comment">// the process in which this service runs</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// the number of clients of the service, including servicemanager itself</span></span><br><span class="line">    <span class="function"><span class="keyword">ssize_t</span> <span class="title">getNodeStrongRefCount</span><span class="params">()</span></span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> ServiceMap = std::map&lt;std::string, Service&gt;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//用于保存添加进来的Services</span></span><br><span class="line">ServiceMap mNameToService;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/cmds/servicemanager/ServiceManager.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function">Status <span class="title">ServiceManager::checkService</span><span class="params">(<span class="keyword">const</span> std::string&amp; name, sp&lt;IBinder&gt;* outBinder)</span> </span>&#123;</span><br><span class="line">    *outBinder = <span class="built_in">tryGetService</span>(name, <span class="literal">false</span>);</span><br><span class="line">    <span class="comment">// returns ok regardless of result for legacy reasons</span></span><br><span class="line">    <span class="keyword">return</span> Status::<span class="built_in">ok</span>();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function">sp&lt;IBinder&gt; <span class="title">ServiceManager::tryGetService</span><span class="params">(<span class="keyword">const</span> std::string&amp; name, <span class="keyword">bool</span> startIfNotFound)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">auto</span> ctx = mAccess-&gt;<span class="built_in">getCallingContext</span>();</span><br><span class="line"></span><br><span class="line">    sp&lt;IBinder&gt; out;</span><br><span class="line">    Service* service = <span class="literal">nullptr</span>;</span><br><span class="line">    <span class="comment">//查找mNameToServices是否存在名字为name的Service</span></span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">auto</span> it = mNameToService.<span class="built_in">find</span>(name); it != mNameToService.<span class="built_in">end</span>()) &#123;</span><br><span class="line">        service = &amp;(it-&gt;second);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (!service-&gt;allowIsolated) &#123;</span><br><span class="line">            <span class="keyword">uid_t</span> appid = <span class="built_in">multiuser_get_app_id</span>(ctx.uid);</span><br><span class="line">            <span class="keyword">bool</span> isIsolated = appid &gt;= AID_ISOLATED_START &amp;&amp; appid &lt;= AID_ISOLATED_END;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (isIsolated) &#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">nullptr</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        out = service-&gt;binder;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!mAccess-&gt;<span class="built_in">canFind</span>(ctx, name)) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nullptr</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!out &amp;&amp; startIfNotFound) &#123;</span><br><span class="line">        <span class="built_in">tryStartService</span>(name);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (out) &#123;</span><br><span class="line">        <span class="comment">// Setting this guarantee each time we hand out a binder ensures that the client-checking</span></span><br><span class="line">        <span class="comment">// loop knows about the event even if the client immediately drops the service</span></span><br><span class="line">        service-&gt;guaranteeClient = <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> out;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


<h2 id="小结-1"><a href="#小结-1" class="headerlink" title="小结"></a>小结</h2><p>和注册服务类似，都是需要依赖Native层的接口与Binder驱动通信，获取服务主要是从Native的ServieManager取到Binder对象。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>Framework层获取服务、注册服务，其实都是由JAVA层的ServiceManager代理ServiecManagerProxy，通过Binder驱动，访问Native层的ServiceManager，进行服务注册和获取动作。native层中，binder是C/S架构，分为Bn端(Server)和Bp端(Client)。对于Java层在命令与架构上非常相近，同时实现了一套IPC通信架构。<br><img src="https://cdn.nlark.com/yuque/0/2021/png/1759879/1634455950233-80899488-0715-4c97-915b-edea13872520.png#clientId=u971b965b-56bd-4&from=paste&id=u7a75d73d&margin=%5Bobject%20Object%5D&name=image.png&originHeight=971&originWidth=1409&originalType=binary&ratio=1&size=268750&status=done&style=none&taskId=ueff7d322-d4df-4778-9f14-e2d248ef2fd" alt="image.png"></p>
<ul>
<li>上层framework层的Binder逻辑是建立在Native层架构基础上的，核心逻辑都是交于Native层来处理</li>
<li>framework层的ServiceManager类与Native层的功能并不完全对应，framework层的ServiceManager的实现对最终是通过BinderProxy传递给Native层来完成的。</li>
<li>ServiceManager：通过getIServiceManager方法获取的是ServiceManagerProxy对象。ServiceMnager的addService()，getService()实际工作都交给ServiceManagerProxy的相应方法来处理。</li>
<li>ServiceManagerProxy：其成员变量mRemote指向BinderProxy对象，ServiceManagerProxy的addService()，getService()方法最终是交给mRemote去完成。</li>
<li>ServiceManagerNative：其方法asInterface()返回的是ServiceManagerProxy对象，ServiceManager便是借助ServiceManagerNative类来找到ServiceManagerProxy。</li>
<li>Binder：其成员mObject和方法execTransact()用于native方法</li>
<li>BinderInternal：内部有一个GcWatcher类，用于处理和调试与Binder相关的拦击回收。</li>
<li>IBinder：接口中常量FLAG_ONEWAY:客户端利用binder跟服务端通信是阻塞式的，但如果设置了FLAG_ONEWAY，这成为非阻塞的调用方式，客户端能立即返回，服务端采用回调方式来通知客户端完成情况。另外IBinder接口有一个内部接口DeathDecipent(死亡通告)。</li>
</ul>
<h1 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h1><p><a href="https://www.jianshu.com/p/ee56313a2c9e">Android Binder机制原理java层系列二（有图有代码很详细）</a><br><a href="https://www.cnblogs.com/samchen2009/p/3316001.html">图解Android - Binder 和 Service</a></p>
]]></content>
      <categories>
        <category>Android</category>
      </categories>
      <tags>
        <tag>Android</tag>
        <tag>Binder</tag>
      </tags>
  </entry>
  <entry>
    <title>Binder机制03-Framework-Native</title>
    <url>/archives/937d16d8.html</url>
    <content><![CDATA[<span id="more"></span>
<meta name="referrer" content="no-referrer"/>


<h1 id="Binder机制文章列表"><a href="#Binder机制文章列表" class="headerlink" title="Binder机制文章列表"></a>Binder机制文章列表</h1><p><a href="https://i-rtfsc.github.io/archives/d6aea0f1.html">Binder概述</a><br><a href="https://i-rtfsc.github.io/archives/3e87e0ce.html">Binder机制01-驱动</a><br><a href="https://i-rtfsc.github.io/archives/f17b1d52.html">Binder机制02-ServiceManager</a><br><a href="https://i-rtfsc.github.io/archives/937d16d8.html">Binder机制03-Framework-Native</a><br><a href="https://i-rtfsc.github.io/archives/73029508.html">Binder机制04-Framework-Jave</a><br><a href="https://i-rtfsc.github.io/archives/ed3eb50b.html">Binder机制05-AIDL</a><br><a href="https://i-rtfsc.github.io/archives/4e7f84b4.html">Binder机制06-框架</a><br><a href="https://i-rtfsc.github.io/archives/d30c7ecb.html">Binder机制07-实例分析</a><br><a href="https://i-rtfsc.github.io/archives/720ded9.html">Binder机制08-总结</a><br><a href="https://i-rtfsc.github.io/archives/f1684132.html">Ashmem简介（Android IPC传输大数据）</a></p>
<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>Framework是一个中间层，它对接了底层实现，封装了复杂的内部逻辑，并提供供外部使用的接口。Framework层是应用程序开发的基础。Binder Framework层分为C++和Java两个部分，为了达到功能的复用，中间通过JNI进行衔接。<br>Binder Framework的C++部分</p>
<ul>
<li>头文件位于这个路径：<a href="http://aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/include/binder/">http://aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/include/binder/</a></li>
<li>实现位于这个路径：<a href="http://aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/">http://aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/</a></li>
</ul>
<p>Binder库最终会编译成一个动态链接库：libbinder.so，供其他进程链接使用。</p>
<p>在上一章节中，我们了解到了ServiceManager的启动过程，注册、获取服务的细节。如果让我们开发应该Native服务，我们要怎么做呢？我们以media的服务来进行分析。在了解如何开发native服务之前我们先了解Native层的Binder架构。</p>
<h1 id="Native层的Binder架构"><a href="#Native层的Binder架构" class="headerlink" title="Native层的Binder架构"></a>Native层的Binder架构</h1><p>Binder Framework Native(libbinder)中，将实现分为Proxy和Native两端。</p>
<ul>
<li>Proxy对应Client端，是服务对外提供的接口。类名中带有小写字母p的（例如BpInterface），就是指Proxy端。</li>
<li>Native是服务实现的一端，对应了上文提到的Server端。类名带有小写字母n的（例如BnInterface），就是指Native端。<blockquote>
<p>Proxy代表了调用方，通常与服务的实现不在同一个进程，因此下文中，我们也称Proxy端为“远程”端。<br>Native端是服务实现的自身，因此下文中，我们也称Native端为“本地”端。</p>
</blockquote>
</li>
</ul>
<h2 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h2><p>在前面的Binder概述、Binder驱动等章节我们都聊到了BpBinder、BBinder。我们先用一个表格来列举libbinder中的主要类。</p>
<table>
<thead>
<tr>
<th>类名</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>BpRefBase</td>
<td>RefBase的子类，提供remote()方法获取远程Binder</td>
</tr>
<tr>
<td>IInterface</td>
<td>Binder服务接口的基类，Binder服务通常需要同时提供本地接口和远程接口</td>
</tr>
<tr>
<td>BpInterface</td>
<td>远程接口的基类，远程接口是供客户端调用的接口集</td>
</tr>
<tr>
<td>BnInterface</td>
<td>本地接口的基类，本地接口是需要服务中真正实现的接口集</td>
</tr>
<tr>
<td>IBiner</td>
<td>Binder对象的基类，BBinder和BpBinder都是这个类的子类</td>
</tr>
<tr>
<td>BpBinder</td>
<td>远程Binder，这个类提供transact方法来发送请求，BpXXX实现中会用到</td>
</tr>
<tr>
<td>BBinder</td>
<td>本地Binder，服务实现方的基类，提供了onTransact接口来接收请求</td>
</tr>
<tr>
<td>ProcessState</td>
<td>代表了使用Binder的进程</td>
</tr>
<tr>
<td>IPCThreadState</td>
<td>代表了使用Binder的线程，这个类中封装了与Binder驱动通信的逻辑</td>
</tr>
<tr>
<td>Parcel</td>
<td>在Binder上传递的数据的包装器</td>
</tr>
</tbody></table>
<p>下图描述了Binder Framework Native(libbinder)主要类的关系。<br><img src="https://cdn.nlark.com/yuque/0/2021/png/1759879/1634111763519-79b3c0f2-cf57-4aaa-bd66-42d25abbadf0.png#clientId=ucfce5313-484a-4&from=paste&id=u900bfb47&margin=%5Bobject%20Object%5D&name=image.png&originHeight=2018&originWidth=2000&originalType=url&ratio=1&size=451147&status=done&style=none&taskId=u2315f0bc-66bd-43c9-9d49-a4008e39484" alt="image.png"></p>
<blockquote>
<p>Binder服务的实现类（图中紫色部分）通常都会遵守下面的命名规则：</p>
<ul>
<li>服务的接口使用I字母作为前缀</li>
<li>远程接口使用Bp作为前缀</li>
<li>本地接口使用Bn作为前缀</li>
</ul>
</blockquote>
<h2 id="IBinder-h"><a href="#IBinder-h" class="headerlink" title="IBinder.h"></a>IBinder.h</h2><p>IBinder从Refbase继承而来，一提供强弱指针计数能力。IBinder有几个方法比较重要：</p>
<ul>
<li>localBinder()：获取本地Binder对象</li>
<li>remoteBinder()：获取远程Binder对象</li>
<li>transact()：进行一次Binder操作</li>
<li>queryLocalInterface()：尝试获取本地Binder，如何失败返回NULL</li>
<li>getInterfaceDescriptor()：获取Binder的服务接口描述，其实就是Binder服务的唯一标识</li>
<li>isBinderAlive()：查询Binder服务是否还活着</li>
<li>pingBinder()：发送PING_TRANSACTION给Binder服务</li>
</ul>
<p><a href="http://aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/include/binder/IBinder.h">IBinder.h</a>的代码可以点链接查看。对于IBinder中的方法，基本都是没有实现的，这些方法的实现都交给继承它的子类来实现，那下面直接看BpBinder的内容。</p>
<h2 id="BpBinder-h"><a href="#BpBinder-h" class="headerlink" title="BpBinder.h"></a>BpBinder.h</h2><p><a href="http://aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/include/binder/BpBinder.h">BpBinder.h</a>的代码比较多不贴出来了，可以点击链接自行查看代码。我们主要来分析上述提到的几个函数的具体实现。<br>​</p>
<h3 id="BpBinder构造函数"><a href="#BpBinder构造函数" class="headerlink" title="BpBinder构造函数"></a>BpBinder构造函数</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/BpBinder.cpp</span></span><br><span class="line"></span><br><span class="line">BpBinder::<span class="built_in">BpBinder</span>(<span class="keyword">int32_t</span> handle, <span class="keyword">int32_t</span> trackedUid)</span><br><span class="line">    : <span class="built_in">mHandle</span>(handle)</span><br><span class="line">    , <span class="built_in">mStability</span>(<span class="number">0</span>)</span><br><span class="line">    , <span class="built_in">mAlive</span>(<span class="number">1</span>)</span><br><span class="line">    , <span class="built_in">mObitsSent</span>(<span class="number">0</span>)</span><br><span class="line">    , <span class="built_in">mObituaries</span>(<span class="literal">nullptr</span>)</span><br><span class="line">    , <span class="built_in">mTrackedUid</span>(trackedUid)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">ALOGV</span>(<span class="string">&quot;Creating BpBinder %p handle %d\n&quot;</span>, <span class="keyword">this</span>, mHandle);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">extendObjectLifetime</span>(OBJECT_LIFETIME_WEAK);</span><br><span class="line">    IPCThreadState::<span class="built_in">self</span>()-&gt;<span class="built_in">incWeakHandle</span>(handle, <span class="keyword">this</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>将传入的handle值保存到mHandle成员变量里</li>
<li>mAlive设置为1</li>
<li>extendObjectLifetime将对象改为弱引用控制</li>
<li>通过IPCThreadState的incWeakHandle增加Binder Service的如引用计数值。<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">&#125;http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/IPCThreadState.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">IPCThreadState::incWeakHandle</span><span class="params">(<span class="keyword">int32_t</span> handle, BpBinder *proxy)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">LOG_REMOTEREFS</span>(<span class="string">&quot;IPCThreadState::incWeakHandle(%d)\n&quot;</span>, handle);</span><br><span class="line">    mOut.<span class="built_in">writeInt32</span>(BC_INCREFS);</span><br><span class="line">    mOut.<span class="built_in">writeInt32</span>(handle);</span><br><span class="line">    <span class="comment">// Create a temp reference until the driver has handled this command.</span></span><br><span class="line">    proxy-&gt;<span class="built_in">getWeakRefs</span>()-&gt;<span class="built_in">incWeak</span>(mProcess.<span class="built_in">get</span>());</span><br><span class="line">    mPostWriteWeakDerefs.<span class="built_in">push</span>(proxy-&gt;<span class="built_in">getWeakRefs</span>());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
这一步只是将BC_INCREFS请求写到mOut中，还没有发送出去。</li>
</ul>
<blockquote>
<p>除了incWeakHandle函数外还有decWeakHandle，incStrongHandle和decStrongHandle与Binder协议中的其他命令对应起来。</p>
</blockquote>
<h3 id="getInterfaceDescriptor"><a href="#getInterfaceDescriptor" class="headerlink" title="getInterfaceDescriptor()"></a>getInterfaceDescriptor()</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/BpBinder.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">const</span> String16&amp; <span class="title">BpBinder::getInterfaceDescriptor</span><span class="params">()</span> <span class="keyword">const</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">isDescriptorCached</span>() == <span class="literal">false</span>) &#123;</span><br><span class="line">        Parcel send, reply;</span><br><span class="line">        <span class="comment">// do the IPC without a lock held.</span></span><br><span class="line">        <span class="comment">//通过BpBinder的transact函数将INTERFACE_TRANSACTION请求发送出去</span></span><br><span class="line">        <span class="keyword">status_t</span> err = <span class="keyword">const_cast</span>&lt;BpBinder*&gt;(<span class="keyword">this</span>)-&gt;<span class="built_in">transact</span>(</span><br><span class="line">                INTERFACE_TRANSACTION, send, &amp;reply);</span><br><span class="line">        <span class="keyword">if</span> (err == NO_ERROR) &#123;</span><br><span class="line">          <span class="comment">//读取返回值得内容</span></span><br><span class="line">            <span class="function">String16 <span class="title">res</span><span class="params">(reply.readString16())</span></span>;</span><br><span class="line">            Mutex::Autolock _l(mLock);</span><br><span class="line">            <span class="comment">// mDescriptorCache could have been assigned while the lock was</span></span><br><span class="line">            <span class="comment">// released.</span></span><br><span class="line">            <span class="keyword">if</span> (mDescriptorCache.<span class="built_in">size</span>() == <span class="number">0</span>)</span><br><span class="line">                mDescriptorCache = res;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// we&#x27;re returning a reference to a non-static object here. Usually this</span></span><br><span class="line">    <span class="comment">// is not something smart to do, however, with binder objects it is</span></span><br><span class="line">    <span class="comment">// (usually) safe because they are reference-counted.</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> mDescriptorCache;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这里是通过BpBinder的transact函数将请求发送出去</p>
<h3 id="transact"><a href="#transact" class="headerlink" title="transact()"></a>transact()</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/BpBinder.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// NOLINTNEXTLINE(google-default-arguments)</span></span><br><span class="line"><span class="function"><span class="keyword">status_t</span> <span class="title">BpBinder::transact</span><span class="params">(</span></span></span><br><span class="line"><span class="params"><span class="function">    <span class="keyword">uint32_t</span> code, <span class="keyword">const</span> Parcel&amp; data, Parcel* reply, <span class="keyword">uint32_t</span> flags)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// Once a binder has died, it will never come back to life.</span></span><br><span class="line">    <span class="keyword">if</span> (mAlive) &#123;</span><br><span class="line">        ...</span><br><span class="line">        <span class="comment">//调用IPCThreadState的transact函数</span></span><br><span class="line">        <span class="keyword">status_t</span> status = IPCThreadState::<span class="built_in">self</span>()-&gt;<span class="built_in">transact</span>(</span><br><span class="line">            mHandle, code, data, reply, flags);</span><br><span class="line">        <span class="keyword">if</span> (status == DEAD_OBJECT) mAlive = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> status;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> DEAD_OBJECT;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>BpBinder这个代理端的数据发送其实也不是自己执行数据传输的操作，而是调用IPCThreadState的transact()函数进行数据传输。</p>
<h4 id="IPCThreadState-transact"><a href="#IPCThreadState-transact" class="headerlink" title="IPCThreadState.transact()"></a>IPCThreadState.transact()</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/IPCThreadState.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">status_t</span> <span class="title">IPCThreadState::transact</span><span class="params">(<span class="keyword">int32_t</span> handle,</span></span></span><br><span class="line"><span class="params"><span class="function">                                  <span class="keyword">uint32_t</span> code, <span class="keyword">const</span> Parcel&amp; data,</span></span></span><br><span class="line"><span class="params"><span class="function">                                  Parcel* reply, <span class="keyword">uint32_t</span> flags)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">status_t</span> err;</span><br><span class="line"></span><br><span class="line">    flags |= TF_ACCEPT_FDS;</span><br><span class="line"></span><br><span class="line">    ...</span><br><span class="line">    <span class="comment">//调用writeTransactionData，将data中的数据打包成binder_transaction_data结构并写入到mOut中</span></span><br><span class="line">    err = <span class="built_in">writeTransactionData</span>(BC_TRANSACTION, flags, handle, code, data, <span class="literal">nullptr</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (err != NO_ERROR) &#123;</span><br><span class="line">        <span class="keyword">if</span> (reply) reply-&gt;<span class="built_in">setError</span>(err);</span><br><span class="line">        <span class="keyword">return</span> (mLastError = err);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> ((flags &amp; TF_ONE_WAY) == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">UNLIKELY</span>(mCallRestriction != ProcessState::CallRestriction::NONE)) &#123;</span><br><span class="line">            <span class="keyword">if</span> (mCallRestriction == ProcessState::CallRestriction::ERROR_IF_NOT_ONEWAY) &#123;</span><br><span class="line">                <span class="built_in">ALOGE</span>(<span class="string">&quot;Process making non-oneway call (code: %u) but is restricted.&quot;</span>, code);</span><br><span class="line">                CallStack::<span class="built_in">logStack</span>(<span class="string">&quot;non-oneway call&quot;</span>, CallStack::<span class="built_in">getCurrent</span>(<span class="number">10</span>).<span class="built_in">get</span>(),</span><br><span class="line">                    ANDROID_LOG_ERROR);</span><br><span class="line">            &#125; <span class="keyword">else</span> <span class="comment">/* FATAL_IF_NOT_ONEWAY */</span> &#123;</span><br><span class="line">                <span class="built_in">LOG_ALWAYS_FATAL</span>(<span class="string">&quot;Process may not make oneway calls (code: %u).&quot;</span>, code);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        ...</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (reply) &#123;</span><br><span class="line">            <span class="comment">//和Binder驱动通信，等待数据回复</span></span><br><span class="line">            err = <span class="built_in">waitForResponse</span>(reply);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            Parcel fakeReply;</span><br><span class="line">            <span class="comment">//和Binder驱动通信，等待数据回复</span></span><br><span class="line">            err = <span class="built_in">waitForResponse</span>(&amp;fakeReply);</span><br><span class="line">        &#125;</span><br><span class="line">        ...</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="comment">//和Binder驱动通信，等待数据回复</span></span><br><span class="line">        err = <span class="built_in">waitForResponse</span>(<span class="literal">nullptr</span>, <span class="literal">nullptr</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> err;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>调用writeTransactionData，将Parcel类变量data打包成一个binder_transaction_data，然后将其写到mOut中</li>
<li>调用waitForResponse，等待binder驱动处理数据</li>
</ul>
<h4 id="IPCThreadState-waitForResponse"><a href="#IPCThreadState-waitForResponse" class="headerlink" title="IPCThreadState.waitForResponse()"></a>IPCThreadState.waitForResponse()</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/IPCThreadState.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">status_t</span> <span class="title">IPCThreadState::waitForResponse</span><span class="params">(Parcel *reply, <span class="keyword">status_t</span> *acquireResult)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">uint32_t</span> cmd;</span><br><span class="line">    <span class="keyword">int32_t</span> err;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">        <span class="comment">//Binder驱动进行交互</span></span><br><span class="line">        <span class="keyword">if</span> ((err=<span class="built_in">talkWithDriver</span>()) &lt; NO_ERROR) <span class="keyword">break</span>;</span><br><span class="line">        err = mIn.<span class="built_in">errorCheck</span>();</span><br><span class="line">        <span class="keyword">if</span> (err &lt; NO_ERROR) <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">if</span> (mIn.<span class="built_in">dataAvail</span>() == <span class="number">0</span>) <span class="keyword">continue</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment">//拿到cmd</span></span><br><span class="line">        cmd = (<span class="keyword">uint32_t</span>)mIn.<span class="built_in">readInt32</span>();</span><br><span class="line"></span><br><span class="line">        <span class="built_in"><span class="keyword">switch</span></span> (cmd) &#123;</span><br><span class="line">        <span class="keyword">case</span> BR_TRANSACTION_COMPLETE:</span><br><span class="line">            <span class="keyword">if</span> (!reply &amp;&amp; !acquireResult) <span class="keyword">goto</span> finish;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">case</span> BR_DEAD_REPLY:</span><br><span class="line">            err = DEAD_OBJECT;</span><br><span class="line">            <span class="keyword">goto</span> finish;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">case</span> BR_FAILED_REPLY:</span><br><span class="line">            err = FAILED_TRANSACTION;</span><br><span class="line">            <span class="keyword">goto</span> finish;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">case</span> BR_FROZEN_REPLY:</span><br><span class="line">            err = FAILED_TRANSACTION;</span><br><span class="line">            <span class="keyword">goto</span> finish;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">case</span> BR_ACQUIRE_RESULT:</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="built_in">ALOG_ASSERT</span>(acquireResult != <span class="literal">NULL</span>, <span class="string">&quot;Unexpected brACQUIRE_RESULT&quot;</span>);</span><br><span class="line">                <span class="keyword">const</span> <span class="keyword">int32_t</span> result = mIn.<span class="built_in">readInt32</span>();</span><br><span class="line">                <span class="keyword">if</span> (!acquireResult) <span class="keyword">continue</span>;</span><br><span class="line">                *acquireResult = result ? NO_ERROR : INVALID_OPERATION;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">goto</span> finish;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">case</span> BR_REPLY:</span><br><span class="line">            &#123;</span><br><span class="line">                binder_transaction_data tr;</span><br><span class="line">                err = mIn.<span class="built_in">read</span>(&amp;tr, <span class="built_in"><span class="keyword">sizeof</span></span>(tr));</span><br><span class="line">                <span class="built_in">ALOG_ASSERT</span>(err == NO_ERROR, <span class="string">&quot;Not enough command data for brREPLY&quot;</span>);</span><br><span class="line">                <span class="keyword">if</span> (err != NO_ERROR) <span class="keyword">goto</span> finish;</span><br><span class="line"></span><br><span class="line">                <span class="keyword">if</span> (reply) &#123;</span><br><span class="line">                    <span class="keyword">if</span> ((tr.flags &amp; TF_STATUS_CODE) == <span class="number">0</span>) &#123;</span><br><span class="line">                        reply-&gt;<span class="built_in">ipcSetDataReference</span>(</span><br><span class="line">                            <span class="keyword">reinterpret_cast</span>&lt;<span class="keyword">const</span> <span class="keyword">uint8_t</span>*&gt;(tr.data.ptr.buffer),</span><br><span class="line">                            tr.data_size,</span><br><span class="line">                            <span class="keyword">reinterpret_cast</span>&lt;<span class="keyword">const</span> <span class="keyword">binder_size_t</span>*&gt;(tr.data.ptr.offsets),</span><br><span class="line">                            tr.offsets_size/<span class="built_in"><span class="keyword">sizeof</span></span>(<span class="keyword">binder_size_t</span>),</span><br><span class="line">                            freeBuffer, <span class="keyword">this</span>);</span><br><span class="line">                    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                        err = *<span class="keyword">reinterpret_cast</span>&lt;<span class="keyword">const</span> <span class="keyword">status_t</span>*&gt;(tr.data.ptr.buffer);</span><br><span class="line">                        <span class="built_in">freeBuffer</span>(<span class="literal">nullptr</span>,</span><br><span class="line">                            <span class="keyword">reinterpret_cast</span>&lt;<span class="keyword">const</span> <span class="keyword">uint8_t</span>*&gt;(tr.data.ptr.buffer),</span><br><span class="line">                            tr.data_size,</span><br><span class="line">                            <span class="keyword">reinterpret_cast</span>&lt;<span class="keyword">const</span> <span class="keyword">binder_size_t</span>*&gt;(tr.data.ptr.offsets),</span><br><span class="line">                            tr.offsets_size/<span class="built_in"><span class="keyword">sizeof</span></span>(<span class="keyword">binder_size_t</span>), <span class="keyword">this</span>);</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="built_in">freeBuffer</span>(<span class="literal">nullptr</span>,</span><br><span class="line">                        <span class="keyword">reinterpret_cast</span>&lt;<span class="keyword">const</span> <span class="keyword">uint8_t</span>*&gt;(tr.data.ptr.buffer),</span><br><span class="line">                        tr.data_size,</span><br><span class="line">                        <span class="keyword">reinterpret_cast</span>&lt;<span class="keyword">const</span> <span class="keyword">binder_size_t</span>*&gt;(tr.data.ptr.offsets),</span><br><span class="line">                        tr.offsets_size/<span class="built_in"><span class="keyword">sizeof</span></span>(<span class="keyword">binder_size_t</span>), <span class="keyword">this</span>);</span><br><span class="line">                    <span class="keyword">continue</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">goto</span> finish;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">default</span>:</span><br><span class="line">            err = <span class="built_in">executeCommand</span>(cmd);</span><br><span class="line">            <span class="keyword">if</span> (err != NO_ERROR) <span class="keyword">goto</span> finish;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">finish:</span><br><span class="line">    <span class="keyword">if</span> (err != NO_ERROR) &#123;</span><br><span class="line">        <span class="keyword">if</span> (acquireResult) *acquireResult = err;</span><br><span class="line">        <span class="keyword">if</span> (reply) reply-&gt;<span class="built_in">setError</span>(err);</span><br><span class="line">        mLastError = err;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> err;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>调用talkWithDriver()函数跟驱动交互，在<a href="https://i-rtfsc.github.io/archives/f17b1d52.html">Binder机制02-ServiceManager</a>章节中我们讲过talkWithDriver()函数调用binder_ioctl()跟驱动交互的细节，这里不在展开描述</li>
<li>根据binder驱动处理后的结果做操作，BR码在<a href="https://i-rtfsc.github.io/archives/3e87e0ce.html">Binder机制01-驱动</a>有描述</li>
</ul>
<h3 id="linkToDeath"><a href="#linkToDeath" class="headerlink" title="linkToDeath()"></a>linkToDeath()</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/BpBinder.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">status_t</span> <span class="title">BpBinder::linkToDeath</span><span class="params">(</span></span></span><br><span class="line"><span class="params"><span class="function">    <span class="keyword">const</span> sp&lt;DeathRecipient&gt;&amp; recipient, <span class="keyword">void</span>* cookie, <span class="keyword">uint32_t</span> flags)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Obituary ob;</span><br><span class="line">    ob.recipient = recipient;</span><br><span class="line">    ob.cookie = cookie;</span><br><span class="line">    ob.flags = flags;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    &#123;</span><br><span class="line">        AutoMutex _l(mLock);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (!mObitsSent) &#123;<span class="comment">//还没有发送过死亡通知</span></span><br><span class="line">            <span class="keyword">if</span> (!mObituaries) &#123;<span class="comment">//第一次调用的话，mObituaries还没有分配内存</span></span><br><span class="line">                mObituaries = <span class="keyword">new</span> Vector&lt;Obituary&gt;;</span><br><span class="line">                <span class="keyword">if</span> (!mObituaries) &#123;</span><br><span class="line">                    <span class="keyword">return</span> NO_MEMORY;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="built_in">ALOGV</span>(<span class="string">&quot;Requesting death notification: %p handle %d\n&quot;</span>, <span class="keyword">this</span>, mHandle);</span><br><span class="line">                <span class="built_in">getWeakRefs</span>()-&gt;<span class="built_in">incWeak</span>(<span class="keyword">this</span>);</span><br><span class="line">                IPCThreadState* self = IPCThreadState::<span class="built_in">self</span>();</span><br><span class="line">                self-&gt;<span class="built_in">requestDeathNotification</span>(mHandle, <span class="keyword">this</span>);</span><br><span class="line">                self-&gt;<span class="built_in">flushCommands</span>();</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">ssize_t</span> res = mObituaries-&gt;<span class="built_in">add</span>(ob);</span><br><span class="line">            <span class="keyword">return</span> res &gt;= (<span class="keyword">ssize_t</span>)NO_ERROR ? (<span class="keyword">status_t</span>)NO_ERROR : res;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> DEAD_OBJECT;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>判断是否已经发送过死亡通知，如果没有发送过那么就分配内存</li>
<li>调用IPCThreadState的requestDeathNotification，将自己注册到binder驱动里面</li>
<li>将Obituary添加到vector里</li>
</ul>
<h4 id="IPCThreadState-requestDeathNotification"><a href="#IPCThreadState-requestDeathNotification" class="headerlink" title="IPCThreadState.requestDeathNotification()"></a>IPCThreadState.requestDeathNotification()</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/IPCThreadState.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">status_t</span> <span class="title">IPCThreadState::requestDeathNotification</span><span class="params">(<span class="keyword">int32_t</span> handle, BpBinder* proxy)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    mOut.<span class="built_in">writeInt32</span>(BC_REQUEST_DEATH_NOTIFICATION);</span><br><span class="line">    mOut.<span class="built_in">writeInt32</span>((<span class="keyword">int32_t</span>)handle);</span><br><span class="line">    mOut.<span class="built_in">writePointer</span>((<span class="keyword">uintptr_t</span>)proxy);</span><br><span class="line">    <span class="keyword">return</span> NO_ERROR;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>将BC_REQUEST_DEATH_NOTIFICATION（请求接收死亡通知）写到mOut中。BC码在<a href="https://i-rtfsc.github.io/archives/3e87e0ce.html">Binder机制01-驱动</a>有描述</p>
<h4 id="IPCThreadState-flushCommands"><a href="#IPCThreadState-flushCommands" class="headerlink" title="IPCThreadState.flushCommands()"></a>IPCThreadState.flushCommands()</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/IPCThreadState.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">IPCThreadState::flushCommands</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (mProcess-&gt;mDriverFD &lt; <span class="number">0</span>)</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    <span class="built_in">talkWithDriver</span>(<span class="literal">false</span>);</span><br><span class="line">    <span class="comment">// The flush could have caused post-write refcount decrements to have</span></span><br><span class="line">    <span class="comment">// been executed, which in turn could result in BC_RELEASE/BC_DECREFS</span></span><br><span class="line">    <span class="comment">// being queued in mOut. So flush again, if we need to.</span></span><br><span class="line">    <span class="keyword">if</span> (mOut.<span class="built_in">dataSize</span>() &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">talkWithDriver</span>(<span class="literal">false</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (mOut.<span class="built_in">dataSize</span>() &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">ALOGW</span>(<span class="string">&quot;mOut.dataSize() &gt; 0 after flushCommands()&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>调用talkWithDriver和binder驱动进行交互，在<a href="https://i-rtfsc.github.io/archives/f17b1d52.html">Binder机制02-ServiceManager</a>章节中我们讲过talkWithDriver()函数调用binder_ioctl()跟驱动交互的细节，这里不在展开描述。</p>
<h3 id="unlinkToDeath"><a href="#unlinkToDeath" class="headerlink" title="unlinkToDeath()"></a>unlinkToDeath()</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/BpBinder.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">status_t</span> <span class="title">BpBinder::unlinkToDeath</span><span class="params">(</span></span></span><br><span class="line"><span class="params"><span class="function">    <span class="keyword">const</span> wp&lt;DeathRecipient&gt;&amp; recipient, <span class="keyword">void</span>* cookie, <span class="keyword">uint32_t</span> flags,</span></span></span><br><span class="line"><span class="params"><span class="function">    wp&lt;DeathRecipient&gt;* outRecipient)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    AutoMutex _l(mLock);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (mObitsSent) &#123;</span><br><span class="line">        <span class="keyword">return</span> DEAD_OBJECT;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">size_t</span> N = mObituaries ? mObituaries-&gt;<span class="built_in">size</span>() : <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">size_t</span> i=<span class="number">0</span>; i&lt;N; i++) &#123;</span><br><span class="line">        <span class="keyword">const</span> Obituary&amp; obit = mObituaries-&gt;<span class="built_in">itemAt</span>(i);</span><br><span class="line">        <span class="keyword">if</span> ((obit.recipient == recipient</span><br><span class="line">                    || (recipient == <span class="literal">nullptr</span> &amp;&amp; obit.cookie == cookie))</span><br><span class="line">                &amp;&amp; obit.flags == flags) &#123;</span><br><span class="line">            <span class="keyword">if</span> (outRecipient != <span class="literal">nullptr</span>) &#123;</span><br><span class="line">                *outRecipient = mObituaries-&gt;<span class="built_in">itemAt</span>(i).recipient;</span><br><span class="line">            &#125;</span><br><span class="line">            mObituaries-&gt;<span class="built_in">removeAt</span>(i);</span><br><span class="line">            <span class="keyword">if</span> (mObituaries-&gt;<span class="built_in">size</span>() == <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="built_in">ALOGV</span>(<span class="string">&quot;Clearing death notification: %p handle %d\n&quot;</span>, <span class="keyword">this</span>, mHandle);</span><br><span class="line">                IPCThreadState* self = IPCThreadState::<span class="built_in">self</span>();</span><br><span class="line">                self-&gt;<span class="built_in">clearDeathNotification</span>(mHandle, <span class="keyword">this</span>);</span><br><span class="line">                self-&gt;<span class="built_in">flushCommands</span>();</span><br><span class="line">                <span class="keyword">delete</span> mObituaries;</span><br><span class="line">                mObituaries = <span class="literal">nullptr</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> NO_ERROR;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> NAME_NOT_FOUND;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>unlinkToDeath()函数跟linkToDeath()函数调用流程一直，只不过是BC码变成了BC_CLEAR_DEATH_NOTIFICATION（去除接收死亡通知）。</p>
<h3 id="sendObituary"><a href="#sendObituary" class="headerlink" title="sendObituary()"></a>sendObituary()</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/BpBinder.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">BpBinder::sendObituary</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">ALOGV</span>(<span class="string">&quot;Sending obituary for proxy %p handle %d, mObitsSent=%s\n&quot;</span>,</span><br><span class="line">        <span class="keyword">this</span>, mHandle, mObitsSent ? <span class="string">&quot;true&quot;</span> : <span class="string">&quot;false&quot;</span>);</span><br><span class="line"></span><br><span class="line">    mAlive = <span class="number">0</span>;</span><br><span class="line">    <span class="comment">//如果已经发送过死亡通知了就直接返回</span></span><br><span class="line">    <span class="keyword">if</span> (mObitsSent) <span class="keyword">return</span>;</span><br><span class="line"></span><br><span class="line">    mLock.<span class="built_in">lock</span>();</span><br><span class="line">    Vector&lt;Obituary&gt;* obits = mObituaries;</span><br><span class="line">    <span class="keyword">if</span>(obits != <span class="literal">nullptr</span>) &#123;</span><br><span class="line">        <span class="built_in">ALOGV</span>(<span class="string">&quot;Clearing sent death notification: %p handle %d\n&quot;</span>, <span class="keyword">this</span>, mHandle);</span><br><span class="line">        IPCThreadState* self = IPCThreadState::<span class="built_in">self</span>();</span><br><span class="line">        <span class="comment">//清除注册在binder驱动中的死亡通知</span></span><br><span class="line">        self-&gt;<span class="built_in">clearDeathNotification</span>(mHandle, <span class="keyword">this</span>);</span><br><span class="line">        self-&gt;<span class="built_in">flushCommands</span>();</span><br><span class="line">        mObituaries = <span class="literal">nullptr</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    mObitsSent = <span class="number">1</span>;</span><br><span class="line">    mLock.<span class="built_in">unlock</span>();</span><br><span class="line"></span><br><span class="line">    <span class="built_in">ALOGV</span>(<span class="string">&quot;Reporting death of proxy %p for %zu recipients\n&quot;</span>,</span><br><span class="line">        <span class="keyword">this</span>, obits ? obits-&gt;<span class="built_in">size</span>() : <span class="number">0U</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (obits != <span class="literal">nullptr</span>) &#123;</span><br><span class="line">        <span class="keyword">const</span> <span class="keyword">size_t</span> N = obits-&gt;<span class="built_in">size</span>();</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">size_t</span> i=<span class="number">0</span>; i&lt;N; i++) &#123;</span><br><span class="line">            <span class="comment">//调用死亡通知函数</span></span><br><span class="line">            <span class="built_in">reportOneDeath</span>(obits-&gt;<span class="built_in">itemAt</span>(i));</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">delete</span> obits;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">BpBinder::reportOneDeath</span><span class="params">(<span class="keyword">const</span> Obituary&amp; obit)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    sp&lt;DeathRecipient&gt; recipient = obit.recipient.<span class="built_in">promote</span>();</span><br><span class="line">    <span class="built_in">ALOGV</span>(<span class="string">&quot;Reporting death to recipient: %p\n&quot;</span>, recipient.<span class="built_in">get</span>());</span><br><span class="line">    <span class="keyword">if</span> (recipient == <span class="literal">nullptr</span>) <span class="keyword">return</span>;</span><br><span class="line"></span><br><span class="line">    recipient-&gt;<span class="built_in">binderDied</span>(<span class="keyword">this</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>发出死亡通知。</p>
<h2 id="BBinder"><a href="#BBinder" class="headerlink" title="BBinder"></a>BBinder</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/include/binder/Binder.h</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">BBinder</span> :</span> <span class="keyword">public</span> IBinder</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">                        <span class="built_in">BBinder</span>();</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">const</span> String16&amp; <span class="title">getInterfaceDescriptor</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">bool</span>        <span class="title">isBinderAlive</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">status_t</span>    <span class="title">pingBinder</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">status_t</span>    <span class="title">dump</span><span class="params">(<span class="keyword">int</span> fd, <span class="keyword">const</span> Vector&lt;String16&gt;&amp; args)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// NOLINTNEXTLINE(google-default-arguments)</span></span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">status_t</span>    <span class="title">transact</span><span class="params">(   <span class="keyword">uint32_t</span> code,</span></span></span><br><span class="line"><span class="params"><span class="function">                                    <span class="keyword">const</span> Parcel&amp; data,</span></span></span><br><span class="line"><span class="params"><span class="function">                                    Parcel* reply,</span></span></span><br><span class="line"><span class="params"><span class="function">                                    <span class="keyword">uint32_t</span> flags = <span class="number">0</span>)</span> <span class="keyword">final</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// NOLINTNEXTLINE(google-default-arguments)</span></span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">status_t</span>    <span class="title">linkToDeath</span><span class="params">(<span class="keyword">const</span> sp&lt;DeathRecipient&gt;&amp; recipient,</span></span></span><br><span class="line"><span class="params"><span class="function">                                    <span class="keyword">void</span>* cookie = <span class="literal">nullptr</span>,</span></span></span><br><span class="line"><span class="params"><span class="function">                                    <span class="keyword">uint32_t</span> flags = <span class="number">0</span>)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// NOLINTNEXTLINE(google-default-arguments)</span></span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">status_t</span>    <span class="title">unlinkToDeath</span><span class="params">(  <span class="keyword">const</span> wp&lt;DeathRecipient&gt;&amp; recipient,</span></span></span><br><span class="line"><span class="params"><span class="function">                                        <span class="keyword">void</span>* cookie = <span class="literal">nullptr</span>,</span></span></span><br><span class="line"><span class="params"><span class="function">                                        <span class="keyword">uint32_t</span> flags = <span class="number">0</span>,</span></span></span><br><span class="line"><span class="params"><span class="function">                                        wp&lt;DeathRecipient&gt;* outRecipient = <span class="literal">nullptr</span>)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span>        <span class="title">attachObject</span><span class="params">(   <span class="keyword">const</span> <span class="keyword">void</span>* objectID,</span></span></span><br><span class="line"><span class="params"><span class="function">                                        <span class="keyword">void</span>* object,</span></span></span><br><span class="line"><span class="params"><span class="function">                                        <span class="keyword">void</span>* cleanupCookie,</span></span></span><br><span class="line"><span class="params"><span class="function">                                        object_cleanup_func func)</span> <span class="keyword">final</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span>*       <span class="title">findObject</span><span class="params">(<span class="keyword">const</span> <span class="keyword">void</span>* objectID)</span> <span class="keyword">const</span> <span class="keyword">final</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span>        <span class="title">detachObject</span><span class="params">(<span class="keyword">const</span> <span class="keyword">void</span>* objectID)</span> <span class="keyword">final</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> BBinder*    <span class="title">localBinder</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">bool</span>                <span class="title">isRequestingSid</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="comment">// This must be called before the object is sent to another process. Not thread safe.</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span>                <span class="title">setRequestingSid</span><span class="params">(<span class="keyword">bool</span> requestSid)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function">sp&lt;IBinder&gt;         <span class="title">getExtension</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="comment">// This must be called before the object is sent to another process. Not thread safe.</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span>                <span class="title">setExtension</span><span class="params">(<span class="keyword">const</span> sp&lt;IBinder&gt;&amp; extension)</span></span>;</span><br><span class="line"></span><br><span class="line">    __attribute__((weak))</span><br><span class="line">    <span class="function"><span class="keyword">void</span>                <span class="title">setMinSchedulerPolicy</span><span class="params">(<span class="keyword">int</span> policy, <span class="keyword">int</span> priority)</span></span>;</span><br><span class="line">    __attribute__((weak))</span><br><span class="line">    <span class="function"><span class="keyword">int</span>                 <span class="title">getMinSchedulerPolicy</span><span class="params">()</span></span>;</span><br><span class="line">    __attribute__((weak))</span><br><span class="line">    <span class="function"><span class="keyword">int</span>                 <span class="title">getMinSchedulerPriority</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">pid_t</span>               <span class="title">getDebugPid</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">virtual</span>             ~<span class="built_in">BBinder</span>();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// NOLINTNEXTLINE(google-default-arguments)</span></span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">status_t</span>    <span class="title">onTransact</span><span class="params">( <span class="keyword">uint32_t</span> code,</span></span></span><br><span class="line"><span class="params"><span class="function">                                    <span class="keyword">const</span> Parcel&amp; data,</span></span></span><br><span class="line"><span class="params"><span class="function">                                    Parcel* reply,</span></span></span><br><span class="line"><span class="params"><span class="function">                                    <span class="keyword">uint32_t</span> flags = <span class="number">0</span>)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">                        <span class="built_in">BBinder</span>(<span class="keyword">const</span> BBinder&amp; o);</span><br><span class="line">            BBinder&amp;    <span class="keyword">operator</span>=(<span class="keyword">const</span> BBinder&amp; o);</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">Extras</span>;</span></span><br><span class="line"></span><br><span class="line">    <span class="function">Extras*             <span class="title">getOrCreateExtras</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">    std::atomic&lt;Extras*&gt; mExtras;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">friend</span> ::android::internal::Stability;</span><br><span class="line">    <span class="class"><span class="keyword">union</span> &#123;</span></span><br><span class="line">        <span class="keyword">int32_t</span> mStability;</span><br><span class="line">        <span class="keyword">void</span>* mReserved0;</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>BBinder类在<a href="http://aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/include/binder/Binder.h">Binder.h</a>中定义，其方法在<a href="https://aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/Binder.cpp">Binder.cpp</a>里实现。</p>
<h3 id="transact-1"><a href="#transact-1" class="headerlink" title="transact()"></a>transact()</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">https:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/Binder.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// NOLINTNEXTLINE(google-default-arguments)</span></span><br><span class="line"><span class="function"><span class="keyword">status_t</span> <span class="title">BBinder::transact</span><span class="params">(</span></span></span><br><span class="line"><span class="params"><span class="function">    <span class="keyword">uint32_t</span> code, <span class="keyword">const</span> Parcel&amp; data, Parcel* reply, <span class="keyword">uint32_t</span> flags)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    data.<span class="built_in">setDataPosition</span>(<span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">status_t</span> err = NO_ERROR;</span><br><span class="line">    <span class="built_in"><span class="keyword">switch</span></span> (code) &#123;</span><br><span class="line">        <span class="keyword">case</span> PING_TRANSACTION:</span><br><span class="line">            err = <span class="built_in">pingBinder</span>();</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> EXTENSION_TRANSACTION:</span><br><span class="line">            err = reply-&gt;<span class="built_in">writeStrongBinder</span>(<span class="built_in">getExtension</span>());</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> DEBUG_PID_TRANSACTION:</span><br><span class="line">            err = reply-&gt;<span class="built_in">writeInt32</span>(<span class="built_in">getDebugPid</span>());</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">default</span>:</span><br><span class="line">            err = <span class="built_in">onTransact</span>(code, data, reply, flags);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// In case this is being transacted on in the same process.</span></span><br><span class="line">    <span class="keyword">if</span> (reply != <span class="literal">nullptr</span>) &#123;</span><br><span class="line">        reply-&gt;<span class="built_in">setDataPosition</span>(<span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> err;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>那么接下来直接看onTransact函数好了。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">https:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/Binder.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// NOLINTNEXTLINE(google-default-arguments)</span></span><br><span class="line"><span class="function"><span class="keyword">status_t</span> <span class="title">BBinder::onTransact</span><span class="params">(</span></span></span><br><span class="line"><span class="params"><span class="function">    <span class="keyword">uint32_t</span> code, <span class="keyword">const</span> Parcel&amp; data, Parcel* reply, <span class="keyword">uint32_t</span> <span class="comment">/*flags*/</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in"><span class="keyword">switch</span></span> (code) &#123;</span><br><span class="line">        <span class="keyword">case</span> INTERFACE_TRANSACTION:</span><br><span class="line">            reply-&gt;<span class="built_in">writeString16</span>(<span class="built_in">getInterfaceDescriptor</span>());</span><br><span class="line">            <span class="keyword">return</span> NO_ERROR;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">case</span> DUMP_TRANSACTION: &#123;</span><br><span class="line">            <span class="keyword">int</span> fd = data.<span class="built_in">readFileDescriptor</span>();</span><br><span class="line">            <span class="keyword">int</span> argc = data.<span class="built_in">readInt32</span>();</span><br><span class="line">            Vector&lt;String16&gt; args;</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; argc &amp;&amp; data.<span class="built_in">dataAvail</span>() &gt; <span class="number">0</span>; i++) &#123;</span><br><span class="line">               args.<span class="built_in">add</span>(data.<span class="built_in">readString16</span>());</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> <span class="built_in">dump</span>(fd, args);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">case</span> SHELL_COMMAND_TRANSACTION: &#123;</span><br><span class="line">            <span class="keyword">int</span> in = data.<span class="built_in">readFileDescriptor</span>();</span><br><span class="line">            <span class="keyword">int</span> out = data.<span class="built_in">readFileDescriptor</span>();</span><br><span class="line">            <span class="keyword">int</span> err = data.<span class="built_in">readFileDescriptor</span>();</span><br><span class="line">            <span class="keyword">int</span> argc = data.<span class="built_in">readInt32</span>();</span><br><span class="line">            Vector&lt;String16&gt; args;</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; argc &amp;&amp; data.<span class="built_in">dataAvail</span>() &gt; <span class="number">0</span>; i++) &#123;</span><br><span class="line">               args.<span class="built_in">add</span>(data.<span class="built_in">readString16</span>());</span><br><span class="line">            &#125;</span><br><span class="line">            sp&lt;IShellCallback&gt; shellCallback = IShellCallback::<span class="built_in">asInterface</span>(</span><br><span class="line">                    data.<span class="built_in">readStrongBinder</span>());</span><br><span class="line">            sp&lt;IResultReceiver&gt; resultReceiver = IResultReceiver::<span class="built_in">asInterface</span>(</span><br><span class="line">                    data.<span class="built_in">readStrongBinder</span>());</span><br><span class="line"></span><br><span class="line">            <span class="comment">// XXX can&#x27;t add virtuals until binaries are updated.</span></span><br><span class="line">            <span class="comment">//return shellCommand(in, out, err, args, resultReceiver);</span></span><br><span class="line">            (<span class="keyword">void</span>)in;</span><br><span class="line">            (<span class="keyword">void</span>)out;</span><br><span class="line">            (<span class="keyword">void</span>)err;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (resultReceiver != <span class="literal">nullptr</span>) &#123;</span><br><span class="line">                resultReceiver-&gt;<span class="built_in">send</span>(INVALID_OPERATION);</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">return</span> NO_ERROR;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">case</span> SYSPROPS_TRANSACTION: &#123;</span><br><span class="line">            <span class="built_in">report_sysprop_change</span>();</span><br><span class="line">            <span class="keyword">return</span> NO_ERROR;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">default</span>:</span><br><span class="line">            <span class="keyword">return</span> UNKNOWN_TRANSACTION;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>没有什么实际的内容，在<a href="https://i-rtfsc.github.io/archives/f17b1d52.html">Binder机制02-ServiceManager</a>章节中提到过编写实际的Binder服务端程序的时候应该是会重载这个函数，以提供实际的功能。事实是否如此呢，我们可以带着这个疑问在下文中寻找答案。</p>
<blockquote>
<p>BBinder的代码不多，看样子主要还是依靠具体服务端的特性来添加相应的功能的。<br>还有就是像attachObject、detachObject、findObject的功能是通过BpBinder中的ObjectManager来实现的。</p>
</blockquote>
<h2 id="IInterface-h"><a href="#IInterface-h" class="headerlink" title="IInterface.h"></a>IInterface.h</h2><p><a href="http://aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/include/binder/IInterface.h">IInterface.h</a>的代码比较多，可以通过点链接自行查看。除了构造函数和析构函数外，IInterface类里面只带有一个实现了的类方法，两个不同的重载，就是将IInterface参数转换成IBinder对象，然后返回给调用者，这样子调用者就可以使用IBinder提供的transact、onTransact等函数进行Binder数据传输了。<br>​</p>
<h3 id="DECLARE-META-INTERFACE"><a href="#DECLARE-META-INTERFACE" class="headerlink" title="DECLARE_META_INTERFACE"></a>DECLARE_META_INTERFACE</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/include/binder/IInterface.h</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> DECLARE_META_INTERFACE(INTERFACE)                               \</span></span><br><span class="line"><span class="meta">public:                                                                 \</span></span><br><span class="line"><span class="meta">    static const ::android::String16 descriptor;                        \</span></span><br><span class="line"><span class="meta">    static ::android::sp<span class="meta-string">&lt;I##INTERFACE&gt;</span> asInterface(                     \</span></span><br><span class="line"><span class="meta">            const ::android::sp<span class="meta-string">&lt;::android::IBinder&gt;</span>&amp; obj);              \</span></span><br><span class="line"><span class="meta">    virtual const ::android::String16&amp; getInterfaceDescriptor() const;  \</span></span><br><span class="line"><span class="meta">    I##INTERFACE();                                                     \</span></span><br><span class="line"><span class="meta">    virtual ~I##INTERFACE();                                            \</span></span><br><span class="line"><span class="meta">    static bool setDefaultImpl(std::unique_ptr<span class="meta-string">&lt;I##INTERFACE&gt;</span> impl);     \</span></span><br><span class="line"><span class="meta">    static const std::unique_ptr<span class="meta-string">&lt;I##INTERFACE&gt;</span>&amp; getDefaultImpl();       \</span></span><br><span class="line"><span class="meta">private:                                                                \</span></span><br><span class="line"><span class="meta">    static std::unique_ptr<span class="meta-string">&lt;I##INTERFACE&gt;</span> default_impl;                  \</span></span><br><span class="line"><span class="meta">public: </span></span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>以AudioPolicyService为例</p>
<h3 id="DECLARE-META-INTERFACE-AudioPolicyService"><a href="#DECLARE-META-INTERFACE-AudioPolicyService" class="headerlink" title="DECLARE_META_INTERFACE(AudioPolicyService)"></a>DECLARE_META_INTERFACE(AudioPolicyService)</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">const</span> ::android::String16 descriptor;</span><br><span class="line">    <span class="keyword">static</span> ::<span class="function">android::sp&lt;IAudioPolicyService&gt; <span class="title">asInterface</span><span class="params">(</span></span></span><br><span class="line"><span class="params"><span class="function">            <span class="keyword">const</span> ::android::sp&lt;::android::IBinder&gt;&amp; obj)</span></span>;</span><br><span class="line">    <span class="keyword">virtual</span> <span class="keyword">const</span> ::<span class="function">android::String16&amp; <span class="title">getInterfaceDescriptor</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line">    <span class="built_in">IAudioPolicyService</span>();</span><br><span class="line">    <span class="keyword">virtual</span> ~<span class="built_in">IAudioPolicyService</span>();</span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">bool</span> <span class="title">setDefaultImpl</span><span class="params">(std::unique_ptr&lt;IAudioPolicyService&gt; impl)</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">const</span> std::unique_ptr&lt;IAudioPolicyService&gt;&amp; <span class="title">getDefaultImpl</span><span class="params">()</span></span>;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">static</span> std::unique_ptr&lt;IAudioPolicyService&gt; default_impl;</span><br><span class="line"><span class="keyword">public</span>:</span><br></pre></td></tr></table></figure>
<p>替换之后的内容就很容易理解了，无非就是定义了一些方法。那么IMPLEMENT_META_INTERFACE应该就是实现这些方法的宏了，下面看看替换后的IMPLEMENT_META_INTERFACE的内容是什么。</p>
<h3 id="IMPLEMENT-META-INTERFACE"><a href="#IMPLEMENT-META-INTERFACE" class="headerlink" title="IMPLEMENT_META_INTERFACE"></a>IMPLEMENT_META_INTERFACE</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/include/binder/IInterface.h</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> IMPLEMENT_META_INTERFACE(INTERFACE, NAME)                       \</span></span><br><span class="line"><span class="meta">    DO_NOT_DIRECTLY_USE_ME_IMPLEMENT_META_INTERFACE(INTERFACE, NAME)    \</span></span><br><span class="line"><span class="meta"></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> DO_NOT_DIRECTLY_USE_ME_IMPLEMENT_META_INTERFACE(INTERFACE, NAME)\</span></span><br><span class="line"><span class="meta">    const ::android::StaticString16                                     \</span></span><br><span class="line"><span class="meta">        I##INTERFACE##_descriptor_static_str16(__IINTF_CONCAT(u, NAME));\</span></span><br><span class="line"><span class="meta">    const ::android::String16 I##INTERFACE::descriptor(                 \</span></span><br><span class="line"><span class="meta">        I##INTERFACE##_descriptor_static_str16);                        \</span></span><br><span class="line"><span class="meta">    const ::android::String16&amp;                                          \</span></span><br><span class="line"><span class="meta">            I##INTERFACE::getInterfaceDescriptor() const &#123;              \</span></span><br><span class="line"><span class="meta">        return I##INTERFACE::descriptor;                                \</span></span><br><span class="line"><span class="meta">    &#125;                                                                   \</span></span><br><span class="line"><span class="meta">    ::android::sp<span class="meta-string">&lt;I##INTERFACE&gt;</span> I##INTERFACE::asInterface(              \</span></span><br><span class="line"><span class="meta">            const ::android::sp<span class="meta-string">&lt;::android::IBinder&gt;</span>&amp; obj)               \</span></span><br><span class="line"><span class="meta">    &#123;                                                                   \</span></span><br><span class="line"><span class="meta">        ::android::sp<span class="meta-string">&lt;I##INTERFACE&gt;</span> intr;                               \</span></span><br><span class="line"><span class="meta">        <span class="meta-keyword">if</span> (obj != nullptr) &#123;                                           \</span></span><br><span class="line"><span class="meta">            intr = static_cast<span class="meta-string">&lt;I##INTERFACE*&gt;</span>(                          \</span></span><br><span class="line"><span class="meta">                obj-&gt;queryLocalInterface(                               \</span></span><br><span class="line"><span class="meta">                        I##INTERFACE::descriptor).get());               \</span></span><br><span class="line"><span class="meta">            <span class="meta-keyword">if</span> (intr == nullptr) &#123;                                      \</span></span><br><span class="line"><span class="meta">                intr = new Bp##INTERFACE(obj);                          \</span></span><br><span class="line"><span class="meta">            &#125;                                                           \</span></span><br><span class="line"><span class="meta">        &#125;                                                               \</span></span><br><span class="line"><span class="meta">        return intr;                                                    \</span></span><br><span class="line"><span class="meta">    &#125;                                                                   \</span></span><br><span class="line"><span class="meta">    std::unique_ptr<span class="meta-string">&lt;I##INTERFACE&gt;</span> I##INTERFACE::default_impl;           \</span></span><br><span class="line"><span class="meta">    bool I##INTERFACE::setDefaultImpl(std::unique_ptr<span class="meta-string">&lt;I##INTERFACE&gt;</span> impl)\</span></span><br><span class="line"><span class="meta">    &#123;                                                                   \</span></span><br><span class="line"><span class="meta">        <span class="comment">/* Only one user of this interface can use this function     */</span> \</span></span><br><span class="line"><span class="meta">        <span class="comment">/* at a time. This is a heuristic to detect if two different */</span> \</span></span><br><span class="line"><span class="meta">        <span class="comment">/* users in the same process use this function.              */</span> \</span></span><br><span class="line"><span class="meta">        assert(!I##INTERFACE::default_impl);                            \</span></span><br><span class="line"><span class="meta">        <span class="meta-keyword">if</span> (impl) &#123;                                                     \</span></span><br><span class="line"><span class="meta">            I##INTERFACE::default_impl = std::move(impl);               \</span></span><br><span class="line"><span class="meta">            return true;                                                \</span></span><br><span class="line"><span class="meta">        &#125;                                                               \</span></span><br><span class="line"><span class="meta">        return false;                                                   \</span></span><br><span class="line"><span class="meta">    &#125;                                                                   \</span></span><br><span class="line"><span class="meta">    const std::unique_ptr<span class="meta-string">&lt;I##INTERFACE&gt;</span>&amp; I##INTERFACE::getDefaultImpl() \</span></span><br><span class="line"><span class="meta">    &#123;                                                                   \</span></span><br><span class="line"><span class="meta">        return I##INTERFACE::default_impl;                              \</span></span><br><span class="line"><span class="meta">    &#125;                                                                   \</span></span><br><span class="line"><span class="meta">    I##INTERFACE::I##INTERFACE() &#123; &#125;                                    \</span></span><br><span class="line"><span class="meta">    I##INTERFACE::~I##INTERFACE() &#123; &#125;                                   \</span></span><br></pre></td></tr></table></figure>
<p>以AudioPolicyService为例</p>
<h3 id="IMPLEMENT-META-INTERFACE-AudioPolicyService-“android-media-IAudioPolicyService”"><a href="#IMPLEMENT-META-INTERFACE-AudioPolicyService-“android-media-IAudioPolicyService”" class="headerlink" title="IMPLEMENT_META_INTERFACE(AudioPolicyService, “android.media.IAudioPolicyService”)"></a>IMPLEMENT_META_INTERFACE(AudioPolicyService, “android.media.IAudioPolicyService”)</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> ::<span class="function">android::String16 <span class="title">IAudioPolicyService::descriptor</span><span class="params">(<span class="string">&quot;android.media.IAudioPolicyService&quot;</span>)</span></span>;</span><br><span class="line"><span class="keyword">const</span> ::<span class="function">android::String16&amp;</span></span><br><span class="line"><span class="function">    <span class="title">IAudioPolicyService::getInterfaceDescriptor</span><span class="params">()</span> <span class="keyword">const</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> IAudioPolicyService::descriptor;</span><br><span class="line">&#125;</span><br><span class="line">::<span class="function">android::sp&lt;IAudioPolicyService&gt; <span class="title">IAudioPolicyService::asInterface</span><span class="params">(</span></span></span><br><span class="line"><span class="params"><span class="function">    <span class="keyword">const</span> ::android::sp&lt;::android::IBinder&gt;&amp; obj)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    ::android::sp&lt;IAudioPolicyService&gt; intr;</span><br><span class="line">    <span class="keyword">if</span> (obj != <span class="literal">nullptr</span>) &#123;</span><br><span class="line">        intr = <span class="keyword">static_cast</span>&lt;IAudioPolicyService*&gt;(</span><br><span class="line">            obj-&gt;<span class="built_in">queryLocalInterface</span>(</span><br><span class="line">                IAudioPolicyService::descriptor).<span class="built_in">get</span>());</span><br><span class="line">        <span class="keyword">if</span> (intr == <span class="literal">nullptr</span>) &#123;</span><br><span class="line">            intr = <span class="keyword">new</span> <span class="built_in">BpAudioPolicyService</span>(obj);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> intr;</span><br><span class="line">&#125;</span><br><span class="line">std::unique_ptr&lt;IAudioPolicyService&gt; IAudioPolicyService::default_impl;</span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">IAudioPolicyService::setDefaultImpl</span><span class="params">(std::unique_ptr&lt;IAudioPolicyService&gt; impl)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!IAudioPolicyService::default_impl &amp;&amp; impl) &#123;</span><br><span class="line">        IAudioPolicyService::default_impl = std::<span class="built_in">move</span>(impl);</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">const</span> std::unique_ptr&lt;IAudioPolicyService&gt;&amp; <span class="title">IAudioPolicyService::getDefaultImpl</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> IAudioPolicyService::default_impl;</span><br><span class="line">&#125;</span><br><span class="line">IAudioPolicyService::<span class="built_in">IAudioPolicyService</span>() &#123; &#125;</span><br><span class="line">IAudioPolicyService::~<span class="built_in">IAudioPolicyService</span>() &#123; &#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>其实将descriptor定义为android.os.IAudioPolicyService，然后getInterfaceDescriptor函数可以获取到这个descriptor。然后在asInterface函数中，将一个IBinder对象转换为一个BpInterface类对象。下一节还有举例补充说明。</p>
<h2 id="小节"><a href="#小节" class="headerlink" title="小节"></a>小节</h2><p>每个Binder服务都是为了某个功能而实现的，因此其本身会定义一套接口集（通常是C++的一个类）来描述自己提供的所有功能。而Binder服务既有自身实现服务的类，也要有给客户端进程调用的类。为了便于开发，这两中类里面的服务接口应当是一致的，例如：假设服务实现方提供了一个接口为add(int a, int b)的服务方法，那么其远程接口中也应当有一个add(int a, int b)方法。因此为了实现方便，本地实现类和远程接口类需要有一个公共的描述服务接口的基类（即上图中的IXXXService）来继承。而这个基类通常是IInterface的子类。<br>BnInterface继承了BBinder类，由此可以通过复写onTransact方法来提供实现。BpInterface继承了BpRefBase，通过这个类的remote方法可以获取到指向服务实现方的句柄。在客户端接口的实现类中，每个接口在组装好参数之后，都会调用remote()-&gt;transact来发送请求，而这里其实就是调用的BpBinder的transact方法，这样请求便通过Binder到达了服务实现方的onTransact中。这个过程如下图所示：<br><img src="https://cdn.nlark.com/yuque/0/2021/png/1759879/1634279389319-1599ea20-d410-4092-86d8-6e356d504da2.png#clientId=ucf06e1cf-361e-4&from=paste&id=u9eb22643&margin=%5Bobject%20Object%5D&name=image.png&originHeight=1610&originWidth=2542&originalType=url&ratio=1&size=201268&status=done&style=none&taskId=u7b5700a6-e83e-4043-af4b-bee79185373" alt="image.png"></p>
<h1 id="注册Native服务（media服务）"><a href="#注册Native服务（media服务）" class="headerlink" title="注册Native服务（media服务）"></a>注册Native服务（media服务）</h1><p>mediaserver服务如何被拉起，后面会专门找个章节详细讲解，本章我们就默认执行到mediaserver的main()函数了。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/av/media/mediaserver/main_mediaserver.cpp</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc __unused, <span class="keyword">char</span> **argv __unused)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">signal</span>(SIGPIPE, SIG_IGN);</span><br><span class="line"></span><br><span class="line">    <span class="function">sp&lt;ProcessState&gt; <span class="title">proc</span><span class="params">(ProcessState::self())</span></span>;<span class="comment">//获得ProcessState实例对象</span></span><br><span class="line">    <span class="function">sp&lt;IServiceManager&gt; <span class="title">sm</span><span class="params">(defaultServiceManager())</span></span>;<span class="comment">//获取BpServiceManager对象</span></span><br><span class="line">    <span class="built_in">ALOGI</span>(<span class="string">&quot;ServiceManager: %p&quot;</span>, sm.<span class="built_in">get</span>());</span><br><span class="line">    <span class="built_in">AIcu_initializeIcuOrDie</span>();</span><br><span class="line">    MediaPlayerService::<span class="built_in">instantiate</span>();<span class="comment">//注册多媒体服务</span></span><br><span class="line">    ResourceManagerService::<span class="built_in">instantiate</span>();</span><br><span class="line">    <span class="built_in">registerExtensions</span>();</span><br><span class="line">    ::android::hardware::<span class="built_in">configureRpcThreadpool</span>(<span class="number">16</span>, <span class="literal">false</span>);<span class="comment">//配置HwBinder的线程池</span></span><br><span class="line">    ProcessState::<span class="built_in">self</span>()-&gt;<span class="built_in">startThreadPool</span>();<span class="comment">//启动Binder线程池</span></span><br><span class="line">    IPCThreadState::<span class="built_in">self</span>()-&gt;<span class="built_in">joinThreadPool</span>();<span class="comment">//当前线程加入到线程池</span></span><br><span class="line">    ::android::hardware::<span class="built_in">joinRpcThreadpool</span>();<span class="comment">//把当前的通信加入HwBinder的线程池进行循环</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>获得ProcessState实例对象</li>
<li>调用defualtServiceManager方法获取IServiceManager实例（即BpServiceManager）</li>
<li>注册media服务</li>
<li>启动Binder线程池</li>
<li>把当前线程加入到线程池</li>
</ul>
<h2 id="打开Binder驱动"><a href="#打开Binder驱动" class="headerlink" title="打开Binder驱动"></a>打开Binder驱动</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/ProcessState.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function">sp&lt;ProcessState&gt; <span class="title">ProcessState::self</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Mutex::Autolock _l(gProcessMutex);</span><br><span class="line">    <span class="keyword">if</span> (gProcess != <span class="literal">nullptr</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> gProcess;</span><br><span class="line">    &#125;</span><br><span class="line">    gProcess = <span class="keyword">new</span> <span class="built_in">ProcessState</span>(kDefaultDriver);</span><br><span class="line">    <span class="keyword">return</span> gProcess;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>ProcessState代表进程状态，一个进程只有一个ProcessState对象。ProcessState采用了单例的模式保存了一个ProcessState全局对象gProcess，供每个线程使用。<br>这里的new ProcessState(kDefaultDriver)其实就是去打开binder驱动等操作，如果看过<a href="https://i-rtfsc.github.io/archives/f17b1d52.html">Binder机制02-ServiceManager</a>或者<a href="https://i-rtfsc.github.io/archives/f1684132.html">Ashmem简介（Android IPC传输大数据）</a>会这流程有所了解，为了照顾没看过这两篇文档的同学，这里我们还是把之前的分析拷贝过来。</p>
<h3 id="new-ProcessState-driver"><a href="#new-ProcessState-driver" class="headerlink" title="new ProcessState(driver)"></a>new ProcessState(driver)</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">http://aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/ProcessState.cpp</span><br><span class="line"></span><br><span class="line">ProcessState::ProcessState(const char *driver)</span><br><span class="line">    : mDriverName(String8(driver))</span><br><span class="line">    , mDriverFD(open_driver(driver))</span><br><span class="line">    , mVMStart(MAP_FAILED)</span><br><span class="line">    , mThreadCountLock(PTHREAD_MUTEX_INITIALIZER)</span><br><span class="line">    , mThreadCountDecrement(PTHREAD_COND_INITIALIZER)</span><br><span class="line">    , mExecutingThreadsCount(0)</span><br><span class="line">    , mMaxThreads(DEFAULT_MAX_BINDER_THREADS)</span><br><span class="line">    , mStarvationStartTimeMs(0)</span><br><span class="line">    , mThreadPoolStarted(<span class="literal">false</span>)</span><br><span class="line">    , mThreadPoolSeq(1)</span><br><span class="line">    , mCallRestriction(CallRestriction::NONE)</span><br><span class="line">&#123;</span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (mDriverFD &gt;= 0) &#123;</span><br><span class="line">        // mmap the binder, providing a chunk of virtual address space to receive transactions.</span><br><span class="line">        //<span class="comment">#define BINDER_VM_SIZE ((1 * 1024 * 1024) - sysconf(_SC_PAGE_SIZE) * 2)</span></span><br><span class="line">        //_SC_PAGE_SIZE表示一个page页的大小，通常情况下为4K</span><br><span class="line">        //3. 通过mmap内存映射（1M-4K*2）的内存空间</span><br><span class="line">        mVMStart = mmap(nullptr, BINDER_VM_SIZE, PROT_READ, MAP_PRIVATE | MAP_NORESERVE, mDriverFD, 0);</span><br><span class="line">        <span class="keyword">if</span> (mVMStart == MAP_FAILED) &#123;</span><br><span class="line">            // *sigh*</span><br><span class="line">            ALOGE(<span class="string">&quot;Using %s failed: unable to mmap transaction memory.\n&quot;</span>, mDriverName.c_str());</span><br><span class="line">            close(mDriverFD);</span><br><span class="line">            mDriverFD = -1;</span><br><span class="line">            mDriverName.clear();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这里看到了很多有用的信息，设置驱动的名字、最大线程数等等。还调用了open_driver(driver)和mmap(nullptr, BINDER_VM_SIZE, PROT_READ, MAP_PRIVATE | MAP_NORESERVE, mDriverFD, 0)。</p>
<h3 id="open-driver-driver"><a href="#open-driver-driver" class="headerlink" title="open_driver(driver)"></a>open_driver(driver)</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">http://aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/ProcessState.cpp</span><br><span class="line"></span><br><span class="line">static int open_driver(const char *driver)</span><br><span class="line">&#123;</span><br><span class="line">    //1. 打开Binder设备驱动</span><br><span class="line">    int fd = open(driver, O_RDWR | O_CLOEXEC);</span><br><span class="line">    <span class="keyword">if</span> (fd &gt;= 0) &#123;</span><br><span class="line">        int vers = 0;</span><br><span class="line">        //2. 获取Binder的版本信息</span><br><span class="line">        status_t result = ioctl(fd, BINDER_VERSION, &amp;vers);</span><br><span class="line">        <span class="keyword">if</span> (result == -1) &#123;</span><br><span class="line">            ALOGE(<span class="string">&quot;Binder ioctl to obtain version failed: %s&quot;</span>, strerror(errno));</span><br><span class="line">            close(fd);</span><br><span class="line">            fd = -1;</span><br><span class="line">        &#125;</span><br><span class="line">        //2.1 比较协议版本是否相同，不同则跳出</span><br><span class="line">        <span class="keyword">if</span> (result != 0 || vers != BINDER_CURRENT_PROTOCOL_VERSION) &#123;</span><br><span class="line">          ALOGE(<span class="string">&quot;Binder driver protocol(%d) does not match user space protocol(%d)! ioctl() return value: %d&quot;</span>,</span><br><span class="line">                vers, BINDER_CURRENT_PROTOCOL_VERSION, result);</span><br><span class="line">            close(fd);</span><br><span class="line">            fd = -1;</span><br><span class="line">        &#125;</span><br><span class="line">        size_t maxThreads = DEFAULT_MAX_BINDER_THREADS;</span><br><span class="line">        //设置最大线程数，BINDER_SET_MAX_THREADS=16</span><br><span class="line">        result = ioctl(fd, BINDER_SET_MAX_THREADS, &amp;maxThreads);</span><br><span class="line">        <span class="keyword">if</span> (result == -1) &#123;</span><br><span class="line">            ALOGE(<span class="string">&quot;Binder ioctl to set max threads failed: %s&quot;</span>, strerror(errno));</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        ALOGW(<span class="string">&quot;Opening &#x27;%s&#x27; failed: %s\n&quot;</span>, driver, strerror(errno));</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">return</span> fd;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>调用open（对应内核中的binder_open）打开Binder设备驱动</li>
<li>调用ioctl（对应内核中的binder_ioctl）获取Binder的版本信息</li>
<li>调用ioctl（对应内核中的binder_ioctl）设置最大线程数，BINDER_SET_MAX_THREADS=16</li>
</ul>
<h3 id="mmap"><a href="#mmap" class="headerlink" title="mmap()"></a>mmap()</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">http://aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/ProcessState.cpp</span><br><span class="line"></span><br><span class="line">//<span class="comment">#define BINDER_VM_SIZE ((1 * 1024 * 1024) - sysconf(_SC_PAGE_SIZE) * 2)</span></span><br><span class="line">//_SC_PAGE_SIZE表示一个page页的大小，通常情况下为4K</span><br><span class="line">//3. 通过mmap内存映射（1M-4K*2）的内存空间</span><br><span class="line">mVMStart = mmap(nullptr, BINDER_VM_SIZE, PROT_READ, MAP_PRIVATE | MAP_NORESERVE, mDriverFD, 0);</span><br></pre></td></tr></table></figure>
<p>调用mmap（对应内核中的binder_mmap）内存映射（1M-4K*2）的内存空间</p>
<h3 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h3><p><img src="https://cdn.nlark.com/yuque/__mermaid_v3/31c63119b07a7105f03eb9ef3d74e68f.svg#lake_card_v2=eyJ0eXBlIjoibWVybWFpZCIsImNvZGUiOiJzZXF1ZW5jZURpYWdyYW1cbm1haW5fbWVkaWFzZXJ2ZXIuY3BwIC0-PisgUHJvY2Vzc1N0YXRlOnNlbGYoKVxuUHJvY2Vzc1N0YXRlIC0-PiBQcm9jZXNzU3RhdGU6bmV3IFByb2Nlc3NTdGF0ZShkcml2ZXIpXG5Qcm9jZXNzU3RhdGUgLT4-IFByb2Nlc3NTdGF0ZTpvcGVuX2RyaXZlcihkcml2ZXIpXG5Qcm9jZXNzU3RhdGUgLT4-IEJpbmRlcumpseWKqDpiaW5kZXJfb3BlbigpXG5Qcm9jZXNzU3RhdGUgLT4-IEJpbmRlcumpseWKqDpiaW5kZXJfaW9jdGwoKVxuUHJvY2Vzc1N0YXRlIC0-Pi0gQmluZGVy6amx5YqoOmJpbmRlcl9tbWFwKCkiLCJ1cmwiOiJodHRwczovL2Nkbi5ubGFyay5jb20veXVxdWUvX19tZXJtYWlkX3YzLzMxYzYzMTE5YjA3YTcxMDVmMDNlYjllZjNkNzRlNjhmLnN2ZyIsImlkIjoiYndMUTQiLCJtYXJnaW4iOnsidG9wIjp0cnVlLCJib3R0b20iOnRydWV9LCJjYXJkIjoiZGlhZ3JhbSJ9">ProcessState:self的工作也比较简单，分为以下几步：</p>
<ul>
<li>通过系统调用open()来打开”/dev/binder”，获得一个句柄信息，在Binder驱动中对应的是函数binder_open()</li>
<li>通过ioctl获取binder的版本信息，比较binder协议版本是否相同，不同则跳出；再次通过ioctl设置最大线程数，在Binder驱动中对应的是函数binder_ioctl()</li>
<li>通过mmap内存映射（1M-4K*2）的内存空间，即把binder驱动文件的1016K字节映射到了内存空间，这1016K内存空间为servicemanager使用。在Binder驱动中对应的是函数binder_mmap()。<ul>
<li>_SC_PAGE_SIZE表示一个page页的大小，通常情况下为4K，即(1M - 4K*2)=1016K。</li>
<li>这个page的大小，不同厂家有时候也会调整大小，一般有1M，64K，4K，1KB，通常为4K。</li>
<li>Android11之前servicemanager映射的内存空间是128K。</li>
</ul>
</li>
</ul>
<h2 id="获取ServiceManager代理对象"><a href="#获取ServiceManager代理对象" class="headerlink" title="获取ServiceManager代理对象"></a>获取ServiceManager代理对象</h2><p>当服务进程获取到了ProcessState对象后，接下来要拿到ServiceManager的代理对象(BpServiceManager)。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/IServiceManager.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//AidlServiceManager其实就是IServiceManager</span></span><br><span class="line"><span class="keyword">using</span> AidlServiceManager = android::os::IServiceManager;</span><br><span class="line"></span><br><span class="line"><span class="function">sp&lt;IServiceManager&gt; <span class="title">defaultServiceManager</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    std::<span class="built_in">call_once</span>(gSmOnce, []() &#123;</span><br><span class="line">        sp&lt;AidlServiceManager&gt; sm = <span class="literal">nullptr</span>;</span><br><span class="line">        <span class="keyword">while</span> (sm == <span class="literal">nullptr</span>) &#123;</span><br><span class="line">            sm = interface_cast&lt;AidlServiceManager&gt;(ProcessState::<span class="built_in">self</span>()-&gt;<span class="built_in">getContextObject</span>(<span class="literal">nullptr</span>));</span><br><span class="line">            <span class="keyword">if</span> (sm == <span class="literal">nullptr</span>) &#123;</span><br><span class="line">                <span class="built_in">ALOGE</span>(<span class="string">&quot;Waiting 1s on context object on %s.&quot;</span>, ProcessState::<span class="built_in">self</span>()-&gt;<span class="built_in">getDriverName</span>().<span class="built_in">c_str</span>());</span><br><span class="line">                <span class="built_in">sleep</span>(<span class="number">1</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        gDefaultServiceManager = <span class="keyword">new</span> <span class="built_in">ServiceManagerShim</span>(sm);</span><br><span class="line">    &#125;);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> gDefaultServiceManager;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>获取ServiceManager对象采用单例模式，当gDefaultServiceManager存在，则直接返回，否则创建一个新对象。<br>其创建过程分为几个步骤：</p>
<ul>
<li>调用ProcessState::self()获取ProcessState对象，上一步刚获取，所以全局对象gProcess</li>
<li>调用ProcessState::getContextObject(nullptr)获取获取BpBinder对象</li>
<li>调用interface_cast()获取BpServiceManager对象</li>
</ul>
<h3 id="getContextObject"><a href="#getContextObject" class="headerlink" title="getContextObject()"></a>getContextObject()</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/ProcessState.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function">sp&lt;IBinder&gt; <span class="title">ProcessState::getContextObject</span><span class="params">(<span class="keyword">const</span> sp&lt;IBinder&gt;&amp; <span class="comment">/*caller*/</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    sp&lt;IBinder&gt; context = <span class="built_in">getStrongProxyForHandle</span>(<span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (context == <span class="literal">nullptr</span>) &#123;</span><br><span class="line">       <span class="built_in">ALOGW</span>(<span class="string">&quot;Not able to get context object on %s.&quot;</span>, mDriverName.<span class="built_in">c_str</span>());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// The root object is special since we get it directly from the driver, it is never</span></span><br><span class="line">    <span class="comment">// written by Parcell::writeStrongBinder.</span></span><br><span class="line">    internal::Stability::<span class="built_in">tryMarkCompilationUnit</span>(context.<span class="built_in">get</span>());</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> context;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>getContextObject()调用getStrongProxyForHandle()进行处理，传入handle=0(系统中约定了ServiceManager的handle=0)</p>
<h3 id="getStrongProxyForHandle"><a href="#getStrongProxyForHandle" class="headerlink" title="getStrongProxyForHandle()"></a>getStrongProxyForHandle()</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/ProcessState.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function">sp&lt;IBinder&gt; <span class="title">ProcessState::getStrongProxyForHandle</span><span class="params">(<span class="keyword">int32_t</span> handle)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    sp&lt;IBinder&gt; result;</span><br><span class="line"></span><br><span class="line">    AutoMutex _l(mLock);</span><br><span class="line">    <span class="comment">//查找handle对应的资源项</span></span><br><span class="line">    handle_entry* e = <span class="built_in">lookupHandleLocked</span>(handle);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (e != <span class="literal">nullptr</span>) &#123;</span><br><span class="line">        IBinder* b = e-&gt;binder;</span><br><span class="line">        <span class="keyword">if</span> (b == <span class="literal">nullptr</span> || !e-&gt;refs-&gt;<span class="built_in">attemptIncWeak</span>(<span class="keyword">this</span>)) &#123;</span><br><span class="line">            <span class="keyword">if</span> (handle == <span class="number">0</span>) &#123;</span><br><span class="line"></span><br><span class="line">                Parcel data;</span><br><span class="line">                <span class="comment">//通过ping操作测试binder是否已经准备就绪</span></span><br><span class="line">                <span class="keyword">status_t</span> status = IPCThreadState::<span class="built_in">self</span>()-&gt;<span class="built_in">transact</span>(</span><br><span class="line">                        <span class="number">0</span>, IBinder::PING_TRANSACTION, data, <span class="literal">nullptr</span>, <span class="number">0</span>);</span><br><span class="line">                <span class="keyword">if</span> (status == DEAD_OBJECT)</span><br><span class="line">                   <span class="keyword">return</span> <span class="literal">nullptr</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">//当handle值所对应的IBinder不存在或弱引用无效时，则创建BpBinder对象</span></span><br><span class="line">            <span class="comment">//handle=0</span></span><br><span class="line">            <span class="comment">//create的实现其实就是new BpBinder(0,trackedUid)</span></span><br><span class="line">            b = BpBinder::<span class="built_in">create</span>(handle);</span><br><span class="line">            e-&gt;binder = b;</span><br><span class="line">            <span class="keyword">if</span> (b) e-&gt;refs = b-&gt;<span class="built_in">getWeakRefs</span>();</span><br><span class="line">            result = b;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="comment">// This little bit of nastyness is to allow us to add a primary</span></span><br><span class="line">            <span class="comment">// reference to the remote proxy when this team doesn&#x27;t have one</span></span><br><span class="line">            <span class="comment">// but another team is sending the handle to us.</span></span><br><span class="line">            result.force_set(b);</span><br><span class="line">            e-&gt;refs-&gt;<span class="built_in">decWeak</span>(<span class="keyword">this</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function">ProcessState::handle_entry* <span class="title">ProcessState::lookupHandleLocked</span><span class="params">(<span class="keyword">int32_t</span> handle)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">size_t</span> N=mHandleToObject.<span class="built_in">size</span>();</span><br><span class="line">    <span class="comment">//当handle大于mHandleToObject的长度时，进入该分支</span></span><br><span class="line">    <span class="keyword">if</span> (N &lt;= (<span class="keyword">size_t</span>)handle) &#123;</span><br><span class="line">        handle_entry e;</span><br><span class="line">        e.binder = <span class="literal">nullptr</span>;</span><br><span class="line">        e.refs = <span class="literal">nullptr</span>;</span><br><span class="line">        <span class="comment">//从mHandleToObject的第N个位置开始，插入(handle+1-N)个e到队列中</span></span><br><span class="line">        <span class="keyword">status_t</span> err = mHandleToObject.<span class="built_in">insertAt</span>(e, N, handle+<span class="number">1</span>-N);</span><br><span class="line">        <span class="keyword">if</span> (err &lt; NO_ERROR) <span class="keyword">return</span> <span class="literal">nullptr</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> &amp;mHandleToObject.<span class="built_in">editItemAt</span>(handle);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>当handle=0所对应的IBinder不存在或弱引用无效时，创建BpBinder</li>
<li>通过PING_TRANSACTION来判断ServiceManager是否已经就绪</li>
<li>创建一个BpBinder(0, trackedUid)，同时将handle相对应Binder的弱引用增加1</li>
</ul>
<p>lookupHandleLocked()方法根据handle值来查找对应的handle_entry，handle_entry是一个结构体，里面记录了IBinder和weakref_type两个指针。当handle大于mHandleToObject的Vector长度时，则向Vector中添加(handle+1-N)个handle_entry结构体，然后再返回handle向对应位置的handle_entry结构体指针。</p>
<h3 id="获取BpServiceManager对象"><a href="#获取BpServiceManager对象" class="headerlink" title="获取BpServiceManager对象"></a>获取BpServiceManager对象</h3><h4 id="interface-cast"><a href="#interface-cast" class="headerlink" title="interface_cast()"></a>interface_cast()</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/include/binder/IInterface.h</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> INTERFACE&gt;</span></span><br><span class="line"><span class="function"><span class="keyword">inline</span> sp&lt;INTERFACE&gt; <span class="title">interface_cast</span><span class="params">(<span class="keyword">const</span> sp&lt;IBinder&gt;&amp; obj)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> INTERFACE::<span class="built_in">asInterface</span>(obj);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这里采用了C++的模板函数，因为AidlServiceManager其实就是IServiceManager，interface_cast<AidlServiceManager>其实就是interface_cast<IServiceManager>，也就是调用了IServiceManager::asInterface(obj);  这里的obj就是new BpBinder(0)。</p>
<h4 id="IServiceManager-asInterface"><a href="#IServiceManager-asInterface" class="headerlink" title="IServiceManager::asInterface()"></a>IServiceManager::asInterface()</h4><p>但是IServiceManager中没有看到 asInterface()的实现，还记得前面我聊到DECLARE_META_INTERFACE和IMPLEMENT_META_INTERFACE中实现asInterface()。<br>看过Android 10及之前相关源码的同学应该会对BpServiceManager这个类比较熟悉，它是ServiceManager在其它Binder线程的代理。不过这一部分代码现在已经被删除了。在此之后，BpServiceManager不再通过手动实现，而是采用AID（文件为 IServiceManager.aidl），生成IServiceManager、BnServiceManager、BpServiceManager的头文件及具体实现。<br><a href="https://android-review.googlesource.com/c/platform/frameworks/native/+/1132445">https://android-review.googlesource.com/c/platform/frameworks/native/+/1132445</a><br><a href="https://android.googlesource.com/platform/system/tools/aidl/+/brillo-m10-dev/docs/aidl-cpp.md">https://android.googlesource.com/platform/system/tools/aidl/+/brillo-m10-dev/docs/aidl-cpp.md</a><br>生成的代码如下：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="built_in">DECLARE_META_INTERFACE</span>(ServiceManager)</span><br><span class="line"></span><br><span class="line"><span class="built_in">IMPLEMENT_META_INTERFACE</span>(ServiceManager, <span class="string">&quot;android.os.IServiceManager&quot;</span>)</span><br></pre></td></tr></table></figure>
<p>​</p>
<h5 id="DECLARE-META-INTERFACE-ServiceManager"><a href="#DECLARE-META-INTERFACE-ServiceManager" class="headerlink" title="DECLARE_META_INTERFACE(ServiceManager)"></a>DECLARE_META_INTERFACE(ServiceManager)</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">const</span> ::android::String16 descriptor;</span><br><span class="line">    <span class="keyword">static</span> ::<span class="function">android::sp&lt;IServiceManager&gt; <span class="title">asInterface</span><span class="params">(</span></span></span><br><span class="line"><span class="params"><span class="function">            <span class="keyword">const</span> ::android::sp&lt;::android::IBinder&gt;&amp; obj)</span></span>;</span><br><span class="line">    <span class="keyword">virtual</span> <span class="keyword">const</span> ::<span class="function">android::String16&amp; <span class="title">getInterfaceDescriptor</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line">    <span class="built_in">IServiceManager</span>();</span><br><span class="line">    <span class="keyword">virtual</span> ~<span class="built_in">IServiceManager</span>();</span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">bool</span> <span class="title">setDefaultImpl</span><span class="params">(std::unique_ptr&lt;IServiceManager&gt; impl)</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">const</span> std::unique_ptr&lt;IServiceManager&gt;&amp; <span class="title">getDefaultImpl</span><span class="params">()</span></span>;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">static</span> std::unique_ptr&lt;IServiceManager&gt; default_impl;</span><br><span class="line"><span class="keyword">public</span>:</span><br></pre></td></tr></table></figure>
<p>该过程主要是声明asInterface()、getInterfaceDescriptor()方法。</p>
<h5 id="IMPLEMENT-META-INTERFACE-ServiceManager-“android-os-IServiceManager”"><a href="#IMPLEMENT-META-INTERFACE-ServiceManager-“android-os-IServiceManager”" class="headerlink" title="IMPLEMENT_META_INTERFACE(ServiceManager, “android.os.IServiceManager”)"></a>IMPLEMENT_META_INTERFACE(ServiceManager, “android.os.IServiceManager”)</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> ::<span class="function">android::String16 <span class="title">IServiceManager::descriptor</span><span class="params">(<span class="string">&quot;android.os.IServiceManager&quot;</span>)</span></span>;</span><br><span class="line"><span class="keyword">const</span> ::<span class="function">android::String16&amp;</span></span><br><span class="line"><span class="function">    <span class="title">IServiceManager::getInterfaceDescriptor</span><span class="params">()</span> <span class="keyword">const</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> IServiceManager::descriptor;</span><br><span class="line">&#125;</span><br><span class="line">::<span class="function">android::sp&lt;IServiceManager&gt; <span class="title">IServiceManager::asInterface</span><span class="params">(</span></span></span><br><span class="line"><span class="params"><span class="function">    <span class="keyword">const</span> ::android::sp&lt;::android::IBinder&gt;&amp; obj)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    ::android::sp&lt;IServiceManager&gt; intr;</span><br><span class="line">    <span class="keyword">if</span> (obj != <span class="literal">nullptr</span>) &#123;</span><br><span class="line">        intr = <span class="keyword">static_cast</span>&lt;IServiceManager*&gt;(</span><br><span class="line">            obj-&gt;<span class="built_in">queryLocalInterface</span>(</span><br><span class="line">                IServiceManager::descriptor).<span class="built_in">get</span>());</span><br><span class="line">        <span class="keyword">if</span> (intr == <span class="literal">nullptr</span>) &#123;</span><br><span class="line">            intr = <span class="keyword">new</span> <span class="built_in">BpServiceManager</span>(obj);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> intr;</span><br><span class="line">&#125;</span><br><span class="line">std::unique_ptr&lt;IServiceManager&gt; IServiceManager::default_impl;</span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">IServiceManager::setDefaultImpl</span><span class="params">(std::unique_ptr&lt;IServiceManager&gt; impl)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!IServiceManager::default_impl &amp;&amp; impl) &#123;</span><br><span class="line">        IServiceManager::default_impl = std::<span class="built_in">move</span>(impl);</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">const</span> std::unique_ptr&lt;IServiceManager&gt;&amp; <span class="title">IServiceManager::getDefaultImpl</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> IServiceManager::default_impl;</span><br><span class="line">&#125;</span><br><span class="line">IServiceManager::<span class="built_in">IServiceManager</span>() &#123; &#125;</span><br><span class="line">IServiceManager::~<span class="built_in">IServiceManager</span>() &#123; &#125;</span><br></pre></td></tr></table></figure>
<p>最终IServiceManager::asInterface()等价于new Bp##INTERFACE(obj)，即new BpServiceManager(obj)，所以gDefaultServiceManager = new ServiceManagerShim(sm);转换后，变成gDefaultServiceManager = new ServiceManagerShim(new BpBinder(0，trackedUid));</p>
<h2 id="MediaPlayerService-instantiate"><a href="#MediaPlayerService-instantiate" class="headerlink" title="MediaPlayerService::instantiate()"></a>MediaPlayerService::instantiate()</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/av/media/libmediaplayerservice/MediaPlayerService.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">MediaPlayerService::instantiate</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="built_in">defaultServiceManager</span>()-&gt;<span class="built_in">addService</span>(</span><br><span class="line">            <span class="built_in">String16</span>(<span class="string">&quot;media.player&quot;</span>), <span class="keyword">new</span> <span class="built_in">MediaPlayerService</span>());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>创建一个新的Service——BnMediaPlayerService，想把它告诉ServiceManager。然后调用BnServiceManager的addService的addService来向ServiceManager中添加一个Service，其他进程可以通过字符串”media.player”来向ServiceManager查询此服务。</li>
<li>注册服务MediaPlayerService：由defaultServiceManager()返回的是BpServiceManager，同时会创建ProcessState对象和BpBinder对象。故此处等价于调用BpServiceManager-&gt;addService。</li>
</ul>
<h3 id="类图"><a href="#类图" class="headerlink" title="类图"></a>类图</h3><blockquote>
<p>懒得画图，复用之前的图片，把图中XXX换成MediaPlayerService即可。</p>
</blockquote>
<p>MediaPlayerService继承自BnMediaPlayerService，BnMediaPlayerService继承自BnInterface，BnInterface继承自BBinder，最终转换为一个Binder实体，也就是BBinder对象。<br><img src="https://cdn.nlark.com/yuque/0/2021/png/1759879/1634111763519-79b3c0f2-cf57-4aaa-bd66-42d25abbadf0.png#clientId=ucfce5313-484a-4&from=paste&id=byXwQ&margin=%5Bobject%20Object%5D&name=image.png&originHeight=2018&originWidth=2000&originalType=url&ratio=1&size=451147&status=done&style=none&taskId=u2315f0bc-66bd-43c9-9d49-a4008e39484" alt="image.png"></p>
<h3 id="BpServiceManager-addService"><a href="#BpServiceManager-addService" class="headerlink" title="BpServiceManager.addService()"></a>BpServiceManager.addService()</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/IServiceManager.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">status_t</span> <span class="title">ServiceManagerShim::addService</span><span class="params">(<span class="keyword">const</span> String16&amp; name, <span class="keyword">const</span> sp&lt;IBinder&gt;&amp; service,</span></span></span><br><span class="line"><span class="params"><span class="function">                                        <span class="keyword">bool</span> allowIsolated, <span class="keyword">int</span> dumpsysPriority)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Status status = mTheRealServiceManager-&gt;<span class="built_in">addService</span>(</span><br><span class="line">        <span class="built_in">String8</span>(name).<span class="built_in">c_str</span>(), service, allowIsolated, dumpsysPriority);</span><br><span class="line">    <span class="keyword">return</span> status.<span class="built_in">exceptionCode</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>添加Service到ServiceManager。</p>
<h3 id="ServiceManager-addService"><a href="#ServiceManager-addService" class="headerlink" title="ServiceManager.addService()"></a>ServiceManager.addService()</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/cmds/servicemanager/ServiceManager.h</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//定义Service结构体</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Service</span> &#123;</span></span><br><span class="line">    sp&lt;IBinder&gt; binder; <span class="comment">// not null</span></span><br><span class="line">    <span class="keyword">bool</span> allowIsolated;</span><br><span class="line">    <span class="keyword">int32_t</span> dumpPriority;</span><br><span class="line">    <span class="keyword">bool</span> hasClients = <span class="literal">false</span>; <span class="comment">// notifications sent on true -&gt; false.</span></span><br><span class="line">    <span class="keyword">bool</span> guaranteeClient = <span class="literal">false</span>; <span class="comment">// forces the client check to true</span></span><br><span class="line">    <span class="keyword">pid_t</span> debugPid = <span class="number">0</span>; <span class="comment">// the process in which this service runs</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// the number of clients of the service, including servicemanager itself</span></span><br><span class="line">    <span class="function"><span class="keyword">ssize_t</span> <span class="title">getNodeStrongRefCount</span><span class="params">()</span></span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> ServiceMap = std::map&lt;std::string, Service&gt;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//用于保存添加进来的Services</span></span><br><span class="line">ServiceMap mNameToService;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/cmds/servicemanager/ServiceManager.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function">Status <span class="title">ServiceManager::addService</span><span class="params">(<span class="keyword">const</span> std::string&amp; name, <span class="keyword">const</span> sp&lt;IBinder&gt;&amp; binder, <span class="keyword">bool</span> allowIsolated, <span class="keyword">int32_t</span> dumpPriority)</span> </span>&#123;</span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="comment">//添加 Service 到 mNameToService 中，完成 Binder Service 注册过程</span></span><br><span class="line">    mNameToService[name] = Service &#123;</span><br><span class="line">        .binder = binder,</span><br><span class="line">        .allowIsolated = allowIsolated,</span><br><span class="line">        .dumpPriority = dumpPriority,</span><br><span class="line">        .debugPid = ctx.debugPid,</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">auto</span> it = mNameToRegistrationCallback.<span class="built_in">find</span>(name);</span><br><span class="line">    <span class="keyword">if</span> (it != mNameToRegistrationCallback.<span class="built_in">end</span>()) &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">const</span> sp&lt;IServiceCallback&gt;&amp; cb : it-&gt;second) &#123;</span><br><span class="line">            mNameToService[name].guaranteeClient = <span class="literal">true</span>;</span><br><span class="line">            <span class="comment">// permission checked in registerForNotifications</span></span><br><span class="line">            cb-&gt;<span class="built_in">onRegistration</span>(name, binder);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> Status::<span class="built_in">ok</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这一个过程在<a href="https://i-rtfsc.github.io/archives/f17b1d52.html">Binder机制02-ServiceManager</a>章节中有讲解，这里就不展开描述。</p>
<h2 id="小结-1"><a href="#小结-1" class="headerlink" title="小结"></a>小结</h2><ul>
<li>服务进程先获得一个 ProcessState()的对象</li>
<li>获取ServiceManager的代理对象BpServiceManager，主要通过new BpBinder(0，xxx)得到</li>
<li>调用BpServiceManager的addService，ServiceManager保存到mNameToService中</li>
<li>通过ProcessState::startThreadPool启动线程池</li>
<li>通过IPCThreadState::joinThreadPool将主线程加入的Binder中</li>
<li>通过::android::hardware::joinRpcThreadpool()将主线程加入HwBinder的线程池进行循环</li>
</ul>
<h1 id="获取Native服务（media服务）"><a href="#获取Native服务（media服务）" class="headerlink" title="获取Native服务（media服务）"></a>获取Native服务（media服务）</h1><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/av/media/libmedia/IMediaDeathNotifier.cpp</span></span><br><span class="line"></span><br><span class="line">IMediaDeathNotifier::<span class="built_in">getMediaPlayerService</span>()</span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">ALOGV</span>(<span class="string">&quot;getMediaPlayerService&quot;</span>);</span><br><span class="line">    Mutex::Autolock _l(sServiceLock);</span><br><span class="line">    <span class="keyword">if</span> (sMediaPlayerService == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="comment">//获取ServiceManager的代理对象BpServiceManager</span></span><br><span class="line">        sp&lt;IServiceManager&gt; sm = <span class="built_in">defaultServiceManager</span>();</span><br><span class="line">        sp&lt;IBinder&gt; binder;</span><br><span class="line">        <span class="keyword">do</span> &#123;</span><br><span class="line">            <span class="comment">//调用BpServiceManager的getService接口，来获取服务名称为“media.player”的Binder服务对象</span></span><br><span class="line">            binder = sm-&gt;<span class="built_in">getService</span>(<span class="built_in">String16</span>(<span class="string">&quot;media.player&quot;</span>));</span><br><span class="line">            <span class="keyword">if</span> (binder != <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="built_in">ALOGW</span>(<span class="string">&quot;Media player service not published, waiting...&quot;</span>);</span><br><span class="line">            <span class="comment">//如果media的服务还没有准备好，休眠0.5秒进行等待</span></span><br><span class="line">            <span class="built_in">usleep</span>(<span class="number">500000</span>); <span class="comment">// 0.5 s</span></span><br><span class="line">        &#125; <span class="keyword">while</span> (<span class="literal">true</span>);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (sDeathNotifier == <span class="literal">NULL</span>) &#123;</span><br><span class="line">            <span class="comment">//创建死亡通知对象</span></span><br><span class="line">            sDeathNotifier = <span class="keyword">new</span> <span class="built_in">DeathNotifier</span>();</span><br><span class="line">        &#125;</span><br><span class="line">        binder-&gt;<span class="built_in">linkToDeath</span>(sDeathNotifier);</span><br><span class="line">        <span class="comment">//转换得到服务对象</span></span><br><span class="line">        sMediaPlayerService = interface_cast&lt;IMediaPlayerService&gt;(binder);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">ALOGE_IF</span>(sMediaPlayerService == <span class="number">0</span>, <span class="string">&quot;no media player service!?&quot;</span>);</span><br><span class="line">    <span class="keyword">return</span> sMediaPlayerService;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>获取ServiceManager的代理对象BpServiceManager</li>
<li>调用BpServiceManager的getService接口，来获取服务名称为“media.player”的Binder服务对象</li>
<li>创建死亡通知对象</li>
<li>将死亡通知连接到binder</li>
<li>得到服务对象</li>
</ul>
<h2 id="获取ServiceManager代理对象-1"><a href="#获取ServiceManager代理对象-1" class="headerlink" title="获取ServiceManager代理对象"></a>获取ServiceManager代理对象</h2><p>其中defaultServiceManager()过程在上面已经说了。</p>
<h2 id="BpServiceManager-getService"><a href="#BpServiceManager-getService" class="headerlink" title="BpServiceManager.getService()"></a>BpServiceManager.getService()</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/IServiceManager.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function">sp&lt;IBinder&gt; <span class="title">ServiceManagerShim::getService</span><span class="params">(<span class="keyword">const</span> String16&amp; name)</span> <span class="keyword">const</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">bool</span> gSystemBootCompleted = <span class="literal">false</span>;</span><br><span class="line">    <span class="comment">//检查指定服务是否存在</span></span><br><span class="line">    sp&lt;IBinder&gt; svc = <span class="built_in">checkService</span>(name);</span><br><span class="line">    <span class="keyword">if</span> (svc != <span class="literal">nullptr</span>) <span class="keyword">return</span> svc;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//如果ProcessState和Binder驱动交互的是&quot;/dev/vndbinder&quot;，那么isVendorService为True，表明Vendor进程之间可以进行Binder通信</span></span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">bool</span> isVendorService =</span><br><span class="line">        <span class="built_in">strcmp</span>(ProcessState::<span class="built_in">self</span>()-&gt;<span class="built_in">getDriverName</span>().<span class="built_in">c_str</span>(), <span class="string">&quot;/dev/vndbinder&quot;</span>) == <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">long</span> timeout = <span class="built_in">uptimeMillis</span>() + <span class="number">5000</span>;</span><br><span class="line">    <span class="comment">// Vendor code can&#x27;t access system properties</span></span><br><span class="line">    <span class="keyword">if</span> (!gSystemBootCompleted &amp;&amp; !isVendorService) &#123;</span><br><span class="line">    <span class="comment">//vendor分区的代码不能访问system的属性</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifdef</span> __ANDROID__</span></span><br><span class="line">        <span class="keyword">char</span> bootCompleted[PROPERTY_VALUE_MAX];</span><br><span class="line">        <span class="built_in">property_get</span>(<span class="string">&quot;sys.boot_completed&quot;</span>, bootCompleted, <span class="string">&quot;0&quot;</span>);</span><br><span class="line">        gSystemBootCompleted = <span class="built_in">strcmp</span>(bootCompleted, <span class="string">&quot;1&quot;</span>) == <span class="number">0</span> ? <span class="literal">true</span> : <span class="literal">false</span>;</span><br><span class="line"><span class="meta">#<span class="meta-keyword">else</span></span></span><br><span class="line">        gSystemBootCompleted = <span class="literal">true</span>;</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// retry interval in millisecond; note that vendor services stay at 100ms</span></span><br><span class="line">    <span class="comment">//vendor分区的服务sleepTime 为100ms， system为1000ms</span></span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">long</span> sleepTime = gSystemBootCompleted ? <span class="number">1000</span> : <span class="number">100</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> n = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">while</span> (<span class="built_in">uptimeMillis</span>() &lt; timeout) &#123;</span><br><span class="line">        n++;</span><br><span class="line">        <span class="built_in">ALOGI</span>(<span class="string">&quot;Waiting for service &#x27;%s&#x27; on &#x27;%s&#x27;...&quot;</span>, <span class="built_in">String8</span>(name).<span class="built_in">string</span>(),</span><br><span class="line">            ProcessState::<span class="built_in">self</span>()-&gt;<span class="built_in">getDriverName</span>().<span class="built_in">c_str</span>());</span><br><span class="line">        <span class="built_in">usleep</span>(<span class="number">1000</span>*sleepTime);</span><br><span class="line"></span><br><span class="line">        sp&lt;IBinder&gt; svc = <span class="built_in">checkService</span>(name);</span><br><span class="line">        <span class="keyword">if</span> (svc != <span class="literal">nullptr</span>) <span class="keyword">return</span> svc;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">ALOGW</span>(<span class="string">&quot;Service %s didn&#x27;t start. Returning NULL&quot;</span>, <span class="built_in">String8</span>(name).<span class="built_in">string</span>());</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">nullptr</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>getService()的核心就是调用checkService()来检查服务是否存在，如果不存在，继续等待查找。注意：vendor分区的服务等待时间为100ms，system分区的为1000ms.</p>
<h2 id="BpServiceManager-checkService"><a href="#BpServiceManager-checkService" class="headerlink" title="BpServiceManager.checkService()"></a>BpServiceManager.checkService()</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/IServiceManager.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function">sp&lt;IBinder&gt; <span class="title">ServiceManagerShim::checkService</span><span class="params">(<span class="keyword">const</span> String16&amp; name)</span> <span class="keyword">const</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    sp&lt;IBinder&gt; ret;</span><br><span class="line">    <span class="keyword">if</span> (!mTheRealServiceManager-&gt;<span class="built_in">checkService</span>(<span class="built_in">String8</span>(name).<span class="built_in">c_str</span>(), &amp;ret).<span class="built_in">isOk</span>()) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nullptr</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> ret;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>checkService()和前面的addService()类似，最终调到ServiceManager.cpp里。</p>
<h2 id="ServiceManager-checkService"><a href="#ServiceManager-checkService" class="headerlink" title="ServiceManager.checkService()"></a>ServiceManager.checkService()</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">ttp:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/cmds/servicemanager/ServiceManager.h</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//定义Service结构体</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Service</span> &#123;</span></span><br><span class="line">    sp&lt;IBinder&gt; binder; <span class="comment">// not null</span></span><br><span class="line">    <span class="keyword">bool</span> allowIsolated;</span><br><span class="line">    <span class="keyword">int32_t</span> dumpPriority;</span><br><span class="line">    <span class="keyword">bool</span> hasClients = <span class="literal">false</span>; <span class="comment">// notifications sent on true -&gt; false.</span></span><br><span class="line">    <span class="keyword">bool</span> guaranteeClient = <span class="literal">false</span>; <span class="comment">// forces the client check to true</span></span><br><span class="line">    <span class="keyword">pid_t</span> debugPid = <span class="number">0</span>; <span class="comment">// the process in which this service runs</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// the number of clients of the service, including servicemanager itself</span></span><br><span class="line">    <span class="function"><span class="keyword">ssize_t</span> <span class="title">getNodeStrongRefCount</span><span class="params">()</span></span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> ServiceMap = std::map&lt;std::string, Service&gt;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//用于保存添加进来的Services</span></span><br><span class="line">ServiceMap mNameToService;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/cmds/servicemanager/ServiceManager.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function">Status <span class="title">ServiceManager::checkService</span><span class="params">(<span class="keyword">const</span> std::string&amp; name, sp&lt;IBinder&gt;* outBinder)</span> </span>&#123;</span><br><span class="line">    *outBinder = <span class="built_in">tryGetService</span>(name, <span class="literal">false</span>);</span><br><span class="line">    <span class="comment">// returns ok regardless of result for legacy reasons</span></span><br><span class="line">    <span class="keyword">return</span> Status::<span class="built_in">ok</span>();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function">sp&lt;IBinder&gt; <span class="title">ServiceManager::tryGetService</span><span class="params">(<span class="keyword">const</span> std::string&amp; name, <span class="keyword">bool</span> startIfNotFound)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">auto</span> ctx = mAccess-&gt;<span class="built_in">getCallingContext</span>();</span><br><span class="line"></span><br><span class="line">    sp&lt;IBinder&gt; out;</span><br><span class="line">    Service* service = <span class="literal">nullptr</span>;</span><br><span class="line">    <span class="comment">//查找mNameToServices是否存在名字为name的Service</span></span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">auto</span> it = mNameToService.<span class="built_in">find</span>(name); it != mNameToService.<span class="built_in">end</span>()) &#123;</span><br><span class="line">        service = &amp;(it-&gt;second);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (!service-&gt;allowIsolated) &#123;</span><br><span class="line">            <span class="keyword">uid_t</span> appid = <span class="built_in">multiuser_get_app_id</span>(ctx.uid);</span><br><span class="line">            <span class="keyword">bool</span> isIsolated = appid &gt;= AID_ISOLATED_START &amp;&amp; appid &lt;= AID_ISOLATED_END;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (isIsolated) &#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">nullptr</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        out = service-&gt;binder;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!mAccess-&gt;<span class="built_in">canFind</span>(ctx, name)) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nullptr</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!out &amp;&amp; startIfNotFound) &#123;</span><br><span class="line">        <span class="built_in">tryStartService</span>(name);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (out) &#123;</span><br><span class="line">        <span class="comment">// Setting this guarantee each time we hand out a binder ensures that the client-checking</span></span><br><span class="line">        <span class="comment">// loop knows about the event even if the client immediately drops the service</span></span><br><span class="line">        service-&gt;guaranteeClient = <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> out;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>到此我们就拿到服务，拿到服务后可以调用linkToDeath()函数监听服务进程的状态，前面我们有聊到过如何处罚linkToDeath，这里就不再展开描述。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>不过是服务的注册还是获取，都是通过ServiceManager交互。说得简单一点其实就是：</p>
<ul>
<li>通过调用defaultServiceManager()获取ServiceManager的代理对象BpServiceManager</li>
<li>调用BpServiceManager.addService()获取BpServiceManager.getService()</li>
</ul>
<p>当然在实际coding过程中没这么简单，在后续得章节中我们会写一个native服务demo，这过程中就不难发现其实出来刚才说得，其实还需要添加seliunx、编译文件、启动脚本等等。</p>
]]></content>
      <categories>
        <category>Android</category>
      </categories>
      <tags>
        <tag>Android</tag>
        <tag>Binder</tag>
      </tags>
  </entry>
  <entry>
    <title>Binder机制02-ServiceManager</title>
    <url>/archives/f17b1d52.html</url>
    <content><![CDATA[<span id="more"></span>
<meta name="referrer" content="no-referrer"/>


<h1 id="Binder机制文章列表"><a href="#Binder机制文章列表" class="headerlink" title="Binder机制文章列表"></a>Binder机制文章列表</h1><p><a href="https://i-rtfsc.github.io/archives/d6aea0f1.html">Binder概述</a><br><a href="https://i-rtfsc.github.io/archives/3e87e0ce.html">Binder机制01-驱动</a><br><a href="https://i-rtfsc.github.io/archives/f17b1d52.html">Binder机制02-ServiceManager</a><br><a href="https://i-rtfsc.github.io/archives/937d16d8.html">Binder机制03-Framework-Native</a><br><a href="https://i-rtfsc.github.io/archives/73029508.html">Binder机制04-Framework-Jave</a><br><a href="https://i-rtfsc.github.io/archives/ed3eb50b.html">Binder机制05-AIDL</a><br><a href="https://i-rtfsc.github.io/archives/4e7f84b4.html">Binder机制06-框架</a><br><a href="https://i-rtfsc.github.io/archives/d30c7ecb.html">Binder机制07-实例分析</a><br><a href="https://i-rtfsc.github.io/archives/720ded9.html">Binder机制08-总结</a><br><a href="https://i-rtfsc.github.io/archives/f1684132.html">Ashmem简介（Android IPC传输大数据）</a></p>
<p>Service Manager是整个Binder机制的守护进程，用来管理开发者创建的各种Server，并且向Client提供查询Server远程接口的功能。Service Manager在init.rc中作为service启动，运行在一个独立的进程中。而Client和Server也运行在独立的进程中。如果Client和Server需要进行通信，Client就需要向Service Manager查询服务，获得Server才能正常跟Server通信。Service Manager在Binder机制的守护进程的角色的同时，也在充当Server的角色。它是一种特殊的Server。</p>
<h1 id="ServiceManager启动"><a href="#ServiceManager启动" class="headerlink" title="ServiceManager启动"></a>ServiceManager启动</h1><p>init进程启动时，加载servicemanager.rc，启动serviecmanager。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/system/core/rootdir/init.rc</span></span><br><span class="line"></span><br><span class="line">on init</span><br><span class="line">    sysclktz <span class="number">0</span></span><br><span class="line"></span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    # Start essential services.</span><br><span class="line">    start servicemanager</span><br><span class="line">    start hwservicemanager</span><br><span class="line">    start vndservicemanager</span><br></pre></td></tr></table></figure>
<p>三个服务的定义如链接中的代码，我们只以servicemanager举例。<br><a href="http://aospxref.com/android-11.0.0_r21/xref/frameworks/native/cmds/servicemanager/servicemanager.rc">servicemanager</a><br><a href="http://aospxref.com/android-11.0.0_r21/xref/frameworks/native/cmds/servicemanager/vndservicemanager.rc">hwservicemanager</a><br><a href="http://aospxref.com/android-11.0.0_r21/xref/system/hwservicemanager/hwservicemanager.rc">vndservicemanager</a></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/cmds/servicemanager/servicemanager.rc</span></span><br><span class="line"></span><br><span class="line">service servicemanager /system/bin/servicemanager</span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">core</span> <span class="title">animation</span></span></span><br><span class="line"><span class="class">    <span class="title">user</span> <span class="title">system</span></span></span><br><span class="line"><span class="class">    <span class="title">group</span> <span class="title">system</span> <span class="title">readproc</span></span></span><br><span class="line"><span class="class">    <span class="title">critical</span></span></span><br><span class="line"><span class="class">    <span class="title">onrestart</span> <span class="title">restart</span> <span class="title">apexd</span></span></span><br><span class="line"><span class="class">    <span class="title">onrestart</span> <span class="title">restart</span> <span class="title">audioserver</span></span></span><br><span class="line"><span class="class">    <span class="title">onrestart</span> <span class="title">restart</span> <span class="title">gatekeeperd</span></span></span><br><span class="line"><span class="class">    <span class="title">onrestart</span> <span class="title">class_restart</span> <span class="title">main</span></span></span><br><span class="line"><span class="class">    <span class="title">onrestart</span> <span class="title">class_restart</span> <span class="title">hal</span></span></span><br><span class="line"><span class="class">    <span class="title">onrestart</span> <span class="title">class_restart</span> <span class="title">early_hal</span></span></span><br><span class="line"><span class="class">    <span class="title">writepid</span> /<span class="title">dev</span>/<span class="title">cpuset</span>/<span class="title">system</span>-<span class="title">background</span>/<span class="title">tasks</span></span></span><br><span class="line"><span class="class">    <span class="title">shutdown</span> <span class="title">critical</span></span></span><br></pre></td></tr></table></figure>
<p>从上面的rc文件可知道，servicemanager编译后，位于手机的/system/bin中，这是一个Native C/C++的进程，C/C++的进程都会有一个main()的入口。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/cmds/servicemanager/Android.bp</span></span><br><span class="line"></span><br><span class="line">cc_binary &#123;</span><br><span class="line">    name: <span class="string">&quot;servicemanager&quot;</span>,</span><br><span class="line">    defaults: [<span class="string">&quot;servicemanager_defaults&quot;</span>],</span><br><span class="line">    init_rc: [<span class="string">&quot;servicemanager.rc&quot;</span>],</span><br><span class="line">    srcs: [<span class="string">&quot;main.cpp&quot;</span>],</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>通过查看Android.bp我们找到了servicemanager的入口：main.cpp的main()。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/cmds/servicemanager/main.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span>** argv)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (argc &gt; <span class="number">2</span>) &#123;</span><br><span class="line">        <span class="built_in">LOG</span>(FATAL) &lt;&lt; <span class="string">&quot;usage: &quot;</span> &lt;&lt; argv[<span class="number">0</span>] &lt;&lt; <span class="string">&quot; [binder driver]&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//servicemanager和vndservicemanager共享一套代码，vndservicemanager传入参数&quot;/dev/vndbinder&quot;</span></span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span>* driver = argc == <span class="number">2</span> ? argv[<span class="number">1</span>] : <span class="string">&quot;/dev/binder&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//android11跟之前的代码差异比较大，打开binder设备文件是在ProcessState里执行</span></span><br><span class="line">    <span class="comment">//下文我们将重点分析initWithDriver都做里什么事情</span></span><br><span class="line">    sp&lt;ProcessState&gt; ps = ProcessState::<span class="built_in">initWithDriver</span>(driver);</span><br><span class="line">    ps-&gt;<span class="built_in">setThreadPoolMaxThreadCount</span>(<span class="number">0</span>);</span><br><span class="line">    ps-&gt;<span class="built_in">setCallRestriction</span>(ProcessState::CallRestriction::FATAL_IF_NOT_ONEWAY);</span><br><span class="line">    <span class="comment">//分配一个实例</span></span><br><span class="line">    sp&lt;ServiceManager&gt; manager = <span class="keyword">new</span> <span class="built_in">ServiceManager</span>(std::make_unique&lt;Access&gt;());</span><br><span class="line">    <span class="comment">//将 ServiceManager对象插入ServiceManager::mNameToService中</span></span><br><span class="line">    <span class="keyword">if</span> (!manager-&gt;<span class="built_in">addService</span>(<span class="string">&quot;manager&quot;</span>, manager, <span class="literal">false</span> <span class="comment">/*allowIsolated*/</span>, IServiceManager::DUMP_FLAG_PRIORITY_DEFAULT).<span class="built_in">isOk</span>()) &#123;</span><br><span class="line">        <span class="built_in">LOG</span>(ERROR) &lt;&lt; <span class="string">&quot;Could not self register servicemanager&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//告诉binder驱动自己是binder上下文管理者</span></span><br><span class="line">    IPCThreadState::<span class="built_in">self</span>()-&gt;<span class="built_in">setTheContextObject</span>(manager);</span><br><span class="line">    ps-&gt;<span class="built_in">becomeContextManager</span>();</span><br><span class="line"></span><br><span class="line">    sp&lt;Looper&gt; looper = Looper::<span class="built_in">prepare</span>(<span class="literal">false</span> <span class="comment">/*allowNonCallbacks*/</span>);</span><br><span class="line"></span><br><span class="line">    BinderCallback::<span class="built_in">setupTo</span>(looper);</span><br><span class="line">    ClientCallbackCallback::<span class="built_in">setupTo</span>(looper, manager);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//进入一个无穷循环，充当Server的角色，等待Client的请求</span></span><br><span class="line">    <span class="keyword">while</span>(<span class="literal">true</span>) &#123;</span><br><span class="line">        <span class="comment">//进入循环，调用 epoll_wait()等待消息</span></span><br><span class="line">        <span class="comment">//有消息后调用BinderCallback::handleEvent()</span></span><br><span class="line">        looper-&gt;<span class="built_in">pollAll</span>(<span class="number">-1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// should not be reached</span></span><br><span class="line">    <span class="keyword">return</span> EXIT_FAILURE;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在Android8.0后，谷歌引入Treble机制，binder机制增加了hwbinder和vndbinder，其中vndbinder的守护进程为vndservicemanager。<br>vndservicemanager和servicemanager共用同一份代码，只是传入的参数和宏控制的流程有部分差异。vndservicemanager会传入参数”/dev/vndbinder”, servicemanager使用默认的”/dev/binder”。</p>
<h1 id="ProcessState-initWithDriver"><a href="#ProcessState-initWithDriver" class="headerlink" title="ProcessState.initWithDriver()"></a>ProcessState.initWithDriver()</h1><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/ProcessState.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function">sp&lt;ProcessState&gt; <span class="title">ProcessState::initWithDriver</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span>* driver)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Mutex::Autolock _l(gProcessMutex);</span><br><span class="line">    <span class="keyword">if</span> (gProcess != <span class="literal">nullptr</span>) &#123;</span><br><span class="line">        <span class="comment">// Allow for initWithDriver to be called repeatedly with the same</span></span><br><span class="line">        <span class="comment">// driver.</span></span><br><span class="line">        <span class="keyword">if</span> (!<span class="built_in">strcmp</span>(gProcess-&gt;<span class="built_in">getDriverName</span>().<span class="built_in">c_str</span>(), driver)) &#123;</span><br><span class="line">            <span class="keyword">return</span> gProcess;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="built_in">LOG_ALWAYS_FATAL</span>(<span class="string">&quot;ProcessState was already initialized.&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">access</span>(driver, R_OK) == <span class="number">-1</span>) &#123;</span><br><span class="line">        <span class="built_in">ALOGE</span>(<span class="string">&quot;Binder driver %s is unavailable. Using /dev/binder instead.&quot;</span>, driver);</span><br><span class="line">        driver = <span class="string">&quot;/dev/binder&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    gProcess = <span class="keyword">new</span> <span class="built_in">ProcessState</span>(driver);</span><br><span class="line">    <span class="keyword">return</span> gProcess;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>initWithDriver函数没看到有什么玄机，那应该是在ProcessState的构造函数里。</p>
<h2 id="new-ProcessState-driver"><a href="#new-ProcessState-driver" class="headerlink" title="new ProcessState(driver)"></a>new ProcessState(driver)</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/ProcessState.cpp</span></span><br><span class="line"></span><br><span class="line">ProcessState::<span class="built_in">ProcessState</span>(<span class="keyword">const</span> <span class="keyword">char</span> *driver)</span><br><span class="line">    : <span class="built_in">mDriverName</span>(<span class="built_in">String8</span>(driver))</span><br><span class="line">    , <span class="built_in">mDriverFD</span>(<span class="built_in">open_driver</span>(driver))</span><br><span class="line">    , <span class="built_in">mVMStart</span>(MAP_FAILED)</span><br><span class="line">    , <span class="built_in">mThreadCountLock</span>(PTHREAD_MUTEX_INITIALIZER)</span><br><span class="line">    , <span class="built_in">mThreadCountDecrement</span>(PTHREAD_COND_INITIALIZER)</span><br><span class="line">    , <span class="built_in">mExecutingThreadsCount</span>(<span class="number">0</span>)</span><br><span class="line">    , <span class="built_in">mMaxThreads</span>(DEFAULT_MAX_BINDER_THREADS)</span><br><span class="line">    , <span class="built_in">mStarvationStartTimeMs</span>(<span class="number">0</span>)</span><br><span class="line">    , <span class="built_in">mThreadPoolStarted</span>(<span class="literal">false</span>)</span><br><span class="line">    , <span class="built_in">mThreadPoolSeq</span>(<span class="number">1</span>)</span><br><span class="line">    , <span class="built_in">mCallRestriction</span>(CallRestriction::NONE)</span><br><span class="line">&#123;</span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (mDriverFD &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="comment">// mmap the binder, providing a chunk of virtual address space to receive transactions.</span></span><br><span class="line">        <span class="comment">//#define BINDER_VM_SIZE ((1 * 1024 * 1024) - sysconf(_SC_PAGE_SIZE) * 2)</span></span><br><span class="line">        <span class="comment">//_SC_PAGE_SIZE表示一个page页的大小，通常情况下为4K</span></span><br><span class="line">        <span class="comment">//3. 通过mmap内存映射（1M-4K*2）的内存空间</span></span><br><span class="line">        mVMStart = <span class="built_in">mmap</span>(<span class="literal">nullptr</span>, BINDER_VM_SIZE, PROT_READ, MAP_PRIVATE | MAP_NORESERVE, mDriverFD, <span class="number">0</span>);</span><br><span class="line">        <span class="keyword">if</span> (mVMStart == MAP_FAILED) &#123;</span><br><span class="line">            <span class="comment">// *sigh*</span></span><br><span class="line">            <span class="built_in">ALOGE</span>(<span class="string">&quot;Using %s failed: unable to mmap transaction memory.\n&quot;</span>, mDriverName.<span class="built_in">c_str</span>());</span><br><span class="line">            <span class="built_in">close</span>(mDriverFD);</span><br><span class="line">            mDriverFD = <span class="number">-1</span>;</span><br><span class="line">            mDriverName.<span class="built_in">clear</span>();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这里看到了很多有用的信息，设置驱动的名字、最大线程数等等。还调用了open_driver(driver)和mmap(nullptr, BINDER_VM_SIZE, PROT_READ, MAP_PRIVATE | MAP_NORESERVE, mDriverFD, 0)。<br>​</p>
<h2 id="open-driver-driver"><a href="#open-driver-driver" class="headerlink" title="open_driver(driver)"></a>open_driver(driver)</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/ProcessState.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">open_driver</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *driver)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">//1. 打开Binder设备驱动</span></span><br><span class="line">    <span class="keyword">int</span> fd = <span class="built_in">open</span>(driver, O_RDWR | O_CLOEXEC);</span><br><span class="line">    <span class="keyword">if</span> (fd &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">int</span> vers = <span class="number">0</span>;</span><br><span class="line">        <span class="comment">//2. 获取Binder的版本信息</span></span><br><span class="line">        <span class="keyword">status_t</span> result = <span class="built_in">ioctl</span>(fd, BINDER_VERSION, &amp;vers);</span><br><span class="line">        <span class="keyword">if</span> (result == <span class="number">-1</span>) &#123;</span><br><span class="line">            <span class="built_in">ALOGE</span>(<span class="string">&quot;Binder ioctl to obtain version failed: %s&quot;</span>, <span class="built_in">strerror</span>(errno));</span><br><span class="line">            <span class="built_in">close</span>(fd);</span><br><span class="line">            fd = <span class="number">-1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//2.1 比较协议版本是否相同，不同则跳出</span></span><br><span class="line">        <span class="keyword">if</span> (result != <span class="number">0</span> || vers != BINDER_CURRENT_PROTOCOL_VERSION) &#123;</span><br><span class="line">          <span class="built_in">ALOGE</span>(<span class="string">&quot;Binder driver protocol(%d) does not match user space protocol(%d)! ioctl() return value: %d&quot;</span>,</span><br><span class="line">                vers, BINDER_CURRENT_PROTOCOL_VERSION, result);</span><br><span class="line">            <span class="built_in">close</span>(fd);</span><br><span class="line">            fd = <span class="number">-1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">size_t</span> maxThreads = DEFAULT_MAX_BINDER_THREADS;</span><br><span class="line">        <span class="comment">//设置最大线程数，BINDER_SET_MAX_THREADS=16</span></span><br><span class="line">        result = <span class="built_in">ioctl</span>(fd, BINDER_SET_MAX_THREADS, &amp;maxThreads);</span><br><span class="line">        <span class="keyword">if</span> (result == <span class="number">-1</span>) &#123;</span><br><span class="line">            <span class="built_in">ALOGE</span>(<span class="string">&quot;Binder ioctl to set max threads failed: %s&quot;</span>, <span class="built_in">strerror</span>(errno));</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="built_in">ALOGW</span>(<span class="string">&quot;Opening &#x27;%s&#x27; failed: %s\n&quot;</span>, driver, <span class="built_in">strerror</span>(errno));</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> fd;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>调用open（对应内核中的binder_open）打开Binder设备驱动</li>
<li>调用ioctl（对应内核中的binder_ioctl）获取Binder的版本信息</li>
<li>调用ioctl（对应内核中的binder_ioctl）设置最大线程数，BINDER_SET_MAX_THREADS=16</li>
</ul>
<p>​</p>
<h2 id="mmap"><a href="#mmap" class="headerlink" title="mmap()"></a>mmap()</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/ProcessState.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//#define BINDER_VM_SIZE ((1 * 1024 * 1024) - sysconf(_SC_PAGE_SIZE) * 2)</span></span><br><span class="line"><span class="comment">//_SC_PAGE_SIZE表示一个page页的大小，通常情况下为4K</span></span><br><span class="line"><span class="comment">//3. 通过mmap内存映射（1M-4K*2）的内存空间</span></span><br><span class="line">mVMStart = <span class="built_in">mmap</span>(<span class="literal">nullptr</span>, BINDER_VM_SIZE, PROT_READ, MAP_PRIVATE | MAP_NORESERVE, mDriverFD, <span class="number">0</span>);</span><br></pre></td></tr></table></figure>
<p>调用mmap（对应内核中的binder_mmap）内存映射（1M-4K*2）的内存空间</p>
<h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><pre class="mermaid">sequenceDiagram
main.cpp ->>+ main.cpp::main()
main.cpp ->>+ ProcessState::initWithDriver(driver)
ProcessState ->> ProcessState:new ProcessState(driver)
ProcessState ->> ProcessState:open_driver(driver)
ProcessState ->> Binder驱动:binder_open()
ProcessState ->> Binder驱动:binder_ioctl()
ProcessState ->>- Binder驱动:binder_mmap()
main.cpp ->>- ProcessState:becomeContextManager()</pre>
<p>initWithDriver的工作也比较简单，分为以下几步：</p>
<ul>
<li>通过系统调用open()来打开”/dev/binder”，获得一个句柄信息，在Binder驱动中对应的是函数binder_open()</li>
<li>通过ioctl获取binder的版本信息，比较binder协议版本是否相同，不同则跳出；再次通过ioctl设置最大线程数，在Binder驱动中对应的是函数binder_ioctl()</li>
<li>通过mmap内存映射（1M-4K*2）的内存空间，即把binder驱动文件的1016K字节映射到了内存空间，这1016K内存空间为servicemanager使用。在Binder驱动中对应的是函数binder_mmap()。<ul>
<li>_SC_PAGE_SIZE表示一个page页的大小，通常情况下为4K，即(1M - 4K*2)=1016K。</li>
<li>这个page的大小，不同厂家有时候也会调整大小，一般有1M，64K，4K，1KB，通常为4K。</li>
<li>Android11之前servicemanager映射的内存空间是128K。<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">执行命令找到servicemanager的进程号</span><br><span class="line">$ adb shell ps | grep servicemanager</span><br><span class="line">system          564      1 12348096  3368 do_epoll_wait       0 S servicemanager</span><br><span class="line">system          565      1 12349220  4516 do_epoll_wait       0 S hwservicemanager</span><br><span class="line">system          566      1 12348016  2032 do_epoll_wait       0 S vndservicemanager</span><br><span class="line"></span><br><span class="line">servicemanager进程mmap的内存大小</span><br><span class="line">$ adb shell cat /proc/564/maps | grep binder</span><br><span class="line">78a294f000-78a2a4d000 r--p 00000000 00:2a 4                              /dev/binderfs/binder</span><br><span class="line"></span><br><span class="line">78a2a4d000-78a294f000=FE000 ---&gt; 1016K</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">查看别的进程比如systemUI，计算出来也是1016K，并且android10也是1016K。android10之前的大家可以自行查看或者看代码。</span><br><span class="line">$ adb shell ps -eT | grep systemui</span><br><span class="line">system         2675  2675    750 16963640 113868 do_epoll_wait      0 S ndroid.systemui</span><br><span class="line">$ adb shell cat /proc/2675/maps | grep binder</span><br><span class="line">6e6a7d8000-6e6a8d6000 r--p 00000000 00:2a 5                              /dev/binderfs/hwbinder</span><br><span class="line">6e8eb07000-6e8ec05000 r--p 00000000 00:2a 5                              /dev/binderfs/hwbinder</span><br><span class="line">6e92e1d000-6e92f1b000 r--p 00000000 00:2a 4                              /dev/binderfs/binder</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<h1 id="becomeContextManager"><a href="#becomeContextManager" class="headerlink" title="becomeContextManager()"></a>becomeContextManager()</h1><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/ProcessState.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">ProcessState::becomeContextManager</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    AutoMutex _l(mLock);</span><br><span class="line"></span><br><span class="line">    flat_binder_object obj &#123;</span><br><span class="line">        .flags = FLAT_BINDER_FLAG_TXN_SECURITY_CTX,</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//Android10.0之后中引入BINDER_SET_CONTEXT_MGR_EXT，用来把ServiecManager设置成为安全的上下文</span></span><br><span class="line">    <span class="keyword">int</span> result = <span class="built_in">ioctl</span>(mDriverFD, BINDER_SET_CONTEXT_MGR_EXT, &amp;obj);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// fallback to original method</span></span><br><span class="line">    <span class="keyword">if</span> (result != <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">android_errorWriteLog</span>(<span class="number">0x534e4554</span>, <span class="string">&quot;121035042&quot;</span>);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">int</span> unused = <span class="number">0</span>;</span><br><span class="line">        <span class="comment">//如果安全上下文设置失败，继续使用原有的BINDER_SET_CONTEXT_MGR来进行控制</span></span><br><span class="line">        result = <span class="built_in">ioctl</span>(mDriverFD, BINDER_SET_CONTEXT_MGR, &amp;unused);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (result == <span class="number">-1</span>) &#123;</span><br><span class="line">        <span class="built_in">ALOGE</span>(<span class="string">&quot;Binder ioctl to become context manager failed: %s\n&quot;</span>, <span class="built_in">strerror</span>(errno));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> result == <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
ioctl对应于驱动中的函数为binder_ioctl，从用户空间拷贝ioctl的参数，调用binder_ioctl_set_ctx_mgr()进行设置。BINDER_SET_CONTEXT_MGR_EXT带参数，BINDER_SET_CONTEXT_MGR不带参数。</li>
</ul>
</li>
</ul>
<h2 id="binder-ioctl"><a href="#binder-ioctl" class="headerlink" title="binder_ioctl()"></a>binder_ioctl()</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">https:<span class="comment">//android.googlesource.com/kernel/common/+/refs/tags/5.4-android11-0/drivers/android/binder.c</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">long</span> <span class="title">binder_ioctl</span><span class="params">(struct file *filp, <span class="keyword">unsigned</span> <span class="keyword">int</span> cmd, <span class="keyword">unsigned</span> <span class="keyword">long</span> arg)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="keyword">switch</span> (cmd) &#123;</span><br><span class="line">    <span class="keyword">case</span> BINDER_SET_CONTEXT_MGR_EXT: &#123;<span class="comment">//设置Service Manager节点，带flag参数， servicemanager进程成为上下文管理者</span></span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">flat_binder_object</span> <span class="title">fbo</span>;</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (copy_from_user(&amp;fbo, ubuf, <span class="keyword">sizeof</span>(fbo))) &#123;</span><br><span class="line">            ret = -EINVAL;</span><br><span class="line">            <span class="keyword">goto</span> err;</span><br><span class="line">        &#125;</span><br><span class="line">        ret = binder_ioctl_set_ctx_mgr(filp, &amp;fbo);</span><br><span class="line">        <span class="keyword">if</span> (ret)</span><br><span class="line">            <span class="keyword">goto</span> err;</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">case</span> BINDER_SET_CONTEXT_MGR:<span class="comment">//设置Service Manager节点，不带flag参数， servicemanager进程成为上下文管理者</span></span><br><span class="line">        ret = binder_ioctl_set_ctx_mgr(filp, <span class="literal">NULL</span>);</span><br><span class="line">        <span class="keyword">if</span> (ret)</span><br><span class="line">            <span class="keyword">goto</span> err;</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    <span class="keyword">default</span>:</span><br><span class="line">        ret = -EINVAL;</span><br><span class="line">        <span class="keyword">goto</span> err;</span><br><span class="line">    &#125;</span><br><span class="line">    ret = <span class="number">0</span>;</span><br><span class="line">err:</span><br><span class="line">    <span class="keyword">if</span> (thread)</span><br><span class="line">        thread-&gt;looper_need_return = <span class="literal">false</span>;</span><br><span class="line">    wait_event_interruptible(binder_user_error_wait, binder_stop_on_user_error &lt; <span class="number">2</span>);</span><br><span class="line">    <span class="keyword">if</span> (ret &amp;&amp; ret != -ERESTARTSYS)</span><br><span class="line">        pr_info(<span class="string">&quot;%d:%d ioctl %x %lx returned %d\n&quot;</span>, proc-&gt;pid, current-&gt;pid, cmd, arg, ret);</span><br><span class="line">err_unlocked:</span><br><span class="line">    trace_binder_ioctl_done(ret);</span><br><span class="line">    <span class="keyword">return</span> ret;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>不管是BINDER_SET_CONTEXT_MGR_EXT还是BINDER_SET_CONTEXT_MGR最终都是调用binder_ioctl_set_ctx_mgr()进行设置。</p>
<h2 id="binder-ioctl-set-ctx-mgr"><a href="#binder-ioctl-set-ctx-mgr" class="headerlink" title="binder_ioctl_set_ctx_mgr()"></a>binder_ioctl_set_ctx_mgr()</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">https:<span class="comment">//android.googlesource.com/kernel/common/+/refs/tags/5.4-android11-0/drivers/android/binder.c</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">binder_ioctl_set_ctx_mgr</span><span class="params">(struct file *filp,</span></span></span><br><span class="line"><span class="params"><span class="function">            struct flat_binder_object *fbo)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="keyword">int</span> ret = <span class="number">0</span>;</span><br><span class="line">  <span class="comment">//进程的binder_proc， 这里是ServiceManager的 binder_proc,之前通过open(&quot;/dev/binder&quot;)得来</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">binder_proc</span> *<span class="title">proc</span> =</span> filp-&gt;private_data;</span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">binder_context</span> *<span class="title">context</span> =</span> proc-&gt;context;</span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">binder_node</span> *<span class="title">new_node</span>;</span></span><br><span class="line">  <span class="keyword">kuid_t</span> curr_euid = current_euid();<span class="comment">//线程的uid</span></span><br><span class="line"></span><br><span class="line">  mutex_lock(&amp;context-&gt;context_mgr_node_lock);<span class="comment">//互斥锁</span></span><br><span class="line">  <span class="comment">//正常第一次为null，如果不为null则说明该进程已经设置过context mgr则直接退出</span></span><br><span class="line">  <span class="keyword">if</span> (context-&gt;binder_context_mgr_node) &#123;</span><br><span class="line">    pr_err(<span class="string">&quot;BINDER_SET_CONTEXT_MGR already set\n&quot;</span>);</span><br><span class="line">    ret = -EBUSY;</span><br><span class="line">    <span class="keyword">goto</span> out;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">//检查当前进程是否具有注册Context Manager的SEAndroid安全权限</span></span><br><span class="line">  ret = security_binder_set_context_mgr(proc-&gt;tsk);</span><br><span class="line">  <span class="keyword">if</span> (ret &lt; <span class="number">0</span>)</span><br><span class="line">    <span class="keyword">goto</span> out;</span><br><span class="line">  <span class="keyword">if</span> (uid_valid(context-&gt;binder_context_mgr_uid)) &#123;</span><br><span class="line">    <span class="comment">//读取binder_context_mgr_uid和当前的比，如果不一样，报错。</span></span><br><span class="line">    <span class="keyword">if</span> (!uid_eq(context-&gt;binder_context_mgr_uid, curr_euid)) &#123;</span><br><span class="line">      pr_err(<span class="string">&quot;BINDER_SET_CONTEXT_MGR bad uid %d != %d\n&quot;</span>,</span><br><span class="line">             from_kuid(&amp;init_user_ns, curr_euid),</span><br><span class="line">             from_kuid(&amp;init_user_ns,</span><br><span class="line">           context-&gt;binder_context_mgr_uid));</span><br><span class="line">      ret = -EPERM;</span><br><span class="line">      <span class="keyword">goto</span> out;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="comment">//将当前进程的uid赋值给context的binder_context_mgr_uid变量以作保存</span></span><br><span class="line">    context-&gt;binder_context_mgr_uid = curr_euid;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">//创建binder_node对象</span></span><br><span class="line">  new_node = binder_new_node(proc, fbo);</span><br><span class="line">  <span class="keyword">if</span> (!new_node) &#123;</span><br><span class="line">    ret = -ENOMEM;</span><br><span class="line">    <span class="keyword">goto</span> out;</span><br><span class="line">  &#125;</span><br><span class="line">  binder_node_lock(new_node);</span><br><span class="line">  <span class="comment">//增加强弱引用计数</span></span><br><span class="line">  new_node-&gt;local_weak_refs++;</span><br><span class="line">  new_node-&gt;local_strong_refs++;</span><br><span class="line">  new_node-&gt;has_strong_ref = <span class="number">1</span>;</span><br><span class="line">  new_node-&gt;has_weak_ref = <span class="number">1</span>;</span><br><span class="line">  <span class="comment">//把新创建的node对象赋值给context-&gt;binder_context_mgr_node，成为serviceManager的binder管理实体</span></span><br><span class="line">  context-&gt;binder_context_mgr_node = new_node;</span><br><span class="line">  binder_node_unlock(new_node);</span><br><span class="line">  binder_put_node(new_node);</span><br><span class="line">out:</span><br><span class="line">  mutex_unlock(&amp;context-&gt;context_mgr_node_lock);<span class="comment">//释放锁</span></span><br><span class="line">  <span class="keyword">return</span> ret;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>检查进程已经设置过context mgr</li>
<li>通过调用security_binder_set_context_mgr检查当前进程是否具有注册Context Manager的SEAndroid安全权限。最后会调到<a href="https://android.googlesource.com/kernel/common/+/refs/tags/5.4-android11-0/security/security.c">security.c</a>中，具体实现不再深入分析。</li>
<li>检查uid是否有效，并将有效uid设置到全局变量binder_context_mgr_uid</li>
<li>为当前ServiceManager进程创建一个binder实体对象，并保存到binder_context_mgr_node全局变量中</li>
</ul>
<h2 id="binder-new-node"><a href="#binder-new-node" class="headerlink" title="binder_new_node()"></a>binder_new_node()</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">https:<span class="comment">//android.googlesource.com/kernel/common/+/refs/tags/5.4-android11-0/drivers/android/binder.c</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> struct binder_node *<span class="title">binder_new_node</span><span class="params">(struct binder_proc *proc,</span></span></span><br><span class="line"><span class="params"><span class="function">             struct flat_binder_object *fp)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">binder_node</span> *<span class="title">node</span>;</span></span><br><span class="line">    <span class="comment">//创建binder_node节点</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">binder_node</span> *<span class="title">new_node</span> =</span> kzalloc(<span class="keyword">sizeof</span>(*node), GFP_KERNEL);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (!new_node)</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">  binder_inner_proc_lock(proc);</span><br><span class="line">  <span class="comment">//从进程描述符binder_proc的nodes红黑树中查找是否存在指定的binder实体对象，即查找第一个叶节点</span></span><br><span class="line">  node = binder_init_node_ilocked(proc, new_node, fp);</span><br><span class="line">  binder_inner_proc_unlock(proc);</span><br><span class="line">  <span class="keyword">if</span> (node != new_node)</span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">     * The node was already added by another thread</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    kfree(new_node);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> node;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>创建binder_node节点</li>
<li>调用binder_init_node_ilocked()从进程描述符binder_proc的nodes红黑树中查找是否存在指定的binder实体对象</li>
<li>如果node已经被别的线程添加了，则free掉</li>
</ul>
<h2 id="binder-init-node-ilocked"><a href="#binder-init-node-ilocked" class="headerlink" title="binder_init_node_ilocked()"></a>binder_init_node_ilocked()</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">https:<span class="comment">//android.googlesource.com/kernel/common/+/refs/tags/5.4-android11-0/drivers/android/binder.c</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> struct binder_node *<span class="title">binder_init_node_ilocked</span><span class="params">(</span></span></span><br><span class="line"><span class="params"><span class="function">            struct binder_proc *proc,</span></span></span><br><span class="line"><span class="params"><span class="function">            struct binder_node *new_node,</span></span></span><br><span class="line"><span class="params"><span class="function">            struct flat_binder_object *fp)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">rb_node</span> **<span class="title">p</span> =</span> &amp;proc-&gt;nodes.rb_node;</span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">rb_node</span> *<span class="title">parent</span> =</span> <span class="literal">NULL</span>;</span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">binder_node</span> *<span class="title">node</span>;</span></span><br><span class="line">  <span class="keyword">binder_uintptr_t</span> ptr = fp ? fp-&gt;binder : <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">binder_uintptr_t</span> cookie = fp ? fp-&gt;cookie : <span class="number">0</span>;</span><br><span class="line">  __u32 flags = fp ? fp-&gt;flags : <span class="number">0</span>;</span><br><span class="line">  s8 priority;</span><br><span class="line"></span><br><span class="line">  assert_spin_locked(&amp;proc-&gt;inner_lock);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">while</span> (*p) &#123;</span><br><span class="line">    <span class="comment">//从进程描述符binder_proc的nodes红黑树中查找是否存在指定的binder实体对象</span></span><br><span class="line">    parent = *p;</span><br><span class="line">    node = rb_entry(parent, struct binder_node, rb_node);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (ptr &lt; node-&gt;ptr)</span><br><span class="line">      p = &amp;(*p)-&gt;rb_left;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (ptr &gt; node-&gt;ptr)</span><br><span class="line">      p = &amp;(*p)-&gt;rb_right;</span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="comment">/*</span></span><br><span class="line"><span class="comment">       * A matching node is already in</span></span><br><span class="line"><span class="comment">       * the rb tree. Abandon the init</span></span><br><span class="line"><span class="comment">       * and return it.</span></span><br><span class="line"><span class="comment">       */</span></span><br><span class="line">      binder_inc_node_tmpref_ilocked(node);</span><br><span class="line">      <span class="keyword">return</span> node;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  node = new_node;</span><br><span class="line">  binder_stats_created(BINDER_STAT_NODE);</span><br><span class="line">  node-&gt;tmp_refs++;</span><br><span class="line">  rb_link_node(&amp;node-&gt;rb_node, parent, p);<span class="comment">//插入节点操作1</span></span><br><span class="line">  rb_insert_color(&amp;node-&gt;rb_node, &amp;proc-&gt;nodes);<span class="comment">//插入节点操作2</span></span><br><span class="line">  node-&gt;debug_id = atomic_inc_return(&amp;binder_last_id);</span><br><span class="line">  node-&gt;proc = proc;</span><br><span class="line">  node-&gt;ptr = ptr;</span><br><span class="line">  node-&gt;cookie = cookie;</span><br><span class="line">  node-&gt;work.type = BINDER_WORK_NODE;</span><br><span class="line">  priority = flags &amp; FLAT_BINDER_FLAG_PRIORITY_MASK;</span><br><span class="line">  node-&gt;sched_policy = (flags &amp; FLAT_BINDER_FLAG_SCHED_POLICY_MASK) &gt;&gt;</span><br><span class="line">    FLAT_BINDER_FLAG_SCHED_POLICY_SHIFT;</span><br><span class="line">  node-&gt;min_priority = to_kernel_prio(node-&gt;sched_policy, priority);</span><br><span class="line">  node-&gt;accept_fds = !!(flags &amp; FLAT_BINDER_FLAG_ACCEPTS_FDS);</span><br><span class="line">  node-&gt;inherit_rt = !!(flags &amp; FLAT_BINDER_FLAG_INHERIT_RT);</span><br><span class="line">  node-&gt;txn_security_ctx = !!(flags &amp; FLAT_BINDER_FLAG_TXN_SECURITY_CTX);</span><br><span class="line">  spin_lock_init(&amp;node-&gt;lock);</span><br><span class="line">  INIT_LIST_HEAD(&amp;node-&gt;work.entry);</span><br><span class="line">  INIT_LIST_HEAD(&amp;node-&gt;async_todo);</span><br><span class="line">  binder_debug(BINDER_DEBUG_INTERNAL_REFS,</span><br><span class="line">         <span class="string">&quot;%d:%d node %d u%016llx c%016llx created\n&quot;</span>,</span><br><span class="line">         proc-&gt;pid, current-&gt;pid, node-&gt;debug_id,</span><br><span class="line">         (u64)node-&gt;ptr, (u64)node-&gt;cookie);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> node;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>binder_proc的成员node是binder_node的根节点，这是一颗红黑树，该函数首先根据规则找到第一个叶节点作为新插入的节点的父节点，然后创建binder_node节点并插入。</p>
<h2 id="小结-1"><a href="#小结-1" class="headerlink" title="小结"></a>小结</h2><pre class="mermaid">sequenceDiagram
main.cpp ->> main.cpp::main()
main.cpp ->> ProcessState:becomeContextManager()
ProcessState ->>+ Binder驱动:binder_ioctl()
Binder驱动 ->> Binder驱动:binder_ioctl_set_ctx_mgr()
Binder驱动 ->> Binder驱动:binder_new_node()
Binder驱动 ->>- Binder驱动:binder_init_node_ilocked()</pre>
<p>becomeContextManager的流程比较简单，如：</p>
<ul>
<li>先检查当前进程是否具有注册Context Manager的SEAndroid安全权限。</li>
<li>如果具有SELinux权限，会为整个系统的上下文管理器专门生成一个binder_node节点，使该节点的强弱应用加1。</li>
<li>新创建的binder_node 节点，记入context-&gt;binder_context_mgr_node，即ServiceManager 进程的context binder节点，使之成为serviceManager的binder管理实体。</li>
</ul>
<h1 id="循环处理"><a href="#循环处理" class="headerlink" title="循环处理"></a>循环处理</h1><p>经过前面两步的操作，我们不仅打开了Binder，而且把当前的ServiceManager注册成为了管理员，下面要做的就是去承担管理员的职责，也就是接收各种请求。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/cmds/servicemanager/main.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span>** argv)</span> </span>&#123;</span><br><span class="line">    </span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    sp&lt;Looper&gt; looper = Looper::<span class="built_in">prepare</span>(<span class="literal">false</span> <span class="comment">/*allowNonCallbacks*/</span>);</span><br><span class="line"></span><br><span class="line">    BinderCallback::<span class="built_in">setupTo</span>(looper);</span><br><span class="line">    ClientCallbackCallback::<span class="built_in">setupTo</span>(looper, manager);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span>(<span class="literal">true</span>) &#123;</span><br><span class="line">        looper-&gt;<span class="built_in">pollAll</span>(<span class="number">-1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// should not be reached</span></span><br><span class="line">    <span class="keyword">return</span> EXIT_FAILURE;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">BinderCallback</span> :</span> <span class="keyword">public</span> LooperCallback &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">static</span> sp&lt;BinderCallback&gt; <span class="title">setupTo</span><span class="params">(<span class="keyword">const</span> sp&lt;Looper&gt;&amp; looper)</span> </span>&#123;</span><br><span class="line">        sp&lt;BinderCallback&gt; cb = <span class="keyword">new</span> BinderCallback;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">int</span> binder_fd = <span class="number">-1</span>;</span><br><span class="line">        </span><br><span class="line">        <span class="comment">//得到open(&quot;/dev/binder&quot;)的文件句柄fd</span></span><br><span class="line">        <span class="comment">//并且设置 BC_ENTER_LOOPER 命令</span></span><br><span class="line">        IPCThreadState::<span class="built_in">self</span>()-&gt;<span class="built_in">setupPolling</span>(&amp;binder_fd);</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// Flush after setupPolling(), to make sure the binder driver</span></span><br><span class="line">        <span class="comment">// knows about this thread handling commands.</span></span><br><span class="line">        <span class="comment">//将上面设置好的命令写入binder驱动</span></span><br><span class="line">        IPCThreadState::<span class="built_in">self</span>()-&gt;<span class="built_in">flushCommands</span>();</span><br><span class="line">    <span class="comment">//将 binder_fd 注册进 epoll 监听</span></span><br><span class="line">        <span class="keyword">int</span> ret = looper-&gt;<span class="built_in">addFd</span>(binder_fd,</span><br><span class="line">                                Looper::POLL_CALLBACK,</span><br><span class="line">                                Looper::EVENT_INPUT,</span><br><span class="line">                                cb,<span class="number">2</span></span><br><span class="line">                                <span class="literal">nullptr</span> <span class="comment">/*data*/</span>);</span><br><span class="line">        ...</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> cb;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">int</span> <span class="title">handleEvent</span><span class="params">(<span class="keyword">int</span> <span class="comment">/* fd */</span>, <span class="keyword">int</span> <span class="comment">/* events */</span>, <span class="keyword">void</span>* <span class="comment">/* data */</span>)</span> <span class="keyword">override</span> </span>&#123;</span><br><span class="line">        IPCThreadState::<span class="built_in">self</span>()-&gt;<span class="built_in">handlePolledCommands</span>();</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;  <span class="comment">// Continue receiving callbacks.</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// LooperCallback for IClientCallback</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ClientCallbackCallback</span> :</span> <span class="keyword">public</span> LooperCallback &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">static</span> sp&lt;ClientCallbackCallback&gt; <span class="title">setupTo</span><span class="params">(<span class="keyword">const</span> sp&lt;Looper&gt;&amp; looper, <span class="keyword">const</span> sp&lt;ServiceManager&gt;&amp; manager)</span> </span>&#123;</span><br><span class="line">        sp&lt;ClientCallbackCallback&gt; cb = <span class="keyword">new</span> <span class="built_in">ClientCallbackCallback</span>(manager);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">int</span> fdTimer = <span class="built_in">timerfd_create</span>(CLOCK_MONOTONIC, <span class="number">0</span> <span class="comment">/*flags*/</span>);</span><br><span class="line">        <span class="built_in">LOG_ALWAYS_FATAL_IF</span>(fdTimer &lt; <span class="number">0</span>, <span class="string">&quot;Failed to timerfd_create: fd: %d err: %d&quot;</span>, fdTimer, errno);</span><br><span class="line"></span><br><span class="line">        itimerspec timespec &#123;</span><br><span class="line">            .it_interval = &#123;</span><br><span class="line">                .tv_sec = <span class="number">5</span>,</span><br><span class="line">                .tv_nsec = <span class="number">0</span>,</span><br><span class="line">            &#125;,</span><br><span class="line">            .it_value = &#123;</span><br><span class="line">                .tv_sec = <span class="number">5</span>,</span><br><span class="line">                .tv_nsec = <span class="number">0</span>,</span><br><span class="line">            &#125;,</span><br><span class="line">        &#125;;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">int</span> timeRes = <span class="built_in">timerfd_settime</span>(fdTimer, <span class="number">0</span> <span class="comment">/*flags*/</span>, &amp;timespec, <span class="literal">nullptr</span>);</span><br><span class="line">        <span class="built_in">LOG_ALWAYS_FATAL_IF</span>(timeRes &lt; <span class="number">0</span>, <span class="string">&quot;Failed to timerfd_settime: res: %d err: %d&quot;</span>, timeRes, errno);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">int</span> addRes = looper-&gt;<span class="built_in">addFd</span>(fdTimer,</span><br><span class="line">                                   Looper::POLL_CALLBACK,</span><br><span class="line">                                   Looper::EVENT_INPUT,</span><br><span class="line">                                   cb,</span><br><span class="line">                                   <span class="literal">nullptr</span>);</span><br><span class="line">        <span class="built_in">LOG_ALWAYS_FATAL_IF</span>(addRes != <span class="number">1</span>, <span class="string">&quot;Failed to add client callback FD to Looper&quot;</span>);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> cb;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">int</span> <span class="title">handleEvent</span><span class="params">(<span class="keyword">int</span> fd, <span class="keyword">int</span> <span class="comment">/*events*/</span>, <span class="keyword">void</span>* <span class="comment">/*data*/</span>)</span> <span class="keyword">override</span> </span>&#123;</span><br><span class="line">        <span class="keyword">uint64_t</span> expirations;</span><br><span class="line">        <span class="keyword">int</span> ret = <span class="built_in">read</span>(fd, &amp;expirations, <span class="built_in"><span class="keyword">sizeof</span></span>(expirations));</span><br><span class="line">        <span class="keyword">if</span> (ret != <span class="built_in"><span class="keyword">sizeof</span></span>(expirations)) &#123;</span><br><span class="line">            <span class="built_in">ALOGE</span>(<span class="string">&quot;Read failed to callback FD: ret: %d err: %d&quot;</span>, ret, errno);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        mManager-&gt;<span class="built_in">handleClientCallbacks</span>();</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;  <span class="comment">// Continue receiving callbacks.</span></span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="built_in">ClientCallbackCallback</span>(<span class="keyword">const</span> sp&lt;ServiceManager&gt;&amp; manager) : <span class="built_in">mManager</span>(manager) &#123;&#125;</span><br><span class="line">    sp&lt;ServiceManager&gt; mManager;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>


<h2 id="进入循环"><a href="#进入循环" class="headerlink" title="进入循环"></a>进入循环</h2><h3 id="setupPolling"><a href="#setupPolling" class="headerlink" title="setupPolling"></a>setupPolling</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/IPCThreadState.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">status_t</span> <span class="title">IPCThreadState::setupPolling</span><span class="params">(<span class="keyword">int</span>* fd)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (mProcess-&gt;mDriverFD &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> -EBADF;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    mOut.<span class="built_in">writeInt32</span>(BC_ENTER_LOOPER);</span><br><span class="line">    *fd = mProcess-&gt;mDriverFD;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>我们看到关键的代码：</p>
<ul>
<li>mOut.writeInt32(BC_ENTER_LOOPER);设置BC_ENTER_LOOPER</li>
<li>*fd = mProcess-&gt;mDriverFD;得到文件句柄fd</li>
</ul>
<p>但这个时候还没看到调用驱动接口，我们继续往下看。</p>
<h3 id="flushCommands"><a href="#flushCommands" class="headerlink" title="flushCommands"></a>flushCommands</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/IPCThreadState.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">IPCThreadState::flushCommands</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (mProcess-&gt;mDriverFD &lt; <span class="number">0</span>)</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    <span class="built_in">talkWithDriver</span>(<span class="literal">false</span>);</span><br><span class="line">    <span class="comment">// The flush could have caused post-write refcount decrements to have</span></span><br><span class="line">    <span class="comment">// been executed, which in turn could result in BC_RELEASE/BC_DECREFS</span></span><br><span class="line">    <span class="comment">// being queued in mOut. So flush again, if we need to.</span></span><br><span class="line">    <span class="keyword">if</span> (mOut.<span class="built_in">dataSize</span>() &gt; <span class="number">0</span>) &#123;    </span><br><span class="line">        <span class="built_in">talkWithDriver</span>(<span class="literal">false</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (mOut.<span class="built_in">dataSize</span>() &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">ALOGW</span>(<span class="string">&quot;mOut.dataSize() &gt; 0 after flushCommands()&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>继续看talkWithDriver()函数。</p>
<h3 id="talkWithDriver"><a href="#talkWithDriver" class="headerlink" title="talkWithDriver"></a>talkWithDriver</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/IPCThreadState.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">status_t</span> <span class="title">IPCThreadState::talkWithDriver</span><span class="params">(<span class="keyword">bool</span> doReceive)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (mProcess-&gt;mDriverFD &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> -EBADF;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    binder_write_read bwr;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Is the read buffer empty?</span></span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">bool</span> needRead = mIn.<span class="built_in">dataPosition</span>() &gt;= mIn.<span class="built_in">dataSize</span>();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// We don&#x27;t want to write anything if we are still reading</span></span><br><span class="line">    <span class="comment">// from data left in the input buffer and the caller</span></span><br><span class="line">    <span class="comment">// has requested to read the next data.</span></span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">size_t</span> outAvail = (!doReceive || needRead) ? mOut.<span class="built_in">dataSize</span>() : <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    bwr.write_size = outAvail;</span><br><span class="line">    bwr.write_buffer = (<span class="keyword">uintptr_t</span>)mOut.<span class="built_in">data</span>();</span><br><span class="line"></span><br><span class="line">    ...</span><br><span class="line">    </span><br><span class="line">    bwr.write_consumed = <span class="number">0</span>;</span><br><span class="line">    bwr.read_consumed = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">status_t</span> err;</span><br><span class="line">    <span class="keyword">do</span> &#123;</span><br><span class="line">        ...</span><br><span class="line"><span class="meta">#<span class="meta-keyword">if</span> defined(__ANDROID__)</span></span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">ioctl</span>(mProcess-&gt;mDriverFD, BINDER_WRITE_READ, &amp;bwr) &gt;= <span class="number">0</span>)</span><br><span class="line">            err = NO_ERROR;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            err = -errno;</span><br><span class="line"><span class="meta">#<span class="meta-keyword">else</span></span></span><br><span class="line">        err = INVALID_OPERATION;</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br><span class="line">        ...</span><br><span class="line">    &#125; <span class="keyword">while</span> (err == -EINTR);</span><br><span class="line"></span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> err;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>因为我们当前的流程是向binder驱动发送命令，所以跟read相关的都不考虑。这个函数里我们看到继续调用ioctl(mProcess-&gt;mDriverFD, BINDER_WRITE_READ, &amp;bwr)函数，其实就调到了驱动的binder_ioctl()。</p>
<h3 id="binder-ioctl-1"><a href="#binder-ioctl-1" class="headerlink" title="binder_ioctl()"></a>binder_ioctl()</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">https:<span class="comment">//android.googlesource.com/kernel/common/+/refs/tags/5.4-android11-0/drivers/android/binder.c</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">long</span> <span class="title">binder_ioctl</span><span class="params">(struct file *filp, <span class="keyword">unsigned</span> <span class="keyword">int</span> cmd, <span class="keyword">unsigned</span> <span class="keyword">long</span> arg)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> ret;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_proc</span> *<span class="title">proc</span> =</span> filp-&gt;private_data;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_thread</span> *<span class="title">thread</span>;</span></span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> size = _IOC_SIZE(cmd);</span><br><span class="line">    <span class="keyword">void</span> __user *ubuf = (<span class="keyword">void</span> __user *)arg;</span><br><span class="line"></span><br><span class="line">    binder_selftest_alloc(&amp;proc-&gt;alloc);</span><br><span class="line"></span><br><span class="line">    trace_binder_ioctl(cmd, arg);</span><br><span class="line"></span><br><span class="line">    ret = wait_event_interruptible(binder_user_error_wait, binder_stop_on_user_error &lt; <span class="number">2</span>);</span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    thread = binder_get_thread(proc);</span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="keyword">switch</span> (cmd) &#123;</span><br><span class="line">    <span class="keyword">case</span> BINDER_WRITE_READ:</span><br><span class="line">        ret = binder_ioctl_write_read(filp, cmd, arg, thread);</span><br><span class="line">        <span class="keyword">if</span> (ret)</span><br><span class="line">            <span class="keyword">goto</span> err;</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">        ...</span><br><span class="line">    &#125;</span><br><span class="line">    ret = <span class="number">0</span>;</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">return</span> ret;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>传进来的cmd是BINDER_WRITE_READ，所以我们继续看binder_ioctl_write_read()函数。</p>
<h3 id="binder-ioctl-write-read"><a href="#binder-ioctl-write-read" class="headerlink" title="binder_ioctl_write_read()"></a>binder_ioctl_write_read()</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">https:<span class="comment">//android.googlesource.com/kernel/common/+/refs/tags/5.4-android11-0/drivers/android/binder.c</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">binder_ioctl_write_read</span><span class="params">(struct file *filp,</span></span></span><br><span class="line"><span class="params"><span class="function">                <span class="keyword">unsigned</span> <span class="keyword">int</span> cmd, <span class="keyword">unsigned</span> <span class="keyword">long</span> arg,</span></span></span><br><span class="line"><span class="params"><span class="function">                struct binder_thread *thread)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> ret = <span class="number">0</span>;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_proc</span> *<span class="title">proc</span> =</span> filp-&gt;private_data;</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> size = _IOC_SIZE(cmd);</span><br><span class="line">    <span class="keyword">void</span> __user *ubuf = (<span class="keyword">void</span> __user *)arg;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_write_read</span> <span class="title">bwr</span>;</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (size != <span class="keyword">sizeof</span>(struct binder_write_read)) &#123;</span><br><span class="line">        ret = -EINVAL;</span><br><span class="line">        <span class="keyword">goto</span> out;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (copy_from_user(&amp;bwr, ubuf, <span class="keyword">sizeof</span>(bwr))) &#123;</span><br><span class="line">        ret = -EFAULT;</span><br><span class="line">        <span class="keyword">goto</span> out;</span><br><span class="line">    &#125;</span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (bwr.write_size &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        ret = binder_thread_write(proc, thread,</span><br><span class="line">                      bwr.write_buffer,</span><br><span class="line">                      bwr.write_size,</span><br><span class="line">                      &amp;bwr.write_consumed);</span><br><span class="line">        trace_binder_write_done(ret);</span><br><span class="line">        <span class="keyword">if</span> (ret &lt; <span class="number">0</span>) &#123;</span><br><span class="line">            bwr.read_consumed = <span class="number">0</span>;</span><br><span class="line">            <span class="keyword">if</span> (copy_to_user(ubuf, &amp;bwr, <span class="keyword">sizeof</span>(bwr)))</span><br><span class="line">                ret = -EFAULT;</span><br><span class="line">            <span class="keyword">goto</span> out;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">if</span> (copy_to_user(ubuf, &amp;bwr, <span class="keyword">sizeof</span>(bwr))) &#123;</span><br><span class="line">        ret = -EFAULT;</span><br><span class="line">        <span class="keyword">goto</span> out;</span><br><span class="line">    &#125;</span><br><span class="line">out:</span><br><span class="line">    <span class="keyword">return</span> ret;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>调用binder_thread_write()函数，告诉binder驱动ServiceManager已经准备好了。</p>
<h3 id="binder-thread-write"><a href="#binder-thread-write" class="headerlink" title="binder_thread_write()"></a>binder_thread_write()</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">https:<span class="comment">//android.googlesource.com/kernel/common/+/refs/tags/5.4-android11-0/drivers/android/binder.c</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">binder_thread_write</span><span class="params">(struct binder_proc *proc,</span></span></span><br><span class="line"><span class="params"><span class="function">            struct binder_thread *thread,</span></span></span><br><span class="line"><span class="params"><span class="function">            <span class="keyword">binder_uintptr_t</span> binder_buffer, <span class="keyword">size_t</span> size,</span></span></span><br><span class="line"><span class="params"><span class="function">            <span class="keyword">binder_size_t</span> *consumed)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">uint32_t</span> cmd;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_context</span> *<span class="title">context</span> =</span> proc-&gt;context;</span><br><span class="line">    <span class="keyword">void</span> __user *buffer = (<span class="keyword">void</span> __user *)(<span class="keyword">uintptr_t</span>)binder_buffer;</span><br><span class="line">    <span class="keyword">void</span> __user *ptr = buffer + *consumed;</span><br><span class="line">    <span class="keyword">void</span> __user *end = buffer + size;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (ptr &lt; end &amp;&amp; thread-&gt;return_error.cmd == BR_OK) &#123;</span><br><span class="line">        <span class="keyword">int</span> ret;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (get_user(cmd, (<span class="keyword">uint32_t</span> __user *)ptr))</span><br><span class="line">            <span class="keyword">return</span> -EFAULT;</span><br><span class="line">        ptr += <span class="keyword">sizeof</span>(<span class="keyword">uint32_t</span>);</span><br><span class="line">        trace_binder_command(cmd);</span><br><span class="line">        <span class="keyword">if</span> (_IOC_NR(cmd) &lt; ARRAY_SIZE(binder_stats.bc)) &#123;</span><br><span class="line">            atomic_inc(&amp;binder_stats.bc[_IOC_NR(cmd)]);</span><br><span class="line">            atomic_inc(&amp;proc-&gt;stats.bc[_IOC_NR(cmd)]);</span><br><span class="line">            atomic_inc(&amp;thread-&gt;stats.bc[_IOC_NR(cmd)]);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">switch</span> (cmd) &#123;</span><br><span class="line">        ...</span><br><span class="line">        <span class="keyword">case</span> BC_ENTER_LOOPER:</span><br><span class="line">            binder_debug(BINDER_DEBUG_THREADS,</span><br><span class="line">                     <span class="string">&quot;%d:%d BC_ENTER_LOOPER\n&quot;</span>,</span><br><span class="line">                     proc-&gt;pid, thread-&gt;pid);</span><br><span class="line">            <span class="keyword">if</span> (thread-&gt;looper &amp; BINDER_LOOPER_STATE_REGISTERED) &#123;</span><br><span class="line">                thread-&gt;looper |= BINDER_LOOPER_STATE_INVALID;</span><br><span class="line">                binder_user_error(<span class="string">&quot;%d:%d ERROR: BC_ENTER_LOOPER called after BC_REGISTER_LOOPER\n&quot;</span>,</span><br><span class="line">                    proc-&gt;pid, thread-&gt;pid);</span><br><span class="line">            &#125;</span><br><span class="line">            thread-&gt;looper |= BINDER_LOOPER_STATE_ENTERED;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        ...</span><br><span class="line"></span><br><span class="line">        <span class="keyword">default</span>:</span><br><span class="line">            pr_err(<span class="string">&quot;%d:%d unknown command %d\n&quot;</span>,</span><br><span class="line">                   proc-&gt;pid, thread-&gt;pid, cmd);</span><br><span class="line">            <span class="keyword">return</span> -EINVAL;</span><br><span class="line">        &#125;</span><br><span class="line">        *consumed = ptr - buffer;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>设置thread-&gt;looper |= BINDER_LOOPER_STATE_ENTERED，到这来binder驱动终于知道ServiceManager准备好了，接下来可以接收数据了。<br>前面一大推分析都是向binder驱动发送命令协议BC_ENTER_LOOPER，接下来才进入循环状态。</p>
<h3 id="pollAll"><a href="#pollAll" class="headerlink" title="pollAll()"></a>pollAll()</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/cmds/servicemanager/main.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span>** argv)</span> </span>&#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">while</span>(<span class="literal">true</span>) &#123;</span><br><span class="line">        looper-&gt;<span class="built_in">pollAll</span>(<span class="number">-1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">  ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>进入循环等到消息的到来。</p>
<h3 id="小结-2"><a href="#小结-2" class="headerlink" title="小结"></a>小结</h3><pre class="mermaid">sequenceDiagram
main.cpp ->>+ main.cpp::main()
main.cpp ->> Looper:prepare()
main.cpp ->> BinderCallback:setupTo()
BinderCallback ->> IPCThreadState:setupPolling()
    Note over IPCThreadState : mOut.writeInt32(BC_ENTER_LOOPER);
IPCThreadState ->> IPCThreadState:flushCommands()
IPCThreadState ->> IPCThreadState:talkWithDriver()
IPCThreadState ->> Binder驱动:binder_ioctl()
Binder驱动 ->> Binder驱动:binder_ioctl_write_read()
Binder驱动 ->> Binder驱动:binder_thread_write()
main.cpp ->> Looper:pollAll()</pre>
<p>这一小结主要是告诉binder驱动ServiceManager准备好了，接下来可以接收数据了。最后就进入循环等待处理信息。</p>
<h2 id="处理信息"><a href="#处理信息" class="headerlink" title="处理信息"></a>处理信息</h2><p>从前面的死循环我们可以知道用到了epoll机制循环获取消息，关于epoll机制如何获取到消息我们找时间另起一个章节再讨论。下面流程涉及到的<a href="http://aospxref.com/android-11.0.0_r21/xref/system/core/libutils/include/utils/Looper.h">Looper.h</a>和<a href="http://aospxref.com/android-11.0.0_r21/xref/system/core/libutils/Looper.cpp">Looper.cpp</a>可点链接查看源码。当前我们给出一个简单的流程然后直接看消息处理过程的细节。</p>
<pre class="mermaid">sequenceDiagram
main.cpp ->> Looper.h:pollAll(-1)
Looper.h ->> Looper.cpp:pollAll(timeoutMillis, nullptr, nullptr, nullptr)
Looper.cpp ->> Looper.cpp:pollOnce(timeoutMillis, outFd, outEvents, outData)
Looper.cpp ->> Looper.cpp:pollInner(timeoutMillis)
Looper.cpp ->> BinderCallback:handleEvent(fd, events, data)
BinderCallback ->>IPCThreadState:handlePolledCommands()</pre>
<p>现在我们直接从handleEvent()函数开始看。</p>
<h3 id="handleEvent"><a href="#handleEvent" class="headerlink" title="handleEvent()"></a>handleEvent()</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/cmds/servicemanager/main.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">handleEvent</span><span class="params">(<span class="keyword">int</span> <span class="comment">/* fd */</span>, <span class="keyword">int</span> <span class="comment">/* events */</span>, <span class="keyword">void</span>* <span class="comment">/* data */</span>)</span> <span class="keyword">override</span> </span>&#123;</span><br><span class="line">    IPCThreadState::<span class="built_in">self</span>()-&gt;<span class="built_in">handlePolledCommands</span>();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;  <span class="comment">// Continue receiving callbacks.</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>调用IPCThreadState的handlePolledCommands()获取消息。</p>
<h3 id="handlePolledCommands"><a href="#handlePolledCommands" class="headerlink" title="handlePolledCommands()"></a>handlePolledCommands()</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/IPCThreadState.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">status_t</span> <span class="title">IPCThreadState::handlePolledCommands</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">status_t</span> result;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">do</span> &#123;</span><br><span class="line">        result = <span class="built_in">getAndExecuteCommand</span>();</span><br><span class="line">    &#125; <span class="keyword">while</span> (mIn.<span class="built_in">dataPosition</span>() &lt; mIn.<span class="built_in">dataSize</span>());</span><br><span class="line"></span><br><span class="line">    <span class="built_in">processPendingDerefs</span>();</span><br><span class="line">    <span class="built_in">flushCommands</span>();</span><br><span class="line">    <span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>从函数getAndExecuteCommand()的名字不难猜出就是获取并执行信息。</p>
<h3 id="getAndExecuteCommand"><a href="#getAndExecuteCommand" class="headerlink" title="getAndExecuteCommand()"></a>getAndExecuteCommand()</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/IPCThreadState.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">status_t</span> <span class="title">IPCThreadState::getAndExecuteCommand</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">status_t</span> result;</span><br><span class="line">    <span class="keyword">int32_t</span> cmd;</span><br><span class="line"></span><br><span class="line">    result = <span class="built_in">talkWithDriver</span>();</span><br><span class="line">    <span class="keyword">if</span> (result &gt;= NO_ERROR) &#123;</span><br><span class="line">        <span class="keyword">size_t</span> IN = mIn.<span class="built_in">dataAvail</span>();</span><br><span class="line">        <span class="keyword">if</span> (IN &lt; <span class="built_in"><span class="keyword">sizeof</span></span>(<span class="keyword">int32_t</span>)) <span class="keyword">return</span> result;</span><br><span class="line">        <span class="comment">//获取cmd</span></span><br><span class="line">        cmd = mIn.<span class="built_in">readInt32</span>();</span><br><span class="line">        ...</span><br><span class="line">        <span class="comment">//执行cmd</span></span><br><span class="line">        result = <span class="built_in">executeCommand</span>(cmd);</span><br><span class="line"></span><br><span class="line">        ...</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>函数talkWithDriver()前面我们已经分享过了，这里不再展开分享，其大致的调用流程如：<br>talkWithDriver() –&gt; binder_ioctl() –&gt; binder_ioctl_write_read() –&gt; copy_to_user()。<br>经过talkWithDriver()从驱动读取消息之后就取出并执行，跟我们前面猜测的是一样的。</p>
<h3 id="executeCommand"><a href="#executeCommand" class="headerlink" title="executeCommand()"></a>executeCommand()</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/IPCThreadState.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">status_t</span> <span class="title">IPCThreadState::executeCommand</span><span class="params">(<span class="keyword">int32_t</span> cmd)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    BBinder* obj;</span><br><span class="line">    RefBase::weakref_type* refs;</span><br><span class="line">    <span class="keyword">status_t</span> result = NO_ERROR;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//每个命令中可能包含多个BR码，因此需要用while循环来解析buffer中所有的BR_XX码</span></span><br><span class="line">    <span class="built_in"><span class="keyword">switch</span></span> ((<span class="keyword">uint32_t</span>)cmd) &#123;</span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="keyword">case</span> BR_TRANSACTION_SEC_CTX:</span><br><span class="line">    <span class="keyword">case</span> BR_TRANSACTION:</span><br><span class="line">        &#123;</span><br><span class="line">            binder_transaction_data_secctx tr_secctx;</span><br><span class="line">            binder_transaction_data&amp; tr = tr_secctx.transaction_data;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (cmd == (<span class="keyword">int</span>) BR_TRANSACTION_SEC_CTX) &#123;</span><br><span class="line">                result = mIn.<span class="built_in">read</span>(&amp;tr_secctx, <span class="built_in"><span class="keyword">sizeof</span></span>(tr_secctx));</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                result = mIn.<span class="built_in">read</span>(&amp;tr, <span class="built_in"><span class="keyword">sizeof</span></span>(tr));</span><br><span class="line">                tr_secctx.secctx = <span class="number">0</span>;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            ...</span><br><span class="line">            </span><br><span class="line">            <span class="comment">//在binder驱动章节讲过binder_transaction_data结构体</span></span><br><span class="line">            <span class="comment">//到这来才发现还需要在聊一聊BBinder和BpBinder的区别，后面专门找一章节再分析</span></span><br><span class="line">            <span class="comment">//目前我们只需要知道服务管理器不会走这个分支，camera、audio服务提供者才会走这个分支</span></span><br><span class="line">            <span class="keyword">if</span> (tr.target.ptr) &#123;</span><br><span class="line">                <span class="comment">// We only have a weak reference on the target object, so we must first try to</span></span><br><span class="line">                <span class="comment">// safely acquire a strong reference before doing anything else with it.</span></span><br><span class="line">                <span class="keyword">if</span> (<span class="keyword">reinterpret_cast</span>&lt;RefBase::weakref_type*&gt;(</span><br><span class="line">                        tr.target.ptr)-&gt;<span class="built_in">attemptIncStrong</span>(<span class="keyword">this</span>)) &#123;</span><br><span class="line">                    error = <span class="keyword">reinterpret_cast</span>&lt;BBinder*&gt;(tr.cookie)-&gt;<span class="built_in">transact</span>(tr.code, buffer,</span><br><span class="line">                            &amp;reply, tr.flags);</span><br><span class="line">                    <span class="keyword">reinterpret_cast</span>&lt;BBinder*&gt;(tr.cookie)-&gt;<span class="built_in">decStrong</span>(<span class="keyword">this</span>);</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    error = UNKNOWN_TRANSACTION;</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                error = the_context_object-&gt;<span class="built_in">transact</span>(tr.code, buffer, &amp;reply, tr.flags);</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> ((tr.flags &amp; TF_ONE_WAY) == <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="built_in">sendReply</span>(reply, <span class="number">0</span>);</span><br><span class="line">            &#125;</span><br><span class="line">            </span><br><span class="line">            ...</span><br><span class="line">           </span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line"></span><br><span class="line">    ...</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (result != NO_ERROR) &#123;</span><br><span class="line">        mLastError = result;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>还记得main.cpp里这么一段代码么：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/cmds/servicemanager/main.cpp</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span>** argv)</span> </span>&#123;</span><br><span class="line">    ...</span><br><span class="line">    sp&lt;ServiceManager&gt; manager = <span class="keyword">new</span> <span class="built_in">ServiceManager</span>(std::make_unique&lt;Access&gt;());</span><br><span class="line">    ...</span><br><span class="line">    <span class="comment">//告诉binder驱动自己是binder上下文管理者</span></span><br><span class="line">    IPCThreadState::<span class="built_in">self</span>()-&gt;<span class="built_in">setTheContextObject</span>(manager);</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>setTheContextObject的实现又是：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/IPCThreadState.cpp</span></span><br><span class="line"></span><br><span class="line">sp&lt;BBinder&gt; the_context_object;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">IPCThreadState::setTheContextObject</span><span class="params">(sp&lt;BBinder&gt; obj)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    the_context_object = obj;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>所以the_context_object-&gt;transact(tr.code, buffer, &amp;reply, tr.flags);也就是调用了serviceManger的transact，这个其实就比较简单了，是通过BBinder的方法实现如下：</p>
<h3 id="transact"><a href="#transact" class="headerlink" title="transact()"></a>transact()</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">http:<span class="comment">//aospxref.com/android-11.0.0_r21/xref/frameworks/native/libs/binder/Binder.cpp</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">status_t</span> <span class="title">BBinder::transact</span><span class="params">(</span></span></span><br><span class="line"><span class="params"><span class="function">    <span class="keyword">uint32_t</span> code, <span class="keyword">const</span> Parcel&amp; data, Parcel* reply, <span class="keyword">uint32_t</span> flags)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    data.<span class="built_in">setDataPosition</span>(<span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">status_t</span> err = NO_ERROR;</span><br><span class="line">    <span class="built_in"><span class="keyword">switch</span></span> (code) &#123;</span><br><span class="line">        <span class="keyword">case</span> PING_TRANSACTION:</span><br><span class="line">            err = <span class="built_in">pingBinder</span>();</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> EXTENSION_TRANSACTION:</span><br><span class="line">            err = reply-&gt;<span class="built_in">writeStrongBinder</span>(<span class="built_in">getExtension</span>());</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> DEBUG_PID_TRANSACTION:</span><br><span class="line">            err = reply-&gt;<span class="built_in">writeInt32</span>(<span class="built_in">getDebugPid</span>());</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">default</span>:</span><br><span class="line">            err = <span class="built_in">onTransact</span>(code, data, reply, flags);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// In case this is being transacted on in the same process.</span></span><br><span class="line">    <span class="keyword">if</span> (reply != <span class="literal">nullptr</span>) &#123;</span><br><span class="line">        reply-&gt;<span class="built_in">setDataPosition</span>(<span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> err;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>豁然开朗 ，这就是我们普通定义的onTransact方法，不过因为ServiceManager接口是通过aidl声明的，因为这写代码都是系统生成的，不方便阅读，我们这里只大概介绍一下，是再out目录下通过aidl生成的文件。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">::<span class="function">android::<span class="keyword">status_t</span> <span class="title">BnServiceManager::onTransact</span><span class="params">(<span class="keyword">uint32_t</span> _aidl_code, <span class="keyword">const</span> ::android::Parcel&amp; _aidl_data, ::android::Parcel* _aidl_reply, <span class="keyword">uint32_t</span> _aidl_flags)</span> </span>&#123;</span><br><span class="line">    ::android::<span class="keyword">status_t</span> _aidl_ret_status = ::android::OK;</span><br><span class="line">    <span class="built_in"><span class="keyword">switch</span></span> (_aidl_code) &#123;</span><br><span class="line">    <span class="keyword">case</span> ::android::IBinder::FIRST_CALL_TRANSACTION + <span class="number">0</span> <span class="comment">/* getService */</span>:</span><br><span class="line">    &#123;</span><br><span class="line">    ::std::string in_name;</span><br><span class="line">    ::android::sp&lt;::android::IBinder&gt; _aidl_return;</span><br><span class="line">    <span class="keyword">if</span> (!(_aidl_data.<span class="built_in">checkInterface</span>(<span class="keyword">this</span>))) &#123;</span><br><span class="line">      _aidl_ret_status = ::android::BAD_TYPE;</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    _aidl_ret_status = _aidl_data.<span class="built_in">readUtf8FromUtf16</span>(&amp;in_name);</span><br><span class="line">    <span class="keyword">if</span> (((_aidl_ret_status) != (::android::OK))) &#123;</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    ::android::binder::Status _aidl_status(<span class="built_in">getService</span>(in_name, &amp;_aidl_return));</span><br><span class="line">    _aidl_ret_status = _aidl_status.<span class="built_in">writeToParcel</span>(_aidl_reply);</span><br><span class="line">    <span class="keyword">if</span> (((_aidl_ret_status) != (::android::OK))) &#123;</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (!_aidl_status.<span class="built_in">isOk</span>()) &#123;</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    _aidl_ret_status = _aidl_reply-&gt;<span class="built_in">writeStrongBinder</span>(_aidl_return);</span><br><span class="line">    <span class="keyword">if</span> (((_aidl_ret_status) != (::android::OK))) &#123;</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">    <span class="keyword">case</span> ::android::IBinder::FIRST_CALL_TRANSACTION + <span class="number">1</span> <span class="comment">/* checkService */</span>:</span><br><span class="line">    &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">    <span class="keyword">case</span> ::android::IBinder::FIRST_CALL_TRANSACTION + <span class="number">2</span> <span class="comment">/* addService */</span>:</span><br><span class="line">    &#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> _aidl_ret_status;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>因为之前在main.cpp里有调用addService()注册成为服务的过程了，我们现在就以getService()为例，所以最终调到ServiceManager的getService()函数。ServiceManager在Android 11上改动非常大了。ServiceManager的addService()和getService()除了检查SELinux权限外就是通过<a href="http://aospxref.com/android-11.0.0_r21/s?defs=mNameToService&project=frameworks">mNameToService</a>对象添加或者获取服务。所以这里不再深入分析。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>servicemanager主要做了以下几件事：</p>
<ul>
<li>打开binder驱动。</li>
<li>调用addService把servicemanager（vndservicemanager）添加到服务。</li>
<li>调用becomeContextManager()让自己成为整个系统中唯一的上下文管理器，其实也就是所有service管理器。</li>
<li>进入死循环处理信息（添加服务，获取服务等）。</li>
</ul>
<h1 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h1><p><a href="https://blog.csdn.net/zhanshenwu/article/details/106438220">Binder驱动之进程间通信及设置</a><br><a href="http://kernel.meizu.com/android-binder.html">Android Binder</a></p>
]]></content>
      <categories>
        <category>Android</category>
      </categories>
      <tags>
        <tag>Android</tag>
        <tag>Binder</tag>
      </tags>
  </entry>
  <entry>
    <title>Binder机制01-驱动</title>
    <url>/archives/3e87e0ce.html</url>
    <content><![CDATA[<span id="more"></span>
<meta name="referrer" content="no-referrer"/>


<h1 id="Binder机制文章列表"><a href="#Binder机制文章列表" class="headerlink" title="Binder机制文章列表"></a>Binder机制文章列表</h1><p><a href="https://i-rtfsc.github.io/archives/d6aea0f1.html">Binder概述</a><br><a href="https://i-rtfsc.github.io/archives/3e87e0ce.html">Binder机制01-驱动</a><br><a href="https://i-rtfsc.github.io/archives/f17b1d52.html">Binder机制02-ServiceManager</a><br><a href="https://i-rtfsc.github.io/archives/937d16d8.html">Binder机制03-Framework-Native</a><br><a href="https://i-rtfsc.github.io/archives/73029508.html">Binder机制04-Framework-Jave</a><br><a href="https://i-rtfsc.github.io/archives/ed3eb50b.html">Binder机制05-AIDL</a><br><a href="https://i-rtfsc.github.io/archives/4e7f84b4.html">Binder机制06-框架</a><br><a href="https://i-rtfsc.github.io/archives/d30c7ecb.html">Binder机制07-实例分析</a><br><a href="https://i-rtfsc.github.io/archives/720ded9.html">Binder机制08-总结</a><br><a href="https://i-rtfsc.github.io/archives/f1684132.html">Ashmem简介（Android IPC传输大数据）</a></p>
<h1 id="核心"><a href="#核心" class="headerlink" title="核心"></a>核心</h1><p>Binder机制的实现中，最核心的就是Binder驱动。 Binder是一个miscellaneous类型的驱动，本身不对应任何硬件，所有的操作都在软件层。 Binder驱动的核心是维护一个binder_proc类型的链表。里面记录了包括Service Manager在内的所有Client信息，当Client去请求得到某个Service时，Binder驱动就去binder_proc中查找相应的Service返回给Client，同时增加当前Service的引用个数。<br>​</p>
<p>​</p>
<h1 id="结构体"><a href="#结构体" class="headerlink" title="结构体"></a>结构体</h1><p>Binder驱动中包含了很多的结构体。为了便于下文讲解，这里我们先对这些结构体做一些介绍。<br>驱动中的结构体可以分为两类：<br>一类是与用户空间共用的，这些结构体在Binder通信协议过程中会用到。因此，这些结构体定义在<a href="https://android.googlesource.com/kernel/common/+/refs/tags/5.4-android11-0/include/uapi/linux/android/binder.h">binder.h</a>中，包括：</p>
<table>
<thead>
<tr>
<th>结构体名称</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>flat_binder_object</td>
<td>描述在Binder IPC中传递的对象，见下文</td>
</tr>
<tr>
<td><strong>binder_write_read</strong></td>
<td>存储一次读写操作的数据</td>
</tr>
<tr>
<td>binder_version</td>
<td>存储Binder的版本号</td>
</tr>
<tr>
<td>transaction_flags</td>
<td>描述事务的flag，例如是否是异步请求，是否支持fd</td>
</tr>
<tr>
<td><strong>binder_transaction_data</strong></td>
<td>存储一次事务的数据</td>
</tr>
<tr>
<td>binder_ptr_cookie</td>
<td>包含了一个指针和一个cookie</td>
</tr>
</tbody></table>
<p>Binder驱动中，还有一类结构体是仅仅Binder驱动内部实现过程中需要的，它们定义在<a href="https://android.googlesource.com/kernel/common/+/refs/tags/5.4-android11-0/drivers/android/binder.c">binder.c</a>中，包括：</p>
<table>
<thead>
<tr>
<th>结构体名称</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>binder_node</strong></td>
<td>描述Binder实体节点，即：对应了一个Server</td>
</tr>
<tr>
<td><strong>binder_ref</strong></td>
<td>描述对于Binder实体的引用</td>
</tr>
<tr>
<td><strong>binder_buffer</strong></td>
<td>描述Binder通信过程中存储数据的Buffer</td>
</tr>
<tr>
<td><strong>binder_proc</strong></td>
<td>描述使用Binder的进程</td>
</tr>
<tr>
<td><strong>binder_thread</strong></td>
<td>描述使用Binder的线程</td>
</tr>
<tr>
<td>binder_work</td>
<td>描述通信过程中的一项任务</td>
</tr>
<tr>
<td>binder_transaction</td>
<td>描述一次事务的相关信息</td>
</tr>
<tr>
<td>binder_deferred_state</td>
<td>描述延迟任务</td>
</tr>
<tr>
<td>binder_stats</td>
<td>Binder线程相关统计数据</td>
</tr>
<tr>
<td>binder_ref_death</td>
<td>死亡通知的结构体</td>
</tr>
</tbody></table>
<h2 id="binder-write-read"><a href="#binder-write-read" class="headerlink" title="binder_write_read"></a><strong>binder_write_read</strong></h2><p>binder的读写结构， 记录了binder中读和写的数据信息：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">binder_write_read</span> &#123;</span></span><br><span class="line">   <span class="keyword">binder_size_t</span>       write_size;     <span class="comment">//要写入的字节数,write_buffer的总字节数</span></span><br><span class="line">   <span class="keyword">binder_size_t</span>       write_consumed; <span class="comment">//驱动程序占用的字节数,write_buffer已消费的字节数</span></span><br><span class="line">   <span class="keyword">binder_uintptr_t</span>    write_buffer;   <span class="comment">//写缓冲数据的指针</span></span><br><span class="line">   <span class="keyword">binder_size_t</span>       read_size;      <span class="comment">//要读的字节数,read_buffer的总字节数</span></span><br><span class="line">   <span class="keyword">binder_size_t</span>       read_consumed;  <span class="comment">//驱动程序占用的字节数,read_buffer已消费的字节数</span></span><br><span class="line">   <span class="keyword">binder_uintptr_t</span>    read_buffer;    <span class="comment">//读缓存数据的指针</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>用户空间程序和Binder驱动程序交互基本都是通过BINDER_WRITE_READ命令，来进行数据的读写操作。</p>
<ul>
<li>write_buffer：用于发送IPC(或IPC reply)数据，即传递经由Binder Driver的数据时使用。</li>
<li>read_buffer：用于接收来自Binder Driver的数据，即Binder Driver在接收IPC(或IPC reply)数据后，保存到read_buffer，再传递到用户空间。</li>
</ul>
<p>write_buffer和read_buffer都是包含Binder协议命令和binder_transaction_data结构体。</p>
<ul>
<li>copy_from_user()：将用户空间IPC数据拷贝到内核态binder_write_read结构体。</li>
<li>copy_to_user()：将用内核态binder_write_read结构体数据拷贝到用户空间。</li>
</ul>
<h2 id="binder-transaction-data"><a href="#binder-transaction-data" class="headerlink" title="binder_transaction_data"></a><strong>binder_transaction_data</strong></h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">binder_transaction_data</span> &#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">union</span> &#123;</span></span><br><span class="line">        __u32    handle;                 <span class="comment">//binder_ref（即handle）</span></span><br><span class="line">        <span class="keyword">binder_uintptr_t</span> ptr;            <span class="comment">//Binder_node的内存地址</span></span><br><span class="line">    &#125; target;                            <span class="comment">//RPC目标</span></span><br><span class="line">    <span class="keyword">binder_uintptr_t</span>    cookie;          <span class="comment">//BBinder指针</span></span><br><span class="line">    __u32        code;                   <span class="comment">//RPC代码，代表Client与Server双方约定的命令码</span></span><br><span class="line"></span><br><span class="line">    __u32            flags;              <span class="comment">//标志位，比如TF_ONE_WAY代表异步，即不等待Server端回复</span></span><br><span class="line">    <span class="keyword">pid_t</span>        sender_pid;             <span class="comment">//发送端进程的pid</span></span><br><span class="line">    <span class="keyword">uid_t</span>        sender_euid;            <span class="comment">//发送端进程的uid</span></span><br><span class="line">    <span class="keyword">binder_size_t</span>    data_size;          <span class="comment">//data数据的总大小</span></span><br><span class="line">    <span class="keyword">binder_size_t</span>    offsets_size;       <span class="comment">//IPC对象的大小</span></span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">union</span> &#123;</span></span><br><span class="line">        <span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">            <span class="keyword">binder_uintptr_t</span>    buffer;  <span class="comment">//数据区起始地址</span></span><br><span class="line">            <span class="keyword">binder_uintptr_t</span>    offsets; <span class="comment">//数据区IPC对象偏移量</span></span><br><span class="line">        &#125; ptr;</span><br><span class="line">       __u8    buf[<span class="number">8</span>];</span><br><span class="line">    &#125; data;                              <span class="comment">//RPC数据</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>当BINDER_WRITE_READ命令的目标是本地Binder node时，target使用ptr，否则使用handle。只有当这是Binder node时，cookie才有意义，表示附加数据，由进程自己解释。</p>
<ul>
<li><p>target：对于BpBinder则使用handle，对于BBinder则使用ptr，故使用union数据类型来表示。</p>
</li>
<li><p>code：比如注册服务过程code为ADD_SERVICE_TRANSACTION，又比如获取服务code为CHECK_SERVICE_TRANSACTION。</p>
</li>
<li><p>data：代表整个数据区，其中data.ptr指向的是传递给Binder驱动的数据区的起始地址，data.offsets指的是数据区中IPC数据地址的偏移量。</p>
</li>
<li><p>cookie: 记录着BBinder指针。</p>
</li>
<li><p>data_size：代表本次传输的parcel数据的大小。</p>
</li>
<li><p>offsets_size： 代表传递的IPC对象的大小；根据这个可以推测出传递了多少个binder对象。</p>
<ul>
<li>对于64位IPC，一个IPC对象大小等于8。</li>
<li>对于32位IPC，一个IPC对象大小等于4。<h2 id="binder-node"><a href="#binder-node" class="headerlink" title="binder_node"></a>binder_node</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * struct binder_node - binder node bookkeeping</span></span><br><span class="line"><span class="comment"> * @debug_id:             unique ID for debugging</span></span><br><span class="line"><span class="comment"> *                        (invariant after initialized)</span></span><br><span class="line"><span class="comment"> * @lock:                 lock for node fields</span></span><br><span class="line"><span class="comment"> * @work:                 worklist element for node work</span></span><br><span class="line"><span class="comment"> *                        (protected by @proc-&gt;inner_lock)</span></span><br><span class="line"><span class="comment"> * @rb_node:              element for proc-&gt;nodes tree</span></span><br><span class="line"><span class="comment"> *                        (protected by @proc-&gt;inner_lock)</span></span><br><span class="line"><span class="comment"> * @dead_node:            element for binder_dead_nodes list</span></span><br><span class="line"><span class="comment"> *                        (protected by binder_dead_nodes_lock)</span></span><br><span class="line"><span class="comment"> * @proc:                 binder_proc that owns this node</span></span><br><span class="line"><span class="comment"> *                        (invariant after initialized)</span></span><br><span class="line"><span class="comment"> * @refs:                 list of references on this node</span></span><br><span class="line"><span class="comment"> *                        (protected by @lock)</span></span><br><span class="line"><span class="comment"> * @internal_strong_refs: used to take strong references when</span></span><br><span class="line"><span class="comment"> *                        initiating a transaction</span></span><br><span class="line"><span class="comment"> *                        (protected by @proc-&gt;inner_lock if @proc</span></span><br><span class="line"><span class="comment"> *                        and by @lock)</span></span><br><span class="line"><span class="comment"> * @local_weak_refs:      weak user refs from local process</span></span><br><span class="line"><span class="comment"> *                        (protected by @proc-&gt;inner_lock if @proc</span></span><br><span class="line"><span class="comment"> *                        and by @lock)</span></span><br><span class="line"><span class="comment"> * @local_strong_refs:    strong user refs from local process</span></span><br><span class="line"><span class="comment"> *                        (protected by @proc-&gt;inner_lock if @proc</span></span><br><span class="line"><span class="comment"> *                        and by @lock)</span></span><br><span class="line"><span class="comment"> * @tmp_refs:             temporary kernel refs</span></span><br><span class="line"><span class="comment"> *                        (protected by @proc-&gt;inner_lock while @proc</span></span><br><span class="line"><span class="comment"> *                        is valid, and by binder_dead_nodes_lock</span></span><br><span class="line"><span class="comment"> *                        if @proc is NULL. During inc/dec and node release</span></span><br><span class="line"><span class="comment"> *                        it is also protected by @lock to provide safety</span></span><br><span class="line"><span class="comment"> *                        as the node dies and @proc becomes NULL)</span></span><br><span class="line"><span class="comment"> * @ptr:                  userspace pointer for node</span></span><br><span class="line"><span class="comment"> *                        (invariant, no lock needed)</span></span><br><span class="line"><span class="comment"> * @cookie:               userspace cookie for node</span></span><br><span class="line"><span class="comment"> *                        (invariant, no lock needed)</span></span><br><span class="line"><span class="comment"> * @has_strong_ref:       userspace notified of strong ref</span></span><br><span class="line"><span class="comment"> *                        (protected by @proc-&gt;inner_lock if @proc</span></span><br><span class="line"><span class="comment"> *                        and by @lock)</span></span><br><span class="line"><span class="comment"> * @pending_strong_ref:   userspace has acked notification of strong ref</span></span><br><span class="line"><span class="comment"> *                        (protected by @proc-&gt;inner_lock if @proc</span></span><br><span class="line"><span class="comment"> *                        and by @lock)</span></span><br><span class="line"><span class="comment"> * @has_weak_ref:         userspace notified of weak ref</span></span><br><span class="line"><span class="comment"> *                        (protected by @proc-&gt;inner_lock if @proc</span></span><br><span class="line"><span class="comment"> *                        and by @lock)</span></span><br><span class="line"><span class="comment"> * @pending_weak_ref:     userspace has acked notification of weak ref</span></span><br><span class="line"><span class="comment"> *                        (protected by @proc-&gt;inner_lock if @proc</span></span><br><span class="line"><span class="comment"> *                        and by @lock)</span></span><br><span class="line"><span class="comment"> * @has_async_transaction: async transaction to node in progress</span></span><br><span class="line"><span class="comment"> *                        (protected by @lock)</span></span><br><span class="line"><span class="comment"> * @sched_policy:         minimum scheduling policy for node</span></span><br><span class="line"><span class="comment"> *                        (invariant after initialized)</span></span><br><span class="line"><span class="comment"> * @accept_fds:           file descriptor operations supported for node</span></span><br><span class="line"><span class="comment"> *                        (invariant after initialized)</span></span><br><span class="line"><span class="comment"> * @min_priority:         minimum scheduling priority</span></span><br><span class="line"><span class="comment"> *                        (invariant after initialized)</span></span><br><span class="line"><span class="comment"> * @inherit_rt:           inherit RT scheduling policy from caller</span></span><br><span class="line"><span class="comment"> * @txn_security_ctx:     require sender&#x27;s security context</span></span><br><span class="line"><span class="comment"> *                        (invariant after initialized)</span></span><br><span class="line"><span class="comment"> * @async_todo:           list of async work items</span></span><br><span class="line"><span class="comment"> *                        (protected by @proc-&gt;inner_lock)</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * Bookkeeping structure for binder nodes.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">binder_node</span> &#123;</span></span><br><span class="line">    <span class="keyword">int</span> debug_id;                       <span class="comment">//节点创建时分配，具有全局唯一性，用于调试使用</span></span><br><span class="line">    <span class="keyword">spinlock_t</span> lock;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_work</span> <span class="title">work</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">union</span> &#123;</span></span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">rb_node</span> <span class="title">rb_node</span>;</span>         <span class="comment">//binder节点正常使用，union</span></span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">hlist_node</span> <span class="title">dead_node</span>;</span>    <span class="comment">//binder节点已销毁，union</span></span><br><span class="line">    &#125;;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_proc</span> *<span class="title">proc</span>;</span>           <span class="comment">//binder所在的进程</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">hlist_head</span> <span class="title">refs</span>;</span>             <span class="comment">//所有指向该节点的binder引用队列</span></span><br><span class="line">    <span class="keyword">int</span> internal_strong_refs;</span><br><span class="line">    <span class="keyword">int</span> local_weak_refs;</span><br><span class="line">    <span class="keyword">int</span> local_strong_refs;</span><br><span class="line">    <span class="keyword">int</span> tmp_refs</span><br><span class="line">    <span class="keyword">binder_uintptr_t</span> ptr;               <span class="comment">//指向用户空间binder_node的指针，对应flat_binder_object.binder</span></span><br><span class="line">    <span class="keyword">binder_uintptr_t</span> cookie;            <span class="comment">//数据，对应flat_binder_object.cookie</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">        <span class="comment">/*</span></span><br><span class="line"><span class="comment">         * bitfield elements protected by</span></span><br><span class="line"><span class="comment">         * proc inner_lock</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        u8 has_strong_ref:<span class="number">1</span>;</span><br><span class="line">        u8 pending_strong_ref:<span class="number">1</span>;</span><br><span class="line">        u8 has_weak_ref:<span class="number">1</span>;</span><br><span class="line">        u8 pending_weak_ref:<span class="number">1</span>;</span><br><span class="line">    &#125;;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">        <span class="comment">/*</span></span><br><span class="line"><span class="comment">         * invariant after initialization</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        u8 sched_policy:<span class="number">2</span>;</span><br><span class="line">        u8 inherit_rt:<span class="number">1</span>;</span><br><span class="line">        u8 accept_fds:<span class="number">1</span>;</span><br><span class="line">        u8 txn_security_ctx:<span class="number">1</span>;</span><br><span class="line">        u8 min_priority;</span><br><span class="line">    &#125;;</span><br><span class="line">    <span class="keyword">bool</span> has_async_transaction;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> <span class="title">async_todo</span>;</span>        <span class="comment">//异步todo队列</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
结构struct binder_node表示一个binder实体。rb_node和dead_node组成一个联合体。如果这个Binder实体还在正常使用，则使用rb_node来连入proc-&gt;nodes所表示的红黑树的节点，这棵红黑树用来组织属于这个进程的所有Binder实体。如果这个Binder实体所属的进程已经销毁，而这个Binder实体又被其它进程所引用，则这个Binder实体通过dead_node进入到一个哈希表中去存放。proc成员变量就是表示这个Binder实例所属于进程了。refs成员变量把所有引用了该Binder实体的Binder引用连接起来构成一个链表。internal_strong_refs、local_weak_refs和local_strong_refs表示这个Binder实体的引用计数。ptr和cookie成员变量分别表示这个Binder实体在用户空间的地址以及附加数据。</li>
</ul>
</li>
<li><p>debug_id：用于调试目的，和ref的debug_id共用一个整数空间。</p>
</li>
<li><p>work：当本节点引用计数发生改变，需要通知所属进程时，通过该成员挂入所属进程的to-do队列，唤醒所属进程执行Binder实体引用计数的修改。</p>
</li>
<li><p>rb_node：每个进程都维护一棵红黑树，以Binder实体在用户空间的指针，即本结构的ptr成员为索引存放该进程所有的Binder实体。这样驱动可以根据Binder实体在用户空间的指针很快找到其位于内核的节点。rb_node用于将本节点链入该红黑树中。</p>
</li>
<li><p>dead_node：销毁时必须将rb_node从红黑树中摘除，如果本节点还有引用切断，就用dead_node将节点隔离到另外一个链表中，直到通知所有进程切断该节点的引用后，该节点才可能被销毁。</p>
</li>
<li><p>proc：本成员指向节点所属的进程，即提供该节点的进程。</p>
</li>
<li><p>refs：本成员是队列头，所有指向本节点的引用都连接到该队列里，这些引用可能属于不同的进程。通过该对列可以遍历指向该节点的所有引用。</p>
</li>
<li><p>internal_strong_refs：用来实现强指针的计数器：产生一个指向本节点的强引用，该计数器则加1</p>
</li>
<li><p>local_weak_refs：驱动为传输中的Binder设置的弱引用计数器。如果一个Binder打包在数据包中从一个进程发送到另外一个进程，驱动会为该Binder增加引用计数，知道接收进程通过BC_FREE_BUFFER通知驱动释放该数据包的数据区为止。</p>
</li>
<li><p>local_strong_refs：驱动为传输中的Binder设置的强引用计数，同上</p>
</li>
<li><p>ptr：指向用户空间Binder实体的指针，来自于flat_binder_object的binder成员。</p>
</li>
<li><p>cookie：指向用户空间的附加指针，来自于flat_binder_object的cookie成员</p>
</li>
<li><p>has_strong_ref, pending_strong_ref, has_weak_ref, pending_weak_ref：这一组标志用于控制驱动与Binder实体所在进程交互式修改引用计数。</p>
</li>
</ul>
<h2 id="binder-ref"><a href="#binder-ref" class="headerlink" title="binder_ref"></a>binder_ref</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * struct binder_ref - struct to track references on nodes</span></span><br><span class="line"><span class="comment"> * @data:        binder_ref_data containing id, handle, and current refcounts</span></span><br><span class="line"><span class="comment"> * @rb_node_desc: node for lookup by @data.desc in proc&#x27;s rb_tree</span></span><br><span class="line"><span class="comment"> * @rb_node_node: node for lookup by @node in proc&#x27;s rb_tree</span></span><br><span class="line"><span class="comment"> * @node_entry:  list entry for node-&gt;refs list in target node</span></span><br><span class="line"><span class="comment"> *               (protected by @node-&gt;lock)</span></span><br><span class="line"><span class="comment"> * @proc:        binder_proc containing ref</span></span><br><span class="line"><span class="comment"> * @node:        binder_node of target node. When cleaning up a</span></span><br><span class="line"><span class="comment"> *               ref for deletion in binder_cleanup_ref, a non-NULL</span></span><br><span class="line"><span class="comment"> *               @node indicates the node must be freed</span></span><br><span class="line"><span class="comment"> * @death:       pointer to death notification (ref_death) if requested</span></span><br><span class="line"><span class="comment"> *               (protected by @node-&gt;lock)</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * Structure to track references from procA to target node (on procB). This</span></span><br><span class="line"><span class="comment"> * structure is unsafe to access without holding @proc-&gt;outer_lock.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">binder_ref</span> &#123;</span></span><br><span class="line">    <span class="comment">/* Lookups needed: */</span></span><br><span class="line">    <span class="comment">/*   node + proc =&gt; ref (transaction) */</span></span><br><span class="line">    <span class="comment">/*   desc + proc =&gt; ref (transaction, inc/dec ref) */</span></span><br><span class="line">    <span class="comment">/*   node =&gt; refs + procs (proc exit) */</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_ref_data</span> <span class="title">data</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">rb_node</span> <span class="title">rb_node_desc</span>;</span>        <span class="comment">//以desc为索引的红黑树</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">rb_node</span> <span class="title">rb_node_node</span>;</span>        <span class="comment">//以node为索引的红黑树</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">hlist_node</span> <span class="title">node_entry</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_proc</span> *<span class="title">proc</span>;</span>           <span class="comment">//binder进程</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_node</span> *<span class="title">node</span>;</span>           <span class="comment">//binder节点</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_ref_death</span> *<span class="title">death</span>;</span>     <span class="comment">//如果不为空，则client想获知binder的死亡</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>binder_ref 描述了每个对服务对象的引用，对应与Client端。如上图所示，每个Ref通过node指向binder_node. 一个进程所有的binder_ref通过两个红黑树（RbTree)进行管理，通过binder_get_ref() 和 binder_get_ref_for_node快速查找。</p>
<h2 id="binder-proc"><a href="#binder-proc" class="headerlink" title="binder_proc"></a>binder_proc</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * struct binder_proc - binder process bookkeeping</span></span><br><span class="line"><span class="comment"> * @proc_node:            element for binder_procs list</span></span><br><span class="line"><span class="comment"> * @threads:              rbtree of binder_threads in this proc</span></span><br><span class="line"><span class="comment"> *                        (protected by @inner_lock)</span></span><br><span class="line"><span class="comment"> * @nodes:                rbtree of binder nodes associated with</span></span><br><span class="line"><span class="comment"> *                        this proc ordered by node-&gt;ptr</span></span><br><span class="line"><span class="comment"> *                        (protected by @inner_lock)</span></span><br><span class="line"><span class="comment"> * @refs_by_desc:         rbtree of refs ordered by ref-&gt;desc</span></span><br><span class="line"><span class="comment"> *                        (protected by @outer_lock)</span></span><br><span class="line"><span class="comment"> * @refs_by_node:         rbtree of refs ordered by ref-&gt;node</span></span><br><span class="line"><span class="comment"> *                        (protected by @outer_lock)</span></span><br><span class="line"><span class="comment"> * @waiting_threads:      threads currently waiting for proc work</span></span><br><span class="line"><span class="comment"> *                        (protected by @inner_lock)</span></span><br><span class="line"><span class="comment"> * @pid                   PID of group_leader of process</span></span><br><span class="line"><span class="comment"> *                        (invariant after initialized)</span></span><br><span class="line"><span class="comment"> * @tsk                   task_struct for group_leader of process</span></span><br><span class="line"><span class="comment"> *                        (invariant after initialized)</span></span><br><span class="line"><span class="comment"> * @deferred_work_node:   element for binder_deferred_list</span></span><br><span class="line"><span class="comment"> *                        (protected by binder_deferred_lock)</span></span><br><span class="line"><span class="comment"> * @deferred_work:        bitmap of deferred work to perform</span></span><br><span class="line"><span class="comment"> *                        (protected by binder_deferred_lock)</span></span><br><span class="line"><span class="comment"> * @is_dead:              process is dead and awaiting free</span></span><br><span class="line"><span class="comment"> *                        when outstanding transactions are cleaned up</span></span><br><span class="line"><span class="comment"> *                        (protected by @inner_lock)</span></span><br><span class="line"><span class="comment"> * @todo:                 list of work for this process</span></span><br><span class="line"><span class="comment"> *                        (protected by @inner_lock)</span></span><br><span class="line"><span class="comment"> * @stats:                per-process binder statistics</span></span><br><span class="line"><span class="comment"> *                        (atomics, no lock needed)</span></span><br><span class="line"><span class="comment"> * @delivered_death:      list of delivered death notification</span></span><br><span class="line"><span class="comment"> *                        (protected by @inner_lock)</span></span><br><span class="line"><span class="comment"> * @max_threads:          cap on number of binder threads</span></span><br><span class="line"><span class="comment"> *                        (protected by @inner_lock)</span></span><br><span class="line"><span class="comment"> * @requested_threads:    number of binder threads requested but not</span></span><br><span class="line"><span class="comment"> *                        yet started. In current implementation, can</span></span><br><span class="line"><span class="comment"> *                        only be 0 or 1.</span></span><br><span class="line"><span class="comment"> *                        (protected by @inner_lock)</span></span><br><span class="line"><span class="comment"> * @requested_threads_started: number binder threads started</span></span><br><span class="line"><span class="comment"> *                        (protected by @inner_lock)</span></span><br><span class="line"><span class="comment"> * @tmp_ref:              temporary reference to indicate proc is in use</span></span><br><span class="line"><span class="comment"> *                        (protected by @inner_lock)</span></span><br><span class="line"><span class="comment"> * @default_priority:     default scheduler priority</span></span><br><span class="line"><span class="comment"> *                        (invariant after initialized)</span></span><br><span class="line"><span class="comment"> * @debugfs_entry:        debugfs node</span></span><br><span class="line"><span class="comment"> * @alloc:                binder allocator bookkeeping</span></span><br><span class="line"><span class="comment"> * @context:              binder_context for this proc</span></span><br><span class="line"><span class="comment"> *                        (invariant after initialized)</span></span><br><span class="line"><span class="comment"> * @inner_lock:           can nest under outer_lock and/or node lock</span></span><br><span class="line"><span class="comment"> * @outer_lock:           no nesting under innor or node lock</span></span><br><span class="line"><span class="comment"> *                        Lock order: 1) outer, 2) node, 3) inner</span></span><br><span class="line"><span class="comment"> * @binderfs_entry:       process-specific binderfs log file</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * Bookkeeping structure for binder processes</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">binder_proc</span> &#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">hlist_node</span> <span class="title">proc_node</span>;</span>                <span class="comment">//进程节点</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">rb_root</span> <span class="title">threads</span>;</span>                     <span class="comment">//binder_thread红黑树的根节点，存放指针，指向进程所有的binder_thread, 用于Server端</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">rb_root</span> <span class="title">nodes</span>;</span>                       <span class="comment">//binder_node红黑树的根节点，存放指针，指向进程所有的binder 对象</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">rb_root</span> <span class="title">refs_by_desc</span>;</span>                <span class="comment">//记录binder引用, 便于快速查找,binder_ref红黑树的根节点(以handle为key)，它是Client在Binder驱动中的体现</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">rb_root</span> <span class="title">refs_by_node</span>;</span>                <span class="comment">//记录binder引用, 便于快速查找,binder_ref红黑树的根节点（以ptr为key），它是Client在Binder驱动中的体现</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> <span class="title">waiting_threads</span>;</span></span><br><span class="line">    <span class="keyword">int</span> pid;                                    <span class="comment">//相应进程id</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">task_struct</span> *<span class="title">tsk</span>;</span>                    <span class="comment">//相应进程的task结构体</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">hlist_node</span> <span class="title">deferred_work_node</span>;</span></span><br><span class="line">    <span class="keyword">int</span> deferred_work;</span><br><span class="line">    <span class="keyword">bool</span> is_dead;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> <span class="title">todo</span>;</span>                      <span class="comment">//进程将要做的事</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_stats</span> <span class="title">stats</span>;</span>                  <span class="comment">//binder统计信息</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> <span class="title">delivered_death</span>;</span></span><br><span class="line">    <span class="keyword">int</span> max_threads;</span><br><span class="line">    <span class="keyword">int</span> requested_threads;                      <span class="comment">//请求的线程数</span></span><br><span class="line">    <span class="keyword">int</span> requested_threads_started;              <span class="comment">//已启动的请求线程数</span></span><br><span class="line">    <span class="keyword">int</span> tmp_ref;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_priority</span> <span class="title">default_priority</span>;</span>    <span class="comment">//默认优先级</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">dentry</span> *<span class="title">debugfs_entry</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_alloc</span> <span class="title">alloc</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_context</span> *<span class="title">context</span>;</span></span><br><span class="line">    <span class="keyword">spinlock_t</span> inner_lock;</span><br><span class="line">    <span class="keyword">spinlock_t</span> outer_lock;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">dentry</span> *<span class="title">binderfs_entry</span>;</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>一个进程既包含的Service对象，也可能包含对其他Service对象的引用. 如果作为Service对象进程，它可能会存在多个Binder_Thread。这些信息都在binder_proc结构体进行管理。</p>
<h2 id="binder-buffer"><a href="#binder-buffer" class="headerlink" title="binder_buffer"></a>binder_buffer</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * struct binder_buffer - buffer used for binder transactions</span></span><br><span class="line"><span class="comment"> * @entry:              entry alloc-&gt;buffers</span></span><br><span class="line"><span class="comment"> * @rb_node:            node for allocated_buffers/free_buffers rb trees</span></span><br><span class="line"><span class="comment"> * @free:               %true if buffer is free</span></span><br><span class="line"><span class="comment"> * @allow_user_free:    %true if user is allowed to free buffer</span></span><br><span class="line"><span class="comment"> * @async_transaction:  %true if buffer is in use for an async txn</span></span><br><span class="line"><span class="comment"> * @debug_id:           unique ID for debugging</span></span><br><span class="line"><span class="comment"> * @transaction:        pointer to associated struct binder_transaction</span></span><br><span class="line"><span class="comment"> * @target_node:        struct binder_node associated with this buffer</span></span><br><span class="line"><span class="comment"> * @data_size:          size of @transaction data</span></span><br><span class="line"><span class="comment"> * @offsets_size:       size of array of offsets</span></span><br><span class="line"><span class="comment"> * @extra_buffers_size: size of space for other objects (like sg lists)</span></span><br><span class="line"><span class="comment"> * @user_data:          user pointer to base of buffer space</span></span><br><span class="line"><span class="comment"> * @pid:                pid to attribute the buffer to (caller)</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * Bookkeeping structure for binder transaction buffers</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">binder_buffer</span> &#123;</span></span><br><span class="line">    <span class="comment">/* free and allocated entries by address */</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> <span class="title">entry</span>;</span>                     <span class="comment">//buffer实体的地址</span></span><br><span class="line">    <span class="comment">/* free entry by size or allocated entry */</span></span><br><span class="line">    <span class="comment">/* by address */</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">rb_node</span> <span class="title">rb_node</span>;</span>                     <span class="comment">//buffer实体的地址</span></span><br><span class="line">    <span class="keyword">unsigned</span> <span class="built_in">free</span>:<span class="number">1</span>;                            <span class="comment">//标记是否是空闲buffer，占位1bit</span></span><br><span class="line">    <span class="keyword">unsigned</span> allow_user_free:<span class="number">1</span>;                 <span class="comment">//是否允许用户释放，占位1bit</span></span><br><span class="line">    <span class="keyword">unsigned</span> async_transaction:<span class="number">1</span>;</span><br><span class="line">    <span class="keyword">unsigned</span> debug_id:<span class="number">29</span>;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_transaction</span> *<span class="title">transaction</span>;</span>     <span class="comment">//该缓存区的需要处理的事务</span></span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_node</span> *<span class="title">target_node</span>;</span>            <span class="comment">//该缓存区所需处理的Binder实体</span></span><br><span class="line">    <span class="keyword">size_t</span> data_size;                           <span class="comment">//数据大小</span></span><br><span class="line">    <span class="keyword">size_t</span> offsets_size;                        <span class="comment">//数据偏移量</span></span><br><span class="line">    <span class="keyword">size_t</span> extra_buffers_size;</span><br><span class="line">    <span class="keyword">void</span> __user *user_data;                     <span class="comment">//用户数据</span></span><br><span class="line">    <span class="keyword">int</span>    pid;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>进程间通信除了命令，还有参数和返回值的交换，要将数据从一个进程的地址空间，传到另外一个进程的地址空间，通常需要两次拷贝，进程A -&gt; 内核 -&gt; 进程B。binder_buffer 就是内核里存放交换数据的空间（这些数据是以Parcel的形式存在）。为了提高效率，Android 的 binder 只需要一次拷贝，因为binder 进程通过mmap将内核空间地址映射到用户空间，从而可以直接访问binder_buffer的内容而无需一次额外拷贝。binder_buffer由内核在每次发起的binder调用创建，并赋给binder_transaction-&gt;buffer.binder driver 根据binder_transaction 生产 transaction_data（包含buffer的指针而非内容）, 并将其复制到用户空间。</p>
<h2 id="binder-thread"><a href="#binder-thread" class="headerlink" title="binder_thread"></a>binder_thread</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * struct binder_thread - binder thread bookkeeping</span></span><br><span class="line"><span class="comment"> * @proc:                 binder process for this thread</span></span><br><span class="line"><span class="comment"> *                        (invariant after initialization)</span></span><br><span class="line"><span class="comment"> * @rb_node:              element for proc-&gt;threads rbtree</span></span><br><span class="line"><span class="comment"> *                        (protected by @proc-&gt;inner_lock)</span></span><br><span class="line"><span class="comment"> * @waiting_thread_node:  element for @proc-&gt;waiting_threads list</span></span><br><span class="line"><span class="comment"> *                        (protected by @proc-&gt;inner_lock)</span></span><br><span class="line"><span class="comment"> * @pid:                  PID for this thread</span></span><br><span class="line"><span class="comment"> *                        (invariant after initialization)</span></span><br><span class="line"><span class="comment"> * @looper:               bitmap of looping state</span></span><br><span class="line"><span class="comment"> *                        (only accessed by this thread)</span></span><br><span class="line"><span class="comment"> * @looper_needs_return:  looping thread needs to exit driver</span></span><br><span class="line"><span class="comment"> *                        (no lock needed)</span></span><br><span class="line"><span class="comment"> * @transaction_stack:    stack of in-progress transactions for this thread</span></span><br><span class="line"><span class="comment"> *                        (protected by @proc-&gt;inner_lock)</span></span><br><span class="line"><span class="comment"> * @todo:                 list of work to do for this thread</span></span><br><span class="line"><span class="comment"> *                        (protected by @proc-&gt;inner_lock)</span></span><br><span class="line"><span class="comment"> * @process_todo:         whether work in @todo should be processed</span></span><br><span class="line"><span class="comment"> *                        (protected by @proc-&gt;inner_lock)</span></span><br><span class="line"><span class="comment"> * @return_error:         transaction errors reported by this thread</span></span><br><span class="line"><span class="comment"> *                        (only accessed by this thread)</span></span><br><span class="line"><span class="comment"> * @reply_error:          transaction errors reported by target thread</span></span><br><span class="line"><span class="comment"> *                        (protected by @proc-&gt;inner_lock)</span></span><br><span class="line"><span class="comment"> * @wait:                 wait queue for thread work</span></span><br><span class="line"><span class="comment"> * @stats:                per-thread statistics</span></span><br><span class="line"><span class="comment"> *                        (atomics, no lock needed)</span></span><br><span class="line"><span class="comment"> * @tmp_ref:              temporary reference to indicate thread is in use</span></span><br><span class="line"><span class="comment"> *                        (atomic since @proc-&gt;inner_lock cannot</span></span><br><span class="line"><span class="comment"> *                        always be acquired)</span></span><br><span class="line"><span class="comment"> * @is_dead:              thread is dead and awaiting free</span></span><br><span class="line"><span class="comment"> *                        when outstanding transactions are cleaned up</span></span><br><span class="line"><span class="comment"> *                        (protected by @proc-&gt;inner_lock)</span></span><br><span class="line"><span class="comment"> * @task:                 struct task_struct for this thread</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * Bookkeeping structure for binder threads.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">binder_thread</span> &#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_proc</span> *<span class="title">proc</span>;</span>   <span class="comment">//线程所属的进程</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">rb_node</span> <span class="title">rb_node</span>;</span>         <span class="comment">//红黑树节点</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> <span class="title">waiting_thread_node</span>;</span></span><br><span class="line">    <span class="keyword">int</span> pid;                          <span class="comment">//线程pid</span></span><br><span class="line">    <span class="keyword">int</span> looper;               <span class="comment">//looper的状态</span></span><br><span class="line">    <span class="keyword">bool</span> looper_need_return;  </span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_transaction</span> *<span class="title">transaction_stack</span>;</span>   <span class="comment">//线程正在处理的事务</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> <span class="title">todo</span>;</span>                   <span class="comment">//将要处理的链表</span></span><br><span class="line">    <span class="keyword">bool</span> process_todo;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_error</span> <span class="title">return_error</span>;</span>   <span class="comment">//write失败后，返回的错误码</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_error</span> <span class="title">reply_error</span>;</span></span><br><span class="line">    <span class="keyword">wait_queue_head_t</span> wait;                 <span class="comment">//等待队列的队头</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_stats</span> <span class="title">stats</span>;</span>          <span class="comment">//binder线程的统计信息</span></span><br><span class="line">    <span class="keyword">atomic_t</span> tmp_ref;</span><br><span class="line">    <span class="keyword">bool</span> is_dead;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">task_struct</span> *<span class="title">task</span>;</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>binder_proc里的threads 红黑树存放着指向binder_thread对象的指针。这里的binder_thread 不仅仅包括service的binder thread，也包括访问其他service的调用thread。 也就是说所有与binder相关的线程都会在binder_proc的threads红黑树里留下记录。binder_thread里最重要的两个成员变量是 transaction_stack 和 wait。<br>在binder_proc里面我们也能看到一个wait 队列，是不是意味着线程既可以在proc-&gt;wait上等待，也可以在thread-&gt;wait上等待？binder driver 对此有明确的用法，所有的binder threads (server 端）都等待在proc-&gt;wait上。因为对于服务端来说，用哪个thread来响应远程调用请求都是一样的。然而所有的ref thread(client端）的返回等待都发生在调用thread的wait 队列，因为，当某个binder thread 完成服务请求后，他必须唤醒特定的等待返回的线程。但是有一个例外，在双向调用的情况下，某个Server端的thread将会挂在thread-&gt;wait上等待，而不是proc-&gt;wait. 举个例子，假设两个进程P1 和 P2，各自运行了一个Service， S1，S2， P1 在 thread T1 里调用S2提供的服务，然后在T1-&gt;wait里等待返回。S2的服务在P2的binder thread(T2)里执行，执行过程中，S2又调到S1里的某个接口，按理S1 将在P1的binder thread T3里执行， 如果P1接下来又调到了P2，那又会产生新的进程 T4， 如果这个反复调用栈很深，需要耗费大量的线程，显然这是非常不高效的设计。所以，binder driver 里做了特殊的处理。当T2 调用 S1的接口函数时，binder driver 会遍历T2的transaction_stack, 如果发现这是一个双向调用（binder_transaction-&gt;from-&gt;proc 等于P1), 便会唤醒正在等待reply的T1，T1 完成这个请求后，继续等待S2的回复。这样，只需要最多两个Thread就可以完成多层的双向调用。<br>binder_thread里的transaction_stack 是用链表实现的堆栈， 调用线程和服务线程的transaction有着不同的堆栈。下图是上面这个例子的堆栈情形：<br><img src="https://cdn.nlark.com/yuque/0/2020/png/1759879/1601276620542-79b2eecc-f9bf-4a9a-be01-5913c0d3106c.png#height=454&id=Tmk3J&margin=%5Bobject%20Object%5D&name=binder_thread.png&originHeight=454&originWidth=1074&originalType=binary&ratio=1&size=49573&status=done&style=none&width=1074" alt="binder_thread.png"></p>
<h2 id="关系"><a href="#关系" class="headerlink" title="关系"></a>关系</h2><p><img src="https://cdn.nlark.com/yuque/0/2020/png/1759879/1601276823463-791dd136-7021-494b-ad96-702311539cf4.png#height=769&id=m3g6o&margin=%5Bobject%20Object%5D&name=binder_main_struct.png&originHeight=769&originWidth=1042&originalType=binary&ratio=1&size=313830&status=done&style=none&width=1042" alt="binder_main_struct.png"></p>
<h1 id="协议"><a href="#协议" class="headerlink" title="协议"></a>协议</h1><p>Binder协议可以分为控制协议和驱动协议两类。</p>
<h2 id="控制协议"><a href="#控制协议" class="headerlink" title="控制协议"></a>控制协议</h2><p>控制协议是进程通过ioctl(“/dev/binder”) 与Binder设备进行通讯的协议，该协议包含以下几种命令：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> BINDER_WRITE_READ       _IOWR(<span class="meta-string">&#x27;b&#x27;</span>, 1, struct binder_write_read)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> BINDER_SET_IDLE_TIMEOUT     _IOW(<span class="meta-string">&#x27;b&#x27;</span>, 3, __s64)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> BINDER_SET_MAX_THREADS      _IOW(<span class="meta-string">&#x27;b&#x27;</span>, 5, __u32)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> BINDER_SET_IDLE_PRIORITY    _IOW(<span class="meta-string">&#x27;b&#x27;</span>, 6, __s32)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> BINDER_SET_CONTEXT_MGR      _IOW(<span class="meta-string">&#x27;b&#x27;</span>, 7, __s32)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> BINDER_THREAD_EXIT      _IOW(<span class="meta-string">&#x27;b&#x27;</span>, 8, __s32)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> BINDER_VERSION          _IOWR(<span class="meta-string">&#x27;b&#x27;</span>, 9, struct binder_version)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> BINDER_GET_NODE_DEBUG_INFO  _IOWR(<span class="meta-string">&#x27;b&#x27;</span>, 11, struct binder_node_debug_info)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> BINDER_GET_NODE_INFO_FOR_REF    _IOWR(<span class="meta-string">&#x27;b&#x27;</span>, 12, struct binder_node_info_for_ref)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> BINDER_SET_CONTEXT_MGR_EXT  _IOW(<span class="meta-string">&#x27;b&#x27;</span>, 13, struct flat_binder_object)</span></span><br></pre></td></tr></table></figure>
<p>这些Binder IOCTL码的作用如下：</p>
<table>
<thead>
<tr>
<th>命令</th>
<th>说明</th>
<th>参数类型</th>
</tr>
</thead>
<tbody><tr>
<td><strong>BINDER_WRITE_READ</strong></td>
<td>读写操作，最常用的命令。IPC过程就是通过这个命令进行数据传递</td>
<td>binder_write_read</td>
</tr>
<tr>
<td>BINDER_SET_MAX_THREADS</td>
<td>设置进程支持的最大线程数量</td>
<td>size_t</td>
</tr>
<tr>
<td>BINDER_SET_CONTEXT_MGR</td>
<td>设置自身为ServiceManager</td>
<td>无</td>
</tr>
<tr>
<td>BINDER_THREAD_EXIT</td>
<td>通知驱动Binder线程退出</td>
<td>无</td>
</tr>
<tr>
<td>BINDER_VERSION</td>
<td>获取Binder驱动的版本号</td>
<td>binder_version</td>
</tr>
<tr>
<td>BINDER_SET_IDLE_PRIORITY</td>
<td>暂未用到</td>
<td>-</td>
</tr>
<tr>
<td>BINDER_SET_IDLE_TIMEOUT</td>
<td>暂未用到</td>
<td>-</td>
</tr>
</tbody></table>
<h2 id="驱动协议"><a href="#驱动协议" class="headerlink" title="驱动协议"></a>驱动协议</h2><p>Binder的驱动协议描述了对于Binder驱动的具体使用过程。驱动协议又可以分为两类：</p>
<ul>
<li>一类是binder_driver_command_protocol，描述了进程发送给Binder驱动的命令；也称BC码</li>
<li>一类是binder_driver_return_protocol，描述了Binder驱动发送给进程的命令；也称BR码<h3 id="BC码"><a href="#BC码" class="headerlink" title="BC码"></a>BC码</h3>BC码的作用如下：</li>
</ul>
<table>
<thead>
<tr>
<th>命令</th>
<th>说明</th>
<th>参数类型</th>
</tr>
</thead>
<tbody><tr>
<td>BC_TRANSACTION</td>
<td>Binder事务，即：Client对于Server的请求</td>
<td>binder_transaction_data</td>
</tr>
<tr>
<td>BC_REPLY</td>
<td>事务的应答，即：Server对于Client的回复</td>
<td>binder_transaction_data</td>
</tr>
<tr>
<td>BC_FREE_BUFFER</td>
<td>通知驱动释放Buffer</td>
<td>binder_uintptr_t</td>
</tr>
<tr>
<td>BC_ACQUIRE</td>
<td>强引用计数+1</td>
<td>__u32</td>
</tr>
<tr>
<td>BC_RELEASE</td>
<td>强引用计数-1</td>
<td>__u32</td>
</tr>
<tr>
<td>BC_INCREFS</td>
<td>弱引用计数+1</td>
<td>__u32</td>
</tr>
<tr>
<td>BC_DECREFS</td>
<td>弱引用计数-1</td>
<td>__u32</td>
</tr>
<tr>
<td>BC_ACQUIRE_DONE</td>
<td>BR_ACQUIRE的回复</td>
<td>binder_ptr_cookie</td>
</tr>
<tr>
<td>BC_INCREFS_DONE</td>
<td>BR_INCREFS的回复</td>
<td>binder_ptr_cookie</td>
</tr>
<tr>
<td>BC_ENTER_LOOPER</td>
<td>通知驱动主线程ready</td>
<td>void</td>
</tr>
<tr>
<td>BC_REGISTER_LOOPER</td>
<td>通知驱动子线程ready</td>
<td>void</td>
</tr>
<tr>
<td>BC_EXIT_LOOPER</td>
<td>通知驱动线程已经退出</td>
<td>void</td>
</tr>
<tr>
<td>BC_REQUEST_DEATH_NOTIFICATION</td>
<td>请求接收死亡通知</td>
<td>binder_handle_cookie</td>
</tr>
<tr>
<td>BC_CLEAR_DEATH_NOTIFICATION</td>
<td>去除接收死亡通知</td>
<td>binder_handle_cookie</td>
</tr>
<tr>
<td>BC_DEAD_BINDER_DONE</td>
<td>已经处理完死亡通知</td>
<td>binder_uintptr_t</td>
</tr>
<tr>
<td>BC_ATTEMPT_ACQUIRE</td>
<td>暂未实现</td>
<td>-</td>
</tr>
<tr>
<td>BC_ACQUIRE_RESULT</td>
<td>暂未实现</td>
<td>-</td>
</tr>
</tbody></table>
<p>​</p>
<h3 id="BR码"><a href="#BR码" class="headerlink" title="BR码"></a>BR码</h3><p>BR码的作用如下：</p>
<table>
<thead>
<tr>
<th>返回类型</th>
<th>说明</th>
<th>参数类型</th>
</tr>
</thead>
<tbody><tr>
<td>BR_OK</td>
<td>操作完成</td>
<td>void</td>
</tr>
<tr>
<td>BR_NOOP</td>
<td>操作完成</td>
<td>void</td>
</tr>
<tr>
<td>BR_ERROR</td>
<td>发生错误</td>
<td>__s32</td>
</tr>
<tr>
<td><strong>BR_TRANSACTION</strong></td>
<td>通知进程收到一次Binder请求（Server端）</td>
<td>binder_transaction_data</td>
</tr>
<tr>
<td><strong>BR_REPLY</strong></td>
<td>通知进程收到Binder请求的回复（Client）</td>
<td>binder_transaction_data</td>
</tr>
<tr>
<td>BR_TRANSACTION_COMPLETE</td>
<td>驱动对于接受请求的确认回复</td>
<td>void</td>
</tr>
<tr>
<td>BR_FAILED_REPLY</td>
<td>告知发送方通信目标不存在</td>
<td>void</td>
</tr>
<tr>
<td><strong>BR_SPAWN_LOOPER</strong></td>
<td>通知Binder进程创建一个新的线程</td>
<td>void</td>
</tr>
<tr>
<td>BR_ACQUIRE</td>
<td>强引用计数+1请求</td>
<td>binder_ptr_cookie</td>
</tr>
<tr>
<td>BR_RELEASE</td>
<td>强引用计数-1请求</td>
<td>binder_ptr_cookie</td>
</tr>
<tr>
<td>BR_INCREFS</td>
<td>弱引用计数+1请求</td>
<td>binder_ptr_cookie</td>
</tr>
<tr>
<td>BR_DECREFS</td>
<td>若引用计数-1请求</td>
<td>binder_ptr_cookie</td>
</tr>
<tr>
<td>BR_DEAD_BINDER</td>
<td>发送死亡通知</td>
<td>binder_uintptr_t</td>
</tr>
<tr>
<td>BR_CLEAR_DEATH_NOTIFICATION_DONE</td>
<td>清理死亡通知完成</td>
<td>binder_uintptr_t</td>
</tr>
<tr>
<td>BR_DEAD_REPLY</td>
<td>告知发送方对方已经死亡</td>
<td>void</td>
</tr>
<tr>
<td>BR_ACQUIRE_RESULT</td>
<td>暂未实现</td>
<td>-</td>
</tr>
<tr>
<td>BR_ATTEMPT_ACQUIRE</td>
<td>暂未实现</td>
<td>-</td>
</tr>
<tr>
<td>BR_FINISHED</td>
<td>暂未实现</td>
<td>-</td>
</tr>
</tbody></table>
<p>单独看上面的协议可能很难理解，这里我们以一次Binder请求过程来详细看一下Binder协议是如何通信的，就比较好理解了。<br><img src="https://cdn.nlark.com/yuque/0/2020/png/1759879/1601277652131-ec9e902c-703e-48a0-8d4a-c3dee5223fff.png#height=1110&id=sg5BT&margin=%5Bobject%20Object%5D&name=binder_request_sequence.png&originHeight=1110&originWidth=1559&originalType=binary&ratio=1&size=110963&status=done&style=none&width=1559" alt="binder_request_sequence.png"><br>从上图我们可以知道：</p>
<ul>
<li>Binder是C/S架构的，通信过程牵涉到：Client，Server以及Binder驱动三个角色</li>
<li>Client对于Server的请求以及Server对于Client回复都需要通过Binder驱动来中转数据</li>
<li>BC_XXX命令是进程发送给驱动的命令</li>
<li>BR_XXX命令是驱动发送给进程的命令</li>
<li>整个通信过程由Binder驱动控制</li>
</ul>
<p>通过上面的Binder协议的说明表中我们看到，Binder协议的通信过程中，不仅仅是发送请求和接受数据这些命令。同时包括了对于引用计数的管理和对于死亡通知的管理（告知一方，通讯的另外一方已经死亡）等功能。这些功能的通信过程和上面这幅图是类似的：一方发送BC_XXX，然后由驱动控制通信过程，接着发送对应的BR_XXX命令给通信的另外一方。</p>
<h1 id="Binder-init"><a href="#Binder-init" class="headerlink" title="Binder init"></a>Binder init</h1><p>内核初始化时，会调用到device_initcall()进行初始化，从而启动binder_init。binder_init()主要负责注册misc设备,通过调用misc_register()来实现。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> __init <span class="title">binder_init</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> ret;</span><br><span class="line">    <span class="keyword">char</span> *device_name, *device_tmp;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_device</span> *<span class="title">device</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">hlist_node</span> *<span class="title">tmp</span>;</span></span><br><span class="line">    <span class="keyword">char</span> *device_names = <span class="literal">NULL</span>;</span><br><span class="line"></span><br><span class="line">    ret = binder_alloc_shrinker_init();</span><br><span class="line">    <span class="keyword">if</span> (ret)</span><br><span class="line">        <span class="keyword">return</span> ret;</span><br><span class="line"></span><br><span class="line">    atomic_set(&amp;binder_transaction_log.cur, ~<span class="number">0U</span>);</span><br><span class="line">    atomic_set(&amp;binder_transaction_log_failed.cur, ~<span class="number">0U</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 在debugfs文件系统中创建一个目录，返回值是指向dentry的指针</span></span><br><span class="line"><span class="comment">     * 在手机对应的目录：/sys/kernel/debug/binder 里面创建了几个文件，用来记录binder操作过程中的信息和日志 </span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    binder_debugfs_dir_entry_root = debugfs_create_dir(<span class="string">&quot;binder&quot;</span>, <span class="literal">NULL</span>);</span><br><span class="line">    <span class="keyword">if</span> (binder_debugfs_dir_entry_root)</span><br><span class="line">        binder_debugfs_dir_entry_proc = debugfs_create_dir(<span class="string">&quot;proc&quot;</span>,</span><br><span class="line">                         binder_debugfs_dir_entry_root);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (binder_debugfs_dir_entry_root) &#123;</span><br><span class="line">        debugfs_create_file(<span class="string">&quot;state&quot;</span>,</span><br><span class="line">                    <span class="number">0444</span>,</span><br><span class="line">                    binder_debugfs_dir_entry_root,</span><br><span class="line">                    <span class="literal">NULL</span>,</span><br><span class="line">                    &amp;binder_state_fops);</span><br><span class="line">        debugfs_create_file(<span class="string">&quot;stats&quot;</span>,</span><br><span class="line">                    <span class="number">0444</span>,</span><br><span class="line">                    binder_debugfs_dir_entry_root,</span><br><span class="line">                    <span class="literal">NULL</span>,</span><br><span class="line">                    &amp;binder_stats_fops);</span><br><span class="line">        debugfs_create_file(<span class="string">&quot;transactions&quot;</span>,</span><br><span class="line">                    <span class="number">0444</span>,</span><br><span class="line">                    binder_debugfs_dir_entry_root,</span><br><span class="line">                    <span class="literal">NULL</span>,</span><br><span class="line">                    &amp;binder_transactions_fops);</span><br><span class="line">        debugfs_create_file(<span class="string">&quot;transaction_log&quot;</span>,</span><br><span class="line">                    <span class="number">0444</span>,</span><br><span class="line">                    binder_debugfs_dir_entry_root,</span><br><span class="line">                    &amp;binder_transaction_log,</span><br><span class="line">                    &amp;binder_transaction_log_fops);</span><br><span class="line">        debugfs_create_file(<span class="string">&quot;failed_transaction_log&quot;</span>,</span><br><span class="line">                    <span class="number">0444</span>,</span><br><span class="line">                    binder_debugfs_dir_entry_root,</span><br><span class="line">                    &amp;binder_transaction_log_failed,</span><br><span class="line">                    &amp;binder_transaction_log_fops);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!IS_ENABLED(CONFIG_ANDROID_BINDERFS) &amp;&amp;</span><br><span class="line">        <span class="built_in">strcmp</span>(binder_devices_param, <span class="string">&quot;&quot;</span>) != <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="comment">/*</span></span><br><span class="line"><span class="comment">        * Copy the module_parameter string, because we don&#x27;t want to</span></span><br><span class="line"><span class="comment">        * tokenize it in-place.</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        device_names = kstrdup(binder_devices_param, GFP_KERNEL);</span><br><span class="line">        <span class="keyword">if</span> (!device_names) &#123;</span><br><span class="line">            ret = -ENOMEM;</span><br><span class="line">            <span class="keyword">goto</span> err_alloc_device_names_failed;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">/**</span></span><br><span class="line"><span class="comment">         * Android8.0 中引入了hwbinder，vndbinder，所以现在有三个binder，分别需要创建三个binder device:</span></span><br><span class="line"><span class="comment">         * 循环注册binder 的三个设备：</span></span><br><span class="line"><span class="comment">         * /dev/binder</span></span><br><span class="line"><span class="comment">         * /dev/hwbinder</span></span><br><span class="line"><span class="comment">         * /dev/vndbinder</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        device_tmp = device_names;</span><br><span class="line">        <span class="keyword">while</span> ((device_name = strsep(&amp;device_tmp, <span class="string">&quot;,&quot;</span>))) &#123;</span><br><span class="line">            ret = init_binder_device(device_name);</span><br><span class="line">            <span class="keyword">if</span> (ret)</span><br><span class="line">                <span class="keyword">goto</span> err_init_binder_device_failed;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    ret = init_binderfs();</span><br><span class="line">    <span class="keyword">if</span> (ret)</span><br><span class="line">        <span class="keyword">goto</span> err_init_binder_device_failed;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> ret;</span><br><span class="line"></span><br><span class="line">err_init_binder_device_failed:</span><br><span class="line">    hlist_for_each_entry_safe(device, tmp, &amp;binder_devices, hlist) &#123;</span><br><span class="line">        misc_deregister(&amp;device-&gt;miscdev);</span><br><span class="line">        hlist_del(&amp;device-&gt;hlist);</span><br><span class="line">        kfree(device);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    kfree(device_names);</span><br><span class="line"></span><br><span class="line">err_alloc_device_names_failed:</span><br><span class="line">    debugfs_remove_recursive(binder_debugfs_dir_entry_root);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> ret;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>其实binder_init()中最重要的是创建设备文件的函数misc_register(&amp;device-&gt;miscdev); 也就是将device-&gt;miscdev注册为misc设备。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">miscdevice</span>  &#123;</span></span><br><span class="line">    <span class="keyword">int</span> minor;                          <span class="comment">//次设备号 动态分配 MISC_DYNAMIC_MINOR</span></span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span> *name;                   <span class="comment">//设备名如/dev/binder、/dev/hwbinder、/dev/vndbinder&quot;</span></span><br><span class="line">    <span class="keyword">const</span> <span class="class"><span class="keyword">struct</span> <span class="title">file_operations</span> *<span class="title">fops</span>;</span> <span class="comment">//设备的文件操作结构，这是file_operations结构</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> <span class="title">list</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">device</span> *<span class="title">parent</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">device</span> *<span class="title">this_device</span>;</span></span><br><span class="line">    <span class="keyword">const</span> <span class="class"><span class="keyword">struct</span> <span class="title">attribute_group</span> **<span class="title">groups</span>;</span></span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span> *nodename;</span><br><span class="line">    <span class="keyword">umode_t</span> mode;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * struct binder_device - information about a binder device node</span></span><br><span class="line"><span class="comment"> * @hlist:          list of binder devices (only used for devices requested via</span></span><br><span class="line"><span class="comment"> *                  CONFIG_ANDROID_BINDER_DEVICES)</span></span><br><span class="line"><span class="comment"> * @miscdev:        information about a binder character device node</span></span><br><span class="line"><span class="comment"> * @context:        binder context information</span></span><br><span class="line"><span class="comment"> * @binderfs_inode: This is the inode of the root dentry of the super block</span></span><br><span class="line"><span class="comment"> *                  belonging to a binderfs mount.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">binder_device</span> &#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">hlist_node</span> <span class="title">hlist</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">miscdevice</span> <span class="title">miscdev</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_context</span> <span class="title">context</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">inode</span> *<span class="title">binderfs_inode</span>;</span></span><br><span class="line">    <span class="keyword">refcount_t</span> ref;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> <span class="class"><span class="keyword">struct</span> <span class="title">file_operations</span> <span class="title">binder_fops</span> =</span> &#123;</span><br><span class="line">    .owner = THIS_MODULE,</span><br><span class="line">    .poll = binder_poll,</span><br><span class="line">    .unlocked_ioctl = binder_ioctl,</span><br><span class="line">    .compat_ioctl = binder_ioctl,</span><br><span class="line">    .mmap = binder_mmap,</span><br><span class="line">    .open = binder_open,</span><br><span class="line">    .flush = binder_flush,</span><br><span class="line">    .release = binder_release,</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> __init <span class="title">init_binder_device</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *name)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> ret;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_device</span> *<span class="title">binder_device</span>;</span></span><br><span class="line"></span><br><span class="line">    binder_device = kzalloc(<span class="keyword">sizeof</span>(*binder_device), GFP_KERNEL);</span><br><span class="line">    <span class="keyword">if</span> (!binder_device)</span><br><span class="line">        <span class="keyword">return</span> -ENOMEM;</span><br><span class="line"></span><br><span class="line">    binder_device-&gt;miscdev.fops = &amp;binder_fops;</span><br><span class="line">    binder_device-&gt;miscdev.minor = MISC_DYNAMIC_MINOR;</span><br><span class="line">    binder_device-&gt;miscdev.name = name;</span><br><span class="line"></span><br><span class="line">    refcount_set(&amp;binder_device-&gt;ref, <span class="number">1</span>);</span><br><span class="line">    binder_device-&gt;context.binder_context_mgr_uid = INVALID_UID;</span><br><span class="line">    binder_device-&gt;context.name = name;</span><br><span class="line">    mutex_init(&amp;binder_device-&gt;context.context_mgr_node_lock);</span><br><span class="line"></span><br><span class="line">    ret = misc_register(&amp;binder_device-&gt;miscdev);</span><br><span class="line">    <span class="keyword">if</span> (ret &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        kfree(binder_device);</span><br><span class="line">        <span class="keyword">return</span> ret;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    hlist_add_head(&amp;binder_device-&gt;hlist, &amp;binder_devices);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> ret;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>binder_device结构体包含了一个哈希链表、一个misc设备结构、一个binder context等<br>miscdevice结构体我们主要关注name，fops；其中name就是设备名如/dev/binder、/dev/hwbinder、/dev/vndbinder；fops设备的文件操作结构，作用是标识当前设备的操作函数。从这个成员变量的定义中我们可以看到这个设备所具备的接口，如binder_fops中可以看到有binder_ioctl、binder_open和binder_mmap等接口。当我们对这个设备进行各种操作时，就会调用其中的函数。</p>
<h1 id="Binder-open"><a href="#Binder-open" class="headerlink" title="Binder open"></a>Binder open</h1><p>任何进程在使用Binder之前，都需要先通过open(“/dev/binder”)打开Binder设备。上文已经提到，用户空间的open系统调用对应了驱动中的binder_open函数。在这个函数，Binder驱动会为调用的进程做一些初始化工作。binder_open函数代码如下所示：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">binder_open</span><span class="params">(struct inode *nodp, struct file *filp)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_proc</span> *<span class="title">proc</span>, *<span class="title">itr</span>;</span><span class="comment">//binder进程</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_device</span> *<span class="title">binder_dev</span>;</span><span class="comment">//binder device</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binderfs_info</span> *<span class="title">info</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">dentry</span> *<span class="title">binder_binderfs_dir_entry_proc</span> =</span> <span class="literal">NULL</span>;</span><br><span class="line">    <span class="keyword">bool</span> existing_pid = <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">    binder_debug(BINDER_DEBUG_OPEN_CLOSE, <span class="string">&quot;%s: %d:%d\n&quot;</span>, __func__,</span><br><span class="line">             current-&gt;group_leader-&gt;pid, current-&gt;pid);</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 为binder_proc结构体在分配kernel内存空间</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    proc = kzalloc(<span class="keyword">sizeof</span>(*proc), GFP_KERNEL);</span><br><span class="line">    <span class="keyword">if</span> (proc == <span class="literal">NULL</span>)</span><br><span class="line">        <span class="keyword">return</span> -ENOMEM;</span><br><span class="line">    spin_lock_init(&amp;proc-&gt;inner_lock);</span><br><span class="line">    spin_lock_init(&amp;proc-&gt;outer_lock);</span><br><span class="line">    get_task_struct(current-&gt;group_leader);<span class="comment">//增加线程引用计数</span></span><br><span class="line">    proc-&gt;tsk = current-&gt;group_leader;<span class="comment">//将当前线程的task保存到binder进程的tsk</span></span><br><span class="line">    INIT_LIST_HEAD(&amp;proc-&gt;todo);<span class="comment">//初始化todo队列，用于存放待处理的请求（server端）</span></span><br><span class="line">    <span class="keyword">if</span> (binder_supported_policy(current-&gt;policy)) &#123;</span><br><span class="line">        proc-&gt;default_priority.sched_policy = current-&gt;policy;</span><br><span class="line">        proc-&gt;default_priority.prio = current-&gt;normal_prio;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        proc-&gt;default_priority.sched_policy = SCHED_NORMAL;</span><br><span class="line">        proc-&gt;default_priority.prio = NICE_TO_PRIO(<span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* binderfs stashes devices in i_private */</span></span><br><span class="line">    <span class="keyword">if</span> (is_binderfs_device(nodp)) &#123;</span><br><span class="line">        binder_dev = nodp-&gt;i_private;</span><br><span class="line">        info = nodp-&gt;i_sb-&gt;s_fs_info;</span><br><span class="line">        binder_binderfs_dir_entry_proc = info-&gt;proc_log_dir;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        binder_dev = container_of(filp-&gt;private_data,</span><br><span class="line">                      struct binder_device, miscdev);</span><br><span class="line">    &#125;</span><br><span class="line">    refcount_inc(&amp;binder_dev-&gt;ref);</span><br><span class="line">    proc-&gt;context = &amp;binder_dev-&gt;context;<span class="comment">//拿到binder device的context，传给binder_proc</span></span><br><span class="line">    binder_alloc_init(&amp;proc-&gt;alloc);</span><br><span class="line"></span><br><span class="line">    binder_stats_created(BINDER_STAT_PROC);<span class="comment">//类型为BINDER_STAT_PROC对象的创建个数加1</span></span><br><span class="line">    proc-&gt;pid = current-&gt;group_leader-&gt;pid;<span class="comment">//记录当前进程的pid</span></span><br><span class="line">    INIT_LIST_HEAD(&amp;proc-&gt;delivered_death);</span><br><span class="line">    INIT_LIST_HEAD(&amp;proc-&gt;waiting_threads);</span><br><span class="line">    filp-&gt;private_data = proc;<span class="comment">//将binder_proc存放在filp的private_data域，以便于在之后的mmap、ioctl中获取</span></span><br><span class="line"></span><br><span class="line">    mutex_lock(&amp;binder_procs_lock);</span><br><span class="line">    hlist_for_each_entry(itr, &amp;binder_procs, proc_node) &#123;</span><br><span class="line">        <span class="keyword">if</span> (itr-&gt;pid == proc-&gt;pid) &#123;</span><br><span class="line">            existing_pid = <span class="literal">true</span>;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    hlist_add_head(&amp;proc-&gt;proc_node, &amp;binder_procs);<span class="comment">//将proc_node节点添加到binder_procs为表头的队列</span></span><br><span class="line">    mutex_unlock(&amp;binder_procs_lock);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//如果/sys/kernel/debug/binder/proc 目录存在，并且pid不存在</span></span><br><span class="line">    <span class="comment">//在该目录中创建相应pid对应的文件，名称为pid，用来记录binder_proc的状态</span></span><br><span class="line">    <span class="keyword">if</span> (binder_debugfs_dir_entry_proc &amp;&amp; !existing_pid) &#123;</span><br><span class="line">        <span class="keyword">char</span> strbuf[<span class="number">11</span>];</span><br><span class="line"></span><br><span class="line">        <span class="built_in">snprintf</span>(strbuf, <span class="keyword">sizeof</span>(strbuf), <span class="string">&quot;%u&quot;</span>, proc-&gt;pid);</span><br><span class="line">        <span class="comment">/*</span></span><br><span class="line"><span class="comment">         * proc debug entries are shared between contexts.</span></span><br><span class="line"><span class="comment">         * Only create for the first PID to avoid debugfs log spamming</span></span><br><span class="line"><span class="comment">         * The printing code will anyway print all contexts for a given</span></span><br><span class="line"><span class="comment">         * PID so this is not a problem.</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        proc-&gt;debugfs_entry = debugfs_create_file(strbuf, <span class="number">0444</span>,</span><br><span class="line">            binder_debugfs_dir_entry_proc,</span><br><span class="line">            (<span class="keyword">void</span> *)(<span class="keyword">unsigned</span> <span class="keyword">long</span>)proc-&gt;pid,</span><br><span class="line">            &amp;proc_fops);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (binder_binderfs_dir_entry_proc &amp;&amp; !existing_pid) &#123;</span><br><span class="line">        <span class="keyword">char</span> strbuf[<span class="number">11</span>];</span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">dentry</span> *<span class="title">binderfs_entry</span>;</span></span><br><span class="line"></span><br><span class="line">        <span class="built_in">snprintf</span>(strbuf, <span class="keyword">sizeof</span>(strbuf), <span class="string">&quot;%u&quot;</span>, proc-&gt;pid);</span><br><span class="line">        <span class="comment">/*</span></span><br><span class="line"><span class="comment">         * Similar to debugfs, the process specific log file is shared</span></span><br><span class="line"><span class="comment">         * between contexts. Only create for the first PID.</span></span><br><span class="line"><span class="comment">         * This is ok since same as debugfs, the log file will contain</span></span><br><span class="line"><span class="comment">         * information on all contexts of a given PID.</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        binderfs_entry = binderfs_create_file(binder_binderfs_dir_entry_proc,</span><br><span class="line">            strbuf, &amp;proc_fops, (<span class="keyword">void</span> *)(<span class="keyword">unsigned</span> <span class="keyword">long</span>)proc-&gt;pid);</span><br><span class="line">        <span class="keyword">if</span> (!IS_ERR(binderfs_entry)) &#123;</span><br><span class="line">            proc-&gt;binderfs_entry = binderfs_entry;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">int</span> error;</span><br><span class="line"></span><br><span class="line">            error = PTR_ERR(binderfs_entry);</span><br><span class="line">            pr_warn(<span class="string">&quot;Unable to create file %s in binderfs (error %d)\n&quot;</span>,</span><br><span class="line">                strbuf, error);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>**binder_open()**职责如下：</p>
<ul>
<li>首先创建了binder_proc结构体实例proc</li>
<li>接着开始初始化一系列成员：tsk, todo, default_priority, pid， delivered_death等</li>
<li>更新了统计数据：binder_proc的创建个数加1</li>
<li>紧接着将初始化好的proc，存放到文件指针filp-&gt;private_data中，以便于在之后的mmap、ioctl中获取</li>
<li>将binder_proc链入binder_procs哈希链表中</li>
<li>最后查看是否创建的了/sys/kernel/debug/binde/proc/目录，有的话再创建一个/sys/kernel/debug/binde/proc/pid文件，用来记录binder_proc的状态</li>
<li></li>
</ul>
<p><img src="https://cdn.nlark.com/yuque/0/2020/png/1759879/1601278060393-6945df61-e592-406f-83eb-7d38ecef9511.png#height=504&id=uYde0&margin=%5Bobject%20Object%5D&name=binder_procs.png&originHeight=504&originWidth=817&originalType=binary&ratio=1&size=29997&status=done&style=none&width=817" alt="binder_procs.png"></p>
<h1 id="Binder-mmap"><a href="#Binder-mmap" class="headerlink" title="Binder mmap"></a>Binder mmap</h1><p>与open操作类似，这里的mmap操作对应了Binder驱动中binder_fops的mmap，也就是binder_mmap()。其的作用有如下两个：</p>
<ul>
<li><p>申请一块内存空间，用来接收Binder通信过程中的数据。</p>
</li>
<li><p>对这块内存进行地址映射，以便将来访问。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">binder_mmap</span><span class="params">(struct file *filp, struct vm_area_struct *vma)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> ret;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_proc</span> *<span class="title">proc</span> =</span> filp-&gt;private_data;<span class="comment">//private_data保存了我们open设备时创建的binder_proc信息</span></span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span> *failure_string;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (proc-&gt;tsk != current-&gt;group_leader)</span><br><span class="line">        <span class="keyword">return</span> -EINVAL;</span><br><span class="line"></span><br><span class="line">    binder_debug(BINDER_DEBUG_OPEN_CLOSE,</span><br><span class="line">             <span class="string">&quot;%s: %d %lx-%lx (%ld K) vma %lx pagep %lx\n&quot;</span>,</span><br><span class="line">             __func__, proc-&gt;pid, vma-&gt;vm_start, vma-&gt;vm_end,</span><br><span class="line">             (vma-&gt;vm_end - vma-&gt;vm_start) / SZ_1K, vma-&gt;vm_flags,</span><br><span class="line">             (<span class="keyword">unsigned</span> <span class="keyword">long</span>)pgprot_val(vma-&gt;vm_page_prot));</span><br><span class="line"></span><br><span class="line">    <span class="comment">//mmap 的 buffer 禁止用户进行写操作。mmap 只是为了分配内核空间，传递数据通过 ioctl()</span></span><br><span class="line">    <span class="keyword">if</span> (vma-&gt;vm_flags &amp; FORBIDDEN_MMAP_FLAGS) &#123;</span><br><span class="line">        ret = -EPERM;</span><br><span class="line">        failure_string = <span class="string">&quot;bad vm_flags&quot;</span>;</span><br><span class="line">        <span class="keyword">goto</span> err_bad_arg;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 将 VM_DONTCOP 置起，禁止 拷贝，禁止 写操作</span></span><br><span class="line">    vma-&gt;vm_flags |= VM_DONTCOPY | VM_MIXEDMAP;</span><br><span class="line">    vma-&gt;vm_flags &amp;= ~VM_MAYWRITE;</span><br><span class="line"></span><br><span class="line">    vma-&gt;vm_ops = &amp;binder_vm_ops;</span><br><span class="line">    vma-&gt;vm_private_data = proc;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 再次完善 binder buffer allocator</span></span><br><span class="line">    ret = binder_alloc_mmap_handler(&amp;proc-&gt;alloc, vma);</span><br><span class="line">    <span class="keyword">if</span> (ret)</span><br><span class="line">        <span class="keyword">return</span> ret;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">err_bad_arg:</span><br><span class="line">    pr_err(<span class="string">&quot;%s: %d %lx-%lx %s failed %d\n&quot;</span>, __func__,</span><br><span class="line">           proc-&gt;pid, vma-&gt;vm_start, vma-&gt;vm_end, failure_string, ret);</span><br><span class="line">    <span class="keyword">return</span> ret;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * binder_alloc_mmap_handler() - map virtual address space for proc</span></span><br><span class="line"><span class="comment"> * @alloc:  alloc structure for this proc</span></span><br><span class="line"><span class="comment"> * @vma:    vma passed to mmap()</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * Called by binder_mmap() to initialize the space specified in</span></span><br><span class="line"><span class="comment"> * vma for allocating binder buffers</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * Return:</span></span><br><span class="line"><span class="comment"> *      0 = success</span></span><br><span class="line"><span class="comment"> *      -EBUSY = address space already mapped</span></span><br><span class="line"><span class="comment"> *      -ENOMEM = failed to map memory to given address space</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">binder_alloc_mmap_handler</span><span class="params">(struct binder_alloc *alloc,</span></span></span><br><span class="line"><span class="params"><span class="function">                  struct vm_area_struct *vma)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> ret;</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span> *failure_string;</span><br><span class="line">    <span class="comment">//每一次Binder传输数据时，都会先从Binder内存缓存区中分配一个binder_buffer来存储传输数据</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_buffer</span> *<span class="title">buffer</span>;</span></span><br><span class="line"></span><br><span class="line">    mutex_lock(&amp;binder_alloc_mmap_lock);<span class="comment">//同步锁</span></span><br><span class="line">    <span class="keyword">if</span> (alloc-&gt;buffer_size) &#123;<span class="comment">// 不需要重复mmap</span></span><br><span class="line">        ret = -EBUSY;</span><br><span class="line">        failure_string = <span class="string">&quot;already mapped&quot;</span>;</span><br><span class="line">        <span class="keyword">goto</span> err_already_mapped;</span><br><span class="line">    &#125;</span><br><span class="line">    alloc-&gt;buffer_size = <span class="keyword">min_t</span>(<span class="keyword">unsigned</span> <span class="keyword">long</span>, vma-&gt;vm_end - vma-&gt;vm_start,</span><br><span class="line">                   SZ_4M);</span><br><span class="line">    mutex_unlock(&amp;binder_alloc_mmap_lock);<span class="comment">//释放锁</span></span><br><span class="line"></span><br><span class="line">    alloc-&gt;buffer = (<span class="keyword">void</span> __user *)vma-&gt;vm_start;<span class="comment">//指向用户进程内核虚拟空间的 start地址</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">//分配物理页的指针数组，数组大小为vma的等效page个数</span></span><br><span class="line">    alloc-&gt;pages = kcalloc(alloc-&gt;buffer_size / PAGE_SIZE,</span><br><span class="line">                   <span class="keyword">sizeof</span>(alloc-&gt;pages[<span class="number">0</span>]),</span><br><span class="line">                   GFP_KERNEL);</span><br><span class="line">    <span class="keyword">if</span> (alloc-&gt;pages == <span class="literal">NULL</span>) &#123;</span><br><span class="line">        ret = -ENOMEM;</span><br><span class="line">        failure_string = <span class="string">&quot;alloc page array&quot;</span>;</span><br><span class="line">        <span class="keyword">goto</span> err_alloc_pages_failed;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    buffer = kzalloc(<span class="keyword">sizeof</span>(*buffer), GFP_KERNEL);<span class="comment">//申请一个binder_buffer的内存</span></span><br><span class="line">    <span class="keyword">if</span> (!buffer) &#123;</span><br><span class="line">        ret = -ENOMEM;</span><br><span class="line">        failure_string = <span class="string">&quot;alloc buffer struct&quot;</span>;</span><br><span class="line">        <span class="keyword">goto</span> err_alloc_buf_struct_failed;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    buffer-&gt;user_data = alloc-&gt;buffer;<span class="comment">//指向用户进程内核虚拟空间的 start地址，即为当前进程mmap的内核空间地址</span></span><br><span class="line">    list_add(&amp;buffer-&gt;entry, &amp;alloc-&gt;buffers);<span class="comment">//将binder_buffer地址 加入到所属进程的buffers队列</span></span><br><span class="line">    buffer-&gt;<span class="built_in">free</span> = <span class="number">1</span>;</span><br><span class="line">    binder_insert_free_buffer(alloc, buffer);<span class="comment">//将 当前 buffer 加入到 红黑树 alloc-&gt;free_buffers 中，表示当前 buffer 是空闲buffer</span></span><br><span class="line">    alloc-&gt;free_async_space = alloc-&gt;buffer_size / <span class="number">2</span>;<span class="comment">// 将 异步事务 的空间大小设置为 整个空间的一半</span></span><br><span class="line">    binder_alloc_set_vma(alloc, vma);</span><br><span class="line">    mmgrab(alloc-&gt;vma_vm_mm);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">err_alloc_buf_struct_failed:</span><br><span class="line">    kfree(alloc-&gt;pages);</span><br><span class="line">    alloc-&gt;pages = <span class="literal">NULL</span>;</span><br><span class="line">err_alloc_pages_failed:</span><br><span class="line">    alloc-&gt;buffer = <span class="literal">NULL</span>;</span><br><span class="line">    mutex_lock(&amp;binder_alloc_mmap_lock);</span><br><span class="line">    alloc-&gt;buffer_size = <span class="number">0</span>;</span><br><span class="line">err_already_mapped:</span><br><span class="line">    mutex_unlock(&amp;binder_alloc_mmap_lock);</span><br><span class="line">    binder_alloc_debug(BINDER_DEBUG_USER_ERROR,</span><br><span class="line">               <span class="string">&quot;%s: %d %lx-%lx %s failed %d\n&quot;</span>, __func__,</span><br><span class="line">               alloc-&gt;pid, vma-&gt;vm_start, vma-&gt;vm_end,</span><br><span class="line">               failure_string, ret);</span><br><span class="line">    <span class="keyword">return</span> ret;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>首先在内核虚拟地址空间，申请一块与用户虚拟内存相同大小的内存；然后再申请page物理内存，再将同一块物理内存分别映射到内核虚拟地址空间和用户虚拟内存空间，从而实现了用户空间的Buffer和内核空间的Buffer同步操作的功能。</p>
</li>
<li><p>filp-&gt;private_data保存了我们open设备时创建的binder_proc信息。</p>
</li>
<li><p>为用户进程分配一块内核空间作为缓冲区。</p>
</li>
<li><p>把分配的缓冲区指针存放到binder_proc的buffer字段。</p>
</li>
<li><p>分配pages空间。</p>
</li>
<li><p>在内核分配一块同样页数的内核空间，并把它的物理内存和前面为用户进程分配的内存地址关联。</p>
</li>
<li><p>将刚才分配的内存块加入用户进程内存链表。</p>
</li>
</ul>
<p>​</p>
<p>binder_mmap这个函数中，会申请一块物理内存，然后在用户空间和内核空间同时对应到这块内存上。在这之后，当有Client要发送数据给Server的时候，只需一次，将Client发送过来的数据拷贝到Server端的内核空间指定的内存地址即可，由于这个内存地址在服务端已经同时映射到用户空间，因此无需再做一次复制，Server即可直接访问，整个过程如下图所示：<br><img src="https://cdn.nlark.com/yuque/0/2020/png/1759879/1601279559463-03d52f64-ebd2-4ece-91d0-d6559e114ba5.png#height=708&id=kpblY&margin=%5Bobject%20Object%5D&name=mmap_and_transaction.png&originHeight=708&originWidth=1196&originalType=binary&ratio=1&size=120117&status=done&style=none&width=1196" alt="mmap_and_transaction.png"></p>
<blockquote>
<ul>
<li>Server在启动之后，调用对/dev/binder设备调用mmap。</li>
<li>内核中的binder_mmap函数进行对应的处理：申请一块物理内存，然后在用户空间和内核空间同时进行映射。</li>
<li>Client通过BINDER_WRITE_READ命令发送请求，这个请求将先到驱动中，同时需要将数据从Client进程的用户空间拷贝到内核空间。</li>
<li>驱动通过BR_TRANSACTION通知Server有人发出请求，Server进行处理。由于这块内存也在用户空间进行了映射，因此Server进程的代码可以直接访问。</li>
</ul>
</blockquote>
<p>这就是我们常说的使用Binder机制，数据只需要经历一次拷贝就可以了，其原理就在这。</p>
<h1 id="Binder-数据交换"><a href="#Binder-数据交换" class="headerlink" title="Binder 数据交换"></a>Binder 数据交换</h1><p>对Binder驱动而言，不管是ServiceManager还是Service和Client，都为客户端。客户端调用Binder驱动ioctl()进行数据交换的方法如：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">ioctl(mProcess-&gt;mDriverFD, BINDER_WRITE_READ, &amp;bwr)</span><br></pre></td></tr></table></figure>

<ul>
<li>mProcess-&gt;mDriverFD：对应了打开Binder设备时的fd。</li>
<li>BINDER_WRITE_READ：对应了具体要做的操作码，这个操作码将由Binder驱动解析。</li>
<li>bwr：存储了请求数据，其类型是binder_write_read。</li>
</ul>
<p>binder_write_read其实是一个相对外层的数据结构，其内部会包含一个binder_transaction_data结构的数据。binder_transaction_data包含了发出请求者的标识，请求的目标对象以及请求所需要的参数。它们的关系如下图所示：<br><img src="https://cdn.nlark.com/yuque/0/2020/png/1759879/1601291303139-8119287e-9fbe-4d30-bba2-80eea8a3a985.png#height=1588&id=ClfKk&margin=%5Bobject%20Object%5D&name=binder_write_read.png&originHeight=1588&originWidth=2374&originalType=binary&ratio=1&size=267567&status=done&style=none&width=2374" alt="binder_write_read.png"></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">long</span> <span class="title">binder_ioctl</span><span class="params">(struct file *filp, <span class="keyword">unsigned</span> <span class="keyword">int</span> cmd, <span class="keyword">unsigned</span> <span class="keyword">long</span> arg)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> ret;</span><br><span class="line">    <span class="comment">//filp-&gt;private_data 在open()binder驱动时，保存了一个创建的binder_proc，即是此时调用进程的binder_proc</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_proc</span> *<span class="title">proc</span> =</span> filp-&gt;private_data;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_thread</span> *<span class="title">thread</span>;</span><span class="comment">//binder线程</span></span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> size = _IOC_SIZE(cmd);</span><br><span class="line">    <span class="keyword">void</span> __user *ubuf = (<span class="keyword">void</span> __user *)arg;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/*pr_info(&quot;binder_ioctl: %d:%d %x %lx\n&quot;,</span></span><br><span class="line"><span class="comment">            proc-&gt;pid, current-&gt;pid, cmd, arg);*/</span></span><br><span class="line"></span><br><span class="line">    binder_selftest_alloc(&amp;proc-&gt;alloc);</span><br><span class="line"></span><br><span class="line">    trace_binder_ioctl(cmd, arg);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//进入休眠状态，直到中断唤醒</span></span><br><span class="line">    ret = wait_event_interruptible(binder_user_error_wait, binder_stop_on_user_error &lt; <span class="number">2</span>);</span><br><span class="line">    <span class="keyword">if</span> (ret)</span><br><span class="line">        <span class="keyword">goto</span> err_unlocked;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//获取binder线程信息，如果是第一次调用ioctl()，则会为该进程创建一个线程</span></span><br><span class="line">    thread = binder_get_thread(proc);</span><br><span class="line">    <span class="keyword">if</span> (thread == <span class="literal">NULL</span>) &#123;</span><br><span class="line">        ret = -ENOMEM;</span><br><span class="line">        <span class="keyword">goto</span> err;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">switch</span> (cmd) &#123;</span><br><span class="line">    <span class="keyword">case</span> BINDER_WRITE_READ:<span class="comment">//binder的读写操作，使用频率较高</span></span><br><span class="line">        ret = binder_ioctl_write_read(filp, cmd, arg, thread);</span><br><span class="line">        <span class="keyword">if</span> (ret)</span><br><span class="line">            <span class="keyword">goto</span> err;</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    <span class="keyword">case</span> BINDER_SET_MAX_THREADS: &#123;<span class="comment">//设置Binder线程最大个数</span></span><br><span class="line">        <span class="keyword">int</span> max_threads;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (copy_from_user(&amp;max_threads, ubuf,</span><br><span class="line">                   <span class="keyword">sizeof</span>(max_threads))) &#123;</span><br><span class="line">            ret = -EINVAL;</span><br><span class="line">            <span class="keyword">goto</span> err;</span><br><span class="line">        &#125;</span><br><span class="line">        binder_inner_proc_lock(proc);</span><br><span class="line">        proc-&gt;max_threads = max_threads;</span><br><span class="line">        binder_inner_proc_unlock(proc);</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">case</span> BINDER_SET_CONTEXT_MGR_EXT: &#123;<span class="comment">//设置Service Manager节点，带flag参数， servicemanager进程成为上下文管理者</span></span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">flat_binder_object</span> <span class="title">fbo</span>;</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (copy_from_user(&amp;fbo, ubuf, <span class="keyword">sizeof</span>(fbo))) &#123;</span><br><span class="line">            ret = -EINVAL;</span><br><span class="line">            <span class="keyword">goto</span> err;</span><br><span class="line">        &#125;</span><br><span class="line">        ret = binder_ioctl_set_ctx_mgr(filp, &amp;fbo);</span><br><span class="line">        <span class="keyword">if</span> (ret)</span><br><span class="line">            <span class="keyword">goto</span> err;</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">case</span> BINDER_SET_CONTEXT_MGR:<span class="comment">//设置Service Manager节点，不带flag参数， servicemanager进程成为上下文管理者</span></span><br><span class="line">        ret = binder_ioctl_set_ctx_mgr(filp, <span class="literal">NULL</span>);</span><br><span class="line">        <span class="keyword">if</span> (ret)</span><br><span class="line">            <span class="keyword">goto</span> err;</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    <span class="keyword">case</span> BINDER_THREAD_EXIT:<span class="comment">//当binder线程退出，释放binder线程</span></span><br><span class="line">        binder_debug(BINDER_DEBUG_THREADS, <span class="string">&quot;%d:%d exit\n&quot;</span>,</span><br><span class="line">                 proc-&gt;pid, thread-&gt;pid);</span><br><span class="line">        binder_thread_release(proc, thread);</span><br><span class="line">        thread = <span class="literal">NULL</span>;</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    <span class="keyword">case</span> BINDER_VERSION: &#123;<span class="comment">//获取Binder版本信息</span></span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">binder_version</span> __<span class="title">user</span> *<span class="title">ver</span> =</span> ubuf;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (size != <span class="keyword">sizeof</span>(struct binder_version)) &#123;</span><br><span class="line">            ret = -EINVAL;</span><br><span class="line">            <span class="keyword">goto</span> err;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (put_user(BINDER_CURRENT_PROTOCOL_VERSION,</span><br><span class="line">                 &amp;ver-&gt;protocol_version)) &#123;</span><br><span class="line">            ret = -EINVAL;</span><br><span class="line">            <span class="keyword">goto</span> err;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">case</span> BINDER_GET_NODE_INFO_FOR_REF: &#123;</span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">binder_node_info_for_ref</span> <span class="title">info</span>;</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (copy_from_user(&amp;info, ubuf, <span class="keyword">sizeof</span>(info))) &#123;</span><br><span class="line">            ret = -EFAULT;</span><br><span class="line">            <span class="keyword">goto</span> err;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        ret = binder_ioctl_get_node_info_for_ref(proc, &amp;info);</span><br><span class="line">        <span class="keyword">if</span> (ret &lt; <span class="number">0</span>)</span><br><span class="line">            <span class="keyword">goto</span> err;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (copy_to_user(ubuf, &amp;info, <span class="keyword">sizeof</span>(info))) &#123;</span><br><span class="line">            ret = -EFAULT;</span><br><span class="line">            <span class="keyword">goto</span> err;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">case</span> BINDER_GET_NODE_DEBUG_INFO: &#123;</span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">binder_node_debug_info</span> <span class="title">info</span>;</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (copy_from_user(&amp;info, ubuf, <span class="keyword">sizeof</span>(info))) &#123;</span><br><span class="line">            ret = -EFAULT;</span><br><span class="line">            <span class="keyword">goto</span> err;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        ret = binder_ioctl_get_node_debug_info(proc, &amp;info);</span><br><span class="line">        <span class="keyword">if</span> (ret &lt; <span class="number">0</span>)</span><br><span class="line">            <span class="keyword">goto</span> err;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (copy_to_user(ubuf, &amp;info, <span class="keyword">sizeof</span>(info))) &#123;</span><br><span class="line">            ret = -EFAULT;</span><br><span class="line">            <span class="keyword">goto</span> err;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">default</span>:</span><br><span class="line">        ret = -EINVAL;</span><br><span class="line">        <span class="keyword">goto</span> err;</span><br><span class="line">    &#125;</span><br><span class="line">    ret = <span class="number">0</span>;</span><br><span class="line">err:</span><br><span class="line">    <span class="keyword">if</span> (thread)</span><br><span class="line">        thread-&gt;looper_need_return = <span class="literal">false</span>;</span><br><span class="line">    wait_event_interruptible(binder_user_error_wait, binder_stop_on_user_error &lt; <span class="number">2</span>);</span><br><span class="line">    <span class="keyword">if</span> (ret &amp;&amp; ret != -ERESTARTSYS)</span><br><span class="line">        pr_info(<span class="string">&quot;%d:%d ioctl %x %lx returned %d\n&quot;</span>, proc-&gt;pid, current-&gt;pid, cmd, arg, ret);</span><br><span class="line">err_unlocked:</span><br><span class="line">    trace_binder_ioctl_done(ret);</span><br><span class="line">    <span class="keyword">return</span> ret;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">binder_ioctl_write_read</span><span class="params">(struct file *filp,</span></span></span><br><span class="line"><span class="params"><span class="function">                <span class="keyword">unsigned</span> <span class="keyword">int</span> cmd, <span class="keyword">unsigned</span> <span class="keyword">long</span> arg,</span></span></span><br><span class="line"><span class="params"><span class="function">                struct binder_thread *thread)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> ret = <span class="number">0</span>;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_proc</span> *<span class="title">proc</span> =</span> filp-&gt;private_data;</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> size = _IOC_SIZE(cmd);</span><br><span class="line">    <span class="keyword">void</span> __user *ubuf = (<span class="keyword">void</span> __user *)arg;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_write_read</span> <span class="title">bwr</span>;</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (size != <span class="keyword">sizeof</span>(struct binder_write_read)) &#123;</span><br><span class="line">        ret = -EINVAL;</span><br><span class="line">        <span class="keyword">goto</span> out;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (copy_from_user(&amp;bwr, ubuf, <span class="keyword">sizeof</span>(bwr))) &#123;</span><br><span class="line">        ret = -EFAULT;</span><br><span class="line">        <span class="keyword">goto</span> out;</span><br><span class="line">    &#125;</span><br><span class="line">    binder_debug(BINDER_DEBUG_READ_WRITE,</span><br><span class="line">             <span class="string">&quot;%d:%d write %lld at %016llx, read %lld at %016llx\n&quot;</span>,</span><br><span class="line">             proc-&gt;pid, thread-&gt;pid,</span><br><span class="line">             (u64)bwr.write_size, (u64)bwr.write_buffer,</span><br><span class="line">             (u64)bwr.read_size, (u64)bwr.read_buffer);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (bwr.write_size &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="comment">//write_size大于0，表示用户进程有数据发送到驱动，则调用binder_thread_write发送数据</span></span><br><span class="line">        ret = binder_thread_write(proc, thread,</span><br><span class="line">                      bwr.write_buffer,</span><br><span class="line">                      bwr.write_size,</span><br><span class="line">                      &amp;bwr.write_consumed);</span><br><span class="line">        trace_binder_write_done(ret);</span><br><span class="line">        <span class="keyword">if</span> (ret &lt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="comment">//binder_thread_write中有错误发生，则read_consumed设为0，表示kernel没有数据返回给进程</span></span><br><span class="line">            bwr.read_consumed = <span class="number">0</span>;</span><br><span class="line">            <span class="comment">//将bwr返回给用户态调用者，bwr在binder_thread_write中会被修改</span></span><br><span class="line">            <span class="keyword">if</span> (copy_to_user(ubuf, &amp;bwr, <span class="keyword">sizeof</span>(bwr)))</span><br><span class="line">                ret = -EFAULT;</span><br><span class="line">            <span class="keyword">goto</span> out;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (bwr.read_size &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="comment">//read_size大于0， 表示进程用户态地址空间希望有数据返回给它，则调用binder_thread_read进行处理</span></span><br><span class="line">        ret = binder_thread_read(proc, thread, bwr.read_buffer,</span><br><span class="line">                     bwr.read_size,</span><br><span class="line">                     &amp;bwr.read_consumed,</span><br><span class="line">                     filp-&gt;f_flags &amp; O_NONBLOCK);</span><br><span class="line">        trace_binder_read_done(ret);</span><br><span class="line">        binder_inner_proc_lock(proc);</span><br><span class="line">        <span class="comment">//读取完后，如果proc-&gt;todo链表不为空，则唤醒在proc-&gt;wait等待队列上的进程</span></span><br><span class="line">        <span class="keyword">if</span> (!binder_worklist_empty_ilocked(&amp;proc-&gt;todo))</span><br><span class="line">            binder_wakeup_proc_ilocked(proc);</span><br><span class="line">        binder_inner_proc_unlock(proc);</span><br><span class="line">        <span class="keyword">if</span> (ret &lt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="comment">//如果binder_thread_read返回小于0，可能处理一半就中断了，需要将bwr拷贝回进程的用户态地址</span></span><br><span class="line">            <span class="keyword">if</span> (copy_to_user(ubuf, &amp;bwr, <span class="keyword">sizeof</span>(bwr)))</span><br><span class="line">                ret = -EFAULT;</span><br><span class="line">            <span class="keyword">goto</span> out;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    binder_debug(BINDER_DEBUG_READ_WRITE,</span><br><span class="line">             <span class="string">&quot;%d:%d wrote %lld of %lld, read return %lld of %lld\n&quot;</span>,</span><br><span class="line">             proc-&gt;pid, thread-&gt;pid,</span><br><span class="line">             (u64)bwr.write_consumed, (u64)bwr.write_size,</span><br><span class="line">             (u64)bwr.read_consumed, (u64)bwr.read_size);</span><br><span class="line">    <span class="comment">//处理成功的情况，也需要将bwr拷贝回进程的用户态地址空间</span></span><br><span class="line">    <span class="keyword">if</span> (copy_to_user(ubuf, &amp;bwr, <span class="keyword">sizeof</span>(bwr))) &#123;</span><br><span class="line">        ret = -EFAULT;</span><br><span class="line">        <span class="keyword">goto</span> out;</span><br><span class="line">    &#125;</span><br><span class="line">out:</span><br><span class="line">    <span class="keyword">return</span> ret;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>binder_ioctl函数对应了ioctl系统调用的处理。这个函数的逻辑比较简单，就是根据ioctl的命令来确定进一步处理的逻辑，具体如下:</p>
<ul>
<li>BINDER_WRITE_READ：<ul>
<li>bwr.write_size &gt; 0：调用binder_thread_write</li>
<li>bwr.read_size &gt; 0：调用binder_thread_read</li>
</ul>
</li>
<li>BINDER_SET_MAX_THREADS：设置进程的max_threads，即进程支持的最大线程数。</li>
<li>BINDER_SET_CONTEXT_MGR：设置当前进程为ServiceManager，后续文章会详解。</li>
<li>BINDER_THREAD_EXIT：调用binder_free_thread，释放binder_thread。</li>
<li>BINDER_VERSION：返回当前的Binder版本号。</li>
</ul>
<p>​</p>
<p>对于BINDER_WRITE_READ分支处理里，结构还是很清晰的，就是把用户态传递过来的binder_write_read数据结构从用户态拷贝到内核态，然后再根据这一数据结构里的read_size和write_size是否大于0来决定是否进行后续操作。由于这两个值都可能同时存在，于是一个ioctl周期里，有可能同时进行读写。之后的代码则可以进入到binder_thread_write()或是binder_thread_read()，write和read都是相对于用户态来说的，thread也是，就是如果调度到合适的IPCThreadState对象，以线程方式处理用户态的写请求或是读请求。<br>​</p>
<h2 id="binder-thread-write"><a href="#binder-thread-write" class="headerlink" title="binder_thread_write"></a>binder_thread_write</h2><p>binder_thread_write()的实现相对来说就比较简单，只是通过解析binder_ioctl()得到的BC_*命令，然后再根据命令作出处理。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">binder_thread_write</span><span class="params">(struct binder_proc *proc,</span></span></span><br><span class="line"><span class="params"><span class="function">            struct binder_thread *thread,</span></span></span><br><span class="line"><span class="params"><span class="function">            <span class="keyword">binder_uintptr_t</span> binder_buffer, <span class="keyword">size_t</span> size,</span></span></span><br><span class="line"><span class="params"><span class="function">            <span class="keyword">binder_size_t</span> *consumed)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">uint32_t</span> cmd;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_context</span> *<span class="title">context</span> =</span> proc-&gt;context;<span class="comment">//原来Service进程的proc的context</span></span><br><span class="line">    <span class="keyword">void</span> __user *buffer = (<span class="keyword">void</span> __user *)(<span class="keyword">uintptr_t</span>)binder_buffer;</span><br><span class="line">    <span class="keyword">void</span> __user *ptr = buffer + *consumed;</span><br><span class="line">    <span class="keyword">void</span> __user *end = buffer + size;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (ptr &lt; end &amp;&amp; thread-&gt;return_error.cmd == BR_OK) &#123;</span><br><span class="line">        <span class="keyword">int</span> ret;</span><br><span class="line"></span><br><span class="line">        <span class="comment">//拷贝用户空间的cmd命令，addService和getService时，为 BC_TRANSACTION</span></span><br><span class="line">        <span class="keyword">if</span> (get_user(cmd, (<span class="keyword">uint32_t</span> __user *)ptr))</span><br><span class="line">            <span class="keyword">return</span> -EFAULT;</span><br><span class="line">        ptr += <span class="keyword">sizeof</span>(<span class="keyword">uint32_t</span>);</span><br><span class="line">        trace_binder_command(cmd);</span><br><span class="line">        <span class="keyword">if</span> (_IOC_NR(cmd) &lt; ARRAY_SIZE(binder_stats.bc)) &#123;</span><br><span class="line">            atomic_inc(&amp;binder_stats.bc[_IOC_NR(cmd)]);</span><br><span class="line">            atomic_inc(&amp;proc-&gt;stats.bc[_IOC_NR(cmd)]);</span><br><span class="line">            atomic_inc(&amp;thread-&gt;stats.bc[_IOC_NR(cmd)]);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">switch</span> (cmd) &#123;</span><br><span class="line">        </span><br><span class="line">        ...</span><br><span class="line"></span><br><span class="line">        <span class="keyword">case</span> BC_TRANSACTION:</span><br><span class="line">        <span class="keyword">case</span> BC_REPLY: &#123;</span><br><span class="line">            <span class="class"><span class="keyword">struct</span> <span class="title">binder_transaction_data</span> <span class="title">tr</span>;</span></span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (copy_from_user(&amp;tr, ptr, <span class="keyword">sizeof</span>(tr)))</span><br><span class="line">                <span class="keyword">return</span> -EFAULT;</span><br><span class="line">            ptr += <span class="keyword">sizeof</span>(tr);</span><br><span class="line">            binder_transaction(proc, thread, &amp;tr,</span><br><span class="line">                       cmd == BC_REPLY, <span class="number">0</span>);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        ...</span><br><span class="line"></span><br><span class="line">        <span class="keyword">default</span>:</span><br><span class="line">            pr_err(<span class="string">&quot;%d:%d unknown command %d\n&quot;</span>,</span><br><span class="line">                   proc-&gt;pid, thread-&gt;pid, cmd);</span><br><span class="line">            <span class="keyword">return</span> -EINVAL;</span><br><span class="line">        &#125;</span><br><span class="line">        *consumed = ptr - buffer;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>BC_系列命令的定义是在枚举型binder_driver_command_protocol中，对于其他的BC_系列命令，处理只是直接完成即可，但对于附带数据的BC_TRANASACTION和BC_REPLY，因为有可能涉及到复杂数据结构的解析以及数据拷贝，于是会通过binder_transaction()来进行传输。</p>
<h2 id="binder-transaction"><a href="#binder-transaction" class="headerlink" title="binder_transaction"></a>binder_transaction</h2><p>从基本操作上来看，整个binder_transaction()操作，就是把需要发送的数据直接找到目的，然后再归类整理好，等待接收者在它的读取周期里取走数据。也就是把发送方提供的binder_transaction_data转换成binder driver专用的binder_transaction对象，并保存到接收者进程/线程的todo队列中。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">binder_transaction</span><span class="params">(struct binder_proc *proc,</span></span></span><br><span class="line"><span class="params"><span class="function">                   struct binder_thread *thread,</span></span></span><br><span class="line"><span class="params"><span class="function">                   struct binder_transaction_data *tr, <span class="keyword">int</span> reply,</span></span></span><br><span class="line"><span class="params"><span class="function">                   <span class="keyword">binder_size_t</span> extra_buffers_size)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> ret;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_transaction</span> *<span class="title">t</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_work</span> *<span class="title">tcomplete</span>;</span></span><br><span class="line">    <span class="keyword">binder_size_t</span> buffer_offset = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">binder_size_t</span> off_start_offset, off_end_offset;</span><br><span class="line">    <span class="keyword">binder_size_t</span> off_min;</span><br><span class="line">    <span class="keyword">binder_size_t</span> sg_buf_offset, sg_buf_end_offset;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_proc</span> *<span class="title">target_proc</span> =</span> <span class="literal">NULL</span>;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_thread</span> *<span class="title">target_thread</span> =</span> <span class="literal">NULL</span>;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_node</span> *<span class="title">target_node</span> =</span> <span class="literal">NULL</span>;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_transaction</span> *<span class="title">in_reply_to</span> =</span> <span class="literal">NULL</span>;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_transaction_log_entry</span> *<span class="title">e</span>;</span></span><br><span class="line">    <span class="keyword">uint32_t</span> return_error = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">uint32_t</span> return_error_param = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">uint32_t</span> return_error_line = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">binder_size_t</span> last_fixup_obj_off = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">binder_size_t</span> last_fixup_min_off = <span class="number">0</span>;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">binder_context</span> *<span class="title">context</span> =</span> proc-&gt;context;</span><br><span class="line">    <span class="keyword">int</span> t_debug_id = atomic_inc_return(&amp;binder_last_id);</span><br><span class="line">    <span class="keyword">char</span> *secctx = <span class="literal">NULL</span>;</span><br><span class="line">    u32 secctx_sz = <span class="number">0</span>;</span><br><span class="line">   ...</span><br><span class="line">    <span class="comment">//addService第一次进来时，reply为空</span></span><br><span class="line">    <span class="keyword">if</span> (reply) &#123;</span><br><span class="line">        ...</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="comment">//addService和getService时，传入的handle=0，表明想访问ServiceManager</span></span><br><span class="line">        <span class="keyword">if</span> (tr-&gt;target.handle) &#123;</span><br><span class="line">               <span class="comment">//先从tr-&gt;target.handle句柄值，找到对应的binder_ref节点，及binder_node节点</span></span><br><span class="line">            struct binder_ref *ref;</span><br><span class="line">            binder_proc_lock(proc);</span><br><span class="line">            ref = binder_get_ref_olocked(proc, tr-&gt;target.handle,</span><br><span class="line">                             <span class="literal">true</span>);</span><br><span class="line">            <span class="keyword">if</span> (ref) &#123;</span><br><span class="line">                target_node = binder_get_node_refs_for_txn(</span><br><span class="line">                        ref-&gt;node, &amp;target_proc,</span><br><span class="line">                        &amp;return_error);</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line"> </span><br><span class="line">                return_error = BR_FAILED_REPLY;</span><br><span class="line">            &#125;</span><br><span class="line">            binder_proc_unlock(proc);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            mutex_lock(&amp;context-&gt;context_mgr_node_lock); <span class="comment">//互斥锁</span></span><br><span class="line">            <span class="comment">// handle=0则找到servicemanager实体</span></span><br><span class="line">            target_node = context-&gt;binder_context_mgr_node;</span><br><span class="line">            <span class="keyword">if</span> (target_node)</span><br><span class="line">                <span class="comment">//获取ServiceManager进程的target_proc</span></span><br><span class="line">                target_node = binder_get_node_refs_for_txn(</span><br><span class="line">                        target_node, &amp;target_proc,</span><br><span class="line">                        &amp;return_error);</span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">                return_error = BR_DEAD_REPLY;</span><br><span class="line">            mutex_unlock(&amp;context-&gt;context_mgr_node_lock); <span class="comment">//释放锁</span></span><br><span class="line">            <span class="keyword">if</span> (target_node &amp;&amp; target_proc == proc) &#123;</span><br><span class="line">                binder_user_error(<span class="string">&quot;%d:%d got transaction to context manager from process owning it\n&quot;</span>,</span><br><span class="line">                          proc-&gt;pid, thread-&gt;pid);</span><br><span class="line">                return_error = BR_FAILED_REPLY;</span><br><span class="line">                return_error_param = -EINVAL;</span><br><span class="line">                return_error_line = __LINE__;</span><br><span class="line">                <span class="keyword">goto</span> err_invalid_target_handle;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">            ...</span><br><span class="line">        <span class="comment">//检查Client进程是否有权限向Server进程发送请求</span></span><br><span class="line">        <span class="keyword">if</span> (security_binder_transaction(proc-&gt;tsk,</span><br><span class="line">                        target_proc-&gt;tsk) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">            return_error = BR_FAILED_REPLY;</span><br><span class="line">            return_error_param = -EPERM;</span><br><span class="line">            return_error_line = __LINE__;</span><br><span class="line">            <span class="keyword">goto</span> err_invalid_target_handle;</span><br><span class="line">        &#125;</span><br><span class="line">        binder_inner_proc_lock(proc);</span><br><span class="line">        <span class="comment">//如果flag不是oneway,并且现场的transaction_stack存在内容</span></span><br><span class="line">        <span class="keyword">if</span> (!(tr-&gt;flags &amp; TF_ONE_WAY) &amp;&amp; thread-&gt;transaction_stack) &#123;</span><br><span class="line">            <span class="class"><span class="keyword">struct</span> <span class="title">binder_transaction</span> *<span class="title">tmp</span>;</span></span><br><span class="line">            tmp = thread-&gt;transaction_stack;</span><br><span class="line">            <span class="keyword">if</span> (tmp-&gt;to_thread != thread) &#123;</span><br><span class="line">                spin_lock(&amp;tmp-&gt;lock);</span><br><span class="line">                binder_user_error(<span class="string">&quot;%d:%d got new transaction with bad transaction stack, transaction %d has target %d:%d\n&quot;</span>,</span><br><span class="line">                    proc-&gt;pid, thread-&gt;pid, tmp-&gt;debug_id,</span><br><span class="line">                    tmp-&gt;to_proc ? tmp-&gt;to_proc-&gt;pid : <span class="number">0</span>,</span><br><span class="line">                    tmp-&gt;to_thread ?</span><br><span class="line">                    tmp-&gt;to_thread-&gt;pid : <span class="number">0</span>);</span><br><span class="line">                spin_unlock(&amp;tmp-&gt;lock);</span><br><span class="line">                binder_inner_proc_unlock(proc);</span><br><span class="line">                return_error = BR_FAILED_REPLY;</span><br><span class="line">                return_error_param = -EPROTO;</span><br><span class="line">                return_error_line = __LINE__;</span><br><span class="line">                <span class="keyword">goto</span> err_bad_call_stack;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">while</span> (tmp) &#123;</span><br><span class="line">                <span class="class"><span class="keyword">struct</span> <span class="title">binder_thread</span> *<span class="title">from</span>;</span></span><br><span class="line"> </span><br><span class="line">                spin_lock(&amp;tmp-&gt;lock);</span><br><span class="line">                from = tmp-&gt;from;</span><br><span class="line">                <span class="keyword">if</span> (from &amp;&amp; from-&gt;proc == target_proc) &#123;</span><br><span class="line">                    atomic_inc(&amp;from-&gt;tmp_ref);</span><br><span class="line">                    target_thread = from;</span><br><span class="line">                    spin_unlock(&amp;tmp-&gt;lock);</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line">                spin_unlock(&amp;tmp-&gt;lock);</span><br><span class="line">                tmp = tmp-&gt;from_parent;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        binder_inner_proc_unlock(proc);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (target_thread)</span><br><span class="line">        e-&gt;to_thread = target_thread-&gt;pid;</span><br><span class="line">    e-&gt;to_proc = target_proc-&gt;pid;</span><br><span class="line"> </span><br><span class="line">    <span class="comment">// 创建binder_transaction节点</span></span><br><span class="line">    t = kzalloc(<span class="keyword">sizeof</span>(*t), GFP_KERNEL); </span><br><span class="line">    <span class="keyword">if</span> (t == <span class="literal">NULL</span>) &#123;</span><br><span class="line">        return_error = BR_FAILED_REPLY;</span><br><span class="line">        return_error_param = -ENOMEM;</span><br><span class="line">        return_error_line = __LINE__;</span><br><span class="line">        <span class="keyword">goto</span> err_alloc_t_failed;</span><br><span class="line">    &#125;</span><br><span class="line">    binder_stats_created(BINDER_STAT_TRANSACTION);</span><br><span class="line">    spin_lock_init(&amp;t-&gt;lock);</span><br><span class="line">    <span class="comment">//创建一个binder_work节点</span></span><br><span class="line">    tcomplete = kzalloc(<span class="keyword">sizeof</span>(*tcomplete), GFP_KERNEL);</span><br><span class="line">        ...</span><br><span class="line">    binder_stats_created(BINDER_STAT_TRANSACTION_COMPLETE);</span><br><span class="line"> </span><br><span class="line">    t-&gt;debug_id = t_debug_id;</span><br><span class="line">    ...</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">if</span> (!reply &amp;&amp; !(tr-&gt;flags &amp; TF_ONE_WAY))</span><br><span class="line">        t-&gt;from = thread;   <span class="comment">// 返回proc的当前线程</span></span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        t-&gt;from = <span class="literal">NULL</span>;</span><br><span class="line">    t-&gt;sender_euid = task_euid(proc-&gt;tsk);  <span class="comment">// 源线程用户id</span></span><br><span class="line">    t-&gt;to_proc = target_proc;               <span class="comment">// 负责处理该事务的进程--ServiceManager</span></span><br><span class="line">    t-&gt;to_thread = target_thread;           <span class="comment">// 负责处理该事务的线程</span></span><br><span class="line">        <span class="comment">// 将binder_transaction_data的code、flags域记入binder_transaction节点</span></span><br><span class="line">    t-&gt;code = tr-&gt;code;                     <span class="comment">// ADD_SERVICE_TRANSACTION</span></span><br><span class="line">    t-&gt;flags = tr-&gt;flags;                   <span class="comment">// TF_ACCEPT_FDS</span></span><br><span class="line">    <span class="comment">// 源线程优先级设置</span></span><br><span class="line">    <span class="keyword">if</span> (!(t-&gt;flags &amp; TF_ONE_WAY) &amp;&amp;</span><br><span class="line">        binder_supported_policy(current-&gt;policy)) &#123;</span><br><span class="line">        <span class="comment">/* Inherit supported policies for synchronous transactions */</span></span><br><span class="line">        t-&gt;priority.sched_policy = current-&gt;policy;</span><br><span class="line">        t-&gt;priority.prio = current-&gt;normal_prio;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="comment">/* Otherwise, fall back to the default priority */</span></span><br><span class="line">        t-&gt;priority = target_proc-&gt;default_priority;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">if</span> (target_node &amp;&amp; target_node-&gt;txn_security_ctx) &#123;</span><br><span class="line">        u32 secid;</span><br><span class="line">        <span class="keyword">size_t</span> added_size;</span><br><span class="line"> </span><br><span class="line">        security_task_getsecid(proc-&gt;tsk, &amp;secid);</span><br><span class="line">        ret = security_secid_to_secctx(secid, &amp;secctx, &amp;secctx_sz);</span><br><span class="line">        <span class="keyword">if</span> (ret) &#123;</span><br><span class="line">            return_error = BR_FAILED_REPLY;</span><br><span class="line">            return_error_param = ret;</span><br><span class="line">            return_error_line = __LINE__;</span><br><span class="line">            <span class="keyword">goto</span> err_get_secctx_failed;</span><br><span class="line">        &#125;</span><br><span class="line">        added_size = ALIGN(secctx_sz, <span class="keyword">sizeof</span>(u64));</span><br><span class="line">        extra_buffers_size += added_size;</span><br><span class="line">        <span class="keyword">if</span> (extra_buffers_size &lt; added_size) &#123;</span><br><span class="line">            <span class="comment">/* integer overflow of extra_buffers_size */</span></span><br><span class="line">            return_error = BR_FAILED_REPLY;</span><br><span class="line">            return_error_param = EINVAL;</span><br><span class="line">            return_error_line = __LINE__;</span><br><span class="line">            <span class="keyword">goto</span> err_bad_extra_size;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="comment">//分配一块buffer用于保存mOut中的data和mObjects 中的offset数据</span></span><br><span class="line">    t-&gt;buffer = binder_alloc_new_buf(&amp;target_proc-&gt;alloc, tr-&gt;data_size,</span><br><span class="line">        tr-&gt;offsets_size, extra_buffers_size,</span><br><span class="line">        !reply &amp;&amp; (t-&gt;flags &amp; TF_ONE_WAY));</span><br><span class="line">        ...</span><br><span class="line">    <span class="keyword">if</span> (secctx) &#123;</span><br><span class="line">        <span class="keyword">size_t</span> buf_offset = ALIGN(tr-&gt;data_size, <span class="keyword">sizeof</span>(<span class="keyword">void</span> *)) +</span><br><span class="line">                    ALIGN(tr-&gt;offsets_size, <span class="keyword">sizeof</span>(<span class="keyword">void</span> *)) +</span><br><span class="line">                    ALIGN(extra_buffers_size, <span class="keyword">sizeof</span>(<span class="keyword">void</span> *)) -</span><br><span class="line">                    ALIGN(secctx_sz, <span class="keyword">sizeof</span>(u64));</span><br><span class="line"> </span><br><span class="line">        t-&gt;security_ctx = (<span class="keyword">uintptr_t</span>)t-&gt;buffer-&gt;user_data + buf_offset;</span><br><span class="line">        binder_alloc_copy_to_buffer(&amp;target_proc-&gt;alloc,</span><br><span class="line">                        t-&gt;buffer, buf_offset,</span><br><span class="line">                        secctx, secctx_sz);</span><br><span class="line">        security_release_secctx(secctx, secctx_sz);</span><br><span class="line">        secctx = <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    t-&gt;buffer-&gt;debug_id = t-&gt;debug_id;</span><br><span class="line">    t-&gt;buffer-&gt;transaction = t;             <span class="comment">// 缓冲区正交给哪个事务使用</span></span><br><span class="line">    t-&gt;buffer-&gt;target_node = target_node;   <span class="comment">// 缓冲区正交给哪个Binder实体对象使用</span></span><br><span class="line">    trace_binder_transaction_alloc_buf(t-&gt;buffer);</span><br><span class="line"> </span><br><span class="line">    <span class="comment">//拷贝用户空间的binder_transaction_data中ptr.buffer到内核，存入t-&gt;buffer</span></span><br><span class="line">    <span class="comment">//从mOut中将data拷贝到buffer中</span></span><br><span class="line">    <span class="keyword">if</span> (binder_alloc_copy_user_to_buffer(</span><br><span class="line">                &amp;target_proc-&gt;alloc,</span><br><span class="line">                t-&gt;buffer, <span class="number">0</span>,</span><br><span class="line">                (<span class="keyword">const</span> <span class="keyword">void</span> __user *)</span><br><span class="line">                    (<span class="keyword">uintptr_t</span>)tr-&gt;data.ptr.buffer,</span><br><span class="line">                tr-&gt;data_size)) &#123;</span><br><span class="line">               ...</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//拷贝用户空间的binder_transaction_data中ptr.offsets到内核,存入t-&gt;buffer</span></span><br><span class="line">    <span class="comment">//从mObjects 中将offset数据拷贝到buffer中data之后</span></span><br><span class="line">    <span class="keyword">if</span> (binder_alloc_copy_user_to_buffer(</span><br><span class="line">                &amp;target_proc-&gt;alloc,</span><br><span class="line">                t-&gt;buffer,</span><br><span class="line">                ALIGN(tr-&gt;data_size, <span class="keyword">sizeof</span>(<span class="keyword">void</span> *)),</span><br><span class="line">                (<span class="keyword">const</span> <span class="keyword">void</span> __user *)</span><br><span class="line">                    (<span class="keyword">uintptr_t</span>)tr-&gt;data.ptr.offsets,</span><br><span class="line">                tr-&gt;offsets_size)) &#123;</span><br><span class="line">               ...</span><br><span class="line">    &#125;</span><br><span class="line">    ...</span><br><span class="line">    off_start_offset = ALIGN(tr-&gt;data_size, <span class="keyword">sizeof</span>(<span class="keyword">void</span> *));</span><br><span class="line">    buffer_offset = off_start_offset;</span><br><span class="line">    off_end_offset = off_start_offset + tr-&gt;offsets_size;</span><br><span class="line">    sg_buf_offset = ALIGN(off_end_offset, <span class="keyword">sizeof</span>(<span class="keyword">void</span> *));</span><br><span class="line">    sg_buf_end_offset = sg_buf_offset + extra_buffers_size -</span><br><span class="line">        ALIGN(secctx_sz, <span class="keyword">sizeof</span>(u64));</span><br><span class="line">    off_min = <span class="number">0</span>;</span><br><span class="line">    </span><br><span class="line"> </span><br><span class="line">    <span class="comment">//从data中解析出所有的binder实体并为其创建binder_node和binder_ref</span></span><br><span class="line">    <span class="keyword">for</span> (buffer_offset = off_start_offset; buffer_offset &lt; off_end_offset;</span><br><span class="line">         buffer_offset += <span class="keyword">sizeof</span>(<span class="keyword">binder_size_t</span>)) &#123;</span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">binder_object_header</span> *<span class="title">hdr</span>;</span></span><br><span class="line">        <span class="keyword">size_t</span> object_size;</span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">binder_object</span> <span class="title">object</span>;</span></span><br><span class="line">        <span class="keyword">binder_size_t</span> object_offset;</span><br><span class="line"> </span><br><span class="line">        <span class="comment">//得到object_offset的大小</span></span><br><span class="line">        binder_alloc_copy_from_buffer(&amp;target_proc-&gt;alloc,</span><br><span class="line">                          &amp;object_offset,</span><br><span class="line">                          t-&gt;buffer,</span><br><span class="line">                          buffer_offset,</span><br><span class="line">                          <span class="keyword">sizeof</span>(object_offset));</span><br><span class="line">        <span class="comment">//从t-&gt;buffer中解析object，object_size=sizeof(flat_binder_object)</span></span><br><span class="line">        object_size = binder_get_object(target_proc, t-&gt;buffer,</span><br><span class="line">                        object_offset, &amp;object);</span><br><span class="line">        <span class="keyword">if</span> (object_size == <span class="number">0</span> || object_offset &lt; off_min) &#123;</span><br><span class="line">            ...</span><br><span class="line">            return_error = BR_FAILED_REPLY;</span><br><span class="line">            return_error_param = -EINVAL;</span><br><span class="line">            return_error_line = __LINE__;</span><br><span class="line">            <span class="keyword">goto</span> err_bad_offset;</span><br><span class="line">        &#125;</span><br><span class="line"> </span><br><span class="line">        hdr = &amp;object.hdr;</span><br><span class="line">        off_min = object_offset + object_size;</span><br><span class="line">        <span class="comment">//根据addService时，writeStrongBinder()写的是实体Binder--BINDER_TYPE_BINDER，还是代理Binder--BINDER_TYPE_HANDLE类型来决定</span></span><br><span class="line">        <span class="keyword">switch</span> (hdr-&gt;type) &#123;</span><br><span class="line">        <span class="keyword">case</span> BINDER_TYPE_BINDER:</span><br><span class="line">        <span class="keyword">case</span> BINDER_TYPE_WEAK_BINDER: &#123;</span><br><span class="line">            <span class="comment">// 如果是binder实体</span></span><br><span class="line">            <span class="class"><span class="keyword">struct</span> <span class="title">flat_binder_object</span> *<span class="title">fp</span>;</span></span><br><span class="line"> </span><br><span class="line">            fp = to_flat_binder_object(hdr);</span><br><span class="line">            <span class="comment">//在目标进程的binder_proc中创建对应的binder_ref红黑树节点</span></span><br><span class="line">            <span class="comment">//BINDER_TYPE_BINDER 转成BINDER_TYPE_HANDLE</span></span><br><span class="line">            <span class="comment">//为binder实体创建binder_node并添加到红黑树proc-&gt;nodes.rb_node中</span></span><br><span class="line">            ret = binder_translate_binder(fp, t, thread);</span><br><span class="line">            <span class="keyword">if</span> (ret &lt; <span class="number">0</span>) &#123;</span><br><span class="line">                return_error = BR_FAILED_REPLY;</span><br><span class="line">                return_error_param = ret;</span><br><span class="line">                return_error_line = __LINE__;</span><br><span class="line">                <span class="keyword">goto</span> err_translate_failed;</span><br><span class="line">            &#125;</span><br><span class="line">            binder_alloc_copy_to_buffer(&amp;target_proc-&gt;alloc,</span><br><span class="line">                            t-&gt;buffer, object_offset,</span><br><span class="line">                            fp, <span class="keyword">sizeof</span>(*fp));</span><br><span class="line">        &#125; <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> BINDER_TYPE_HANDLE:</span><br><span class="line">        <span class="keyword">case</span> BINDER_TYPE_WEAK_HANDLE: &#123;</span><br><span class="line">            <span class="comment">// 如果是 Binder引用，拿到Binder的handle</span></span><br><span class="line">            <span class="class"><span class="keyword">struct</span> <span class="title">flat_binder_object</span> *<span class="title">fp</span>;</span></span><br><span class="line"> </span><br><span class="line">            fp = to_flat_binder_object(hdr);</span><br><span class="line">             <span class="comment">//在远端进程的binder_proc中找到一个binder_ref红黑树节点</span></span><br><span class="line">            <span class="comment">//解析出引用的flat_binder_object,并在请求proc下创建服务的引用</span></span><br><span class="line">            ret = binder_translate_handle(fp, t, thread);</span><br><span class="line">            <span class="keyword">if</span> (ret &lt; <span class="number">0</span>) &#123;</span><br><span class="line">                return_error = BR_FAILED_REPLY;</span><br><span class="line">                return_error_param = ret;</span><br><span class="line">                return_error_line = __LINE__;</span><br><span class="line">                <span class="keyword">goto</span> err_translate_failed;</span><br><span class="line">            &#125;</span><br><span class="line">            binder_alloc_copy_to_buffer(&amp;target_proc-&gt;alloc,</span><br><span class="line">                            t-&gt;buffer, object_offset,</span><br><span class="line">                            fp, <span class="keyword">sizeof</span>(*fp));</span><br><span class="line">        &#125; <span class="keyword">break</span>;</span><br><span class="line">            ...</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//transaction事务处理完成</span></span><br><span class="line">    tcomplete-&gt;type = BINDER_WORK_TRANSACTION_COMPLETE;</span><br><span class="line">    t-&gt;work.type = BINDER_WORK_TRANSACTION; <span class="comment">//设置事务类型</span></span><br><span class="line"> </span><br><span class="line">    <span class="keyword">if</span> (reply) &#123;</span><br><span class="line">    ... </span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (!(t-&gt;flags &amp; TF_ONE_WAY)) &#123;</span><br><span class="line">        BUG_ON(t-&gt;buffer-&gt;async_transaction != <span class="number">0</span>);</span><br><span class="line">        binder_inner_proc_lock(proc);</span><br><span class="line">        <span class="comment">//延迟 TRANSACTION_COMPLETE，因此我们不会立即返回到用户空间；</span></span><br><span class="line">        <span class="comment">//这允许目标进程立即开始处理此事务，从而减少延迟。</span></span><br><span class="line">        <span class="comment">//然后，当目标回复（或出现错误）时，我们将返回TRANSACTION_COMPLETE。</span></span><br><span class="line">        <span class="comment">//把binder_transaction节点插入target_list（即目标todo队列）</span></span><br><span class="line">        binder_enqueue_deferred_thread_work_ilocked(thread, tcomplete);</span><br><span class="line">        t-&gt;need_reply = <span class="number">1</span>;  <span class="comment">//是双向的所以需要回复</span></span><br><span class="line">        t-&gt;from_parent = thread-&gt;transaction_stack;</span><br><span class="line">        thread-&gt;transaction_stack = t;  <span class="comment">//将传递数据保存在请求线程的中,以后后续缓存释放等</span></span><br><span class="line">        binder_inner_proc_unlock(proc);</span><br><span class="line">        <span class="comment">//将数据放到目标进程的todo list中去,唤醒目标进程处理请求,这里唤醒ServiceManager进行处理</span></span><br><span class="line">        <span class="keyword">if</span> (!binder_proc_transaction(t, target_proc, target_thread)) &#123;</span><br><span class="line">            binder_inner_proc_lock(proc);</span><br><span class="line">            binder_pop_transaction_ilocked(thread, t);</span><br><span class="line">            binder_inner_proc_unlock(proc);</span><br><span class="line">            <span class="keyword">goto</span> err_dead_proc_or_thread;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        ...</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (target_thread)</span><br><span class="line">        binder_thread_dec_tmpref(target_thread);</span><br><span class="line">    binder_proc_dec_tmpref(target_proc);</span><br><span class="line">    <span class="keyword">if</span> (target_node)</span><br><span class="line">        binder_dec_node_tmpref(target_node);</span><br><span class="line">    smp_wmb();</span><br><span class="line">    WRITE_ONCE(e-&gt;debug_id_done, t_debug_id);</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line"> </span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>同样是BC_命令，BC_TRANSACTION与BC_REPLY是不一样的，对于BC_REPLY，一般会是基于某个BR_TRANSACTION处理之后的结果，前面处理的transaction也会被保存到binder_thread的transaction_stack里。于是，对于BC_REPLY的处理，就会是通过transaction_stack，再基于它处理BC_REPLY请求。首先会设置进程优先级，返回值传输时的优先级总会由发送时来决定，于是这里会重设优先级。然后把transaction_stack弹出一级（之所有叫stack，会记录transaction的栈式关系），相当于函数调用上已经退出了，然后回退一次栈。最后会设置好target_thread和target_proc，为后续操作作好准备。</li>
<li>对于BC_TRANSACTION，不是基于某个已有的transaction，相反是要发起一个新的，会决定后续BC_REPLY操作的transaction。</li>
<li>如果有用户态传过来的binder_transaction包含target.handle，这说明是通过Proxy找到明确的操作目标，则会尝试在Binder驱动里查找该target.handle是否已经存在，找到则会直接把它作为操作目标。当然这时可能找不到，因为Binder必然是先有Service，然后才由Proxy引用，如果找不到这时就可以认为是非法访问，出错退出。如果用户态进程没有指定target.handle，这只能说明是通过servicemanager来查找Service的请求，会直接把目标设置为binder_context_mgr_node。</li>
<li>如果传输不是单向的，并且binder_thread的transaction_stack不为空，说明此时正在处理某个或者多个BC_TRANSACTION过程中。我们也知道栈式结构里栈顶永远会是最新的上下文现场，这不符合传输的实际需求。于是，这里会通过一次循环找到transaction_stack的目标符合的target_thread。这也有助于线程池操作，如果存在于transaction_stack中，则说明该transaction肯定处于某个处理过程中，运行这个处理过程的Binder线程必然无法被用于接收这一transaction，而主Binder线程按照同样的原则（命令交给处于栈底的Binder线程来读，交互则发生在栈顶的Binder线程里），总会是最“闲”的。</li>
<li>跟3的处理原理类似，如果找到目标binder_thread，则说明该线程是属于某个已经被访问过的Service的，直接更新target_list和target_wait信息。如果找不到，则根据目标的binder_proc来不更新这两个列表信息，此时则发生在ServiceManager访问或是Service的Binder线程没有就绪的情况，可以存起来等待就绪后再执行。</li>
<li>分配传输时，内核态需要使用的binder_transaction备份。所有经由内核而在两个进程间进行消息传输的数据，出于进程独立性考虑，必须两个用户进程空间内各一份，然后在内核态有一份，处理传输出错恢复或是重试。而另一个t_complete的申请，只是建立一个操作成功的链表而已。</li>
<li>根据用户态的当前信息，来更新内核的binder_transaction备份，并把查找到的target_thread和target_proc填入这一binder_transaction。</li>
<li>分配内核态binder_transaction所需要使用的存储空间，使用内部的binder_buffer来分配，这一空间源自于进程空间的mmap()得到的用户空间页面，这些页面会映射到内核态，所以也不会造成内核分配上的开销。</li>
<li>处于一个binder_transaction内的数据，都会保存在data.ptr.buffer的用户内存里，并通过用户内存里的另一段data.ptr.offsets来索引buffer里各个对象的拆分情况。于是这里会把这两部分内容通过copy_from_user()拷贝到内核态。</li>
<li>通过一次循环处理data.ptr.buffer里所包含的每个对象，当然，在传输到binder时，这些对象已经是被“压扁”了的对象，flat_binder_object。</li>
<li>如果对象的类型是Binder对象（WEAK_BINDER，有可能被自动回收的Binder对象），则尝试查找系统使用过的binder_node是否存在这样的对象，不存在则创建一个。然后给其加上引用计数。</li>
<li>如果对象类型是HANDLE（或是WEAK_HANDLE，有可能被自动回收的HANDLE对象），这时必须有提供者（Service）提供这一对象，不存在便出错退出。如果存在，并非target_proc与查找到的handle的proc一致，则将对象与handle关联起来，不一致时必然发生在对servicemanager的调用时，则尝试创建一起对handle的引用。</li>
<li>如果对象类型是文件描述符FD，则需要对文件本身的一些引用信息进行更新。</li>
<li>如果执行到这一步，证明该transaction就已经完成了，把它加入到target_thread的todo尾部，等待下一个binder_thread_read()周期的读取过程。因为加入链表是最后一步操作，于是传输过程里的原子性可以得到保证。</li>
</ul>
<p>​</p>
<p>Client的数据传来后，进行事务处理后，唤醒Server端，Server端进行数据读取。假设我们这里的Server端为ServiceManager，在ServiceManager中有一个循环，不停的向Binder驱动发送读写的信息，读到内容后调用binder_parse()进行解析。<br><img src="https://cdn.nlark.com/yuque/0/2020/png/1759879/1601302007382-a6b9abe7-4827-41eb-95c6-ed26e5b1305c.png#height=2496&id=NM0Gw&margin=%5Bobject%20Object%5D&name=binder_transaction.png&originHeight=2496&originWidth=3804&originalType=binary&ratio=1&size=616214&status=done&style=none&width=3804" alt="binder_transaction.png"></p>
<h2 id="binder-thread-read"><a href="#binder-thread-read" class="headerlink" title="binder_thread_read"></a>binder_thread_read</h2><p>前面已经解释过biner_transaction函数，主要是把binder_transaction.work放入到接收端的todo队列。而binder_thread_read函数主要是处理todo队列中的binder_work对象。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">binder_thread_read</span><span class="params">(struct binder_proc *proc,</span></span></span><br><span class="line"><span class="params"><span class="function">                  struct binder_thread *thread,</span></span></span><br><span class="line"><span class="params"><span class="function">                  <span class="keyword">binder_uintptr_t</span> binder_buffer, <span class="keyword">size_t</span> size,</span></span></span><br><span class="line"><span class="params"><span class="function">                  <span class="keyword">binder_size_t</span> *consumed, <span class="keyword">int</span> non_block)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">retry:</span><br><span class="line">    binder_inner_proc_lock(proc);</span><br><span class="line">    <span class="comment">//优先考虑thread节点的todo链表中有没有工作需要完成</span></span><br><span class="line">    wait_for_proc_work = binder_available_for_proc_work_ilocked(thread);</span><br><span class="line">    binder_inner_proc_unlock(proc);</span><br><span class="line"></span><br><span class="line">    thread-&gt;looper |= BINDER_LOOPER_STATE_WAITING;</span><br><span class="line"></span><br><span class="line">    trace_binder_wait_for_work(wait_for_proc_work,</span><br><span class="line">                   !!thread-&gt;transaction_stack,</span><br><span class="line">                   !binder_worklist_empty(proc, &amp;thread-&gt;todo));</span><br><span class="line">    <span class="keyword">if</span> (wait_for_proc_work) &#123;</span><br><span class="line">        <span class="keyword">if</span> (!(thread-&gt;looper &amp; (BINDER_LOOPER_STATE_REGISTERED |</span><br><span class="line">                    BINDER_LOOPER_STATE_ENTERED))) &#123;</span><br><span class="line">            binder_user_error(<span class="string">&quot;%d:%d ERROR: Thread waiting for process work before calling BC_REGISTER_LOOPER or BC_ENTER_LOOPER (state %x)\n&quot;</span>,</span><br><span class="line">                proc-&gt;pid, thread-&gt;pid, thread-&gt;looper);</span><br><span class="line">            wait_event_interruptible(binder_user_error_wait,</span><br><span class="line">                         binder_stop_on_user_error &lt; <span class="number">2</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        trace_android_vh_binder_restore_priority(<span class="literal">NULL</span>, current);</span><br><span class="line">        binder_restore_priority(current, proc-&gt;default_priority);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (non_block) &#123;</span><br><span class="line">        <span class="keyword">if</span> (!binder_has_work(thread, wait_for_proc_work))</span><br><span class="line">            ret = -EAGAIN;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="comment">//休眠在这里, wait_for_proc_work为false</span></span><br><span class="line">        ret = binder_wait_for_work(thread, wait_for_proc_work);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    thread-&gt;looper &amp;= ~BINDER_LOOPER_STATE_WAITING;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (ret)</span><br><span class="line">        <span class="keyword">return</span> ret;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">        </span><br><span class="line">        ...</span><br><span class="line"></span><br><span class="line">        <span class="keyword">switch</span> (w-&gt;type) &#123;</span><br><span class="line">        <span class="keyword">case</span> BINDER_WORK_TRANSACTION: &#123;</span><br><span class="line">            binder_inner_proc_unlock(proc);</span><br><span class="line">            t = container_of(w, struct binder_transaction, work);</span><br><span class="line">        &#125; <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        ...</span><br><span class="line"></span><br><span class="line">        <span class="comment">//数据在内核中封装成结构体binder_transaction传输,</span></span><br><span class="line">        <span class="comment">//据返回到应用层之后要封装成结构体 binder_transaction_data,所以这里就是将数据从新封装成binder_transaction_data返回给用户空间</span></span><br><span class="line">        <span class="keyword">if</span> (t-&gt;buffer-&gt;target_node) &#123;</span><br><span class="line">            <span class="class"><span class="keyword">struct</span> <span class="title">binder_node</span> *<span class="title">target_node</span> =</span> t-&gt;buffer-&gt;target_node;</span><br><span class="line">            <span class="class"><span class="keyword">struct</span> <span class="title">binder_priority</span> <span class="title">node_prio</span>;</span></span><br><span class="line"></span><br><span class="line">            <span class="comment">//用目标binder_node中记录的cookie值给binder_transaction_data的cookie域赋值</span></span><br><span class="line">            <span class="comment">//这个值就是目标binder实体的地址</span></span><br><span class="line">            trd-&gt;target.ptr = target_node-&gt;ptr;</span><br><span class="line">            trd-&gt;cookie =  target_node-&gt;cookie;</span><br><span class="line">            node_prio.sched_policy = target_node-&gt;sched_policy;</span><br><span class="line">            node_prio.prio = target_node-&gt;min_priority;</span><br><span class="line">            binder_transaction_priority(current, t, node_prio,</span><br><span class="line">                            target_node-&gt;inherit_rt);</span><br><span class="line">            cmd = BR_TRANSACTION;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            trd-&gt;target.ptr = <span class="number">0</span>;</span><br><span class="line">            trd-&gt;cookie = <span class="number">0</span>;</span><br><span class="line">            cmd = BR_REPLY;</span><br><span class="line">        &#125;</span><br><span class="line">        trd-&gt;code = t-&gt;code;</span><br><span class="line">        trd-&gt;flags = t-&gt;flags;</span><br><span class="line">        trd-&gt;sender_euid = from_kuid(current_user_ns(), t-&gt;sender_euid);</span><br><span class="line"></span><br><span class="line">        t_from = binder_get_txn_from(t);</span><br><span class="line">        <span class="keyword">if</span> (t_from) &#123;</span><br><span class="line">            <span class="class"><span class="keyword">struct</span> <span class="title">task_struct</span> *<span class="title">sender</span> =</span> t_from-&gt;proc-&gt;tsk;</span><br><span class="line"></span><br><span class="line">            trd-&gt;sender_pid =</span><br><span class="line">                task_tgid_nr_ns(sender,</span><br><span class="line">                        task_active_pid_ns(current));</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            trd-&gt;sender_pid = <span class="number">0</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        ret = binder_apply_fd_fixups(proc, t);</span><br><span class="line">        <span class="keyword">if</span> (ret) &#123;</span><br><span class="line">            <span class="class"><span class="keyword">struct</span> <span class="title">binder_buffer</span> *<span class="title">buffer</span> =</span> t-&gt;buffer;</span><br><span class="line">            <span class="keyword">bool</span> oneway = !!(t-&gt;flags &amp; TF_ONE_WAY);</span><br><span class="line">            <span class="keyword">int</span> tid = t-&gt;debug_id;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (t_from)</span><br><span class="line">                binder_thread_dec_tmpref(t_from);</span><br><span class="line">            buffer-&gt;transaction = <span class="literal">NULL</span>;</span><br><span class="line">            binder_cleanup_transaction(t, <span class="string">&quot;fd fixups failed&quot;</span>,</span><br><span class="line">                           BR_FAILED_REPLY);</span><br><span class="line">            binder_free_buf(proc, buffer);</span><br><span class="line">            </span><br><span class="line">            ...</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (cmd == BR_REPLY) &#123;</span><br><span class="line">                cmd = BR_FAILED_REPLY;</span><br><span class="line">                <span class="keyword">if</span> (put_user(cmd, (<span class="keyword">uint32_t</span> __user *)ptr))</span><br><span class="line">                    <span class="keyword">return</span> -EFAULT;</span><br><span class="line">                ptr += <span class="keyword">sizeof</span>(<span class="keyword">uint32_t</span>);</span><br><span class="line">                binder_stat_br(proc, thread, cmd);</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        trd-&gt;data_size = t-&gt;buffer-&gt;data_size;</span><br><span class="line">        trd-&gt;offsets_size = t-&gt;buffer-&gt;offsets_size;</span><br><span class="line">        trd-&gt;data.ptr.buffer = (<span class="keyword">uintptr_t</span>)t-&gt;buffer-&gt;user_data;</span><br><span class="line">        trd-&gt;data.ptr.offsets = trd-&gt;data.ptr.buffer +</span><br><span class="line">                    ALIGN(t-&gt;buffer-&gt;data_size,</span><br><span class="line">                        <span class="keyword">sizeof</span>(<span class="keyword">void</span> *));</span><br><span class="line"></span><br><span class="line">        tr.secctx = t-&gt;security_ctx;</span><br><span class="line">        <span class="keyword">if</span> (t-&gt;security_ctx) &#123;</span><br><span class="line">            cmd = BR_TRANSACTION_SEC_CTX;</span><br><span class="line">            trsize = <span class="keyword">sizeof</span>(tr);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//将cmd命令写入用户态，此时应该是BR_TRANSACTION</span></span><br><span class="line">        <span class="keyword">if</span> (put_user(cmd, (<span class="keyword">uint32_t</span> __user *)ptr)) &#123;</span><br><span class="line">            <span class="keyword">if</span> (t_from)</span><br><span class="line">                binder_thread_dec_tmpref(t_from);</span><br><span class="line"></span><br><span class="line">            binder_cleanup_transaction(t, <span class="string">&quot;put_user failed&quot;</span>,</span><br><span class="line">                           BR_FAILED_REPLY);</span><br><span class="line"></span><br><span class="line">            <span class="keyword">return</span> -EFAULT;</span><br><span class="line">        &#125;</span><br><span class="line">        ptr += <span class="keyword">sizeof</span>(<span class="keyword">uint32_t</span>);</span><br><span class="line">        <span class="comment">//将重新封装后的数据拷贝到用户空间</span></span><br><span class="line">        <span class="keyword">if</span> (copy_to_user(ptr, &amp;tr, trsize)) &#123;</span><br><span class="line">            <span class="keyword">if</span> (t_from)</span><br><span class="line">                binder_thread_dec_tmpref(t_from);</span><br><span class="line"></span><br><span class="line">            binder_cleanup_transaction(t, <span class="string">&quot;copy_to_user failed&quot;</span>,</span><br><span class="line">                           BR_FAILED_REPLY);</span><br><span class="line"></span><br><span class="line">            <span class="keyword">return</span> -EFAULT;</span><br><span class="line">        &#125;</span><br><span class="line">        ptr += trsize;</span><br><span class="line"></span><br><span class="line">        trace_binder_transaction_received(t);</span><br><span class="line">        binder_stat_br(proc, thread, cmd);</span><br><span class="line">    </span><br><span class="line">        ...</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (t_from)</span><br><span class="line">            binder_thread_dec_tmpref(t_from);</span><br><span class="line">        t-&gt;buffer-&gt;allow_user_free = <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">if</span> (cmd != BR_REPLY &amp;&amp; !(t-&gt;flags &amp; TF_ONE_WAY)) &#123;</span><br><span class="line">            <span class="comment">//binder_transaction节点插入了目标线程的transaction_stack堆栈，而且是以to_thread域来连接堆栈中的其他节点</span></span><br><span class="line">            binder_inner_proc_lock(thread-&gt;proc);</span><br><span class="line">            t-&gt;to_parent = thread-&gt;transaction_stack;</span><br><span class="line">            t-&gt;to_thread = thread;</span><br><span class="line">            thread-&gt;transaction_stack = t;</span><br><span class="line">            binder_inner_proc_unlock(thread-&gt;proc);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="comment">//TF_ONE_WAY情况，此时会删除binder_transaction节点</span></span><br><span class="line">            binder_free_transaction(t);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">done:</span><br><span class="line"></span><br><span class="line">    *consumed = ptr - buffer;</span><br><span class="line">    binder_inner_proc_lock(proc);</span><br><span class="line">    <span class="keyword">if</span> (proc-&gt;requested_threads == <span class="number">0</span> &amp;&amp;</span><br><span class="line">        list_empty(&amp;thread-&gt;proc-&gt;waiting_threads) &amp;&amp;</span><br><span class="line">        proc-&gt;requested_threads_started &lt; proc-&gt;max_threads &amp;&amp;</span><br><span class="line">        (thread-&gt;looper &amp; (BINDER_LOOPER_STATE_REGISTERED |</span><br><span class="line">         BINDER_LOOPER_STATE_ENTERED)) <span class="comment">/* the user-space code fails to */</span></span><br><span class="line">         <span class="comment">/*spawn a new thread if we leave this out */</span>) &#123;</span><br><span class="line">        proc-&gt;requested_threads++;</span><br><span class="line">        binder_inner_proc_unlock(proc);</span><br><span class="line">        </span><br><span class="line">        ...</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (put_user(BR_SPAWN_LOOPER, (<span class="keyword">uint32_t</span> __user *)buffer))</span><br><span class="line">            <span class="keyword">return</span> -EFAULT;</span><br><span class="line">        binder_stat_br(proc, thread, BR_SPAWN_LOOPER);</span><br><span class="line">    &#125; <span class="keyword">else</span></span><br><span class="line">        binder_inner_proc_unlock(proc);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>binder_driver首先确定当前线程是否有未处理完成的工作，如果没有，那么就让本线程去处理发给进程的工作（即不限定由那个线程完成的工作）。</li>
<li>等待binder_work。</li>
<li>循环处理binder_work。</li>
</ul>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><ul>
<li>Binder采用了AIDL来描述进程间的接口。</li>
<li>Binder是一个特殊的字符型设备，设备节点为dev/binder。</li>
<li>在Binder驱动的实现过程中，以下函数起着关键作用：<ul>
<li>使用binder_ioctl()函数与用户空间交换数据。</li>
<li>BINDER_WRITE_READ用来读写数据，数据包中的cmd域用于区分不同的请求；</li>
<li>使用binder_thread_write()函数来发送请求或返回结果，在binder_thread_write()函数中，通过调用binder_transaction()函数来转发请求并返回结果。当收到请求时，binder_transaction()函数会通过对象的handle找到对象所在的进程，如果handle结果为空，则认为此对象是context_mgr，然后把请求发给context_mgr所在的进程，并将请求中所有的Binder对象放到RB树中，最后把请求放到目标进程的队列中以等待目标进程的读取。</li>
<li>使用binder_thread_read()函数来读取结果。</li>
<li>在函数binder_parse()中实现数据解析工作。</li>
</ul>
</li>
</ul>
<p>参考：<br><a href="https://blog.csdn.net/a220315410/article/details/17914531">「Binder机制学习总结(二)-Driver部分」</a><br><a href="http://wangkuiwu.github.io/2014/09/02/Binder-Datastruct/">「Binder中的数据结构」</a><br><a href="https://blog.csdn.net/daichunjiangye/article/details/75208816">「理解Android Binder机制：驱动篇」</a><br><a href="https://blog.csdn.net/yiranfeng/article/details/105232709">「Android10.0 Binder通信原理(五)-Binder驱动分析」</a><br><a href="https://www.cnblogs.com/samchen2009/p/3316001.html">「图解Android - Binder 和 Service」</a></p>
]]></content>
      <categories>
        <category>Android</category>
      </categories>
      <tags>
        <tag>Android</tag>
        <tag>Binder</tag>
        <tag>驱动</tag>
      </tags>
  </entry>
  <entry>
    <title>Android Studio 快捷键</title>
    <url>/archives/baa4e22a.html</url>
    <content><![CDATA[<span id="more"></span>
<meta name="referrer" content="no-referrer"/>

<h1 id="IDE"><a href="#IDE" class="headerlink" title="IDE"></a>IDE</h1><table>
<thead>
<tr>
<th><strong>按键</strong></th>
<th><strong>说明</strong></th>
</tr>
</thead>
<tbody><tr>
<td>F1</td>
<td>帮助</td>
</tr>
<tr>
<td>Alt(Option)+F1</td>
<td>查找文件所在目录位置</td>
</tr>
<tr>
<td>Alt(Option)+1</td>
<td>快速打开或隐藏工程面板</td>
</tr>
<tr>
<td>Ctrl(Command)+Alt(Option)+</td>
<td>打开设置对话框</td>
</tr>
<tr>
<td>Alt(Option)+Home</td>
<td>跳转到导航栏</td>
</tr>
<tr>
<td>Esc</td>
<td>光标返回编辑框</td>
</tr>
<tr>
<td>Shift+Esc</td>
<td>光标返回编辑框,关闭无用的窗口</td>
</tr>
<tr>
<td>Shift+Click</td>
<td>关闭标签页</td>
</tr>
<tr>
<td>F12</td>
<td>把焦点从编辑器移到最近使用的工具窗口</td>
</tr>
<tr>
<td>Ctrl(Command)+Alt(Option)+Y</td>
<td>同步</td>
</tr>
<tr>
<td>Ctrl(Command)+Alt(Option)+S</td>
<td>打开设置对话框</td>
</tr>
<tr>
<td>Alt(Option)+Shift+Inert</td>
<td>开启/关闭列选择模式</td>
</tr>
<tr>
<td>Ctrl(Command)+Alt(Option)+Shift+S</td>
<td>打开当前项目/模块属性</td>
</tr>
<tr>
<td>Alt(Option)+Shift+C</td>
<td>查看文件的变更历史</td>
</tr>
<tr>
<td>Ctrl(Command)+Shift+F10</td>
<td>运行</td>
</tr>
<tr>
<td>Ctrl(Command)+Shift+F9</td>
<td>debug运行</td>
</tr>
<tr>
<td>Ctrl(Command)+Alt(Option)+F12</td>
<td>资源管理器打开文件夹</td>
</tr>
</tbody></table>
<h1 id="编辑"><a href="#编辑" class="headerlink" title="编辑"></a>编辑</h1><table>
<thead>
<tr>
<th><strong>按键</strong></th>
<th><strong>说明</strong></th>
</tr>
</thead>
<tbody><tr>
<td>Ctrl(Command)+C</td>
<td>复制当前行或选中的内容</td>
</tr>
<tr>
<td>Ctrl(Command)+D</td>
<td>粘贴当前行或选中的内容</td>
</tr>
<tr>
<td>Ctrl(Command)+X</td>
<td>剪切当前行或选中的内容</td>
</tr>
<tr>
<td>Ctrl(Command)+Y</td>
<td>删除行</td>
</tr>
<tr>
<td>Ctrl(Command)+Z</td>
<td>倒退</td>
</tr>
<tr>
<td>Ctrl(Command)+Shift+Z</td>
<td>向前</td>
</tr>
<tr>
<td>Alt(Option)+Enter</td>
<td>自动修正</td>
</tr>
<tr>
<td>Ctrl(Command)+Alt(Option)+L</td>
<td>格式化代码</td>
</tr>
<tr>
<td>Ctrl(Command)+Alt(Option)+I</td>
<td>将选中的代码进行自动缩进编排</td>
</tr>
<tr>
<td>Ctrl(Command)+Alt(Option)+O</td>
<td>优化导入的类和包</td>
</tr>
<tr>
<td>Alt(Option)+Insert</td>
<td>得到一些Intention Action，可以生成构造器、Getter、Setter、将 == 改为 equals() 等</td>
</tr>
<tr>
<td>Ctrl(Command)+Shift+V</td>
<td>选最近使用的剪贴板内容并插入</td>
</tr>
<tr>
<td>Ctrl(Command)+Alt(Option)+Shift+V</td>
<td>简单粘贴</td>
</tr>
<tr>
<td>Ctrl(Command)+Shift+Insert</td>
<td>选最近使用的剪贴板内容并插入（同Ctrl(Command)+Shift+V）</td>
</tr>
<tr>
<td>Ctrl(Command)+Enter</td>
<td>在当前行的上面插入新行，并移动光标到新行（此功能光标在行首时有效）</td>
</tr>
<tr>
<td>Shift+Enter</td>
<td>在当前行的下面插入新行，并移动光标到新行</td>
</tr>
<tr>
<td>Ctrl(Command)+J</td>
<td>自动代码</td>
</tr>
<tr>
<td>Ctrl(Command)+Alt(Option)+T</td>
<td>把选中的代码放在 try{} 、if{} 、 else{} 里</td>
</tr>
<tr>
<td>Shift+Alt(Option)+Insert</td>
<td>竖编辑模式</td>
</tr>
<tr>
<td>Ctrl(Command)+ /</td>
<td>注释 //</td>
</tr>
<tr>
<td>Ctrl(Command)+Shift+ /</td>
<td>注释 /<em>…</em>/</td>
</tr>
<tr>
<td>Ctrl(Command)+Shift+J</td>
<td>合并成一行</td>
</tr>
<tr>
<td>F2/Shift+F2</td>
<td>跳转到下/上一个错误语句处</td>
</tr>
<tr>
<td>Ctrl(Command)+Shift+Back</td>
<td>跳转到上次编辑的地方</td>
</tr>
<tr>
<td>Ctrl(Command)+Alt(Option)+Space</td>
<td>类名自动完成</td>
</tr>
<tr>
<td>Shift+Alt(Option)+Up/Down</td>
<td>内容向上/下移动</td>
</tr>
<tr>
<td>Ctrl(Command)+Shift+Up/Down</td>
<td>语句向上/下移动</td>
</tr>
<tr>
<td>Ctrl(Command)+Shift+U</td>
<td>大小写切换</td>
</tr>
<tr>
<td>Tab</td>
<td>代码标签输入完成后，按 Tab，生成代码</td>
</tr>
<tr>
<td>Ctrl(Command)+Backspace</td>
<td>按单词删除</td>
</tr>
<tr>
<td>Ctrl(Command)+Shift+Enter</td>
<td>语句完成</td>
</tr>
<tr>
<td>Ctrl(Command)+Alt(Option)+J</td>
<td>用动态模板环绕</td>
</tr>
</tbody></table>
<h1 id="文件"><a href="#文件" class="headerlink" title="文件"></a>文件</h1><table>
<thead>
<tr>
<th><strong>按键</strong></th>
<th><strong>说明</strong></th>
</tr>
</thead>
<tbody><tr>
<td>Ctrl(Command)+F12</td>
<td>显示当前文件的结构</td>
</tr>
<tr>
<td>Ctrl(Command)+H</td>
<td>显示类继承结构图</td>
</tr>
<tr>
<td>Ctrl(Command)+Q</td>
<td>显示注释文档</td>
</tr>
<tr>
<td>Ctrl(Command)+P</td>
<td>方法参数提示</td>
</tr>
<tr>
<td>Ctrl(Command)+U</td>
<td>打开当前类的父类或者实现的接口</td>
</tr>
<tr>
<td>Alt(Option)+Left/Right</td>
<td>切换代码视图</td>
</tr>
<tr>
<td>Ctrl(Command)+Alt(Option)+Left/Right</td>
<td>返回上次编辑的位置</td>
</tr>
<tr>
<td>Alt(Option)+Up/Down</td>
<td>在方法间快速移动定位</td>
</tr>
<tr>
<td>Ctrl(Command)+B</td>
<td>快速打开光标处的类或方法</td>
</tr>
<tr>
<td>Ctrl(Command)+W</td>
<td>选中代码，连续按会有其他效果</td>
</tr>
<tr>
<td>Ctrl(Command)+Shift+W</td>
<td>取消选择光标所在词</td>
</tr>
<tr>
<td>Ctrl(Command)+ - / +</td>
<td>折叠/展开代码</td>
</tr>
<tr>
<td>Ctrl(Command)+Shift+ - / +</td>
<td>折叠/展开全部代码</td>
</tr>
<tr>
<td>Ctrl(Command)+Shift+.</td>
<td>折叠/展开当前花括号中的代码</td>
</tr>
<tr>
<td>Ctrl(Command)+ ] / [</td>
<td>跳转到代码块结束/开始处</td>
</tr>
<tr>
<td>F2 或 Shift+F2</td>
<td>高亮错误或警告快速定位</td>
</tr>
<tr>
<td>Ctrl(Command)+Shift+C</td>
<td>复制路径</td>
</tr>
<tr>
<td>Ctrl(Command)+Alt(Option)+Shift+C</td>
<td>复制引用，必须选择类名</td>
</tr>
<tr>
<td>Alt(Option)+Up/Down</td>
<td>在方法间快速移动定位</td>
</tr>
<tr>
<td>Shift+F1</td>
<td>要打开编辑器光标字符处使用的类或者方法 Java 文档的浏览器</td>
</tr>
<tr>
<td>Ctrl(Command)+G</td>
<td>定位行</td>
</tr>
</tbody></table>
<h1 id="查找"><a href="#查找" class="headerlink" title="查找"></a>查找</h1><table>
<thead>
<tr>
<th><strong>按键</strong></th>
<th><strong>说明</strong></th>
</tr>
</thead>
<tbody><tr>
<td>Ctrl(Command)+F</td>
<td>在当前窗口查找文本</td>
</tr>
<tr>
<td>Ctrl(Command)+Shift+F</td>
<td>在指定环境下查找文本</td>
</tr>
<tr>
<td>F3</td>
<td>向下查找关键字出现位置</td>
</tr>
<tr>
<td>Shift+F3</td>
<td>向上一个关键字出现位置</td>
</tr>
<tr>
<td>Ctrl(Command)+R</td>
<td>在当前窗口替换文本</td>
</tr>
<tr>
<td>Ctrl(Command)+Shift+R</td>
<td>在指定窗口替换文本</td>
</tr>
<tr>
<td>Ctrl(Command)+N</td>
<td>查找类</td>
</tr>
<tr>
<td>Ctrl(Command)+Shift+N</td>
<td>查找文件</td>
</tr>
<tr>
<td>Ctrl(Command)+Shift+Alt(Option)+N</td>
<td>查找项目中的方法或变量</td>
</tr>
<tr>
<td>Ctrl(Command)+B</td>
<td>查找变量的来源</td>
</tr>
<tr>
<td>Ctrl(Command)+Alt(Option)+B</td>
<td>快速打开光标处的类或方法</td>
</tr>
<tr>
<td>Ctrl(Command)+Shift+B</td>
<td>跳转到类或方法实现处</td>
</tr>
<tr>
<td>Ctrl(Command)+E</td>
<td>最近打开的文件</td>
</tr>
<tr>
<td>Alt(Option)+F3</td>
<td>快速查找，效果和Ctrl(Command)+F相同</td>
</tr>
<tr>
<td>F4</td>
<td>跳转至定义变量的位置</td>
</tr>
<tr>
<td>Alt(Option)+F7</td>
<td>查询当前元素在工程中的引用</td>
</tr>
<tr>
<td>Ctrl(Command)+F7</td>
<td>查询当前元素在当前文件中的引用，然后按 F3 可以选择</td>
</tr>
<tr>
<td>Ctrl(Command)+Alt(Option)+F7</td>
<td>选中查询当前元素在工程中的引用</td>
</tr>
<tr>
<td>Ctrl(Command)+Shift+F7</td>
<td>高亮显示匹配的字符，按 Esc 高亮消失</td>
</tr>
<tr>
<td>Ctrl(Command)+Alt(Option)+F7</td>
<td>查找某个方法的所有调用地方</td>
</tr>
<tr>
<td>Ctrl(Command)+Shift+Alt(Option)+N</td>
<td>查找类中的方法或变量</td>
</tr>
<tr>
<td><em>Ctrl(Command)+Shift+O</em></td>
<td><em>弹出显示查找内容</em></td>
</tr>
<tr>
<td><em>Ctrl(Command)+Alt(Option)+Up/Down</em></td>
<td>快速跳转搜索结果</td>
</tr>
<tr>
<td><em>Ctrl(Command)+Shift+S</em></td>
<td>高级搜索、搜索结构</td>
</tr>
</tbody></table>
<h1 id="重构"><a href="#重构" class="headerlink" title="重构"></a>重构</h1><table>
<thead>
<tr>
<th><strong>按键</strong></th>
<th><strong>说明</strong></th>
</tr>
</thead>
<tbody><tr>
<td>F5</td>
<td>复制</td>
</tr>
<tr>
<td>F6</td>
<td>移动</td>
</tr>
<tr>
<td>Alt(Option)+Delete</td>
<td>安全删除</td>
</tr>
<tr>
<td>Ctrl(Command)+U</td>
<td>转到父类</td>
</tr>
<tr>
<td>Ctrl(Command)+O</td>
<td>重写父类的方法</td>
</tr>
<tr>
<td>Ctrl(Command)+I</td>
<td>实现方法</td>
</tr>
<tr>
<td>Ctrl(Command)+Alt(Option)+N</td>
<td>内联</td>
</tr>
<tr>
<td>Ctrl(Command)+Alt(Option)+Shift+T</td>
<td>弹出重构菜单</td>
</tr>
<tr>
<td>Shift+F6</td>
<td>重构-重命名</td>
</tr>
<tr>
<td>Ctrl(Command)+Alt(Option)+M</td>
<td>提取代码组成方法</td>
</tr>
<tr>
<td>Ctrl(Command)+Alt(Option)+C</td>
<td>将变量更改为常量</td>
</tr>
<tr>
<td>Ctrl(Command)+Alt(Option)+V</td>
<td>定义变量引用当前对象或者方法的返回值</td>
</tr>
<tr>
<td>Ctrl(Command)+Alt(Option)+F</td>
<td>将局部变量更改为类的成员变量</td>
</tr>
<tr>
<td>Ctrl(Command)+Alt(Option)+P</td>
<td>将变量更改为方法的参数</td>
</tr>
</tbody></table>
<h1 id="调试"><a href="#调试" class="headerlink" title="调试"></a>调试</h1><table>
<thead>
<tr>
<th><strong>按键</strong></th>
<th><strong>说明</strong></th>
</tr>
</thead>
<tbody><tr>
<td>F8</td>
<td>跳到下一步</td>
</tr>
<tr>
<td>Shift+F8</td>
<td>跳出函数、跳到下一个断点</td>
</tr>
<tr>
<td>Alt(Option)+Shift+F8</td>
<td>强制跳出函数</td>
</tr>
<tr>
<td>F7</td>
<td>进入代码</td>
</tr>
<tr>
<td>Shift+F7</td>
<td>智能进入代码</td>
</tr>
<tr>
<td>Alt(Option)+Shift+F7</td>
<td>强制进入代码</td>
</tr>
<tr>
<td>Alt(Option)+F9</td>
<td>运行至光标处</td>
</tr>
<tr>
<td>Ctrl(Command)+Alt(Option)+F9</td>
<td>强制运行至光标处</td>
</tr>
<tr>
<td>Ctrl(Command)+F2</td>
<td>停止运行</td>
</tr>
<tr>
<td>Alt(Option)+F8</td>
<td>计算变量值</td>
</tr>
</tbody></table>
]]></content>
      <categories>
        <category>Android</category>
      </categories>
      <tags>
        <tag>tools</tag>
      </tags>
  </entry>
  <entry>
    <title>搭建（更新）hexo、next主题美化</title>
    <url>/archives/7f4dd193.html</url>
    <content><![CDATA[<span id="more"></span>

<h1 id="目的"><a href="#目的" class="headerlink" title="目的"></a>目的</h1><p>2018年搭建过博客之后就没继续用了，今年想把这几年在印象笔记、语雀的笔记分享出来。想再用时发现Next主题更新了好多版本，顺便把Hexo迁移到mac上面来。故把新增的过程做一个记录。</p>
<h1 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h1><p>首先下载安装git和node.js。其次是通过以下命令安装hexo。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo npm install -g hexo</span><br></pre></td></tr></table></figure>
<p>新建一个用于存放博客文件的文件夹，我们为了后续方便更新文章或者在不同电脑使用，把放置博客的文件也托管到github上。如我自己的<a href="https://github.com/i-rtfsc/blog">博客源文件</a><br>而<a href="https://github.com/i-rtfsc/i-rtfsc.github.io">https://github.com/i-rtfsc/i-rtfsc.github.io</a>就放置静态网页，其中project名字是i-rtfsc.github.io要跟你github名字对上。<br>在博客源文件夹下的_config.yml文件添加如下：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">deploy:</span><br><span class="line">  <span class="built_in">type</span>: git</span><br><span class="line">  repository: https://github.com/i-rtfsc/i-rtfsc.github.io.git</span><br><span class="line">  branch: master</span><br></pre></td></tr></table></figure>
<p>这样，你每写完一遍博客并执行hexo deploy，就会自动把静态网页上传到i-rtfsc.github.io仓。</p>
<h1 id="更新-node"><a href="#更新-node" class="headerlink" title="更新 node"></a>更新 node</h1><p>首先更新 npm</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install -g npm</span><br></pre></td></tr></table></figure>
<p>清除 npm 缓存</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo npm cache clean -f</span><br></pre></td></tr></table></figure>


<h1 id="更新-hexo"><a href="#更新-hexo" class="headerlink" title="更新 hexo"></a>更新 hexo</h1><p>进入自己的<a href="https://github.com/i-rtfsc/blog">博客目录</a>，执行如下命令：</p>
<ul>
<li><p>更新 package.json 中的 hexo 及个插件版本</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install -g npm-check</span><br><span class="line">npm install -g npm-upgrade</span><br><span class="line">npm-check</span><br><span class="line">npm-upgrade</span><br></pre></td></tr></table></figure>
<p>如果执行npm-check不成功，可以换成npm-check -u，或者如下（版本号自己定）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install --save hexo@5.4.0</span><br><span class="line">npm install --save hexo-deployer-git@3.0.0</span><br><span class="line">npm install --save hexo-generator-archive@1.0.0</span><br><span class="line">npm install --save hexo-generator-category@1.0.0</span><br><span class="line">npm install --save hexo-generator-index@2.0.0</span><br><span class="line">npm install --save hexo-generator-tag@1.0.0</span><br><span class="line">npm install --save hexo-renderer-ejs@1.0.0</span><br><span class="line">npm install --save hexo-renderer-marked@4.1.0</span><br><span class="line">npm install --save hexo-renderer-stylus@2.0.1 </span><br><span class="line">npm install --save hexo-server@2.0.0</span><br></pre></td></tr></table></figure></li>
<li><p>更新 hexo 及所有插件</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm update</span><br></pre></td></tr></table></figure></li>
<li><p>查看版本号</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo version</span><br></pre></td></tr></table></figure></li>
<li><p>主题</p>
</li>
</ul>
<p>主题变化很大，github仓都不一样，如果之前有配置过，则先把themes文件夹删除，然后通过git submodule方式下载。<br>下载完成后到根目录下的_config.yml在theme：后添加next即可。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git submodule add https://github.com/theme-next/hexo-theme-next themes/next</span><br></pre></td></tr></table></figure>
<p>​</p>
<h1 id="根目录下-config-yml"><a href="#根目录下-config-yml" class="headerlink" title="根目录下_config.yml"></a>根目录下_config.yml</h1><h2 id="Site"><a href="#Site" class="headerlink" title="Site"></a>Site</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Site</span></span><br><span class="line">title: Solo</span><br><span class="line">subtitle: anqi.huang@outlook.com</span><br><span class="line">description: 重学Android系列</span><br><span class="line">keywords: Android、Framework</span><br><span class="line">author: Solo</span><br><span class="line">language: zh-CN</span><br><span class="line">timezone: UTC</span><br></pre></td></tr></table></figure>

<ul>
<li>title: 填写标题</li>
<li>subtitle: 填写副标题</li>
<li>description: 简介</li>
<li>keywords: 关键词</li>
<li>author: 作者，文章中显示的作者名称</li>
<li>language: 设置语言，zh-CN是简体中文，en是英语，默认是德语</li>
</ul>
<h2 id="URL"><a href="#URL" class="headerlink" title="URL"></a>URL</h2><p>文章永久链接插件安装命令如下：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install hexo-abbrlink --save</span><br></pre></td></tr></table></figure>
<p>修改配置文件_config.yml如下：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">url: http://i-rtfsc.com</span><br><span class="line">root: /</span><br><span class="line">permalink: archives/:abbrlink.html</span><br><span class="line">abbrlink:</span><br><span class="line">  alg: crc32  <span class="comment"># 算法：crc16(default) and crc32</span></span><br><span class="line">  rep: hex    <span class="comment"># 进制：dec(default) and hex</span></span><br><span class="line">permalink_defaults:</span><br></pre></td></tr></table></figure>


<h2 id="翻页设置"><a href="#翻页设置" class="headerlink" title="翻页设置"></a>翻页设置</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Home page setting</span></span><br><span class="line"><span class="comment"># path: Root path for your blogs index page. (default = &#x27;&#x27;)</span></span><br><span class="line"><span class="comment"># per_page: Posts displayed per page. (0 = disable pagination)</span></span><br><span class="line"><span class="comment"># order_by: Posts order. (Order by date descending by default)</span></span><br><span class="line">index_generator:</span><br><span class="line">  path: <span class="string">&#x27;&#x27;</span></span><br><span class="line">  per_page: 12</span><br><span class="line">  order_by: -date</span><br><span class="line">  </span><br><span class="line"><span class="comment"># Pagination</span></span><br><span class="line"><span class="comment">## Set per_page to 0 to disable pagination</span></span><br><span class="line">per_page: 12</span><br><span class="line">pagination_dir: page</span><br></pre></td></tr></table></figure>
<p>共有两处，用于控制每一页显示多少篇博文，per_page:后设置博文数量，0代表不翻页全部平铺显示。<br>​</p>
<h2 id="Deployment设置"><a href="#Deployment设置" class="headerlink" title="Deployment设置"></a>Deployment设置</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Deployment</span></span><br><span class="line"><span class="comment">## Docs: https://hexo.io/docs/deployment.html</span></span><br><span class="line">deploy:</span><br><span class="line">  <span class="built_in">type</span>: git</span><br><span class="line">  repository: https://github.com/i-rtfsc/i-rtfsc.github.io.git</span><br><span class="line">  branch: master</span><br></pre></td></tr></table></figure>

<ul>
<li>branch:分支填写master</li>
<li>repo:后把username换成自己的用户名</li>
</ul>
<h1 id="主题目录下-config-yml"><a href="#主题目录下-config-yml" class="headerlink" title="主题目录下_config.yml"></a>主题目录下_config.yml</h1><h2 id="推荐文章插件"><a href="#推荐文章插件" class="headerlink" title="推荐文章插件"></a>推荐文章插件</h2><p>安装插件命令如下：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install hexo-related-popular-posts --save</span><br></pre></td></tr></table></figure>
<p>修改配置文件_config.yml如下：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">related_posts:</span><br><span class="line">  <span class="built_in">enable</span>: <span class="literal">true</span></span><br><span class="line">  title: 推荐文章 <span class="comment"># Custom header, leave empty to use the default one</span></span><br><span class="line">  display_in_home: <span class="literal">false</span></span><br><span class="line">  params:</span><br><span class="line">    maxCount: 5</span><br><span class="line">    PPMixingRate: 0.25</span><br><span class="line">    isDate: <span class="literal">false</span></span><br><span class="line">    isImage: <span class="literal">false</span></span><br><span class="line">    isExcerpt: <span class="literal">false</span></span><br></pre></td></tr></table></figure>

<h2 id="文章排序"><a href="#文章排序" class="headerlink" title="文章排序"></a>文章排序</h2><p>安装插件命令如下：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install hexo-generator-index-pin-top --save</span><br></pre></td></tr></table></figure>
<p>然后打开任何一篇博客,加上top属性,top后面数字越大优先级越大。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">top: 01</span><br></pre></td></tr></table></figure>
<p>修改node_modules/hexo-generator-index/lib/generator.js<br>就可以变成：</p>
<ol>
<li>默认按照发布时间排序</li>
<li>设置top可以置顶，top的值越大优先级越大<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">//sort(posts.data, (a, b) =&gt; (b.sticky || 0) - (a.sticky || 0));</span><br><span class="line">posts.data = posts.data.sort(<span class="keyword">function</span>(a, b) &#123;</span><br><span class="line">    <span class="keyword">if</span>(a.top &amp;&amp; b.top) &#123; // 两篇文章top都有定义</span><br><span class="line">        <span class="keyword">if</span>(a.top == b.top) <span class="built_in">return</span> b.date - a.date; // 若top值一样则按照文章日期降序排</span><br><span class="line">        <span class="keyword">else</span> <span class="built_in">return</span> b.top - a.top; // 否则按照top值降序排</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span>(a.top &amp;&amp; !b.top) &#123; // 以下是只有一篇文章top有定义，那么将有top的排在前面（这里用异或操作居然不行233）</span><br><span class="line">        <span class="built_in">return</span> -1;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span>(!a.top &amp;&amp; b.top) &#123;</span><br><span class="line">        <span class="built_in">return</span> 1;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> <span class="built_in">return</span> b.date - a.date; // 都没定义按照文章日期降序排</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure></li>
</ol>
<h2 id="本地搜索功能"><a href="#本地搜索功能" class="headerlink" title="本地搜索功能"></a>本地搜索功能</h2><p>安装插件命令如下：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install hexo-generator-searchdb --save</span><br></pre></td></tr></table></figure>
<p>修改配置文件_config.yml如下：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 本地搜索</span></span><br><span class="line">search:</span><br><span class="line">  path: search.xml</span><br><span class="line">  field: post</span><br><span class="line">  format: html</span><br><span class="line">  <span class="built_in">limit</span>: 100</span><br></pre></td></tr></table></figure>

<ul>
<li>path：索引文件的路径，相对于站点根目录</li>
<li>field：搜索范围，默认是 post，还可以选择 page、all，设置成 all 表示搜索所有页面</li>
<li>limit：限制搜索的条目数</li>
</ul>
<p>然后修改主题配置文件_config.yml如下：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Local Search</span></span><br><span class="line"><span class="comment"># Dependencies: https://github.com/theme-next/hexo-generator-searchdb</span></span><br><span class="line">local_search:</span><br><span class="line">  <span class="built_in">enable</span>: <span class="literal">true</span></span><br><span class="line">  <span class="comment"># If auto, trigger search by changing input.</span></span><br><span class="line">  <span class="comment"># If manual, trigger search by pressing enter key or search button.</span></span><br><span class="line">  trigger: auto</span><br><span class="line">  <span class="comment"># Show top n results per article, show all results by setting to -1</span></span><br><span class="line">  top_n_per_article: 1</span><br><span class="line">  <span class="comment"># Unescape html strings to the readable one.</span></span><br><span class="line">  unescape: <span class="literal">false</span></span><br><span class="line">  <span class="comment"># Preload the search data when the page loads.</span></span><br><span class="line">  preload: <span class="literal">false</span></span><br></pre></td></tr></table></figure>


<h2 id="网站Logo设置"><a href="#网站Logo设置" class="headerlink" title="网站Logo设置"></a>网站Logo设置</h2><p>把选好的图标放入\themes\next\source\images目录中并改成上面的文件名即可，注意图片尺寸。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">favicon:</span><br><span class="line">  small: /images/favicon-16x16-next.png</span><br><span class="line">  medium: /images/favicon-32x32-next.png</span><br><span class="line">  apple_touch_icon: /images/apple-touch-icon-next.png</span><br></pre></td></tr></table></figure>


<h2 id="页脚设置"><a href="#页脚设置" class="headerlink" title="页脚设置"></a>页脚设置</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">footer:</span><br><span class="line">  <span class="comment"># Specify the date when the site was setup. If not defined, current year will be used.</span></span><br><span class="line">  since: 2018</span><br><span class="line"></span><br><span class="line">  <span class="comment"># Icon between year and copyright info.</span></span><br><span class="line">  icon:</span><br><span class="line">    <span class="comment"># Icon name in fontawesome, see: https://fontawesome.com/v4.7.0/icons/</span></span><br><span class="line">    <span class="comment"># `heart` is recommended with animation in red (#ff0000).</span></span><br><span class="line">    name: heart</span><br><span class="line">    <span class="comment"># If you want to animate the icon, set it to true.</span></span><br><span class="line">    animated: <span class="literal">true</span></span><br><span class="line">    <span class="comment"># Change the color of icon, using Hex Code.</span></span><br><span class="line">    color: <span class="string">&quot;#f08080&quot;</span></span><br><span class="line"></span><br><span class="line">  <span class="comment"># If not defined, `author` from Hexo main config will be used.</span></span><br><span class="line">  copyright: Solo</span><br><span class="line"></span><br><span class="line">  powered:</span><br><span class="line">    <span class="comment"># Hexo link (Powered by Hexo).</span></span><br><span class="line">    <span class="built_in">enable</span>: <span class="literal">true</span></span><br><span class="line">    <span class="comment"># Version info of Hexo after Hexo link (vX.X.X).</span></span><br><span class="line">    version: <span class="literal">true</span></span><br><span class="line"></span><br><span class="line">  theme:</span><br><span class="line">    <span class="comment"># Theme &amp; scheme info link (Theme - NexT.scheme).</span></span><br><span class="line">    <span class="built_in">enable</span>: <span class="literal">true</span></span><br><span class="line">    <span class="comment"># Version info of NexT after scheme info (vX.X.X).</span></span><br><span class="line">    version: <span class="literal">true</span></span><br></pre></td></tr></table></figure>

<ul>
<li>since: 表示网站成立的年份，不设置的话默认是当前年份，显示为© 2021</li>
<li>icon: 中设置年份后面的图标，默认是心形，想修改的话去Fontawesome选择图标，然后再name栏修改</li>
<li>animated: 用于设置心形是否跳动</li>
<li>color: 用于设置图标颜色，百度对照色表查就好</li>
<li>copyright: 显示图标后面的版权所属，也就是作者名，不设置的话默认显示根目录配置文件中的作者名</li>
<li>powered: 后面的栏目用于设定是否显示Powered by Hexo以及当前主题的版本信息，显示的话设为true</li>
<li>theme: 后面的栏目用于设定是否显示当前主题的版本信息，显示的话将设为true</li>
</ul>
<h2 id="Github角标"><a href="#Github角标" class="headerlink" title="Github角标"></a>Github角标</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Github角标</span></span><br><span class="line">github_banner:</span><br><span class="line">  <span class="built_in">enable</span>: <span class="literal">true</span></span><br><span class="line">  permalink: https://github.com/i-rtfsc</span><br><span class="line">  title: Follow me on GitHub</span><br></pre></td></tr></table></figure>


<h2 id="社交网站链接设置"><a href="#社交网站链接设置" class="headerlink" title="社交网站链接设置"></a>社交网站链接设置</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">social:</span><br><span class="line">  GitHub: https://github.com/i-rtfsc || github</span><br><span class="line">  E-Mail: mailto:anqi.huang@outlook.com || envelope</span><br></pre></td></tr></table></figure>


<h2 id="头像设置"><a href="#头像设置" class="headerlink" title="头像设置"></a>头像设置</h2><p>用于设置是否在侧边栏显示头像</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">avatar:</span><br><span class="line">  <span class="comment"># In theme directory (source/images): /images/avatar.gif</span></span><br><span class="line">  <span class="comment"># In site directory (source/uploads): /uploads/avatar.gif</span></span><br><span class="line">  <span class="comment"># You can also use other linking images.</span></span><br><span class="line">  url: /images/avatar.gif</span><br><span class="line">  <span class="comment"># If true, the avatar would be dispalyed in circle.</span></span><br><span class="line">  rounded: <span class="literal">true</span></span><br><span class="line">  <span class="comment"># The value of opacity should be choose from 0 to 1 to set the opacity of the avatar.</span></span><br><span class="line">  opacity: 1</span><br><span class="line">  <span class="comment"># If true, the avatar would be rotated with the cursor.</span></span><br><span class="line">  rotated: <span class="literal">true</span></span><br></pre></td></tr></table></figure>

<ul>
<li>url:后填写头像图片的路径，默认在\themes\next\source\images中</li>
<li>rounded:用于设置是否显示圆形头像</li>
<li>opacity:用于设置头像透明度，1为不透明</li>
<li>rotated:用于设置头像鼠标移动到头像上时头像是否旋转</li>
</ul>
<h2 id="侧边栏目录设置"><a href="#侧边栏目录设置" class="headerlink" title="侧边栏目录设置"></a>侧边栏目录设置</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">toc:</span><br><span class="line">  <span class="built_in">enable</span>: <span class="literal">true</span></span><br><span class="line">  <span class="comment"># Automatically add list number to toc.</span></span><br><span class="line">  number: <span class="literal">true</span></span><br><span class="line">  <span class="comment"># If true, all words will placed on next lines if header width longer then sidebar width.</span></span><br><span class="line">  wrap: <span class="literal">false</span></span><br><span class="line">  <span class="comment"># If true, all level of TOC in a post will be displayed, rather than the activated part of it.</span></span><br><span class="line">  expand_all: <span class="literal">true</span></span><br><span class="line">  <span class="comment"># Maximum heading depth of generated toc. You can set it in one post through `toc_max_depth` in Front Matter.</span></span><br><span class="line">  max_depth: 6</span><br></pre></td></tr></table></figure>

<ul>
<li>enable:用于设置是否开启侧边栏目录</li>
<li>number:用于设置自动编号</li>
<li>wrap:用于设置当标题长度超过侧边栏长度时是否自动换行</li>
<li>expand_all:用于设置是否展开全部目录，否的话会自动展开正在看的那部分</li>
<li>max_depth:用于设置自动生成目录的最大深度，也就是生成到几级标题</li>
</ul>
<h2 id="侧边栏显示设置"><a href="#侧边栏显示设置" class="headerlink" title="侧边栏显示设置"></a>侧边栏显示设置</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sidebar:</span><br><span class="line">  <span class="comment"># Sidebar Position, available values: left | right (only for Pisces | Gemini).</span></span><br><span class="line">  <span class="comment">#position: left</span></span><br><span class="line">  position: right</span><br><span class="line"></span><br><span class="line">  <span class="comment"># Manual define the sidebar width. If commented, will be default for:</span></span><br><span class="line">  <span class="comment"># Muse | Mist: 320</span></span><br><span class="line">  <span class="comment"># Pisces | Gemini: 240</span></span><br><span class="line">  width: 320</span><br><span class="line"></span><br><span class="line">  <span class="comment"># Sidebar Display, available values (only for Muse | Mist):</span></span><br><span class="line">  <span class="comment">#  - post    expand on posts automatically. Default.</span></span><br><span class="line">  <span class="comment">#  - always  expand for all pages automatically.</span></span><br><span class="line">  <span class="comment">#  - hide    expand only when click on the sidebar toggle icon.</span></span><br><span class="line">  <span class="comment">#  - remove  totally remove sidebar including sidebar toggle.</span></span><br><span class="line">  display: post</span><br><span class="line"></span><br><span class="line">  <span class="comment"># Sidebar offset from top menubar in pixels (only for Pisces | Gemini).</span></span><br><span class="line">  offset: 12</span><br><span class="line">  <span class="comment"># Enable sidebar on narrow view (only for Muse | Mist).</span></span><br><span class="line">  onmobile: <span class="literal">false</span></span><br><span class="line">  <span class="comment"># Click any blank part of the page to close sidebar (only for Muse | Mist).</span></span><br><span class="line">  dimmer: <span class="literal">false</span></span><br></pre></td></tr></table></figure>

<ul>
<li>position:用于设置侧边栏在左边还是右边，把要选择的位置前面的#去掉再把另一个前面加上#即可，这个选项只对Pisces和Gemini两个主题起作用</li>
<li>width:用于设置侧边栏的宽度，默认Muse和Mist是320,Pisces和Gemini是240</li>
<li>display:用于设置侧边栏的显示方式，只对Muse和Mist两个主题起作用，可以挨个试一下</li>
<li>offset:用于设置侧边栏距离顶部介绍的距离，只对Pisces和Gemini两个主题起作用</li>
<li>onmobile:用于设置是否在手机等宽度较小的设备上显示侧边栏，只对Muse和Mist两个主题起作用</li>
<li>dimmer:用于设置点击屏幕空白处是否关闭侧边栏，同样只对Muse和Mist两个主题起作用</li>
</ul>
<h2 id="返回顶部按钮"><a href="#返回顶部按钮" class="headerlink" title="返回顶部按钮"></a>返回顶部按钮</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">back2top:</span><br><span class="line">  <span class="built_in">enable</span>: <span class="literal">true</span></span><br><span class="line">  <span class="comment"># Back to top in sidebar.</span></span><br><span class="line">  sidebar: <span class="literal">true</span></span><br><span class="line">  <span class="comment"># Scroll percent label in b2t button.</span></span><br><span class="line">  scrollpercent: <span class="literal">true</span></span><br></pre></td></tr></table></figure>

<ul>
<li>enable:用于设置是否开启回到顶部按钮</li>
<li>sidebar:用于设置是否将按钮放到侧边栏中</li>
<li>scrollpercent:用于设置是否显示阅读进度百分比</li>
</ul>
<h2 id="代码块高亮主题"><a href="#代码块高亮主题" class="headerlink" title="代码块高亮主题"></a>代码块高亮主题</h2><p>Next主题内置了tomorrow代码高亮主题，一共有五个，可以去Tomorrow查看预览效果，在下面的highlight_theme:中改成自己想要的即可。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 代码块高亮主题</span></span><br><span class="line">codeblock:</span><br><span class="line">  <span class="comment"># Code Highlight theme</span></span><br><span class="line">  <span class="comment"># Available values: normal | night | night eighties | night blue | night bright | solarized | solarized dark | galactic</span></span><br><span class="line">  <span class="comment"># See: https://github.com/chriskempson/tomorrow-theme</span></span><br><span class="line">  highlight_theme: night</span><br><span class="line">  <span class="comment"># Add copy button on codeblock</span></span><br><span class="line">  copy_button:</span><br><span class="line">    <span class="built_in">enable</span>: <span class="literal">true</span></span><br><span class="line">    <span class="comment"># Show text copy result.</span></span><br><span class="line">    show_result: <span class="literal">false</span></span><br><span class="line">    <span class="comment"># Available values: default | flat | mac</span></span><br><span class="line">    style: mac</span><br></pre></td></tr></table></figure>


<h2 id="Mermaid"><a href="#Mermaid" class="headerlink" title="Mermaid"></a>Mermaid</h2><p>近来用Markdown写文章，越来越不喜欢插入图片了，一切能用语法解决的问题坚决不放图，原因有二：</p>
<ul>
<li>如果把流程图和数学公式都以图片方式放到文章内，当部署到Github上后，访问博客时图片加载实在太慢，有时一篇文章需要画10来个流程图，那你就得截图10来多次，还得给这些图片想一个合适的名字，同时插入图片的时候还要注意图片的插入位置和顺序；</li>
<li>如果你要把文章发布到其他博客平台，如CSDN、博客园，在每一个平台上你都要插入10来多次图片，作为程序员，这种笨拙又耗时的方法，我实在不能忍。</li>
</ul>
<p>这也是我为什么放弃印象笔记改用语雀的一大主要原因。语雀里有一个功能“文本绘图”可以用PlantUML、Mermaid、Flowchart、Graphviz等。如下如就是用Mermaid绘制的。</p>
<pre class="mermaid">sequenceDiagram
Activity ->> Activity:startActivity
Activity ->> Activity:startActivityForResult()
Activity ->> Instrumentation:execStartActivity()
Instrumentation ->>+ ActivityTaskManager:getService()
ActivityTaskManager ->>+ ServiceManager:getService()
ServiceManager ->>+ BinderInternal:getContextObject()
BinderInternal ->>- ServiceManager:IServiceManager
ServiceManager ->>- ActivityTaskManager:IBinder
ActivityTaskManager ->>- Instrumentation:IActivityTaskManager
Instrumentation ->> ActivityTaskManagerService:startActivity()</pre>
<p>我们现在想办法在hexo上也能用上这个插件，首先使用以下命令安装插件：（最新版本hexo就自带了，无需安装）</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install hexo-filter-mermaid-diagrams --save</span><br></pre></td></tr></table></figure>
<p>修改</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Mermaid tag</span></span><br><span class="line">mermaid:</span><br><span class="line">  <span class="built_in">enable</span>: <span class="literal">true</span></span><br><span class="line">  <span class="comment"># Available themes: default | dark | forest | neutral</span></span><br><span class="line">  theme: neutral</span><br></pre></td></tr></table></figure>
<p>并在themes/next/source/css/_common/components/components.styl添加：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">.mermaid &#123;</span><br><span class="line"> background: transparent;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>添加这个目前是为了避免你设置黑色主题时背景为黑色导致看不到流程，不一定是加在components.styl，你可以找个合适的地方。<br>​</p>
<h1 id="深度美化"><a href="#深度美化" class="headerlink" title="深度美化"></a>深度美化</h1><h2 id="字数和阅读时间统计插件"><a href="#字数和阅读时间统计插件" class="headerlink" title="字数和阅读时间统计插件"></a>字数和阅读时间统计插件</h2><p>安装插件命令如下：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install hexo-symbols-count-time --save</span><br></pre></td></tr></table></figure>
<p>修改配置文件_config.yml如下：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">symbols_count_time:</span><br><span class="line">  symbols: <span class="literal">true</span> <span class="comment"># 文章字数</span></span><br><span class="line">  time: <span class="literal">true</span> <span class="comment"># 阅读时长</span></span><br><span class="line">  total_symbols: <span class="literal">true</span> <span class="comment"># 所有文章总字数</span></span><br><span class="line">  total_time: <span class="literal">true</span> <span class="comment"># 所有文章阅读中时长</span></span><br></pre></td></tr></table></figure>


<h2 id="鼠标点击特效"><a href="#鼠标点击特效" class="headerlink" title="鼠标点击特效"></a>鼠标点击特效</h2><p>在网页中加入鼠标点击特效，目前有心形和爆炸两种特效，两者可以叠加，特效对性能较低的设备非常不友好，尤其是爆炸特效，在平板上卡成PPT，慎用……</p>
<h3 id="心形特效"><a href="#心形特效" class="headerlink" title="心形特效"></a>心形特效</h3><ul>
<li>创建js文件</li>
</ul>
<p>在./themes/next/source/js中新建clicklove.js，并将粘贴下列代码。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">!<span class="keyword">function</span>(e,t,a)&#123;<span class="keyword">function</span> <span class="function"><span class="title">n</span></span>()&#123;c(<span class="string">&quot;.heart&#123;width: 10px;height: 10px;position: fixed;background: #f00;transform: rotate(45deg);-webkit-transform: rotate(45deg);-moz-transform: rotate(45deg);&#125;.heart:after,.heart:before&#123;content: &#x27;&#x27;;width: inherit;height: inherit;background: inherit;border-radius: 50%;-webkit-border-radius: 50%;-moz-border-radius: 50%;position: fixed;&#125;.heart:after&#123;top: -5px;&#125;.heart:before&#123;left: -5px;&#125;&quot;</span>),o(),r()&#125;<span class="keyword">function</span> <span class="function"><span class="title">r</span></span>()&#123;<span class="keyword">for</span>(var e=0;e&lt;d.length;e++)d[e].alpha&lt;=0?(t.body.removeChild(d[e].el),d.splice(e,1)):(d[e].y--,d[e].scale+=.004,d[e].alpha-=.013,d[e].el.style.cssText=<span class="string">&quot;left:&quot;</span>+d[e].x+<span class="string">&quot;px;top:&quot;</span>+d[e].y+<span class="string">&quot;px;opacity:&quot;</span>+d[e].alpha+<span class="string">&quot;;transform:scale(&quot;</span>+d[e].scale+<span class="string">&quot;,&quot;</span>+d[e].scale+<span class="string">&quot;) rotate(45deg);background:&quot;</span>+d[e].color+<span class="string">&quot;;z-index:99999&quot;</span>);requestAnimationFrame(r)&#125;<span class="keyword">function</span> <span class="function"><span class="title">o</span></span>()&#123;var t=<span class="string">&quot;function&quot;</span>==typeof e.onclick&amp;&amp;e.onclick;e.onclick=<span class="keyword">function</span>(e)&#123;t&amp;&amp;t(),i(e)&#125;&#125;<span class="keyword">function</span> i(e)&#123;var a=t.createElement(<span class="string">&quot;div&quot;</span>);a.className=<span class="string">&quot;heart&quot;</span>,d.push(&#123;el:a,x:e.clientX-5,y:e.clientY-5,scale:1,alpha:1,color:s()&#125;),t.body.appendChild(a)&#125;<span class="keyword">function</span> c(e)&#123;var a=t.createElement(<span class="string">&quot;style&quot;</span>);a.type=<span class="string">&quot;text/css&quot;</span>;try&#123;a.appendChild(t.createTextNode(e))&#125;catch(t)&#123;a.styleSheet.cssText=e&#125;t.getElementsByTagName(<span class="string">&quot;head&quot;</span>)[0].appendChild(a)&#125;<span class="keyword">function</span> <span class="function"><span class="title">s</span></span>()&#123;<span class="built_in">return</span><span class="string">&quot;rgb(&quot;</span>+~~(255*Math.random())+<span class="string">&quot;,&quot;</span>+~~(255*Math.random())+<span class="string">&quot;,&quot;</span>+~~(255*Math.random())+<span class="string">&quot;)&quot;</span>&#125;var d=[];e.requestAnimationFrame=<span class="function"><span class="title">function</span></span>()&#123;<span class="built_in">return</span> e.requestAnimationFrame||e.webkitRequestAnimationFrame||e.mozRequestAnimationFrame||e.oRequestAnimationFrame||e.msRequestAnimationFrame||<span class="keyword">function</span>(e)&#123;setTimeout(e,1e3/60)&#125;&#125;(),n()&#125;(window,document);</span><br></pre></td></tr></table></figure>

<ul>
<li>修改layout文件</li>
</ul>
<p>打开./themes/next/layout/_layout.swig并在末尾添加以下代码。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">&lt;!-- 页面点击心形 --&gt;</span><br><span class="line">&lt;script <span class="built_in">type</span>=<span class="string">&quot;text/javascript&quot;</span> src=<span class="string">&quot;/js/clicklove.js&quot;</span>&gt;&lt;/script&gt;</span><br></pre></td></tr></table></figure>


<h3 id="爆炸特效"><a href="#爆炸特效" class="headerlink" title="爆炸特效"></a>爆炸特效</h3><ul>
<li>创建js文件</li>
</ul>
<p>在./themes/next/source/js中新建firework.js，并将粘贴下列代码。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="string">&quot;use strict&quot;</span>;<span class="keyword">function</span> updateCoords(e)&#123;pointerX=(e.clientX||e.touches[0].clientX)-canvasEl.getBoundingClientRect().left,pointerY=e.clientY||e.touches[0].clientY-canvasEl.getBoundingClientRect().top&#125;<span class="keyword">function</span> setParticuleDirection(e)&#123;var t=anime.random(0,360)*Math.PI/180,a=anime.random(50,180),n=[-1,1][anime.random(0,1)]*a;<span class="built_in">return</span>&#123;x:e.x+n*Math.cos(t),y:e.y+n*Math.sin(t)&#125;&#125;<span class="keyword">function</span> createParticule(e,t)&#123;var a=&#123;&#125;;<span class="built_in">return</span> a.x=e,a.y=t,a.color=colors[anime.random(0,colors.length-1)],a.radius=anime.random(16,32),a.endPos=setParticuleDirection(a),a.draw=<span class="function"><span class="title">function</span></span>()&#123;ctx.beginPath(),ctx.arc(a.x,a.y,a.radius,0,2*Math.PI,!0),ctx.fillStyle=a.color,ctx.fill()&#125;,a&#125;<span class="keyword">function</span> createCircle(e,t)&#123;var a=&#123;&#125;;<span class="built_in">return</span> a.x=e,a.y=t,a.color=<span class="string">&quot;#F00&quot;</span>,a.radius=0.1,a.alpha=0.5,a.lineWidth=6,a.draw=<span class="function"><span class="title">function</span></span>()&#123;ctx.globalAlpha=a.alpha,ctx.beginPath(),ctx.arc(a.x,a.y,a.radius,0,2*Math.PI,!0),ctx.lineWidth=a.lineWidth,ctx.strokeStyle=a.color,ctx.stroke(),ctx.globalAlpha=1&#125;,a&#125;<span class="keyword">function</span> renderParticule(e)&#123;<span class="keyword">for</span>(var t=0;t&lt;e.animatables.length;t++)&#123;e.animatables[t].target.draw()&#125;&#125;<span class="keyword">function</span> animateParticules(e,t)&#123;<span class="keyword">for</span>(var a=createCircle(e,t),n=[],i=0;i&lt;numberOfParticules;i++)&#123;n.push(createParticule(e,t))&#125;anime.timeline().add(&#123;targets:n,x:<span class="keyword">function</span>(e)&#123;<span class="built_in">return</span> e.endPos.x&#125;,y:<span class="keyword">function</span>(e)&#123;<span class="built_in">return</span> e.endPos.y&#125;,radius:0.1,duration:anime.random(1200,1800),easing:<span class="string">&quot;easeOutExpo&quot;</span>,update:renderParticule&#125;).add(&#123;targets:a,radius:anime.random(80,160),lineWidth:0,alpha:&#123;value:0,easing:<span class="string">&quot;linear&quot;</span>,duration:anime.random(600,800)&#125;,duration:anime.random(1200,1800),easing:<span class="string">&quot;easeOutExpo&quot;</span>,update:renderParticule,offset:0&#125;)&#125;<span class="keyword">function</span> debounce(e,t)&#123;var a;<span class="built_in">return</span> <span class="function"><span class="title">function</span></span>()&#123;var n=this,i=arguments;clearTimeout(a),a=setTimeout(<span class="function"><span class="title">function</span></span>()&#123;e.apply(n,i)&#125;,t)&#125;&#125;var canvasEl=document.querySelector(<span class="string">&quot;.fireworks&quot;</span>);<span class="keyword">if</span>(canvasEl)&#123;var ctx=canvasEl.getContext(<span class="string">&quot;2d&quot;</span>),numberOfParticules=30,pointerX=0,pointerY=0,tap=<span class="string">&quot;mousedown&quot;</span>,colors=[<span class="string">&quot;#FF1461&quot;</span>,<span class="string">&quot;#18FF92&quot;</span>,<span class="string">&quot;#5A87FF&quot;</span>,<span class="string">&quot;#FBF38C&quot;</span>],setCanvasSize=debounce(<span class="function"><span class="title">function</span></span>()&#123;canvasEl.width=2*window.innerWidth,canvasEl.height=2*window.innerHeight,canvasEl.style.width=window.innerWidth+<span class="string">&quot;px&quot;</span>,canvasEl.style.height=window.innerHeight+<span class="string">&quot;px&quot;</span>,canvasEl.getContext(<span class="string">&quot;2d&quot;</span>).scale(2,2)&#125;,500),render=anime(&#123;duration:1/0,update:<span class="function"><span class="title">function</span></span>()&#123;ctx.clearRect(0,0,canvasEl.width,canvasEl.height)&#125;&#125;);document.addEventListener(tap,<span class="keyword">function</span>(e)&#123;<span class="string">&quot;sidebar&quot;</span>!==e.target.id&amp;&amp;<span class="string">&quot;toggle-sidebar&quot;</span>!==e.target.id&amp;&amp;<span class="string">&quot;A&quot;</span>!==e.target.nodeName&amp;&amp;<span class="string">&quot;IMG&quot;</span>!==e.target.nodeName&amp;&amp;(render.play(),updateCoords(e),animateParticules(pointerX,pointerY))&#125;,!1),setCanvasSize(),window.addEventListener(<span class="string">&quot;resize&quot;</span>,setCanvasSize,!1)&#125;<span class="string">&quot;use strict&quot;</span>;<span class="keyword">function</span> updateCoords(e)&#123;pointerX=(e.clientX||e.touches[0].clientX)-canvasEl.getBoundingClientRect().left,pointerY=e.clientY||e.touches[0].clientY-canvasEl.getBoundingClientRect().top&#125;<span class="keyword">function</span> setParticuleDirection(e)&#123;var t=anime.random(0,360)*Math.PI/180,a=anime.random(50,180),n=[-1,1][anime.random(0,1)]*a;<span class="built_in">return</span>&#123;x:e.x+n*Math.cos(t),y:e.y+n*Math.sin(t)&#125;&#125;<span class="keyword">function</span> createParticule(e,t)&#123;var a=&#123;&#125;;<span class="built_in">return</span> a.x=e,a.y=t,a.color=colors[anime.random(0,colors.length-1)],a.radius=anime.random(16,32),a.endPos=setParticuleDirection(a),a.draw=<span class="function"><span class="title">function</span></span>()&#123;ctx.beginPath(),ctx.arc(a.x,a.y,a.radius,0,2*Math.PI,!0),ctx.fillStyle=a.color,ctx.fill()&#125;,a&#125;<span class="keyword">function</span> createCircle(e,t)&#123;var a=&#123;&#125;;<span class="built_in">return</span> a.x=e,a.y=t,a.color=<span class="string">&quot;#F00&quot;</span>,a.radius=0.1,a.alpha=0.5,a.lineWidth=6,a.draw=<span class="function"><span class="title">function</span></span>()&#123;ctx.globalAlpha=a.alpha,ctx.beginPath(),ctx.arc(a.x,a.y,a.radius,0,2*Math.PI,!0),ctx.lineWidth=a.lineWidth,ctx.strokeStyle=a.color,ctx.stroke(),ctx.globalAlpha=1&#125;,a&#125;<span class="keyword">function</span> renderParticule(e)&#123;<span class="keyword">for</span>(var t=0;t&lt;e.animatables.length;t++)&#123;e.animatables[t].target.draw()&#125;&#125;<span class="keyword">function</span> animateParticules(e,t)&#123;<span class="keyword">for</span>(var a=createCircle(e,t),n=[],i=0;i&lt;numberOfParticules;i++)&#123;n.push(createParticule(e,t))&#125;anime.timeline().add(&#123;targets:n,x:<span class="keyword">function</span>(e)&#123;<span class="built_in">return</span> e.endPos.x&#125;,y:<span class="keyword">function</span>(e)&#123;<span class="built_in">return</span> e.endPos.y&#125;,radius:0.1,duration:anime.random(1200,1800),easing:<span class="string">&quot;easeOutExpo&quot;</span>,update:renderParticule&#125;).add(&#123;targets:a,radius:anime.random(80,160),lineWidth:0,alpha:&#123;value:0,easing:<span class="string">&quot;linear&quot;</span>,duration:anime.random(600,800)&#125;,duration:anime.random(1200,1800),easing:<span class="string">&quot;easeOutExpo&quot;</span>,update:renderParticule,offset:0&#125;)&#125;<span class="keyword">function</span> debounce(e,t)&#123;var a;<span class="built_in">return</span> <span class="function"><span class="title">function</span></span>()&#123;var n=this,i=arguments;clearTimeout(a),a=setTimeout(<span class="function"><span class="title">function</span></span>()&#123;e.apply(n,i)&#125;,t)&#125;&#125;var canvasEl=document.querySelector(<span class="string">&quot;.fireworks&quot;</span>);<span class="keyword">if</span>(canvasEl)&#123;var ctx=canvasEl.getContext(<span class="string">&quot;2d&quot;</span>),numberOfParticules=30,pointerX=0,pointerY=0,tap=<span class="string">&quot;mousedown&quot;</span>,colors=[<span class="string">&quot;#FF1461&quot;</span>,<span class="string">&quot;#18FF92&quot;</span>,<span class="string">&quot;#5A87FF&quot;</span>,<span class="string">&quot;#FBF38C&quot;</span>],setCanvasSize=debounce(<span class="function"><span class="title">function</span></span>()&#123;canvasEl.width=2*window.innerWidth,canvasEl.height=2*window.innerHeight,canvasEl.style.width=window.innerWidth+<span class="string">&quot;px&quot;</span>,canvasEl.style.height=window.innerHeight+<span class="string">&quot;px&quot;</span>,canvasEl.getContext(<span class="string">&quot;2d&quot;</span>).scale(2,2)&#125;,500),render=anime(&#123;duration:1/0,update:<span class="function"><span class="title">function</span></span>()&#123;ctx.clearRect(0,0,canvasEl.width,canvasEl.height)&#125;&#125;);document.addEventListener(tap,<span class="keyword">function</span>(e)&#123;<span class="string">&quot;sidebar&quot;</span>!==e.target.id&amp;&amp;<span class="string">&quot;toggle-sidebar&quot;</span>!==e.target.id&amp;&amp;<span class="string">&quot;A&quot;</span>!==e.target.nodeName&amp;&amp;<span class="string">&quot;IMG&quot;</span>!==e.target.nodeName&amp;&amp;(render.play(),updateCoords(e),animateParticules(pointerX,pointerY))&#125;,!1),setCanvasSize(),window.addEventListener(<span class="string">&quot;resize&quot;</span>,setCanvasSize,!1)&#125;;</span><br></pre></td></tr></table></figure>

<ul>
<li>修改layout文件</li>
</ul>
<p>打开./themes/next/layout/_layout.swig并在<head>中间添加以下代码。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">  &lt;!-- 爆炸效果 --&gt;</span><br><span class="line">&lt;canvas class=<span class="string">&quot;fireworks&quot;</span> style=<span class="string">&quot;position: fixed;left: 0;top: 0;z-index: 1; pointer-events: none;&quot;</span> &gt;&lt;/canvas&gt; </span><br><span class="line">&lt;script <span class="built_in">type</span>=<span class="string">&quot;text/javascript&quot;</span> src=<span class="string">&quot;//cdn.bootcss.com/animejs/2.2.0/anime.min.js&quot;</span>&gt;&lt;/script&gt; </span><br><span class="line">&lt;script <span class="built_in">type</span>=<span class="string">&quot;text/javascript&quot;</span> src=<span class="string">&quot;/js/firework.js&quot;</span>&gt;&lt;/script&gt;</span><br></pre></td></tr></table></figure>

<ul>
<li>配置主题文件</li>
</ul>
<p>打开./themes/next路径下的_config.yml文件并在末尾添加以下代码。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Fireworks</span></span><br><span class="line">fireworks: <span class="literal">true</span></span><br></pre></td></tr></table></figure>


<h2 id="博文压缩"><a href="#博文压缩" class="headerlink" title="博文压缩"></a>博文压缩</h2><p>md文件转化为html后会有很多空格，js和css文件中也会有许多空格，js中的空格会影响性能。</p>
<ul>
<li><p>安装</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install hexo-neat --save</span><br></pre></td></tr></table></figure></li>
<li><p>配置</p>
</li>
</ul>
<p>打开博客根目录下的配置文件_config.yml并在末尾添加如下代码：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># hexo-neat</span></span><br><span class="line"><span class="comment"># 博文压缩</span></span><br><span class="line">neat_enable: <span class="literal">true</span></span><br><span class="line"><span class="comment"># 压缩html</span></span><br><span class="line">neat_html:</span><br><span class="line">  <span class="built_in">enable</span>: <span class="literal">true</span></span><br><span class="line">  exclude:</span><br><span class="line"><span class="comment"># 压缩css</span></span><br><span class="line">neat_css:</span><br><span class="line">  <span class="built_in">enable</span>: <span class="literal">true</span></span><br><span class="line">  exclude:</span><br><span class="line">    - <span class="string">&#x27;**/*.min.css&#x27;</span></span><br><span class="line"><span class="comment"># 压缩js</span></span><br><span class="line">neat_js:</span><br><span class="line">  <span class="built_in">enable</span>: <span class="literal">true</span></span><br><span class="line">  mangle: <span class="literal">true</span></span><br><span class="line">  output:</span><br><span class="line">  compress:</span><br><span class="line">  exclude:</span><br><span class="line">    - <span class="string">&#x27;**/*.min.js&#x27;</span></span><br><span class="line">    - <span class="string">&#x27;**/jquery.fancybox.pack.js&#x27;</span></span><br><span class="line">    - <span class="string">&#x27;**/index.js&#x27;</span></span><br><span class="line">    - <span class="string">&#x27;**/clicklove.js&#x27;</span></span><br><span class="line">    - <span class="string">&#x27;**/firework.js&#x27;</span></span><br></pre></td></tr></table></figure>
<blockquote>
<p>可能会有部分插件压缩后报错，在exclude里面添加对应的文件就可以取消压缩了</p>
</blockquote>
<p>​</p>
<h2 id="访客统计"><a href="#访客统计" class="headerlink" title="访客统计"></a>访客统计</h2><p>Next主题已经内置了不统计，在Next主题的配置文件_config.yml中搜索busuanzi_count并将enable设置为true，icon可以自定义设置，下面是我的配置：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">busuanzi_count:</span><br><span class="line">  <span class="built_in">enable</span>: <span class="literal">true</span></span><br><span class="line">  total_visitors: <span class="literal">true</span> <span class="comment"># 总访客数量，在页脚显示</span></span><br><span class="line">  total_visitors_icon: user</span><br><span class="line">  total_views: <span class="literal">true</span> <span class="comment"># 总阅读数量，在页脚显示</span></span><br><span class="line">  total_views_icon: eye</span><br><span class="line">  post_views: <span class="literal">true</span> <span class="comment"># 单篇文章阅读数量，在标题下显示</span></span><br><span class="line">  post_views_icon: eye</span><br></pre></td></tr></table></figure>


<h2 id="添加评论系统"><a href="#添加评论系统" class="headerlink" title="添加评论系统"></a>添加评论系统</h2><p>评论系统采用的是Gitalk，与原来的Gitment差不多(Gitment已经凉了)。新版本的Next主题已经自带Gitalk了，我们只需要开启即可，下面的安装过程请忽略。</p>
<h3 id="注册OAuth-application"><a href="#注册OAuth-application" class="headerlink" title="注册OAuth application"></a>注册OAuth application</h3><p>点击<a href="https://github.com/settings/applications/new">这里</a>注册新的OAuth application</p>
<ul>
<li>其中Application name随便填就好</li>
<li>Homepage URL和Authorization callback URL填写自己的网站首页地址即可</li>
<li>Application description选填，记录这个东西是干什么的</li>
</ul>
<p>填写完成后点击Register application，跳转页面会显示Client ID和Client Secret用于以后登录。<br>​</p>
<h3 id="配置"><a href="#配置" class="headerlink" title="配置"></a>配置</h3><p>打开Next主题的配置文件.\themes\next_config.yml并搜索gitalk，将enable设置为true,并配置相关选项。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Gitalk</span></span><br><span class="line"><span class="comment"># For more information: https://gitalk.github.io, https://github.com/gitalk/gitalk</span></span><br><span class="line">gitalk:</span><br><span class="line">  <span class="built_in">enable</span>: <span class="literal">true</span></span><br><span class="line">  github_id: i-rtfsc <span class="comment"># 你的github用户名(GitHub repo owner)</span></span><br><span class="line">  repo: i-rtfsc.github.io <span class="comment"># Repository name to store issues</span></span><br><span class="line">  client_id: fe0e1f117dc426e14e98 <span class="comment"># GitHub Application Client ID</span></span><br><span class="line">  client_secret: b5e4d8cb3d52a38ce7bb19f2894fbc6d7248b8d4 <span class="comment"># GitHub Application Client Secret</span></span><br><span class="line">  admin_user: i-rtfsc <span class="comment"># GitHub repo owner and collaborators, only these guys can initialize gitHub issues</span></span><br><span class="line">  distraction_free_mode: <span class="literal">true</span> <span class="comment"># Facebook-like distraction free mode</span></span><br><span class="line">  <span class="comment"># Gitalk&#x27;s display language depends on user&#x27;s browser or system environment</span></span><br><span class="line">  <span class="comment"># If you want everyone visiting your site to see a uniform language, you can set a force language value</span></span><br><span class="line">  <span class="comment"># Available values: en | es-ES | fr | ru | zh-CN | zh-TW</span></span><br><span class="line">  language: zh-CN</span><br></pre></td></tr></table></figure>


<h2 id="背景及半透明效果"><a href="#背景及半透明效果" class="headerlink" title="背景及半透明效果"></a>背景及半透明效果</h2><p>将选好的背景图片放在博客根目录/themes/next/source/images中。打开根目录/themes/next/source/css/_common/components/components.styl文件添加如下代码。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">body &#123;</span><br><span class="line">    background:url(/images/background.jpeg);//图片路径</span><br><span class="line">    background-repeat: no-repeat;//是否重复平铺</span><br><span class="line">    background-attachment:fixed;//是否随着网页上下滚动而滚动，fixed为固定</span><br><span class="line">    background-position:50% 50%;//图片位置</span><br><span class="line">    background-size: cover;//图片展示大小</span><br><span class="line">    -webkit-background-size: cover;</span><br><span class="line">    -o-background-size: cover;</span><br><span class="line">    -moz-background-size: cover;</span><br><span class="line">    -ms-background-size: cover;</span><br><span class="line"></span><br><span class="line">     <span class="comment">#footer &gt; div &gt; div &#123;</span></span><br><span class="line">        color:<span class="comment">#000000;//底部文字颜色</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">.header-inner &#123;</span><br><span class="line">    background: <span class="comment">#ffffffe8;//设置侧边栏透明</span></span><br><span class="line">    opacity: 1;//这里设置为1，否则搜索栏会消失</span><br><span class="line">&#125;</span><br><span class="line">.main-inner &#123;</span><br><span class="line">        background: <span class="comment">#fff;</span></span><br><span class="line">        opacity: 0.9;//设置文章主体透明度</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


<h2 id="文章宽度过小"><a href="#文章宽度过小" class="headerlink" title="文章宽度过小"></a>文章宽度过小</h2><p>我们配置的scheme是Mist，运行后发现文章宽度过小，需要修改themes/next/source/css/_schemes/Mist/_layout.styl配置如下：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">.main-inner &#123;</span><br><span class="line">  padding: 20px;</span><br><span class="line">  width: 85%;</span><br><span class="line">  +<span class="function"><span class="title">mobile</span></span>() &#123;</span><br><span class="line">    +<span class="function"><span class="title">tablet</span></span>() &#123;</span><br><span class="line">        width: 100%;</span><br><span class="line">    &#125;</span><br><span class="line">    +<span class="function"><span class="title">mobile</span></span>() &#123;</span><br><span class="line">        width: 100%;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


<h2 id="网站底部加上访问量"><a href="#网站底部加上访问量" class="headerlink" title="网站底部加上访问量"></a>网站底部加上访问量</h2><p>在themes\next\layout_partials\footer.swig添加：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">&lt;div class=<span class="string">&quot;powered-by&quot;</span>&gt;</span><br><span class="line">&lt;i class=<span class="string">&quot;fa fa-user-md&quot;</span>&gt;&lt;/i&gt;&lt;span id=<span class="string">&quot;busuanzi_container_site_uv&quot;</span>&gt;</span><br><span class="line">  本站总访问量&lt;span id=<span class="string">&quot;busuanzi_value_site_uv&quot;</span>&gt;&lt;/span&gt;次</span><br><span class="line">&lt;/span&gt;</span><br><span class="line">&lt;/div&gt;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>pv的方式，单个用户连续点击n篇文章，记录n次访问量</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">&lt;span id=<span class="string">&quot;busuanzi_container_site_pv&quot;</span>&gt;</span><br><span class="line">    本站总访问量&lt;span id=<span class="string">&quot;busuanzi_value_site_pv&quot;</span>&gt;&lt;/span&gt;次</span><br><span class="line">&lt;/span&gt;</span><br></pre></td></tr></table></figure></li>
<li><p>uv的方式，单个用户连续点击n篇文章，只记录1次访客数</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">&lt;span id=<span class="string">&quot;busuanzi_container_site_uv&quot;</span>&gt;</span><br><span class="line">  本站总访问量&lt;span id=<span class="string">&quot;busuanzi_value_site_uv&quot;</span>&gt;&lt;/span&gt;次</span><br><span class="line">&lt;/span&gt;</span><br></pre></td></tr></table></figure>
<p>​</p>
</li>
</ul>
<h2 id="文章显示图片"><a href="#文章显示图片" class="headerlink" title="文章显示图片"></a>文章显示图片</h2><p>在文章头部添加：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">&lt;meta name=<span class="string">&quot;referrer&quot;</span> content=<span class="string">&quot;no-referrer&quot;</span>/&gt;</span><br></pre></td></tr></table></figure>
<p>引用图片方式如下：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">![avatar](https://cdn.nlark.com/yuque/0/2020/png/1759879/1601210476189-c4fc849a-1c9e-44f6-bdb9-248960ed5209.png)</span><br></pre></td></tr></table></figure>

<h2 id="首页文章区域添加阴影"><a href="#首页文章区域添加阴影" class="headerlink" title="首页文章区域添加阴影"></a>首页文章区域添加阴影</h2><p>在目录/themes/next/source/css/_common/components/components.styl文件添加如下代码。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">.post &#123;</span><br><span class="line">  margin-top: 60px;</span><br><span class="line">  margin-bottom: 60px;</span><br><span class="line">  padding: 25px;</span><br><span class="line">  -webkit-box-shadow: 0 0 5px rgba(202, 203, 203, .5);</span><br><span class="line">  -moz-box-shadow: 0 0 5px rgba(202, 203, 204, .5);</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure>

<h2 id="打开“关于”、“分类”、“标签”"><a href="#打开“关于”、“分类”、“标签”" class="headerlink" title="打开“关于”、“分类”、“标签”"></a>打开“关于”、“分类”、“标签”</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo new page <span class="string">&quot;about&quot;</span></span><br><span class="line">生成<span class="built_in">source</span>/about/index.md</span><br><span class="line">---</span><br><span class="line">title: about</span><br><span class="line">date: 2021-10-07 18:14:14</span><br><span class="line">---</span><br><span class="line">添加<span class="built_in">type</span>: <span class="string">&quot;about&quot;</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">hexo new page <span class="string">&quot;tags&quot;</span></span><br><span class="line">生成<span class="built_in">source</span>/tags/index.md</span><br><span class="line">---</span><br><span class="line">title: tags</span><br><span class="line">date: 2021-10-07 18:14:14</span><br><span class="line">---</span><br><span class="line">添加<span class="built_in">type</span>: <span class="string">&quot;tags&quot;</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">hexo new page <span class="string">&quot;categories&quot;</span></span><br><span class="line">生成<span class="built_in">source</span>/tags/index.md</span><br><span class="line">---</span><br><span class="line">title: categories</span><br><span class="line">date: 2021-10-07 18:14:14</span><br><span class="line">---</span><br><span class="line">添加<span class="built_in">type</span>: <span class="string">&quot;categories&quot;</span></span><br></pre></td></tr></table></figure>


<h1 id="附"><a href="#附" class="headerlink" title="附"></a>附</h1><table>
<thead>
<tr>
<th>命令</th>
<th>含义</th>
<th>简写</th>
</tr>
</thead>
<tbody><tr>
<td>npm install hexo -g</td>
<td>安装</td>
<td>无</td>
</tr>
<tr>
<td>npm update hexo -g</td>
<td>升级</td>
<td>无</td>
</tr>
<tr>
<td>hexo init</td>
<td>初始化</td>
<td>无</td>
</tr>
<tr>
<td>hexo new</td>
<td>hexo new “new article”  //新建文章‘new article’<br>hexo new page “about”  //新建页面 ‘about’</td>
<td>hexo n</td>
</tr>
<tr>
<td>hexo generate</td>
<td>使用 Hexo 生成静态文件快速而且简单<br>hexo generate –watch #监视文件变动</td>
<td>hexo g</td>
</tr>
<tr>
<td>hexo server</td>
<td>启动服务预览<br>hexo server -s #静态模式<br>hexo server -p 5000 #更改端口<br>hexo server -i 192.168.1.1 #自定义 IP</td>
<td>hexo s</td>
</tr>
<tr>
<td>hexo deploy</td>
<td>部署(自动上传代码到github)</td>
<td>hexo d</td>
</tr>
</tbody></table>
<h1 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h1><p><a href="https://siriusq.top/Next%E4%B8%BB%E9%A2%98%E7%BE%8E%E5%8C%96.html">Next主题美化</a><br><a href="https://siriusq.top/Next%E5%8D%87%E7%BA%A7-Mac%E8%BF%81%E7%A7%BB.html">Next升级-Mac迁移</a><br><a href="https://doingself.github.io/2017/06/16/2017-06-16-Hexo%E4%B8%BB%E9%A2%98-NexT-%E4%BD%BF%E7%94%A8%E6%80%BB%E7%BB%93/">Hexo-NexT 主题使用总结</a></p>
]]></content>
      <categories>
        <category>Hexo</category>
      </categories>
      <tags>
        <tag>Hexo</tag>
        <tag>主题</tag>
      </tags>
  </entry>
  <entry>
    <title>Hexo结合Github搭建博客</title>
    <url>/archives/6ada37b2.html</url>
    <content><![CDATA[<span id="more"></span>

<h1 id="环境搭建"><a href="#环境搭建" class="headerlink" title="环境搭建"></a>环境搭建</h1><h2 id="安装Node-js"><a href="#安装Node-js" class="headerlink" title="安装Node.js"></a>安装Node.js</h2><p><a href="https://nodejs.org/en/">官网</a> 安装即可，我们用它来生成静态网页。</p>
<h2 id="hexo"><a href="#hexo" class="headerlink" title="hexo"></a>hexo</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ sudo npm install -g hexo</span><br></pre></td></tr></table></figure>

<h1 id="初始化"><a href="#初始化" class="headerlink" title="初始化"></a>初始化</h1><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ mkdir blog</span><br><span class="line">$ <span class="built_in">cd</span> blog</span><br><span class="line">$ hexo init</span><br><span class="line">$ sudo npm install</span><br><span class="line">$ hexo generate</span><br><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure>
<blockquote>
<p>这个blog文件夹我们在github上也新建一个仓来管理，方便后续换电脑等情况维护（<a href="https://github.com/i-rtfsc/blog%EF%BC%89">https://github.com/i-rtfsc/blog）</a><br>blog下放置的就是我们博客的配置文件，博客的源md文件等等</p>
</blockquote>
<p>然后通过 <a href="http://localhost:4000/">http://localhost:4000</a> 查看本地博客。</p>
<h1 id="关联GitHub"><a href="#关联GitHub" class="headerlink" title="关联GitHub"></a>关联GitHub</h1><ul>
<li>注册并登陆GitHub账号后，新建仓库，名称必须为user.github.io，如i-rtfsc.github.io</li>
<li>修改_config.yml</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">deploy:</span><br><span class="line">  type: git</span><br><span class="line">  repository: https:<span class="comment">//github.com/i-rtfsc/i-rtfsc.github.io.git</span></span><br><span class="line">  branch: master</span><br></pre></td></tr></table></figure>
<blockquote>
<p><a href="https://github.com/i-rtfsc/i-rtfsc.github.io.git%E6%94%BE%E7%BD%AE%E7%9A%84%E6%98%AF%E7%94%9F%E6%88%90html%E5%90%8E%E7%9A%84%E6%96%87%E4%BB%B6">https://github.com/i-rtfsc/i-rtfsc.github.io.git放置的是生成html后的文件</a></p>
</blockquote>
<p>重新执行</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo generate</span><br><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure>
<p>若执行hexo generate出错则执行npm install hexo –save，若执行hexo deploy出错则执行npm install hexo-deployer-git –save。错误修正后再次执行hexo generate和hexo deploy上传到服务器。</p>
<h1 id="这篇博客是2018年粗略记的笔记，基本上没啥内容，有需要搭建博客的请参考《搭建hexo、next主题美化》"><a href="#这篇博客是2018年粗略记的笔记，基本上没啥内容，有需要搭建博客的请参考《搭建hexo、next主题美化》" class="headerlink" title="这篇博客是2018年粗略记的笔记，基本上没啥内容，有需要搭建博客的请参考《搭建hexo、next主题美化》"></a>这篇博客是2018年粗略记的笔记，基本上没啥内容，有需要搭建博客的请参考《搭建hexo、next主题美化》</h1>]]></content>
      <categories>
        <category>Hexo</category>
      </categories>
      <tags>
        <tag>Hexo</tag>
        <tag>主题</tag>
      </tags>
  </entry>
</search>
